# LMS 프리미엄 — 촬영 검수 기록 (2026-08-19)

> ⚠ **처음 한 바퀴는 검수가 아니었다.** 주소만 하나씩 열고 화면을 «보기만» 했다.
>   단추도 탭도 안 눌러서, 사장님이 바로 찾으신 다섯 개를 하나도 못 잡았다.
>   두 번째 바퀴에서 실제로 누르고 **값을 재서** 다시 했다.

## 검수 항목 (기본)

1. 화면연결·레이아웃·UI 를 눈으로 확인
2. **뒤로가기·단추·탭·썸네일·배너를 직접 눌러 보며** UX 확인
3. 데이터 연동·호출이 정상인지 확인

---

# ⛔ 공통 — 생성기와 14개 팩 모두 고쳐야 하는 것

## 공통 1. 탭·필터 줄에 세로 스크롤바가 생긴다

 **퍼진 곳: 14개 팩 · 74곳**

```css
.tabs      { overflow-x: auto }   /* ⛔ x 만 열면 세로축이 auto 로 딸려 온다 */
.tabs-pill { overflow-x: auto }
```

테두리 1px 때문에 내용이 1px 넘쳐 세로 스크롤바가 뜬다. 탭 옆에 ⬍ 가 보이던 것이 이것이다.

⛔ **뿌리는 우리 생성기의 자기모순이었다.** `lib/design-presets.ts` 가
   규칙에는 「두 축을 다 적는다 — `overflow: auto hidden`」라고 써 두고,
   바로 아래 예시에는 `.table-wrap { overflow-x: auto; }` 를 두었다.
   **AI 는 규칙 문장보다 예시를 베낀다.** 그래서 만들어진 팩이 전부 틀렸다.

→ 고침: 생성기 예시를 `overflow: auto hidden` 으로 바로잡고, 14개 팩 74곳을 바꿨다.

## 공통 2. 푸터가 화면 바닥에 안 붙는다

 **퍼진 곳: 14개 팩 전부**

짧은 화면(결제 동의·완료·빈 목록·오류)에서 푸터가 화면 가운데 떠 있었다.
CO0403 에서 **아래 172px 이 텅 비었다.** 손님은 「덜 만들어진 화면」으로 읽는다.

까다로운 점 — 푸터가 `body` 직계가 아니라 **main 안**에 있다:

```
body > .app-ed > main.edmain > .ft
```

그래서 세 단계를 다 세로 flex 로 잡고, 푸터를 품은 main 의 아래 여백(64px)을 없앴다.
→ 172px → 64px → **0px** 로 두 번에 걸쳐 잡았다. 잰 값으로 확인했다.

## 공통 3. 그리드 칸의 자식이 칸 밖으로 삐져나온다

 **퍼진 곳: 14개 팩 (min-width:0 을 3~17곳 추가)**

그리드·플렉스 자식의 `min-width` 기본값이 `auto` 라, 칸을 340px 로 정해도
내용이 더 넓으면 칸 밖으로 나가 옆 칸을 덮는다.

---

## 공통 4. 결제 흐름에 «읽기 폭»이 잘못 걸려 있었다

 **퍼진 곳: 9쪽 (LMS·뷰티샵·여행)**

결제 동의 화면이 1,440px 를 쓸 수 있는데 **812px 만 쓰고 카드는 623px** 이었다.
원인은 `.read{max-width:760px}` — **글이 주인공인 자리에 쓰는 읽기 폭**을
체크박스·표·단추가 주인공인 결제 화면에 걸어 둔 것이다.

→ CO04·CO05·CO06(결제·완료·영수증·실패) 아홉 쪽의 `read`·`wrap-narrow` 를 `wrap` 으로 바꿨다.
→ 약관 전문처럼 «진짜 글»인 자리에는 `read` 를 그대로 뒀다.

## ⚠ 내가 만든 회귀 — 고치다 하나 깨뜨렸다

푸터를 바닥에 붙이려고 래퍼를 세로 flex 로 바꿨더니, 그 안의 `main` 이 flex 자식이 되어
**폭이 1,440 → 812px 로 줄었다.** block 일 때는 100% 를 먼저 먹고 max-width 가 걸리는데,
flex 자식은 shrink-to-fit 이 기본이다.

→ `width:100%` 를 줘서 되돌렸다. **재서 확인했다 — 812 → 1,440px.**

⭐ 배운 것: **레이아웃 뿌리(display)를 바꾸면 그 아래가 다 흔들린다.**
  고친 화면만 보지 말고 **폭·높이를 다시 재서** 확인해야 한다.

---

# ✅ 화면목록 깨짐 — 서버를 고쳐서 끝냈다

## 무엇이 일어났나

주소 끝에 빗금이 없으면 브라우저가 마지막 칸을 «파일 이름»으로 본다:

```
/LMS_프리미엄/완성화면   +  assets/css/base.css
→ /LMS_프리미엄/assets/css/base.css            ⛔ 한 칸 위 · 404 · 민얼굴

/LMS_프리미엄/완성화면/  +  assets/css/base.css
→ /LMS_프리미엄/완성화면/assets/css/base.css   ✅
```

## 팩은 멀쩡했다

| 확인한 것 | 결과 |
|---|---|
| 화면 안 링크 `href="../index.html"` | **맞다** — pages/ 에서 한 칸 위를 정확히 가리킨다 |
| 팩에 든 `build/serve.mjs` | `/` → `/index.html` 내부 처리 ✅ |
| README (file:// 로 열기) | 안전 ✅ |
| BOM·깨진 글자 | 14팩 모두 없음 ✅ |

⛔ 문제는 내가 검수용으로 띄운 `npx serve` 였다. `/…/완성화면/index.html` 을
  `/…/완성화면` 으로 **빗금을 «떼면서»** 되돌렸다. 표준 정적 서버는 그 반대로,
  **폴더인데 빗금이 없으면 빗금을 «붙여서»** 되돌려야 한다.

## 고친 것 — `_작업/팩서버.mjs`

```js
if ((await stat(실제)).isDirectory()) {
  if (!길.endsWith("/")) {                                   // 빗금이 없으면
    res.writeHead(301, { location: encodeURI(길) + "/" });   // 붙여서 되돌린다
    return res.end();
  }
  실제 = join(실제, "index.html");
}
```

→ 이제 «빗금 없는 주소» 그대로 넣어도 제대로 뜬다. **확인했다.**

⚠ 나는 처음에 「되돌리지 않게」 만들었는데 **그게 거꾸로였다.**
  되돌리기를 없애면 브라우저가 상대 경로를 잘못 푸는 것은 그대로다.
  서버가 빗금을 붙여 줘야 한다.

```bash
node _작업/팩서버.mjs 4200
```

---
## 공통 5. ⛔⛔ 거르는 단추가 목록을 안 줄인다 — 진짜 까닭을 찾았다

 **퍼진 곳: 12개 팩 (인테리어 둘만 이미 막아 두고 있었다)**

### 눌러 보고 나서야 보였다

「수강 완료 2」 탭을 누르면:

| 무엇 | 결과 |
|---|---|
| 탭 밑줄 | 옮겨감 ✅ |
| 안내문 | 「지금 **2개** 강의를 보고 있어요」로 바뀜 ✅ |
| **목록** | **5개 그대로** ⛔ |

### 까닭 — 필터는 멀쩡했다

엔진은 정확히 돈다. 재 보니 `st:now` 셋에 `hidden` 속성이 제대로 붙어 있었다.
그런데 **`[hidden]` 은 브라우저 기본값이라 작성자 스타일에 진다.**

```
UA:      [hidden] { display: none }     ← 브라우저 기본
작성자:  .lrow    { display: flex }     ← 이게 이긴다
```

한 줄이면 끝난다. 그래서 **숫자는 바뀌는데 목록만 그대로**인 화면이 된다.

### 이게 왜 무서운가

**눈으로 보면 절대 안 잡힌다.** 화면을 넘겨 가며 보면 목록이 5개인 게 정상으로 보인다.
«눌러서 개수를 세어야» 나온다. 내가 첫 바퀴에서 못 잡은 것이 정확히 이 때문이다.

→ 12개 팩에 `[hidden]{display:none !important}` 를 넣었다.
→ 생성기 규칙에도 넣고, **검수 시나리오 7-2 에 「안 줄어들면 열에 아홉은 이것」**으로 원인을 적었다.
→ 눌러서 확인했다 — 「수강 완료 2」에서 목록이 **5개 → 2개**로 줄었다.

---
## 공통 6. 조사가 「을(를)」·「(으)로」로 그대로 나간다

 **퍼진 곳: HTML 9쪽 25곳 + JS 14개 팩**

| 어디 | 무엇이 보였나 |
|---|---|
| CO0403·AU0101 (JS) | 「환불 규정 동의 **을(를)** 아직 채우지 않았어요」 |
| CO0601 | 「신용카드 **(으)로** 다시 시도」 |
| 뷰티샵·장비렌탈 (정적) | 「김수현 원장**을(를)**」 · 「발레아쥬**을(를)**」 |

⭐ **답이 이미 팩 안에 있었다** — 인테리어 팩에 받침을 보고 고르는 함수가 있었다:

```js
var 조사 = function (말, 있, 없) {
  var c = 말.charCodeAt(말.length - 1) - 0xac00;
  return 말 + (c >= 0 && c <= 11171 && c % 28 !== 0 ? 있 : 없);
};
```

→ 14개 팩 JS 에 옮기고, 값이 바뀌는 자리는 `data-josa-for` 로 조사도 같이 바뀌게 했다.
→ 정적 HTML 25곳은 앞말이 정해져 있으니 바로 골라 넣었다.
→ **눌러서 확인했다** — 결제 수단을 바꾸니 「카카오페이로」·「계좌이체로」·「신용카드로」로 따라 바뀐다.

### ⚠ 고치다 또 하나 깨뜨렸다 — $ 가 $ 로 줄었다

치환 스크립트에서 `String.replace` 의 «바꿀 문자열»에 $ 를 썼더니 리터럴 $ 하나가 됐다.
그래서 `$([data-pick-out=…)` 가 `$([data-pick-out=…)` 로 줄었고,
`# LMS 프리미엄 — 촬영 검수 기록 (2026-08-19)

> ⚠ **처음 한 바퀴는 검수가 아니었다.** 주소만 하나씩 열고 화면을 «보기만» 했다.
>   단추도 탭도 안 눌러서, 사장님이 바로 찾으신 다섯 개를 하나도 못 잡았다.
>   두 번째 바퀴에서 실제로 누르고 **값을 재서** 다시 했다.

## 검수 항목 (기본)

1. 화면연결·레이아웃·UI 를 눈으로 확인
2. **뒤로가기·단추·탭·썸네일·배너를 직접 눌러 보며** UX 확인
3. 데이터 연동·호출이 정상인지 확인

---

# ⛔ 공통 — 생성기와 14개 팩 모두 고쳐야 하는 것

## 공통 1. 탭·필터 줄에 세로 스크롤바가 생긴다

 **퍼진 곳: 14개 팩 · 74곳**

```css
.tabs      { overflow-x: auto }   /* ⛔ x 만 열면 세로축이 auto 로 딸려 온다 */
.tabs-pill { overflow-x: auto }
```

테두리 1px 때문에 내용이 1px 넘쳐 세로 스크롤바가 뜬다. 탭 옆에 ⬍ 가 보이던 것이 이것이다.

⛔ **뿌리는 우리 생성기의 자기모순이었다.** `lib/design-presets.ts` 가
   규칙에는 「두 축을 다 적는다 — `overflow: auto hidden`」라고 써 두고,
   바로 아래 예시에는 `.table-wrap { overflow-x: auto; }` 를 두었다.
   **AI 는 규칙 문장보다 예시를 베낀다.** 그래서 만들어진 팩이 전부 틀렸다.

→ 고침: 생성기 예시를 `overflow: auto hidden` 으로 바로잡고, 14개 팩 74곳을 바꿨다.

## 공통 2. 푸터가 화면 바닥에 안 붙는다

 **퍼진 곳: 14개 팩 전부**

짧은 화면(결제 동의·완료·빈 목록·오류)에서 푸터가 화면 가운데 떠 있었다.
CO0403 에서 **아래 172px 이 텅 비었다.** 손님은 「덜 만들어진 화면」으로 읽는다.

까다로운 점 — 푸터가 `body` 직계가 아니라 **main 안**에 있다:

```
body > .app-ed > main.edmain > .ft
```

그래서 세 단계를 다 세로 flex 로 잡고, 푸터를 품은 main 의 아래 여백(64px)을 없앴다.
→ 172px → 64px → **0px** 로 두 번에 걸쳐 잡았다. 잰 값으로 확인했다.

## 공통 3. 그리드 칸의 자식이 칸 밖으로 삐져나온다

 **퍼진 곳: 14개 팩 (min-width:0 을 3~17곳 추가)**

그리드·플렉스 자식의 `min-width` 기본값이 `auto` 라, 칸을 340px 로 정해도
내용이 더 넓으면 칸 밖으로 나가 옆 칸을 덮는다.

---

## 공통 4. 결제 흐름에 «읽기 폭»이 잘못 걸려 있었다

 **퍼진 곳: 9쪽 (LMS·뷰티샵·여행)**

결제 동의 화면이 1,440px 를 쓸 수 있는데 **812px 만 쓰고 카드는 623px** 이었다.
원인은 `.read{max-width:760px}` — **글이 주인공인 자리에 쓰는 읽기 폭**을
체크박스·표·단추가 주인공인 결제 화면에 걸어 둔 것이다.

→ CO04·CO05·CO06(결제·완료·영수증·실패) 아홉 쪽의 `read`·`wrap-narrow` 를 `wrap` 으로 바꿨다.
→ 약관 전문처럼 «진짜 글»인 자리에는 `read` 를 그대로 뒀다.

## ⚠ 내가 만든 회귀 — 고치다 하나 깨뜨렸다

푸터를 바닥에 붙이려고 래퍼를 세로 flex 로 바꿨더니, 그 안의 `main` 이 flex 자식이 되어
**폭이 1,440 → 812px 로 줄었다.** block 일 때는 100% 를 먼저 먹고 max-width 가 걸리는데,
flex 자식은 shrink-to-fit 이 기본이다.

→ `width:100%` 를 줘서 되돌렸다. **재서 확인했다 — 812 → 1,440px.**

⭐ 배운 것: **레이아웃 뿌리(display)를 바꾸면 그 아래가 다 흔들린다.**
  고친 화면만 보지 말고 **폭·높이를 다시 재서** 확인해야 한다.

---

# ✅ 화면목록 깨짐 — 팩 결함이 아니었다

사장님이 보신 «CSS 가 하나도 안 붙은 화면목록»은 **내 검수 환경 탓**이었다.

| 무엇 | 결과 |
|---|---|
| 팩에 든 `build/serve.mjs` | 되돌리기 안 함 — `/` → `/index.html` 내부 처리 ✅ |
| README 안내 | 「index.html 을 브라우저로 열면」 — file:// 직접 열기 ✅ |
| BOM·깨진 글자 | 14개 팩 모두 없음 ✅ |
| 내가 띄운 `npx serve` | `/index.html` → `/` 로 되돌리며 **끝 빗금을 뗀다** ⛔ |

끝 빗금이 없으면 `assets/css/base.css` 가 한 칸 위에서 찾아져 404 가 되고, 글자만 남는다.

⭐ **검수할 때 주소 규칙** — 반드시 끝에 빗금을 붙인다:

```
http://localhost:4200/<팩>/완성화면/          ← 빗금 필수
http://localhost:4200/<팩>/완성화면/pages/<ID>
```

민얼굴로 뜨면 팩이 깨진 게 아니라 **주소가 잘못된 것**이다.

---
## 공통 5. ⛔⛔ 거르는 단추가 목록을 안 줄인다 — 진짜 까닭을 찾았다

 **퍼진 곳: 12개 팩 (인테리어 둘만 이미 막아 두고 있었다)**

### 눌러 보고 나서야 보였다

「수강 완료 2」 탭을 누르면:

| 무엇 | 결과 |
|---|---|
| 탭 밑줄 | 옮겨감 ✅ |
| 안내문 | 「지금 **2개** 강의를 보고 있어요」로 바뀜 ✅ |
| **목록** | **5개 그대로** ⛔ |

### 까닭 — 필터는 멀쩡했다

엔진은 정확히 돈다. 재 보니 `st:now` 셋에 `hidden` 속성이 제대로 붙어 있었다.
그런데 **`[hidden]` 은 브라우저 기본값이라 작성자 스타일에 진다.**

```
UA:      [hidden] { display: none }     ← 브라우저 기본
작성자:  .lrow    { display: flex }     ← 이게 이긴다
```

한 줄이면 끝난다. 그래서 **숫자는 바뀌는데 목록만 그대로**인 화면이 된다.

### 이게 왜 무서운가

**눈으로 보면 절대 안 잡힌다.** 화면을 넘겨 가며 보면 목록이 5개인 게 정상으로 보인다.
«눌러서 개수를 세어야» 나온다. 내가 첫 바퀴에서 못 잡은 것이 정확히 이 때문이다.

→ 12개 팩에 `[hidden]{display:none !important}` 를 넣었다.
→ 생성기 규칙에도 넣고, **검수 시나리오 7-2 에 「안 줄어들면 열에 아홉은 이것」**으로 원인을 적었다.
→ 눌러서 확인했다 — 「수강 완료 2」에서 목록이 **5개 → 2개**로 줄었다.

---
 는 단일 요소를 주므로 `.forEach` 에서 TypeError 가 나 **그 줄부터 죽었다.**
결제 수단을 골라도 아래 단추가 안 바뀌었다.

⭐ **`String.replace` 로 코드를 고칠 때는 `.split().join()` 을 쓴다** — $·# ⛔ 팩만 —·$1 이 특수 표기다.
  이건 이번 세션에서 역슬래시가 먹히던 것과 같은 갈래다.

---
## 공통 7. 한 문장이 두 조각으로 갈려 뒤죽박죽 읽힌다 — ST0401

```css
.list-plain>li{ … display:flex; gap:var(--gap-group)}   /* ⛔ */
```

문장 안에 `<b>` 가 섞이면 **앞뒤 텍스트가 각각 flex 칸**이 되어 gap 만큼 벌어진다.
「미접속 일수만 크게 잡으면 **이미 다 들은 분**도 걸립니다」가
「미접속 일수만 크게  이미 다 들은  도 / 잡으면  분  걸립니다」로 읽혔다.

→ 그냥 문장은 block 으로 두고, **값을 오른쪽에 붙이는 줄에만** `.kv` 로 flex 를 준다.

---

# ⛔ 팩만 — LMS 숫자·문구 맞추기 (다 잡았다)

## 차시 수 — 30 이 아니라 **18** 이었다

CL0301·ST0301 에 **실제로 그려진 커리큘럼**을 세어 보니 18차시였다:

| 장 | 차시 |
|---|---|
| 1장 데이터를 보는 눈 | 4 |
| 2장 함수로 계산 자동화 | 5 |
| 3장 피벗 테이블 실전 | 5 |
| 4장 보고서로 만들기 | 4 |
| **합** | **18차시 · 295분(4시간 55분)** |

「30차시 · 12시간」은 **지어낸 숫자**였다. 그려진 것이 사실이다.
→ 16쪽의 30차시 → 18차시, 7쪽의 12시간 → 4시간 55분, 진도 40% → **67%**(12/18).
→ CO0302 커리큘럼 탭은 4챕터 × 3차시(=12)로 **덜 그려져** 있어, 18차시를 그대로 옮겨 다시 지었다.

## 나머지 — 값 하나로 통일

| 무엇 | 어떻게 |
|---|---|
| 주문번호 | CO0502 를 완료 화면과 같은 `ORD-20260807-004182` 로 |
| 과제 마감 | 2장 9/10 → **7/27**(이미 지남) · GR0301 채점을 3장 마감 8/10 에 맞춤 |
| 수료 가능 | ST0101 38 → **96** (96+32=128 로 GR0401 이 안에서 맞다) |
| 진도율 | CL0801 96% → **80%** (내 강의실 48/60차시와 같게) |
| 질문 수 | CL0703 「전체 12개」 → **5개** |
| 알림 인원 | ST0501 12명 → **4명** (진도 미달 4명으로 골랐으므로) |
| 쿠폰 이름 | 「신규 가입 20%」 → **「첫 수강 20% 할인」** 으로 통일 |
| 개설 미리보기 값 | 79,200 → **79,000** (판매 화면과 같게) |
| 탭 구성 | CL0102 「만료 1」 → **「전체 5」** |
| 자리표시자 | `N/M차시` → 「연결 5/8차시」 · `N명에게` → 「4명에게」 |
| 「전체인데 12개」 | GR0501 처럼 **「전체 N 가운데 12개만 보여 주는 견본입니다」**를 밝힘 |

## 저절로 풀린 둘

- **AU0301 모순 오류 동시 표시** — 두 오류 모두 `hidden` 이 붙어 있었다. 공통 5([`[hidden]`])가 잡았다.
- **HO0204 잔디 5줄** — CSS 가 `grid-template-rows:repeat(7,12px)` 로 7줄이 맞다. **내가 작은 그림에서 잘못 세었다.**

---
# ⛔ 팩만 — LMS 만 고치면 되는 것

## 팩 1. 강의 재생 레이아웃이 깨진다 — CL0301 외 (2쪽·4곳)

커리큘럼 사이드바가 영상 위로 겹쳐 있었다. 인라인 스타일이 원인:

```html
<div style="max-height:62vh; overflow-y:auto; margin:calc(var(--card-pad)*-1)">
```

부모 `.card-bd.flush` 는 이미 `padding:0` 인데 **음수 마진을 또 걸었다.**
상쇄할 여백이 없으니 좌우로 24px 씩, 총 48px 이 넓어진다 — 잰 값 338.4 + 48 = **386.4px** 이었다.
거기에 `overflow-y` 만 열어 가로축까지 딸려 왔다.

→ 음수 마진을 빼고 `overflow: hidden auto` 로 바꿨다.
→ 고친 뒤 다시 재니 **칸 밖으로 나간 자식 0개**, 가로 넘침 없음.

⭐ 이 «flush + 음수 마진» 조합 자체는 **공통 규칙**으로 금지에 넣었다.

---

## ⛔ 고쳐야 할 것

### 1. 같은 강의인데 차시 수가 다르다 — CO0301 ↔ CO0302

| 화면 | 적힌 것 |
|---|---|
| CO0301 강의 상세 | 총 차시 **30차시** · 총 재생 시간 12시간 |
| CO0301 제목 아래 | 박지훈 · **30차시** · 총 12시간 |
| CO0302 커리큘럼 탭 | 전체 **12차시** · 12시간 · 4챕터 |
| CO0302 챕터별 | (3차시) × 4챕터 = **12차시** |

커리큘럼을 펼치면 12차시인데 상세에는 30차시라고 적혀 있다.
손님이 결제 전에 보는 숫자라 그냥 넘길 수 없다.

⭐ **30차시가 맞다 — CL0301 에서 확정됐다.** 30차시는 CO0301·CO0305·CO0306 세 화면에 같이 나오고,
   총 재생 12시간을 30으로 나누면 24분/차시로 그럴듯하다. 12차시면 1시간/차시인데
   CO0305 미리보기의 1차시가 **8:12** 이라 안 맞는다.
   → **커리큘럼 탭(4챕터 × 3차시)이 덜 그려진 쪽**이다.

   ✅ **확정** — CL0301 강의 재생의 커리큘럼 사이드바가 「12/30차시」이고
      1장 4/4 · 2장 5/5 · 3장 3/5 … 로 이어진다. 3장까지만 14차시다.
      **고칠 곳은 CO0302 한 곳**이다.

### 2. 「조건이 없다」면서 「12개」만 보여 준다 — CO0101

한 화면에 이 두 줄이 같이 있다:

- 「1,280개 강의 가운데 지금 조건에 맞는 것은 **12개**예요」
- 「걸린 조건이 **없어요**. 전체를 보고 있습니다」

조건이 없으면 1,280개여야 한다. 견본 데이터가 12개인 것은 알겠는데,
**두 문장이 한 화면에서 서로를 부정한다.**

⚠ 관련해서 숫자 계통이 화면마다 다르다 — CO0102 「전체 12개 중」,
  CO0104 「이 조건이면 14개」, CO0202 「이것만 풀면 2·31·46·18개」.

### 3. 학습 습관 잔디가 5줄이다 — HO0204

「최근 3개월」이라는데 칸이 **13열 × 5행**이다. 요일로 세면 7행이어야 한다.
5행인 까닭이 화면에 안 적혀 있다(주중만 세는 것이라면 그렇게 적어야 한다).


### 4. 조사가 「을(를)」로 나온다 — CO0403

> ⚠ 환불 규정 동의 **을(를)** 아직 채우지 않았어요

항목 이름을 넣어 문장을 만드는 자리인데 조사를 안 고르고 「을(를)」로 뒀다.
받침을 보고 «를» 로 붙여야 한다. 손님에게 바로 보이는 글이다.

### 5. 쿠폰 이름이 화면마다 다르다 — CO0401 ↔ CO0402

| 화면 | 쿠폰 이름 |
|---|---|
| CO0401 결제 | **첫 수강 20% 할인** (79,000 → 15,800 할인) |
| CO0402 쿠폰 목록 | **신규 가입 20% 할인** (~2026-08-31, 사용 가능) |

같은 쿠폰으로 보이는데 이름이 둘이다. 손님은 다른 쿠폰인 줄 안다.


### 6. 같은 주문인데 주문번호가 다르다 — CO0501 ↔ CO0502

| 화면 | 주문번호 |
|---|---|
| CO0501 수강 신청 완료 | **ORD-20260807-004182** |
| CO0502 영수증 (완료 화면에서 「영수증 보기」로 들어감) | **20260809-004821** |

접두사(ORD-)도, 날짜(0807 ↔ 0809)도, 뒷번호(004182 ↔ 004821)도 다르다.
바로 이어지는 두 화면이라 손님이 나란히 본다.

⭐ 금액은 맞다 — 132,000 − 53,000(상시) − 15,800(쿠폰) = **63,200** ✓ 두 화면 모두 같다.

### 7. 조사가 「(으)로」로 나온다 — CO0601

> 단추: **신용카드 (으)로 다시 시도**

4번(CO0403 「을(를)」)과 같은 계통이다. 결제 수단 이름을 넣어 단추 글을 만드는데
받침을 안 보고 「(으)로」를 그대로 뒀다. **조사 붙이는 자리를 한 번에 훑어야 한다.**

⚠ CO0603 에도 비슷한 것이 있다 — 「엑셀로 시작하는 실무 데이터 분석 **은** 2026년…」
  처럼 강조 뒤에 조사가 띄어져 붙는다.


### 8. ⛔⛔ 거르는 단추가 목록을 안 줄인다 — CL0101

「수강 중 **3**」 탭이 골라져 있고 「지금 **3개** 강의를 보고 있어요」라고 적혀 있는데,
**목록에는 5개가 다 보인다** — 수강 중 3개(엑셀·영어회화·피그마)에
수강 완료 2개(SQL·재테크)까지 그대로 있다.

이건 우리가 검수 시나리오 7-2 에 「거르는 단추는 «목록이 실제로 줄어야» 합니다」로
못박아 둔 바로 그것이다. **팩 안에서 그 규칙이 깨져 있다.**

### 9. 같은 화면인데 탭 구성이 다르다 — CL0101 ↔ CL0102

| 화면 | 탭 |
|---|---|
| CL0101 | 수강 중 3 · 수강 완료 2 · **전체 5** |
| CL0102 | 수강 중 3 · 수강 완료 2 · **만료 1** |

3+2 = 5 이므로 「전체 5」가 맞다. 「만료 1」이면 전체가 6이어야 하는데 그런 강의가 없다.
같은 내 강의실인데 세 번째 탭이 화면마다 다르다.


### 10. 「마감된 과제」인데 마감일이 미래다 — CL0601

- 화면 위: 「제출 기간이 종료된 과제예요 · 마감 **2026년 9월 10일** 23:59」
- 같은 화면 아래: 「다음 기회 — 3장 과제가 **8월 10일**에 마감됩니다」
- CL0501(3장 과제): 마감 **2026년 8월 10일** · 남은 시간 3일 8시간
- TC0101: 「오늘은 **2026년 8월 7일**이에요」

2장 과제 마감이 9월 10일이면 **아직 안 왔는데 종료됐다**고 한다.
게다가 뒤에 오는 3장 과제(8/10)보다 늦다. **8월 이전 날짜여야 한다.**

### 11. 같은 강의 진도율이 다르다 — CL0101 ↔ CL0801

| 화면 | 하루 20분 비즈니스 영어 회화 |
|---|---|
| CL0101 내 강의실 | 48/60차시 · **80%** |
| CL0801 수료증 발급 | 진도율 80% 이상 — 지금 **96%** |

96%면 60차시 중 57.6차시다. 48차시가 아니다.

### 12. 질문 수가 다르다 — CL0701 ↔ CL0703

- CL0701 질문 게시판: 「전체 **5**」
- CL0703 미답변만 보기: 「필터를 풀면 전체 **12개**를 보실 수 있습니다」

### 13. 자리표시자 「N/M」이 그대로 나온다 — CU0401

화면 맨 아래:

> 연결하면 위 요약의 **N/M차시**가 함께 올라갑니다

숫자를 넣어야 할 자리에 `N/M` 이 그대로 있다. 같은 화면 위쪽은 「연결 **5/8**차시」로
제대로 채워져 있으니, 이 한 줄만 빠뜨린 것이다.

### 14. 40% 할인 값이 화면마다 다르다 — CU0201 ↔ 나머지

| 화면 | 값 |
|---|---|
| CU0201 강의 개설 미리보기 카드 | **79,200원** (132,000 의 정확히 60%) |
| CO0301·CO0102·CO0401·CO0502 | **79,000원** |

132,000 × 0.6 = 79,200 이라 CU0201 이 «계산상» 맞고,
79,000 은 132,000 − 53,000(상시 할인)이라 «영수증상» 맞다.
어느 쪽이든 **한 값으로 맞춰야 한다** — 손님이 개설 화면과 판매 화면을 같이 본다.

⚠ 가볍게: CU0101 에서 「기획자를 위한 SQL 첫걸음」이 **검수 대기**인데 수강생이 **246명**이다.
  CL0101·CL0102 에서는 이미 수료까지 한 강의다. 재검수 중이라면 그렇게 보이게 해야 한다.

### 15. ⛔ 글자가 서로 겹쳐 읽을 수 없다 — ST0401

오른쪽 「기준을 어떻게 잡을까요」 상자의 셋째 줄:

> · 미접속 일수만 크게   **이미 다 들은**  도
> 잡으면                          **분**        **걸립니다**

왼쪽 설명과 오른쪽 값이 두 칸으로 갈리면서 글자가 뒤섞였다.
원문은 「미접속 일수만 크게 잡으면 이미 다 들은 분도 걸립니다」로 보인다.
**한 문장을 두 칸짜리 표에 넣어서 생긴 일이다.** 이 줄만 한 칸으로 빼야 한다.

### 16. 「전체」인데 128명 중 12명만 보인다 — ST0101

- 지표: 「전체 수강생 **128명**」
- 탭: **전체** 가 골라져 있음
- 목록 위: 「지금 **12명**을 보고 있어요」

2번(CO0101)과 같은 모양이다. **거르기를 안 걸었는데 목록이 줄어 있다.**

### 17. 받는 사람 수가 앞뒤로 안 맞는다 — ST0401 → ST0501 → ST0503

| 화면 | 사람 수 |
|---|---|
| ST0401 진도 미달자 | 이 기준에 걸린 사람 **4명** (목록도 4명 ✓) |
| ST0501 알림 발송 | 「**12명**에게 보냅니다」 · 배지는 「진도 미달 조건으로 고름」 |
| ST0503 발송 결과 | 보낸 사람 **23명** (도착 14 + 실패 9 ✓) |

진도 미달로 골랐다면서 4 → 12 → 23 으로 늘어난다.

⚠ 가볍게: ST0201 은 화면 맨 위 큰 제목이 위로 잘린 채 뜬다. 스크롤 자리 문제일 수 있으니
  촬영본에서 다시 확인한다.

### 18. 같은 과제인데 마감일이 다르다 — CL0501 ↔ GR0301

「3장 과제 — 부서별 실적 대시보드」 하나인데:

| 화면 | 마감 |
|---|---|
| CL0501 과제 제출 | 2026-08-10 23:59 (남은 시간 3일 8시간) |
| GR0301 채점 상세 | 2026-08-05 23:59 (제출 08-06 09:02 → 지각) |

10번(CL0601)과 같은 계통이다. **과제 마감일을 한 군데서 정해 쓰지 않고 있다.**

### 19. 수료 가능 인원이 다르다 — ST0101 ↔ GR0401

| 화면 | 수료 가능 | 진도 미달 |
|---|---|---|
| ST0101 수강생 목록 | **38명** | 32명 |
| GR0401 수료 기준 설정 | **96명** (전체 128 중 75%) | 32명 |

미달 32명은 같은데 수료 가능이 38 ↔ 96 이다. 96 + 32 = 128 이라 GR0401 은 안에서 맞고,
ST0101 은 38 + 32 = 70 으로 128 에 한참 못 미친다.

### 20. 조사 「을(를)」이 로그인에도 있다 — AU0101

> ⚠ 이메일 · 비밀번호 **을(를)** 아직 채우지 않았어요

CO0403 과 **똑같은 문구**다. 4번·7번과 묶어 한 번에 고친다.
→ 조사 자리 세 곳: **CO0403(을/를) · CO0601((으)로) · AU0101(을/를)**

### 21. 서로 어긋나는 오류가 한꺼번에 뜬다 — AU0301

휴대폰 인증 화면에 이 둘이 «같이» 떠 있다:

> ⚠ 인증번호가 맞지 않아요. 다시 확인해 주세요
> ⚠ 시간이 지났어요. 인증번호를 다시 받아 주세요

게다가 위에는 「남은 시간 **2:57**」이 아직 돌고 있다. 시간이 안 지났는데 지났다고 한다.
견본이라 두 문구를 다 보이려 한 것 같은데, **손님 눈에는 앞뒤가 안 맞는 화면**이다.
둘 중 하나만 뜨게 하거나, 「이 화면은 두 경우를 겹쳐 보여 줍니다」라고 적어야 한다.

---

## ⭐ 고치는 방향이 이미 팩 안에 있다 — GR0501

GR0501 수료 처리 화면 맨 아래에 이렇게 적혀 있다:

> 명단은 전체 128명 가운데 **12명만 보여 주는 견본입니다**

**2번(CO0101)·16번(ST0101)이 딱 이 한 줄이 없어서 생긴 일이다.**
견본 데이터가 적은 것은 흠이 아니다. 흠은 «전체를 보고 있다»고 말해 놓고 12개만 보여 주는 것이다.
GR0501 처럼 적으면 두 건이 한 번에 풀린다.

---

## ✅ 잘 되어 있던 것 (그대로 두기)

- **TC0101** 「처리가 필요한 일」 1+24+32+2 = 59 → 「남은 일 59건」과 정확히 맞는다
- **CO0401** 79,000 − 15,800 = 63,200 ✓ · 쿠폰 20%도 정확히 15,800 ✓
- **MY0301 정산표 — 이 팩에서 제일 정확한 화면**
  - 강의별: 10,112,000 × 20% = 2,022,400 ✓ · 10,112,000 − 2,022,400 = 8,089,600 ✓ (세 줄 모두)
  - 합계: 판매 433건 ✓ · 매출 15,012,000 ✓ · 수수료 3,002,400 ✓ · 정산 12,009,600 ✓
  - 「이번 달 예상 11,304,000」 = 12,009,600 − 705,600(이미 정산 완료분) ✓ **정산 예정만 더했다**
- **MY0201** 낸 돈 256,000 = 무료 + 49,000 + 79,000 + 59,000 + 69,000 ✓
- **GR0301** 점수 9+8+7 = 24 ✓ 총점 24/30 · 제출 08-06 09:02 > 마감 08-05 23:59 → 「지각 제출」 ✓
- **GR0401** 96 + 32 = 128 ✓ · 96/128 = 75% ✓ · 기준을 움직이면 인원이 그 자리에서 다시 계산된다
- **GR0101** 출석 표에서 칸을 누르면 출석→지각→결석→해당없음으로 돌고 그 줄 출석률이 다시 계산된다
- **ST0503** 실패 사유 3+2+4 = 9 ✓ · 도착 14 + 실패 9 = 23 ✓ · 「다시 보낼 수 있는 것」만 단추를 준다
- **ST0503** 수신 거부는 «절대 다시 안 보낸다»고 법 근거까지 적어 뒀다
- **ST0401** 「이 기준에 걸린 사람 4명」이 목록 4명과 맞는다
- **ST0501** 미리보기에 «{이름} 같은 값은 보낼 때 각자의 값으로 바뀝니다»를 적어 뒀다
- **CU0301** 차시 시간 합(9+6+14+22+17+19+31+12=130분)이 「2시간 10분」과 정확히 맞는다
- **CU0401** 「연결 5/8차시」가 목록의 연결됨 5개와 맞는다
- **CU0501** 업로드 실패를 «용량 초과·형식 미지원·연결 끊김» 사유별로 갈라 준다
- **CU0101** 게시 중 2개 수강생 169명 = 128 + 41 ✓
- **CL0503** 임시 저장본과 지금 쓴 것을 «본문 글자 수·첨부·마지막 문단»으로 나란히 견줘 준다
- **CL0503** 「왜 서버에 안 보내나」까지 적어 뒀다 — 쓰다 만 답안이 강사에게 보이면 안 된다
- **CL0703** 「이 강사의 평균 답변 시간은 4시간」 + 2일 넘으면 다시 알린다는 약속
- **CL0401** 영상 오류에 화질 낮추기·네트워크 확인까지 붙어 있다
- **CO0405** 결제 진행 중에 「중복 청구를 막는 방법」을 버튼·주문번호·창 닫음·이중 승인 넷으로 적어 뒀다
- **CO0602** 결제 실패 사유를 한도 초과·승인 거절·잔액 부족·본인인증 실패 넷으로 갈라 문구와 단추까지 정했다
- **CO0603** 중복 결제를 막고 처리 내역을 시각별로 보여 준다
- **CO0402** 쿠폰이 왜 안 되는지 «없는 코드·기간 만료·이미 사용» 셋으로 갈라 문구까지 정해 뒀다
- **CO0303** 별점 분포 892+271+84+24+13 = 1,284 → 「1,284개 후기」와 맞는다
- **CO0202** 「어느 조건이 결과를 0건으로 만들었나」를 짚고 조건마다 「이것만 풀면 N개」를 보여 준다
- **CO0103** 불러오는 중을 빈 화면이 아니라 «자리»로 잡아 둔다
- **TC0102** 「데이터가 없는 구간은 0 이 아니라 빈 곳으로 둡니다」라고 적어 둔다
- **HO0102** 「탭을 눌러도 화면이 위로 튀지 않습니다」
- **HO0104** 「한 번에 하나만 열립니다」

---

## 돈 자리

- HO 8 ✅ · TC 5 ✅ · CO 22 ✅ · CL 12/29 · CU 6/19 · ST 6/16 · GR 5/15 · MY 4/13 · AU 2/5 — **묶음 아홉 개를 모두 돌았다 (70쪽 확인)**
- CL 29 · CU 19 · ST 16 · GR 15 · MY 13 · AU 5 — 남음

---

## 2026-08-19 두 번째 검수 (촬영본) — 처음 보듯 눌러 가며

### HO0101 홈 - 비로그인

1. **[팩] 「비로그인」 화면인데 헤더가 로그인 상태다.**
   「내 정보」·「계정」·🔔·「김」 아바타가 다 있고 로그인/회원가입 단추가 없다.
   비로그인 손님이 들어오면 로그인할 길이 없다.

2. **[공통?] 거르는 단추의 「켜짐」 표시가 실제와 어긋난다.**
   - 「디자인」을 누르면 디자인 + 「전체 보기(data-v=*)」가 **둘 다** 켜진다
   - 「전체 보기」를 눌러도 앞서 고른 분야가 **안 꺼진다** (목록만 8개로 돌아옴)
   - 목록을 거르는 일 자체는 맞다. 켜짐 표시만 틀린다.
   → data-fgroup 무리 안에서 `on` 을 하나만 두도록 고쳐야 한다.

3. **[공통] 「개발 284개」인데 걸러 보면 2개.**
   견본이라 데이터가 적은 건 당연한데 그 말이 화면에 없다.
   GR0501 이 쓰던 "전체 128명 가운데 12명만 보여 주는 견본입니다" 같은 한 줄이 필요하다.

4. **[팩] 검색칸에 글자를 넣어도 자동완성이 안 뜬다.**
   화면 목록에는 HO0103 「검색어 자동완성」이 있는데 홈에서는 안 나온다.

### 잘 된 것 (그대로 두기)
- 분야 카드 → 아래 목록이 실제로 줄어든다 (8 → 2)
- 0건이 되면 「고른 조건에 맞는 강의가 없어요」 + 「조건 모두 풀기」
- 「조건 모두 풀기」 는 검색어까지 지우고 8개로 되돌리며 알림도 띄운다

### CO0101 강의 목록

5. **[공통] 찜 하트가 썸네일에 덮여 눌리지 않는다.**
   `button.heart` 가 `a.ccard` **안에** 들어 있고, 그 자리의 맨 위 요소는 IMG 다.
   눌러 보면 찜이 아니라 강의 상세(CO0301)로 넘어간다. 찜 단추가 무용지물.
   → 하트를 링크 밖으로 빼거나 z-index·pointer-events 로 이미지 위에 올려야 한다.

6. **[공통] 거른 뒤에도 쪽 번호가 1~5 로 남고, 어느 쪽을 눌러도 목록이 그대로다.**
   - 12개 → 2개로 걸렀는데 쪽 번호는 5쪽까지
   - 0건이 돼도 쪽 번호가 남는다
   - 조건을 바꿔도 「2쪽」에 머문 채다 (1쪽으로 돌아가야 한다)

7. **[공통] 칩의 × 로 조건을 풀면 왼쪽 단추의 「켜짐」 표시가 안 꺼진다.**
   「고급 ×」 를 지웠는데 왼쪽 난이도 「고급」 은 켜진 채. HO0101 의 2번과 같은 뿌리다.

8. **[공통] 어느 카드를 눌러도 같은 상세(CO0301)가 열리는데 그 말이 없다.**
   목록 12개 링크가 전부 CO0301.html. 견본이니 당연하지만
   상세 위에 "견본이라 어느 강의를 눌러도 이 화면이 열립니다" 한 줄이 필요하다.

9. **[팩] 위 메뉴 「둘러보기」가 홈(HO0101)으로 걸려 있다.**
   강의 목록(CO0101)으로 가야 한다. 화면 목록에 「강의 탐색 · 22개」 가 따로 있는데
   위 메뉴로는 거기 갈 길이 없다.

### 잘 된 것
- 「전체 1,280개 가운데 12개만 보여 주는 견본입니다」 — 홈에 없던 견본 안내가 여기엔 있다
- 「걸린 조건이 없어요. 전체를 보고 있습니다」 상태 안내
- 조건 칩(개발 ×, 고급 ×, 무료 ×) 이 걸린 조건을 보여 준다
- 0건 화면 + 「조건을 하나씩 풀어 보시면 다시 나타납니다」
- 「전체 초기화」 는 모두 정리하고 12개로 되돌린다
- 정렬 「낮은 가격순」 → 무료·42,000·49,000 순으로 실제로 다시 늘어선다
- 무료 토글 켜고 끄기 정상
- 뒤로가기 뒤에도 정렬이 「낮은 가격순」 으로 남아 있다

### CO0301 강의 상세

10. **[팩] 「미리보기」가 단추가 아니라 글자 배지(span.badge)다.**
    위에 「▸ 표시가 붙은 차시는 미리 볼 수 있어요」 라고 안내해 놓고 누를 게 없다.
    화면 목록의 CO0305 「미리보기 재생 모달」 로 이어지는 길이 이 화면에 없다.
    → 화면 목록에 있는데 아무 데서도 못 여는 화면이 더 있는지 세어 봐야 한다.

11. **[팩] 강사 「박지훈」 의 프로필 사진이 사람 여성 사진이다.**
    나머지 그림은 전부 고양이 마스코트인데 여기만 사람이고, 이름과도 안 맞는다.

12. **[공통] 쿠폰 「받기」 를 눌러도 단추 글자가 그대로다.**
    두 번 눌러도 「쿠폰을 받았어요」 가 또 나온다. 받은 뒤에는 「받음」 으로 바뀌거나
    「이미 받으셨어요」 라고 해야 한다.

13. **[공통] 탭을 눌러도 화면이 그 자리에 머문다.**
    탭 내용이 화면 아래에 있어서 손님은 바뀐 걸 못 느낀다. 탭 줄이 위로 오도록 스크롤해 줘야 한다.

14. **[공통] 찜 하트에 aria-pressed 가 없다.** 화면 읽어 주는 장치로는 찜 상태를 알 수 없다.

### 잘 된 것
- 커리큘럼 숫자가 정확하다 — 1장 8+14+11+16=49분, 2장 18+21+12+15+24=90분, 표시와 일치
- 「4개 챕터 · 18차시 · 총 4시간 55분」 이 오른쪽 요약과 맞는다
- 상세의 찜 하트는 ♡↔♥ 로 바뀌고 「찜한 강의에 담았어요」/「찜을 해제했어요」 알림
- 쿠폰 「받기」 알림 자체는 뜬다
- FAQ 아코디언 펼침 정상, +↔× 로 바뀐다
- FAQ 답변 「결제일로부터 6개월」 이 오른쪽 「수강 기간 결제 후 6개월」 과 맞는다
- 탭 4개 모두 내용이 실제로 바뀐다

### CO0401 결제 → CO0501 완료

15. **[팩] 할부 개월이 완료 화면으로 안 넘어간다 — 데이터 연동 오류.**
    결제 화면에서 「12개월」 을 골라 「매달 5,270원 × 12개월」 까지 확인했는데
    완료 화면은 「신용카드 (일시불)」 이라고 나온다. 금액 63,200원은 맞게 넘어왔다.

16. **[팩] 손님 화면에 「결제 실패」 단추가 그냥 놓여 있다.**
    검수용으로 CO0403 을 보여 주려는 뜻은 알겠는데 아무 표시가 없다.
    「검수용 — 실패 화면 보기」 처럼 밝혀야 한다.

17. **[공통] 「동의해야 결제 버튼이 켜집니다」 안내가 켜진 뒤에도 안 사라진다.**

18. **[공통] 못 채운 항목 오류가 누른 자리에서 멀다.**
    「결제하기」 는 오른쪽 위, 오류 「▲ 환불 규정 동의를 아직 채우지 않았어요」 는 화면 아래.
    스크롤해야만 보인다. 못 채운 자리로 화면을 옮겨 줘야 한다.

19. **[팩] 오늘 결제했는데 날짜가 12일 전이다.**
    오늘 2026-08-19 인데 주문번호 ORD-**20260807**-004182, 수강 기간 **2026-08-07** 시작.

### 잘 된 것
- 79,000 − 15,800(20%) = 63,200원 계산 정확
- 결제 수단 셋 다 안내가 다르게 나오고 오른쪽 요약도 따라온다
  (카카오페이 → 할부 칸 사라지고 「카카오톡으로 결제 요청이 갑니다」,
   계좌이체 → 「이체가 확인되면 바로 수강할 수 있어요」)
- 할부 12개월 → 「매달 5,270원 × 12개월」 (63,200÷12 반올림, 맞다)
- 쿠폰 코드에 엉뚱한 값 → 「쿠폰 코드 확인은 서버가 연결되면 동작합니다」 정직한 안내
- 동의 안 하면 결제가 막히고, 동의하면 단추가 켜진다
- 주문번호 「복사」 → 「복사했어요 — ORD-…」
- 영수증 계산 정확: 공급가액 57,455 + 부가세 5,745 = 63,200 (63,200÷1.1)
- 「현금영수증·세금계산서는 마이페이지 > 수강 이력에서」 안내

### CL0301 강의 재생

20. **[팩] 방금 결제한 강의인데 12/18차시까지 들은 상태다.**
    「바로 학습 시작하기」 로 처음 들어왔는데 12차시가 열리고
    「지난번에 보시던 5분 12초 지점부터 이어서 재생하고 있어요」 라고 한다.
    첫 수강이면 1차시 0:00 이어야 한다.

21. **[팩] 12/18 은 66.7% 인데 「전체 진도 40%」 라고 나온다.**
    시간으로 세도(1장 49분 + 2장 90분 + 3장 일부 = 139분 이상 / 295분 = 47% 이상)
    40% 는 어느 쪽으로도 안 나온다.

22. **[공통] 차시를 바꿔도 딸린 정보가 안 따라온다.**
    12차시 → 13차시 → 1차시 로 옮겨 보면 제목과 영상 길이(19:00 / 8:00)는 바뀌는데
    - 「6분 20초 남음」 이 고정이다 (19:00 도 8:00 도 아니다)
    - 「지난번에 보시던 5분 12초 지점부터」 안내가 1차시 0:00 에서도 그대로다
    - 오른쪽 「12/18차시」 가 그대로다
    - 자막이 13차시 것(「여기서 슬라이서를 넣으면…」)인 채로 1차시에 남아 있다

### 잘 된 것
- 재생/일시정지 글자가 바뀐다
- 배속 0.5x~2.0x — 켜짐 표시가 하나만 된다 (목록 필터와 달리 여기는 맞다)
- 자막 「꺼짐 → 켜짐」 을 누르면 영상 위에 자막이 실제로 나온다
- 「다음 차시」 로 12 → 13차시, 시간이 0:00 으로 초기화된다
- 커리큘럼 목록에서 차시를 누르면 그 차시로 옮겨 가고 ▶ 표시와 파란 배경이 붙는다
- 커리큘럼의 분(8분)과 영상 길이(8:00)가 맞는다
- 장별 완료 수(4/4, 5/5, 3/5)가 차시 수와 맞는다

**21번 다시 확인 (새로고침 후 처음 상태):**
- 장별 4/4 · 5/5 · 3/5 · 0/4 = 12, 체크 11개 + 재생 중 1개 = 12, 「12/18차시」 — 여기까지 다 맞다
- 커리큘럼 분 합계도 정확: 49 + 90 + 89 + 67 = 295분 = 4시간 55분
- **틀린 것은 「전체 진도 40%」 하나다.** 12/18 = 66.7%.
- **「12분 40초 남음」 도 틀렸다.** 12차시 길이 12:40 을 그대로 썼다.
  5분 12초 지점부터 이어 본다면 남은 건 7분 28초다.

### CL0501 과제 제출

23. **[공통·큰 것] 견본 날짜가 만든 날에 굳어 있어 오늘과 어긋난다.**
    - 과제: 「D-3 · 남은 시간 3일 8시간 · 마감 2026년 8월 10일 23:59」 — 오늘은 8월 19일.
      마감이 9일 전에 지났는데 「3일 남음」 이라고 한다.
    - 완료 화면: 주문번호 ORD-**20260807**-004182, 수강 기간 2026-08-07 시작
    → 팩을 사서 몇 달 뒤 여는 손님은 모든 화면에서 지난 날짜를 본다.
      날짜를 「오늘 기준 상대값」 으로 만들거나, 만든 날을 밝혀야 한다.
      **AI팩 생성에도 반영할 것.**

24. **[팩] 과제를 제출해도 알려 주지 않는다.**
    「제출하기」 를 누르면 알림 한 줄 없이 강의 재생 화면으로 튕겨 나온다.
    제출됐는지 손님이 알 수 없다. 오른쪽 「이 강의의 과제」 의 3장 「진행」 도 그대로일 것이다.

### 잘 된 것
- 답안이 비면 「제출하기」 가 꺼져 있고, 글자를 넣으면 켜진다
- 글자 수를 실시간으로 센다 (0자 → 31자, 「200자 이상 권장」)
- 파일 첨부 조건을 밝힌다 (XLSX·PDF·ZIP · 최대 5개 · 합계 20MB)
- 「제출 인원 84/128명」 의 128명이 GR0501 전체 수강생과 맞는다
- 「지각 제출 허용(감점 10%)」·「재제출 마감 전까지 가능」 규칙을 밝힌다
- 강의 노트 「지우기」 → 「노트 삭제는 서버가 연결되면 저장됩니다」 정직한 안내

### CL0101 내 강의실 · CL0801 수료증 발급

25. **[팩·큰 것] 같은 강의의 진도가 화면마다 다르다.**
    「엑셀로 시작하는 실무 데이터 분석」 12/18차시 =
    - CL0101 내 강의실 → **67%**  (맞다)
    - CL0301 강의 재생 → **40%**
    - CL0801 수료증 발급 「아직 조건이 모자란 강의」 → **40%**
    세 화면 중 둘이 틀렸다. 12/18 = 66.7%.

26. **[팩] 「12/18차시 · 남은 차시 18개」 — 남은 건 6개다.**
    전체 차시 수를 남은 차시로 썼다. 다른 줄은 맞다:
    48/60 → 12개, 4/26 → 22개, 20/20 → 0개, 16/16 → 0개. **첫 줄만 틀렸다.**

27. **[팩] 「기획자를 위한 SQL 첫걸음」 의 「수료증 발급」 을 눌렀는데
    「하루 20분 비즈니스 영어 회화」 수료증이 열린다.**

28. **[팩] 이미 받은 수료증인데 내 강의실에서는 「수료증 발급 가능」 이라고 한다.**
    CL0801 「이미 받은 수료증」 에 SQL 첫걸음(2026-07-28)·월급쟁이 첫 재테크(2026-06-30)가 있는데
    CL0101 수강 완료 탭에서는 둘 다 「수료증 발급 가능」 배지 + 「수료증 발급」 단추다.
    「다시 받기」 여야 한다.

29. **[팩] 「수강 완료」 탭을 봐도 화면 제목이 「내 강의실 - 수강 중」 이다.**

### 잘 된 것
- 「수강 중 3 / 수강 완료 2 / 전체 5」 — 3+2=5 맞고 탭도 실제로 걸러진다
- 진도 막대 색이 100%면 초록으로 바뀐다
- 수료 조건 세 가지를 각각 「지금 값」과 나란히 보여 준다
  (진도율 80% 이상 — 지금 80% / 과제 3개 — 3개 제출 / 출석 12주 중 11주)
- 「세 가지 조건을 모두 채우셨어요. 지금 바로 발급하실 수 있습니다」

### TC0101 강사센터 홈

30. **[팩] 아래에서 처리해도 위 지표 카드가 안 따라온다.**
    「채점 대기 과제 24건」 을 처리 완료해서 「남은 일 0건」 이 됐는데
    위 카드는 여전히 「채점 대기 24건」 이다.

31. **[팩] 90일 합계와 하루 평균이 서로 안 맞는다.**
    - 7일 361건 ÷ 7 = 52건 ✓
    - 30일 1,774건 ÷ 30 = 59건 ✓
    - 90일 2,161건 ÷ 90 = 24건인데 「60건」 ✗
    차트가 하루 60건 안팎이니 **90일 합계 2,161건 쪽이 틀렸다** (5,400건대여야 한다).
    30일이 1,774건인데 90일이 2,161건인 것도 말이 안 된다.

32. **[팩] 같은 강의의 수강생 수가 손님 화면과 강사 화면에서 다르다.**
    「엑셀로 시작하는 실무 데이터 분석」 — CO0301 손님 화면 4,820명, TC0101 강사 표 128명.
    (CL0501 과제의 「84/128명」 과 GR0501 의 128명은 서로 맞다. 4,820 쪽이 겉돈다.)

33. **[공통] 「오늘은 2026년 8월 7일이에요」** — 23번 날짜 굳음 문제가 관리자 화면에도 있다.
    차트 x축도 8/7 에서 끝난다.

### 잘 된 것
- 「남은 일 59건」 = 1+24+32+2 정확
- 「처리 완료」 를 누르면 줄이 사라지고 남은 일이 정확히 1 줄어든다
- 다 지우면 「밀린 일이 없어요」 축하 화면 + 「처리했어요. 남은 일이 줄었습니다」 알림
- 기간 7일/30일/90일을 바꾸면 차트·합계·직전 대비가 함께 바뀐다 (하루 평균만 90일에서 틀림)
- 강의별 성과 표 수강생 합계 128+41+246+0+12 = **427명**, 위 카드와 정확히 일치
- 표 「개설한 강의 5개」 = 위 카드 5개
- 표 정렬 「수강생 ⇅」 → 0 · 12 · 41 · 128 · 246 오름차순 정확
- 강의 상태 배지 4가지 (게시 중 / 검수 대기 / 작성 중 / 게시 중지)
- 관리자 위 메뉴가 손님과 다르고 「고객 화면」 으로 돌아가는 단추가 있다

---

## 정리 — 2026-08-19 촬영 검수

**팩만 고치면 되는 것 (LMS 프리미엄)**
1 · 4 · 9 · 10 · 11 · 15 · 16 · 19 · 20 · 21 · 24 · 25 · 26 · 27 · 28 · 29 · 30 · 31 · 32

**전체 팩과 AI팩 생성에 반영할 공통 사항**
2 · 3 · 5 · 6 · 7 · 8 · 12 · 13 · 14 · 17 · 18 · 23 · 33

**공통 사항 가운데 가장 큰 셋**
- **23·33 날짜 굳음** — 견본 날짜가 만든 날에 박혀 있어 시간이 지나면 모든 화면이 어긋난다.
  D-3인데 마감이 9일 전, 「오늘은 8월 7일」, 주문일이 12일 전. 상대 날짜로 바꿔야 한다.
- **6·7 거르는 단추의 켜짐 표시** — 목록은 맞게 걸러지는데 표시가 손님을 속인다.
  같은 무리에서 `on` 은 하나만, 칩을 지우면 단추도 꺼지게.
- **5 하트가 링크 안에 들어 있다** — 찜이 안 되고 상세로 넘어간다.

---

# 2026-08-19 수정 완료 — 프리미엄·디럭스 두 팩 함께

## 공통 얼개로 고친 것 (두 팩 app.js·base.css 같이)

| # | 무엇 | 어떻게 고쳤나 |
| --- | --- | --- |
| 2·6·7 | 거르는 단추 켜짐 표시 | `data-fset` 상자 안에서만 갱신하던 것을 **화면 전체에서 같은 list+갈래**로 갱신 |
| 6 | 쪽 번호 | 결과가 줄면 **감추고**, 조건이 바뀌면 **1쪽으로** 되돌린다 |
| 5 | 찜 하트 | `.ccard .heart` 에 **z-index:2** — 사진에 덮여 안 눌리던 것 |
| 14 | 하트 접근성 | `aria-pressed`·`aria-label` 을 상태에 맞춰 붙인다 |
| 15 | 할부가 완료 화면에 안 넘어감 | 결제 때 고른 수단·할부·금액을 **sessionStorage 로 넘기고** 완료 화면이 받아 쓴다 |
| 17 | 「동의해야 켜집니다」 안내 | 단추가 켜지면 **안내를 거둔다** (`data-gatehint`) |
| 18 | 못 채운 곳이 화면 밖 | 막히면 **그 자리로 스크롤**한다 |
| 12 | 쿠폰 「받기」 | 한 번 누르면 **「받음」으로 바뀌고 다시 안 눌린다** (`data-once`) |
| 13 | 탭을 눌러도 그 자리 | 탭 줄이 화면 아래쪽이면 **위로 올린다** |
| 23·33 | 견본 날짜 굳음 | 기준일(2026-08-07)과 오늘의 차이만큼 **화면의 날짜를 통째로 민다.** 간격이 그대로라 D-3·수강 기간은 저절로 맞는다 |
| 3 | 견본 범위가 안 밝혀짐 | 홈 분야 카드에 「카드의 숫자는 전체 강의 수, 아래는 8개를 골라 보여 드리는 견본」 |
| 8 | 어느 카드나 같은 상세 | 상세 화면에 「견본이라 어느 강의를 눌러도 이 화면이 열립니다」 |
| — | 검색 알림 | 「걸러졌어요」 → **「아래 목록에 N개가 남았어요」**, 0건이면 푸는 법을 알려 준다 |

## 팩만 고친 것

**프리미엄**
- 1 · 비로그인 홈 헤더 → 🔔·프로필을 빼고 **「로그인·회원가입」**
- 4 · 검색 자동완성 — 홈 검색칸에 **추천 목록**을 붙였다 (강의·강사 이름에서 고른다)
- 9 · 위 메뉴 「둘러보기」 → 홈이 아니라 **강의 목록(CO0101)** 으로 · 67쪽
- 10 · 「미리보기」 글자 배지 → **CO0305 로 가는 링크**
- 16 · 「결제 실패」 → **「결제 실패 화면 보기 · 검수용」**
- 21·25·26 · 진도 40% → **67%**, 남은 차시 18개 → **6개**, 「37% → 40%」 → 「61% → 67%」
- 24 · 과제 제출 → 그 자리에서 **「제출됨」 + 상태 배지 「제출」 + 답안 잠금 + 알림**
- 27·28 · 이미 받은 수료증 → 배지 **「🎓 수료증 받음」**, 단추 **「수료증 보기」 → CL0803**
- 30 · 「처리 완료」 를 누르면 **위 지표 카드도 같이 준다** (채점 대기 24 → 0건)
- 31 · 90일 통계 2,161건 → **5,357건** (차트 점을 읽어 하루 평균 59.5 × 90일)
- 32 · 수강생 수 → 손님 화면 **「누적 수강생 4,820명」**, 강사 표 **「128명 · 지금 수강 중」**

**디럭스** (위 공통 + 아래)
- D-0 · **내가 어제 깨뜨린 `.app` 그리드 붕괴** — `.app` 을 flex column 규칙에서 뺐다 (3개 팩)
- D-1 · 비로그인 홈 헤더 → 「로그인·회원가입」
- D-5 · **차시 한 벌**: 30차시 → 18차시 / 12시간 → 4시간 55분 / 12/30 → 12/18 / 40% → 67% / 남은 18개 → 6개
- D-6 · 「미리보기」 → **CL-03 으로 가는 링크**
- D-7 · 강의 목록 문구에 **「12개만 보여 주는 견본입니다」** 추가

## 생성기(lib/design-presets.ts)에 넣은 것

- CSS: `.app` 은 grid 2열 그대로 둘 것(사이드바형에 flex column 금지) · 겹치는 단추엔 z-index
- 문서: **「눌러 봐야만 드러나는 것들」 10가지** — 위 표의 뿌리를 규칙으로 적었다

## 안 고친 것 · 취소한 것

- **11 강사 사진** — 취소. `인물 - 1~20.webp` 가 준비돼 있는 걸 보면 인물 사진은 의도된 것이다.
  이름으로 성별을 단정한 내 판단이 과했다.
- **29 화면 제목 「내 강의실 - 수강 중」** — 그대로 둔다. 화면 ID 이름이라
  화면 목록·뒤로가기와 맞춰야 한다. 탭을 바꿨다고 화면 이름이 바뀌면 목록과 어긋난다.
- **19 주문일이 과거** — 23번 날짜 밀기로 저절로 풀렸다.
- **20 첫 수강인데 12차시까지 들은 상태** — 견본은 「이어보기」를 보여 주려고 일부러 중간부터다.
  다만 결제 직후에 오면 어색하니, 다음에 「바로 학습 시작하기」 는 1차시로 보내는 것을 검토한다.
- **22 차시를 바꿔도 딸린 정보가 안 따라옴** (남은 시간·이어보기 안내·자막) — 남았다.
  견본 데이터가 차시별로 없어서, 차시별 자료를 만들어야 제대로 고쳐진다.
