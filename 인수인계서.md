# 인수인계서 — 카페인컬러

**마지막 갱신: 2026-08-28 (밀린 일에서 «이미 끝난 것» 일곱을 걷어내고, 출처.csv 331줄을 채운 날)**

> 🔁 **작업이 끝날 때마다 이 파일을 갱신한다.** 새 대화방은 여기부터 읽는다.
> 「무엇을 했나」보다 **「다음 사람이 모르면 사고 나는 것」**을 우선해서 적는다.

---

## 1. 지금 어디까지 왔나

### ⭐⭐⭐ 「사람처럼 보는 검수」 15항목 — 팩 열넷이 모두 깨끗해졌다 (2026-08-21)

사장님이 조목조목 적어 주신 **15항목**(레이아웃 6 · UX 2 · UI 7)을 `check-눈으로.mts` 가
잰다. 열네 팩 × 대표 14장을 훑은 **첫 판 120건 → 0건.**

```bash
npx tsx check-눈으로.mts <팩이름>
```

**팩이 정말 아팠던 것 다섯**

| 무엇 | 어디 | 무슨 일이었나 |
|---|---|---|
| 머리말이 통째로 무너짐 | 매칭 프리미엄 **47쪽** | 사이드바형 GNB 의 바깥 상자만 `:not(.pro)` 로 막고 속 규칙은 맨몸이었다. 머리말이 폭 1385 · 키 418px 로 퍼져 본문을 밀어냈다 |
| 머리말 밑 큰 흰 띠 | 14벌 **40쪽** | `body>*:first-child{flex:1 0 auto}` — 첫째는 거의 늘 `<header>` 다. 짧은 화면에서 남는 자리를 머리말이 먹었다 |
| 눌러도 아무 일 없는 탭 | 여행 · LMS | 다섯 탭이 모두 «제 화면»을 가리켰다. 「알림 설정」은 「내 정보」와 같은 곳으로 갔다 |
| 눌리지 않는 단추 10개 | 여행 | 「App Store」·「Google Play」가 `<span>` 이었다 |
| 표가 칸 밖으로 236px | 매칭 | `.table{min-width:520px}` 이 286px 짜리 옆칸에서도 버텼다 |

**⚠ 다음 사람이 모르면 사고 나는 것 셋**

1. **흠이 나오면 반드시 브라우저로 그 화면을 열어 본다.** 첫 판 120건 가운데 **절반쯤이
   검사기의 헛짚음**이었다. 「align-items 를 안 줘서」라고 단정하던 자리는 실제로는
   `flex:1 1 0%` — 반씩 나눠 쓰라고 일부러 적어 둔 것이었다. 열어 보지 않고 고치면
   잘 만든 자리를 망가뜨린다. 고칠 것이 팩일 수도, 검사기일 수도 있다.
2. **없는 CSS 변수를 쓰면 그 줄이 통째로 버려진다.** 팩이 두 세대라 이름이 다르다
   (`--sec-gap`/`--sp-sec`, `--sp-block`/`--s4`). 겹쳐 쓴다 — `var(새, var(옛, 기본값))`.
   처음에 한쪽만 적었더니 옛 팩 다섯 벌에서 **아무 소리 없이** 안 먹었다.
3. **낱말 끝(역슬래시 b)은 `check-눈으로` 의 브라우저 쪽 글에 쓰지 않는다.** 그 글은
   통째로 템플릿 문자열이라 **백스페이스 문자로 먹힌다.** 눈에는 멀쩡한데 정규식이
   영영 안 맞는다. 하루에 두 번 당했다. 양옆에 빈칸을 붙여 찾는다.

**⛔ `build-all.mts` 에 인테리어가 빠져 있었다** — 팩은 일곱인데 업종 목록에 여섯만
적혀 있었다. 그래서 build-all 을 아무리 돌려도 인테리어 네 등급의 문서·스펙팩은
옛것 그대로였다. **아무 소리도 안 났다.** 이번에 손님 스펙팩 28칸 중 24칸에만
새 글이 실려서 들통났다. **업종을 새로 만들면 거기에도 이름을 넣는다.**

손님 스펙팩 7-9(손님이 제 사이트에 붙여 넣는 검사 글)에도 같은 헛짚음이 있어서
함께 고쳤다. 28/28 실렸고 붙여 넣을 js 세 덩어리 다 문법이 성하다.
zip 다시 구움 · `check-zip` 0건 · 완성화면 2209개 그대로.

**루틴 일곱에도 넣었다** (`~/.claude/scheduled-tasks/`)

| 루틴 | 무엇을 넣었나 |
|---|---|
| `pack-qa-dispatch` | 배분표에 **「검사기 자체가 바뀜」** 갈래 추가 — 검사기가 바뀌면 팩이 안 바뀌었어도 열넷을 다시 훑는다 |
| `pack-qa-new` | 「흠이 나오면 열어 본다」 + 「없는 CSS 변수는 그 줄을 버린다」 |
| `pack-qa-spec-a` · `-b` | 「흠이 나오면 열어 본다」 + **다시 구운 뒤 28칸을 숫자로 센다** |
| `pack-qa-userpack` | 「흠이 나오면 열어 본다」 |
| `pack-qa-finish` | 굽기 전에 `build-all` 업종 목록부터 본다 + 28칸 세기 |
| `weekly-sales-pack` | 위 셋 다 |

⚠ 루틴 파일은 **저장소 밖**(`~/.claude/scheduled-tasks/`)에 산다.
그래서 **`루틴/` 에 사본을 두기로 했다** (2026-08-21 사장님 지시).

```bash
npx tsx 루틴-백업.mts              # 재기만 한다 (기본) — 다르면 1 로 끝난다
npx tsx 루틴-백업.mts --저장        # 살아 있는 루틴 → 저장소   (평소 백업)
npx tsx 루틴-백업.mts --되돌리기    # 저장소 → 살아 있는 루틴   (새 컴퓨터에서 복구)
```

⛔ **말없이 갈라지는 백업은 없느니만 못하다.** 그래서 기본이 «아무것도 안 하고 재기»고,
화요일 `pack-qa-finish` 가 매주 이것을 돌려 어긋나면 담고 커밋한다.

#### 되돌리기는 진짜 되나 — **2026-08-22 에 확인했다**

말로만 「된다」고 하지 않았다. 따로 사본을 떠 두고 **네 가지로 망가뜨려 보고 되돌렸다.**

| 망가뜨린 것 | 되돌린 뒤 |
| --- | --- |
| 내용을 고쳐 놓기 | 한 자도 안 틀리고 돌아옴 |
| 폴더를 통째로 지우기 | 돌아옴 |
| 폴더는 두고 파일만 지우기 | 돌아옴 |
| 루틴 폴더를 **통째로 치우기**(새 컴퓨터인 척) | 열둘 다 돌아옴 |
| 사본에 없는 폴더를 만들어 두기 | **건드리지 않고 그대로 뒀다** |

지문(sha1)으로 견줘 **12/12 가 한 자도 안 틀렸다.** 되돌린 뒤 예약 아홉의 등록도
그대로 살아 있었고 다음 실행 시각(월 11:00 · 화 00:25~07:08)도 멀쩡했다.

#### ⛔⛔ 그러다 «백업하려다 백업을 지우는» 자리를 봤다

루틴 폴더를 통째로 치운 상태에서 재기가 이렇게 안내하고 있었다 —
「살아 있는 쪽이 맞으면 `--저장`」. **새 컴퓨터에서 그 말을 따르면 사본 열둘이
통째로 날아간다.** 「살아 있는 쪽에서 다 지워졌구나」로 읽기 때문이다.

막이 둘을 달았고, 둘 다 실제로 막히는지 돌려서 확인했다.

| 언제 | 어떻게 막나 |
| --- | --- |
| 살아 있는 루틴이 **하나도 없을 때** | 담지 않고 멈춘다 — 「새 컴퓨터라면 되돌리세요」 |
| 사본의 **절반 넘게 지우려 할 때** | 지워질 이름을 다 보여 주고 멈춘다 (`--정말지운다` 를 붙여야 진행) |

#### 시각표(`루틴/_시각표.json`)는 «저절로» 갱신된다 — 손대지 않는다

`SKILL.md` 에는 「언제 도는지」가 없다. 그래서 `--저장` 이 돌 때마다
**예약 등록 원본에서 다시 만든다.** 손으로 고쳐 봐야 다음 번에 덮어쓴다.
원본은 세션 아이디 밑에 있어 자리가 바뀔 수 있으므로 **찾아서** 읽는다.

```
AppData/Roaming/Claude/claude-code-sessions/*/*/scheduled-tasks.json
```

시각·켜짐·하는 일·승인 권한 수가 바뀌면 재기가 짚어 준다.
⚠ 마지막 실행 시각·만든 때는 **담지 않는다** — 돌 때마다 바뀌어 매주 헛경보가 난다.
⚠ 시각표는 `--되돌리기` 로 못 고친다. 되돌리기는 `SKILL.md` 만 옮기고 등록은 안 건드린다.

#### 되돌린 뒤에 «사람이 해야 하는 것» 둘

되돌리기는 **파일만** 돌려놓는다. 새 컴퓨터에서는 이 둘이 남는다.

1. **등록** — `루틴/_시각표.json` 을 보고 Claude 에게 예약을 시킨다. 안 하면 안 돈다.
2. **권한 승인** — 승인해 둔 권한은 이 컴퓨터의 경로라 안 따라온다(개수만 남긴다).
   루틴이 처음 돌 때 다시 승인해야 한다.

⚠ 폴더는 열둘인데 **실제 등록은 아홉**이다. `weekly-pack-review` 셋은 `pack-qa-*` 가
자리를 물려받아 등록이 빠져 있다 — **되돌릴 때 켜지 않는다.**


### 이번 주 SNS 영상 — 올림 (비공개)

**2026-08-13 에 네 편을 통째로 다시 만들어 여덟 개(세로·가로)를 올렸다. 전부 비공개다.**

| 편 | 언제 올릴 것 | 세로(쇼츠) | 가로 |
|---|---|---|---|
| 결제 실패 화면 놓치더니 | 8/17주 화 21:00 | `Fe5TSPDDTL8` | `d3kX_hQk8Xs` |
| 8월 15일 예약 실패, 내 머리도 하얘져 | 8/17주 목 09:00 | `VYvoVnzopsg` | `1TlL4hgtOCg` |
| 161개 중 37개 완성해 놓고 사이트 완료 | 8/17주 금 18:00 | `pdTw9qkx5yE` | `VVyl6ifIjaQ` |
| 바이브코딩이 놓치는 시작과 끝 | 8/24 화 21:00 | `iYttJFRAjac` | `75Qq8f_PUzk` |

⚠ **스튜디오에서 지워야 할 것 넷** (사장님이 직접)

| 영상 ID | 무엇 |
|---|---|
| `7WGwcEuGdyQ` · `w0Ot3tUCYS4` | 2026-08-11 에 올린 영상4 **옛 판**. 내용이 아예 다르다(유의사항 톤 → 홍보 톤) |
| `nD7kI3EXfF4` · `HxkVUUGh5w8` | 영상4 를 **자막 채우기 전**에 올린 것. 위 표의 `iYttJFRAjac` · `75Qq8f_PUzk` 로 대체됨 |

2주차 것도 이미 올라가 있으면 같이 정리한다.

#### 2026-08-25 에 올린 것 · 그리고 «사라진» 넷

**올렸다** — 5주차_2026-09-07 · `영상9_업종마다몇개` (「내 사이트에는 몇 개의 화면이 필요할까요」)

| 세로(쇼츠) | 가로 |
|---|---|
| `uOZ4h1Bolkg` | `CNvo06x4gLo` |

둘 다 **비공개** · 설명 986글자 들어감 · 유튜브에서 직접 확인함.
8/25 오후 4:09 부터 「등록 중」에 멈춰 보이던 편이다. 까닭은 도장 만료였다(3-N 절).

#### ✅ 「유튜브에 없다」던 넷 — 잃은 게 아니었다 (2026-08-25 밝힘)

지킴이가 `영상6_내컴퓨터에서만` · `펫유치원_컷편집` 넷을 「없습니다」라고 짚었다.
사장님이 **「영상6이 사이트 배포, 펫이 146개 화면 목록 아니야?」** 하고 물으셔서 되짚어 보니
**맞았고, 둘 다 채널에 «공개»로 살아 있었다.** 검수기가 **죽은 옛 번호**를 붙들고 있던 것이다.

| 편 | 검수기가 들고 있던 옛 번호 (지워짐) | 실제로 살아 있는 번호 (공개) |
|---|---|---|
| `영상6_내컴퓨터에서만` 세로 | `eXzgHF7cipk` | **`WbaLJaRr0sE`** (1080×1920 · 8/24) |
| `영상6_내컴퓨터에서만` 가로 | `OG3GYXZAoMQ` | **`WYyvooB3F20`** (1920×1080 · 8/24) |
| `펫유치원_컷편집` 세로 | `U4annq7WhsY` | **`Tbfm1R7M-4E`** (1080×1920 · 8/20) |
| `펫유치원_컷편집` 가로 | `gA6FQ_tgAzg` | **`kiwfc9qhuQI`** (1920×1080 · 8/21) |

옛 넷은 8/18 밤에 올렸다가 지워진 것이다 — **그날 밤 같은 영상이 스물여섯 번 올라간
사고**(아래 절)를 정리하면서 같이 지워졌다. 그 뒤 새로 올린 것이 위 오른쪽 넷이다.
**검수기를 살아 있는 번호로 바로잡았고, 지킴이가 조용해졌다.**

⚠ **다음 사람이 모르면 헛걸음하는 것** — 지킴이가 「없습니다」라고 짚으면 **지워진 것이
아니라 «번호가 갈린 것»일 수 있다.** 지우기 전에 채널의 «남아 있는 영상 목록»에서
같은 제목을 찾아본다. 제목으로 찾으면 5초면 나온다. 못 찾을 때만 정말 없는 것이다.

**⛔ 그런데 그 두 편은 제목이 깨진 채로 «공개»돼 있다**

> 다 만들었는데 실제 사이트 **배** 어떻게? -사이트 배포편-

「배**포는**」이 「배」로 잘렸다. `WYyvooB3F20` · `WbaLJaRr0sE` 둘 다 그렇다.
검수기에 적힌 본디 제목은 「다 만들었는데 **실제 사이트 배포는** 어떻게?」다.
공개된 글이라 **사장님 승낙 없이는 안 고친다.** 채널의 나머지 25개는 멀쩡하다.

**⚠ 드라이브 복사는 안 됐다** — `G:` 가 안 붙어 있었다(구글 드라이브 앱이 꺼져 있음).
유튜브에는 올라갔다. 드라이브는 앱을 켠 뒤 손으로 넣으시면 된다:
`G:\내 드라이브\릴스\카페인컬러_주간콘텐츠\5주차_2026-09-07`

#### ⛔⛔ 드라이브 사본은 «조용히» 빠질 수 있다 (2026-08-25)

사장님이 물으셨다. 「내 사이트에는 몇 개의 화면이 필요할까요, 이거 구글 드라이브에는
안 올라온 거 같은데」 — 맞았다. 드라이브를 직접 훑어 재 봤다.

| 회차 | 드라이브에 있나 |
|---|---|
| 5주차_2026-09-07 | 운영자 편 둘은 **있음** · **영상9 둘이 없음** ← 이것만 빠졌다 |
| 4주차_2026-08-31 | 펫유치원 둘 다 있음 (다만 «옛 제목»으로) |
| 3주차_2026-08-24 | 영상6 둘 다 있음 |

**까닭.** 올릴 때 구글 드라이브 앱이 꺼져 있어 `G:` 가 안 붙었다.
드라이브 복사는 **일부러 «안 죽게»** 돼 있다 — 유튜브엔 이미 올라갔으니 되돌리면
같은 영상을 두 번 올린다. 그래서 창에 한 줄 찍히고 끝났고 상태는 published 로
넘어갔다. **다시 채울 기회가 영영 없었다.**

**고친 것 둘**

1. 빠지면 **검수기 화면에 남는다** — `watcher_error` 에 적혀 `/admin/sns` 에 뜬다.
   성공이 그 경고를 덮지 않게, 잘된 뒤에도 «지우는» 대신 «갈아 끼운다».
2. 빠진 것만 채우는 길을 냈다:

```bash
npx tsx "판매용_템플릿/_마케팅/_작업/sns지킴이.mts" --드라이브채우기
```

⚠ **저절로 돌게는 «안» 했다.** 드라이브 파일 이름은 «그때의 제목»으로 굳는다.
나중에 검수기에서 제목을 고치면 이름이 어긋나고, 이름만 보고 채우면 **두 벌**이 쌓인다.
실제로 4주차 펫유치원이 그 짝이다 — 드라이브는 「146개 화면 **목록이 나왔어요**」,
검수기는 「146개 화면**이 만들어 졌어요**」다. 그래서 채우기 전에 그 폴더에 이미 있는
것을 다 찍어 보여 준다. 두 벌로 쌓는 것보다 한 번 눈으로 보는 편이 싸다.

#### ⛔⛔ 지킴이가 «할 일이 없는 날마다» 실패로 적히고 있었다 (2026-08-25 고침)

한 바퀴 돌고 나갈 때 **나간 값이 127** 이었다. 할 일을 다 하고 「할 일이 없습니다」까지
찍은 뒤에 죽는 것이라 **창에서 볼 때는 멀쩡해 보였다.** 그런데 작업 스케줄러와 루틴은
«나간 값»만 본다 — 헛도는 날마다 «실패»로 적히고 있었다.

뿌리는 자식 프로세스(`유튜브있나.mjs`)를 부르고 **바로** 나가는 것이었다.
나가는 길을 `나가기()` 한 군데로 모으고 한 숨 쉬었다 나가게 했다.

⚠ **「창에서 멀쩡해 보인다」는 증거가 아니다.** 나간 값을 따로 재야 한다:

```bash
npx tsx "판매용_템플릿/_마케팅/_작업/sns지킴이.mts" --시늉 > /dev/null 2>&1; echo $?
```

⚠ `판매용_템플릿/_마케팅/_작업/watcher-README.md` 에 더 자세히 적어 뒀는데,
  그 파일은 **저장소에 안 들어간다**(`판매용_템플릿/` 이 .gitignore 다). 이 컴퓨터에만 있다.

### 파일이 있는 곳

```
G:\내 드라이브\릴스\카페인컬러_주간콘텐츠\
  2주차_2026-08-17\   세로_·가로_·썸네일_ + 올릴글.md
  3주차_2026-08-24\   〃
```

- 이름은 **맨 앞이 무엇인지**를 말한다 — `세로_` / `가로_` / `썸네일_`.
- 옛 판은 **모아두지 않고 덮어쓴다.** 필요하면 `_작업/` 의 대본·설정으로 다시 굽는다.
- 원본(대본·표지 설정·올릴글)은 전부 `판매용_템플릿/_마케팅/_작업/` 에 있다.

---

## 1-A. 2026-08-25 홈페이지 — 캐릭터·영상·스크롤 (실서버까지 나갔다)

하루에 스물둘을 담고 올렸다. 굵직한 것만 적는다.

| 무엇 | 어디 |
|---|---|
| 캐릭터 그림 열 장 | 홈 4곳 · 사용가이드 5곳 + 가로 띠 1 |
| 사용가이드 「AI팩」 탭 | 벤토 2단 · 칸마다 다른 파스텔 |
| 사용가이드 「만드는 방법」 탭 | 셋씩 두 줄 카드 · 정지 캡처 → **움직이는 영상** |
| 스크롤 인터랙션 | 사이트 전체 |

### 다시 만들 때 꼭 알아야 하는 것 다섯

**① 그림에 «투명 여백»이 얼마나 있는지 먼저 잰다.**
받은 캐릭터 열 장은 사방이 평균 **29%**(많은 건 47%) 비어 있었다. 칸을 300px 로 줘도
캐릭터는 200px 로 보인다 — 「붕 떠 보인다」의 정체가 이것이었다. 알파 자리를 직접 세서
사방 8px 만 남기고 잘랐다.

**② 그림은 webp 로 굽는다.** 같은 열 장이 PNG 7.9MB → webp **604KB**(열세 배)였다.

**③ 영상은 GIF 로 만들지 않는다.** 같은 5초가 GIF 면 수 MB 인데 h264 는 100KB 안팎이다.
여섯 토막 + 표지 여섯 장을 다 합쳐 1.3MB 다.
⚠ 원본(`릴스영상/_촬영영상/1. 팩생성_펫유치원.mp4`)은 «10배쯤 빨리 감긴» 녹화다.
  그대로 넣으면 0.9초라 볼 새가 없다. 자른 값과 늦춘 배수는 `_작업/영상토막내기.mjs` 와
  `how-to-make.tsx` 머리말에 적어 뒀다(그 스크립트는 .gitignore 라 저장소에 없다).
⚠ 녹화본 «왼쪽 2px» 이 브라우저 창 테두리다. 안 자르면 영상마다 세로줄이 그어진다.

**④ 파스텔 위에 글을 얹으면 대비를 다시 잰다.**
muted-foreground(#6B6F76)는 파스텔 위에서 **3.9:1** 밖에 안 나온다(AA 기준 4.5 미달).
foreground/75 로 내려 6 위로 올렸다. 진한 카드에서는 글자·선·뱃지를 «한 자리에서» 뒤집는다 —
낱낱이 고르면 하나를 빠뜨리고, 그 하나만 안 보인다.

**⑤ 색은 페이지 «전체»를 보고 고른다.**
「만드는 방법」 카드 여섯 중 둘을 진한 색으로 눌렀는데, 처음에 아랫줄 가운데를 주황으로
뒀더니 페이지 맨 아래 주황 CTA 띠와 한 덩어리로 보였다. 초록과 자리를 맞바꿨다.

### ⛔⛔ 스크롤 인터랙션 — 실서버에서 잡은 함정

장치는 `components/scroll-reveal.tsx` 하나다. 화면은 표시만 단다:

```html
<section data-나타남>…</section>
<li data-나타남 data-늦게="2">…</li>   <!-- 줄줄이 나올 때 늦춘다 (1~5) -->
```

**⛔ IntersectionObserver 를 쓰면 안 된다.** 처음에 그걸로 만들었다가 실서버에서 걸렸다.
그것은 «들어왔다/나갔다»가 바뀔 때만 울린다. 그런데 화면을 훌쩍 건너뛰면 — 닻 링크로
뛰거나, 뒤로 가기가 자리를 되살리거나, 손가락으로 확 튕기면 — 그 칸은 「아래에서 안 보임」
에서 「위에서 안 보임」으로 바뀐다. **둘 다 «안 보임»이라 아무 일도 안 일어난다.**
그래서 그 칸은 영영 숨은 채로 남는다 — 손님 눈에는 빈 종이다.
→ 스크롤할 때마다 rAF 한 번으로 «자리를 직접 재는» 쪽으로 바꿨다. 다 뜨면 듣기를 그만둔다.

**⚠ 안전장치 셋은 지운다고 될 일이 아니다.**
1. 「움직임을 줄여 주세요」로 맞춰 둔 분에게는 아예 안 켠다(멀미·현기증).
2. 첫 모습(숨김)을 CSS 에 그냥 박지 않는다. `html[data-나타남켬="on"]` 안에만 두고,
   그 표시는 스크립트가 «켤 수 있을 때만» 붙인다. 안 그러면 스크립트가 막힐 때 글이 통째로
   안 보이는 화면이 된다.
3. 켬 스위치 이름을 «나타남켬»으로 갈랐다. 같은 이름이면 html 자신이 대상에 끼어든다.

### ⚠⚠ 재는 도구가 틀리면 멀쩡한 것을 고치러 든다

이날 재다가 세 번 헛짚었다. 다음 사람은 이걸로 시간 버리지 말 것.

1. **붙어 있는 브라우저 칸이 «접혀» 있으면 innerHeight 가 0 이다.** 높이가 0 이면 아무것도
   «화면에 들어올» 수 없다. 그 상태로 재고 「안 된다」고 결론 내렸다. → 헤드리스 크롬을
   CDP 로 띄워 `Emulation.setDeviceMetricsOverride` 로 폭·키를 못 박고 잰다.
2. **한글 코드값을 손으로 적지 않는다.** 「떴다」의 첫 글자를 0xB5A0·0xB5EC 로 두 번 틀렸다
   (진짜는 0xB5B4). CDP 는 UTF-8 을 그대로 받는다 — 한글을 그냥 쓰면 된다.
3. **배포를 기다릴 때 «새것에만 있는» 글자로 기다린다.** 「나타남」으로 기다렸더니 첫 배포에
   이미 있던 글자라 곧바로 통과했고, 옛 빌드를 재고 있었다.

### 실서버에서 확인한 것 (2026-08-25)

맨 아래로 «훌쩍 뛴» 뒤 — 홈 11칸이 모두 떴다(opacity 1). 사용가이드 6칸, 무료샘플 6칸도 같다.
/ · /deliverables · /packages · /free · /contact 모두 200. 영상·그림도 내려온다.

⚠ 반려동물케어 팩(만드는중)은 **안 올렸다** — 그건 8/31 월요일 B 회차 몫이다.

## 1-B. 2026-08-26 홈·구매목록·팩상세를 시안대로 다시 지었다 (실서버까지 나갔다)

claude.ai/design 의 「카페인컬러 홈 리뉴얼 v2」를 옮기는 것으로 시작해, 결국 **팩 카드가
나오는 세 화면을 한 몸으로** 맞추는 일이 됐다. 커밋 열하나, 전부 실서버(main)에 나갔다.

| 무엇 | 어디 |
|---|---|
| 첫 화면 전부 | `app/(marketing)/home-landing.tsx` (통째로 다시 씀) |
| 업종별 팩 칸의 데이터 | `lib/packages.ts` 의 `homePacks()` (새로 만듦) |
| 「지금 마우스로 보고 있나」 | `components/mouse-watch.tsx` (새로 만듦) |
| 구성 목록·팩 카드의 색 | `app/globals.css` 의 `--list-*` · `--pack-*` (새로 만듦) |
| 구매 목록 카드 | `app/(marketing)/packages/browse.tsx` |
| 팩 상세의 4개 등급 카드 | `app/(marketing)/packages/[packageId]/page.tsx` |

⚠ **반려동물 팩은 안 올렸다.** 8/31 월요일 B 회차 몫이다. 올린 열하나가 `판매용_템플릿` 을
한 글자도 안 건드렸는지 파일 목록으로 확인하고 밀었다.

### ⭐ 세 화면을 한 몸으로 — 색과 치수는 «한 곳»에 둔다

같은 팩 카드가 홈·구매 목록·팩 상세 세 곳에 나온다. 세 곳에 각각 색을 적어 두면
**하나만 고쳤을 때 조용히 어긋난다** — 이날 어긋나 있던 것이 정확히 그 모양이었다.

```
app/globals.css
  --list-ink #5C5545   구성 목록 글자      --list-dot #A79C86   가운뎃점
  --pack-ink #191713   이름·값             --pack-sub #77705F   범위·단위
  --pack-tint #F4F1EA  값을 담는 칸        --pack-tier(-bg)     등급 알약
```

셋이 같은 것: 카드 폭 **289px** · 한 줄에 **4개** · 모서리 22px · 안여백 30/26 ·
구성 목록 13px/1.7. (`폭재기.mjs`·`셋비교.mjs` 로 계산된 값을 견줘 확인한다.)

⚠ 마지막 2px 이 홈의 좌우 여백이었다. 시안이 20px 인데 사이트 기준은 24px(px-6)이라
카드가 291 대 289 로 갈렸다. 홈을 사이트 기준에 맞췄다.

⚠ **「2×2 로 보여주지 않는다」는 옛 원칙을 지웠다.** `lib/packages.ts` 에 「2×2를 대놓고
보여주면 구매자가 표를 만들어 계산하기 시작한다. 대신 홈에서 넷을 한 줄에 나란히 놓지
않는다」고 적혀 있었는데, 시안이 이미 넷을 한 줄에 놓았고 사장님 지시로 나머지도 맞췄다.
틀린 말이 남아 있으면 다음 사람이 그것을 지키려다 화면을 되돌린다. 등급 이름을 사다리처럼
두는 것 자체는 그대로다.

### ⭐ 시안의 숫자를 베끼지 않았다

시안에는 44·270·330 같은 수가 손으로 적혀 있었다. 그건 시안을 그린 날의 사진일 뿐이라
팩이 자라면 곧 어긋난다 — 이 파일은 2026-08-03 에 「확인 항목 368개」로 똑같이 겪었다.
`homePacks()` 가 상품 원본(`planContents`·`packCredits`)에서 읽는다.

⚠ 값 스위치도 물려 뒀다. `PACKAGE_PRICES_PUBLIC` 이 꺼지면 홈도 「판매 준비 중」이 된다.
그리고 `PACKAGE_SALE_OPEN` 이 꺼져 있는 동안 홈은 **「구매하기」가 아니라 「자세히 보기」**
라고 말한다 — 시안 글자를 그대로 옮겼더니 눌러 들어간 손님이 「판매 준비 중」을 만났다.
못 지킬 약속이다. 판매를 열면 저절로 「구매하기」로 돌아온다.

### ⚠ 시안의 머리·꼬리는 안 옮겼다

진짜 머리는 로그인 상태·크레딧 잔액·좁은 화면 메뉴를 안고 있고, 바꾸면 사용가이드·구매·
검수 화면의 머리도 함께 바뀐다. 홈만 보고 갈아치울 수 없다.

### ⛔⛔⛔ 마우스 효과를 @media (hover: hover) 로 지키면 안 된다

**사장님 노트북은 터치스크린이다**(`navigator.maxTouchPoints` = 10). 윈도우는 그런 기기를
통째로 「손가락으로 쓰는 기기」로 알리고, 크롬은 그대로 옮긴다:

```
hover: hover      false        pointer: fine    false
any-hover: hover  false        pointer: coarse  true
```

**마우스를 꽂고 쓰셔도 그렇다.** `any-hover` 까지 false 라 «어떤» 미디어 쿼리로도 못 가른다.
그래서 8/25 에 넣은 «마우스 올리면 카드가 떠오르기»가 **사장님 화면에서 내내 안 걸리고
있었다.** 터치스크린 달린 윈도우 노트북은 흔하다 — 사장님만의 일이 아니다.

→ **묻지 말고 본다.** `components/mouse-watch.tsx` 가 pointer 이벤트의 `pointerType` 을 보고,
마우스(또는 펜)가 실제로 움직이면 `html[data-마우스="on"]` 을 붙인다. 손가락이 닿으면 뗀다.
CSS 는 미디어 쿼리 대신 그 표시 안에 넣는다(`app/globals.css` 의 `data-들썩`).
원래 막으려던 것(손가락으로 누른 카드가 «떠 있는 채로 붙는» 것)은 그대로 막힌다.

### ⛔ <Link> 에는 styled-jsx 가 안 묻는다

styled-jsx 는 «그 파일이 직접 쓴 태그»에만 제 반 이름을 붙인다. `next/link` 의 `<Link>` 는
남의 컴포넌트라 안 붙는다. 그냥 `.btn { … }` 이라고 적으면 규칙이 통째로 헛돌아 단추가
맨몸 글자로 나온다 — 옮기던 날 실제로 그랬다. `.cc :global(.btn)` 으로 감싼다.

### ⚠ 흘러가는 띠를 화면 끝까지 펴면 «빈 칸»이 따라온다

띠는 track 의 절반만큼 밀고 처음으로 돌아간다. 그 절반이 화면보다 좁으면 돌아가는 순간
뒤가 비어 **빈 띠가 스쳐 지나간다.** 1240px 안에 갇혀 있을 땐 두 벌로 충분해 안 드러났지만,
화면을 다 쓰면 넓은 모니터에서 바로 보인다 → 같은 벌을 **넷** 이어 붙였다(2,625px 까지 덮음).
알약 모서리도 뗐다 — 화면을 꽉 채우면 둥근 끝이 밖으로 나가 잘린 자국처럼 보인다.
⚠ 띠는 `.wrap` 밖에 있어 **자기 위쪽 여백이 없다.** 첫 화면과의 틈은 `.hero-grid` 의
아래 여백(112px, 폰 60px)이 전부다.

### ⛔ 사장님 말을 잘못 읽어 같은 말을 세 번 하시게 했다

사장님이 「홈에 있는 **리스트**랑 동일하게」라고 세 번 말씀하셨는데, 나는 그것을 «글자 목록»
(가운뎃점으로 이어 쓰는 줄)으로만 읽었다. 짚으신 것은 «**카드 묶음**»이었다 — 선택하신
element 가 셋 다 카드 그리드(`#plan-panel`·`div.grid`·등급 그리드)였는데 못 봤다.
글자 목록만 맞추고 있었으니 사장님 눈엔 내내 그대로였다.
→ **짚어 주신 element 의 정체를 먼저 본다.** 말보다 그쪽이 정확하다.

### ⚠⚠ 재다가 헛짚은 셋 — 1-A 의 목록에 이어 붙인다

4. **마우스 효과는 헤드리스로 «영영» 못 잰다.** 헤드리스 크롬(147 확인)은 가리키는 장치가
   아예 없어 `(hover: hover)` 가 언제나 false 다. `Emulation.setEmulatedMedia` 로 못 박아도,
   화면 크기를 못 박아도, 터치를 꺼도 안 바뀐다. 멀쩡한 CSS 를 두고 「안 된다」는 답이 나온다.
   → `마우스검사.mjs`·`손가락검사.mjs` — 창을 화면 밖(-3000,-3000)에 띄운 «진짜 크롬»에
   CDP 로 마우스·손가락을 보낸다.
5. **`next dev` 와 `next build` 는 같은 `.next` 를 쓴다.** 검사하느라 build 를 여러 번 돌리면
   개발 서버가 내려주는 CSS 가 옛것으로 굳는다. 파일에는 멀쩡히 있는데 화면에는 없어서
   「Tailwind 가 안 만들어 준다」로 잘못 읽을 뻔했다.
   → dev 를 켜 둔 채 build 를 돌렸으면 **끄고 → `.next` 지우고 → 켠다.** 이 순서다.
   (돌아가는 서버 밑에서 `.next` 를 지우면 500 이 난다. 실제로 한 번 냈다.)
6. **실서버 CSS 파일은 «둘»이다.** 배포 확인을 첫 번째 파일에서만 찾다가 10분을 버렸다.
   배포는 진작 끝나 있었다. → 새 표시를 찾을 땐 `.css` 를 **전부** 뒤진다.
7. **`drizzle-kit migrate` 가 «조용히 아무것도 안 하고» 0 으로 끝난다.** 마이그레이션 파일은
   24개인데 DB 에 적힌 것은 20개다 — 어긋난 지 오래고, 이 저장소는 그동안 `push` 로 맞춰 왔다.
   migrate 를 믿고 「칸이 생겼겠지」 하면 안 된다. → `npx drizzle-kit push` 로 맞추고,
   `information_schema.columns` 로 «칸이 진짜 생겼는지» 눈으로 확인한다.

### 실서버에서 확인한 것 (2026-08-26)

```
/ · /packages · /packages/lms · /deliverables · /free      모두 200
카드 폭·한 줄 개수    홈 289/4 · 구매목록 289/4 · 상세 289/4     셋이 같다
구성 목록 글자색·점    #5C5545 · #A79C86                        셋이 같다
흘러가는 띠           1440·1920·2560 에서 빈 칸 없음
맨 아래로 훌쩍 뛴 뒤   숨은 칸 0개   (8/25 에 났던 사고 재발 없음)
```

## 1-C. 2026-08-26 SNS 지킴이 — 「계속 제작중」의 뿌리 넷 (실서버까지 나갔다)

사장님: 「제작중이라고 하는데 반영을 안 하고 계속 제작중이야. 계속 이러는 거 같은데
근본적인 원인을 찾아서 고쳐줘, 단편적인 수정 말고.」

지킴이는 «고장 나지 않았다». 일부러 멈춰 있었는데 그 사실을 말할 방법이 없었을 뿐이다.
파고들자 같은 모양의 구멍이 넷 나왔다 — **넷 다 「고쳤는데 저장이 안 되거나, 저장된 것이
화면에 안 닿는」 이야기다.**

### ⛔ ① 지킴이에게 결과가 «둘»뿐이었다

    됨      다시 굽고 검토대기로 돌려보냄
    막힘    사람이 손봐야 다음으로 감          ← 이름이 없었다
    실패    예외가 던져짐

「막힘」은 예외를 안 던지고 그냥 `return` 했다. 그래서 한바퀴가 그것을 «됨»으로 보고 성공
경로를 탔고, **그 경로가 `watcher_error` 를 빈 칸으로 지웠다. 30초마다.**
보여 줄 자리(list-row 의 빨간 상자)는 2026-08-25 에 이미 만들어 뒀는데 아무것도 도착하지 않았다.

→ `막힘(까닭)` 을 만들어 세 자리에 태웠다(그 밖에 고칠 것 · 대본 없음 · 올릴 영상 없음).
  **앞으로 «⛔ 찍고 그냥 return» 하지 않는다.** 사람 손이 필요하면 반드시 그 함수를 거친다.
  화면도 막혀 있으면 「제작중 · **막힘**」으로 이름과 풀이가 바뀐다(`lib/sns-status.ts`).
  상태값 자체는 안 건드린다 — 손본 뒤 지킴이가 그대로 다시 집어야 한다.

### ⛔ ② 「그 밖에 고칠 것」 한 칸이 두 가지 일을 했다

    ① 「이것 좀 고쳐 주세요」라는 «주문서»
    ② 지킴이를 멈추는 «스위치»

그래서 푸는 길이 «주문서를 지우는 것»뿐이었다. 사장님: 「그 밖에 고칠 것이 있는데 왜 지워?
이것도 고쳐줘서 제작해 주는 거 아니야?」 — 맞는 말씀이다. 자기 말을 지워야 다음으로 갈 수
있는 구조였고, 무엇을 부탁했는지 기록도 함께 사라졌다.

→ 둘을 갈랐다. 주문서(`fix_note`)는 **남는다**. 굽는 쪽을 손본 사람이 검수 화면의
  「손봤습니다 — 다시 구워 주세요」를 누르면 `fix_note_done_at` 에 때가 찍히고 지킴이가 돈다.
  ⚠ 주문서 «글이 바뀌면» 그 표시가 저절로 풀린다 — 새 부탁은 아직 아무도 안 손봤다.

### ⛔ ③ 겹쳐 도는 것을 막는 것이 «한쪽 안»에만 있었다

지킴이가 도는 길이 둘이다 — 검수 화면의 「지킴이 켜기」(`/api/sns/tick`)와 터미널.
그런데 겹침 막이는 tick 라우트 안의 모듈 변수 하나뿐이라 «그 프로세스 안에서만» 통했다.
둘이 같이 돌면 같은 임시 폴더를 같이 쓴다. 오늘 실제로 이렇게 깨졌다:

    · 뒤에 시작한 쪽이 폴더를 지우려다 EPERM — 앞선 쪽 ffmpeg 이 파일을 붙잡고 있다
    · 반쯤 쓰인 mp4 를 읽어 「moov atom not found」

둘 다 «굽다가 죽는» 모양이고, 로그에는 바이트 배열만 쏟아져 까닭을 찾기 어렵다.
→ 프로세스 «밖»에 자물쇠 파일을 뒀다(`%TEMP%/sns지킴이.lock`). 어느 길로 들어와도 보인다.
  못 걸면 조용히 나간다 — 실패가 아니라 「지금은 남이 쓰는 중」이다.
  죽은 자물쇠는 `process.kill(pid, 0)` 으로 살아 있는지 보고 넘겨받는다.

### ⛔ ④ 글가져오기가 «고치기 전에» 저장하고 있었다

    ① 대본을 저장한다
    ② 그러고 «나서» 대본의 ep 를 고친다        ← 이미 저장한 뒤라 그대로 버려진다
    ③ 인트로는 «커버 카피가 있을 때만» 저장한다  ← 부제·태그만 고치면 안 저장된다

영상 아래 띠(서브카피)를 만드는 것이 «대본의 ep» 다(영상굽기.mjs 의 subcap — 대본에
서브카피가 따로 없으면 ep 로 「-…편-」을 만든다). 그 ep 가 한 번도 저장된 적이 없었으니,
검수 화면에서 몇 번을 고치고 몇 번을 다시 구워도 **옛 회차 이름이 그대로 박혔다.**

⚠ 찾기 어려웠던 까닭 — 커버(인트로)는 «제대로» 바뀌었다. 반쯤만 반영되니 「가져오기가
  안 되는 것」으로 안 보이고 「이 자리만 이상한 것」으로 보인다.
→ 고칠 것을 다 고치고 마지막에 저장한다. **저장을 조건 안에 넣지 않는다.**
⚠ 인트로설정이 없는 편은 대본과 인트로가 «같은 파일»이다(지킴이가 대본 길을 두 번 넘긴다).
  그때 인트로를 따로 읽어 쓰면 먼저 저장한 자막 고침을 덮는다 — 같은 파일이면 한 번만 쓴다.

### ⛔ 연장들이 git 밖에 살고 있었다

`.gitignore` 가 `판매용_템플릿/` 를 **통째로** 무시한다. 그래서 SNS 영상을 만드는 연장들이
«이 컴퓨터에만» 있었다 — 고쳐도 저장소에 안 남고, 컴퓨터가 바뀌면 사라진다.
굽는 쪽 큰 것들(영상굽기·영상인트로·썸네일굽기·표지붙이기·화면캡처·검수보내기)은 예전에
누군가 `-f` 로 담아 둬서 살아 있었다. 안 담긴 나머지를 채웠다.

    연장 12개   검수목록 글보내기 본보기모으기 움직임표 자막검사 자막속도 캡션보기
                산출물그림 썸네일굽기_1주차보충 watcher-once.cmd README watcher-README
    설정 16개   인트로설정_*.json

⭐ 인트로설정을 담는 까닭 — `pose`·`sheet`·`rows` 는 **검수기 DB 에 없다.** 손으로 적은
  것이라 되살릴 곳이 없다. `title`·`cap`·`ep` 는 DB 에서 내려오니 잃어도 되찾는다.
  ⚠ 담기 전에 이 폴더 63개를 전부 훑어 열쇠(api key·client_secret·refresh_token·sk-·AIza·
    ya29.)가 하나도 없는 것을 확인했다. **열쇠는 뿌리 쪽 `<루트>/_작업` 에 산다** —
    `_작업` 폴더가 «둘»이라는 것을 헷갈리지 말 것.
  ⚠ 대본(`대본_*.json`)은 «안 담았다». 지킴이가 굽을 때마다 다시 쓰므로 회차마다 diff 가
    흔들린다. 그 글의 뿌리는 검수기 DB 이고 글가져오기가 DB → 파일로 내려 준다.

### 죽은 값 `pos` 를 지웠다

영상인트로.mjs 가 2026-08-20 부터 설정의 `pos` 를 안 본다(tr 로 두었더니 제목을 가려
`br` 로 못 박았다). 그런데 칸은 파일 열여섯에 그대로 남아 있어서, 다음 사람이 그것을
고치고 «왜 안 바뀌는지» 한참 찾게 돼 있었다. 파일 16개 · 편 17개에서 지웠다.
⚠ 썸네일굽기 두 개는 제 안에 표를 따로 들고 있어 그쪽 `pos` 는 그대로다.
  표지붙이기.mjs 도 `pos` 를 읽지만 2026-08-11 에 버려진 파일이다(부르는 곳 없음).

### 이번에 막고 있던 부탁은 «고칠 것이 없었다»

「오른쪽 위 태그 텍스트가 반영되지 않았음」 — 세로(9:16) 커버는 2026-08-19 가이드 실측 때
오른쪽 위 회차를 **일부러** 뺐다(`영상틀_916.html` 의 `.rtop .ep { display: none }`).
그 몫은 아래 서브카피가 한다. 가로(16:9)에는 그대로 나온다.
사장님이 「그대로 둔다」로 정하셨고, 주문서에 그 답을 붙여 두고 풀었다 —
안 붙이면 다시 구워 검토대기로 온 뒤 같은 것을 또 적으시게 된다.

### 잰 것 (2026-08-26)

```
지킴이 한 바퀴     막힘 → DB 에 watcher_error 가 남는 것 확인 (두 번 돌려 같은 자국)
손봤다 단추        찍고 --시늉 으로 돌려 가져오기 → 자막검사 → 굽기 → 커버 → 되올리기 흐름 확인
자물쇠             혼자 돌 때 그대로 · 겹쳐 돌 때 「이미 돌고 있습니다」 하고 조용히 나감
글가져오기 고침     대본의 ep 「한 줄 프롬프트 편」 → 「고수매칭 사이트 편」,
                  다시 구운 영상의 아래 띠를 프레임으로 떠서 눈으로 확인 (2분 7초)
pos 지운 뒤        커버를 다시 구워 마스코트·표·제목·부제가 그대로인 것을 확인
```

⚠ DB 칸(`fix_note_done_at`)은 로컬과 실서버가 **같은 Neon DB** 를 쓰므로 이미 들어가 있다.
  올리기 전에 `information_schema` 로 확인하고 밀었다.

### 어디서 무엇을 하나 — 오늘 세 번 헷갈린 자리

사장님이 하루에 세 번 물으셨다. 「실서버에서 지킴이가 켜면 바로 꺼지네」 ·
「그럼 영상은 로컬, 검토는 실서버? 실서버에 만들 이유가 있나」 ·
「로컬에선 글 고치기가 안 되나?」 — 내가 표를 «이쪽/저쪽»으로 그려 드려서 더 헷갈리셨다.

⭐ **«이쪽/저쪽»이 아니다. «전부/일부»다.**

| | 로컬 (그 컴퓨터) | 실서버 (폰 포함) |
|---|---|---|
| 글 고치기 (자막·카피·캡션·해시태그) | ✓ | ✓ |
| 칸별 그림 보기 | ✓ | ✓ |
| 검토완료 · 최종완료 | ✓ | ✓ |
| 영상 재생 | ✓ | ✗ |
| 굽기 | ✓ | ✗ |
| 유튜브·드라이브 올리기 | ✓ | ✗ |
| 지킴이 | ✓ | ✗ |

**로컬이 전부고, 실서버는 그중 「글·그림·검토」만** 된다. 실서버가 더 하는 것이 아니라 덜 한다.
나누는 기준은 하나다 — **「파일을 건드리는 일인가」.** 녹화본·ffmpeg·유튜브 열쇠가 그
컴퓨터에만 있어서 옮길 수 없다.

→ 사장님 결론(2026-08-26): **어지간하면 로컬에서 하고, 밖에 있을 때 글만 실서버에서 고친다.**

### ⛔ 지킴이는 «상주하는 프로그램»이 아니다 — 내가 이걸로 틀린 답을 했다

30초마다 **켜졌다 → 1~2초 일하고 → 꺼진다.** 검수 화면이 그렇게 두드린다(`/api/sns/tick`).
그래서 **아무 순간에 프로세스를 세면 거의 항상 0** 이다.

나는 그 0을 보고 「지금 지킴이가 안 돌고 있습니다」라고 말씀드렸다. **틀렸다.**
그때 사장님 화면에는 「지킴이 켜짐」이 떠 있었고 로그도 찍히고 있었다.
→ **도는지 보려면 프로세스가 아니라 «화면의 로그»를 본다.** 켜져 있으면 30초마다
  「검수기를 들여다봅니다」가 새로 찍힌다.

⚠ 작업 스케줄러에 등록된 것은 **없다**(확인함). 도는 길은 둘뿐이다 —
  검수 화면의 「지킴이 켜기」, 그리고 터미널(`npm run sns-watch`).
  화면을 닫으면 멈춘다. 이건 사장님이 정하신 것이다:
  「영상이 매번 작업되는 건 아니여서 컴퓨터 켤 때 계속 배치 도는 건 좀 부담스럽고.」

### 흐름 — 누가 무엇을 하나

| 누가 | 무엇 | 상태 |
|---|---|---|
| 루틴(내가 돌림) | 처음 만들어 검수기에 올림 | 검토대기 |
| 사장님 | 검토 완료 | 제작중 |
| **지킴이** | 다시 굽고 되돌림 | 검토대기 |
| 사장님 | 최종 완료 | 등록 중 |
| **지킴이** | 유튜브(비공개)·드라이브에 올림 | 올림 |

⛔ **폰에서 검토완료를 눌러도, 그 컴퓨터에서 지킴이가 켜지기 전까지는 아무 일도 안 일어난다.**
  「제작중」에 얌전히 기다리다가, 나중에 켜면 그때 밀린 것을 집어 간다.
  이것이 「계속 제작중」의 또 다른 얼굴이다 — 막힌 것이 아니라 «아무도 안 돌고 있는 것»이다.

### 드라이브 사본이 빠졌을 때

G: 가 안 붙어 있으면 유튜브에는 올라가고 드라이브 사본만 빠진다. 그러면 화면에 빨간 상자로
남는다. → **드라이브 앱을 켠 뒤 검수 화면의 「드라이브 사본 채우기」 단추**를 누르면 된다.
⚠ 전에는 그 안내가 «명령어»였는데, 그 글을 읽는 곳이 검수 화면이라 거기서 할 수 있는 일이
  없었다 — 막다른 안내였다. 2026-08-26 에 단추를 달았다(로컬에서만 보인다).
⚠ 채운 뒤 빨간불도 저절로 꺼진다. 「이미 다 들어가 있는」 편도 꺼진다 —
  낡은 빨간불이 남으면 무엇이 진짜 막힌 건지 못 가린다.

## 2. 2026-08-13 에 크게 바뀐 것

### ⭐ 글을 쓰는 법이 바뀌었다 — 제일 중요하다

사장님 지적 한 줄로 요약하면 **「조심스럽게 쓰지 말고 홍보를 해라」**다.
자세한 규칙은 `~/.claude/scheduled-tasks/weekly-sns-content/SKILL.md` 의 ⓪·⑦~⑨.

| | 무엇 |
|---|---|
| ⓪ | **전하고 싶은 말은 하나다** — 에이전시는 IA·검수를 한다 → 바이브코딩은 건너뛴다 → 우리가 그 둘을 준다 |
| ⑦ | **홍보를 한다.** 유의사항·조심스러운 말투 금지. 우리 산출물의 «규모»를 «부담»처럼 쓰지 않는다 |
| ⑦-2 | 앞뒤를 **「바이브코딩만으로 → AI팩 넣고」**로 잡는다. 「내가 안 적어서 → 내가 적어서」로 쓰면 우리 팩이 글에서 사라진다 |
| ⑦-3 | **무엇을 눌렀는지 적는다.** 화자는 18년차 기획자다. 경력은 연차가 아니라 «손버릇»으로 보인다 |
| ⑦-4 | **자막을 끊어서 개수를 늘리지 않는다.** 속도는 «컷»으로 낸다 |
| ⑦-5 | **포인트 색** — 숫자·핵심어에 `<span class='o'>`, 한 칸에 한 군데 |
| ⑦-6 | **격식 차리지 않는다.** 없는 상황을 지어내지 않는다 |
| ⑧ | **숫자에는 할 일을 붙인다.** 링크 바로 앞에는 숫자를 두지 않는다 |

**새 파일: `판매용_템플릿/_마케팅/왜_만들었나.md`**
사장님이 왜 이 서비스를 만들었는지(pople 팝업 매칭 서비스 이야기)가 **말씀 그대로** 들어 있다.
대본·캡션·상세페이지를 쓰기 전에 **제일 먼저 읽는다.** 루틴 1단계에도 걸어 뒀다.

### 영상 만드는 법이 바뀌었다

| | 전 | 지금 |
|---|---|---|
| 자막 한 칸 | 2.5초 | **1.8초** (대본의 `"칸초"`) |
| 한 편 컷 | 20 안팎 | **30개 이상**, 평균 1.2초 안쪽 |
| 재료 | 캡처만 | **녹화본 + 캡처를 한 편 안에서 섞음** (`"clip"`) |
| 세로 표지 | 영상틀에 고양이만 | **썸네일틀 `v=9`** — 큰 카피 3~4줄 + 산출물 목록 칸 |
| 표지 부제 | 있었음 | **없앰** (2초에 못 읽는다) |
| 표지 큰 글자 | — | **크기 고정.** 카피를 고쳐서 맞춘다 |
| 화면 칸 비율 | 세로·가로 1.28 | **세로 1.565 · 가로 1.28** (세로가 폭을 꽉 채운다) |

### 코드

| 파일 | 무엇 |
|---|---|
| `_작업/화면캡처.mjs` | **새로 만듦.** 팩의 완성화면 → 1440×2600 캡처. 팩마다 폴더를 갈라 뜬다 |
| `_작업/영상굽기.mjs` | 칸초·녹화 토막·캡처방·비율 분리·가로 자름 검사 |
| `_작업/영상인트로.mjs` | 표지를 썸네일틀로, **한 벌로** 굽기, 제목 줄 넘침·서명줄 검사 |
| `_작업/썸네일굽기.mjs` | 넉넉히 찍고 잘라내기(아래 함정 ① 참고) |
| `_마케팅/썸네일틀.html` | 세로 제목 위로, 고양이 `bottom` 175, 빈 부제 숨김 |
| `package-free-sample.mts` | **무료 샘플 README 숫자를 문서에서 세어 쓰게 바꿈** |

### 홈페이지 쪽에서 고친 것

**무료 샘플 zip 의 README 숫자가 여섯 군데 틀려 있었다** — 손님이 받는 파일 안에서.
화면 15→**16**, 메뉴 5→**6**, 요구사항 87→**94**, 이동 21→**22**, 흐름도 5→**6장**, 일정 15→**16줄**.
지금은 `package-free-sample.mts` 가 xlsx·drawio 를 열어 세어 쓴다. **손으로 적은 숫자는 반드시 썩는다.**

---

## 3. 다음에 이어서 할 일

### 정해진 것 (2026-08-13)

**① 자막 분량 — 공백 제외 «330자»** (처음 말씀하신 600자는 공백 포함이었다).
37.8초에 330자면 초당 8.7자로 편히 읽힌다. 영상4 를 234 → 347자로 채워 다시 올렸다.
⚠ 더 늘리려면 길이도 같이 늘려야 한다. 초당 12자를 넘기면 아무도 못 읽는다.

**② 무료 샘플에 검수 시나리오를 넣는다.**
`08_검수시나리오.xlsx`(확인 항목 105개)를 zip 에 담고, 무료 페이지에도 카드로 올렸다.
「문서 9종」으로 늘었다. 규모로 벌린다 — 무료 105항목 vs 유료 216~944항목.
디자인 프리셋은 계속 유료에만 넣는다.

**③ SNS 링크는 «고정 세트»다 (2026-08-13).**

```
무료샘플 받아보고 싶다면 👉 https://www.caffeinecolor.com/free
IA팩 궁금하다면👉 https://www.caffeinecolor.com/
어떤게 만들어 지는지 궁금하다면 👉 https://www.caffeinecolor.com/dashboard
```

인스타는 한 줄 — 「IA 팩이 궁금하다면 프로필 링크를 확인해 주세요. 무료 샘플 링크도 있어요. 🤗」
⚠ **꺽쇠(`>`)는 캡션에서 못 쓴다.** 화살표는 **👉** 로 쓴다.
⚠ **`/packages` 는 PG 붙기 전까지 SNS 어디에도 안 쓴다.** 살 수 없는 자리로 보내면 헛걸음이다.

✅ **둘째 줄을 2026-08-14 에 홈(`/`)으로 바꿨다** (사장님 지시). 전에는 둘째·셋째가
둘 다 `/dashboard` 라 같은 자리로 두 번 보내고 있었고, 그 자리는 로그인 벽(307 → `/login`)이다.
「IA팩이 궁금하다」는 사람은 소개 화면을 봐야 하고, 「어떤 게 만들어지는지」는 만드는 화면이 맞다.

⚠ **이미 올라간 영상의 설명은 사장님이 직접 고치신다.** 지시서를 고쳐도 옛 영상은 안 바뀐다.

### 다음 회차(8/17 월 루틴)에서 «이게 지켜졌나» 볼 것

오늘 지시서에 넣은 것들이 실제로 나오는지 확인하는 자리다. 사장님이 보실 때 이 순서로 본다.

| 볼 것 | 어떻게 |
|---|---|
| **자막 분량** | 공백 제외 **330자 안팎**인가 (300~360). 대본 JSON 을 세어 본다 |
| **끊어서 늘리지 않았나** | 빗금(`/`)으로 끝나는 자막이 있으면 잘못이다 |
| **포인트 색** | 숫자·핵심어에 주황이 들어갔나. 한 칸에 한 군데만 |
| **말투** | 없는 상황을 지어내지 않았나(「다 됐냐고 물으면」 같은 것) |
| **표지** | 큰 카피만 3~4줄, 부제 없음, 글자 크기 그대로. 줄바꿈이 낱말·뜻으로 끊겼나 |
| **앞뒤 뼈대** | 「바이브코딩만으로 → AI팩 넣고」인가. 「내가 안 적어서」면 다시 |
| **링크** | 👉 세트 그대로인가. **`/packages` 가 들어갔으면 잘못** |
| **컷** | 평균 1.2초 안쪽인가(굽고 나면 첫 줄에 찍힌다) |

### ⭐ 토스페이먼츠 계약심사 — 회신 대기 (2026-08-13)

**드디어 계약 담당자 메일이 왔다.** MID `caffeiew3v` · 계좌이체 | 신용카드.
회신 초안: `_작업/토스페이먼츠_회신초안.md` (지어내지 않고 코드·약관·운영 사이트에서 확인해 씀)

| 답한 것 | 값 |
|---|---|
| 단건 최고가 | **99,000원** (크레딧 「맥스」 1,287크레딧) |
| 최대 서비스제공기간 | **12개월** — 유상 크레딧 유효기간이 1년(약관 제6조 ⑤) |
| 앱 | 없음 (웹만) |
| 휴대폰·상품권 | **N** — 컨텐츠 7%·상품권 9%, 정산 3개월 후 |
| 직접 연동 | **Y** — 토스 결제위젯 SDK v2, 호스팅사 아님 |

⚠ **사장님이 채워야 넘어간다** — ① 테스트 계정 ID/PW(로그인 벽 뒤에 결제 화면이 있다.
**계정은 사장님이 만드신다** — 우리가 비밀번호를 다루지 않는다) ② 결제경로 PPT.

⚠ **계약이 한 달 안에 안 끝나면 신청이 취소되고 재신청해야 한다.**

⚠ 고쳐 두면 심사에 유리한 것 하나 — 토스는 「서비스제공기간을 **상품페이지 내에**」
요구하는데, 충전 화면은 「유효기간은 이용약관·FAQ에서」라고만 되어 있다.
**「충전한 크레딧은 충전일로부터 1년간 쓸 수 있어요」 한 줄을 충전 화면에 직접** 넣는 것이 안전하다.

### ⭐ 다운로드 zip 이 판매팩과 달랐다 (2026-08-14 사장님 지적)

사장님이 손수 만드신 「반려동물 유치원」 팩을 받아 보고 두 가지를 짚으셨다.

**① 파일 이름 — 고쳤다 ✅**
`IA_화면목록_나는 펫 유치원을 운영하고 있어. 반려동물 유치원사이트.xlsx` 처럼
컨셉이 파일마다 따라붙었다. **판매팩처럼 번호 + 무엇인지만** 쓰도록 바꿨다
(`01_메뉴구조.xlsx` … `10_앱으로_내놓는_법.html`). 프로젝트 이름은 **zip 이름 한 곳**에만 둔다.
⚠ 덤으로 2026-08-11 의 「줄바꿈이 든 이름이라 압축 풀면 파일이 조용히 빠진다」가
**구조적으로** 사라졌다 — 안쪽 이름에 손님 글이 아예 안 들어간다.

**② 디자인·레이아웃 프리셋 — 고쳤다 ✅ (2026-08-14, 배포함)**
`lib/preset-pack.ts` 로 끌어냈다(902줄). 이제 **판매팩을 굽는 빌드 스크립트와
손님이 받는 zip 이 같은 곳을 쓴다** — 파는 팩과 받는 팩이 영영 안 갈린다.
손님 zip 은 `디자인프리셋/` 아래 열한 개 + `00_이 프로젝트에 맞춘 설명.md`.
고른 색·뼈대를 맨 앞에 두고 3벌 × 2벌로 채운다.
⚠ 옮기기 전 판매팩 프리셋을 떠 두고 다시 구워 견줬다 — **바이트까지 같았다.**
`build-design-presets.mts` 는 1082줄 → 259줄.

### ⭐ 밀려 있는 일 — 한눈에 (2026-08-17 기준)

문서 여기저기 흩어져 있어서 한 자리에 모았다. **자세한 것은 각 절에 있고, 여기는 목록이다.**

| 무엇 | 누가 | 어디에 자세히 |
|---|---|---|
| ⭐ **펫유치원 — 사장님 컷편집본으로 다시 짰다(63.8초).** 검수만 남았다 | **사장님** | 「자막을 두 배로 몰아넣고 있었다」 절 |
| ⛔ **`대본_내컴퓨터에서만` 은 새 규칙에 걸린다** (초당 10자·칸 1.8초) — 다시 써야 한다 | 나 | 〃 |
| ✅ ~~검수 화면에 **펫유치원이 두 편**~~ **끝** (2026-08-28 확인) — 검수 DB 에 `펫유치원_컷편집` 한 편뿐이다 | — | 〃 |
| ✅ ~~**`deny` 두 줄 지우기**~~ **끝** (2026-08-28 확인) — `git push` 는 allow, `vercel` 은 ask 로 옮겨져 있다 | — | 「배포가 막혀 있다」 절 |
| ✅ ~~유튜브 **옛 판 여덟 개 지우기** (8/17 회차)~~ **끝** (2026-08-28 사장님 확인) | — | `주간작업기록.md` 2026-08-17 맨 위 상자 |
| ✅ ~~유튜브 옛 판 넷 지우기 (8/11·8/13)~~ **끝** (2026-08-28 사장님 확인) | — | 이 문서 위쪽 「스튜디오에서 지워야 할 것 넷」 |
| **토스 계약 회신** — 테스트 계정 + 결제경로 PPT | **사장님** | 「토스페이먼츠 계약심사」 절 |
| ✅ ~~충전 화면에 「크레딧 1년」 한 줄 넣기~~ **끝** (2026-08-28 확인) — `charge-client.tsx:162` 「충전일로부터 1년(365일)」 | — | 〃 |
| 홈 메인 **③ 움직임** (①② 는 끝) | 나 | 「홈 메인 「장 구분」」 절 |
| ✅ ~~**장비렌탈 사진 없는 자리** — 장비 사진 ~10장 필요~~ **끝** (2026-08-28 확인) — 25장 들어와 팩에 깔렸다 | — | 「팩에 아직 사진이 없는 자리」 절 |
| **뷰티샵 프로필 73곳** — 동물 사진에서 얼굴만 오려 채우기 (사장님 승인 받음) | 나 | 아래 ⚠ |
| ✅ ~~**`출처.csv` 가 빈 채다**~~ **끝** (2026-08-28) — 331/331 채움. `npx tsx 출처채우기.mts` 로 잰다 | — | 아래 ⭐ |
| ✅ ~~「검토 완료」된 것을 굽고 올리는 쪽~~ **끝** — `_작업/sns지킴이.mts` | — | 아래 ⭐⭐⭐ |
| ✅ ~~유튜브 설명란 수정 권한~~ **끝** — 앱도 「프로덕션」으로 바꿔 7일 만료를 없앴다 | — | 아래 ⭐⭐ |
| LMS·여행·공동구매 사진 채우기 | 사장님이 사진 주시면 나 | 「팩에 아직 사진이 없는 자리」 절 |

⚠ **뷰티샵 프로필 73곳** — 2026-08-14 에 사장님이 「이 동물 사진들 중 배경 없는 것들은
SNS에서도 잘활용해줘, 뷰티샵 사이트 만들때 뷰티샵 아이들 사진을 넣는다던지」라고 하셨고,
「마스코트 얼굴은 그냥 우리 동물 이미지 있는 거에서 짤라서 써줘」로 방법까지 정해 주셨다.
아직 시작하지 않았다. 대상은 뷰티샵 팩의 프로필·아바타 자리 73곳.

⚠ **`올리기.mts` 가 없다** — `/admin/sns` 에서 「검토 완료」를 누르면 상태만 `approved` 가 된다.
그것을 집어 **다시 굽고 유튜브(비공개)+G드라이브에 올리는 쪽은 아직 내가 손으로 한다.**
사장님이 검수 화면을 써 보시고 손볼 데가 없을 때 붙이는 것으로 미뤄 뒀다.
붙일 때는 `status='approved'` 인 것만 집고, 올린 뒤 `published` 로 바꾸며
`youtube_vertical_id`·`youtube_horizontal_id` 를 채운다(칸은 이미 스키마에 있다).

✅ ~~**유튜브 설명란을 API 로 못 고친다**~~ — **끝났다 (2026-08-18).** `youtube.force-ssl` 을
스코프에 넣고 사장님이 다시 승인하셨다. 이제 `_작업/유튜브설명고치기.mjs` 로 고친다.

### ⭐⭐ 「왜 매번 승인해야 해?」 — 앱이 «테스트» 상태였다 (2026-08-18)

사장님이 물으셨고, 까닭이 **둘**이었다.

**① 권한을 넓히면 다시 승인해야 한다 (한 번뿐)**
8/17 에 403 을 만나고 「다음 로그인 때 `force-ssl` 을 넣어 두라」고 **적어만 두고 안 넣었다.**
그래서 8/18 에 또 하시게 됐다. 지금은 넣었으니 이 까닭으로는 안 생긴다.
⚠ **적어 두는 것과 넣어 두는 것은 다르다.** 다음에도 이런 것은 그 자리에서 코드에 넣는다.

**② 구글 OAuth 앱이 «테스트» 상태였다 — 이게 진짜였다**
테스트 상태에서는 **refresh 토큰이 7일마다 죽는다.** 무슨 짓을 해도 주마다 다시 승인해야 한다.
→ 사장님이 **「프로덕션」으로 바꾸셨다** (구글 클라우드 콘솔 → OAuth 동의 화면 → 게시 상태).
사용자 유형 «외부» · 한도 100명 중 1명. **이제 안 죽는다.**

⚠ 유튜브 권한은 「민감한 범위」라 승인 화면에 「확인되지 않은 앱」 경고가 뜬다 —
본인 채널에 본인이 쓰는 앱이라 심사는 필요 없다. `고급 → 이동` 으로 넘어간다.
⚠ **또 「테스트」로 돌아가 있으면 그게 까닭이다.** 도구 셋(`유튜브올리기`·`유튜브설명고치기`·`유튜브확인`)이
`invalid_grant` 을 만나면 이 안내를 그대로 찍는다 — 오류 덩어리만 보고 헤매지 않게.
⚠ 로그인은 **8788 포트**를 쓴다. 옛 시도가 물고 있으면 `EADDRINUSE` 로 죽으니 그것부터 끈다.
> ~~로그인 끝에 `Assertion failed: !(handle->flags & UV_HANDLE_CLOSING)` 가 찍히는데 무해한 종료 버그다.~~
> **2026-08-25 에 아예 안 나게 고쳤다.** 남은 연결을 끊고(`closeAllConnections`) 150ms 쉬었다 나간다.
> 왜 고쳤나 — 「무해하다」고 적어 둬도 그때그때 겪는 사람은 «실패한 줄» 안다. 실제로
> 저장이 다 끝났는데도 exit 127 이 나서 루틴이 «failed» 로 보고했다. 다 된 것을 실패로
> 보이게 두는 것이 제일 나쁘다.

### ⭐⭐⭐ 「프로덕션인데 왜 또 죽어?」 — 7일 시한은 «토큰을 만들 때» 박힌다 (2026-08-25)

8/18 에 프로덕션으로 바꿨는데도 **8/25 에 또 `invalid_grant` 이 났다.**
나는 「또 테스트로 돌아간 것 아니냐」고 짚었는데 **틀렸다** — 사장님이 콘솔을 보여 주셨고
게시 상태는 **「프로덕션 단계」가 맞았다.**

날짜를 재니 답이 나왔다.

| | |
|---|---|
| 토큰을 받은 때 | 2026-08-18 **09:17** (`_작업/유튜브_인증.json` 손댄 때) |
| 죽은 때 | 2026-08-25 → **정확히 7일** |

**7일 시한은 «토큰을 만들 때» 박힌다.** 나중에 프로덕션으로 올려도 **이미 만든 토큰은
그 시한을 그대로 지고 죽는다.** 그날 프로덕션으로 바꾼 것이 09:17 «뒤»였던 것이다.

⭐ **그러니 게시 상태를 바꾼 «뒤에» `--로그인` 을 한 번 더 돌려야 한다.** 안 그러면
「바꿨는데 왜 또 죽지」를 한 주 뒤에 다시 겪는다. 8/25 에 다시 받은 토큰은 안 죽는다.

**⚠ 다음 사람이 모르면 사고 나는 것 셋**

1. **「등록 중」에서 멈춰 보이면 열에 아홉은 지킴이가 «막힌» 것이다.** 상태는 일부러
   안 건드리게 돼 있어서(다음 바퀴에 다시 해 보려고) 겉으로는 멈춘 것과 똑같이 보인다.
   → 이제 `sns_content.watcher_error` 에 까닭이 **남고** `/admin/sns` 에 빨간 칸으로 뜬다.
   알림은 그 화면을 열어 둔 그때만 스쳐 지나가서 사장님이 못 보셨다.
2. **브라우저를 여는 길이 `cmd /c start` 면 안 된다.** `&` 를 명령 구분자로 읽어 주소가
   잘리고(2026-08-09), 그렇다고 따옴표로 감싸면 node 가 «그 따옴표까지» 한 번 더 감싸서
   주소가 통째로 «파일 이름»이 된다 — 윈도우가 「이름을 올바르게 입력했는지 확인하고」
   창을 띄운다(2026-08-25 사장님이 겪으셨다). → `rundll32 url.dll,FileProtocolHandler` 로
   바꿨다. 주소를 «인자 하나»로 받아서 `&` 도 따옴표도 문제되지 않는다.
3. **`_작업/` 은 `.gitignore` 에 들어 있다** (열쇠가 든 폴더다). 그래서 위 두 고침은
   **저장소에 안 들어간다 — 이 컴퓨터에만 있다.** 방을 옮기거나 컴퓨터를 바꾸면 다시 겪는다.
   그 때문에 여기에 적어 둔다.

**손으로 다시 받는 법** — 브라우저에서 계정 고르기 → 「확인되지 않은 앱」이면 `고급 → 이동`
→ 체크박스 **전부** 체크 → 계속 → 「승인됐습니다」

```bash
node _작업/유튜브올리기.mjs --로그인
```

받은 뒤 **먹히는지 재 보는 것**까지 해야 끝이다 — 채널 이름이 「카페인컬러」로 나오고
권한 셋(`youtube.upload` · `youtube.readonly` · `youtube.force-ssl`)이 다 붙어 있어야 한다.
하나라도 빠지면 올린 뒤 «확인»이나 «설명 고치기»에서 403 이 난다.

### ⭐⭐ 스펙팩에 없어서 «만들 때마다» 생기던 것 둘 (2026-08-18 펫 유치원 실측)

> 「중요한건 생성 AI팩에서 문제 없도록 해줘야해」

만들어진 사이트 하나를 고치는 것으로는 안 끝난다. **스펙팩이 안 시켰기 때문에 생긴 것**이라
다음 팩에서도 똑같이 나온다. 그래서 `lib/design-presets.ts` 에 규칙을 두고
`lib/export/spec-pack.ts` 가 **§2 공통 요소**로 내보내게 했다.

| 상수 | 스펙팩에 나오는 절 |
|---|---|
| `NARROW_OVERFLOW` | 「좁은 화면에서 넘치지 않게」 |
| `SINGLE_SOURCE_DATA` | 「바꿀 값은 한 곳에」 |

#### ① 폰에서 가로로 밀린다 — 표가 아니라 «페이지 번호 줄»이 범인이다

146개 중 여덟 화면이 390px 에서 페이지째 밀렸다(최대 +97px).
**표는 멀쩡했다** — `overflow-x:auto` 상자 안에서 잘 돌고 있었다.
밀어낸 것은 **표 밑의 페이지 번호 줄**과 「다음 프로그램 ›」 같은 **이동 단추**다.
만드는 쪽은 표만 감싸고 그 옆에 붙는 것은 한 줄로 둔다. 쪽수가 늘수록 더 밀린다.

⛔ 스펙팩에 「반응형」·「폰에서 1열」은 있었는데 **«넘치면 안 된다»는 말이 없었다.**
   열 수만 맞추면 통과한 것처럼 보이고, 가로로 밀리는지는 아무도 안 본다.
⚠ 고칠 때 표를 좁히면 안 된다 — 칸 글자가 세로로 접혀 못 읽는다. 감싸서 돌린다.

#### ② 상호를 바꿔도 옛 이름이 남는다

펫 유치원 사이트에서 상호가 데이터 파일 **밖 열 곳**에 글자로 박혀 있었다
(화면 코드 여섯 · `index.html` 의 `<title>`·메타 설명 둘 …).
손님이 받아서 **제일 먼저 하는 일**이 「우리 가게 정보로 바꾸기」인데, 그게 한 곳에서 안 끝났다.

⛔ **`<title>` 과 메타 설명이 제일 자주 빠진다.** 정적 HTML 이라 데이터에서 안 온다.
⚠ 안내 문구 안에 섞인 상호도 마찬가지다 — 「내비게이션에 ‘○○’를 넣으세요」 같은 곳.
⭐ 확인은 **상호를 다른 이름으로 실제로 바꿔 보는 것**이다. 눈으로 훑으면 반드시 놓친다.
   받침 없는 이름으로 바꾸면 조사(이/가)까지 같이 걸린다 — 그래서 `U.josa()` 를 넣었다.

#### 검수 도구

`_작업/_펫검수/` — 설계 대조 스크립트와 결과서. `qa-site.mts` 는 `pages/*.html` 구조를 전제하므로
한 장짜리(SPA)로 만든 사이트에는 그대로 못 쓴다. 같은 검사 항목을 브라우저에서 돌렸다.
⚠ `qa-site.mts` 의 「본문 200자 미만 = FAIL」은 한 장짜리에서 헛짚는다 —
   로그인·완료·확인 같은 폼 화면 17개가 다 걸렸고 전부 정상이었다.

### ⭐⭐⭐ 「아는 것이 손님에게 가나」를 세는 검사기 (2026-08-18)

```bash
npx tsx check-pack.mts
```

사흘 동안 나온 결함 여섯 중 **넷이 같은 모양**이었다 —
**우리가 이미 아는 것이 손님 팩에 안 가고 있었다.**

- `acts` 칸을 만들고 채우는 곳을 안 만듦 (146개 전부 빈칸)
- 검수 시나리오의 «올바른 생성기»가 있는데 내려받기가 안 부름
- 「줄어드는 지점은 두 곳만」이 코드 주석에만 있고 팩엔 안 나감
- 「읽기 폭은 한 값만」은 시켰는데 «확인할 칸»이 없음

사람이 눈으로 찾으면 또 놓친다. 그래서 **팩을 실제로 뽑아 놓고 그 안을 센다.**

| 무엇을 보나 | 어떤 사고를 막나 |
|---|---|
| ① 우리가 정한 **값**이 손님 글에 나오나 | 상수만 있고 안 나가서 520·560·860 을 지어낸 일 |
| ①-2 규칙 **문장**이 손님 글에 있나 | 절을 지웠는데 숫자만 남아 통과하던 일 |
| ② 화면 칸 중 **전부 빈 것**이 있나 | `acts` 처럼 만들고 안 채운 죽은 칸 |
| ③ **시킨 것 ↔ 확인할 칸** 짝 | 지침 원칙 3-2 — 한쪽만 고쳐 갈라지는 일 |
| ④ 검수 시나리오가 **점검표 꼴**인가 | 기대 결과·화면ID 가 없어 PASS/FAIL 을 못 고르던 일 |
| ⑤ **판매팩과 같은 파일**이 나가나 | 산출물이 빠지는 일 |

⭐ `check-presets.mts` 가 「글과 코드가 같은 말을 하나」를 본다면,
   이것은 **「코드가 아는 것이 손님에게 가나」**를 본다. 둘은 짝이다. 릴리스 전에 둘 다 돌린다.

⛔ **첫 실행에서 바로 하나 잡았다** — `화면안동작` 을 새로 넣으면서 죽은 `acts` 를 안 뺐다.
   검사기를 만든 그 자리에서 그 사고가 또 나 있었다.

⚠ **①(값)만으로는 약하다.** 스스로 시험해 보다 알았다 — 「줄어드는 지점」 절을 통째로
   지워도 1024·720 이 CSS 예시에 남아 있어 ① 이 통과했다. 손님이 읽는 것은 숫자가 아니라
   **문장**이다. 그래서 ①-2 를 더했다. 규칙을 늘릴 때 ①-2 의 문장 표에도 한 줄 더한다.

#### 이 검사기는 «언제» 도나 (2026-08-18)

> 「그럼 AI팩을 생성할때 이 검사기가 돈다는거야?」

**아니다.** 손님이 팩을 뽑을 때 도는 것이 아니다. 둘은 다른 일이다.

| | 무엇을 보나 | 언제 |
|---|---|---|
| `check-presets` · `check-pack` | **우리 코드**가 아는 규칙이 손님 글에 나가나 | **코드를 고칠 때마다** (CI) |
| (아직 없음) | **이 손님의 자료**가 온전한가 | 팩을 뽑을 때 |

앞의 둘은 프로젝트가 바뀌어도 답이 같다 — 「1440px 가 팩에 적혀 있나」는 손님마다
다르지 않다. 그래서 **손님이 뽑을 때마다 돌릴 이유가 없고**, 코드를 고칠 때 도는 것이 맞다.
`git push` 하면 GitHub Actions 가 돌린다(CI 에 걸어 뒀다).

⚠ CI 에는 DB 가 없다. 그래서 `check-pack` 은 **판매팩 자료(뷰티샵)로 규칙만** 본다.
  ②(화면 칸이 전부 비었나)는 실제 프로젝트라야 뜻이 있어 그때만 돈다 —
  손으로 `npx tsx check-pack.mts` 를 돌리면 DB 의 진짜 프로젝트로 ②까지 본다.

⛔ **판매팩 자료에는 `acts` 가 손으로 다 적혀 있다.** 그래서 판매팩은 잘 나왔던 것이다.
  손님 팩은 그 칸이 비어 나갔다 — 사람이 적어 준 것과 AI 가 만든 것의 차이였다.
  `화면안동작`(기능정의에서 뽑기)이 그 틈을 메운다.

**아직 없는 것 — 팩을 뽑을 때의 점검.** 이 손님 자료가 온전한지는 아무도 안 본다.
화면 수·기능정의 빈 화면·뒤로가기 계산 결과 같은 것이다. 필요해지면 그때 붙인다.

#### 다듬기(enrich) 실패를 손님에게 알린다 (2026-08-18)

> 「2번부터 붙혀주고, 1번은 굳이 하지 않아도 될거 같아. 우리는 미리보기는 이미 제공하고 있잖아.」

차감 «전» 경고(자료가 이래도 받으시겠어요)는 **안 붙였다** — 미리보기에서 이미 보시니 중복이다.
차감 «후» 알림만 붙였다.

**전에는 결과를 통째로 버리고 있었다** — `try { await fetch(...) } catch {}`.
그래서 다듬기가 실패해도 손님은 «얇은 본문»을 받고도 그런 줄 몰랐다. 돈은 이미 나간 뒤다.

다듬기가 실패하는 경우는 넷이다.

| 값 | 뜻 |
|---|---|
| `unavailable` | 우리 쪽 AI 열쇠가 없음 |
| `no-credit` | **우리 회사** AI 잔액 부족 (손님 크레딧이 아니다) |
| `failed` | 그 밖의 오류 — 화면 100개면 최대 300초라 시간 초과가 실제로 난다 |
| `failedChunks > 0` | 묶음 일부만 실패 — 일부 화면만 얇게 나간다 |

⭐ **「다시 받으면 됩니다」가 사실이라 알리는 것만으로 끝난다.**
- 크레딧은 두 번 안 빠진다 — 이미 풀린 프로젝트는 차감 단계를 건너뛴다
- 이미 다듬은 화면은 `enrichedAt` 으로 건너뛴다 — 실패한 것만 다시 한다

⛔ **PG 승인이 나면 `REVIEW_MODE`(lib/flags.ts)를 꺼야 한다.**
   심사 기간에는 다듬기를 통째로 건너뛴다(테스트 키라 결제는 공짜인데 보강은 진짜 돈이 나간다).
   안 끄면 **손님이 진짜 돈을 내고도 계속 기본본만 받는다.** 승인 직후 제일 먼저 할 일이다.

### ⭐⭐ 디자인·레이아웃 — 재 보니 둘이 틀어져 있었다 (2026-08-18)

빌드된 CSS 를 팩이 못 박은 값과 하나씩 견줬다.

| | 팩이 시킨 값 | 실제 | |
|---|---|---|---|
| 콘텐츠 폭 | 1440px · 좌우 24px | `--content-max: 1440px` | ✔ |
| 격자 열 | 3 / 2 / 1 | `.grid--3` + 1024·720 미디어쿼리 | ✔ |
| 썸네일 비율 | 4:3 | 372×279 = 1.33 | ✔ |
| **읽기 폭** | **760px 하나만** | **46ch · 56ch · 68ch 세 벌** | ❌ |
| **줄어드는 지점** | (안 시킴) | 1024·720 + **520·560·860** | ❌ |

#### ① 읽기 폭 — 시켰는데 안 지켜졌다

팩에 「**이 값 하나만 씁니다.** 화면마다 680·720·760 처럼 다르게 잡지 마세요」라고
적혀 있는데도 세 벌이 나왔다. 규칙은 있었고 **확인할 칸이 없었다.**
→ 공통 점검에 「읽기 폭이 한 값인지」를 넣었다.

#### ② 줄어드는 지점 — 우리가 안 시켰다

`BREAKPOINTS` 주석에 「두 곳만 두는 이유」가 적혀 있는데 **그건 코드 주석이라
손님 팩에 안 나간다.** 그 주석은 이렇게 경고하고 있었다 —
「이 값을 아무 데도 안 적어 뒀더니 같은 스펙팩으로 만든 세 벌이 각자 다른 값을 지어냈다」.
이번에도 520·560·860 을 지어냈다.
→ `COMMON_RULES` 에 「줄어드는 지점 — 두 곳만 씁니다」 절을 넣었다(스펙팩·프리셋 둘 다에 나간다).

#### ③ 썸네일이 «깨져 보인» 진짜 이유

비율은 멀쩡했다(4:3 그대로). 규칙 **둘이 서로를 안 보고 있었다**.

- 자리표시자 규칙: 옅은 파스텔 + 회색 글자로 **읽히게** 둔다
- 「사진 위 겹침」 카드: 그 위에 **검정 72% 어둠막** + 흰 글자

배달 시점엔 사진이 없으니, 옅은 자리표시자 위에 검정이 덮여 회색 글자가 묻혔다.
규칙 둘이 만나는 곳을 아무도 안 정해 둔 것이다.
→ 「사진이 들어오기 전까지 어둠막을 걸지 않는다」를 자리표시자 규칙과 overlay CSS 양쪽에 넣었다.

#### 공통 점검이 11 → 15줄이 됐다

§7(눌러 보기)과 짝을 맞췄다 — 지침 원칙 3-2 가 「가이드에 규칙을 더하면 여기에도
확인 항목을 더한다」고 못 박은 대로다.

```
+ 거르는 단추 — 필터·정렬·탭이 목록을 실제로 바꾸는지
+ 가게 정보를 바꿨을 때 옛 이름이 남는 곳
+ 읽기 폭이 한 값인지
+ 줄어드는 지점이 두 곳뿐인지
```

### ⭐⭐⭐ 손님이 받던 검수 시나리오가 «다른 물건»이었다 (2026-08-18)

> 「검수 시나리오의 완성도 > 이건 추가금이잖아. 돈주고 만들었는데 안맞고 틀어진다면
>  바로 클레임일 수밖에 없어.」

팩에 든 `08_검수시나리오.xlsx` 를 **판매팩의 것과 견줬다.**

| | 판매팩(정답지) | 손님이 받던 것 |
|---|---|---|
| 칸 | 테스트ID·메뉴·화면·**화면ID**·확인 항목·확인 방법·**기대 결과** | 테스트ID·페이지·화면구분·테스트영역·테스트 방법 |
| 줄 | 785 | 1,022 |
| 화면ID | 136줄 채움 | **칸 자체가 없음** |
| 기대 결과 | 785/785 채움 | **칸 자체가 없음** |
| 페이지 | — | **1,022줄 전부 빈칸** |
| 공통 점검(SCN-000) | 1묶음 | **0줄** |

⛔ **「기대 결과」가 없으면 본 사람이 PASS/FAIL 을 못 고른다 — 점검표가 아니다**(지침 원칙 3).
⛔ 「화면ID」가 없으니 어느 화면인지도 못 짚는다. 「페이지」 칸은 1,022줄이 다 비어 있었다.

#### 왜 이렇게 됐나

`zip-all-button.tsx` 가 **검수 «제품»의 결과**(`buildVerifyScenarioSheets` — 라이브 사이트를
돌며 채우는 것)를 팩에 넣고 있었다. 팩의 산출물은 **설계도 기준 점검표**여야 한다.

그 점검표를 만드는 함수는 **이미 있었다** — `lib/export/template-verify.ts` 의
`buildTemplateVerifySheets()`. 판매팩 여덟 벌을 만든 바로 그 함수고,
판매 페이지가 「확인 항목 몇 개」를 광고할 때 세는 것도 이 함수다.
**만드는 쪽만 안 부르고 있었다.**

#### 제일 아픈 것 — 공통 점검 열한 가지

그 점검표 맨 앞(SCN-000)에 이게 들어 있다.

```
뒤로가기 · 모달·팝업의 취소와 닫기 · 현재 위치 표시 · 여러 단계 흐름의 이전 단계
막다른 화면의 다음 행동 · 전체 화면 목록으로 가는 길 · 손님/운영자 헤더 분리
눌러도 반응 없는 버튼 · 이미지 자리의 비율 · 공통 요소 일관성 · 모바일 가로 스크롤
```

**사흘 동안 손으로 찾아낸 결함이 전부 이 열한 줄 안에 있다.**
지침(`docs/검수시나리오_작성지침.md` 원칙 3-2)이 2026-08-04 사고를 겪고 이미 적어 둔 것이다.
손님이 그 칸을 받았더라면 첫 빌드에서 스스로 찾았을 것이다.

#### 고친 뒤

팩에 **늘** 설계도 기준 점검표가 들어간다(펫 유치원 기준 1,098줄 · 기대 결과 1,098/1,098 채움 ·
공통 점검 11줄). 라이브 검수를 돌리셨으면 그 결과는 `08-1_검수결과(사이트).xlsx` 로 **따로** 들어간다.

⚠ 지침이 이렇게 못 박고 있다 — **「공통 점검은 빌드 가이드(스펙팩 6장)와 짝이다.
   가이드에 규칙을 더하면 여기에도 확인 항목을 더한다.」** 한쪽만 고치면 시킨 것과 보는 것이 갈린다.
   §7(눌러 보기)을 넣었으니 공통 점검에도 「거르는 단추가 목록을 줄이나」를 더해야 한다. **아직 안 했다.**

### ⭐⭐⭐ 팩 자체의 결함 셋 — 만드는 쪽 탓이 아니었다 (2026-08-18)

> 「너가 완성본을 안 만들어봐서 문제가 있다는건 안되는 일이야. 유저의 것을 너가
>  만들어 보진 않잖아... 우리는 스펙팩에서 최대한 오류없이 나오고, 검수할 수 있도록
>  완성된 지시서가 되어야해.」

맞는 말이다. 파는 것은 **스펙팩**이다. 세 번 만들어 보며 «만드는 쪽이 안 지켰다»고
적어 왔는데, 셋을 파 보니 **전부 팩이 안 시켰거나 틀리게 시킨 것**이었다.
그리고 답은 새로 만들 필요가 없었다 — **우리 판매팩 여덟 벌에 이미 다 있다.**

#### ① 화면 목록 — 우리 판매팩은 맞게 돼 있었다

판매팩 완성화면: `index.html` = **전체 화면 목록**, 136장 **전부**에 「화면 목록」 통로.
그런데 스펙팩은 파일 그림에 「index.html  전체 화면 목록」 한 줄만 적어 뒀다.
`index.html` 은 어디서나 «첫 화면»을 뜻하니 홈으로 읽혔고, 결과는 홈 복사본 + 목록 없음 + 통로 0개.

→ 이름을 바꾸지 않고(우리 판매팩과 어긋나므로) **«왜 목록이 첫 화면인지»를 적었다** —
   「이 폴더는 손님이 검수하는 **시안**이라 열자마자 몇 개를 만들었는지 보여야 한다」.
⛔ 한때 `화면목록.html` 로 가르려 했는데, 그러면 우리 판매팩 여덟 벌과 어긋난다. 되돌렸다.

#### ② 뒤로가기 계산이 3뎁스를 안 봤다 — 146개 중 67개가 틀렸다

상세 IA 는 **메뉴 → 화면(`screenGroup`) → 상태·탭(`pageName`)** 3뎁스인데,
`backTargets()` 는 `pageName` 안의 `" > "` 만 봤다. 실측:

```
screenGroup 이 있는 화면        146 / 146
pageName 에 " > " 가 있는 화면    0 / 146   ← 계산이 보던 것
```

그래서 **모든 화면이 «메뉴 첫 화면»으로 떨어졌다.** 예약 3단계에서 뒤로 가면
1단계가 아니라 예약 첫 화면으로 튀는 식이다. 고친 뒤 **67개가 바뀌었다**
(`PCBO1002: PCBO1000 → PCBO1001` 처럼). 만드는 쪽이 제 손으로 고쳐 쓴 것이 옳았다.

#### ③ 「화면 안에서 끝나는 동작」 칸이 비어 있었다 — 146개 전부

`acts` 칸은 2026-08-06 에 생겼다. 그때 주석에 이렇게 적혀 있다 —
「이 칸이 없던 동안 … 만들어진 사이트는 **눌러도 값이 안 바뀌는 포스터**가 됐다」.
그런데 **채우는 곳을 안 만들었다.** 146개 화면 전부 빈 채로 나갔고, 포스터가 다시 나왔다
(칩 있는 화면 68개가 색만 바뀜).

→ 새 데이터를 만들지 않고 **기능정의 글에서 뽑는다**(`화면안동작()`).
   「필터·정렬·탭·검색·체크박스·수량·펼치기·토글·페이지네이션」을 찾아
   **무엇이 일어나야 하는지**를 화면마다 적는다. 지금 89개 화면에 붙는다.

⚠ 칸을 만들고 채우지 않으면, 칸이 없는 것보다 나쁘다 — 있는 줄 알고 아무도 다시 안 본다.

#### 남은 것 (사장님이 짚으신 차례대로)

1. ~~화면 목록~~ · ~~플로우~~ · ~~화면 안 동작~~ — 위에서 고침
2. **디자인·레이아웃 완성도** — 사진 없는 자리 위에 글을 얹는 카드가 깨져 보인다(레이아웃 A)
3. **검수 시나리오 완성도** — 추가금 항목이다. 틀어지면 바로 클레임이다

#### 마스코트 그림이 세 갈래가 됐다 (2026-08-18 밤)

휴가 장면 열 장이 더 왔다 — 해변의자·해먹·수영장·발코니아침·들판소풍·호숫가캠핑·
노을산책·스파·바다카페·별보기. 투명본과 배경본 두 벌씩 왔고 **투명본만** 넣었다.

| 갈래 | 어디에 |
|---|---|
| 표정 10 | 큰 칸·커버 — 얼굴로 말한다 |
| 자세 13 | 작은 칸 — 실루엣으로 말한다 |
| 장면 10 | 커버·큰 칸만 |

⚠ **장면 그림은 작은 칸에 쓰지 않는다.** 파라솔·의자처럼 딸린 물건이 있어 가로로 넓다
  (비율 0.90~1.29 · 자세 그림은 0.66~0.77). 같은 높이면 폭을 두 배 가까이 먹어
  옆의 녹화 화면과 다툰다. 실제로 480px 로 얹어 견줘 보고 정했다.
⚠ 장면 그림은 «쉼»을 말한다 — 지금 AI팩 만드는 과정 편들과 결이 다르다.
⭐ 그래서 별명표에는 **표정·자세만** 이어 뒀다. 장면은 대본에 파일 이름을 그대로 적어 쓴다
  — 실수로 작은 칸에 뽑히지 않게.
⛔ 해먹 그림은 모서리에 점 하나가 남아 잘림이 안 됐다. 투명 잘라내기는 «한 줄에 여러 점»이
  있어야 내용으로 치게 해야 한다. 점 하나로도 통째 프레임이 된다.
### ⭐ 마스코트에 «표정»이 생겼다 · 마무리 자막을 키웠다 (2026-08-18 저녁)

#### 표정 열 장 — 자세로 말하던 것을 얼굴로도 말한다

`귀찮음 · 놀람 · 부끄러움 · 슬픔 · 신남 · 우울 · 조용함 · 졸림 · 행복 · 화남`
투명 배경으로 받아 `낱장/긴머리_*.png` 에 넣었다(투명 여백은 잘라서).

별명표를 이렇게 갈랐다 — **자세로 보이는 칸과 얼굴로 보이는 칸은 다르다.**

| 쓰임 | 무엇을 쓰나 |
|---|---|
| 작게 나오는 칸(`cat: cut`) | **자세** 그림 — 골똘히·노트북무릎처럼 실루엣이 다른 것 |
| 크게 나오는 칸(`cat: full`)·커버 | **표정** 그림 — 놀람·행복·신남 |

⚠ 표정 열 장은 전부 «정면으로 선» 같은 자세다. 작은 칸에 두 장을 나란히 쓰면 구분이 안 된다.

#### 마무리 자막(.cta) — 세 줄이 되면서 끊는 선을 넘고 있었다

사장님이 「무료 샘플 링크도 있어요.」 줄을 더하시자 46px 세 줄이 되어
**맨 아래가 1495 — 끊는 선(1430)을 65px 넘었다.** 인스타 UI 가 덮는 자리다.

→ 마무리 칸만 **아래에서부터 쌓아 올린다**(`bottom` 기준). 줄이 늘어도 위로 자라니 안 넘는다.
   그 덕에 글자도 키웠다(46 → 54px, 본문 자막과 같은 크기). 잰 결과 맨 아래 **1407** — 안 넘는다.
⚠ 자막을 위로 올리니 마스코트(z-index 3)가 첫 줄 윗부분을 덮어 잘려 보였다.
  가로 틀에서 이미 겪은 것과 같다 — `.cap` 에 `z-index: 6` 을 줬다.

#### 길이 규칙에 «사유를 적는» 예외를 만들었다

펫 유치원 편은 94.6초라 새 기준(68초)에 걸린다. 사장님이 그대로 두기로 하셨는데,
그 한마디로 규칙을 68 → 95 로 늘리면 **앞으로 만드는 모든 편이 길어져도 안 걸린다.**
→ 대본에 `"길이예외": "왜 예외인지"` 를 적으면 그 글을 찍고 넘어간다.
⛔ 빈 문자열은 안 통한다 — 사유를 안 적으면 예외가 아니라 그냥 구멍이다.

### ⭐ 마스코트를 또 바꿨다 — 긴머리 캐릭터 (2026-08-18)

> 「우리 마스코트를 다시 변경했어. ㅋㅋ 긴머리 사람 이 캐릭터야.」

검은 고양이(8/14) → **긴머리 캐릭터**. 까만 긴 생머리·앞머리, 크림색 얼굴,
**머리 위 주황 커피잔**, 짙은 회색 옷. 표정 없이 자세로만 말한다.

⭐ **대본은 한 줄도 안 고쳤다.** `_이미지/마스코트/낱장/_별명.csv` 만 바꿨다 —
옛 이름(`집중`·`똘망`·`애교` …)이 새 그림을 가리키게 이어 준다.
8/14에 이 구조를 만들어 둔 것이 두 번째로 값을 했다.

- 그림 13장 `낱장/긴머리_*.png` — 받은 정사각 PNG의 **투명 여백을 잘라** 넣었다.
  안 자르면 인물이 작게 붙어 뜬다(기존 대표_* 는 인물이 꽉 찬 세로 그림이다).
- 자세 이름은 «보이는 대로» 지었다 — `두팔위로`·`골똘히`·`인사`·`태블릿안기` …
- 옛 검은 고양이로 되돌리려면 별명표 오른쪽 칸을 `대표_*` 로 되돌린다. 파일은 그대로 있다.
- 가이드: `_마케팅/영상가이드.md` §5

⚠ 작게 나오는 칸(`cat: cut`)은 **실루엣이 다른 자세**끼리 섞는다. 「두 팔 위로」·「손 들어 인사」처럼.
  같은 서 있는 자세 둘을 쓰면 작아졌을 때 구분이 안 된다.

#### ⛔ 승인된 편을 다시 구울 때 — 순서가 있다

배포 편을 새 마스코트로 다시 구우려다 막혔다. `검수보내기` 는 **approved 인 편을 건드리지 않는다**
(사장님 판단을 지우지 않으려는 장치). 그런데 «승인된 것을 다시 굽는 것»이 지킴이가 하는 일이라,
**지킴이도 같은 자리에서 막히고 있었다** — 만들어 두고 그 경로로 안 돌려 봐서 몰랐다.

→ `sns지킴이.mts` 의 순서를 바꿨다. **되올리기 «전에» 검토대기로 돌린다.**
   지금 굽는 것은 더 이상 승인된 그 영상이 아니므로 검토대기가 맞는 상태이기도 하다.

⛔ **덮어쓰기 전에 반드시 검수기 자막과 견준다.** 이번에 18칸 중 **13칸을 사장님이 다시 쓰셨고**,
   그대로 `--덮어쓰기` 했으면 그 글이 통째로 날아갔다. 순서는 늘 이렇다 —
   **글가져오기 → 굽기 → 되올리기(--자막만).**

### ⭐⭐ 검수한 자막이 다음 편의 «본보기»가 된다 (2026-08-18)

> 「그럼 내가 검수한 내용들이 점점 토대가 되어 루틴에서 콘텐츠 만들 때 참고하게 되겠지?」

그렇게 되려면 **어딘가에 쌓여 있어야** 한다. 승인된 자막은 DB 안에만 있고,
다음 편을 쓰는 쪽은 그걸 안 읽는다. 그래서 파일로 뽑는다.

```bash
npx tsx "판매용_템플릿/_마케팅/_작업/본보기모으기.mts"
```
→ `_마케팅/본보기_자막.md` (git 안에 있다). `영상가이드.md` §2 가 이 파일을 가리킨다.

⚠ **손으로 고치지 않는다.** 고칠 것은 검수기에서 고치고 다시 돌린다 — 이 파일은 덮어써진다.
⚠ `waiting(검토대기)`는 안 담는다. 아직 안 보신 글은 본보기가 아니다.
⛔ **오타는 DB 에서 잡아야 한다.** 로컬 대본만 고치면 검수기에는 옛 글이 남고,
   그게 본보기로 굳어 다음 편에 그대로 옮겨진다. 8/18 에 「완성도를 높이도」가 실제로 그랬다.

### ⭐ 배포 편(내컴퓨터에서만)을 가이드대로 다시 만들었다 (2026-08-18)

옛판은 20칸 × 1.8초 = 36초 · **초당 10자**로 검사기에 두 군데 걸렸다.
「AI 전문 크리에이터가 정보를 전달하는 느낌」으로 다시 썼다 — 18칸 × 3.4초 = **61.2초**
(커버 2초 더해 63.2초) · **초당 7.9자** · 검사 통과.

**칸을 놓기 전에 녹화본을 1초 단위로 훑어 «무엇이 찍혀 있는지» 지도를 먼저 만들었다.**
그러지 않으면 자막이 없는 화면을 가리킨다. 부록이 보이는 구간은 **557~580초**뿐이다.

| 어떻게 | 왜 |
|---|---|
| 칸마다 `배속` 을 따로 (0.35~0.6) | 원본 23초를 61초에 펴야 한다. 한 값으로 묶으면 스크롤이 끊기거나 같은 화면에 네 칸이 머문다 |
| `ss` 를 **내용 경계**에 맞춤 | 일정 간격으로 놓으면 칸마다 뒤로 되감긴다 |
| 자막은 화면에 **찍힌 값**만 | 0~10장 · 3대 원인 · 심사 두 달 · 1~3분 — 전부 화면에 있다 |

⚠ 커버 포즈에 `굳이_` 를 쓰면 **옛 삼색 고양이**가 나온다(별명표에 없다). 본편은 `대표_*` 라
  한 영상 안에서 마스코트가 둘이 된다. → `대표_코딩` 으로 바꿨다.
⚠ 가로 오른쪽 기둥은 **네 줄**까지다. 다섯 줄이 되면 산출물 표 아래가 서명에 먹힌다.
  `ep` 가 이미 「사이트 배포 편」을 찍으므로 **제목에 편 이름을 또 넣지 않는다**.
⚠ `인트로설정_*.json` 의 `이름` 이 대본의 `이름` 과 **한 글자라도 다르면 커버가 안 붙는다**
  (조용히 「상단 띠 글만 갑니다」로 넘어간다). 8/18 에 `내컴퓨터에서만` ≠ `영상6_내컴퓨터에서만` 로 겪었다.
⚠ 캡션 **위 3~5줄**은 대본의 `"캡션머리"` 에서 온다(8/18에 붙였다). 비면 모든 편의 캡션이 똑같아진다.

### ⛔⛔⛔ 같은 영상이 «스물여섯 번» 올라갔다 — 적는 것이 늦었다 (2026-08-18 밤)

13시 19분부터 13시 32분까지 30초마다, 「사이트 내놓는 법」 편이 **13번씩 두 판**
(세로 13 · 가로 13) 유튜브에 올라갔다. 사장님이 다 지우셨다.

**왜 그랬나 — 한 줄이다. 「올린 뒤에 적었기 때문」이다.**

옛 차례는 이랬다:

```
유튜브에 올린다 → 설명을 넣는다 → 구글 드라이브에 복사한다 → 그제야 DB 에 ID 를 적는다
                                        ↑
                              G: 가 안 붙어 있어서 여기서 죽었다
```

드라이브 폴더(`G:/내 드라이브/…`)가 없으니 `mkdirSync` 가 던졌고, 그 줄에서 죽으니
**ID 를 적는 줄까지 못 갔다.** 다음 바퀴가 DB 를 보면 ID 칸이 비어 있다 →
「아직 안 올렸구나」 → 또 올린다. 30초마다, 열세 번.

**고친 원칙 — 밖으로 나간 일은 나가자마자 적는다.**

유튜브에 올리는 것은 «되돌릴 수 없는 일»이다. 되돌릴 수 없는 일을 하고 나서
적는 것을 미루면, 그 사이에 무엇이 터지든 **같은 일이 되풀이된다.**
드라이브 복사는 되돌릴 수 있는 «사본»일 뿐이라 실패해도 그냥 알리고 넘어간다.

| | 옛것 | 새것 |
|---|---|---|
| ID 적는 때 | 셋 다 끝난 뒤 한 번 | **올린 직후 각각** |
| 드라이브 실패 | 통째로 죽는다 | 알리고 넘어간다 (`try`) |
| 이미 올라간 편 | 세로 ID 만 보고 돌려보냄 | **세로·가로를 따로 본다** |

⚠ 마지막 줄이 중요하다. 세로만 성공하고 가로가 실패한 편을 「세로 ID 있네」로
  돌려보내면 **영영 가로 없이 등록완료**가 된다. 이제 한쪽씩 건너뛴다.

### ⛔⛔ 아무 데나 눌러도 화면이 덮였다 — LMS 두 팩 (2026-08-19 새벽)

사장님 크롬에 붙어 팩을 눌러 보다 잡았다. 빈 자리를 한 번 눌렀을 뿐인데
**그 화면 글자 전부**가 안내 문구로 튀어나와 화면을 통째로 덮었다. 3.2초 뒤 사라진다.

**까닭 — 이름 하나가 두 뜻으로 쓰였다.**

| 어디 | 무엇을 뜻하나 |
|---|---|
| `<body data-page="HO-01">` | 이 화면의 ID |
| `<button data-page="2">` | 목록 2쪽으로 |

```js
on('[data-page]', 'click', function (e, t) {   // ← closest 가 위로 올라가다
  ...                                        //    결국 body 를 잡는다
  toast(t.textContent.trim() + '페이지를 보고 있어요');  // t.textContent = 화면 글자 전부
});
```

`on()` 은 `document` 에 붙는 위임 처리라, **어디를 눌러도** `closest('[data-page]')` 가
body 를 잡는다. 게다가 `t.classList.add('on')` 이 body 에 붙고 있었다.

**고친 것** — `var box = t.closest('[data-pager]');` 바로 아래에 `if (!box) return;`.
쪽번호 단추는 반드시 `[data-pager]` 안에 있으니, 그 밖이면 아무 일도 안 한다.

고친 뒤 같은 자리를 다시 눌러 확인했다 — 빈 자리는 조용하고, 쪽번호 2 는
「2페이지를 보고 있어요」가 아래에 작게 뜬다. **둘 다 봐야 고친 것이다** —
덮이는 것만 없애고 쪽번호를 죽이면 고친 게 아니다.

### ⭐ 14개 팩을 전수로 훑었다 — 같은 모양이 또 있나 (2026-08-19)

눈으로만 보면 놓친다. **body 에 붙은 이름 ↔ 클릭을 잡는 이름**을 기계로 맞대 봤다.

| 팩 | 부딪힘 |
|---|---|
| **LMS 디럭스(44쪽)·프리미엄(132쪽)** | ⛔ `data-page` — 고쳤다 |
| 나머지 12개 팩 | 괜찮음 |

`data-page` 를 body 에 붙이는 것은 팩 열한 개가 같은데, **그 이름으로 클릭을 잡는 것은
LMS 둘뿐**이었다. 그래서 나머지는 멀쩡했다.

⭐ 기계로 거른 뒤 **14개 팩을 하나씩 열어 실제로 눌러 봤다.** 기계가 놓치는 것이 있다.

### ⭐ 사장님 크롬에 붙어 팩을 눌러 보는 법 (2026-08-19)

> 사장님: 「우리 완성화면을 크롬에서 열어 → 나는 확장으로 연결해 촬영 →
>  너는 그 화면에서 직접 눌러 보고 검수해」

**된다. 다만 걸림돌이 둘 있다.**

⛔ **`file://` 은 못 쓴다.** 크롬 확장이 `https://file://` 로 바꿔 버려 오류 화면이 뜬다.
  그래서 서버로 띄운다 — `.claude/launch.json` 의 **`팩전체` (4200)** 하나로 14개 팩을 다 본다.

```bash
npx --yes serve -l 4200 판매용_템플릿/_판매팩
```

⛔ **주소 끝에 빗금이 없으면 CSS 가 안 붙는다.** `serve` 가 `/index.html` 을 폴더로
  되돌리면서 끝 빗금을 떼는데, 그러면 `assets/css/base.css` 가 한 칸 위에서 찾아진다.
  민얼굴로 뜨면 팩이 깨진 게 아니라 **주소가 잘못된 것**이다.

```
http://localhost:4200/<팩>/완성화면/          ← 화면 목록 (빗금 필수)
http://localhost:4200/<팩>/완성화면/pages/<ID>
```

⭐ **좌표로 찍지 말고 이름으로 찾아 누른다** — `find` 로 `ref_N` 을 받아 그걸 누른다.
  화면이 바뀌어도 안 어긋난다. 좌표는 스크롤 한 번에 뜻이 달라진다.

⚠ **규모를 알고 시작한다.** 14개 팩 1,501쪽이다. 전부 눈으로 보려면 몇 시간짜리다.
  촬영용(누르기만)은 팩당 10~15분, 검수용(눈으로 보기)은 팩당 40분~1시간.

### ⭐ 주간 루틴이 «검수기까지»로 바뀌었다 · 영상 3건 → 4건 (2026-08-18 사장님 지시)

> 「영상을 일주일에 4개 정도는 올려야 할꺼 같아.. 루틴에 월요일 11시 영상을 4개
>  제작해서 검수기에 올리도록 지시해줘」

고친 것: `~/.claude/scheduled-tasks/weekly-sns-content/SKILL.md` (1,171줄)

**① 건수 3 → 4** — 화 21:00 / 목 09:00 / 금 18:00 / **일 11:00**

⚠ 3으로 줄였던 까닭이 그대로 산다. 「많이 올리기」가 아니라 「한 편이 끝까지 보이기」다.
  루틴에 이렇게 적어 뒀다 — **네 편을 다 못 만들 것 같으면 세 편을 «제대로» 만든다.**
  성기게 만든 네 편보다 잘 만든 세 편이 낫다.

**② 끝나는 자리가 바뀌었다 — 드라이브·유튜브 → 검수기**

루틴은 «검수기를 아예 모르고» 있었다. 옛 흐름 그대로 드라이브에 넣고 유튜브까지 올렸다.

```
옛것  루틴 → 드라이브 → 유튜브(비공개) → 사장님이 「공개」
새것  루틴 → 검수기 → 사장님이 「승인」 → 지킴이가 굽고 올린다 → 유튜브 + 드라이브
```

⛔ **루틴이 `유튜브올리기.mjs` 를 부르면 안 된다.** 지킴이는 올린 뒤 DB 에 ID 를 적는데,
  루틴이 따로 올리면 그 ID 를 아무도 모른다 → 지킴이가 「아직 안 올렸네」로 보고 또 올린다.
  스물여섯 번 올라간 그 모양이 된다.

**③ 보내기 전에 있어야 하는 것을 표로 박아 뒀다**

세로·가로 영상, 인트로 붙인 세로, **16:9 썸네일**, 대본의 **`캡션머리`**.
하나라도 없으면 무엇이 어떻게 빠지는지 같이 적었다 — 오늘 밤에 다 한 번씩 당한 것들이다.

⚠ 이 루틴은 **재료가 사장님 손에서 온다.** 녹화본이 안 늘면 네 편을 못 채운다.
  `node _작업/녹화본목록.mjs` 로 매주 «지난주에 없던 것»을 먼저 찾게 해 뒀다.

### ⛔⛔ 캡션은 «가져오지» 않고 있었다 — 사장님 글이 지워질 뻔했다 (2026-08-18 밤)

내가 「캡션머리가 비어서 다른 편과 똑같이 나갑니다」라고 말씀드렸는데 **틀렸다.**
사장님이 검수 화면에서 이미 다 쓰셨고, 나가는 글은 멀쩡했다.
내가 본 것은 **로컬 대본 JSON 의 빈 칸**이었다.

| | 캡션머리 |
|---|---|
| 검수기(DB) | 사장님이 쓰신 글 ✅ |
| 로컬 대본 JSON | **비어 있음** |

**왜 어긋났나 — `글가져오기.mts` 가 캡션을 안 가져왔다.**
자막·상단 띠·커버 글은 가져오는데 캡션만 빠져 있었다.

**왜 위험한가 —** 그 상태로 `검수보내기` 를 다시 돌리면 `캡션머리` 가 비었으니
**고정 블록만** 쓴다. 사장님이 쓰신 글이 «조용히» 지워진다.
검수보내기에 손댐 감지(`손댔나`)가 있어 막히긴 하지만, `--덮어쓰기` 한 번이면 넘어간다.

**고친 것** — `글가져오기.mts` 가 캡션머리도 되가져온다.
고정 블록은 늘 「📍」로 시작하므로 **그 앞을 머리로 본다.**

⚠ 「📍」를 못 찾으면 **아무것도 안 건드린다.** 틀이 바뀐 것일 수 있는데,
  그때 통째로 머리에 넣으면 고정 블록이 대본에 박혀 두 번 나간다.

**되가져온 값** (8/18 밤, 자막은 두 편 다 0칸 달라짐 — 다시 안 구워도 된다):

```
펫유치원      3줄 · 109자
사이트 배포   6줄 · 226자   (대본에 있던 421자는 옛 글이었다)
```

⭐ **검수기와 로컬이 다르면 «검수기가 맞다».** 사장님이 손으로 쓰신 쪽이다.
  굽기 전에 `글가져오기` 를 먼저 돌린다 — 손으로 옮겨 적으면 반드시 어긋난다.

### ⛔ 썸네일이 «11시간 묵은 채»로 붙고 있었다 (2026-08-18 밤)

사장님: 「반려동물 유치원 편 가로 버전 썸네일에 예전 캐릭터가 들어가 있고」 — 맞았다.

| | 고친 때 |
|---|---|
| `_별명.csv` (마스코트 이어 주는 표) | **20:03** |
| 구워 둔 썸네일 | **09:07** ← 11시간 먼저 |

`썸네일굽기.mjs` 는 `_별명.csv` 를 **굽는 그때** 읽는다. 그러니 별명표를 고쳐도
**이미 구워 둔 PNG 는 안 바뀐다.** 옛 얼굴이 그대로 붙는다.

⭐ **마스코트를 바꾸면 썸네일도 다시 굽는다.** 영상만 다시 구우면 반만 바뀐다.

```bash
node "판매용_템플릿/_마케팅/_작업/썸네일굽기.mjs"
```

### ⛔ 오른쪽 위(`tr`) 자리에서 마스코트 머리가 잘렸다 (2026-08-18 밤)

긴머리로 바꾸고 나서 「사이트 내놓는 법」 썸네일의 **머리 위 커피잔이 위로 잘리고**
오른쪽 위 로고와 겹쳤다. 옛 삼색 고양이는 그림 둘레에 빈 자리가 있어 안 걸렸는데,
긴머리 그림은 둘레를 **바짝 잘라 놔서** 같은 값이 다른 결과를 냈다.

`썸네일틀.html` 의 `.face.cat.tr` — `top: 36px` → **118px**, 높이 `330` → **250**.
머리띠 줄이 y 96 에서 끝나고 산출물 표가 y 380 에서 시작하므로 그 사이에 넣는다.

⚠ **그림을 갈아 끼우면 자리값도 다시 봐야 한다.** 같은 CSS 라도 그림 둘레가
  다르면 다른 데 놓인다. 굽고 나서 **눈으로 본다** — 값만 보면 안 보인다.

### ⛔⛔ 「등록완료」인데 유튜브에 없었다 — 조용히 빠지는 구멍 (2026-08-18 밤)

올린 것을 되읽어 확인까지 마쳤는데(비공개·설명 846자·썸네일 maxres), 10분쯤 뒤에
보니 **유튜브에서 사라져 있었다.** 검수기에는 「등록완료」로 남아 있었다.

이게 왜 나쁜가 — 지킴이는 **ID 가 적혀 있으면 「이미 올렸네」 하고 건너뛴다.**
한 번 사라지면 **영영 안 올라간다.** 아무도 모르게.

**막는 법 — `유튜브있나.mjs` + 지킴이의 `사라진것확인()`**

한 바퀴마다 「등록완료」인 편의 ID 가 유튜브에 아직 있는지 되묻는다.
없으면 **큰 소리로 알린다.**

⭐ **찾기만 하고 «다시 안 올린다».** 저절로 다시 올리게 만들면,
  지우는 쪽이 유튜브면 「올린다 → 지워진다 → 또 올린다」로 끝없이 돈다.
  같은 날 낮에 스물여섯 번 올라간 것이 정확히 그 모양이었다.
  다시 올리려면 사람이 검수기에서 **승인으로 되돌린다.**

⚠ **못 물어본 것을 「없음」이라고 말하지 않는다.** 토큰이 죽었거나 HTTP 가 깨졌을 때
  「없다」고 하면 멀쩡한 영상을 지워진 것으로 본다. 그럴 땐 그냥 넘어간다.
⚠ **방금 올린 것(5분 안)은 안 묻는다.** 유튜브가 목록에 넣기까지 몇 분 걸린다.

✅ **까닭은 밝혀졌다 — 사장님이 지우셨다.** (2026-08-18 밤 확인)
   유튜브가 지운 것이 아니고, 우리 코드가 지운 것도 아니다
   (`DELETE` 로 뒤져 봤다 — 유튜브를 지우는 곳은 한 군데도 없다).
   낮에 스물여섯 번 겹쳐 올라간 것을 치우시던 참이라 새로 올라온 것까지 같이 지워졌다.

   ⭐ 그래도 이 확인은 남긴다. **사람이 지워도 검수기는 「등록완료」로 남기 때문이다.**
     지운 쪽이 누구든 「적힌 것과 실제가 다르다」는 사실은 똑같이 위험하다.

### ⛔ 지킴이가 올린 것에는 «썸네일이 없었다» (2026-08-18 밤)

사장님: 「16:9 썸네일이 같이 안올라오는거 같아」 — 맞았다.

`유튜브올리기.mjs` 는 네 번째 인자로 썸네일을 받는데, 지킴이는 **세 개만 넘기고 있었다.**
손으로 올릴 때는 붙고 지킴이가 올리면 안 붙는 이유가 이것이다.

이름이 딱 안 맞는 것도 걸림돌이었다 — 검수기 이름표는 `펫유치원_컷편집` 인데
구운 썸네일은 `썸네일_영상5_펫유치원_16_9.png` 다. 그래서 `썸네일찾기()` 가
양쪽에서 군더더기(`썸네일_`·`영상N_`·`_16_9`·`_컷편집`)를 떼고 **알맹이로** 맞춘다.

⭐ 썸네일은 **가로에만** 붙인다. 유튜브는 9:16 을 커스텀 썸네일로 안 받는다
  (200 을 주고 조용히 버린다 — 2026-08-10 에 당했다).
⭐ 붙였는지 **되읽어 확인한다**: `node _작업/유튜브확인.mjs <ID>` 가 `maxres` 를 보이면 붙은 것이다.

**8/18 밤 확인한 값** (배포 편):

```
세로 5J_smAkfWe0  private ✅  설명 846자 ✅  썸네일 자동 (쇼츠는 원래 이렇다)
가로 mrYrRIirADU  private ✅  설명 846자 ✅  썸네일 maxres ✅ 커스텀
검수기 상태 published (등록완료) ✅
```

⚠ 이 파일을 **스크립트로 고칠 때 역슬래시가 한 겹씩 먹힌다.** 8/18 에 세 번 당했다
  (`\d` → `d`, `[\s_]` → `[s_]`, `\.png` → `.png` — tsc 는 통과한다, 뜻만 바뀐다).
  그래서 `썸네일찾기()` 와 `아이디()` 는 **정규식 없이** `startsWith`·`indexOf`·`slice` 로만 썼다.
  경로에서 파일 이름만 뗄 때도 `basename` 을 쓴다.

### ⭐⭐⭐ SNS 한 바퀴가 이어졌다 — 단추만 누르면 된다 (2026-08-18 사장님 설계)

> 「검수기에서 검수하고 검수완료를 누른다. 이때 어디로 이동될 수 있을까?
>  내가 따로 방에서 말해야 할까?」

**말씀 안 하셔도 된다.** 웹은 클로드코드를 못 부르지만, 「승인된 것을 다시 굽는다」는
**정해진 일**이라 클로드가 필요 없다 — `_작업/sns지킴이.mts` 가 한다.

```
검토대기 ──[검토 완료]──▶ 제작중 ──(지킴이가 다시 굽는다)──▶ 검토대기
                                                         …여러 번 돈다…
검토대기 ──[최종 완료]──▶ 등록 중 ──(지킴이가 올린다)──▶ 올림(비공개)
```

| 상태 | 배지 | 지킴이가 하는 일 |
|---|---|---|
| `waiting` | 검토대기 | (기다린다) |
| `approved` | 제작중 | 검수기 글 가져오기 → 자막검사 → 굽기 → 커버 → 되올리기 → `waiting` |
| `final` | 등록 중 | 유튜브(비공개) + 드라이브 → `published` |
| `published` | 올림(비공개) | (끝) |

```bash
npx tsx "판매용_템플릿/_마케팅/_작업/sns지킴이.mts" --지킴이
```
`--시늉` 을 붙이면 **무엇을 할지만 찍고 아무것도 안 한다.** 붙이기 전에 이걸로 본다.

⛔ **바깥으로 나가는 일은 `final` 하나에만 걸려 있다.** `approved` 는 다시 굽기까지다.
⛔ **유튜브 ID 가 이미 적힌 것은 두 번 안 올린다.** 같은 영상이 두 개 생기는 것이 제일 잦은 사고다.
⚠ 실패하면 **상태를 그대로 둔다.** 조용히 `waiting` 으로 돌리면 «안 만들어진 것»이 검토대기로 보인다.
⚠ 어느 대본에서 왔는지는 `script_path`·`intro_path` 에 적힌다(0018). 이름으로 짐작하지 않는다.

#### 검수기에서 «영상»을 돌려 본다 (2026-08-18)

> 「검수기에서 너가 만든 영상을 봐야 하는데 그게 될까?」

된다. `/admin/sns/<id>/video?판=916|169` 가 구운 mp4 를 흘려 준다. 세로·가로를 단추로 바꾼다.
⚠ **`localhost` 로 열 때만 재생된다** — 영상은 이 컴퓨터의 `%TEMP%` 에 있다.
배포된 주소에서는 칸별 그림과 자막만 보인다. 폰에서도 보고 싶어지면 그때 블롭 저장소로 올린다.
⚠ 아무 파일이나 못 읽게 «구운 영상 폴더 안»만 연다.

### ⛔ 가로판 그라데이션이 «흰색»으로 구워지고 있었다 (2026-08-18)

> 「아래 그라데이션 영역이 틀어진거 같고, 마지막에 …자막이 없어」

둘 다 가로판(16:9) 버그였다.

**① 그라데이션** — 앞장(layer=front)을 투명으로 굽는데(`--default-background-color=00000000`),
크롬이 **투명 바탕 위의 반투명 검정을 «알파는 맞게, RGB 는 흰색으로»** 쓴다.
그래서 어둠이 깔려야 할 자리가 뿌옇게 나왔다. 앞장 PNG 의 **알파 채널만 따로 떠 보고** 알아냈다:
```bash
ffmpeg -v error -i <앞장.png> -vf "format=rgba,alphaextract" 알파.png -y
```
→ 바탕을 **불투명한 어둠**으로 두고 흐려지는 것은 `mask-image` 로 낸다. RGB 가 안 뒤집힌다.
⚠ **앞장에 반투명 색을 쓰지 마라.** 쓸 것이면 mask 로 낸다.

**② 마지막 자막이 사라짐** — 마무리 칸은 고양이가 영상 칸을 통째로 쓴다(`.catfull`, z-index 4).
가로판은 자막이 «영상 안»이라 고양이가 자막을 덮었다. `.cap` 을 z-index 6 으로 올렸다.
(세로판은 자막이 영상 «밖»이라 이 일이 안 났다 — 그래서 여태 안 보였다.)

### ⭐ 세로판 — 영상을 제목과 자막 «가운데»로, 바탕은 어둡게 (2026-08-18)

영상 칸을 838 로 키웠더니 제목 아래 **8px** 밖에 안 남아 붙어 보였다.
제목 아래 끝을 재니(382) 쓸 수 있는 공간이 907px, 영상이 838px 이라 **남는 틈이 69px 뿐**이었다.
절반씩 나눠 **vid-top 416 · cap-gap 34** 로 두었다. 416+838+34+141 = 1429 (끊는 선 1430).
⚠ 영상을 이보다 키우면 제목이나 자막을 덮는다. **여유가 1px 이다.**

**바탕을 `#20261C` 로 바꿨다** (사장님 제안).
> 「SNS 배경이 우리 사이트랑 같다 보니까 GNB 영역이 조금 이상해 보이지 않아?」

맞는 지적이었다. 녹화본 속 우리 앱 바탕이 종이색(#EAE8DE)이라 틀과 똑같아서 영상 칸의
경계가 사라졌다. `#20261C` 는 우리 **글자색(--ink)** 이라, 틀 맨 위에서 `--paper` 와 `--ink` 를
맞바꾸는 것으로 끝났다 — **아래 규칙은 한 줄도 안 고쳤다. 되돌리려면 그 덩어리만 지운다.**
⚠ `--tan` 과 `--teal-text` 는 어두운 바탕에서 안 읽혀 밝은 값으로 같이 바꿨다.
⚠ 아직 **세로판만** 어둡다. 가로 틀·커버(썸네일틀)는 종이색 그대로다 — 사장님이 보시고 정하실 몫.

⚠⚠ **이 절을 쓰다가 백틱 사고를 또 냈다.** 바로 위에 적어 두고도 `node -e "…"` 로 넣었다.
**여러 줄 글은 Edit 도구로 넣는다.** 셸을 거치면 백틱 안쪽이 명령으로 실행되어 통째로 빈다.

---
### ⛔⛔ 세로판이 녹화본 위아래를 «17.6%» 잘라내고 있었다 (2026-08-17 사장님 지적)

> 「우리가 약속한 비율이 있어서 맞춰서 녹화를 했는데 **위아래가 왜 짤리지?**」
> 「0810주차에 작업한거 비율이랑 비교해 보면 **화면 영역 세로가 좀 짧은거 같아**」

**둘 다 맞았다. 사장님은 약속대로 찍으셨고, 어긋난 것은 우리 쪽이었다.**

| | 녹화본 2784×2160 (1.2889) |
|---|---|
| 가로판 | 좌우 16px(0.6%)만 — 사실상 그대로 ✅ |
| 세로판 (전) | **위아래 381px = 17.6% 잘라냄** ❌ |

#### 까닭 — 틀이 아니라 «굽는 쪽»이 잘랐다

`영상틀_916.html` 은 **원래 안 자르게** 짜여 있다:
```
--vid-h = min(--vid-max-h, 1080/ratio)
--vid-w = min(1080, vid-h*ratio)        ← 「어느 쪽이든 잘라내지 않는다」
```
그런데 `영상굽기.mjs` 가 틀에 넘기던 `ratio` 가 **녹화본 비율이 아니라 «칸 비율»(1.5652)** 이었다.
그러니 틀은 늘 1080×690 칸을 뚫었고, 굽는 쪽은 녹화본을 거기 맞춰 잘랐다.

#### 고친 것

| 값 | 전 | 지금 |
|---|---|---|
| `--vid-top` / `세로위` | 500 | **390** |
| `--vid-max-h` / `세로최대` | 690 | **840** |
| `--cap-gap` / 틈 | 88 | **60** |
| `--vid-w` / 칸 폭 | 비율대로 (남을 때가 있었다) | **늘 1080 꽉** |
| 틀에 넘기는 `ratio` | 칸 비율 1.5652 (고정) | **녹화본 비율** |

1.28 녹화본이면 칸이 **1080×838** 이 된다 — **위아래 잘림 0px · 좌우 여백 0px.**
화면 칸이 690 → 838 로 **148px 커졌다.**

⚠ **좌우는 늘 1080 꽉이다** (2026-08-17 사장님: 「어떤건 좌우 여백을 비우고 어떤건 안비우고
들쑥날쑥이야」). 세로만 비율대로 길어진다. 1.286 보다 «납작한» 녹화본은 그때만 위아래가 잘린다.

⚠ **390 + 838 + 60 = 1288 · 자막 두 줄 끝 1429** (끊는 선 1430, 재 보니 1415).
이 네 값(`세로위`·`세로최대`·`틈`·칸 폭)과 틀의 `--vid-top`·`--vid-max-h`·`--cap-gap`·`--vid-w` 는
**늘 같이** 고친다. 하나만 고치면 자막이 잘리거나 영상이 제목을 덮는다.
⚠ 여유가 **15px 뿐이다.** 자막을 키우거나 제목을 세 줄로 만들면 바로 넘친다.

⚠ 굽고 나면 첫 줄에 찍힌다 — `녹화본 2784×2160 → 세로 칸 1018×790 · 위아래 잘림 0px`.
**이 줄을 보고 넘어간다.**

### ⛔ 짧은 순간은 «칸 한가운데»에 안 온다 — 느리게 돌린다 (2026-08-17)

사장님 컷편집본은 홈에서 「AI팩 만들기」를 누르는 장면이 **0.55초**뿐이다. 한 칸이 2.9초인데
검수기는 «칸 한가운데»(1.45초 지점) 프레임을 뜬다. 그래서 **영상에는 있는데 검수기 그림에는 없었다.**
사장님: 「여기 화면씬이 다르다니까?」 · 「너무 빠르게 지나가서 캡쳐에서 빠진거야?」 — 그렇다.

→ `"배속"` 이 이제 **1 아래로도 간다**(0.2~8). `"배속": 0.3` 이면 원본 0.87초를 2.9초에 늘린다.
가운데가 0.435초라 클릭이 그림에도 잡히고, 화면에서도 그 대목이 1.8초쯤 머문다.

### ⛔⛔⛔ 검수 저장이 «조용히 아무것도 안 하고» 성공했다 — 자막을 통째로 잃었다 (2026-08-17)

**이것부터 읽는다. 같은 꼴이면 또 잃는다.**

사장님이 검수 화면에서 제목·커버·캡션·자막을 다 고쳐 넣으시고 저장을 누르셨다.
제목·커버·캡션은 들어갔는데 **자막 22칸이 통째로 사라졌다.** 되돌릴 곳도 없었다 —
**DB 에 한 번도 안 쓰였기 때문이다.**

#### 까닭은 «열쇠»였다

화면은 저장할 때 **칸 row id** 로 보냈다. 그런데 로컬의 `검수보내기.mts` 가 다시 돌 때
`delete` 하고 `insert` 해서 **id 가 통째로 바뀐다.** 그러면

```
update sns_cut set caption_json = … where id = <옛 id>
```

가 **0줄을 고치고 «성공»으로 돌아온다.** 오류도 경고도 없다.
그 사이에 내가 검수보내기를 **세 번** 돌렸다. 사장님은 그동안 화면을 열어 두고 쓰고 계셨다.

#### 고친 것 넷

| 무엇 | 어떻게 |
|---|---|
| **열쇠를 `ord`(1..N)로** | 다시 보내도 안 바뀌는 값이라야 열쇠다. 못 찾은 칸이 하나라도 있으면 「저장했다」고 하지 않는다 |
| **칸을 지우지 않는다** | `ord` 로 고쳐 넣는다(`onConflictDoUpdate`). 줄어든 꼬리만 지운다 |
| **사장님이 손댄 뒤면 멈춘다** | `검수보내기` 가 «마지막 보낸 때»보다 `updatedAt` 이 늦으면 **안 보내고 멈춘다**. 정말 덮어쓸 때만 `--덮어쓰기` |
| ⭐ **`--자막만`** | 영상·프레임·자막만 갈고 **제목·커버 글·캡션·올릴때는 그대로 둔다.** 사장님이 글을 쓰신 뒤 내가 자막만 다시 써야 할 때 이 길로 간다 |

#### ⭐ 글은 «검수기 → 로컬»로 가져온다 — `_작업/글가져오기.mts`

상단 띠와 커버 글은 **구울 때 그림에 박힌다.** 사장님은 검수 화면에서 글을 고치시고
나는 로컬에서 굽는다. 로컬 파일이 옛 글이면 **DB 에는 새 글, 그림에는 옛 글**이 박힌 채로 나간다.
2026-08-17 에 실제로 그렇게 보냈다.

```bash
npx tsx "판매용_템플릿/_마케팅/_작업/글가져오기.mts" <대본.json> <인트로설정.json> <회차> <이름>
```

**굽기 전에 이걸 먼저 돌린다.** 손으로 옮겨 적으면 반드시 어긋난다.

⚠ 커버 글은 **한 줄에 여덟 자 안팎**이다. 사장님 글을 그대로 넣었더니 4줄이 5줄로 넘쳐
`영상인트로.mjs` 가 멈췄다 — **멈추는 게 맞다.** 넘친 채로 구우면 목록 칸과 고양이가 밀린다.
⚠ 커버 오른쪽 위에 `ep`(「반려동물 유치원 편」)가 이미 찍힌다. 커버 카피에 업종을 또 넣으면 겹친다.

⚠ **열쇠는 «안 바뀌는 것»이어야 한다.** row id 는 다시 넣으면 바뀐다.
⚠ **0줄을 고치고 성공으로 돌아오는 갱신을 그냥 두지 마라.** 세어서 확인한다.
⚠ **사장님이 화면을 보고 계실 수 있다.** 루틴이 같은 줄을 다시 쓰기 전에 «손댄 흔적»을 먼저 본다.

**새 도구 — `_작업/자막속도.mts`**: 검수기에 든 지금 자막을 칸마다 재서 «초당 몇 자»를 보여준다.
편 평균이 괜찮아도 어떤 칸은 두 배로 빠를 수 있고, 사람은 그 칸에서 놓친다.

---

### ⭐⭐⭐ 자막을 «두 배로» 몰아넣고 있었다 (2026-08-17 저녁, 사장님 지적)

**이 절이 오늘 제일 중요하다. 아래 「배속 재매핑」 절보다 먼저 읽는다.**

> 「길이의 문제가 아니고 제목과, 내용, 그리고 읽히는 자막이 중요해.
> 우리는 예쁜 영상을 보여주는 쇼츠가 아니라 **정보를 전달하는 영상**이기 때문에…
> **영상 시간 때문에 자막이 이해 안 되게 쓰이는 부분들이 있는 거 같아.**」

#### 우리 최고 성적 편을 열어 재 봤더니 우리가 반대로 하고 있었다

`릴스영상/4. 여행사이트 144개화면/자막_여행_컷편집.srt` — **92.6초 · 유지율 40% · 조회 1,625.**
우리 롱폼 최고 성적이다. 그 편을 세어 보니:

| | 여행 편 (잘된 것) | 우리가 쓰던 규칙 |
|---|---|---|
| 자막 한 칸 | **2.99초** | 1.8초 |
| 칸당 글자 | **13.0자** | 15.6자 |
| **초당** | **4.4자** | **8.7자** |

**읽는 속도가 딱 두 배였다.** 40초 안에 우겨넣으려고 칸을 1.8초로 쪼개고 글자는 더 넣었다.
「자막이 이해가 안 된다」는 말의 뿌리가 여기다 — 낱말이 어려운 게 아니라 **읽을 시간이 없었다.**

⚠ 「40초 넘기지 마라」의 근거였던 표도 다시 읽으면 반대로 읽힌다 —
**93초 1,625회 vs 103초 610회.** 길이는 10초 차이인데 조회수가 2.7배 갈렸다. **길이가 아니라 내용이었다.**

#### 검사기를 고쳤다 (`lib/sns-caption-rules.ts`)

| 규칙 | 전 | 지금 |
|---|---|---|
| 길이 | 25~40초 | **25~95초** |
| 자막 분량 | 절대 300~360자 | **초당 3.5~5.5자** (길이가 바뀌어도 뜻이 산다) |
| 칸 길이 | (없음) | **2.4초 이상** |

⚠ **`대본_내컴퓨터에서만.json` 이 이 규칙에 걸린다** (초당 10자 · 칸 1.8초). 아직 안 올렸으니
새 규칙으로 다시 쓴다. **규칙이 나가기 전에 잡았다** — 그게 검사기를 둔 까닭이다.

#### 사장님 컷편집본으로 다시 만들었다 — 63.8초

`릴스영상/12. AI팩 만드는 과정/1. AI팩 제작_펫유치원.mp4` (62.3초, 2784×2160)

- 자막 **22칸 × 2.9초 = 63.8초** · 공백 제외 310자 · **초당 4.9자**
- 인트로 2초를 붙이면 완성 **65.8초**
- 말투는 여행 편을 그대로 따랐다 — 1인칭 회고체, 한 칸 한 생각, 숫자는 한 칸에 하나,
  가운데에 감정 한 줄(「저는 여기가 제일 좋더라고요」), 끝에 처음을 다시 묶는 한 줄.
- 제목 후보 셋과 캡션은 `_작업/올릴글_펫유치원.md` 에 있다. 캡션은 900자쯤으로 **자세히** 썼다 —
  영상에서 스쳐 간 숫자(요건 127개·이동 25개)를 캡션에서 다시 푼다.

⭐ **화면이 이미 빠르니 자막을 쪼갤 이유가 없다.** 재 보니 사장님 컷편집은 움직이는 프레임이
**92%** 다(여행 기준 67%). **속도는 화면이 내고, 자막은 읽히게 둔다.** 이 한 줄이 이 편의 규칙이다.

재는 법:
```bash
ffmpeg -v error -stats -ss 8 -t 24 -i <영상> \
  -vf "scale=320:-1,mpdecimate=hi=200:lo=100:frac=0.1" -an -f null -
```

⚠ **칸1만 원본 녹화에서 가져왔다.** 컷편집본의 「컨셉 한 줄 타이핑」이 1.3초뿐이라
2.9초 칸의 가운데(`ss+1.45`)에 못 온다. 원본은 10~19초가 통째로 그 타이핑이라 배속 3으로 넣었다.
**한 칸이 한 파일만 봐야 하는 것은 아니다** — 컷마다 `clip` 을 따로 적을 수 있다.

#### ⭐ 검수기에 «커버»가 들어갔다 — 커버 글과 상단 띠는 이제 따로다 (2026-08-17)

> 「커버도 만들어서 검수기에 넣어줘. **커버 카피 수정하는 인풋, 영상 상단 수정하는 인풋 영역**도 추가해서.」

둘이 같은 값을 쓰고 있었는데 그럴 까닭이 없었다. **커버는 맨 앞 2초에만** 나오니 짧고 세야 하고,
**상단 띠는 63.8초 내내** 떠 있다. `sns_content` 에 `cover_title` · `cover_data_uri` 를 더해 갈랐다(**0015, 적용함**).

| 어디 | 어디서 오나 | 무엇 |
|---|---|---|
| 커버 카피 | `인트로설정_*.json` 의 `title` | 맨 앞 2초 표지 |
| 상단 띠 | 대본의 `세로제목`·`가로제목` | 영상 내내 화면 위 |

- 검수 화면에 **「커버와 상단 띠」 절**을 새로 뒀다. **글 옆에 그림을 붙였다** —
  글만 고치면 줄이 넘쳤는지 모른다. 커버는 구운 인트로의 1초 지점, 띠는 첫 칸 프레임을 쓴다.
- `검수보내기.mts` 가 3번째 인자로 **인트로설정 경로**를 받는다(안 주면 대본 옆에서 `이름` 으로 찾는다).
  ⚠ **`영상인트로.mjs` 를 먼저 돌려야** 커버 그림이 들어간다. 안 돌리면 글만 가고 자리는 빈다.
- **커버 글도 자막 규칙을 탄다** — 손님이 읽는 글이라 금지어가 새면 안 된다.
  `자막검사.mts` 도 인트로설정의 `title` 을 대본에 붙여 함께 검사한다.

⚠ **/admin/sns 는 로그인 벽 뒤라 내가 눈으로 못 본다.** 타입 검사와 DB 값으로만 확인했다.
화면이 어긋나 보이면 말씀해 주시면 고친다. 무엇이 들어 있는지는 `검수목록.mts` 로 본다.

#### ⛔ 「검토 완료」를 눌러도 «아무도 안 집어 간다» (2026-08-18)

사장님: 「검토 완료를 눌렀어. 내가 여기에 다시 얘기해야해? 아니면 어디선가 작업을 하나?」

**아무 데서도 안 한다.** `actions.ts` 주석에는 「굽기·올리기는 로컬이 `_작업/sns올리기.mts` 로
집어 간다」고 적혀 있는데 **그 파일이 없다.** 주간 SNS 루틴도 승인 상태를 안 본다.
→ 지금은 사장님이 말씀해 주셔야 이어진다. (`sns올리기.mts` 는 아직 밀린 일이다)

⚠ 더 큰 것 — **검토 완료는 「이 글로 만들어 달라」는 뜻이다.** 사장님이 검수 화면에서
22칸을 통째로 다시 쓰셨는데(320자 → 463자) 구워 둔 mp4 에는 내 옛 자막이 들어 있었다.
**승인 뒤에는 반드시 `글가져오기.mts` 로 끌어와 다시 굽는다.**
`글가져오기.mts` 가 이제 제목·커버뿐 아니라 **자막 22칸도 가져온다**(칸 수가 다르면 멈춘다).

⚠ **여러 줄 글을 `node -e "…"` 로 넣지 마라.** 백틱이 셸에 먹혀 그 대목이 통째로 빈다 —
2026-08-18 에 이 절을 그렇게 넣었다가 낱말이 다섯 군데 사라졌다. Edit 도구나 파일로 넣는다.
(2026-08-14 의 「정규식이 든 코드를 셸 한 줄로 넣지 마라」와 같은 뿌리다.)

#### ⭐ 글이 늘면 «영상을 늘린다» — 글을 깎지 않는다 (2026-08-18)

463자를 63.8초에 넣으면 초당 7.3자다. 22칸 중 19칸이 5.5자를 넘었다.
사장님 선택은 **칸초 2.9 → 4.3초, 94.6초**. 초당 4.9자로 들어왔고 여행 편(92.6초)과 같은 길이다.

⚠ 칸초를 바꾸면 **가운데 초가 밀린다.** 배속 1 칸은 `ss` 를 `(새칸초 − 옛칸초) ÷ 2` 만큼 당긴다
(2.9→4.3 이면 0.7초). 느리게 도는 칸1 은 배속을 다시 잡는다(0.3 → 0.2).
⚠ 자막은 **두 줄까지**다. 한 줄이 18자를 넘으면 세 줄로 접혀 끊는 선을 넘는다 —
글자를 빼지 말고 **줄 나누는 자리를 옮긴다.**

#### ✅ 올렸다 — 유튜브(비공개) · 드라이브 (2026-08-18)

사장님 지시: 「유튜브와 구글드라이브에 영상파일만 올려줘. 썸네일 x, 캡션 x, 영상만」

| | |
|---|---|
| 세로(쇼츠) 96.6초 | `lGR5Mv3nx08` — https://studio.youtube.com/video/lGR5Mv3nx08/edit |
| 가로 94.6초 | `SH8XrKP8VbM` — https://studio.youtube.com/video/SH8XrKP8VbM/edit |
| 드라이브 | `4주차_2026-08-31/{세로,가로}_AI팩으로 만드니 146개 화면이 나왔어요.mp4` |

**둘 다 비공개다.** 공개 전환은 사장님이 예약 시각에 직접 하신다.

⚠ 「썸네일 x, 캡션 x」는 **드라이브에만** 해당했다. 유튜브에는 둘 다 넣는다 —
되읽어 확인한 값: 둘 다 `private` · 설명 711자 · 썸네일 `maxres`(커스텀).

#### ⭐ 올린 뒤 «설명을 고치려면» 권한이 한 단계 더 필요하다 (2026-08-18)

설명을 비워 두고 올린 뒤 채우려니 **403 Insufficient Permission** 이 왔다.
`videos.update` 는 `youtube.upload` 로는 안 되고 **`youtube.force-ssl`** 이 있어야 한다.

- `유튜브올리기.mjs` 의 scope 목록에 그 줄을 더했다.
- ⚠ **줄만 고쳐서는 안 넓어진다.** 이미 받아 둔 `refresh_token` 은 옛 권한 그대로다 —
  `node _작업/유튜브올리기.mjs --로그인` 을 한 번 다시 돌려 사장님이 승인해야 한다. (2026-08-18에 했다)
- ⚠ 그 로그인은 **8788 포트**를 쓴다. 옛 시도가 물고 있으면 `EADDRINUSE` 로 죽는다 — 그 프로세스를 먼저 끈다.

**새 도구 둘**
| 파일 | 무엇 |
|---|---|
| `_작업/유튜브설명고치기.mjs <영상ID> <설명.txt>` | 올린 영상의 **설명만** 갈아 끼운다. 제목·공개상태는 안 건드린다 |
| `_작업/유튜브확인.mjs <영상ID…>` | 공개상태·설명 길이·썸네일을 **되읽어** 본다. 「보냈다」와 「붙었다」는 다르다 |

⚠ `videos.update` 는 snippet 을 **통째로 덮는다.** 지금 값을 먼저 읽어 제목·카테고리를 지켜야 한다.

#### ⚠ 썸네일 문구·고양이가 옛것이었다 (2026-08-18)

`썸네일굽기.mjs` 의 목록은 **손으로 적은 값**이라 영상과 따로 논다. 펫유치원 칸이
「한 줄 적었더니 / 10분 만에」(옛 카피)였고 포즈가 `커피_충전후집중` — 별명표에 없어
**옛 삼색 고양이**로 떨어졌다. 커버는 대표 마스코트인데 썸네일만 옛 고양이였다.
→ 영상 커버와 같은 글로 맞추고 포즈를 `집중`(→대표_집중)으로 바꿨다.
**썸네일을 걸기 전에 커버와 나란히 놓고 본다.**

⚠ 드라이브 4주차 폴더에 **옛 파일(`*_펫 유치원 사이트 10분 만에.*`)이 아직 있다.**
40초판 시절 것이라 이번 것과 다르다 — 사장님이 정리하실 몫으로 남겼다.
⚠ 검수기 상태를 `published` 로 두고 영상 ID 를 적어 뒀다.

#### 지금 상태

- `%TEMP%/cc-vid-w2/펫유치원_컷편집_916.mp4` · `_169.mp4` (각 63.8초)
- **커버까지 붙인 것** — `%TEMP%/cc-vid-w2/인트로_펫유치원_916.mp4` (**65.8초** = 2 + 63.8)
- **22칸 전부 «세로 칸 크기로 잘라» 확인했다.** 다섯 칸이 어긋나 있었고 다 고쳤다
- `/admin/sns` 4주차_2026-08-31 에 넣었다 (`펫유치원_컷편집`)
- ⚠ **옛 40초판(`펫유치원_10분`)이 같은 회차에 남아 있다.** 이름이 달라 덮이지 않았다.
  사장님이 보시고 버릴 쪽을 정하신다 — 내가 지우지 않았다

---

### ✅ 펫유치원 22칸 배속 재매핑 — 끝났다 (2026-08-17) · 옛 40초판 이야기

⚠ **아래는 «40초판»(원본 녹화 + 배속) 이야기다.** 위 절대로 갈아탔으므로 지금 대본에는 안 쓴다.
그래도 **재는 법과 함정은 그대로 쓸 수 있어서** 남긴다. 옛 대본은 `대본_펫유치원_40초_옛판.json`.

**이것부터 읽는다. 다음 사람이 모르면 같은 실수를 되풀이한다.**

사장님 지적: 「컷편집을 너무 뚝뚝 끊어지게 하는데… 그냥 이미지 컷컷 짜르는게 아니라
**화면을 속도감있게 돌리면서** 편집되는 느낌으로」 (기준: `릴스영상/4. 여행사이트 144개화면/여행_컷편집_마스터.mp4`)

#### 답은 «배속» 이었다 — 재서 알아냈다

사장님이 본보기로 주신 `릴스영상/12. AI팩 만드는 과정/1. AI팩 제작_펫유치원.mp4`(62초)를
0.3초 간격으로 떠 보니 **0.9초 만에** 안내문 → 메뉴 목록 → 항목 펼침 → 이동 화면까지 갔다.
사람이 그렇게 못 움직인다 — **빠르게 감은 것**이다.

우리는 1.8초 칸에 «실시간 1.8초»를 넣고 있었다. 그 1.8초는 사장님이 화면을 **읽고 계신**
시간이라 안 움직인다. 그래서 `"배속": 3` 을 넣었다 — 원본 5.4초를 1.8초에 눌러 담는다.

| | 움직이는 프레임 (같은 24초 구간) |
|---|---|
| 기준 여행 마스터 | 67% |
| 우리 (전) | 43% |
| 억지 팬(흘림)으로 밀었을 때 | 83% ← **과했다. 아래 참고** |
| **배속 3** | **세로 65% · 가로 61%** ← 기준과 비슷해졌다 |

재는 법 (`mpdecimate` 로 «앞 프레임과 다른» 프레임을 센다):
```bash
ffmpeg -v error -stats -ss 8 -t 24 -i <영상> \
  -vf "scale=320:-1,mpdecimate=hi=200:lo=100:frac=0.1" -an -f null -
```

#### ⛔ 그런데 배속을 넣고 «전체를 다시 재지 않았다» — 이게 지금 문제다

**배속 3이면 한 칸이 원본 `ss` 부터 `ss + 1.8×3 = ss+5.4초` 까지를 훑는다.**
칸 가운데에 보이는 것은 `ss` 가 아니라 **`ss + 0.9×배속`** 이다(배속 3이면 `ss+2.7`).

나는 칸마다 프레임을 눈으로 확인해 `ss` 를 잡았는데, 그건 **배속을 넣기 전**이었다.
배속이 들어가면서 22칸 전부 보이는 화면이 밀렸고, 나는 그걸 모른 채 두 칸만 `cy` 로
만지다 두 번 실패했다.

| 칸 | 적어 둔 ss | 실제로 가운데에 보이는 초 | 무엇이 어긋났나 |
|---|---|---|---|
| 7 | 196 | **198.7** | 트리가 펼쳐져 「고객 지원·커뮤니티」가 아래로 밀렸다 |
| 8 | 205 | **207.7** | 페이지가 아직 로딩 중(회색 뼈대) — 「총 146개 화면」 칩이 없다 |

#### ✅ 다시 매겼다 — 22칸 다 떠서 확인했다 (2026-08-17 저녁)

**놀랍게도 «대략 3초씩 앞으로 당기면 된다»는 예상이 틀렸다.** 스무 칸 중 **여섯 칸만** 어긋나 있었다.
나머지 열넷은 오래 머무는 화면(흐름도·WBS·프리셋·검수 …)이라 `ss` 든 `ss+2.7` 이든 같은 것이 떠 있었다.
**일괄로 3초를 빼지 않은 것이 옳았다** — 뺐으면 멀쩡한 열네 칸을 망가뜨릴 뻔했다.

| 칸 | 자막 | 전 | 지금 | 배속 | 왜 |
|---|---|---|---|---|---|
| 3 | 디자인 컨셉은 셋 중에 골라요 | 58 | **79.5** | 3 | 58초엔 아직 «메뉴»를 적고 있다. 컨셉을 고르는 순간은 82초 |
| 5 | 「메뉴를 나누고 있어요」 | 100 | **96** | 4 | 가운데(103.6)엔 이미 「메뉴마다 화면을 뽑고 있어요」로 바뀌어 있었다 |
| 7 | 고객 지원·커뮤니티까지 | 196 | **200.3** | 3→**2** | 아래 「두 번 틀렸다」 참고 |
| 8 | 화면 목록은 146개 | 205 | **154.5** | 3→**2** | 205초의 「총 146개 화면」 띠는 **1.3초**만 떠 있다. 155초 쪽이 3초로 더 낫다 |
| 9 | 화면마다 기능과 AI 프롬프트 | 185 | **203** | 3→**2** | 프로필 항목을 펼쳐 «기능정의·버튼+이동 화면·AI 프롬프트»가 다 뜨는 자리 |
| 11 | 흐름도 | 247 | **245** | 3 | 끝(252.4)이 WBS 로딩 뼈대에 걸려 있었다 |
| 20 | AI 도구에 넣으면 그 컨셉대로 | 620 | **594** | 3 | 620초는 flow 배열만 나온다. 594초엔 `designConcept` 가 그대로 찍혀 있다 |

#### ⭐ 가운데 초는 «±0.5초쯤 흔들린다» — 1초짜리 순간은 노리지 마라

`ss + 0.9×배속` 이 딱 떨어지지 않는다. 구운 영상에서 재 보니 **최대 반 초쯤 앞으로 밀린다**
(찾아가는 자리·`-t`·`setpts` 가 겹쳐서 그렇다. 배속 3이면 원본 1.5초어치다).

그래서 **「그 순간에만 보이는 것」을 칸 한가운데에 두면 또 어긋난다.**
칸 8이 그랬다 — 205초의 146 띠는 1.3초짜리라 배속을 2로 낮춰도 못 맞춘다.
**보이는 구간이 최소 2.5초는 되는 자리를 고른다.** 안 되면 그 구간이 있는 다른 대목을 찾는다.

#### ⛔⛔ 눈으로 볼 때는 «영상 칸 크기로 잘라서» 본다 — 두 번 틀렸다

칸 7(「안 적은 고객 지원·커뮤니티까지 있고요」)을 **두 번 틀렸다.** 원본 프레임을 통째로 보고
「커뮤니티 잘 보이네」 하고 넘어갔는데, **세로판은 원본의 위아래를 잘라 낸다.** 잘라 보니 커뮤니티가 없었다.

세로판이 원본에서 쓰는 창은 **폭 전부 · 높이 `원본폭 ÷ 1.5652`**, 위에서 `(원본높이−그 높이)÷2` 부터다.
이 녹화본(1830×1420)이면 **`crop=1830:1170:0:124`**. 원본을 확인할 때 반드시 이걸 씌운다:
```bash
ffmpeg -v error -ss <가운데초> -i <녹화본> -frames:v 1 \
  -vf "crop=1830:1170:0:124,scale=740:-1" out.png -y
```
구운 영상에서 확인할 때는 (세로판 1080×1920, 칸은 **y 500~1190**):
```bash
ffmpeg -v error -ss <칸가운데초> -i <영상> -frames:v 1 \
  -vf "crop=1080:690:0:500,scale=740:-1" out.png -y
```
`<칸가운데초>` = `(칸번호−1)×1.8 + 0.9`. **구운 것에서 한 번 더 보는 것이 마지막 방패다** —
칸 7은 대본만 보고는 절대 못 잡았고, 구워서 잘라 보고서야 잡혔다.

⚠ **다음에 배속·길이·칸초를 건드릴 때마다 이 재매핑을 다시 한다.** 하나만 바꿔도 22칸이 다 밀린다.

#### 지금 상태 — 구웠고, 검수기에 보냈고, **유튜브엔 안 올렸다**

- `%TEMP%/cc-vid-w2/펫유치원_10분_916.mp4` · `_169.mp4` (각 39.6초)
- `자막검사.mts` 통과 · `/admin/sns` 4주차_2026-08-31 을 덮어썼다 (22칸 22프레임)
- **올리는 것은 사장님 몫이다.** 이미 지울 것이 여덟 개 밀려 있어 판을 더 늘리지 않는다.

#### ⛔ 「흘림」(억지 팬)은 기본으로 켜지 마라

한때 모든 컷에 세로 팬을 걸어 83% 를 만들었는데 사장님이 바로 잡으셨다:
「계속 위로 올라가는 효과는 좀 별로인데. **가만히 있어야 하는 곳도 올라가잖아.**」
맞다. 움직임은 **녹화본에서** 와야 한다. `"흘림": 70`(초당 px)은 정말 정지된 컷 하나에만 쓰는 손질이고
**기본값은 꺼짐**이다. 가로판은 녹화본 높이를 꽉 써서 여유가 0 이라 애초에 안 흐른다.

### ⭐⭐ SNS 자막·캡션은 이제 «검사기»가 막는다 (2026-08-17)

낮에 루틴이 만든 영상 두 편을 보시고 사장님이 여섯 가지를 지적하셨는데,
**여섯 다 지시서에 이미 금지되어 있던 것**이었다. 까닭이 두 개였다.

1. 지시서가 1,061줄이고 규칙이 흩어져 있었다. 게다가 같은 문서가 「화자는 18년차 기획자다」를
   여러 번 강조해서, 읽는 쪽이 **「18년 기획하면서」라고 «적으라»는 말로 받았다.**
2. ⭐ **지시서 자신이 금지어로 쓰여 있었다** — 굽는다·얹는다·한 벌·자리·잰다가 본문에 수백 번.
   그걸 읽고 자막을 쓰면 그 말투가 그대로 따라 나온다. **내가 만든 함정에 내가 걸렸다.**

**그래서 읽어서 지키는 규칙을 세어서 막는 규칙으로 옮겼다.**

```bash
node "판매용_템플릿/_마케팅/_작업/자막검사.mjs" "<대본.json>" "<인트로설정.json>"
```

- 열세 가지를 센다 — 금지어 · 경력 자랑 · 견주는 상대 · 글자 수 · 길이 · 읽는 속도 · 말투 ·
  첫 자막 겹침 · 당김 · zoom · 컷 수 · 포즈 중복(별명표 거쳐서) · 포인트 색.
- **통과해야 `영상굽기.mjs` 를 돌린다.** 낮의 두 대본에 걸었더니 8건 + 4건이 나왔고,
  사장님이 못 보고 지나가신 것 둘(포즈 중복)까지 잡혔다.
- 새 지적을 받으면 **기록에 적지 말고 검사기에 규칙을 한 줄 더한다.** 그게 요점이다.

**말 세 개는 다시 쓰지 않는다** — 얼굴→**색과 글꼴**, 뼈대→**레이아웃**, 자리→**항목·화면**.
비유를 지어내지 말고 **우리 화면에 적힌 말**을 쓴다(`컨셉 입력`·`메뉴·디자인 컨셉`·
`IA · 화면 목록`·`기능정의서`·`디자인 프리셋`·`검수 시나리오`).
지시서 맨 위에 **「⛔ 관문」**을 새로 넣었고, 거기에 **AI팩 만드는 순서 여섯 줄**을 못 박아 뒀다 —
그 편을 만들 때 문장을 새로 짓지 말고 거기서 가져다 쓴다.

**도구도 고쳤다** — `영상굽기.mjs` 의 `zoom` 기본값을 **0.6 → 1.0**(가로를 안 자름)으로 바꾸고,
`당김:true` 가 한 편에 두 곳 넘으면 멈추게 했다. 기본값이 «자르는 쪽»이면 아무것도 안 적은
컷이 조용히 잘린다. 그게 8/17 아침 「화면이 다 짤렸어」의 뿌리였다.

**⚠ 검사기가 못 잡는 것 하나 — 자막이 «그 프레임에 실제로 보이는지».**
「결과 없음 화면까지 있어요」라고 말하는 칸에 흐름도가 떠 있었다. 「설명이 이해가 안 된다」의
절반이 여기서 나온다. **초를 보고 자막을 쓴다**(관문 ⑤-2 에 훑는 명령이 있다).
ss 값을 옆 칸에서 복사하는 짓이 이 사고의 씨앗이다.

**남은 것** — 고친 대본 두 편은 다시 구웠지만 **유튜브에 안 올렸다.** 올리면 네 번째 판이
되고 지울 것이 또 는다(이미 여덟 개가 밀려 있다). 사장님이 프레임 보고 정하실 몫이다.

#### ⭐⭐ 캡션 본문은 «편마다 쓰지 않는다» — 공통 틀 하나다 (2026-08-17 저녁)

> 「우리 캡션에 하기 내용을 다 동일하게 넣는건 어떨까? **어차피 AI팩 소개하는거니까**」

맞는 말이다. 영상은 편마다 다른 이야기를 하지만 **캡션은 늘 같은 물건을 소개한다.**
편마다 새로 쓰면 「AI팩이 무엇인가」가 편마다 다르게 설명된다.

**틀: `_마케팅/캡션_공통.md`** (사장님이 직접 쓰신 글). 편마다 다른 것은 **칸 셋뿐**이다 —

| 칸 | 펫유치원 값 | 어디서 왔나 |
|---|---|---|
| `{{컨셉한줄}}` | 나는 펫 유치원을 운영하고 있어 | 그 편에서 실제로 적은 줄 |
| `{{화면수}}` | **146** | 화면에 「총 146개 화면」으로 찍혀 있다 |
| `{{안적은메뉴}}` | 고객 지원, 커뮤니티 | 화면에 그렇게 적혀 있다 |

대본 JSON 의 **`"캡션값"`** 에서 채운다. 하나라도 비면 `검수보내기` 가 멈춘다.
편별 `"캡션"` 칸은 **없앴다.**

⚠ **원고에 136 으로 적혀 있던 것을 146 으로 고쳤다.** 「고객센터」도 화면 표기인 **「고객 지원」**으로 바꿨다.
둘 다 이 문서 아래 「숫자·낱말에서 사장님과 어긋났던 것」에 이미 적혀 있던 것이다 —
**손으로 옮겨 적으면 같은 자리에서 또 틀린다.** 그래서 칸으로 뺐다.

길이: 유튜브 1,707자(상한 5,000) · 인스타 1,600자(**상한 2,200** — 600자 남는다). 늘릴 때 인스타부터 넘친다.

**규칙 두 개를 고쳤다**
- **캡션에서는 「경력 자랑」을 안 막는다.** 8/17 아침 지적은 «자막»을 두고 하신 말이었다
  (「동영상에서」). 1.8초 자막에 경력을 적으면 자랑이지만, 읽으려고 여는 캡션에서 경력은 **근거**다.
  자막·표지에서는 그대로 막는다. **자리가 다르면 규칙도 다르다.**
- 금지어 **「성합니다」를 뺐다** — 생성합니다·작성합니다·구성합니다에 다 걸린다.
  멀쩡한 말을 잡는 규칙은 방해물이다.

⚠ **두 번 새어 나갈 뻔했다. 둘 다 「조용히」 지나가는 꼴이었다.**
- 틀 맨 위 주석 안에 **주석 닫는 표시를 글자로** 써 놨더니 거기서 잘려, 내 메모가 캡션에 그대로 들어갔다.
- 칸 이름이 한글인데 `\w` 로 찾고 있었다. **한 칸도 안 바뀌고**, 빈칸 검사까지 통과했다.
  → `[^}]+` 로 고쳤다. **정규식에 `\w` 를 쓰면 한글이 빠진다.**

#### ⭐ 캡션은 «자막을 받아쓴 것»이 아니다 (2026-08-17)

한때 `검수보내기` 가 자막을 세 줄씩 묶어 캡션을 만들게 했다가 바로 지적받았다:
「**자막이 부족하니까 읽으라고 넣는게 캡션인데.. 그대로 받아쓰면 안되지~**」

그리고 그 전에는 더 나빴다 — `올릴글_*.md` 를 **파일째로** 캡션 칸에 부어서
「⚠ 원래 지시서엔…」 같은 **내 내부 메모가 유튜브 설명란에** 들어가 있었다.

**지금 방식** — 캡션은 대본 JSON 의 **`"캡션"` 칸에 따로 쓴다.** 없으면 `검수보내기` 가 멈춘다.
링크 세 줄과 해시태그는 스크립트가 붙인다. 지금 값: 펫유치원 819자 · 내컴퓨터에서만 426자
(자막은 각 344자 · 356자다 — **길이도 결도 다르다**).

#### 도구 넷 (전부 `판매용_템플릿/_마케팅/_작업/`, git 밖이라 이 컴퓨터에만 있다)

| 파일 | 무엇을 | 언제 |
|---|---|---|
| `자막검사.mts` | 자막 열세 가지를 센다. **굽기 전에 통과해야 한다** | 대본 고칠 때마다 |
| `움직임표.mts` | 녹화본에서 «어느 초가 움직이나» 를 재서 ±4초 이웃을 알려준다 | ss 잡을 때 |
| `검수보내기.mts` | 구운 세로 영상에서 칸 프레임을 떠 DB 로 보낸다 → `/admin/sns` | 굽고 나서 |
| `검수목록.mts` · `캡션보기.mts` | 검수기에 무엇이/어떤 캡션이 들어 있나 (로그인 벽 뒤라 눈으로 못 본다) | 「안 보인다」 들을 때 |

⚠ `자막검사` 는 `.mjs` 가 아니라 **`.mts`** 다 — 규칙 lib(`lib/sns-caption-rules.ts`)를 쓰려면 TS 여야 한다.
`npx tsx` 로 돌린다. 경로 별칭은 `@/lib/...` 를 쓴다(상대 경로로 쓰면 tsx 가 못 찾는다).

#### 도구 쪽에서 고친 것 — 되돌리지 마라

| 무엇 | 값 | 까닭 |
|---|---|---|
| `영상굽기.mjs` `zoom` 기본값 | **1.0** (가로를 안 자름) | 0.6 이었을 때 넓은 PC 화면이 조용히 잘렸다. 기본값은 «안전한 쪽»이어야 한다 |
| `"당김": true` | 한 편에 **최대 1곳**, 넘으면 멈춤 | 열여덟 곳에 붙여 잘림 검사를 통째로 껐다 |
| `"흘림"` | **기본 꺼짐** | 위 「억지 팬」 참고 |
| `"배속"` | 없으면 1. **2~4배**가 쓸 만하다 | 더 올리면 무엇을 보여주는지 못 읽는다 |

#### 숫자·낱말에서 사장님과 어긋났던 것

- **화면 수는 146개다.** 사장님 원고에는 136 으로 적혀 있었는데 화면(206초 띠 · 520초 대시보드)에
  **「총 146개 화면」**으로 찍혀 있다. 화면에 찍힌 값을 쓴다.
- 「고객센터」가 아니라 **「고객 지원」**이다(화면에 그렇게 적혀 있다).
- 제목에서 **「10분 만에」를 뺐다** — 재 본 값이 아니다. 「한 줄 적으니 / 화면 146개가 / 나왔어요」.

### ✅ ~~배포가 막혀 있다 — 사장님이 두 줄을 지워 주셔야 한다~~ **풀렸다 (2026-08-28 확인)**

> **지금은 이렇다** — `.claude/settings.local.json` 의 `deny` 에는 `npm publish`·`gh release` 뿐이고,
> `Bash(git push:*)` 는 **allow**, `vercel` 은 **ask** 에 있다. 그러니 내가 밀 수 있고, 밤에 혼자
> 도는 마무리 루틴도 멈추지 않는다. **아래는 그때 무슨 일이었나 하는 기록이다.**

아침에 내가 `.claude/settings.local.json` 의 `deny` 에 `git push`·`vercel` 을 넣었다.
루틴이 몰래 배포하는 것을 막으려던 것인데 **나에게도 하드 블록**이다(승인 창이 뜨지 않고 아예 안 된다).
그 줄을 내가 지우는 것도 막혀 있다 — 제 권한을 넓히는 일이라 그렇게 되어 있고, 그게 맞다.

**사장님이 `deny` 에서 이 두 줄을 지우시면** 그때부터는 승인 창 방식으로 돌아간다:
```
"Bash(git push:*)",
"Bash(vercel:*)",
```
그전까지는 사장님이 직접 `git push origin HEAD:main` 을 돌리셔야 한다.
**지금 배포 대기 중인 것 — SNS 검수 화면(`dea7b4a`).** DB 마이그레이션(0014)은 이미 적용해 뒀다.

### ⚠⚠ 루틴이 내 커밋을 브랜치에서 밀어냈다 (2026-08-17)

홈 인터랙션을 커밋(`eda2fbc`)한 뒤, 같은 브랜치에서 **인테리어 팩 루틴이 따로 커밋을 쌓아**
갈래가 둘로 갈렸다. 작업 트리에서 홈 인터랙션이 **통째로 사라져 있었는데 `git status` 는 「깨끗함」**
이라 눈으로는 안 보였다. 커밋 개체는 살아 있어서 `git cherry-pick c38686b eda2fbc` 로 되살렸다.

⚠ **루틴과 손작업이 같은 브랜치를 쓰면 또 난다.** 손으로 커밋한 뒤에는
`git log --oneline -3` 으로 **내 커밋이 tip 에 있는지** 한 번 보고 넘어간다.

### ⚠⚠ 루틴이 내 커밋을 브랜치에서 밀어냈다 (2026-08-17) — 되풀이될 수 있다

홈 인터랙션을 커밋(`eda2fbc`)한 뒤, 같은 브랜치(`pack/interior-20260817`)에서 **인테리어 팩
루틴이 따로 커밋을 쌓아** 갈래가 둘로 갈렸다. 그래서 **작업 트리에서 홈 인터랙션이 통째로
사라져 있었다** — `git status` 는 「깨끗함」이라 눈으로는 안 보인다. 커밋 개체는 살아 있어서
`git cherry-pick c38686b eda2fbc` 로 되살렸다.

⚠ **루틴과 손작업이 같은 브랜치를 쓰면 또 난다.** 손으로 커밋한 뒤에는
`git log --oneline -3` 으로 **내 커밋이 tip 에 있는지** 한 번 보고 넘어간다.

### 홈 메인 「장 구분」 — ①② 끝, ③ 움직임만 남았다 (2026-08-14)

**한 것**
- 「한 줄 컨셉이…」 절(2,261px)을 **둘로 쪼갰다.** 자르는 자리는 `.thesis` 뒤 —
  앞은 «AI팩에 뭐가 들어 있나»(문서 6종 + 다른 도구엔 이게 없더라),
  뒤는 «실제로 돌려봤더니»(영상 + 함께 드려요 + CTA).
  `#planning` 988px · `#proof` 1,611px 로 갈렸다.
- 새 절 `#proof` 는 바탕을 `--paper-2` 로 한 톤 밝게 깔았다. 눈표는 「직접 돌려봤어요」.
- 영상 머리글을 `sc-cap`(19px) → **h2** 로 올렸다. 절이 갈라지니 그 절 제일 큰 글자가
  19px 이 되어 옆 절(58px) 옆에서 «잘린 조각»으로 보였다. 문장은 그대로 옮겼고 새로 안 지었다.
- 맺음절(`.moat`)에 가운데 정렬 눈표 「마지막으로」를 세웠다.

**⚠ 새 절에 번호(04)를 안 붙인 이유** — 위쪽 띠가 「01 만들기 전 / 02 오픈 전」이고
아래 절 번호 01·02 가 지금 **딱 맞물려 있다.** 가운데에 번호를 끼우면 그 짝이 통째로 밀린다.
번호 없는 눈표는 «장이 바뀐다»만 알리고 짝은 안 건드린다.

**남은 것 — ③ 움직임.** 아직 손 안 댔다.

<details><summary>왜 이렇게 고쳤나 (재어 둔 값)</summary>

사장님 말씀: 「메인에서 서비스를 다 소개하는데 **그냥 줄줄줄 내려가니까 잘 안 읽힌다.**
https://estsoft.ai 정도로, **내용이 바뀌는 지점에서 포인트가 되는 느낌**으로.」

**레퍼런스를 열어 재 봤다. 애니메이션 문제가 아니었다.**

| | estsoft | 우리 |
|---|---|---|
| 전체 길이 | 22,571px (25화면) | 5,898px (8화면) |
| 절 개수 | 14 | 6 |
| 배경 바뀜 | 흰·회·검 | **이미 4가지 씀** |
| 장 라벨 | 12px 영문(「Business Area」·「Product」) | **없다** |
| 붙박이(sticky) | **없음** | — |

**원인 둘**
1. 「한 줄 컨셉이 바로 만들 재료가 됩니다」 절이 **2,172px = 3화면**인데 이정표가 없다.
   안에 26px·22px 소제목이 둘 있지만 절 제목 58px 에 눌려 안 보인다.
2. 절이 시작할 때 «이제 다른 이야기»라는 신호가 없다. 배경색만 바뀌고 말이 없다.

**할 순서** — ① 절마다 장 라벨(「01 만들기 전」 …) → ② 3화면짜리 절을 둘로 쪼개기 →
③ 그다음에 움직임. **①②가 먼저다.** 글자·배치만 건드리는 일이라 싸고 효과가 크다.

⚠ 위 표의 「장 라벨 — 없다」는 **틀렸다.** 소스를 안 보고 화면만 재서 그랬다.
`01 AI팩 만들기`·`02 검수 시나리오`·`03 AI팩 구매` 셋은 처음부터 있었다.
없던 곳은 맺음절뿐이었고, 진짜 문제는 ②(한 절이 3화면)였다.

</details>

- ⚠ **정규식이 든 코드를 «셸 한 줄»로 넣지 마라** (2026-08-14 에 세 파일을 깨뜨렸다).
  `node -e` 로 `split(/\r?\n/)` 을 써 넣었더니 `\r` 과 `\n` 이 **실제 줄바꿈**으로
  들어가 정규식이 반 토막 났다. 영상굽기·썸네일굽기·영상인트로가 다 문법 오류로 죽었다.
  → 정규식·백슬래시가 든 코드는 **파일로 쓰거나 Edit 도구로** 넣는다.

- ⚠ **영상 도구는 세 개다. 하나만 고치면 나머지가 갈라진다.**
  `영상굽기.mjs` · `썸네일굽기.mjs` · `영상인트로.mjs` 가 모두 고양이방을 본다.
  2026-08-14 에 그림을 `_이미지` 로 옮기며 앞의 둘만 고쳤더니, 표지를 구울 때
  「포즈가 없습니다」로 죽었다. **셋을 함께 본다.**
  ⚠ 이 도구들은 `판매용_템플릿/` 아래라 **git 밖**이다 — 이 컴퓨터에만 있다.

### ⭐ 팩의 사진은 «예시 이미지»다 — 까다롭게 보지 않는다 (2026-08-28 사장님 정리)

**출처.csv 331줄을 다 채웠다.** 그러면서 사장님께 이미 정해진 것을 또 여쭈었다.
적혀 있지 않아서다. 그래서 여기와 `이미지_받을목록.md` 두 곳에 남긴다.

> 「우리는 이미지를 판매하는 곳이 아니기 때문에 무료 이미지를 사용하는 것은 무관하게 보고
> 있고, 워터마크를 넣어서 재사용 불가로 만들고 있어. 팩에 사용하는 이미지들은 모두
> **예시 이미지**이기 때문에 까다로울 필요 없고 내용과 완전히 들어맞지 않아도 돼.」

| 어디 | 몇 줄 | 어디서 왔나 |
|---|---|---|
| `마스코트/장면/` | 111 | 우리가 만든 것 (AI 생성) |
| `사진/반려동물케어/` | 20 | 우리가 만든 것 (AI 생성) |
| `사진/` 나머지 여덟 | 200 | **픽사베이** · https://pixabay.com |

```bash
npx tsx 출처채우기.mts        # 재기만 한다. 빈 줄이 있으면 1 로 끝난다
```

⛔ **다음 사람이 모르면 사장님께 같은 것을 또 묻는다** — 「팩에 실제 사람 얼굴이 2,605곳
  들어가 있습니다」는 **알림이 아니라 이미 끝난 판단이다.** 사진 고를 때는
  **① 내용과 맞는 것 → ② 없으면 다른 것**이면 된다. 여기서 일을 벌이지 않는다.

⚠ 다만 `이미지_받을목록.md` 의 「피할 것」(로고·상표 · 글자 박힌 사진 · 게티)은 그대로다.

⚠ **출처.csv 는 칸이 «셋»이어야 한다.** `이미지-예시만들기.mts` 가 세 칸으로 다시 쓴다 —
  칸을 늘리면 다음에 돌 때 아무 소리 없이 지워진다.

### 밀려 있는 일

- **팩에 아직 사진이 없는 자리 — 원본을 받아야 채운다** (2026-08-13 기준)

  | 어디 | 몇 장 | 무슨 사진이 필요한가 | 넣을 곳 |
  |---|---|---|---|
  | LMS 두 팩 | — | **끝났다.** 마스코트 장면 그림 15장으로 296곳을 채웠다 | — |
  | 매칭 두 팩 | — | **끝났다.** 스톡 사진 대신 마스코트 동물 그림 16장으로 채웠다 (2026-08-14 방침 바뀜) | — |
  | 매칭 두 팩 | — | 짐 사진 18곳은 **일부러 비운다** (손님이 찍어 올릴 자리) | — |
  | 장비렌탈 두 팩 | — | **끝났다.** 장비 사진 25장이 들어와 깔렸다 (2026-08-28 재어 확인) | — |

  ⭐ **매칭은 «스톡 사진을 기다리는 일»이 아니었다** (2026-08-28 에 다시 재어 확인).
  8/13 에 「이사·청소·수리·과외 현장 사진 5~6장이 필요하다」고 적었는데, **바로 다음 날
  마스코트 동물 그림으로 채우는 것으로 바뀌었다.** 표만 그대로 남아 여드레 동안
  「사장님이 사진을 주셔야 하는 일」로 보이고 있었다.

  이어 준 것은 `팩용/매칭/짝.csv` 다 — 한결이사·박준호 기사 → **이사_곰사슴**,
  깔끔한하루·반짝반짝청소 → **청소_라쿤**, 에어컨명가 → **가전수리_수달**,
  수학하는김쌤 → **과외_코끼리**, 세컨홈 → **인테리어_비버**, 스튜디오 온 → **촬영_페넥**.
  열여섯 장이 원본(`마스코트/장면/매칭_*.png`)·워터마크판(`팩용/마스코트/매칭_*.webp`)
  둘 다 있고 팩에도 실려 있다.

  ```
  npx tsx check-사진채움.mts 매칭_디럭스     자리 162 · 사진 149  ✓
  npx tsx check-사진채움.mts 매칭_프리미엄   자리 357 · 사진 338  ✓
  ```

  ⚠ **안 깔린 13곳·19곳을 「빠진 것」으로 세지 마라 — 일부러 비운 자리다.**
  후기 사진 6·9곳(손님이 올릴 것) · 짐 사진 7곳(`LD-02` 「첨부 사진 3장」 — 「사진을 보면
  짐 양을 가늠할 수 있어요」라고 화면이 직접 말한다) · 지도 3곳(프리미엄, 사진 자리가 아니다).

  ⭐ **장비렌탈도 같다** (2026-08-28 재어 확인). 8/13 에 「한 장도 없다」고 적혀 있었는데
  그 뒤 **장비 사진 25장**이 들어와 `배치.csv` 로 깔렸다.

  ```
  npx tsx check-사진채움.mts 장비렌탈_디럭스     자리 101 · 사진  98  ✓
  npx tsx check-사진채움.mts 장비렌탈_프리미엄   자리 176 · 사진 172  ✓
  ```

  남은 3곳·4곳은 **전부 지도**다(`BK-01`·`HO-01`·`RT-03`). 지도는 사진 자리가 아니다.

- **자리표는 «네 가지»를 요구한다. 마스코트가 되는 자리는 그중 둘뿐이다** (2026-08-13 재어 봄)

  | 무엇을 달라나 | 몇 곳 | 마스코트 | 왜 |
  |---|---|---|---|
  | 사람 (강사·고수·프로필) | 884 | ✓ | 마스코트가 제일 잘 맞는 자리 |
  | 장면 (썸네일·배너·소개) | 422 | ✓ | LMS 가 이걸로 다 찼다 |
  | 물건 (상품·장비·메뉴) | 427 | △ | **마스코트가 그 물건을 «쓰는» 그림이어야 한다** |
  | 장소 (매장·살롱·여행지) | 223 | △ | 마스코트가 그 장소에 «간» 그림이어야 한다 |
  | 손님 것 (결과·후기·짐·수료증) | 341 | ✗ | 회색이 정직하다 |

  `_작업/자리성격세기.mjs` 가 센다. **LMS 는 물건·장소가 0곳이라 가장 쉬운 팩이었다** —
  거기서 잘 됐다고 열두 팩이 다 될 것처럼 말하면 안 된다.

- **LMS 는 스톡 사진 대신 «마스코트 장면 그림»을 쓴다** (2026-08-13 사장님 제안).
  배경까지 그려진 20장(`_이미지/마스코트/장면/`)이라 4:3·1:1 로 잘라도 멀쩡하다.
  배경이 뚫린 낱장 마스코트는 **쓰면 안 된다** — 자리표의 「이미지 영역 (…)」 글자가
  고양이 뒤로 비쳐 보인다. 시험해서 눈으로 확인했다.
  스톡 사진과 달리 **우리 것이라 출처·라이선스 문제가 아예 없다.**
  수료증 양식·제출 파일·후기 사진 자리(19곳)는 그대로 회색이다 — 손님 것이 들어갈 자리다.

  사진을 넣은 뒤 `이미지-예시만들기.mts <업종>` → `이미지-끼우기.mts <팩>` 차례로 돌린다.
  **지도·달력 자리(6곳)는 사진 자리가 아니다.** 회색으로 둔다.

- **몇 장이 필요한지는 «자리 수»가 아니라 «이름 수»로 센다.**
  LMS 의 강의 카드 202곳에 들어갈 이름은 열세 가지뿐이라 **13장이면 덮인다.**
  자리 수(234)로 세면 스무 배를 헛구한다. `_작업/자리이름모으기.mjs` 가 이름을 세어 준다.
  `_작업/사진모아보기.mjs <폴더> <낼파일> [열수]` 는 원본을 «번호를 찍어» 한 장에 붙여 준다 —
  번호 없이 붙였다가 격자를 잘못 세어 엉뚱한 파일을 골랐다.
  `_작업/나란히붙이기.mjs <낼파일> "제목=왼쪽.png" "제목=오른쪽.png"` 는 두 화면을 견주게 붙인다.
  **「이게 낫다」고 말로 하지 말고 붙여서 보여 준다.** 사장님이 「보여줘」라고 하시는 자리다.

  `_작업/자름보기.mjs <그림> <낼파일>` 은 세로 그림이 가로 칸에서 얼마나 잘리는지 그려 준다.
  `_작업/배경깔기.mjs <컷폴더> <낼폴더> [비율]` 은 **배경이 뚫린 컷에 크림 바탕을 깐다** —
  업종 컷 19장이 30초 만에 카드에 쓸 수 있는 그림이 됐다.
  `_작업/카드격자흉내.mjs <폴더> <낼파일> [열수]` 는 그림들을 **카드 격자로 늘어놓아** 본다.
  한 장씩 보면 다 괜찮은데 열두 장이 나란히 놓이면 얘기가 달라진다 — 그걸 보고 정한다.

- **바탕 깐 그림이 「밋밋하다」고 했다가 격자로 늘어놓고 뒤집었다** (2026-08-13).
  한 장씩 볼 땐 장면 그림이 훨씬 좋아 보인다. 그런데 **열두 장을 카드로 늘어놓으면 반대다.**
  · 바탕 깐 쪽 — 물건이 또렷하다. 꽃·케이지·저울·드라이기가 한눈에 갈린다. 상품 카탈로그 꼴.
  · 장면 쪽 — 열두 장 중 다섯이 「모니터 앞 고양이」다. **한 장씩 볼 땐 안 보이던 반복이 보인다.**
  → **카드가 여럿 늘어서는 자리는 바탕 깐 것, 큰 한 자리(히어로·소개·배너)는 장면 그림.**
  ⚠ 눈으로 한 장만 보고 정하지 마라. 쓸 자리 꼴로 늘어놓고 봐야 안다.

- ⚠ **뿌리의 `_작업/` 은 git 이 무시한다.** 위 여섯과 `상세화면뜨기.mjs`·`유튜브올리기.mjs` 는
  이 컴퓨터에만 있다. 방이 바뀌어도 파일은 그대로 있지만, **git 기록에는 없다.**

- **9:16 은 카드에서 42%만 보인다.** 자리표에 세로 칸이 거의 없다(정사각 752 · 4:3 693 ·
  동그라미 590 · 세로 13). 쇼츠가 본업이니 9:16 은 그대로 만들되,
  **보여야 할 것은 화면 가운데 40% 안에** 넣어야 한다. 16:9 배너 자리(53곳)는 32%만 남으므로
  그 자리에 쓸 것은 가로로 한 벌 더 뽑는다.

- **아직 안 쓴 원본이 남아 있다** — `_이미지/사진/` 의 인테리어 25장(8/17 인테리어 팩용).
  직업 40장·장소 35장은 2026-08-13 에 LMS 에 쓰기 시작했다.

- **출처.csv 가 죄다 비어 있다.** 공용·직업·장소 모두 「어디서받았나」·「주소」가 빈칸이다.
  「이 사진 어디서 났느냐」는 물음에 답할 수 있어야 한다 — 채워 두는 것이 진짜 방패다.

- ⭐⭐ **대표 마스코트가 «검은 긴머리 고양이»로 바뀌었다** (2026-08-14 사장님 지시).
  사장님 머리 모양을 닮게 만든 캐릭터이고, **SNS 얼굴로 쓰신다.**

  `_이미지/마스코트/낱장/대표_*.png` — **20 포즈, 배경 뚫림**(영상에 바로 겹친다).
  집중 · 코딩 · 화이트보드 · 화상회의 · 차트 · 출근 · 엄지척 · 깜짝 · 팝콘 · 잠 ·
  게임 · 잡지 · 간식 · 커피 음미 · 커피 내리기 · 수건 · 거울 · 가방 · 노트북(소파/의자)

  ⚠ **대본은 옛 포즈 이름(집중·똘망·애교 …)을 부른다.** 그래서
  `_이미지/마스코트/낱장/_별명.csv` 로 이어 두었다 — **대본을 한 줄도 안 고치고 얼굴만 바뀐다.**
  `영상굽기.mjs` · `썸네일굽기.mjs` 가 이 표를 읽는다. 아홉 포즈가 다 이어지는 것을 확인했다.
  **되돌리려면 `_별명.csv` 이름만 바꾸면 된다** — 그러면 옛 삼색 고양이를 다시 쓴다.

  ⭐ **둘을 섞어 쓴다. 새 그림을 기다리지 않는다** (2026-08-14 사장님 지시).
  > 「일단 두 가지 섞어서 사용해줘. 어차피 내용에 맞게 나오지 않는 것 같아.」

  별명표에 있는 포즈는 **대표 마스코트**로, 없는 포즈(상황 컷 40여 장 —
  오류·배포·디버깅·세리머니 …)는 **옛 삼색 고양이**로 자동으로 갈린다.
  `포즈파일()` 이 별명 → 없으면 원래 파일 순으로 찾으므로 **손댈 것이 없다.**
  확인했다: 집중·깜짝·커피_음미 → 대표 / 심야 디버깅·배포 성공·드디어 오류 해결 → 옛 고양이.

  ⚠ **한 영상 안에 두 고양이가 같이 나올 수 있다. 그래도 괜찮다고 정하셨다.**
  나중에 대표 마스코트 상황 컷이 생기면 별명표에 줄만 더하면 그때부터 갈아탄다.

  **배경 있는 판 21장**(`장면/대표_장면_*`)은 **인터뷰·말하는 영상**용이다(2026-08-14 사장님 제안).
  `릴스영상/_인터뷰/얼굴기준.png` 를 그중 「책상정면」으로 세워 두었다 —
  ⚠ **8/18 인터뷰 루틴이 AI로 얼굴을 새로 뽑지 않는다.** 크레딧을 아끼고, 얼굴이 흔들리지 않는다.
  루틴 지시서(`cat-interview/SKILL.md`)도 대표 마스코트 기준으로 고쳤다 —
  안 고쳤으면 첫 회차가 **옛 삼색 고양이 얼굴**로 나올 뻔했다.

  옛 삼색 고양이도 버리지 않았다 — 새 장면 10장이 `장면/삼색_장면_*` 에 있다. 조연으로 쓴다.

- ⚠ **영상 일을 시작하면 `node _작업/녹화본목록.mjs` 부터 돌린다.**
  사장님이 `릴스영상/` 에 새 녹화본을 넣으셔도 알림이 오지 않는다.
  루틴 지시서가 원본 목록을 «손으로» 적어 두고 있었고(「9시간 넘게…」),
  2026-08-13 에 세어 보니 **41개 · 12시간 19분**이었다 — 새로 넣으신
  「12. AI팩 만드는 과정」(반려동물 유치원, 11분)은 목록에 없어 안 보이고 있었다.
  **폴더 번호가 늘었으면 새 재료가 들어온 것이다.** 손으로 적은 목록은 반드시 썩는다.

- ⚠ **그림 일을 시작하면 `_이미지/0_새로받은것/` 부터 열어 본다.**
  사장님이 새 그림을 넣으셔도 «알림이 오지 않는다» — 들여다볼 때만 보인다.
  말씀을 안 하셨어도 먼저 연다. 비어 있으면 그냥 넘어가면 된다.
  파일 이름·배경 유무·종류가 섞여 있어도 된다. 가르는 것은 우리 일이다
  (배경은 알파 채널로 재고, 이름 없는 것은 `_작업/짝찾기.mjs` 로 견줘 찾는다).

- ⭐ **그림은 2026-08-13 에 `_이미지` 한 곳으로 모았다** (사장님 지시).
  전에는 릴스용 고양이가 `_마케팅/릴스영상/_모델/고양이`, 팩용 사진이 `_이미지` 에 따로 있어
  같은 그림을 두 군데서 찾아야 했다. **`_모델/고양이` 폴더는 이제 없다.**

  ```
  _이미지/
    0_읽어보세요.md      ← 새 방은 여기부터
    마스코트/낱장/  69장  배경 뚫림 — 영상에 겹칠 때
    마스코트/장면/  95장  배경 있음 — 팩 카드·표지
    마스코트/이름표.csv   「Frame 37」이 무엇이었는지
    사진/<업종>/          받은 스톡 (워터마크 전) + 출처.csv
    팩용/<업종>/          워터마크 박힌 webp — 팩에 실리는 것은 이것뿐
  ```

  경로를 고친 곳: `이미지-예시만들기.mts` · `이미지-끼우기.mts` ·
  `_작업/영상굽기.mjs` · `_작업/썸네일굽기.mjs` · `_작업/썸네일굽기_1주차보충.mjs` ·
  `이미지_받을목록.md` · `판매팩_자동화_지시서.md` · SNS 루틴 지시서.
  **영상 대본이 부르는 포즈 아홉 개가 낱장에 다 있는지 확인했다.**
  `이미지-예시만들기.mts 마스코트` 라고 부르면 마스코트/장면 을 원본으로 본다.

- **이름 없는 그림은 «견줘서» 이름을 찾는다.** 배경 넣은 55장이 `Frame 37.png` 였다.
  `_작업/짝찾기.mjs <이름없는방> <낱장방> <낼표.csv>` 가 낱장과 그림을 견줘 짝을 찾아 준다.
  53장은 닮은 정도 1~2 로 확실했고, **둘만 수상해서 눈으로 봤다** —
  하나는 맞았고(홈케어), 하나는 짝이 없는 새 그림이라 손으로 이름을 줬다(주문배송).
  눈대중으로 55장을 붙이면 반드시 몇 개는 틀린다.

- **비어 있는 자리는 «일부러» 비워 둔 것이다.** `_이미지/팩용/<업종>/배치.csv` 의 `(비움)` 줄이 그렇게 시킨다.
  맞는 사진이 없을 때 자리표는 「강의 썸네일 이미지 자리」라고 **맞는 말**을 하는데,
  패션 인물 사진을 넣으면 **틀린 말**이 된다. 빈 것보다 틀린 것이 나쁘다.
- ✅ **`검수항목.md` 의 ⬜ 8개 — 끝났다** (2026-08-14, 로컬에서 만들어 올림)

  클라우드 루틴이 두 번 돌아 일은 다 했는데 **GitHub 푸시가 403** 으로 막혀 결과가
  사라졌다(`f8a3b5a` 는 클라우드 안에만 있다). 그래서 로컬에서 다시 만들었다.
  ⚠ **클라우드 루틴을 쓸 때는 푸시 권한부터 확인한다.** 없으면 일하고도 잃는다.

  | 검사기 | 무엇을 재나 | 항목 |
  |---|---|---|
  | `check-겉표지` | index.html 이 말하는 가이드·레이아웃·JSON 이 그 팩에 실제로 있나 | C1·C2·C3 |
  | `check-사진채움` | 통째로 빔 · 첫 화면 히어로 빔 · 한 묶음에 같은 그림 두 번 | F4·E1·F5 |
  | `check-평평함` | 같은 꼴 카드가 12개 넘게 줄지어 있나 | E3 |
  | `check-zip날짜` | 파는 zip 이 «지금 팩»보다 나중인가 | B9 |

  넷 다 **음성 시험을 했다** — 흠을 일부러 넣어 잡히는 것을 보고 되돌렸다.

  ⚠ **거짓 경보를 두 번 걷어냈다. 검사기는 만든 뒤가 아니라 «재본 뒤»에 믿는다.**
  · F5 를 「화면 전체에서 같은 그림」으로 짰더니 열 팩이 걸렸다 — 「매장 대표」와 그
    매장 카드가 같은 그림인 것은 «옳다». 「나란히 붙은 한 묶음 안에서」로 좁혔다.
    작은 프로필 자리는 아예 뺐다(사장님이 중복을 허락하신 자리다).
  · E3 이 LMS 「수강생 진도 현황」의 30차시 목록을 잡았다 — 아코디언은 길어야 맞다.

- D6(375px 넘침)은 CDP 가 필요하다 — 위 여덟과 별개로 👁 로 남는다
- 공동구매 `.tier`(10px 14px)·`.figs`(1px) 디자인 FAIL 은 **일부러** 남겨 둔 것이다
- ~~FLOW 페이지의 `ZipAllButton` 이 프리셋·검수를 빠뜨린다~~ → **끝났다**
  (`05f6cea` 「전체 다운로드」가 디자인 프리셋을 빼고 줬다 — 크레딧이 꺼져 있을 때).
  2026-08-14 에 다시 열어 보니 「함께 받기」 팝업이 프리셋·검수를 다 묻고 있었다.
  ⚠ **밀린 일 목록도 썩는다.** 손대기 전에 실제 코드를 먼저 열어 본다.
- 8/17 인테리어 A 회차 · 8/18 고양이 인터뷰 첫 회

---

## 4. 모르면 사고 나는 것

### 재서 알아낸 함정 (2026-08-13)

**① 창 크기로 준 만큼이 «보이는 자리»가 아니다**
`--window-size=1920,1080` 을 줘도 크롬이 창틀로 96px 을 먹어 **984px 만 그려진다.**
가로 썸네일 넉 장 모두 **맨 아래 서명줄이 잘린 채** 나가고 있었다.
→ 넉넉히 찍고 `crop` 으로 정확히 잘라낸다.

**② 표지를 «따로 얹으면» 자국이 남는다**
고양이를 overlay 로 얹으니 그 자리만 2단계 어두운 네모가 생겼다(232,228,218 대 234,230,220).
→ **한 벌로 굽는다.** 흔들림을 잃는 대신 자국이 사라진다.

**③ 녹화본은 가로를 자르면 안 된다**
16:9 녹화본을 당기면 메뉴·단추가 반씩 잘린다. **녹화본마다 여백이 다르다.**
→ `영상굽기.mjs` 가 재서 2% 넘게 자르면 멈춘다. 일부러면 `"당김": true`.
→ 4:5 녹화본(1080×1350)이 있으면 그걸 쓴다 — 가로를 한 점도 안 자른다.

**④ 화면 ID 는 팩끼리 겹친다**
`HO0101` 은 여행에도 장비렌탈에도 있다. 캡처를 한 폴더에 섞으면 **엉뚱한 팩 화면이 조용히 들어간다.**
→ 대본에 `"캡처방"` 을 적는다(여행 `tall`, 장비렌탈 `rental`).

**⑤ concat «필터» 는 뒤를 조용히 잘라먹는다**
짧은 쪽(표지)을 앞에 두면 **끝이 잘린다.** 경고도 없고 앞은 멀쩡해 보인다.
실제로 마지막 CTA 자막이 통째로 사라질 뻔했다. → 이음표(concat 디먹서) + `-c copy`.

**⑥ 자리표 «이름»이 팩마다 다른 데 적혀 있다 — 못 읽으면 조용히 다 틀린다**
`title` · `data-cap` · `<span class="lb">` · `<span class="cap">` 넷만 보고 있었는데,
**LMS 두 팩은 `aria-label` 에만 적혀 있었다.** 이름이 빈 글로 읽히니 329곳이 죄다 기본역할이 되어
**강사 프로필 자리에도 강의 썸네일에도 똑같은 패션 인물 사진**이 들어갔다. 오류도 경고도 없다.
→ `이미지-끼우기.mts` 가 다섯 군데를 다 본다. 동그란 얼굴 자리도 팩마다 `ph-circle`·`ph-round`·`ph-ava` 로 다르다.
→ **새 팩에 사진을 끼우면 반드시 한 장 찍어서 눈으로 본다.** 검사기(`check-사진.mts`)는
   「칸을 벗어났나」만 재지 「맞는 사진인가」는 못 잰다.

**⑦ `팩재료-캡처영상.mts` 는 프리셋을 돌려 가며 찍는다 — 상세용으로 쓰면 안 된다**
LMS 디럭스를 찍었더니 `LMS_디럭스_코럴선셋` 이라는 **새 폴더**가 생겼다(원래는 모던네이비).
그대로 두면 `팩화면-웹용.mts` 가 같은 팩을 두 번 잡아 그림이 섞인다.
→ 홈페이지 상세용은 **`_작업/상세화면뜨기.mjs`** 하나만 쓴다(주요 10 + 실패 4, 색은 프리미엄 폴더에서 읽는다).

### 드라이브를 만질 때

⚠ **2026-08-12 에 2주차 폴더에서 폴더가 통째로 사라졌다**(카드뉴스 3개 + 백업 1개).
버릴 것들이라 손해는 없었지만, 원인을 확정하지 못했다.

- **지울 때는 파일만.** `isFile()` 을 확인하고 `.mp4`·`.png` 만 지운다. **폴더는 절대 안 건드린다**
- 글로브(`*`)로 쓸어 지우지 않는다. **그 회차에 있어야 할 이름 목록**을 기준으로 고른다
- `올릴글.md` 와 `desktop.ini` 는 남긴다

### 그 밖

- **유튜브 올리기는 «프로젝트 뿌리»에서 돌린다** — `_작업/유튜브_인증.json` 을 상대 경로로 찾는다.
  세로는 제목 끝에 ` #Shorts`, **커스텀 썸네일은 가로에만.**
- **`영상굽기.mjs` 의 결과 폴더(`%TEMP%/cc-vid-w2`)에 다른 것을 두지 마라.** 올릴글을 뒀다가 날렸다.
- **완성화면은 사람이 만든 것이다. 절대 지우지 않는다.**
- **값을 지어내지 않는다.** 색·간격은 `lib/design-presets.ts`, 숫자는 팩의 xlsx 에서 세어 쓴다.

---

## 5. 도구 지도

```
판매용_템플릿/_마케팅/_작업/
  화면캡처.mjs      팩 완성화면 → 영상 재료 캡처 (팩마다 폴더를 갈라)
  영상굽기.mjs      대본 JSON → 세로·가로 (음악까지)
  영상인트로.mjs    세로 맨 앞 2초 표지 (썸네일틀 v=9)
  썸네일굽기.mjs    가로 16:9 썸네일만
  대본_*.json · 인트로설정_*.json · 올릴글_*.md
_작업/유튜브올리기.mjs        (프로젝트 «뿌리»에서 돌린다)
판매용_템플릿/_마케팅/
  왜_만들었나.md    ⭐ 글 쓰기 전에 먼저 읽는다
  본보기_게시글.md  말투 본보기
  썸네일틀.html     표지·썸네일 틀
  영상틀_916/169.html
```

루틴 지시서: `~/.claude/scheduled-tasks/weekly-sns-content/SKILL.md` (933줄)
---

## 6. 검수 촬영 — LMS 두 팩 (2026-08-19)

크롬을 켜 놓고 **링크를 직접 눌러 가며** 검수했다. 주소창으로 건너뛰지 않는다 — 그러면 연결이 안 보인다.

- 검수 기록: `_작업/_촬영검수/LMS_프리미엄.md` (33건) · `_작업/_촬영검수/LMS_디럭스.md` (11건)
- 팩 서버: `.claude/launch.json` 의 **팩전체**(`node _작업/팩서버.mjs 4200`)
  → `http://localhost:4200/<팩이름>/완성화면/` **끝에 / 를 붙여야** 한다

### 두 팩 모두 고쳤다 — 뿌리는 열 가지

`lib/design-presets.ts` 의 **「눌러 봐야만 드러나는 것들」** 10가지에 규칙으로 적어 두었다.
새 팩을 만들 때 그대로 지키면 같은 사고가 안 난다.

### 다음에 꼭 지킬 것 두 가지 (내가 실제로 깨뜨렸다)

1. **공통 CSS 를 여러 팩에 한꺼번에 넣을 때는 레이아웃 갈래를 갈라라.**
   푸터 고정용 `.app{display:flex;flex-direction:column}` 이 사이드바형(`.app` 이 grid 2열)을 깨뜨려
   디럭스 홈이 통째로 뭉개졌다. 규칙은 `body` 기준으로만 쓴다.
2. **숫자 하나를 고치면 그 숫자로 계산되는 값을 전부 따라가라.**
   차시 30 → 18 만 고치고 진도·남은 차시를 안 고쳐서, 화면마다 67% 와 40% 가 갈라졌다.
   차시를 바꿀 때 딸린 것: 총 재생 시간 · N/M차시 · 진도 % · 남은 차시 · 장별 완료 수.

### 아직 남은 것

- 차시를 바꿔도 **딸린 정보**(남은 시간·이어보기 안내·자막)가 안 따라온다 — 차시별 견본 자료가 있어야 고쳐진다
- 나머지 5개 업종 팩(여행·뷰티·인테리어·공동구매·매칭)은 아직 검수 전이다
---

## 7. 검수 루틴 다섯 (2026-08-19 개편)

전에는 셋이었다 — `weekly-pack-review` A/B(화 01:00, 앞·뒤 절반을 «한 줄 한 줄 읽음») + `finish`(화 07:00).
**사람이 읽는다는 전제**였고, 그래서 눌러야만 보이는 것을 33건이나 놓쳤다.

지금은 다섯이다. 모두 `판매용_템플릿/검수공통.md` 를 먼저 읽는다.

| 루틴 | 언제 | 무엇 |
| --- | --- | --- |
| `pack-qa-dispatch` | 화 00:25 | 마지막 검수일 뒤로 **바뀐 것을 찾아 넷에 나눠 준다** → `검수/<날짜>_분배.json` |
| `pack-qa-new` | 화 01:05 | **새로 생긴 판매팩**을 사람처럼 눌러 보며 네 가지로 검수 |
| `pack-qa-spec-a` | 화 01:07 | 새 지시가 **기존 판매팩**에 반영됐는지 — 앞 절반 업종 |
| `pack-qa-spec-b` | 화 01:09 | 〃 — 뒤 절반 업종 (A 지시서를 읽고 셋만 바꿔 따른다) |
| `pack-qa-userpack` | 화 01:11 | **손님이 받는 생성팩** — 변경이 반영됐는지 + 옆 파일로 번지는지 |
| `pack-qa-finish` | 화 07:08 | 넷을 합쳐 포장·zip·시트·총괄보고 (dispatch 지시서의 «② 마무리 회차») |

**검수 항목은 넷뿐이다** (사장님이 정하심):
① 화면연결·레이아웃·UI 를 눈으로 ② 뒤로가기·버튼·탭·썸네일·배너를 **직접 눌러서**
③ 데이터 연동·정상 호출 ④ **나온 오류는 고친다**

**도는 차례** — 기계가 먼저다.
`node _작업/팩검사.mjs` → 검사기들(`check-반응` 포함) → **0 이 된 뒤에** 브라우저로 눌러 본다.
요일·링크·숫자 정합성을 사람 눈으로 세지 않는다.

✅ **완성화면은 일곱 업종 모두 「팩 안이 원본」이다**(2026-08-19 통일).
`build-all.mts` 는 완성화면을 안 건드리고, 전후 지문을 떠서 달라지면 스스로 멈춘다.
`package-template.mts` 의 `siteBase`/`siteDeep` 은 **일곱 업종 다 비어 있다** —
적으면 원본과 대상이 같아져 자기 자신을 덮어쓴다. **새로 적지 마라.**
문서만 다시 구우려면 `build-template.mts <업종>` · `build-template-deep.mts <업종>` 을 쓴다.

옛 루틴 셋은 스케줄에서 뺐고 SKILL.md 파일은 남겨 두었다(되살릴 때 참고).

### 7-1. 첫 `pack-qa-userpack` 이 잡은 것 (2026-08-19)

**규칙을 고칠 때 «예시 CSS»가 규칙을 배신하는 자리가 있다.** 이번에 실제로 그랬다.

`COMMON_RULES` 에 「감싸개(.app)에 세로 flex 를 씌우지 마라」는 경고를 넣으면서,
바로 밑에 `.app { display:grid; grid-template-columns: var(--side-w) 1fr }` 를 예시로 달았다.
그런데 **`COMMON_RULES` 는 모든 팩에 그대로 나간다.** 판매팩 14개 중 11개는
`.app` 을 «세로 flex 감싸개»로 쓰고 `--side-w` 는 사이드바 뼈대에만 있다 —
글은 「건드리지 마라」인데 예시는 감싸개 정의를 통째로 넘겨 주고 있었다.
**만드는 쪽(AI)은 글보다 예시를 따른다.** 예시를 빼고 말로만 남겼다.

같이 배운 것 둘:

- **검사기는 제목을 문자 그대로 찾는다.** §7 이름을 「눌러 보기」에서
  「눈으로 보고, 눌러 보고, 고치기」로 바꿨더니 `check-pack.mts` 가 §7 이 통째로
  없어진 줄 알고 FAIL 했다. 제목 말고 **절 번호(`## 7. 만들고 나서`)** 를 보게 고쳤다.
- **§7 에 시킬 것을 더하면 08 검수시나리오에도 «볼 칸»을 더한다.**
  `template-verify.ts` 안에 그 원칙이 이미 적혀 있는데도 이번에 셋이 비었다 —
  썸네일·배너 눌러보기 / 쪽번호가 결과를 따라가는지 / 화면끼리 숫자·고른 값이 맞는지.
  채워서 공통 점검이 15줄 → 18줄이 됐다.

✅ **판매팩 28칸의 07 은 2026-08-19 마무리 회차가 다시 구웠다.** (전에는 28/28 이 옛 `.app` 규칙이었다)

### 7-2. 첫 `pack-qa-finish` (2026-08-19) — 대기 파일을 안 남기면 고침이 사라진다

**루틴 넷이 팩 10칸을 고쳐 놓고 `검수/대기/` 에는 3칸만 남겼다.**
마무리 회차는 대기 폴더만 보고 굽는다. 나머지 7칸(인테리어 4·장비렌탈 2·매칭 1)은
**고친 것이 zip 에 안 실린 채 옛 파일 그대로 나갈 뻔했다.**

CSV 에는 「고침」이라고 잘 적혀 있었다. **적는 것과 내보내는 것은 다른 일이다.**

막는 장치를 둘 넣었다.

```bash
node _작업/대기점검.mjs      # CSV 의 「고침」 팩 ↔ 검수/대기/ 파일을 견준다
```

- `검수공통.md` 4-3 — 검수 루틴은 **보고를 쓰기 전에** 이것을 돌린다
- `pack-qa-finish` 4-0 — 마무리는 **굽기 전에** 돌리고, 빠진 팩은 CSV 에서 되살려 만든다

### 7-3. 이번에 고친 것 (판매팩 28칸 전부 다시 구움)

| 무엇 | 어디 |
| --- | --- |
| 장비 갈래 칩(텐트·타프…)이 목록을 안 걸렀다 | 장비렌탈 디럭스·프리미엄 5쪽 |
| 「지웠어요」라고 알리고 최근 검색어가 안 지워졌다 | LMS·매칭·여행 프리미엄 각 1쪽 |
| 매장 평점·편의 칩이 목록을 안 걸렀다 | 뷰티샵 디럭스·프리미엄 |
| 07 스펙팩이 옛 `.app` 규칙을 담고 있었다 | **28칸 전부** |

**칩이 안 걸리는 자리는 «자료가 이미 화면에 있었다».**
뷰티는 카드 배지(주차가능·1인샵)에, 장비는 물건 이름(「…4인 텐트」)에 들어 있었다.
없는 것만 뺐다 — 뷰티 「야간 영업」, 장비 「침낭」. **근거 없이 지어 붙이지 않는다.**

⚠ **spec-b 가 매칭 HO0102 를 「칩이 목록을 안 줄인다」고 적은 것은 오진이었다.**
그 화면의 칩은 거르개가 아니라 **검색어 단추**고, 거를 목록 자체가 없다.
칩이 다 같은 칩이 아니다 — 무엇을 하는 칩인지 먼저 본다.

### 7-4. ⚠ 인테리어 네 팩만 git 에 없다 — 사장님이 정하실 일

`.gitignore:45` 가 「판매용_템플릿/」 전체를 무시한다. 그런데 실제로는
**7업종 중 6업종 24칸이 git 에 올라와 있고 인테리어 4칸만 0개다.**
규칙이 생기기 전에 올라간 것은 계속 추적되고, 그 뒤에 만든 인테리어만 규칙에 걸렸다.

- 손님에게 가는 **zip 은 파일에서 굽기 때문에 지금도 문제없다**
- 없는 것은 **«되돌릴 기록»뿐**이다 — 인테리어를 고쳐도 커밋에 안 담긴다

`/admin/qa` 화면에 「기다림」으로 올려 두었다. 둘 중 하나를 고르셔야 한다.
① 인테리어도 올린다(`git add -f`) — 7업종이 같아진다
② 판매팩을 다 뺀다(`git rm --cached`) — 원래 규칙대로지만 24칸을 내려야 한다

### 7-5. 「팩 안이 원본」 통일 — 그리고 README 가 손님에게 거짓말하고 있었다

`package-template.mts` 의 `siteBase`/`siteDeep` 은 완성화면을 **팩 밖**에 두던 시절의
원본 경로다. 뷰티·여행·매칭·공동구매 넷은 아직 그 값을 갖고 있었는데,
**가리키는 폴더 일곱 개가 하나도 없었다.** 죽은 설정이 남아 있던 것이다.
일곱 업종 모두 안 적는 쪽으로 통일했다.

**그 죽은 설정이 손님이 보는 글을 정하고 있었다.**

README 는 「완성화면이 들어 있나」를 `p.sitePath`(=그 설정)로 물었다.
그래서 화면이 **132~207장 들어 있는 팩 넷**이 이렇게 나가고 있었다.

| 팩 | 실제 화면 | README 부제 |
| --- | --- | --- |
| LMS 프리미엄 | 132장 | 「AI팩 — 기획 산출물 한 벌」 |
| 장비렌탈 프리미엄 | 161장 | 〃 |
| 매칭 프리미엄 | 159장 | 〃 |
| 인테리어 프리미엄 | 207장 | 〃 |

부제만이 아니다. 안내에 **「완성된 코드나 사이트를 제공하지 않습니다」**까지 적혀 있었다.
**207장을 주면서 안 준다고 적어 보낸 것이다.** 값어치가 통째로 안 보였다.

이제 «zip 안 `완성화면/pages` 의 HTML 을 직접 세어» 판단한다.
28칸 모두 README 와 실제가 맞는 것을 확인했다.

> 같은 교훈이 «부족한 칸» 셈에는 2026-08-09 에 이미 반영돼 있었다
> (「경로를 적었나」가 아니라 「zip 에 화면이 있나」를 물어야 한다).
> **한 자리를 고칠 때 같은 것을 묻는 다른 자리도 같이 본다.**

### 7-6. 배포까지 마쳤다 (2026-08-19)

40 커밋을 `main` 에 밀어냈고 Vercel 이 production 으로 배포를 마쳤다(커밋 `4bbbf22`).

- `caffeinecolor.com` 홈·판매 화면 응답 확인
- **손님이 받는 zip 28개가 배포 커밋에 담겼다** — 로컬 파일과 해시가 같은 것을 확인했다.
  `packs/` 는 저장소에 함께 두고 `fs-pack-storage` 가 거기서 읽는다.
  그래서 **zip 을 다시 굽고 커밋·push 해야 손님에게 간다**
- `/admin/qa` 는 로그인 화면으로 막힌다(주인만 본다). 사장님이 로그인해 보시면 된다

⚠ `packs/*.zip` 을 커밋 안 하면 홈페이지는 **옛 zip 을 계속 판다.** 파일만 고치면 안 된다.

### 7-7. 검수 결과를 «화면»에 넣었다 — 그리고 CSV 모양이 제각각이었다

사장님이 「시트에 올리거나 `/admin/qa` 에 영역을 만들어 넣어도 된다」고 하셔서 **화면**을 골랐다.
드라이브는 「줄 추가」가 안 돼 매주 새 시트가 쌓이고, 판단하러 들어오시는 자리가 이미 여기다.
「이번에 뭘 고쳤나」와 「내가 정할 것」이 한 화면에 붙는다.

- `lib/qa-record.ts` — `검수기록.csv` 를 회차(날짜)별로 읽는다
- `app/(app)/admin/qa/record-section.tsx` — 회차마다 접힘 하나. 맨 위 회차만 펼쳐 둔다
- 회차 머리에 **고쳤다 / 못 고쳤다 / 나간 팩** 수를 배지로 얹었다.
  ⭐ **「고쳤다」가 있어도 「나간 팩」에 없으면 손님에게는 아직 안 간 것이다**

#### 만들다 걸린 것 — CSV 모양이 루틴마다 달랐다

화면이 CSV 를 읽으려니 **`pack-qa-spec-a` 만 8칸**이었다. 머리줄도 칸 차례도 옛것이고,
`이상없음`·`사람확인필요` 같은 규격 밖 값까지 있었다.

지시서는 「줄 모양은 검수공통 5절」이라고 **제대로 가리키고 있었다.**
그런데 옆 폴더의 `2026-08-18_A.csv` 가 8칸이라 **그것을 본보기로 삼았다.**

> ⭐ **규칙이 글에 있고 옆의 예시가 규칙을 배신하면, 만드는 쪽은 예시를 따른다.**
> 오늘만 세 번째로 나온 모양이다(디자인프리셋 예시 · README 죽은 설정 · 이 CSV).

그 바람에 마무리가 마지막 칸을 「고침여부」로 읽어 **spec-a 의 고침 3칸을 못 봤다.**
고친 팩이 10칸인 줄 알았는데 실은 13칸이었다. (28칸을 전부 다시 구워 결과적으로는 다 나갔다.)

고친 것 넷:

| 무엇 | 어디 |
| --- | --- |
| CSV 규격 검사기 — 머리줄·칸수·고침여부·«등급 없는 팩 이름» | `_작업/검수표검사.mjs` |
| 검수 루틴은 CSV 쓴 뒤, 마무리는 굽기 전에 돌린다 | 검수공통 5절 · finish 4-0 |
| 대기점검이 따옴표를 아는 파서를 쓰게 | `_작업/대기점검.mjs` |
| 옛 모양 CSV 두 개를 `검수/_옛모양/` 으로 치웠다 | 본보기가 안 되게 |

### 7-8. 심사 모드에서 «주인만» 오퍼스 보강이 돌게 했다 (2026-08-19)

사장님이 펫 유치원 팩을 받아 보셨는데 **하이쿠본이 나왔다.** 까닭은 `REVIEW_MODE` 다.

- 「전체 다운로드」는 원래대로 오퍼스를 부른다 — `claude-ia-enricher.ts` 의 `MODEL = "claude-opus-5"`
- 그런데 심사 모드가 **보강 단계를 통째로 건너뛴다**(`application/enrich-download.ts`).
  테스트 키라 결제는 공짜인데 보강은 진짜 돈(1건 3,000~10,000원)이 나가서 손실을 0으로 못 박아 둔 것이다

**막고 있던 것은 코드가 아니라 Vercel 환경변수였다.** 코드는 멀쩡했다.

사장님 지시로 **주인 계정만 예외**로 두었다.

```ts
if (REVIEW_MODE && !isOwner((await getSession())?.user.email)) { …건너뛰기… }
```

- ⚠ **주인이 받을 때는 진짜 돈이 나간다.** 심사관·손님은 그대로 막힌다
- 팔기 전에 손님이 받을 파일을 직접 봐야 하는데 심사 모드가 그것까지 막고 있었다

#### 이미 받은 팩은 다시 받으면 된다

- 크레딧은 두 번 안 빠진다 — 이미 풀린 프로젝트는 차감을 건너뛴다
- 보강 안 된 화면(`enrichedAt` 없음)만 오퍼스로 다시 쓴다

⛔ **PG 승인이 나면 여전히 `REVIEW_MODE` 를 꺼야 한다.** 이 예외는 주인만 여는 것이라,
안 끄면 **손님은 계속 기본본을 받는다.** 끌 때 테스트 크레딧 정리도 잊지 말 것.

### 7-9. 공연 티켓 파트너 관리자 AI팩 (2026-08-19) — ⛔ 2026-08-20 에 접었다

> **이 갈래는 없어졌습니다.** 사장님 말씀 — 「공연은 판매할 거 아니야」.
> 자료·zip·작업 폴더·빌더 자리를 모두 지웠습니다. 아래는 그때 무엇을 했었는지의 기록입니다.

사장님 지시로 만들었다. 티켓링크 같은 예매 플랫폼에 입점한 기획사가 **직접 들어와**
자기 공연 티켓을 등록하고 파는 파트너 관리자(백오피스)다.

**왜 이 팩인가** — 지금 업계는 파트너가 직접 등록하는 구조가 아니다.
티켓링크 공식 안내(2026-08-19 확인)에 「의뢰서·포스터·좌석배치도를 이메일로 접수하면
영업일 3~4일 이내 등록 완료」라고 적혀 있고, 정산도 이메일과 엑셀을 오간다.
이 팩은 그 일을 주최사가 직접 끝내는 셀프서비스로 다시 짠 것이다.

| | 수 |
| --- | --- |
| 메뉴 | 12 |
| 2뎁스 화면 | 46 |
| **3뎁스 화면(프리미엄)** | **228** — 지금까지 가장 큼(전 최대 인테리어 207) |
| 요건 | 1,262 |

**이 업종에만 있는 어려운 것 넷**

- 좌석은 「몇 장 남았나」가 아니라 **「어느 자리가 남았나」**다. 쇼핑몰 재고 모델로는
  시야제한석 하나도 표현이 안 된다 → 격자 좌석 편집기(vn3)와 등급 나누기(vn4)
- 회차 90개짜리 공연을 하나씩 만들 수 없다 → 요일·시간 일괄 생성(sc2), 겹침·공휴일 처리
- **취소 정책은 주최사가 정하는데 예매수수료는 플랫폼 규칙이다.** 두 규칙이 한 화면에서
  부딪히므로 못 바꾸는 항목을 자물쇠로 갈라 두었다(op3)
- 우천 일괄취소는 수수료 0원인데 손님이 직접 취소하면 붙는다. 이 차이가 안 보이면
  반드시 분쟁이 난다 → 사유를 고르면 그 자리에서 알린다(sc4·rs3)

#### ⛔ 판매 목록에는 안 올렸다 (사장님 지시)

**완성화면(HTML)이 아직 없다.** 그런데 디럭스·프리미엄 상세는 「완성 화면 228개」를
약속한다. 그대로 열면 «약속과 실물이 다른» 상태가 된다 — 같은 날 README 에서 겪은 그 문제다.

- `lib/packages.ts` 의 `LISTED_IDS` 에서 뺐다. 판매 목록은 28개 그대로다
- **팩과 zip 은 다 만들어 두었다.** 화면을 만든 뒤 `LISTED_IDS` 에 `"ticket"` 을
  더하면 바로 열린다
- 만들 때 쓰는 것 — `build-template.mts ticket` · `build-template-deep.mts ticket` ·
  `build-design-presets.mts ticket` · `package-template.mts ticket-<등급> --zip`

## 8. SNS 영상 — 마스코트가 움직이는 새 틀 (2026-08-19~20)

사장님이 마스코트 **영상**을 만들어 주시면서 틀을 통째로 다시 잡았다.
전에는 포즈 PNG 를 틀에 구워 넣어 한 칸(1.8~4.3초) 내내 완전히 정지였다.

### 8-1. 마스코트는 «세트»로 받는다 (2026-08-20 개편)

사장님이 `_이미지/0_새로받은것/` 에 **세 개를 한 벌로** 올려 주신다.

| 파일 | 쓰는 곳 |
| --- | --- |
| `<이름>_영상.mp4` | 세로 상단 띠 · 가로 오른쪽 아래 (움직이는 배경) |
| `<이름>_누끼.png` | **커버 마스코트** — 그 편 영상과 같은 장면 |
| `<이름>_배경.png` | (아직 파이프라인에서 안 쓴다) |

**옮기는 곳** — 영상은 `마스코트/영상/<이름>.mp4`, 누끼는 `마스코트/영상/누끼/<이름>.png`.
**이름이 같으면 짝이 맞는다.** 손으로 적는 짝표는 두지 않는다.

⛔ **낱장/ 에서 이름이 비슷한 것을 골라 쓰지 마라.**
거기엔 **은퇴한 고양이**와 지금 캐릭터(긴머리)가 섞여 있다.
2026-08-20 에 `야외독서.mp4` 의 짝으로 `낱장/독서.png` 를 적었다가
커버에 옛 고양이가 나갔다 — 이름만 비슷했지 다른 캐릭터였다.
세트에 누끼가 없는 영상만 `_작업/_커버짝.json` 에 대타를 적고, **`긴머리_` 로 시작하는 것만** 쓴다.

| | |
| --- | --- |
| **받는 크기** | **1080×514** (세로·가로 두 판이 같은 파일 하나를 쓴다) |
| **길이** | 8~12초. 되감기 반복이 영상을 통째로 메모리에 올리므로 길면 무겁다 |
| **구도** | **마스코트를 오른쪽에** — 가로판은 왼쪽을 잘라 쓴다 |

⛔ **513 은 홀수라 mp4 로 못 굽는다**(h264 는 짝수만). 514 로 받아 1px 을 덜어 쓴다.

**어느 것이 뽑히나** — 처음 굽는 편은 `_최근.json` 을 보고 최근 쓴 것을 피해 고른 뒤,
`_편별.json` 에 **편 이름으로 적어 둔다. 다시 구워도 그 영상을 그대로 쓴다.**
일부러 바꾸려면 `_편별.json` 에서 그 편 줄을 지운다.

> ⚠ 2026-08-20 전에는 구울 때마다 다시 뽑았다. 그래서 「예약화면 확인」편이
> **세로는 아침테라스, 가로는 온천**으로 나갔다 — 세로·가로·커버가 따로 구워지기 때문이다.

**워터마크가 박힌 영상** — `마스코트/영상/_워터마크있음/` 에 치워 둔다.
만든 도구의 표식(「?! YEP」)이 왼쪽 아래에 박혀 있으면 상단 띠 안에 그대로 나온다.
⛔ 잘라서 지우지 않는다 — 「자르지 않는다. 높이만 맞춘다」가 굽기의 규칙이다.

**되감아 반복** — 원본 뒤에 역재생을 붙여 한 바퀴를 20초로 만든다.
그냥 반복하면 10초마다 장면이 툭 처음으로 튄다.

### 8-2. 자리 (가이드 실측, `영상틀_새가이드_실측.md`)

**세로 1080×1920**

| 요소 | y |
| --- | --- |
| 마스코트 영상 배경 | 0~**513** |
| 로고 | 258 (회차는 여기 안 둔다) |
| 제목 두 줄 | 329 · 413 — 흰 글자 + **검은 테두리**, 강조는 주황 + 밑줄 |
| 화면 칸 | **513~1353** |
| 자막 두 줄 | 1398 · 1469 |
| 서브카피 | **1620** — 자막 끝에서 104px |

**가로 1920×1080** — 오른쪽 띠 x1392~, 마스코트 영상은 띠 아래 y460~1080(620px).
표(sheet)와 「with. Claude Code」는 뺐다 — 그 자리를 영상이 쓴다.

### 8-3. ⛔ 다시는 밟지 말 것 넷

1. **`세로위`(영상굽기.mjs)와 `--vid-top`(영상틀_916.html)은 같아야 한다.**
   한쪽만 513 으로 고쳤더니 화면 칸이 제목 둘째 줄과 자막을 통째로 덮었다.
2. **제목이 길면 둘째 줄이 화면 칸에 물린다.** 줄 간격을 1.22 로 좁혀 두었다
   (62×1.22×2 + 밑줄 = 493 < 513). 더 늘리지 마라.
3. **칸마다 마스코트를 0초부터 넣으면 안 된다.** 자막 칸마다 ffmpeg 을 따로 돌리는
   구조라, 그렇게 하면 10초 중 앞 4.3초만 스물두 번 반복된다.
   본편 길이만큼 미리 이어 붙여 두고 `-ss` 로 이어지게 한다.
4. **내용 칸에는 PNG 마스코트를 안 그린다.** 상단이 마스코트 영상인데 그 위에 또 앉으면
   둘이 겹친다. 커버와 마무리(`cat:"full"`)에만 남긴다.
5. **커버 마스코트는 «하단 우측» 고정이다.** 설정의 `pos` 는 더 보지 않는다.
   `tr`(우측 위)로 두었더니 세 줄짜리 제목의 오른쪽을 통째로 덮었다
   (2026-08-20 「같은 지시서인데」·「내 사업엔 화면이」). 자리를 옮길 때마다
   제목 아니면 목록 중 하나를 물었다. 자리는 이제 한 곳이다.
6. **로그는 «붙인 파일 이름»을 찍는다.** 설정에 적힌 `pose` 를 찍으면
   실제로 붙은 그림과 달라서 보고가 틀린다 — 2026-08-19 에 사장님께
   엉뚱한 마스코트를 말씀드렸다.

### 8-4. 돌리는 법

```bash
node "판매용_템플릿/_마케팅/_작업/영상굽기.mjs"   "…/_작업/대본_<편>.json"
node "판매용_템플릿/_마케팅/_작업/영상인트로.mjs" "…/_작업/인트로설정_<편>.json"
```

인트로까지 붙인 결과는 `%TEMP%/cc-vid-w2/인트로_<이름>_916.mp4` 다.
가로는 인트로가 없어 `<이름>_169.mp4` 를 그대로 쓴다.

**⛔ 틀을 고쳤으면 «둘 다» 다시 돌린다.** 본편만 다시 굽고 인트로를 안 붙이면
검수기에는 **옛 커버가 그대로** 걸린다. 영상 구멍은 검게 비어 있어서 보기만 해서는 모른다.

`검수보내기.mts` 가 이제 **시각**으로 잡는다(2026-08-20). 이 순서가 아니면 멈춘다:

```
영상틀_916/169.html  →  <이름>_916.mp4 · <이름>_169.mp4
썸네일틀.html        →  인트로_<이름>_916.mp4  (본편보다도 새것이어야 한다)
```

> 사장님: 「커버와 상단 띠를 수정했는데 반영이 안되네 지킴이가 이부분은 안보는거 아닌지 확인 한번 해줘」
> — 맞았다. 그때까지 지킴이는 파일이 **있는지**만 봤지 **언제 구웠는지**는 안 봤다.

⚠ **`_마케팅` 은 .gitignore 대상**이라 `-f` 로 담았다(커밋 9b016f3).
마스코트 mp4 는 안 담는다 — 목록만 `영상/읽어보세요.md` 에 있다.

### 8-5. 드라이브 자리

`G:\내 드라이브\릴스\카페인컬러_주간콘텐츠\<N주차_날짜>\`
이름은 `세로_<제목>.mp4` · `가로_<제목>.mp4` · `썸네일_<제목>.png`.
**드라이브에는 영상만** 올린다(썸네일·캡션은 유튜브 몫).

### 8-6. 올린 것 (2026-08-20)

| 편 | 세로 | 가로 | 상태 |
| --- | --- | --- | --- |
| **다 만들었는데 실제 사이트 배포는 어떻게** | `WbaLJaRr0sE` | `WYyvooB3F20` | **유튜브 비공개** · 드라이브 3주차 |
| AI팩으로 만드니 146개 화면 목록이 나왔어요 | — | — | 사장님이 유튜브 비공개로 올리심 · 드라이브 4주차 |
| 바이브코딩이 놓치는 시작과 끝 | — | — | **이미 공개됨. 건드리지 않는다** |

- 썸네일은 **가로에만** 붙였다(쇼츠는 유튜브가 영상 속 장면을 쓴다)
- 올릴글은 `_작업/올릴글_3주차.md` 118줄부터 — 제목·설명이 이미 준비돼 있었다
- ⛔ **유튜브는 언제나 비공개로 올린다.** 공개 전환은 사장님이 보시고 하신다

#### 지운 것 (드라이브 휴지통에 있다)

같은 편이 이름만 다르게 여러 벌 쌓여 있었다. 아홉 개를 지웠다.

- 4주차 「실제 사이트로 내놓는 방법」 세로·가로 — 3주차 것과 같은 대본인데 자리를 잘못 잡았다
- 4주차 펫유치원 옛판 두 벌(「146개 화면이 나왔어요」·「만들어 졌어요.」)
- 3주차 「내 컴퓨터에서만 보이는 사이트」 세로·가로·썸네일 — 38초 옛판.
  같은 주제 최신판이 「다 만들었는데 실제 사이트 배포는 어떻게」(63.2초)로 있었다

⚠ **한 편이 두 이름으로 굴러다니면 어느 것이 최신인지 알 수 없다.** 길이로 갈랐다.

---

## 2026-08-20 · 「사람이 보듯 눌러 보며 검수」가 붙었다

사장님이 검수 항목을 조목조목 적어 주셨다 — 레이아웃 5, UX 1, UI 7. **모두 「사람이 보는 것처럼
보지 않으면 보기 어려운 것들」**이다.

### 어디에 있나

| 무엇 | 어디 |
|---|---|
| 항목 목록 (규칙 원본, A~H 45개) | `검수항목.md` |
| **새로 붙인 검사기** | `check-눈으로.mts` ← H1~H13 |
| 있던 것들 | `check-레이아웃` `check-반응` `check-사진` `check-design` |

```
npx tsx check-눈으로.mts 매칭_디럭스        (한 팩, 대표 14장)
npx tsx check-눈으로.mts 매칭_디럭스 --전부  (모든 화면)
npx tsx check-눈으로.mts                    (완성화면이 든 팩 전부)
```

### 왜 있던 검사기로는 안 됐나

넷 다 **한 장을 세워 놓고** 잰다. 「옆 화면과 폭이 다르다」·「눌렀더니 화면이 튀었다」·
「끝까지 굴리니 푸터가 버튼에 붙었다」는 **옮겨 다니고 눌러 봐야** 보인다.

⚠ **창 높이를 900 으로 잰다.** 2400 으로 재면 어느 화면이든 자리가 남아
`margin-top:auto` 푸터가 저절로 아래로 밀린다 — 그래서 첫 판이 MY0101 을 못 봤다.

### 고친 것

| 어디 | 무엇이었나 | 어떻게 |
|---|---|---|
| base.css 14벌 | 세로 막대가 «생길 때만» 15px 을 먹어 화면마다 폭이 튐 | `html{scrollbar-gutter:stable}` |
| base.css 14벌 | 내용이 꽉 차면 푸터가 버튼에 맞붙음 | 푸터 앞 덩어리에 `margin-bottom:var(--s8)` |
| 공동구매 promo | `.in` 이 flex 인데 align-items 를 안 줘 배지 767px·버튼 767px | `align-self:flex-start` |
| 매칭 `.unread` | `inline-grid` 라 시각 «옆»에 붙음 | 블록 + 오른쪽 맞춤 |
| 매칭 `.heart.grow` | 동그란 버튼을 늘여 타원 | 넓게 쓸 때만 동그라미를 푼다 |
| 여덟 팩 702장 | 푸터 「카카오톡 상담」·「앱 다운로드」가 `<span class=btn>` — 안 눌림 | 진짜 `<button>` + `data-toast` |
| `.rail` 11벌 | 화살표가 있는데 막대까지 드러남 | 막대만 감춤 |

### ⚠ 아직 안 고친 것 — 사장님이 고르셔야 한다

**H7 「포인트 컬러 위 폰트는 화이트」가 A2 「대비 4.5 넘기」와 어긋난다.**

| | 대비 | 읽히나 |
|---|---|---|
| 코랄 `#D97757` 위 **흰 글자** | 2.96 | ⛔ 안 읽힌다 |
| 코랄 `#D97757` 위 지금 `#111` | 6.38 | ✅ 잘 읽힌다 |

흰 글자로 바꾸려면 **코랄을 더 진하게** 해야 한다(예 `#B5502F` → 흰 글자 대비 5.1).
색을 바꿀지, 어두운 글자를 그대로 둘지는 사장님 몫이다.

### ⚠ 내가 잘못 올린 것

`dd5e52f` 를 만들 때 `git add 판매용_템플릿/_판매팩` 로 폴더째 올렸는데,
그 안에 **내가 손대지 않은 공연티켓 디럭스·스탠다드 파일 94개의 «지움»**이
이미 작업 폴더에 있었고 그것까지 같이 올라갔다.
파일은 `dd5e52f^` 에 그대로 있으니 되살릴 수 있다. **살릴지 사장님이 말씀해 주시면 된다.**

---

## 2026-08-20 · 공연 티켓을 통째로 들어냈다

사장님 말씀 — **「공연은 판매할 거 아니야. 아예 지워줘」.**

전날(8-19)에는 「만들어 두고 진열만 미룬다」로 두었는데, **안 판다는 것과 미룬다는 것은 다르다.**
안 팔 것을 코드에 남겨 두면 다음 사람이 「이건 왜 진열이 안 되지」부터 다시 좇게 된다.

### 지운 것

| 갈래 | 무엇 |
|---|---|
| 자료 | `template-data-ticket.ts` (1,202줄) · `template-data-ticket-deep.ts` (323줄) |
| 압축본 | `packs/ticket-standard·deluxe·plus·premium.zip` (4벌, 1.4MB) |
| 작업 폴더 | `_작업/공연티켓_파트너관리자` (22개) · `_작업/공연티켓_파트너관리자_상세IA` (11개) |
| 판매팩 | `판매용_템플릿/_판매팩/공연티켓_디럭스`·`_스탠다드` (94개, 앞서 지워져 있던 것) |
| 코드 | `lib/packages.ts` 항목 60줄 + import 2줄 · `build-template(-deep).mts` 각 2줄 · `build-design-presets.mts` 15줄 · `package-template.mts` 6줄 |
| 글 | `_부족한구성.md` 의 마지막 한 칸 (이제 **빠진 칸이 없다**) |

### 확인한 것

- `npx tsc --noEmit` — 아무 말 없음
- `/packages` 200, **「공연」이라는 말 0번**, 업종 일곱이 그대로 (여행·뷰티·공동구매·LMS·매칭·장비렌탈·인테리어)

### ⚠ 헷갈리지 말 것

여행 팩 안의 `cat: 'ticket'` 은 **「입장권」**이라는 상품 갈래다. 공연 티켓과 상관없다 — 그대로 둔다.

### 덧 — 반려동물 용품 쇼핑몰 «샘플 산출물»도 지웠다 (2026-08-20)

| | |
|---|---|
| 지운 것 | `[샘플] 반려동물 용품 온라인 쇼핑몰. 회원가입_로그인_산출물/` 7개 + 같은 이름 `.zip` (195KB) |
| 부르는 곳 | **없었다** — 코드·화면·공개 폴더 어디서도 안 가리키고 있었다 |

⚠ **이름이 비슷한 «딴 물건»은 그대로 둔다.** 헷갈리기 쉬운 자리다 —

- `릴스영상/12. AI팩 만드는 과정/1. 반려동물 유치원.mp4` — 마케팅 영상(펫유치원)
- `_이미지/마스코트/…/반려동물케어.png` 세 벌 — 업종 마스코트
- 공동구매 팩 안 「멍냥집사 태호 · 반려동물 용품만 다룹니다」 — 그 팩의 예시 문구

---

## 2026-08-20 · 포인트 색을 진하게, 글자는 흰색으로

사장님 결정 — **「코랄 조금 더 진하게 가고 흰글자로 가자」.**

어긋난 것은 규칙이 아니라 **색**이었다. 「포인트 색 위 흰 글자」와 A2「대비 4.5」가
부딪힌 까닭은 색이 옅어서다. 색기(色氣)는 그대로 두고 **밝기만** 내리니 둘이 같이 선다.

| | 옛 색 | 흰 글자 | 새 색 | 흰 글자 |
|---|---|---|---|---|
| 강조 오렌지 | `#FF7A30` | 2.60 | **`#CC4900`** | 4.65 |
| 강조 코랄 | `#D97757` | 3.12 | **`#C64F28`** | 4.62 |
| 강조 앰버 | `#F59E0B` | 2.15 | **`#A46803`** | 4.61 |
| 강조 노랑 | `#FFD54A` | 1.41 | **`#917000`** | 4.64 |
| 강조 라임 | `#65A30D` | 3.09 | **`#508307`** | 4.57 |
| 강조 틸 | `#0E6F60` | 6.07 | 그대로 | — |
| 주색 코럴선셋 | `#F0654F` | 3.14 | **`#E02A0E`** | 4.66 |
| 주색 레트로 | `#DE6F26` | 3.28 | **`#BC5918`** | 4.58 |

> 주색 둘은 덤이 아니다. 사장님이 이름으로 짚으신 **「코랄」과 「레트로」가 강조색이 아니라 주색**이었다.
> 강조색 다섯을 고치고 나니 그 둘만 남아 드러났다.

### 어디까지 갔나

`lib/design-presets.ts`·`lib/preset-pack.ts`(뿌리) → `base.css` 14벌 →
팩에 딸려 나가는 가이드·스펙팩 106개 → 가이드 안내문 85개
(「흰색이 기본이 아닙니다」 → **「흰색을 쓰세요」**)

### ⛔ 여기서 반드시 조심할 것 — 밝은 바탕과 어두운 바탕은 «반대»다

`accentTextDark` 는 **손대지 않았다.** 어두운 바탕 위 글자용이라 밝은 옛 색이 맞다.

실제로 한 자리가 걸렸다 — 공동구매 디럭스 히어로가 `var(--accent)` 를 **글자**로 쓰고 있어
진하게 한 뒤 **3.83** 까지 떨어졌다. 숫자로는 안 보이고 **찍어 보고서야** 알았다.
`--acc-on-dark` 를 만들어 밝은 옛 색으로 되돌렸다.

> **바탕이 밝으면 진한 글자, 어두우면 밝은 글자.** 한쪽만 보고 고치면 반대쪽이 죽는다.

### 확인한 것

- `npx tsc --noEmit` — 아무 말 없음
- `check-design` — 통과 (남은 경고는 `.warning` 색으로 전부터 있던 것)
- `check-눈으로` 열네 팩 — **H7 23건 → 0건**
- 공동구매 디럭스 홈을 찍어서 눈으로 봤다 — 히어로 값이 밝은 앰버로 살아 있다

### 덧 — 노랑은 되돌렸다 (2026-08-20)

사장님 결정 — **「노랑은 그대로 두고 검은 글씨로 가자」.**

`#FFD54A` 는 진하게 하면 `#917000` 겨자색이 되어 **노란빛을 잃는다.**
노랑은 원래 밝은 색이라 검은 글자가 맞다 — **대비 13.37 로 여덟 색 가운데 가장 잘 읽힌다.**

| | 값 |
|---|---|
| 되돌린 곳 | 뿌리 2개 · `base.css` 2벌(여행 디럭스·뷰티샵 디럭스) · 가이드 10개 = 12개 파일 28줄 |
| 확인 | 여행 디럭스 HO-01 「패스 보기」·「채널 추가하기」 — 바탕 `rgb(255,213,74)` · 글자 `rgb(17,17,17)` · **13.37** |

> **규칙은 「무조건 흰 글자」가 아니다.** 「바탕을 재서 읽히는 쪽을 쓴다」이다.
> 진해진 일곱 색은 흰 글자, 노랑은 검은 글자, 어두운 히어로는 밝은 글자 —
> 셋 다 같은 규칙에서 나온 서로 다른 답이다.

---

## 2026-08-20 · 판매팩 스펙팩에도 검수를 실었다 (⚠ 놓칠 뻔한 자리)

사장님 물음 — **「우리 판매팩 스펙팩에는 들어가 있는거야? 왜냐면 우리가 완성화면을 주지만
유저가 그걸로 따로 만들 수도 있잖아」.**

**안 들어가 있었다.** 28벌 전부 ⛔ 였다.

### 왜 갈렸나 — 갈린 게 아니라 «묵은» 것이었다

`build-template.mts`(판매팩)와 `lib/export/zip-all-button`(손님팩)은 **같은
`buildSpecPackMarkdown` 을 부른다.** 코드는 한 벌이다.
문제는 판매팩의 `07_AI빌드_스펙팩.md` 가 **디스크에 찍혀 있는 파일**이라,
함수를 고쳐도 다시 찍기 전엔 옛것 그대로였다는 것이다.

> **코드를 고쳤다고 «파일»이 바뀌는 것이 아니다.** 찍어 내는 것과 찍혀 있는 것은 다르다.

### 한 일

| 단계 | 무엇 |
|---|---|
| ① | `build-template.mts` · `build-template-deep.mts` 를 **일곱 업종 × 두 뎁스 = 14번** 다시 돌림 |
| ② | 새 스펙팩을 **28개 팩**에 옮김 (2뎁스→스탠다드·디럭스, 3뎁스→플러스·프리미엄) |
| ③ | `완성화면/스펙팩/` 안 **사본 14개**도 같게 맞춤 — 두 벌이 갈리면 안 된다 |
| ④ | **zip 28개를 다시 구움** (`package-template.mts --zip`) |

### ⚠ zip 을 안 구우면 아무 데도 안 간다 — 검수항목 B9

zip 이 **8월 19일 것**이었다. 오늘 고친 색·버튼·배지·CSS 도 **전부 안 들어가 있었다.**
`check-zip날짜` 가 「28개 팩의 zip 이 낡았습니다」로 잡아 줬다.

### 확인한 것 — zip 을 열어서 봤다

- 손님이 받는 zip **28개 모두**에 「7-9 기계로 재기 + 규칙 10가지 + `scrollbar-gutter`」가 들어 있다
- 완성화면이 든 zip **14개 모두**에 오늘 고친 CSS 가 들어 있다
- 다시 구우면서 오늘 고친 CSS 가 덮이지 않았는지도 확인 — **28벌 모두 살아 있다**
  (`완성화면` 은 팩 안이 원본이라 `package-template` 이 안 덮는다)

---

## 2026-08-20 · 루틴 검수에도 전달했다 (⚠ 또 놓칠 뻔한 자리)

사장님 물음 — **「루틴 검수에게도 그 항목들을 전달했던가?」**

**안 했었다.** 검사기를 만들어 놓고 **주간 루틴에는 안 걸어 뒀다.**

### 어디에 걸어야 하나 — 한 벌 자리는 `검수공통.md`

화요일 새벽에 도는 검수 루틴은 여섯이고, **여섯이 모두 `판매용_템플릿/검수공통.md` 를 먼저 읽는다.**
그래서 거기 한 곳만 고치면 다 닿는다.

| 루틴 | 하는 일 |
|---|---|
| `pack-qa-dispatch` | 화 00:30 — 바뀐 것을 찾아 넷에 나눠 준다 |
| `pack-qa-new` | 새로 생긴 팩을 사람처럼 눌러 본다 |
| `pack-qa-spec-a` / `-b` | 새 지시가 기존 팩에 반영됐는지 (앞·뒤 절반) |
| `pack-qa-userpack` | 손님 생성팩에 반영됐는지 |
| `pack-qa-finish` | 화 07:00 — 합쳐 포장·zip·시트 |

### 넣은 것

- **`검수공통.md` 2단계** — `check-눈으로.mts` 추가 (창 높이 900 · `--전부` 주의까지)
- **`검수공통.md` 2단계-덧** — 「내보내기 전에 한 번 더」 절을 새로 만들고
  `check-겉표지` · `check-사진채움` · `check-평평함` · `check-zip` · **`check-zip날짜`** 를 넣음
- **`pack-qa-new`** · **`pack-qa-finish`** — 명령을 따로 적어 둔 자리에도 직접 추가

### ⚠ 여기가 오늘 두 번째로 건진 자리

**`check-zip날짜` 가 검수공통에 없었다.** 그래서 루틴이 한 번도 안 돌렸고,
`packs/` 의 zip 이 **8월 19일 것으로 묵어 있었다.**
`pack-qa-finish` 는 `check-zip`(zip «안»에 파일이 다 있나)만 돌렸지
`check-zip날짜`(zip 이 «지금 팩»으로 구운 것인가)는 안 돌렸다. **둘은 다른 것이다.**

> 손님이 내려받는 것은 폴더가 아니라 **zip** 이다.
> 팩을 고쳤으면 반드시 `npx tsx package-template.mts --zip` 을 돌린다.
> ⚠ `npm run pack` 은 zip 을 **안 굽는다**(검수항목 B9).

### ⚠ 옛 루틴 셋은 지금 안 돈다

`weekly-pack-review` · `-b` · `-finish` 는 폴더만 남아 있고 **예약 목록에 없다.**
`pack-qa-*` 가족이 그 자리를 대신한다. 헷갈리지 말 것.

---

## 2026-08-20 · 올렸다 (푸시 + 배포 확인)

사장님 물음 — **「푸시를 안했다는건 뭐얌?」** 내가 계속 쓰면서 뜻을 안 알려 드렸다.

| | 무엇 |
|---|---|
| **커밋** | 이 컴퓨터 안 작업일지에 「여기까지 고쳤다」고 도장 찍는 것. **밖으로 안 나간다** |
| **푸시** | 그 일지를 GitHub 으로 올려 보내는 것. **올려야 홈페이지가 바뀐다** |

> ⚠ 손님이 받는 zip 은 `packs/` 폴더 파일을 그대로 내주는 구조다
> (`adapters/storage/fs-pack-storage.ts`). **푸시 안 하면 손님은 옛 zip 을 받는다.**

**27개를 올렸다**(`c247fa0..3d37a99`). 사장님 승낙을 받고 했다.

### 올리고 나서 잡은 것 — 색 견본이 갈려 있었다

배포된 홈페이지를 확인하다 **옛 코럴 `#F0654F` 이 살아 있는 것**을 봤다. 세 군데였다.

| 어디 | 무엇이었나 |
|---|---|
| `deliverable-mockups.tsx` | 손님에게 보여 주는 색 견본이 **손으로 적은 값**이라 갈렸다 → 뿌리에서 읽게 고침 |
| `design-presets.ts` 의 `PRESETS` | 강조색 넷이 옛 색 (손님 디자인 가이드가 이 표를 쓴다) |
| `PRIMARY_SWATCHES_BY_STYLE` | 「첫 색 = 그 방향의 주색」인데 코럴·레트로가 옛 색 |

> ⛔ **왜 한 번에 못 고쳤나 — 같은 뜻을 세 가지 이름으로 적어 뒀다.**
> `"accent (강조·배지)"` · `"accent(강조·배지)"` · `"accent(강조)"`.
> 띄어쓰기 있는 것만 잡는 규칙을 썼더니 나머지 둘이 남았다.

### 확인한 것

- 기준(`DESIGN_OPTIONS`) · 손님가이드(`colorsFor`) · 판매팩가이드(`preset-pack`)
  **여섯 테마 × 주색·강조색 열두 자리가 모두 같다** (기계로 견줬다)
- 여섯 세트 모두 **「첫 색 = 그 방향의 주색」**
- **배포된 홈페이지**에서 `#E02A0E` · `#CC4900` · `#C64F28` · `#A46803` 확인, 옛 색 0

---

## 2026-08-25 · 반려견 유치원 41장을 손으로 눌러 봤다 — 그리고 «검수기가 못 보는 자리»를 찾았다

사장님이 「41장 눌러봐줘」 하셔서 한 장씩 눌렀다. **394번 눌렀고 JS 오류는 0건.**
그런데 «눌러도 티가 안 나는» 자리가 여덟 군데 있었다.

### ⛔ 다음 사람이 모르면 사고 나는 것 — 검수기가 «만드는 중»을 못 봤다

**검수기 열한 개가 전부 `_판매팩` 만 보고 있었다.**
만드는 중인 팩(`_만드는중/`)은 옮겨 놓기 전까지 **아무 검수도 못 받는 자리**였다.
반려견 유치원은 8/24 에 41장을 다 만들었는데, 기계가 그 팩을 본 적이 **한 번도 없었다.**

고쳤다. 다만 두 요구가 부딪혀서 자리를 갈랐다.

| 부르는 법 | 보는 곳 | 누가 쓰나 |
|---|---|---|
| `npx tsx check-반응.mts` | `_판매팩` 만 | **루틴** (검수공통 4절 — 만들다 만 팩을 보면 FAIL 이 쏟아진다) |
| `npx tsx check-반응.mts 반려동물케어_디럭스` | `_만드는중` 도 | **만드는 사람** (검수는 만드는 단계 안에) |

여섯 개가 이렇게 돈다 — `check-눈으로 · 레이아웃 · 반응 · 사진 · 사진채움 · 평평함`.
`check-zip날짜 · 겉표지` 둘은 그대로 뒀다 — **아직 안 만든 것을 없다고 나무랄 일이 아니다.**

### ⛔ `git add -A` 가 이틀 새 두 번 남의 일을 삼켰다

작업 폴더는 **하나인데 그것을 보는 손이 여럿**이다.

| 언제 | 무슨 일 |
|---|---|
| 8/24 | 사장님과 하던 작업이 `git add -A` 로 **판매팩 루틴의 반려견 유치원 작업**을 삼켰다 |
| 8/25 | 이번엔 반대로 `pack-qa-new` 가 **「41장 눌러보기」 26개 파일**을 제 커밋에 담았다. 커밋 글은 「이번 주 새 판매팩 없음」인데 알맹이는 41장 검수였다 |

둘 다 갈라서 옮겼다. `판매용_템플릿/검수공통.md` 3절에 못 박았다 —
**`git status --short` 로 먼저 보고, 이름으로 하나씩 담는다. 모르는 파일은 그대로 둔다.**

### 찾아 고친 여덟 군데

| 어디 | 무엇이 샜나 |
|---|---|
| AT-02 | 등원 체크하니 「아직 안 온 아이」는 4→3 인데 **「등원 완료 16」이 그대로**였다. `미체크세기()` 가 한쪽만 고쳤다 |
| AT-02 루키 | 원장 승인으로 잠금을 풀면 단추는 켜지는데 **눌러도 아무 일이 없었다** — 그 단추에만 `data-checkin` 이 없었다 |
| AT-04 | 「보리**을(를)**」 · 「대형반(**15~kg**)」 — `조사()` 가 있는데 안 쓰고 있었다 |
| MY-02 | 회차권을 **8/22 에 샀다는데 쓴 기록이 8/7~8/21** 이다. 90일 규칙대로 구매 8/06 · 만료 11/04 로 |
| NW-04 | 기간 고르개가 **아무 데도 안 붙어** 있었다 |
| PL-03 | 「직접 적기」를 골라도 **적을 칸이 안 나왔다** |
| RE-05 | 할부를 골라도 **달마다 얼마인지 안 나왔다** |
| RE-02 | 시작일을 골라도 **요약에 안 적혔다** |

> ⚠ **붙박이 숫자를 고칠 때 밑값을 조심하라.** AT-02 의 「등원 완료 16」은 명단에
> 보이는 5마리를 **이미 포함한** 수다. 그냥 더하면 21 로 시작한다. 밑값을 빼고 센다.

### 팩 열둘에서 같은 자리가 샜다 — FAQ 의 「＋」

열릴 때 45° 도는데, **세로로 긴 글자 상자를 돌리면 폭이 √2배로 커진다.**
단추 밖으로 4~6px 나갔다. 재서 확인하고 네모난 상자로 바꿨다. 여행 두 팩만 성했다.
**이제 열넷 다 0px.**

### 검수기가 못 가리는 것 — select 는 사람이 다 돌려 봐야 한다

`check-반응` 은 select 를 **한 칸만** 넘겨 본다. 그래서 「고르면 옆이 바뀌어야 하는데
안 바뀌는」 자리를 못 가린다. 12개를 손으로 다 돌려 보니 **셋이 죽어 있었다**
(PL-03 · RE-05 · RE-02). 나머지 아홉은 그냥 «적는 칸»이라 안 바뀌는 게 맞다.

### 재 봤다

41장 394번 다시 눌러 **JS 오류 0** · `check-레이아웃` 15팩 전부 반듯 ·
`check-반응` 죽은 것 0 · `check-눈으로 / 사진 / 사진채움 / 평평함` 모두 깨끗.

커밋 `3cbda9b` · `d28112c`. **푸시는 안 했다** — `pack/petcare-2026-08-24` 는 A 회차 가지다.

---

## 2026-08-25 · 「손님에게 보내는 것」은 마무리 회차 일이다 (사장님 결정)

사장님이 짚으셨다 — **「A·B·NEW 는 검수해서 피니시한테 주고, 피니시는 그걸 올리는 게
역할이잖아. 왜 나에게 올릴 것을 물어봐?」** 맞는 말이라 그렇게 고쳤다.

### 검수 다섯의 몫

| 언제 | 누가 | 무엇을 |
|---|---|---|
| 화 00:30 | dispatch | 지난 검수 뒤로 바뀐 것을 찾아 넷에게 나눠 줌 |
| 화 01:00 | **A · B · NEW · userpack** | **찾아서 고치고** CSV 에 적고, 고친 팩마다 `검수/대기/` 에 쪽지 |
| 화 07:00 | **finish** | 쪽지 있는 팩만 다시 구워 zip 교체 · 커밋 · **밀고 배포 확인** · 시트 |

⚠ **A·B·NEW·userpack 은 여전히 밀지 않는다.** 커밋 번호만 보고에 적어 마무리에 넘긴다.
zip 굽는 동안 옆에서 고치면 반쪽짜리 zip 이 손님에게 나가기 때문이다(자물쇠 `_마무리중.lock`).

### ⛔ 다음 사람이 모르면 사고 나는 것 — 가지를 통째로 합치지 마라

루틴은 **지금 나와 있는 가지에** 커밋을 얹는다. `main` 이 아닐 수 있다.

> **2026-08-25 에 그랬다.** 마침 `pack/petcare-2026-08-24` 가 나와 있어서
> 그날 검수가 고친 것 전부 — 다시 구운 **zip 28벌까지** — 그 가지에 갇혔다.

그런데 **가지를 통째로 합치면 안 된다.** 검수 넷은 다 만들어져 팔리는 팩만 본다
(`_만드는중` 은 안 본다 — 검수공통 4절). 그러니 **검수 결과에는 만드는 중인 업종이 없다.**
그런데 «가지»에는 있다. 통째로 합쳤다면 완성화면도 zip 도 없는 반려동물케어가
**판매 목록에 네 등급 다 떠서 손님이 사도 받을 것이 없게 된다.**
(실제로 `pack-qa-spec-b` 가 그 상태를 B5 로 잡아 뒀다.)

> **커밋을 골라 옮기는 것과 가지를 합치는 것은 다른 일이다.**

그래서 피니시 4-5단계는 **worktree 를 하나 내서 검수 커밋만 골라 옮긴다.**
`git checkout` 으로 가지를 바꾸지 않는다 — 그 가지에서 다른 일이 돌고 있다.

### ⚠ 아직 사람이 해야 하는 것 하나

`.claude/settings.local.json` 에서 `Bash(git push:*)` 가 **「물어보기(ask)」**다.
밤에 혼자 도는 루틴은 그것을 만나면 **답할 사람이 없어 그 자리에 멈춘다.**
`ask` 에서 빼고 `allow` 로 옮겨야 피니시가 스스로 올릴 수 있다.
(그 파일은 에이전트가 못 고치게 막혀 있다 — 사람이 직접 옮겨야 한다.)

---

## 2026-08-25 · 손님에게 나가는 길은 «둘»이다 — 헷갈리면 안 된다

바로 위 절에 「올리는 것은 마무리 회차 일」이라고 적었는데, **그게 전부가 아니다.**
길이 둘이고 **주인이 다르다.**

| 무엇이 나가나 | 누가 올리나 | 언제 |
|---|---|---|
| **새 업종이 처음 팔리는 것** | **`weekly-sales-pack` B 회차** | 월요일 11:20 (2주에 한 번, 그 업종의 마지막 회차) |
| **이미 팔리는 팩을 검수해 고친 것** | **`pack-qa-finish`** | 화요일 07:08 (매주) |

> 「배포는 B 회차가 한다」는 **2026-08-10 사장님 결정**이다. B 회차는 `main` 에 머지하고
> 밀어 실서버에 배포하고 **실서버를 눈으로 확인**한다. 그래서 `git push` 가 그때 풀렸다.

⛔ **그러니 「검수를 통과해야 팔린다」가 아니다.** 팔기 전 관문은 **B 회차 안에** 있다 —
**검사기 4개 FAIL 0 · `check-zip` FAIL 0**, 하나라도 남으면 배포하지 않고 가지에 둔 채 보고한다.
화요일 검수는 **판 뒤에 다시 한 번 보는 것**이다.

### 반려견 유치원(petcare)이 나가는 차례 — 2026-08-31 · 09-01

| 언제 | 누가 | 무엇 |
|---|---|---|
| **월 8/31 11:20** | `weekly-sales-pack` **B 회차** | 프리미엄 172장 → 사진 → 검사 → 두 갈래 검수 → 캡처·영상 → 진열·값 → `_만드는중`에서 `_판매팩`으로 → zip → **머지·배포**. **여기서 팔린다** |
| **화 9/1 00:25** | dispatch | 이제 `_판매팩`에 있으니 **「이번 주 새 판매팩」**으로 잡아 `pack-qa-new` 에 배정 |
| **화 9/1 01:05** | `pack-qa-new` | 반려동물 4등급을 **처음 검수** |
| **화 9/1 07:08** | `pack-qa-finish` | 고친 게 있으면 다시 굽고 zip 갈아 **올림** |

A 회차(8/24)는 끝났다. 남은 것은 지시서의 **`3-f` 프리미엄 완성화면 하나**뿐이다.

### ⛔⛔ 다음 사람이 모르면 사고 나는 것 — 고침의 «절반»은 저절로 안 따라간다

**프리미엄 완성화면은 지금 0장이다.** 8/31 에 새로 만들어진다.

먼저 **프리미엄이 디럭스와 어떤 사이인지** 알아야 한다. 남남이 아니다 —
`template-data-petcare-deep.ts` 머리에 적혀 있다:

> 기존 41화면(`template-data-petcare`)을 **2뎁스 화면으로 재사용하고**,
> 화면마다 탭·상태·세부를 3뎁스 잎사귀로 붙여 실무 IA 해상도로 펼친다.

즉 **프리미엄 172장 = 디럭스의 41장 + 잎사귀 131장**이다. 등원 체크도 반 편성도
프리미엄에 그대로 있다. **다만 파일 이름이 다르다** — 디럭스는 `AT-02.html`,
프리미엄은 `AT0201.html` · `AT0202.html`… 처럼 **잎사귀마다 «별도 파일»**이다.

그래서 8/25 의 고침 여덟은 **두 갈래로 갈린다. 성질이 다르다.**

| | 어디 | 프리미엄에 저절로 가나 |
|---|---|---|
| **기계 장치** | `assets/js/app.js` · `assets/css/base.css` | **가져다 쓰면 간다.** 파일 하나라 통째로 옮겨진다 |
| **화면의 손잡이** | `AT-02` · `MY-02` · `NW-04` · `PL-03` · `RE-02` · `RE-05` **여섯 장** | ⛔ **안 간다.** 프리미엄 잎사귀는 다른 파일이라 **화면마다 다시 달아야 한다** |

⛔ **둘 다 있어야 돈다.** app.js 만 가져오고 손잡이를 안 달면 —
`data-checkin` 없는 등원 체크 단추, `data-filter-sel` 없는 기간 고르개는
**여전히 눌러도 아무 일이 없다.** 8/25 에 루키 단추가 딱 그랬다.

⚠ 그리고 **MY-02 회차권 날짜**는 장치가 아니라 «내용» 흠이다(8/22 에 산 것을 8/7 부터
쓴 것으로 적혀 있었다). 프리미엄 회차권 잎사귀는 **새로 쓰는 글**이라
**같은 실수를 또 할 수 있다.** 날짜는 앞뒤가 맞는지 사람이 세어 봐야 한다.

> **새로 쓰면 프리미엄 172장이 같은 흠을 안고 그날 바로 팔린다.**
> 배포가 B 회차 안에 있으니 **사람이 막을 틈이 없다.**

`판매팩_자동화_지시서.md` 의 `3-f` 에 여덟 군데를 «장치/손잡이»로 갈라 표로 적어 뒀다.

### ⚠ A 회차가 남긴 기록 한 줄이 틀렸다 — 바로잡았다

지시서 `3-h` 에 이렇게 적혀 있었다:

> `check-반응` WARN 10 — select 값만 바뀌는 자리, **재확인 결과 정상**

**정상이 아니었다.** 그중 셋이 진짜로 죽어 있었다 —
PL-03 「직접 적기」를 골라도 적을 칸이 안 나오고, RE-05 할부를 골라도 달마다 얼마인지
안 나오고, RE-02 시작일을 골라도 요약에 안 적혔다.

**까닭 — `check-반응` 은 고르개(select)를 «한 칸만» 넘겨 본다.**
그래서 「고르면 옆이 바뀌어야 하는데 안 바뀌는」 자리를 못 가린다.
**고르개는 사람이 모든 칸을 다 돌려 봐야 안다.** 12개를 다 돌려서 셋을 찾았다.

⚠ 그리고 `3-h` 의 「무인 실행이라 실제 브라우저로는 못 눌러 봤다」도 이제는 아니다.
`mcp__Claude_Browser` 를 설정에서 열어 뒀다. 다만 **무인일 때 화면 캡처는 여전히 안 될 수
있다** — 그때는 `read_page` · `javascript_tool` 로 값을 «재서» 확인한다.
못 했으면 못 했다고 적는다. **모르는 것을 안다고 하지 않는다.**

---

## 2026-08-25 · 마무리가 «두 번» 나간 날 — 검수는 마무리보다 늦게 끝날 수 있다

오늘 판매팩이 두 번 나갔다. 자동으로 한 번, 손으로 한 번.

| 때 | 무슨 일 |
|---|---|
| **07:27** | `pack-qa-finish` 가 제 시각(07:08)에 돌아 **zip 28벌**을 내보내고 `검수/대기/` 를 비웠다 |
| **10:13** | `pack-qa-spec-a` 가 **판단대기 답을 받아** 두 팩을 고치고 대기 2칸을 **새로** 남겼다 |
| **10:52** | 사장님 지시로 그 두 칸만 **손으로 다시 굽고 올렸다** |

### ⚠ 왜 이런 일이 생기나 — 검수가 마무리보다 늦게 끝날 수 있다

검수 넷은 새벽 01:05~01:11 에 «시작»하지만 **끝나는 때는 정해져 있지 않다.**
특히 **`검수/판단대기.json` 에 여쭌 것**은 사장님이 `/admin/qa` 에서 답을 주셔야
고칠 수 있다. 오늘 A 가 「3뎁스 팩에서 탭을 누르면 화면이 93px 쿵 내려간다」를
셋 중 하나로 여쭈었고, 사장님이 ②번을 고르신 것이 **마무리가 나간 뒤**였다.

> **마무리는 07:08 한 번만 돈다.** 그 뒤에 생긴 대기 칸은 그날 안 나간다.

⭐ **다만 잃어버리지는 않는다.** 대기 파일은 그대로 남아 있으므로
**다음 주 화요일 마무리가 집어 간다.** 급하지 않으면 그냥 두면 된다.
급하면 아래대로 손으로 돌린다.

### 마무리를 손으로 돌리는 법 — 오늘 그대로

```bash
# ① 자물쇠부터. 남이 판매팩을 못 건드리게 한다
printf '%s 손수 돌림\n' "$(date '+%F %T')" > 검수/_마무리중.lock

# ② 대기가 CSV 와 맞는지
node _작업/검수표검사.mjs && node _작업/대기점검.mjs

# ③ 대기 파일의 «상품키»로 그 칸만 굽는다. ⚠ --zip 을 빼면 옛 zip 이 그대로 나간다
npx tsx package-template.mts travel-premium --zip

# ④ 검사 — 뒤로가기 줄을 뺐다면 외톨이·끊어진 링크를 특히 본다
node _작업/팩검사.mjs 여행_프리미엄
npx tsx check-zip.mts && npx tsx check-zip날짜.mts

# ⑤ 나간 기록을 «남기고 나서» 대기 파일을 지운다. 순서를 바꾸지 마라
# ⑥ 자물쇠를 지운다
rm -f 검수/_마무리중.lock
```

### ⭐ 굽기 전후로 «도장»을 떠서 견준다

`package-template.mts` 가 완성화면을 덮어쓸까 봐 걱정할 필요는 없다 —
**원본 폴더가 있을 때만 복사**하고, 없으면 팩에 있던 것을 그대로 둔다.
그래도 **말로 믿지 말고 재라.** 오늘 A 가 고친 다섯 장을 이렇게 견줬다.

```bash
git hash-object <고친 파일들>    # 굽기 전
# … 굽고 …
git hash-object <고친 파일들>    # 굽기 뒤 — 같아야 한다
```

다섯 다 그대로였다. 그리고 **zip 을 열어** 안에 든 것까지 확인했다.

### ⭐ 나간팩 기록은 «덮어쓰지» 않는다 — 판을 나눠 적는다

`검수/나간팩_<날짜>.json` 은 하루에 두 번 나갈 수 있다.
덮어쓰면 **아침에 무엇이 나갔는지가 사라진다.** 오늘은 `두번째판` 키를 따로 만들어
「나간때 · 까닭 · 나간팩 · 검사」를 적었다. 첫 판은 손대지 않았다.
