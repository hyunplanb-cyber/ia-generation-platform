# 회원끼리 사고파는 중고거래 장터 — AI 빌드 스펙팩

> 이 문서 하나로 사이트를 만들 수 있게 정리한 스펙이에요. 화면별 **생성 프롬프트**를 그대로 사용하세요.

## 1. 프로젝트 개요
- **컨셉**: 회원 누구나 쓰던 물건을 올리고 회원끼리 사고파는 동네 중고거래 장터. 파는 쪽은 사진 여러 장과 물건 상태·직거래 가능 지역을 적어 판매글을 올리고, 사는 쪽은 내 동네를 인증해 거리순으로 매물을 훑어보고 1:1 채팅으로 값을 흥정하고 약속을 잡는다. 직거래 대신 택배로 보낼 때는 안전결제로 플랫폼이 돈을 잠깐 들고 있다가 물건을 받았다고 하면 보낸다. 거래가 끝나면 사는 쪽과 파는 쪽이 서로 후기를 남겨 매너 점수가 쌓이고, 사기·금지품목은 신고를 받아 운영자가 처리한다. 회원이 쓰는 매물 찾기·판매글 등록·채팅·안전결제 화면부터 운영자가 쓰는 신고 처리 대기열·정산·분쟁 조정까지, 두 쪽이 쓰는 화면 전체를 담는다.
- **디자인 컨셉**: 쓰던 물건 사진이 주인공이라 배경은 중립으로 두고, 포인트는 코럴 선셋(#E02A0E — 배경·배지·큰 글자용, 본문 글자에는 ink #33221E) 하나. 매물 목록은 썸네일 왼쪽·정보 오른쪽 가로 행으로 한 화면에 많이 담고, 가격과 「몇 분 전」·거리는 크고 또렷하게. 채팅과 안전결제 단계는 지금 어디까지 왔는지가 한눈에 보이게
- **디바이스**: 반응형
- **전체 일정**: 2026-09-07 ~ 2026-11-20

## 2. 공통 요소 (모든 화면 공통)
> 아래 요소는 **한 번만 만들어 모든 화면에서 재사용**하세요. 각 화면 스펙(4장)은 이 공통 요소 위에 올라가는 콘텐츠만 설명합니다.
- **영역이 둘입니다. 헤더를 각각 따로 만드세요.**
  - **손님 화면 GNB**: `홈` · `매물 찾기` · `판매하기` · `끌올·광고` · `채팅` · `안전결제` · `마이페이지` · `고객센터`
  - **운영자 화면 GNB**: `운영자` — 좌측 사이드바 + 상단 바 형태가 맞습니다
  - 두 영역을 오가는 길은 **하나만** 둡니다 — 손님 화면 우측 상단에 운영자 영역으로 넘어가는 버튼 하나(예: `운영자 화면으로`), 운영자 화면 우측 상단에 `고객 화면 보기`. 한 계정이 두 권한을 가지며(크몽·숨고와 같은 방식), 아직 권한이 없으면 전환 대신 **신청 화면으로 보냅니다.**
- **콘텐츠 영역**: 최대 1440px, 좌우 여백 24px — 헤더·본문·푸터·하단 고정 바가 모두 이 폭을 쓴다. 화면마다 따로 정하지 않는다. 화면마다 폭을 새로 정하면 위아래가 어긋납니다.
- **읽기 폭**: 글이 주인공인 자리(상세 본문·안내·약관)는 최대 760px로 좁혀 콘텐츠 영역 **안에서** 가운데 정렬 — 카드 격자에는 쓰지 않습니다. 아래 「읽기 폭」 참고.
- **헤더**: 로고 + 검색 + 상단 내비게이션(GNB)
- **푸터**: 저작권 · 기본 링크
- **재사용 컴포넌트**: 반복되는 UI(상품/항목 **카드**, 리스트 **행**, **폼 필드**, **버튼**)는 공용 컴포넌트로 만들어 화면마다 재사용하세요.

## 3. 메뉴 구조
- **[HO] 홈** (Home)
  - `HO-01` 홈
  - `HO-02` 카테고리 전체 보기
  - `HO-03` 안전거래·이용안내
  - `HO-04` 공지·이벤트 목록
- **[SE] 매물 찾기** (Search)
  - `SE-01` 내 동네 설정
  - `SE-02` 매물 목록
  - `SE-03` 검색
  - `SE-04` 매물 상세
  - `SE-05` 결과 없음
- **[SL] 판매하기** (Sell)
  - `SL-01` 판매글 작성
  - `SL-02` 사진 등록·정리
  - `SL-03` 얼마에 팔까 — 시세 보기
  - `SL-04` 등록 완료
  - `SL-05` 내 판매글 관리
- **[BS] 끌올·광고** (Boost)
  - `BS-01` 끌올하기
  - `BS-02` 광고 상품·요금 안내
  - `BS-03` 끌올·광고 내역
- **[CH] 채팅** (Chat)
  - `CH-01` 채팅 목록
  - `CH-02` 채팅방
  - `CH-03` 거래 약속 잡기
  - `CH-04` 신고·차단
- **[PA] 안전결제** (Escrow)
  - `PA-01` 안전결제 시작·안내
  - `PA-02` 결제
  - `PA-03` 발송 등록 (판매자)
  - `PA-04` 수령 확인·구매확정
  - `PA-05` 거래 진행 상태
- **[MY] 마이페이지** (Mypage)
  - `MY-01` 내 프로필·매너 점수
  - `MY-02` 구매 내역
  - `MY-03` 찜한 물건
  - `MY-04` 거래 후기 쓰기
  - `MY-05` 받은 후기·매너 점수 상세
  - `MY-06` 관심 키워드·알림 설정
- **[AD] 운영자** (Admin)
  - `AD-01` 신고 처리 대기열
  - `AD-02` 신고 상세·조치
  - `AD-03` 금지품목·거래규칙 관리
  - `AD-04` 안전결제 정산·분쟁
  - `AD-05` 운영자 로그인
- **[CS] 고객센터** (Support)
  - `CS-01` 자주 묻는 질문
  - `CS-02` 1:1 문의
  - `CS-03` 공지사항 상세

## 4. 화면별 스펙
### HO-01 · 홈
- **소속 메뉴**: 홈
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-09-07 ~ 2026-09-10
- **기능정의**: 상단 동네 이름과 범위 선택(내 동네·가까운 동네·조금 먼 동네) · 검색창 · 카테고리 가로 스크롤 12종 · 오늘 올라온 매물 가로 행 목록(썸네일·제목·가격·동네·몇 분 전·찜 수) · 거리순 정렬 기본 · 인기 매물 묶음 · 끌올된 글 배지 · 안전결제 가능 배지 · 판매글 쓰기 떠 있는 버튼 · 신고·사기 예방 안내 띠 · 공지·이벤트 미리보기 3개
- **버튼 → 이동화면**:
  - `매물 자세히 보기` → `SE-04` 매물 상세
  - `내 동네 설정` → `SE-01` 내 동네 설정
  - `카테고리 전체 보기` → `HO-02` 카테고리 전체 보기
  - `판매글 쓰기` → `SL-01` 판매글 작성
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 정렬 — 고른 기준으로 **순서를 실제로 바꿔** 다시 그립니다
  - 검색 — 입력한 말이 든 것만 남깁니다. 없으면 「검색 결과가 없습니다」와 다시 찾는 길을 보여줍니다
  - 선택 — 고른 개수를 **숫자로 보여주고**, 하나도 안 골랐으면 일괄 단추를 눌리지 않게 둡니다
- **생성 프롬프트**:

  ```
  회원끼리 사고파는 동네 중고거래 장터의 홈 화면을 만들어줘. 배너로 시작하지 말고 매물 목록이 첫 화면의 주인공이 되게 해줘. 1) 상단 GNB — 로고, 검색창, 카테고리, 채팅, 마이페이지, [판매글 쓰기] 버튼 2) 동네 줄 — 지금 설정된 동네 이름과 범위 칩 3개(내 동네 / 가까운 동네 / 조금 먼 동네). 칩을 누르면 아래 목록이 다시 채워진다는 것이 보이게, 각 칩에 건수 배지 3) 카테고리 가로 스크롤 — 디지털기기·생활가전·가구인테리어·의류·유아용품·도서·스포츠·취미·반려동물용품·식물·티켓교환권·기타 4) 매물 목록 — 썸네일을 왼쪽에, 정보를 오른쪽에 두는 가로 행으로 12~16개. 한 행에 제목, 굵은 가격, 「OO동· 3분 전」, 오른쪽 끝에 찜 하트와 채팅 수. 상태 배지(예약중·거래완료)는 썸네일 위 왼쪽 모서리에 5) 끌올된 글은 목록 위쪽에 두고 「끌올」 배지를 작게 붙여줘 — 광고라는 것을 숨기지 않는다 6) 안전결제가 되는 매물에는 「안전결제」 배지 7) 사기 예방 안내 띠 — 「시세보다 너무 싼 물건은 의심하세요」 같은 짧은 문구와 자세히 보기 링크 8) 공지·이벤트 미리보기 3개 9) 화면 오른쪽 아래에 항상 떠 있는 [판매글 쓰기] 둥근 버튼 10) 하단 푸터 가격과 「몇 분 전」·동네 이름이 가장 먼저 읽히게 해줘. 물건 사진의 품질이 제각각이라는 것을 전제로, 썸네일 크기를 모두 같게 맞추고 비율이 다른 사진은 잘라서 채워줘.
  ```

### HO-02 · 카테고리 전체 보기
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈` — 누르면 `HO-01`로 간다
- **일정**: 2026-09-11 ~ 2026-09-15
- **기능정의**: 대분류 12종 왼쪽 세로 목록 · 고른 대분류의 소분류 격자 · 분류별 등록 매물 수 · 분류별 대표 매물 미리보기 4개 · 이 분류에서 자주 찾는 검색어 · 거래 금지 품목 안내 링크 · 최근 본 분류 기록
- **버튼 → 이동화면**:
  - `이 분류 매물 보기` → `SE-02` 매물 목록
  - `거래 금지 품목 보기` → `HO-03` 안전거래·이용안내
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 분류 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
  - 검색 — 입력한 말이 든 것만 남깁니다. 없으면 「검색 결과가 없습니다」와 다시 찾는 길을 보여줍니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 카테고리 전체 보기 화면을 만들어줘. 1) 왼쪽에 대분류 세로 목록 12개(디지털기기·생활가전·가구인테리어·의류·유아용품·도서·스포츠·취미·반려동물용품·식물·티켓교환권·기타), 지금 고른 것은 색으로 구분 2) 오른쪽에 그 대분류의 소분류를 격자로 — 예: 디지털기기 → 휴대폰·노트북·태블릿·카메라·게임기·음향기기·PC부품 3) 소분류마다 「1,204개」처럼 등록 매물 수를 작게 4) 소분류를 고르면 아래에 그 분류의 대표 매물 4개가 가로 행으로 미리보기 5) 「이 분류에서 자주 찾는 말」 칩 6~8개 6) 아래쪽에 거래 금지 품목 안내 박스 — 어떤 것은 올릴 수 없는지 짧게, 자세히 보기 링크 분류가 많으니 글자 크기를 절제하고, 지금 어디를 보고 있는지가 왼쪽 목록에서 늘 보이게 해줘.
  ```

### HO-03 · 안전거래·이용안내
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈` — 누르면 `HO-01`로 간다
- **일정**: 2026-09-16 ~ 2026-09-18
- **기능정의**: 이 장터가 어떻게 돌아가는지 3단계 그림(올린다-이야기한다-거래한다) · 직거래와 안전결제 차이 표 · 안전결제 단계별 설명(결제-발송-수령확인-정산) · 자주 나오는 사기 유형과 대처법 · 거래 금지 품목 목록 · 신고하면 어떻게 되는지 절차 · 매너 점수가 무엇인지 · 분쟁이 생기면 어디로 가는지
- **버튼 → 이동화면**:
  - `안전결제 자세히 보기` → `PA-01` 안전결제 시작·안내
  - `자주 묻는 질문` → `CS-01` 자주 묻는 질문
- **생성 프롬프트**:

  ```
  중고거래 장터의 안전거래·이용안내 화면을 만들어줘. 「믿고 거래해도 되는 곳」이라는 인상이 목적이다. 1) 상단에 3단계 그림 — ① 판매글을 올린다 ② 채팅으로 이야기한다 ③ 직거래 또는 안전결제로 거래한다 2) 직거래 vs 안전결제 비교 표 — 수수료, 돈이 언제 넘어가나, 문제가 생기면 누가 도와주나 3) 안전결제 단계 아코디언 4개 — 결제(플랫폼이 보관) → 발송(운송장 등록) → 수령 확인(구매확정) → 판매자 정산. 각 단계에 「이때 돈은 어디에 있나」를 한 줄로 4) 사기 유형 탭 3개 — 선입금 유도 / 가짜 안전결제 링크 / 물건 바꿔치기. 탭마다 실제 대화 예시와 대처법 5) 거래 금지 품목 목록 — 표로, 왜 안 되는지 한 줄씩 6) 신고 절차 그림 — 신고 → 접수 → 검토 → 조치(경고·정지·삭제), 처리까지 걸리는 시간 안내 7) 매너 점수 설명 — 후기가 쌓여 점수가 되고, 낮으면 어떤 제한이 있는지 8) 분쟁 조정 안내 — 안전결제 거래에서 다툼이 생기면 운영자가 어떻게 개입하는지 글이 많은 화면이니 아코디언과 표로 나눠서 훑어읽을 수 있게 해줘.
  ```

### HO-04 · 공지·이벤트 목록
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈` — 누르면 `HO-01`로 간다
- **일정**: 2026-09-21 ~ 2026-09-23
- **기능정의**: 분류 탭(전체·공지·이벤트·제재안내) · 중요 공지 상단 고정 · 제목·작성일·조회수 목록 · 진행 중 이벤트 배지와 남은 기간 · 지난 이벤트 흐림 처리 · 쪽 번호 · 검색
- **버튼 → 이동화면**:
  - `공지 자세히 보기` → `CS-03` 공지사항 상세
  - `홈으로` → `HO-01` 홈
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 분류 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
  - 검색 — 입력한 말이 든 것만 남깁니다. 없으면 「검색 결과가 없습니다」와 다시 찾는 길을 보여줍니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 공지·이벤트 목록 화면을 만들어줘. 1) 상단 탭 — [전체] [공지] [이벤트] [제재안내], 탭마다 건수 배지 2) 중요 공지 2개는 위쪽에 배경색을 달리해서 고정 3) 목록 — 제목, 분류 배지, 작성일, 조회수. 새 글에는 N 표시 4) 이벤트 글에는 「D-5」처럼 남은 기간 배지, 끝난 이벤트는 흐리게 하고 「종료」 배지 5) 제재안내 분류에는 「이런 글은 삭제됩니다」 같은 운영 정책 공지 6) 아래에 쪽 번호, 위에 검색창 목록이 주인공이니 한 화면에 12~15줄이 들어가게 행 높이를 일정하게 해줘.
  ```

### SE-01 · 내 동네 설정
- **소속 메뉴**: 매물 찾기
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-09-24 ~ 2026-09-28
- **기능정의**: 현재 위치로 찾기 버튼 · 동네 이름 직접 검색 · 지도 위 선택한 동네 표시 · 범위 슬라이더(내 동네·가까운 동네·조금 먼 동네) · 그 범위에 잡히는 동네 이름 목록 · 범위별 매물 수 미리보기 · 동네 인증(그 자리에서 위치 확인) 안내 · 동네는 2개까지 등록 가능 · 인증 없이 쓸 때의 제한 안내
- **버튼 → 이동화면**:
  - `이 동네 매물 보기` → `SE-02` 매물 목록
  - `홈으로` → `HO-01` 홈
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 검색 — 입력한 말이 든 것만 남깁니다. 없으면 「검색 결과가 없습니다」와 다시 찾는 길을 보여줍니다
  - 선택 — 고른 개수를 **숫자로 보여주고**, 하나도 안 골랐으면 일괄 단추를 눌리지 않게 둡니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 「내 동네 설정」 화면을 만들어줘. 이 서비스에서 가장 먼저 하는 일이다. 1) 상단 안내 — 「내 동네를 정하면 가까운 물건부터 보여드려요」 2) [현재 위치로 찾기] 큰 버튼과, 아래에 동네 이름 직접 검색 입력칸 3) 지도 영역 — 고른 동네에 핀을 찍고 범위만큼 원을 그린다. (지도는 이미지가 아니라 자리표로 두고, 「지도 영역· 권장 1200×600」이라고 적어줘. 사진을 끼우지 않는다) 4) 범위 슬라이더 3단계 — 내 동네 / 가까운 동네 / 조금 먼 동네. 움직이면 원 크기와 「이 범위에 매물 3,420개」 숫자가 함께 바뀐다 5) 그 범위에 들어오는 동네 이름을 칩으로 나열 — 몇 개인지 함께 6) 동네 인증 안내 박스 — 「그 동네에서 한 번 위치를 확인해야 글을 쓸 수 있어요」, 인증 안 하면 무엇이 막히는지(판매글 쓰기·채팅) 목록으로 7) 「동네는 2개까지 등록할 수 있어요」 안내와, 이미 등록한 동네 카드 8) 하단 [이 동네로 설정] 버튼 — 동네를 안 고르면 잠겨 있고 「동네를 먼저 골라주세요」라고 알린다 지도가 주인공이지만 지도 없이도 동네 이름 검색만으로 끝낼 수 있게 두 길을 다 열어줘.
  ```

### SE-02 · 매물 목록
- **소속 메뉴**: 매물 찾기
- **뒤로가기**: 좌측 상단에 `‹ 내 동네 설정` — 누르면 `SE-01`로 간다
- **일정**: 2026-09-29 ~ 2026-10-02
- **기능정의**: 왼쪽 필터 패널(카테고리·가격 범위·물건 상태·거래 방식·동네 범위·거래완료 숨기기) · 오른쪽 가로 행 목록 · 정렬 드롭다운(거리순·최신순·낮은 가격순·인기순) · 상단 적용된 필터 칩과 전체 해제 · 결과 건수 · 끌올된 글 상단 노출과 배지 · 상태 배지(판매중·예약중·거래완료) · 더 보기 방식 페이지 넘김 · 관심 키워드 알림 걸기 버튼
- **버튼 → 이동화면**:
  - `매물 자세히 보기` → `SE-04` 매물 상세
  - `검색으로` → `SE-03` 검색
  - `알림 조건 저장` → `MY-06` 관심 키워드·알림 설정
  - `결과 없을 때` → `SE-05` 결과 없음
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 필터 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
  - 정렬 — 고른 기준으로 **순서를 실제로 바꿔** 다시 그립니다
  - 더 보기 — 누르면 그 자리에서 펴지고 접힙니다. 다른 화면으로 가지 않습니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 매물 목록 화면을 만들어줘. 목록 중심형 골격이라 히어로를 두지 말고 제목과 필터로 바로 시작해줘. 1) 왼쪽 필터 사이드바 — 카테고리 접기 목록 / 가격 최저·최고 입력칸 / 물건 상태 체크(새것·거의 새것·사용감 있음·고장·부품용) / 거래 방식 체크(직거래·택배·안전결제) / 동네 범위 라디오 3개 / [거래완료 숨기기] 토글 2) 오른쪽 위 — 결과 건수 「3,420개」와 정렬 드롭다운(거리순·최신순·낮은 가격순·인기순) 3) 그 아래 적용된 필터 칩 줄 — 칩마다 X, 오른쪽 끝에 [전체 해제] 4) 목록 — 가로 행. 왼쪽 정사각 썸네일, 가운데 제목과 「OO동· 12분 전· 1.2km」, 굵은 가격, 오른쪽 끝에 찜 하트와 채팅 수. 행 높이는 모두 같게 5) 끌올된 글 2~3개는 목록 맨 위에 「끌올」 배지를 달고 먼저 6) 예약중·거래완료는 썸네일 위에 반투명 덮개와 배지, 거래완료는 가격에 취소선 7) 목록 아래 [더 보기] 버튼과 쪽 번호 8) 오른쪽 위에 [이 검색 조건으로 알림 받기] 버튼 — 새 매물이 올라오면 알려준다는 뜻 필터를 눌렀을 때 목록이 실제로 줄어드는 것이 이 화면의 알맹이다. 건수와 목록이 늘 같은 말을 하게 해줘.
  ```

### SE-03 · 검색
- **소속 메뉴**: 매물 찾기
- **뒤로가기**: 좌측 상단에 `‹ 내 동네 설정` — 누르면 `SE-01`로 간다
- **일정**: 2026-10-05 ~ 2026-10-07
- **기능정의**: 큰 검색 입력칸 · 자동완성 후보와 후보별 매물 수 · 최근 검색어 칩(개별 삭제·전체 삭제) · 인기 검색어 10개 순위 · 카테고리 안에서만 찾기 토글 · 검색 도움말(따옴표·제외어) · 검색 결과 미리보기 상위 5개
- **버튼 → 이동화면**:
  - `결과 전체 보기` → `SE-02` 매물 목록
  - `결과 없을 때` → `SE-05` 결과 없음
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 검색 — 입력한 말이 든 것만 남깁니다. 없으면 「검색 결과가 없습니다」와 다시 찾는 길을 보여줍니다
  - 토글 — 누르면 **켜짐·꺼짐이 눈에 보이게** 바뀌고 그대로 남습니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 검색 화면을 만들어줘. 1) 화면 위쪽에 큰 검색 입력칸 하나. 입력하면 아래에 자동완성 후보가 뜨고, 후보마다 「아이폰 15· 412개」처럼 건수 2) 최근 검색어 — 칩으로 나열, 칩마다 X, 오른쪽에 [전체 삭제] 3) 인기 검색어 — 1~10위 순위 목록, 순위 변동 화살표 4) 「이 카테고리 안에서만 찾기」 토글과 카테고리 선택 5) 검색 도움말 접기 — 여러 낱말을 함께 찾는 법, 빼고 찾는 법 6) 검색어를 넣으면 아래에 결과 상위 5개를 가로 행으로 미리 보여주고 [결과 전체 보기] 버튼 검색창이 화면의 주인공이다. 다른 것은 모두 그 아래에 작게 둬줘.
  ```

### SE-04 · 매물 상세
- **소속 메뉴**: 매물 찾기
- **뒤로가기**: 좌측 상단에 `‹ 내 동네 설정` — 누르면 `SE-01`로 간다
- **일정**: 2026-10-08 ~ 2026-10-13
- **기능정의**: 사진 여러 장 넘겨보기와 장수 표시 · 제목·가격·카테고리·등록 시각 · 물건 상태 배지와 사용 기간 · 상세 설명 · 직거래 가능 지역과 지도 자리 · 거래 방식(직거래·택배·안전결제) 표시 · 판매자 카드(닉네임·매너 점수·거래 횟수·응답률) · 판매자의 다른 매물 · 조회·찜·채팅 수 · 하단 고정 액션 바(찜·채팅하기·안전결제로 사기) · 신고하기 · 비슷한 매물 추천
- **버튼 → 이동화면**:
  - `채팅하기` → `CH-02` 채팅방
  - `안전결제로 사기` → `PA-01` 안전결제 시작·안내
  - `판매자 프로필 보기` → `MY-01` 내 프로필·매너 점수
  - `신고하기` → `CH-04` 신고·차단
- **생성 프롬프트**:

  ```
  중고거래 장터의 매물 상세 화면을 만들어줘. 사는 쪽이 「이 사람에게 사도 되나」를 판단하는 화면이다. 1) 상단 사진 넘겨보기 — 좌우 화살표와 아래 점 표시, 오른쪽 위에 「3/7」 장수 표시 2) 제목과 굵은 가격, 그 옆에 「가격 제안 받아요」 배지(판매자가 켠 경우) 3) 카테고리· 「OO동· 2시간 전」· 조회 124· 찜 8· 채팅 3 4) 물건 상태 배지(예: 「거의 새것」)와 「사용 기간 3개월」 5) 상세 설명 본문 — 여러 줄, 길면 [더 보기]로 펼침 6) 거래 방식 표 — 직거래 가능 지역(동네 이름과 「지도 영역· 권장 800×400」 자리표), 택배 가능 여부와 배송비, 안전결제 가능 여부 7) 판매자 카드 — 프로필 사진, 닉네임, 매너 점수(예: 42.5°처럼 온도로), 거래 횟수, 응답률과 평균 응답 시간, [판매자의 다른 매물 보기] 8) 판매자의 다른 매물 4개 가로 행 9) 「비슷한 매물」 추천 4개 10) 화면 맨 아래 고정 액션 바 — 왼쪽에 찜 하트, 가운데 [채팅하기], 오른쪽에 [안전결제로 사기] 11) 오른쪽 위 점 세 개 메뉴 — 신고하기· 이 사용자 글 안 보기· 공유 사진과 가격이 먼저 읽히고, 판매자를 믿을 만한지는 스크롤하면 바로 나오게 배치해줘.
  ```

### SE-05 · 결과 없음
- **소속 메뉴**: 매물 찾기
- **뒤로가기**: 좌측 상단에 `‹ 내 동네 설정` — 누르면 `SE-01`로 간다
- **일정**: 2026-10-14 ~ 2026-10-16
- **기능정의**: 찾는 것이 없다는 안내와 지금 걸린 조건 나열 · 조건을 하나씩 푸는 버튼과 풀었을 때 잡히는 건수 · 동네 범위를 넓혀 보라는 제안 · 이 조건으로 알림 받기 · 비슷한 검색어 제안 · 인기 매물로 돌아가기
- **버튼 → 이동화면**:
  - `조건 다시 잡기` → `SE-02` 매물 목록
  - `알림 설정하기` → `MY-06` 관심 키워드·알림 설정
  - `홈으로` → `HO-01` 홈
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 검색 — 입력한 말이 든 것만 남깁니다. 없으면 「검색 결과가 없습니다」와 다시 찾는 길을 보여줍니다
- **생성 프롬프트**:

  ```
  중고거래 장터에서 검색·필터 결과가 하나도 없을 때 나오는 화면을 만들어줘. 빈 화면으로 끝내지 말고 다음 행동을 주는 것이 목적이다. 1) 가운데에 짧은 안내 — 「'OOO' 조건에 맞는 매물이 없어요」 2) 지금 걸려 있는 조건을 칩으로 나열하고, 칩마다 [풀기]. 풀면 「이 조건만 풀면 18개」처럼 잡히는 건수를 미리 보여준다 3) 「동네 범위를 넓혀볼까요?」 카드 — 가까운 동네로 넓히면 몇 개인지 함께 4) [이 조건으로 새 매물 알림 받기] 큰 버튼 — 켜면 「올라오면 알려드릴게요」로 바뀐다 5) 비슷한 검색어 제안 칩 5개 6) 아래에 「지금 인기 있는 매물」 4개 가로 행 안내 문구는 짧게, 다음에 누를 것이 분명하게 보이게 해줘.
  ```

### SL-01 · 판매글 작성
- **소속 메뉴**: 판매하기
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-10-19 ~ 2026-10-22
- **기능정의**: 사진 등록 영역(최대 10장·대표 지정) · 제목 · 카테고리 선택(대·소분류) · 분류별 추가 항목 · 물건 상태 라디오 5단계 · 사용 기간 · 가격 입력과 「나눔」 선택 · 가격 제안 받기 토글과 최저 금액 · 상세 설명 · 직거래 가능 지역 선택(등록한 동네 중에서) · 거래 방식 체크(직거래·택배·안전결제)와 배송비 · 금지 품목 안내 · 임시 저장 · 미리보기 · 등록 버튼과 필수 항목 검사
- **버튼 → 이동화면**:
  - `사진 정리하기` → `SL-02` 사진 등록·정리
  - `얼마에 팔까 보기` → `SL-03` 얼마에 팔까 — 시세 보기
  - `등록하기` → `SL-04` 등록 완료
  - `금지 품목 보기` → `HO-03` 안전거래·이용안내
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 분류 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
  - 선택 — 고른 개수를 **숫자로 보여주고**, 하나도 안 골랐으면 일괄 단추를 눌리지 않게 둡니다
  - 토글 — 누르면 **켜짐·꺼짐이 눈에 보이게** 바뀌고 그대로 남습니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 판매글 작성 화면을 만들어줘. 회원 누구나 여기서 물건을 올린다. 1) 맨 위 사진 등록 — 정사각 자리 10칸, 첫 칸은 [+ 사진 추가], 등록된 사진 첫 장에 「대표」 배지. 「사진은 최대 10장, 첫 장이 목록에 보여요」 안내 2) 제목 입력칸 — 글자 수 표시(0/40) 3) 카테고리 — 대분류 드롭다운 + 소분류 드롭다운. 고르면 그 분류 전용 항목이 아래에 나타난다 (예: 휴대폰 → 제조사·용량·통신사 / 의류 → 사이즈·성별) 4) 물건 상태 — 라디오 5개(새것· 거의 새것· 사용감 있음· 고장· 부품용). 고른 것 아래에 「이 상태는 이렇게 적으면 좋아요」 예시 문구가 바뀌어 나온다 5) 사용 기간 드롭다운 6) 가격 입력칸(원 단위, 세 자리마다 쉼표)과 [나눔(0원)] 체크. 그 아래 [가격 제안 받기] 토글 — 켜면 「제안 받을 최저 금액」 입력칸이 열린다 7) 상세 설명 — 여러 줄 입력칸, 글자 수 표시. 오른쪽에 「이런 걸 적어주세요」 도움말 박스 (산 시기· 쓴 정도· 흠집 위치· 박스와 구성품 유무) 8) 직거래 가능 지역 — 등록한 동네 중에서 고르는 칩. 「동네를 인증해야 고를 수 있어요」 안내 9) 거래 방식 — 체크 3개(직거래· 택배· 안전결제). 택배를 켜면 배송비 입력칸, 안전결제를 켜면 「수수료 3.5%가 정산 때 빠져요」 안내가 열린다 10) 아래 거래 금지 품목 경고 띠와 [금지 품목 보기] 링크 11) 하단 [임시 저장] [미리보기] [등록하기]. 필수 항목이 비면 [등록하기]는 잠겨 있고, 누르면 첫 번째 빈 칸으로 스크롤하며 붉게 표시한다 폼이 길다. 「사진 → 무엇을 → 얼마에 → 어떻게 거래」 순서로 묶고 묶음마다 소제목을 붙여줘.
  ```

### SL-02 · 사진 등록·정리
- **소속 메뉴**: 판매하기
- **뒤로가기**: 좌측 상단에 `‹ 판매글 작성` — 누르면 `SL-01`로 간다
- **일정**: 2026-10-23 ~ 2026-10-28
- **기능정의**: 여러 장 한꺼번에 올리기와 끌어다 놓기 · 올리는 중 진행 표시 · 끌어 옮겨 순서 바꾸기 · 대표 사진 지정 · 정사각 자르기 · 회전 · 개별 삭제 · 용량 초과 자동 압축 안내 · 형식 안내(jpg·png·webp) · 목록에서 어떻게 보이는지 미리보기 · 최소 1장 검사 · 흐린 사진 다시 찍기 권유
- **버튼 → 이동화면**:
  - `판매글로 돌아가기` → `SL-01` 판매글 작성
  - `등록하기` → `SL-04` 등록 완료
- **생성 프롬프트**:

  ```
  중고거래 판매글의 사진 등록·정리 화면을 만들어줘. 물건 사진이 이 장터의 첫인상이다. 1) 위쪽에 큰 끌어다 놓기 영역 — 「사진을 끌어다 놓거나 눌러서 고르세요· 최대 10장· jpg·png·webp· 한 장 10MB까지」 2) 올린 사진은 아래 격자에 정사각 카드로. 카드마다 오른쪽 위 X(삭제), 아래에 [대표로] [자르기] [회전] 3) 첫 카드에 「대표」 배지. 카드를 끌어서 순서를 바꾸면 배지가 따라 옮겨진다 4) 올리는 중인 사진은 진행 막대와 퍼센트, 실패하면 붉은 테두리와 [다시 시도] 5) 10MB를 넘는 사진은 「자동으로 줄였어요 (12MB → 3MB)」 안내 배지 6) 오른쪽에 미리보기 패널 — 이 사진들이 매물 목록에서 어떻게 보이는지 가로 행 한 줄로 그대로 7) 아래 안내 — 「밝은 곳에서 찍은 사진이 더 빨리 팔려요」, 「흠집이 있으면 그 부분도 찍어주세요」 8) 사진이 한 장도 없으면 [다음] 버튼이 잠기고 「사진을 최소 1장 올려주세요」라고 알린다 자르기와 순서 바꾸기가 실제로 되는 것처럼 보이게, 끌고 있는 동안 놓일 자리를 빈 칸으로 표시해줘.
  ```

### SL-03 · 얼마에 팔까 — 시세 보기
- **소속 메뉴**: 판매하기
- **뒤로가기**: 좌측 상단에 `‹ 판매글 작성` — 누르면 `SL-01`로 간다
- **일정**: 2026-10-29 ~ 2026-11-02
- **기능정의**: 같은 물건의 최근 거래가 분포 그래프 · 기간 탭(1·3·6개월) · 물건 상태별 평균가 표 · 최근 거래 사례 목록(언제·얼마에·상태) · 지금 올라와 있는 같은 물건 매물과 값 · 「이 값이면 며칠쯤 걸려요」 예상 · 값을 내렸을 때 팔리는 속도 안내 · 이 가격으로 정하기 버튼
- **버튼 → 이동화면**:
  - `판매글로 돌아가기` → `SL-01` 판매글 작성
  - `같은 물건 매물 보기` → `SE-02` 매물 목록
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
- **생성 프롬프트**:

  ```
  중고거래 장터에서 「내 물건을 얼마에 팔까」를 도와주는 시세 화면을 만들어줘. 1) 맨 위에 물건 이름과 「최근 3개월 거래 128건 기준」 2) 기간 탭 — [1개월] [3개월] [6개월] 3) 가격 분포 그래프 — 가격 구간별 거래 건수 막대. (그래프 자리에는 사진을 넣지 않는다. 「차트 영역· 권장 800×300」이라고 자리표로 둬줘) 4) 상태별 평균가 표 — 새것· 거의 새것· 사용감 있음· 고장. 각 행에 평균가와 거래 건수, 상태 필터로도 쓰인다 5) 최근 거래 사례 목록 6줄 — 「9월 2일· 32만원· 거의 새것· 3일 만에 거래」 6) 「지금 올라와 있는 같은 물건」 4개 가로 행 — 값과 등록 시각 7) 예상 안내 카드 — 「32만원에 올리면 평균 4일쯤 걸려요 / 28만원이면 2일」 8) 아래 [이 가격으로 정하기] 버튼 — 누르면 판매글 가격칸이 채워진다는 안내 숫자가 주인공인 화면이다. 표와 그래프를 크게, 설명 글은 짧게 해줘.
  ```

### SL-04 · 등록 완료
- **소속 메뉴**: 판매하기
- **뒤로가기**: 좌측 상단에 `‹ 판매글 작성` — 누르면 `SL-01`로 간다
- **일정**: 2026-11-03 ~ 2026-11-05
- **기능정의**: 등록됐다는 확인과 올린 매물 카드 미리보기 · 내 글이 목록에서 어떻게 보이는지 · 지금 이 동네에서 몇 명이 볼 수 있는지 · 링크 복사와 공유 · 다음에 할 일 안내(채팅 오면 알림) · 무료 끌올은 24시간 뒤부터 가능하다는 안내 · 바로 끌올하기(유료) · 판매글 더 쓰기 · 내 판매글 관리로 가기
- **버튼 → 이동화면**:
  - `내 판매글 관리` → `SL-05` 내 판매글 관리
  - `끌올하기` → `BS-01` 끌올하기
  - `판매글 더 쓰기` → `SL-01` 판매글 작성
- **생성 프롬프트**:

  ```
  중고거래 판매글을 올린 직후에 나오는 등록 완료 화면을 만들어줘. 1) 가운데에 체크 표시와 「판매글이 올라갔어요」 2) 그 아래 방금 올린 매물이 목록에서 보이는 그대로 가로 행 카드 한 줄 — 「내 글이 이렇게 보여요」 3) 숫자 카드 3개 — 이 동네에서 볼 수 있는 이웃 수· 이 카테고리 오늘 등록 수· 비슷한 물건 평균 거래 소요일 4) [링크 복사] [카카오톡 공유] 버튼 줄 5) 다음에 할 일 안내 — 「채팅이 오면 알려드릴게요」, 「약속을 잡으면 예약중으로 바꿔주세요」 6) 끌올 안내 카드 — 「무료 끌올은 24시간 뒤부터 할 수 있어요」와 남은 시간, 그 아래 [지금 바로 끌올하기(유료)] 7) 아래 [판매글 더 쓰기] [내 판매글 관리] 완료 화면이지만 막다른 길이 아니게, 다음에 누를 것을 분명히 줘.
  ```

### SL-05 · 내 판매글 관리
- **소속 메뉴**: 판매하기
- **뒤로가기**: 좌측 상단에 `‹ 판매글 작성` — 누르면 `SL-01`로 간다
- **일정**: 2026-11-06 ~ 2026-11-11
- **기능정의**: 상태 탭(판매중·예약중·거래완료·숨김)과 건수 배지 · 내가 올린 글 가로 행 목록 · 행마다 조회·찜·채팅 수 · 상태 바꾸기 드롭다운 · 끌올 버튼과 남은 시간 · 가격 내리기 · 수정·삭제 · 거래완료로 바꿀 때 구매자 고르기 · 후기 쓰기 유도 · 오래된 글 자동 숨김 안내 · 여러 개 골라 한꺼번에 처리
- **버튼 → 이동화면**:
  - `끌올·광고 보기` → `BS-01` 끌올하기
  - `후기 쓰기` → `MY-04` 거래 후기 쓰기
  - `판매글 쓰기` → `SL-01` 판매글 작성
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
- **생성 프롬프트**:

  ```
  중고거래 장터에서 내가 올린 판매글을 관리하는 화면을 만들어줘. 1) 상단 탭 — [판매중] [예약중] [거래완료] [숨김], 탭마다 건수 배지 2) 목록 — 가로 행. 썸네일, 제목, 가격, 등록일, 「조회 124· 찜 8· 채팅 3」, 오른쪽에 상태 드롭다운과 점 세 개 메뉴(수정· 가격 내리기· 숨기기· 삭제) 3) 행마다 [끌올] 버튼 — 무료 끌올이 아직이면 잠기고 「7시간 뒤」라고 남은 시간을 보여준다. 가능해지면 색이 살아난다 4) 상태를 [거래완료]로 바꾸면 모달 — 「누구와 거래하셨나요?」 채팅한 사람 목록에서 고르기, 고르면 「후기를 남겨주세요」로 이어진다 5) 위쪽에 체크박스로 여러 개 골라 [한꺼번에 숨기기] [한꺼번에 삭제] 6) 오래 안 팔린 글에는 「30일이 지나 목록에서 내려갔어요. 끌올하면 다시 올라와요」 안내 줄 7) 빈 탭에는 「아직 없어요」와 [판매글 쓰기] 버튼 상태를 바꾸는 것이 이 화면의 알맹이다. 바꾸면 배지와 탭 건수가 함께 움직이게 해줘.
  ```

### BS-01 · 끌올하기
- **소속 메뉴**: 끌올·광고
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-11-12 ~ 2026-11-16
- **기능정의**: 올릴 판매글 고르기와 지금 순위 · 무료 끌올(24시간에 한 번)과 남은 시간 · 유료 끌올 상품 3종(즉시 끌올·하루 상단 고정·일주일 상단 고정)과 값 · 상품별 예상 노출 수 · 끌올 뒤 예상 순위 미리보기 · 결제 수단 고르기 · 「끌올은 광고 배지가 붙어요」 고지 · 최근 끌올 이력 · 확정 버튼
- **버튼 → 이동화면**:
  - `광고 상품 자세히` → `BS-02` 광고 상품·요금 안내
  - `끌올 내역 보기` → `BS-03` 끌올·광고 내역
  - `내 판매글로` → `SL-05` 내 판매글 관리
- **생성 프롬프트**:

  ```
  중고거래 장터의 끌올(끌어올리기) 화면을 만들어줘. 판매글을 목록 위로 올리는 유료 자리다. 1) 위쪽에 올릴 판매글 고르기 — 내 판매중 글을 가로 행으로, 고른 글에 테두리. 고른 글 옆에 「지금 이 동네 목록에서 148번째」 2) 무료 끌올 카드 — 「24시간에 한 번 무료」와 남은 시간(예: 7시간 12분). 남았으면 버튼이 잠기고 회색, 0이면 살아난다 3) 유료 상품 카드 3개 — [즉시 끌올] [하루 상단 고정] [일주일 상단 고정]. 카드마다 값, 얼마나 오래 유지되는지, 「이만큼 더 보여요」 예상 노출 수 4) 고르면 아래 요약 — 「끌올 뒤 예상 순위: 3번째」를 지금 순위와 화살표로 나란히 5) 결제 수단 — 카드·간편결제·보유 포인트, 포인트가 모자라면 충전 안내 6) 고지 박스 — 「끌올한 글에는 목록에서 «끌올» 배지가 붙어요. 광고라는 것을 이웃에게 알려요」 7) 최근 끌올 이력 3줄 — 언제 어느 글을 어떤 상품으로 8) 하단 [끌올하기] 버튼. 글을 안 고르면 잠겨 있다 돈을 쓰는 화면이니 무엇에 얼마를 쓰는지가 흐리지 않게 해줘.
  ```

### BS-02 · 광고 상품·요금 안내
- **소속 메뉴**: 끌올·광고
- **뒤로가기**: 좌측 상단에 `‹ 끌올하기` — 누르면 `BS-01`로 간다
- **일정**: 2026-11-17 ~ 2026-11-19
- **기능정의**: 광고 상품 종류와 값 비교 표 · 상품별 노출 자리 그림(목록 위·검색 결과 위·카테고리 첫 화면) · 예상 노출 계산기(카테고리·동네 범위·기간) · 정산·환불 규정 · 광고 배지 노출 규칙 · 금지되는 광고 문구 · 자주 묻는 질문 · 사업자 광고와 개인 끌올의 차이
- **버튼 → 이동화면**:
  - `끌올하러 가기` → `BS-01` 끌올하기
  - `자주 묻는 질문` → `CS-01` 자주 묻는 질문
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 검색 — 입력한 말이 든 것만 남깁니다. 없으면 「검색 결과가 없습니다」와 다시 찾는 길을 보여줍니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 광고 상품·요금 안내 화면을 만들어줘. 1) 상단 한 줄 설명 — 「끌올은 내 글을 목록 위로 올리는 것이고, 광고는 정해진 자리를 사는 것이에요」 2) 상품 비교 표 — 행: 즉시 끌올 / 하루 상단 고정 / 일주일 상단 고정 / 검색 결과 상단 / 카테고리 첫 화면. 열: 값, 유지 기간, 보이는 자리, 하루 예상 노출. 열 머리글을 눌러 정렬 3) 노출 자리 그림 — 목록 화면을 작게 그려두고 어느 줄이 그 상품 자리인지 표시 (그림 자리는 「도식 영역· 권장 600×400」 자리표로 두고 사진을 넣지 않는다) 4) 예상 계산기 — 카테고리·동네 범위·기간을 고르면 「하루 평균 2,400회 노출· 4,500원」처럼 결과가 바뀐다 5) 정산·환불 규정 접기 — 시작 전 취소는 전액, 시작 뒤는 남은 기간만큼 6) 광고 배지 규칙 — 「모든 유료 노출에는 배지가 붙습니다」 7) 금지되는 광고 문구 목록 — 과장·허위·금지 품목 8) 자주 묻는 질문 6개 아코디언 표가 주인공이다. 값을 크게, 조건은 작게 해줘.
  ```

### BS-03 · 끌올·광고 내역
- **소속 메뉴**: 끌올·광고
- **뒤로가기**: 좌측 상단에 `‹ 끌올하기` — 누르면 `BS-01`로 간다
- **일정**: 2026-11-20 ~ 2026-11-24
- **기능정의**: 기간 고르기와 합계(쓴 돈·집행 건수) · 내역 표(날짜·글 제목·상품·값·상태) · 상태 배지(진행중·끝남·취소) · 건별 성과 펼치기(노출·클릭·채팅) · 광고 대비 채팅 증가 요약 · 영수증·현금영수증 · 진행 중인 건 중단하기와 환불 예상액 · 결과 없을 때 안내
- **버튼 → 이동화면**:
  - `끌올하러 가기` → `BS-01` 끌올하기
  - `내 판매글로` → `SL-05` 내 판매글 관리
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 펼치 — 누르면 그 자리에서 펴지고 접힙니다. 다른 화면으로 가지 않습니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 끌올·광고 내역 화면을 만들어줘. 1) 위쪽 기간 고르기 — [이번 달] [지난 달] [직접 지정], 오른쪽에 합계 카드 2개(쓴 돈· 집행 건수) 2) 표 — 날짜, 글 제목(썸네일 작게), 상품 이름, 값, 상태 배지(진행중·끝남·취소). 헤더 고정, 행 높이 일정 3) 행을 누르면 아래로 펼쳐져 성과가 나온다 — 노출 수·클릭 수·그동안 온 채팅 수를 날짜별 막대로 (차트 자리는 자리표로 두고 사진을 넣지 않는다) 4) 위쪽 요약 띠 — 「광고한 글은 평균 3.2배 더 채팅을 받았어요」 5) 행마다 [영수증] — 결제 수단·금액·부가세가 모달로 6) 진행중인 건에는 [중단하기] — 누르면 「남은 기간만큼 OO원이 돌아와요」 확인 모달 7) 내역이 없으면 「아직 끌올한 적이 없어요」와 [끌올하러 가기] 표가 주인공이다. 상태 배지 색을 셋으로 분명히 갈라줘.
  ```

### CH-01 · 채팅 목록
- **소속 메뉴**: 채팅
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-11-25 ~ 2026-11-27
- **기능정의**: 탭(전체·구매·판매·안 읽음)과 건수 배지 · 대화방 목록(상대 닉네임·매물 썸네일·마지막 말·시각·안 읽음 수) · 거래 상태 배지(이야기중·예약중·거래완료) · 상대 매너 점수 작게 · 검색 · 알림 끈 방 표시 · 나간 방 안내 · 안 읽음만 보기 · 목록이 비었을 때 안내
- **버튼 → 이동화면**:
  - `대화 열기` → `CH-02` 채팅방
  - `매물 보러 가기` → `SE-02` 매물 목록
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
  - 검색 — 입력한 말이 든 것만 남깁니다. 없으면 「검색 결과가 없습니다」와 다시 찾는 길을 보여줍니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 채팅 목록 화면을 만들어줘. 1) 상단 탭 — [전체] [구매] [판매] [안 읽음], 탭마다 건수 배지 2) 위에 대화 상대·매물 이름으로 찾는 검색칸 3) 목록 — 한 줄에: 왼쪽 상대 프로필 동그라미, 가운데 위에 닉네임과 매너 점수(작게), 그 아래 마지막 말 한 줄(길면 …), 오른쪽에 매물 썸네일 작게, 시각, 안 읽음 개수 동그란 배지 4) 거래 상태 배지 — 「이야기중」 「예약중」 「거래완료」를 매물 썸네일 위에 5) 알림을 끈 방은 종 아이콘에 사선 6) 목록이 비면 「아직 대화가 없어요」와 [매물 보러 가기] 7) 화면이 넓을 때는 왼쪽 목록 + 오른쪽 대화 2단으로, 좁으면 목록만 한 줄에 정보가 많다. 닉네임과 마지막 말이 가장 먼저 읽히고 나머지는 작게 해줘.
  ```

### CH-02 · 채팅방
- **소속 메뉴**: 채팅
- **뒤로가기**: 좌측 상단에 `‹ 채팅 목록` — 누르면 `CH-01`로 간다
- **일정**: 2026-11-30 ~ 2026-12-03
- **기능정의**: 상단에 매물 요약 띠(썸네일·제목·가격·상태) · 대화 말풍선(글·사진·가격 제안 카드·약속 카드·시스템 안내) · 읽음 표시와 시각 · 입력칸과 사진 첨부 · 빠른 답장 칩 · 가격 제안 주고받기와 수락·거절 · 약속 잡기 카드 · 안전결제로 넘어가기 버튼 · 매물 상태 바꾸기(판매자만) · 상단 메뉴(신고·차단·알림끄기·나가기) · 사기 주의 시스템 안내 · 상대가 나갔을 때 안내
- **버튼 → 이동화면**:
  - `약속 잡기` → `CH-03` 거래 약속 잡기
  - `안전결제로 거래하기` → `PA-01` 안전결제 시작·안내
  - `신고·차단하기` → `CH-04` 신고·차단
  - `매물 보기` → `SE-04` 매물 상세
  - `채팅 목록으로` → `CH-01` 채팅 목록
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 끄기 — 누르면 **켜짐·꺼짐이 눈에 보이게** 바뀌고 그대로 남습니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 1:1 채팅방을 만들어줘. 값 흥정과 약속 잡기가 여기서 일어난다. 1) 맨 위 고정 띠 — 매물 썸네일·제목·가격과 상태 배지. 오른쪽에 [안전결제로 거래하기] 버튼. 판매자로 들어오면 그 자리가 [예약중으로 바꾸기] 드롭다운 2) 대화 영역 — 내 말은 오른쪽 색 말풍선, 상대는 왼쪽 회색. 말풍선 옆에 시각과 「읽음」. 날짜가 바뀌면 가운데 날짜 구분선 3) 특별한 말풍선 세 가지를 꼭 넣어줘· 가격 제안 카드 — 「28만원에 어때요?」와 [수락] [거절] 버튼. 수락하면 「수락됨」으로 굳는다· 약속 카드 — 날짜·시간·장소가 적힌 카드와 [일정 추가] [약속 바꾸기]· 시스템 안내 — 회색 가운데 정렬, 「돈을 먼저 보내달라는 말은 사기일 수 있어요」 4) 아래 입력칸 — 왼쪽에 + (사진·약속 잡기·안전결제 안내), 오른쪽에 보내기. 입력칸 위에 빠른 답장 칩 4개(「네고 가능할까요?」 「지금 거래 되나요?」 「직거래 어디서 할까요?」 「안전결제 할게요」) 5) 오른쪽 위 점 세 개 — 신고하기· 차단하기· 알림 끄기· 채팅방 나가기 6) 상대가 나갔으면 입력칸이 잠기고 「상대가 채팅방을 나갔어요」 대화가 주인공이다. 말풍선 폭을 화면의 70%까지만 두고, 특별 카드는 테두리로 구분해줘.
  ```

### CH-03 · 거래 약속 잡기
- **소속 메뉴**: 채팅
- **뒤로가기**: 좌측 상단에 `‹ 채팅 목록` — 누르면 `CH-01`로 간다
- **일정**: 2026-12-04 ~ 2026-12-08
- **기능정의**: 날짜 고르기(오늘·내일·직접) · 시간 고르기 · 장소 입력과 추천 장소 칩(안전거래존·지하철역·편의점) · 지도 자리와 서로의 거리 · 상대에게 보낼 메모 · 약속 30분 전 알림 켜기 · 노쇼 방지 안내 · 안전거래존 안내 · 보내기 버튼과 필수 항목 검사 · 이미 잡은 약속이 있을 때 겹침 경고
- **버튼 → 이동화면**:
  - `채팅으로 돌아가기` → `CH-02` 채팅방
  - `안전결제로 거래하기` → `PA-01` 안전결제 시작·안내
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 켜기 — 누르면 **켜짐·꺼짐이 눈에 보이게** 바뀌고 그대로 남습니다
- **생성 프롬프트**:

  ```
  중고거래 장터에서 직거래 약속을 잡는 화면을 만들어줘. 채팅방에서 열린다. 1) 상단에 상대 닉네임과 매물 요약 한 줄 2) 날짜 — [오늘] [내일] 칩과 [직접 고르기] 달력. 지난 날짜는 못 고른다 3) 시간 — 30분 단위 칩 격자(10:00 ~ 22:00). 상대가 「이때 좋아요」라고 한 시간대는 색으로 표시 4) 장소 — 입력칸과 추천 칩 4개([안전거래존] [OO역 2번 출구] [편의점 앞] [최근 장소]). 아래에 「지도 영역· 권장 800×400」 자리표와 「나에게서 1.2km· 상대에게서 0.8km」 5) 안전거래존 안내 박스 — CCTV가 있는 공공 장소 목록과 왜 권하는지 한 줄 6) 상대에게 보낼 짧은 메모 입력칸 7) [약속 30분 전에 알려주기] 체크 8) 이미 같은 시간에 다른 약속이 있으면 주황 경고 줄 — 「이 시간에 다른 약속이 있어요」 9) 하단 [약속 보내기] — 날짜·시간·장소 중 하나라도 비면 잠긴다 고르는 것이 셋뿐이다. 화면을 세 묶음으로 크게 나눠 한눈에 보이게 해줘.
  ```

### CH-04 · 신고·차단
- **소속 메뉴**: 채팅
- **뒤로가기**: 좌측 상단에 `‹ 채팅 목록` — 누르면 `CH-01`로 간다
- **일정**: 2026-12-09 ~ 2026-12-11
- **기능정의**: 무엇을 신고하는지(사용자·매물·대화) · 신고 사유 라디오(사기 의심·금지 품목·욕설·광고·다른 사람 사진 도용·기타) · 사유별 추가 입력 · 자세한 설명 입력칸 · 증거 첨부(대화 캡처·사진) · 함께 차단하기 체크와 설명 · 신고 뒤 절차 안내와 처리 기간 · 허위 신고 경고 · 접수 완료 안내 · 이미 신고한 건이면 중복 안내
- **버튼 → 이동화면**:
  - `채팅으로 돌아가기` → `CH-02` 채팅방
  - `안전거래 안내 보기` → `HO-03` 안전거래·이용안내
- **생성 프롬프트**:

  ```
  중고거래 장터의 신고·차단 화면을 만들어줘. 사기와 금지품목을 걸러내는 자리다. 1) 상단에 무엇을 신고하는지 요약 카드 — 상대 닉네임, 매물 제목, 「이 대화」 2) 신고 사유 라디오 6개 — 사기가 의심돼요 / 팔 수 없는 물건이에요 / 욕설·비방을 해요 / 광고·도배예요 / 남의 사진을 가져다 썼어요 / 그 밖에 고른 사유마다 아래에 다른 입력칸이 열린다(예: 금지품목이면 어떤 품목인지 고르기) 3) 자세한 설명 입력칸 — 여러 줄, 「언제 어떤 일이 있었는지 적어주세요」 4) 증거 첨부 — 대화 캡처·사진 최대 5장, 붙인 것은 아래 미리보기로 5) [이 사람을 차단할게요] 체크 — 켜면 「차단하면 이 사람 글이 목록에 안 보이고 채팅도 못 와요」 설명이 펼쳐진다 6) 안내 박스 — 신고하면 어떻게 되는지 4단계(접수 → 검토 → 조치 → 결과 알림), 평균 처리 시간 7) 경고 줄 — 「사실이 아닌 신고를 반복하면 제한될 수 있어요」 8) 하단 [신고하기] — 사유를 안 고르면 잠긴다 무거운 화면이다. 색을 절제하고 사유 고르기가 가장 크게 보이게 해줘.
  ```

### PA-01 · 안전결제 시작·안내
- **소속 메뉴**: 안전결제
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-12-14 ~ 2026-12-17
- **기능정의**: 이 거래의 매물 요약과 판매자 · 안전결제 4단계 그림(결제-발송-수령확인-정산) · 단계별로 돈이 어디 있는지 · 수수료 계산기(물건 값 → 수수료 → 판매자 수령액) · 누가 수수료를 내는지 선택 안내 · 직거래와 견주기 표 · 반품·환불 규정 · 분쟁이 생기면 어떻게 되는지 · 판매자가 안전결제를 안 켰을 때 안내 · 시작 버튼
- **버튼 → 이동화면**:
  - `결제하기` → `PA-02` 결제
  - `직거래로 하기` → `CH-03` 거래 약속 잡기
  - `안전거래 안내 보기` → `HO-03` 안전거래·이용안내
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 선택 — 고른 개수를 **숫자로 보여주고**, 하나도 안 골랐으면 일괄 단추를 눌리지 않게 둡니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 안전결제 시작·안내 화면을 만들어줘. 「돈을 먼저 보내도 되나」라는 불안을 없애는 것이 목적이다. 1) 상단에 이 거래 요약 카드 — 매물 썸네일·제목·가격, 판매자 닉네임과 매너 점수 2) 4단계 그림을 가로로 — ① 사는 쪽이 결제(돈은 플랫폼이 보관) ② 파는 쪽이 발송(운송장 등록) ③ 사는 쪽이 수령 확인 ④ 판매자에게 정산. 단계를 누르면 「이때 돈은 여기 있어요」 설명이 펼쳐진다 3) 수수료 계산기 — 물건 값 입력칸을 넣으면 「수수료 3.5% = 10,500원 / 판매자가 받는 돈 289,500원」이 함께 바뀐다. 「수수료는 파는 쪽이 냅니다」 같은 규칙을 분명히 4) 직거래와 견주기 탭 — 표 두 열로. 돈이 언제 넘어가나 / 문제가 생기면 누가 도와주나 / 드는 비용 / 만나야 하나 5) 반품·환불 규정 아코디언 — 언제까지 반품할 수 있는지, 배송비는 누가 내는지 6) 분쟁 안내 — 「받은 물건이 설명과 다르면 수령 확인 전에 신고하세요. 운영자가 양쪽 말을 듣고 판단합니다」 7) 판매자가 안전결제를 안 켠 매물이면 위쪽에 주황 띠 — 「이 판매자는 안전결제를 받지 않아요. 직거래로 진행하세요」 8) 하단 [안전결제로 진행하기] 큰 버튼 돈의 흐름이 이 화면의 알맹이다. 4단계 그림을 가장 크게 두고 글은 짧게 해줘.
  ```

### PA-02 · 결제
- **소속 메뉴**: 안전결제
- **뒤로가기**: 좌측 상단에 `‹ 안전결제 시작·안내` — 누르면 `PA-01`로 간다
- **일정**: 2026-12-18 ~ 2026-12-23
- **기능정의**: 주문 요약(매물·물건 값·배송비·수수료 부담·총 결제액) · 받는 사람 정보와 배송지 고르기·새 주소 추가 · 배송 요청 사항 · 결제 수단 탭(카드·간편결제·계좌이체) · 수단별 입력 · 포인트·쿠폰 사용 · 필수 약관 체크 3개 · 결제 진행 중 표시와 중복 클릭 막기 · 결제 실패 안내와 다른 수단 · 판매자 확인 대기 시간 안내
- **버튼 → 이동화면**:
  - `거래 진행 상태 보기` → `PA-05` 거래 진행 상태
  - `안전결제 안내로` → `PA-01` 안전결제 시작·안내
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
- **생성 프롬프트**:

  ```
  중고거래 장터의 안전결제 결제 화면을 만들어줘. 1) 오른쪽(좁은 화면에서는 위쪽)에 따라다니는 주문 요약 카드 — 매물 썸네일·제목 / 물건 값 289,000원 / 배송비 3,000원 / 「수수료는 판매자가 냅니다」 / 총 결제액 굵게 2) 받는 사람 — 이름·연락처, 배송지 고르기(등록된 주소 라디오 + [새 주소 추가]), 배송 요청 사항 드롭다운(문 앞·경비실·직접 받기) 3) 결제 수단 탭 3개 — [카드] [간편결제] [계좌이체]. 탭마다 입력 형태가 다르게 4) 포인트·쿠폰 — 보유 포인트와 사용 입력칸, [모두 사용], 쿠폰 고르기 5) 필수 약관 체크 3개 — 안전결제 이용약관 / 개인정보 제3자 제공(판매자에게 배송지 전달) / 전자금융거래약관. 셋을 다 체크해야 아래 [결제하기]가 열린다. 하나라도 빠지면 잠긴 채 「약관에 동의해주세요」 6) 결제를 누르면 화면 위에 진행 표시와 「창을 닫지 마세요」, 버튼은 다시 눌리지 않는다 7) 실패했을 때 붉은 안내 — 사유(한도 초과·카드 정보 오류)와 [다른 수단으로] [다시 시도] 8) 아래 안내 — 「결제한 돈은 판매자에게 바로 가지 않고 플랫폼이 보관해요」 총 결제액이 화면에서 가장 큰 숫자여야 한다. 약관 잠금이 실제로 걸리게 해줘.
  ```

### PA-03 · 발송 등록 (판매자)
- **소속 메뉴**: 안전결제
- **뒤로가기**: 좌측 상단에 `‹ 안전결제 시작·안내` — 누르면 `PA-01`로 간다
- **일정**: 2026-12-24 ~ 2026-12-29
- **기능정의**: 이 거래 요약과 사는 쪽 배송지 · 발송 기한 남은 시간 · 택배사 고르기와 운송장 번호 입력 · 자릿수·형식 검사 · 직접 전달로 바꾸기 · 포장 안내와 사진 첨부(선택) · 등록하면 사는 쪽에 알림이 간다는 안내 · 기한을 넘기면 자동 취소된다는 경고 · 발송 취소 요청 · 등록 완료 표시
- **버튼 → 이동화면**:
  - `거래 진행 상태 보기` → `PA-05` 거래 진행 상태
  - `채팅으로` → `CH-02` 채팅방
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 선택 — 고른 개수를 **숫자로 보여주고**, 하나도 안 골랐으면 일괄 단추를 눌리지 않게 둡니다
- **생성 프롬프트**:

  ```
  중고거래 안전결제에서 판매자가 물건을 보냈다고 등록하는 화면을 만들어줘. 1) 상단에 거래 요약 — 매물, 사는 쪽 닉네임, 받는 주소(일부 가림), 결제된 금액 2) 그 아래 큰 남은 시간 카드 — 「발송까지 2일 6시간 남았어요」. 기한을 넘기면 붉게 바뀌고 「자동 취소되고 구매자에게 환불됩니다」 3) 택배사 드롭다운 — 고르면 아래 운송장 입력칸의 안내가 바뀐다(「12자리 숫자」 등) 4) 운송장 번호 입력칸 — 형식이 안 맞으면 붉게, 「자릿수가 맞지 않아요」 5) [직접 만나서 전달했어요] 링크 — 누르면 운송장칸이 닫히고 「구매자가 받았다고 확인하면 정산돼요」로 바뀐다 6) 포장 사진 첨부(선택) 2장 — 「나중에 다툼이 생겼을 때 도움이 돼요」 7) 포장 안내 접기 — 깨지는 물건· 액체· 배터리 주의 8) 하단 [발송 등록하기] — 택배사와 운송장이 다 차야 열린다. 누르면 「구매자에게 알림을 보냈어요」 확인 9) 아래에 작게 [발송을 못 하게 됐어요 — 거래 취소 요청] 남은 시간이 화면에서 가장 크게 보여야 한다.
  ```

### PA-04 · 수령 확인·구매확정
- **소속 메뉴**: 안전결제
- **뒤로가기**: 좌측 상단에 `‹ 안전결제 시작·안내` — 누르면 `PA-01`로 간다
- **일정**: 2026-12-30 ~ 2027-01-01
- **기능정의**: 배송 조회 요약과 도착 표시 · 자동 구매확정까지 남은 시간 · 물건 상태 라디오(설명대로예요·설명과 달라요) · 다르면 사진 첨부와 사유 입력 · 구매확정 버튼과 되돌릴 수 없다는 확인 모달 · 확정하면 판매자에게 정산된다는 안내 · 반품 요청 절차 · 분쟁 신청 · 확정 뒤 후기 쓰기 유도
- **버튼 → 이동화면**:
  - `후기 남기기` → `MY-04` 거래 후기 쓰기
  - `거래 진행 상태 보기` → `PA-05` 거래 진행 상태
  - `신고하기` → `CH-04` 신고·차단
- **생성 프롬프트**:

  ```
  중고거래 안전결제에서 구매자가 「받았어요」를 누르는 화면을 만들어줘. 이 버튼을 누르면 돈이 판매자에게 간다. 1) 상단에 배송 요약 — 택배사·운송장·[배송 조회], 상태 배지 「도착」 2) 큰 카드 — 「자동 구매확정까지 5일 12시간」과 설명 「이 시간이 지나면 자동으로 확정돼요」. 하루 밑으로 남으면 붉게 3) 물건 상태 라디오 2개· [설명대로예요] — 고르면 아래 [구매확정하기] 큰 버튼이 열린다· [설명과 달라요] — 고르면 사진 첨부(최대 5장)와 사유 입력칸, [반품·분쟁 신청하기] 버튼으로 바뀐다 4) [구매확정하기]를 누르면 확인 모달 — 「확정하면 289,000원이 판매자에게 갑니다. 되돌릴 수 없어요」와 [취소] [확정하기] 5) 확정 뒤 화면 — 체크 표시와 「거래가 끝났어요」, [후기 남기기] 큰 버튼 6) 아래 접기 — 반품은 언제까지 되는지, 배송비는 누가 내는지, 분쟁이 어떻게 진행되는지 되돌릴 수 없는 버튼이다. 누르기 전 확인이 분명하게 보이게 해줘.
  ```

### PA-05 · 거래 진행 상태
- **소속 메뉴**: 안전결제
- **뒤로가기**: 좌측 상단에 `‹ 안전결제 시작·안내` — 누르면 `PA-01`로 간다
- **일정**: 2027-01-04 ~ 2027-01-07
- **기능정의**: 탭(구매·판매) · 진행 중인 거래 목록과 단계 표시 4칸 · 단계별 시각 기록 · 다음에 누가 무엇을 해야 하는지 안내 · 남은 시간(발송 기한·자동 확정) · 배송 조회 펼치기 · 결제 금액과 정산 예정액 · 취소·환불 요청 · 분쟁 접수 상태 · 끝난 거래 접기 · 상대에게 채팅 보내기
- **버튼 → 이동화면**:
  - `수령 확인하기` → `PA-04` 수령 확인·구매확정
  - `발송 등록하기` → `PA-03` 발송 등록 (판매자)
  - `채팅으로` → `CH-02` 채팅방
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
  - 펼치 — 누르면 그 자리에서 펴지고 접힙니다. 다른 화면으로 가지 않습니다
- **생성 프롬프트**:

  ```
  중고거래 안전결제의 거래 진행 상태 화면을 만들어줘. 「지금 어디까지 왔나」가 한눈에 보여야 한다. 1) 상단 탭 — [내가 산 것] [내가 판 것], 탭마다 진행중 건수 배지 2) 거래마다 카드 하나. 카드 안에· 위: 매물 썸네일·제목·금액, 상대 닉네임· 가운데: 4칸 단계 표시 — 결제 → 발송 → 수령확인 → 정산. 지난 칸은 색을 채우고 그 아래 시각(「9/5 14:20」), 지금 칸은 테두리를 굵게, 남은 칸은 회색· 아래: 지금 무엇을 해야 하는지 한 줄 — 「판매자가 보내기를 기다리는 중이에요 (2일 6시간 남음)」 그리고 그때 눌러야 할 버튼 하나만 3) [배송 조회] 펼치기 — 택배 이동 기록을 시간순 목록으로 4) 판매 탭의 카드에는 「정산 예정 289,500원· 9/12」 5) 카드 오른쪽 위 점 세 개 — 채팅 보내기· 거래 취소 요청· 신고하기 6) 아래 [끝난 거래 보기] 접기 — 거래완료·취소된 것 단계 표시가 이 화면의 알맹이다. 네 칸의 상태가 서로 다르게 보이게 하고, 「지금 누가 무엇을 할 차례인지」를 늘 한 줄로 적어줘.
  ```

### MY-01 · 내 프로필·매너 점수
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2027-01-08 ~ 2027-01-13
- **기능정의**: 프로필 사진·닉네임·동네·가입일 · 매너 점수를 온도로 크게 · 점수 산정 근거 펼치기 · 거래 횟수(산 것·판 것) · 응답률과 평균 응답 시간 · 재거래 희망률 · 받은 후기 요약(좋아요 항목 상위 5개) · 탭(판매중 매물·거래완료·받은 후기) · 프로필 수정 · 다른 사람이 볼 때의 화면 미리보기 · 신뢰 배지(본인인증·동네인증)
- **버튼 → 이동화면**:
  - `받은 후기 보기` → `MY-05` 받은 후기·매너 점수 상세
  - `내 판매글 관리` → `SL-05` 내 판매글 관리
  - `찜한 물건 보기` → `MY-03` 찜한 물건
  - `알림 설정` → `MY-06` 관심 키워드·알림 설정
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
  - 펼치 — 누르면 그 자리에서 펴지고 접힙니다. 다른 화면으로 가지 않습니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 내 프로필·매너 점수 화면을 만들어줘. 이 장터에서 나를 믿을 만한지 보여주는 화면이다. 1) 위쪽 프로필 줄 — 동그란 사진, 닉네임, 「OO동· 2024년 3월 가입」, 오른쪽에 [프로필 수정] 2) 그 아래 매너 점수를 크게 — 온도계 모양으로 「42.5도」, 기본이 36.5도라는 설명과 색 띠(낮음·보통·좋음). 누르면 어떻게 계산됐는지 펼쳐진다 3) 신뢰 배지 줄 — [본인인증 완료] [동네인증 완료] [안전결제 12회], 안 한 것은 회색과 [인증하기] 4) 숫자 카드 4개 — 판 것 24회 / 산 것 11회 / 응답률 92%· 평균 12분 / 재거래 희망 88% 5) 「이웃들이 좋아한 점」 — 후기에서 많이 고른 항목 상위 5개를 막대와 함께 (「시간 약속을 잘 지켜요 18」 처럼) 6) 탭 3개 — [판매중 매물] [거래완료] [받은 후기]. 탭마다 아래 목록이 바뀐다 7) 오른쪽 위에 [다른 사람에게 보이는 화면] 링크 매너 점수가 이 화면의 주인공이다. 가장 크게, 가장 먼저 읽히게 해줘.
  ```

### MY-02 · 구매 내역
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 내 프로필·매너 점수` — 누르면 `MY-01`로 간다
- **일정**: 2027-01-14 ~ 2027-01-19
- **기능정의**: 상태 탭(진행중·거래완료·취소)과 건수 · 거래 방식 필터(직거래·안전결제) · 기간 고르기 · 목록(날짜·매물·판매자·금액·방식 배지·상태) · 안전결제 건은 단계 표시 작게 · 후기 쓰기 버튼과 이미 쓴 것 표시 · 영수증 보기 · 같은 판매자 다시 보기 · 재구매·비슷한 물건 추천 · 비었을 때 안내
- **버튼 → 이동화면**:
  - `후기 쓰기` → `MY-04` 거래 후기 쓰기
  - `거래 진행 상태 보기` → `PA-05` 거래 진행 상태
  - `매물 보러 가기` → `SE-02` 매물 목록
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 필터 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
- **생성 프롬프트**:

  ```
  중고거래 장터의 구매 내역 화면을 만들어줘. 1) 상단 탭 — [진행중] [거래완료] [취소], 건수 배지. 그 아래 기간 고르기와 거래 방식 필터 칩 2) 목록 — 가로 행. 날짜, 매물 썸네일·제목, 판매자 닉네임, 금액, 방식 배지(「직거래」 또는 「안전결제」), 상태 배지 3) 안전결제 건에는 행 아래 얇은 4칸 단계 표시를 작게 4) 아직 후기를 안 쓴 거래완료 건에는 오른쪽에 [후기 쓰기] 버튼. 쓴 것은 회색 「후기 씀」 배지 5) 행 오른쪽 점 세 개 — 영수증 보기· 판매자 프로필· 다시 채팅하기 6) 목록 아래에 「이 판매자의 다른 매물」 4개 7) 비면 「아직 산 물건이 없어요」와 [매물 보러 가기] 표처럼 훑어 읽는 화면이다. 행 높이를 일정하게, 배지 색을 상태별로 갈라줘.
  ```

### MY-03 · 찜한 물건
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 내 프로필·매너 점수` — 누르면 `MY-01`로 간다
- **일정**: 2027-01-20 ~ 2027-01-22
- **기능정의**: 찜한 매물 가로 행 목록 · 묶음 보기(카테고리·동네·가격대) · 값이 내렸으면 이전 값 취소선과 내린 폭 배지 · 판매완료된 것 흐림 처리와 「비슷한 매물 찾기」 · 가격 내림 알림 개별 켜기 · 여러 개 골라 한꺼번에 찜 풀기 · 오래된 찜 정리 제안 · 비었을 때 안내
- **버튼 → 이동화면**:
  - `매물 자세히 보기` → `SE-04` 매물 상세
  - `매물 보러 가기` → `SE-02` 매물 목록
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 켜기 — 누르면 **켜짐·꺼짐이 눈에 보이게** 바뀌고 그대로 남습니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 찜한 물건 목록 화면을 만들어줘. 1) 위쪽에 「찜한 물건 24개」와 묶음 보기 드롭다운(전체·카테고리별·동네별·가격대별) 2) 목록 — 가로 행. 썸네일, 제목, 가격, 「OO동· 3일 전· 1.2km」, 오른쪽에 채워진 하트와 [알림] 종 3) 값이 내려간 매물은 이전 값에 취소선을 긋고 새 값을 굵게, 「2만원 내림」 주황 배지 4) 판매완료된 매물은 흐리게 하고 「거래완료」 덮개, 오른쪽에 [비슷한 매물 찾기] 5) 종 아이콘을 누르면 「값이 내려가면 알려드릴게요」 안내가 잠깐 뜨고 종이 채워진다 6) 위쪽에 체크박스로 여러 개 골라 [찜 풀기] 7) 「30일 넘게 안 본 찜 6개를 정리할까요?」 제안 줄 8) 비면 「찜한 물건이 없어요」와 [매물 보러 가기] 값이 내려간 것과 팔린 것을 한눈에 갈라 보이게 해줘.
  ```

### MY-04 · 거래 후기 쓰기
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 내 프로필·매너 점수` — 누르면 `MY-01`로 간다
- **일정**: 2027-01-25 ~ 2027-01-28
- **기능정의**: 누구와 무엇을 거래했는지 요약 · 내가 산 쪽인지 판 쪽인지 표시 · 별점 · 재거래 의사 라디오 · 좋았던 점 칩 고르기(최대 3개) · 아쉬운 점 켜기와 칩 · 자유롭게 쓰는 칸 · 사진 첨부(선택) · 비공개로 남기기 체크 · 후기는 상대도 나에게 쓴다는 안내 · 후기가 매너 점수에 어떻게 반영되는지 · 보내기와 나중에 하기
- **버튼 → 이동화면**:
  - `받은 후기 보기` → `MY-05` 받은 후기·매너 점수 상세
  - `구매 내역으로` → `MY-02` 구매 내역
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 선택 — 고른 개수를 **숫자로 보여주고**, 하나도 안 골랐으면 일괄 단추를 눌리지 않게 둡니다
  - 켜기 — 누르면 **켜짐·꺼짐이 눈에 보이게** 바뀌고 그대로 남습니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 거래 후기 쓰기 화면을 만들어줘. «양쪽이 서로 쓴다»는 것이 이 장터의 규칙이니 화면에도 그렇게 드러나야 한다. 1) 상단 요약 카드 — 상대 프로필과 닉네임, 매물 썸네일·제목, 거래일. 왼쪽 위에 배지로 「내가 산 거래」 또는 「내가 판 거래」 2) 안내 줄 — 「상대도 나에게 후기를 남길 수 있어요. 서로의 후기는 둘 다 쓴 뒤에 공개돼요」 3) 별점 5개 — 누르면 채워지고 아래 문구가 바뀐다(「아주 좋았어요」 등) 4) 재거래 의사 — [또 거래하고 싶어요] [그저 그래요] [다시는 안 할래요] 라디오 5) 좋았던 점 칩 — 산 쪽·판 쪽에 따라 다른 목록을 보여줘· 판매자를 평가할 때: 시간 약속을 잘 지켜요 / 설명과 물건이 같아요 / 응답이 빨라요 / 친절해요 / 포장이 꼼꼼해요· 구매자를 평가할 때: 시간 약속을 잘 지켜요 / 무리한 값 깎기가 없었어요 / 응답이 빨라요 / 친절해요 / 약속을 안 미뤄요 「3개까지」 남은 수를 옆에 표시 6) [아쉬운 점도 남길게요] 토글 — 켜면 아쉬운 점 칩과 자세히 적는 칸이 열린다 7) 자유롭게 쓰는 칸과 사진 첨부(선택) 8) [이 후기는 상대에게만 보이게 할게요] 체크와 설명 9) 아래 안내 — 「좋은 후기는 매너 점수를 올리고, 아쉬운 점은 점수를 내려요」 10) [후기 보내기] [나중에 할게요] 칩 고르기가 이 화면의 알맹이다. 산 쪽·판 쪽에 따라 목록이 다르다는 것이 보이게 해줘.
  ```

### MY-05 · 받은 후기·매너 점수 상세
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 내 프로필·매너 점수` — 누르면 `MY-01`로 간다
- **일정**: 2027-01-29 ~ 2027-02-03
- **기능정의**: 매너 점수 변화 그래프(최근 6개월) · 점수를 올린 것과 내린 것 · 항목별 받은 횟수 막대 · 탭(받은 후기·쓴 후기)과 건수 · 후기 목록(상대 닉네임·별점·칩·글·거래 매물·날짜) · 산 쪽·판 쪽 구분 배지 · 아직 안 쓴 후기 알림 · 비공개 후기 표시 · 부적절한 후기 신고 · 후기가 없을 때 안내
- **버튼 → 이동화면**:
  - `후기 쓰기` → `MY-04` 거래 후기 쓰기
  - `내 프로필로` → `MY-01` 내 프로필·매너 점수
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
- **생성 프롬프트**:

  ```
  중고거래 장터의 받은 후기·매너 점수 상세 화면을 만들어줘. 1) 맨 위 매너 점수 큰 숫자와 최근 6개월 변화 그래프 (그래프 자리는 「차트 영역· 권장 800×240」 자리표로 두고 사진을 넣지 않는다) 2) 그 옆에 「올린 것· 내린 것」 두 줄 — 「좋은 후기 18건 +6.0 / 아쉬운 후기 2건 -1.5 / 응답률 +0.5」 3) 항목별 막대 — 「시간 약속을 잘 지켜요 18」처럼. 막대를 누르면 아래 목록이 그 항목만 남는다 4) 탭 — [받은 후기 20] [쓴 후기 14] 5) 후기 목록 — 카드마다 상대 프로필과 닉네임, 별점, 고른 칩들, 글, 거래한 매물 썸네일과 날짜. 왼쪽 위에 「이 사람에게 팔았어요 / 이 사람에게 샀어요」 배지 6) 비공개 후기는 자물쇠 표시와 「나에게만 보여요」 7) 위쪽에 「아직 후기를 안 쓴 거래가 3건 있어요」 띠와 [지금 쓰기] 8) 카드 오른쪽 점 세 개 — 신고하기(사실과 다른 후기) 9) 후기가 없으면 「아직 받은 후기가 없어요」 숫자와 막대가 주인공이다. 후기 카드는 아래에 두고 글자를 절제해줘.
  ```

### MY-06 · 관심 키워드·알림 설정
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 내 프로필·매너 점수` — 누르면 `MY-01`로 간다
- **일정**: 2027-02-04 ~ 2027-02-09
- **기능정의**: 관심 키워드 추가·삭제(최대 30개)와 키워드별 현재 매물 수 · 키워드에 카테고리·가격 범위·동네 범위 붙이기 · 저장한 검색 조건 목록 · 알림 종류별 켜기(새 매물·값 내림·채팅·거래 상태·후기·공지) · 알림 받는 곳(앱 푸시·문자·카카오톡·이메일) · 방해 금지 시간 · 알림 묶어 보내기 · 최근 알림 이력 · 알림이 너무 많을 때 줄이는 제안
- **버튼 → 이동화면**:
  - `매물 보러 가기` → `SE-02` 매물 목록
  - `내 프로필로` → `MY-01` 내 프로필·매너 점수
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 검색 — 입력한 말이 든 것만 남깁니다. 없으면 「검색 결과가 없습니다」와 다시 찾는 길을 보여줍니다
  - 켜기 — 누르면 **켜짐·꺼짐이 눈에 보이게** 바뀌고 그대로 남습니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 관심 키워드·알림 설정 화면을 만들어줘. 1) 위쪽 관심 키워드 — 입력칸과 [추가]. 등록된 키워드는 칩으로 나열하고 칩마다 X. 칩 옆에 「지금 12개」처럼 매물 수. 「30개까지 등록할 수 있어요」 2) 키워드를 누르면 조건 붙이기가 펼쳐진다 — 카테고리, 가격 최저·최고, 동네 범위 3) 저장한 검색 조건 목록 — 「휴대폰· 30만원 이하· 내 동네」처럼 한 줄씩, [알림 켜기] 토글과 [지우기] 4) 알림 종류 표 — 행마다 이름과 설명, 오른쪽에 토글: 새 매물 알림 / 찜한 물건 값 내림 / 채팅 / 거래 상태 바뀜 / 후기 도착 / 공지·이벤트 5) 알림 받는 곳 체크 — 앱 푸시· 문자· 카카오톡· 이메일. 종류별로 다르게 고를 수 있게 표로 6) 방해 금지 시간 — 시작·끝 시각 입력, 「이 시간에는 모아뒀다가 아침에 보내드려요」 7) 「하루 알림이 20개를 넘어요. 키워드를 줄여볼까요?」 제안 카드 8) 아래 최근 알림 이력 5줄 토글이 많은 화면이다. 표로 정리하고 켜진 것과 꺼진 것이 한눈에 갈라지게 해줘.
  ```

### AD-01 · 신고 처리 대기열
- **소속 메뉴**: 운영자
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2027-02-10 ~ 2027-02-15
- **기능정의**: 지표 카드 4개(오늘 접수·처리 대기·평균 처리 시간·기한 넘긴 건) · 상태 탭(대기·처리중·완료·보류) · 사유·긴급도·신고 대상 종류 필터 · 표(접수 시각·사유·대상·신고자·누적 신고 수·긴급도·담당자·상태) · 같은 대상에 신고가 여러 건이면 묶어 보기 · 기한 임박 붉은 표시 · 담당자 지정 · 여러 건 일괄 처리 · 정렬(오래된 순·긴급도순·누적 신고순) · 대기열이 비었을 때 안내
- **버튼 → 이동화면**:
  - `신고 자세히 보기` → `AD-02` 신고 상세·조치
  - `금지품목·규칙 관리` → `AD-03` 금지품목·거래규칙 관리
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 필터 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
  - 정렬 — 고른 기준으로 **순서를 실제로 바꿔** 다시 그립니다
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
- **생성 프롬프트**:

  ```
  중고거래 장터 운영자의 신고 처리 대기열 화면을 만들어줘. 대시보드형 골격이라 왼쪽 세로 사이드바와 위쪽 지표 카드로 시작한다. 1) 왼쪽 세로 사이드바 — 신고 처리 / 금지품목·규칙 / 안전결제 정산 / 회원 관리 / 통계 2) 위쪽 지표 카드 4개 — 오늘 접수 42건 / 처리 대기 18건 / 평균 처리 6시간 / 기한 넘김 3건. 기한 넘김 카드만 붉게 3) 상태 탭 — [대기 18] [처리중 7] [완료 231] [보류 4] 4) 그 아래 필터 줄 — 사유 드롭다운(사기 의심·금지품목·욕설·광고·사진 도용·기타), 긴급도(높음·보통·낮음), 대상 종류(회원·매물·대화), 기간 5) 표 — 헤더 고정, 행 높이 일정. 열: 접수 시각 / 사유 배지 / 신고 대상(썸네일 작게 + 이름) / 신고자 / 「이 대상 누적 5건」 / 긴급도 배지 / 담당자 드롭다운 / 상태 배지 6) 같은 대상에 신고가 여러 건이면 행을 묶고 「+4건 더」로 접어둔다 7) 접수 24시간이 넘은 행은 왼쪽 끝에 붉은 띠와 경과 시간 8) 왼쪽 끝 체크박스로 여러 건 골라 위쪽에 [3건 일괄 반려] [3건 담당자 지정] 9) 대기열이 비면 「처리할 신고가 없어요」 표가 주인공이다. 배지 색을 사유·긴급도·상태 세 갈래로 분명히 갈라주고, 필터를 걸면 건수와 목록이 함께 줄어들게 해줘.
  ```

### AD-02 · 신고 상세·조치
- **소속 메뉴**: 운영자
- **뒤로가기**: 좌측 상단에 `‹ 신고 처리 대기열` — 누르면 `AD-01`로 간다
- **일정**: 2027-02-16 ~ 2027-02-19
- **기능정의**: 신고 요약(사유·접수 시각·긴급도) · 신고자가 적은 내용과 첨부 증거 · 신고된 매물·대화 원본 보기 · 신고된 회원 정보(가입일·거래 수·매너 점수·과거 신고·과거 조치) · 조치 고르기(반려·경고·글 삭제·노출 제한·이용 정지·영구 정지)와 조치별 추가 입력 · 정지 기간 고르기 · 양쪽에 갈 안내 문구 미리보기 · 내부 메모 · 처리 이력 · 이전·다음 신고로 이동
- **버튼 → 이동화면**:
  - `대기열로 돌아가기` → `AD-01` 신고 처리 대기열
  - `회원 프로필 보기` → `MY-01` 내 프로필·매너 점수
- **생성 프롬프트**:

  ```
  중고거래 장터 운영자가 신고 하나를 보고 조치를 정하는 화면을 만들어줘. 대시보드형이라 왼쪽에 본문, 오른쪽에 액션 패널 2단으로 나눠줘. 1) 위쪽 요약 띠 — 신고번호, 사유 배지, 긴급도 배지, 접수 시각과 「6시간 경과」, 담당자 2) 왼쪽 본문 — 세 묶음· 신고자가 적은 내용과 첨부한 캡처 4장(누르면 크게)· 신고된 것 원본 — 매물이면 매물 카드, 대화면 대화 기록을 시간순으로 펼치고 신고된 말에 노란 배경· 신고된 회원 카드 — 닉네임, 가입일, 거래 24회, 매너 점수 34.2, 「과거 신고 5건· 경고 1회」. [과거 이력 보기]를 누르면 표로 펼쳐진다 3) 오른쪽 액션 패널· 조치 라디오 6개 — 반려(문제 없음) / 경고 / 글 삭제 / 노출 제한 / 이용 정지 / 영구 정지. 고른 것에 따라 아래 입력이 바뀐다(정지면 기간 드롭다운 3·7·30일, 삭제면 사유 고르기)· 신고자에게 갈 안내 문구와 신고당한 회원에게 갈 안내 문구를 각각 미리보기 상자로. 「이 문구가 그대로 갑니다」· 내부 메모 입력칸 — 「회원에게는 안 보여요」· [조치 확정] 버튼과 그 아래 [보류로 두기] 4) 맨 아래 처리 이력 — 누가 언제 무엇을 했는지 시간순 5) 오른쪽 위에 [이전 신고] [다음 신고] — 대기열을 연달아 처리할 수 있게 사람의 계정을 막는 화면이다. 조치를 고를 때 무엇이 일어나는지가 확정 전에 다 보이게 해줘.
  ```

### AD-03 · 금지품목·거래규칙 관리
- **소속 메뉴**: 운영자
- **뒤로가기**: 좌측 상단에 `‹ 신고 처리 대기열` — 누르면 `AD-01`로 간다
- **일정**: 2027-02-22 ~ 2027-02-25
- **기능정의**: 금지 품목 분류 표(품목·근거·자동 조치·걸린 건수) · 품목 추가·수정·끄기 · 금지 낱말 목록과 예외 낱말 · 규칙별 자동 조치 고르기(바로 숨김·검토 대기·경고만) · 규칙을 걸기 전 미리 재보기 · 거래 규칙 문구 편집(안내 화면에 그대로 나감) · 규칙 바뀐 이력 · 잘못 걸린 사례 되돌리기 · 저장 전 영향 요약
- **버튼 → 이동화면**:
  - `신고 대기열로` → `AD-01` 신고 처리 대기열
  - `이용안내 화면 보기` → `HO-03` 안전거래·이용안내
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 분류 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
  - 끄기 — 누르면 **켜짐·꺼짐이 눈에 보이게** 바뀌고 그대로 남습니다
- **생성 프롬프트**:

  ```
  중고거래 장터 운영자의 금지품목·거래규칙 관리 화면을 만들어줘. 1) 상단 탭 — [금지 품목] [금지 낱말] [거래 규칙 문구] 2) 금지 품목 표 — 열: 품목 / 왜 금지인지(법·정책 근거) / 자동 조치 드롭다운(바로 숨김·검토 대기로·경고만) / 지금 걸리는 매물 수 / 켜기 토글. 행 끝에 수정·삭제 3) 위쪽에 [품목 추가] — 누르면 한 줄이 열려 품목명·근거·조치를 입력 4) 금지 낱말 탭 — 낱말 칩 목록과 [추가]. 그 아래 「이 낱말은 걸지 않기」 예외 칩 목록. 낱말을 넣으면 옆에 「지금 이 말이 든 매물 12개」가 계산돼 나온다 5) [규칙 미리 재보기] 버튼 — 지금 올라와 있는 매물 전체에 이 규칙을 걸면 몇 개가 걸리는지, 그중 잘못 걸릴 만한 것 5개를 예시로 보여준다 6) 거래 규칙 문구 탭 — 안내 화면에 그대로 나가는 글을 편집하는 칸과 미리보기. 「이 글은 이용안내 화면에 그대로 나갑니다」 7) 오른쪽 아래 [저장] — 누르기 전에 「이 규칙을 켜면 매물 34개가 숨겨집니다」 요약이 뜬다 8) 아래 규칙 변경 이력 — 누가 언제 무엇을 바꿨는지 규칙 하나가 매물 수십 개를 내린다. 저장 전에 무엇이 일어나는지 숫자로 보여주는 것이 이 화면의 알맹이다.
  ```

### AD-04 · 안전결제 정산·분쟁
- **소속 메뉴**: 운영자
- **뒤로가기**: 좌측 상단에 `‹ 신고 처리 대기열` — 누르면 `AD-01`로 간다
- **일정**: 2027-02-26 ~ 2027-03-03
- **기능정의**: 지표 카드 4개(보관 중인 돈·오늘 정산 예정·분쟁 진행·수수료 수입) · 탭(정산 대기·정산 완료·분쟁) · 기간 고르기 · 표(거래번호·매물·구매자·판매자·금액·수수료·정산 예정일·상태) · 정산 보류와 사유 · 분쟁 건 양쪽 주장 나란히 보기 · 증거 사진 대조 · 분쟁 판단(구매자 환불·판매자 정산·일부 환불)과 근거 기록 · 양쪽에 갈 안내 문구 · 정산 내보내기 · 합계 검산
- **버튼 → 이동화면**:
  - `신고 대기열로` → `AD-01` 신고 처리 대기열
  - `거래 진행 상태 보기` → `PA-05` 거래 진행 상태
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
- **생성 프롬프트**:

  ```
  중고거래 장터 운영자의 안전결제 정산·분쟁 화면을 만들어줘. 남의 돈을 다루는 화면이다. 1) 위쪽 지표 카드 4개 — 지금 보관 중인 돈 / 오늘 정산 예정 / 분쟁 진행 4건 / 이번 달 수수료 수입. 「보관 중인 돈」에는 「구매확정 전이라 아직 판매자에게 안 간 돈이에요」 설명을 작게 2) 탭 — [정산 대기 36] [정산 완료] [분쟁 4] 3) 기간 고르기와 검색(거래번호·닉네임) 4) 표 — 거래번호 / 매물(썸네일 작게) / 구매자 / 판매자 / 결제액 / 수수료 / 판매자 수령액 / 정산 예정일 / 상태 배지. 열 아래에 합계 줄을 두고 「결제액 합 - 수수료 합 = 수령액 합」이 맞는지 보이게 5) 행 끝에 [정산 보류] — 누르면 사유 입력칸이 열리고 상태가 바뀐다 6) 분쟁 탭의 행을 열면 좌우 2단 — 왼쪽에 구매자 주장과 사진, 오른쪽에 판매자 주장과 사진. 가운데 아래에 판단 라디오 3개(구매자에게 전액 환불 / 판매자에게 정산 / 일부 환불 + 금액 입력), 판단 근거 입력칸, 양쪽에 갈 안내 문구 미리보기, [판단 확정] 7) 오른쪽 위 [정산 내역 내보내기] 숫자가 서로 맞는 것이 가장 중요하다. 합계 줄을 표 안에 두고 세 숫자가 늘 아귀가 맞게 해줘.
  ```

### AD-05 · 운영자 로그인
- **소속 메뉴**: 운영자
- **뒤로가기**: 좌측 상단에 `‹ 신고 처리 대기열` — 누르면 `AD-01`로 간다
- **일정**: 2027-03-04 ~ 2027-03-08
- **기능정의**: 운영자 전용 로그인 안내 · 이메일·비밀번호 입력 · 2단계 인증 코드 · 로그인 실패 사유와 남은 시도 횟수 · 5회 실패 시 잠금 예고 · 비밀번호 찾기 · 최초 로그인 시 비밀번호 변경 강제 · 접속 기록 안내 · 회원용 로그인과 다른 곳이라는 고지
- **버튼 → 이동화면**:
  - `신고 대기열로` → `AD-01` 신고 처리 대기열
  - `정산·분쟁으로` → `AD-04` 안전결제 정산·분쟁
- **생성 프롬프트**:

  ```
  중고거래 장터 운영자 로그인 화면을 만들어줘. 1) 가운데 좁은 상자 하나. 위에 「운영자 전용」 배지와 서비스 이름 2) 안내 한 줄 — 「회원 로그인과 다른 곳이에요. 회원이라면 여기가 아니라 앞 화면에서 로그인하세요」 3) 이메일·비밀번호 입력칸과 [로그인] 4) 실패하면 입력칸 아래 붉은 안내 — 「이메일 또는 비밀번호가 맞지 않아요 (남은 시도 3회)」. 5회 실패하면 「30분 동안 잠깁니다」 5) 로그인이 지나가면 2단계 인증 화면으로 — 6자리 칸 여섯 개, 다 채우면 자동 확인, [코드 다시 받기]와 남은 시간 6) 아래 [비밀번호 찾기] 링크 7) 최초 로그인이면 「임시 비밀번호로 들어오셨어요. 새 비밀번호를 정해주세요」 화면으로 이어진다는 안내 8) 맨 아래 작게 「모든 접속은 기록됩니다」 군더더기 없이 상자 하나에 집중시켜줘.
  ```

### CS-01 · 자주 묻는 질문
- **소속 메뉴**: 고객센터
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2027-03-09 ~ 2027-03-11
- **기능정의**: 검색칸과 검색 결과 강조 · 분류 탭(거래·안전결제·신고·매너점수·끌올·계정) · 질문 아코디언과 답 · 분류별 질문 수 배지 · 많이 본 질문 상위 5개 · 도움 여부 투표와 안 됨 선택 시 문의 유도 · 답 안에서 관련 화면으로 가는 링크 · 검색 결과 없을 때 문의 유도
- **버튼 → 이동화면**:
  - `1:1 문의하기` → `CS-02` 1:1 문의
  - `안전거래 안내 보기` → `HO-03` 안전거래·이용안내
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 분류 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
  - 검색 — 입력한 말이 든 것만 남깁니다. 없으면 「검색 결과가 없습니다」와 다시 찾는 길을 보여줍니다
  - 선택 — 고른 개수를 **숫자로 보여주고**, 하나도 안 골랐으면 일괄 단추를 눌리지 않게 둡니다
  - 아코디언 — 누르면 그 자리에서 펴지고 접힙니다. 다른 화면으로 가지 않습니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 자주 묻는 질문 화면을 만들어줘. 1) 위쪽 큰 검색칸 — 「무엇이 궁금하세요?」 2) 분류 탭 — [전체] [거래] [안전결제] [신고·제재] [매너 점수] [끌올·광고] [계정], 탭마다 건수 배지 3) 「많이 본 질문」 5개를 위에 순위와 함께 4) 질문 목록 아코디언 — 누르면 답이 펼쳐진다. 답 안에는 관련 화면으로 가는 링크를 넣어줘 (예: 「안전결제가 뭔가요?」 답에 [안전거래 안내 보기]) 5) 펼친 답 아래에 「도움이 되었나요?」 [예] [아니요]. [아니요]를 고르면 「무엇이 부족했나요?」와 [문의하기] 6) 검색해서 결과가 없으면 「찾는 답이 없나요?」와 [1:1 문의하기] 큰 버튼 7) 맨 아래 운영 시간과 답변까지 걸리는 시간 안내 훑어 읽는 화면이다. 질문 제목만 보이는 접힌 상태를 기본으로 두고 한 화면에 많이 담아줘.
  ```

### CS-02 · 1:1 문의
- **소속 메뉴**: 고객센터
- **뒤로가기**: 좌측 상단에 `‹ 자주 묻는 질문` — 누르면 `CS-01`로 간다
- **일정**: 2027-03-12 ~ 2027-03-17
- **기능정의**: 문의 종류 고르기(거래 문제·안전결제·신고 결과·계정·끌올 결제·그 밖에) · 종류별 추가 입력 · 관련 거래·매물 고르기와 요약 자동 채움 · 제목·내용 입력 · 파일 첨부와 용량 안내 · 답변 받을 곳(앱 알림·이메일·문자) · 예상 답변 시간 · 문의 전에 자주 묻는 질문 보기 유도 · 보낸 문의 목록과 답변 상태 · 답변에 이어 묻기
- **버튼 → 이동화면**:
  - `자주 묻는 질문` → `CS-01` 자주 묻는 질문
  - `거래 진행 상태 보기` → `PA-05` 거래 진행 상태
- **생성 프롬프트**:

  ```
  중고거래 장터의 1:1 문의 화면을 만들어줘. 1) 위쪽 안내 띠 — 「비슷한 질문이 자주 묻는 질문에 있을 수 있어요」와 [먼저 찾아보기] 2) 문의 종류 드롭다운 — 거래에 문제가 있어요 / 안전결제 / 신고 결과가 궁금해요 / 계정 / 끌올·광고 결제 / 그 밖에. 고른 종류에 따라 아래 입력이 달라진다 (거래 문제면 [거래 고르기], 신고 결과면 [신고번호 입력], 결제면 [결제 내역 고르기]) 3) 거래를 고르면 그 거래 요약 카드가 자동으로 붙는다 — 매물·상대·금액·날짜 4) 제목과 내용 입력칸(글자 수 표시) 5) 파일 첨부 — 최대 5개, 각 10MB. 붙인 것은 미리보기, 넘으면 붉은 안내 6) 답변 받을 곳 체크 — 앱 알림· 이메일· 문자 7) 예상 답변 시간 안내 — 「평일 기준 하루 안에 답변드려요」 8) [보내기] — 종류와 내용이 비면 잠긴다 9) 아래에 「보낸 문의」 목록 — 제목, 보낸 날, 상태 배지(접수·답변 완료). 답변 완료를 누르면 대화 형태로 펼쳐지고 [이어서 묻기] 입력칸이 열린다 문의 종류를 고르면 화면이 바뀌는 것이 이 화면의 알맹이다.
  ```

### CS-03 · 공지사항 상세
- **소속 메뉴**: 고객센터
- **뒤로가기**: 좌측 상단에 `‹ 자주 묻는 질문` — 누르면 `CS-01`로 간다
- **일정**: 2027-03-18 ~ 2027-03-23
- **기능정의**: 제목·분류 배지·작성일·조회수 · 본문 · 첨부 파일 내려받기 · 시행일이 있는 공지는 시행일 강조 · 바뀐 규칙이면 무엇이 어떻게 바뀌는지 표 · 이전·다음 공지 이동 · 관련 공지 펼치기 · 목록으로 돌아가기 · 이 공지에 관해 문의하기
- **버튼 → 이동화면**:
  - `공지 목록으로` → `HO-04` 공지·이벤트 목록
  - `문의하기` → `CS-02` 1:1 문의
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 분류 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
  - 펼치 — 누르면 그 자리에서 펴지고 접힙니다. 다른 화면으로 가지 않습니다
- **생성 프롬프트**:

  ```
  중고거래 장터의 공지사항 상세 화면을 만들어줘. 1) 위쪽 분류 배지(공지·이벤트·제재안내)와 제목, 그 아래 작성일·조회수 2) 시행일이 있는 공지면 눈에 띄는 박스 — 「2026년 10월 1일부터 바뀝니다」와 남은 날짜 3) 본문 — 소제목과 문단, 필요하면 표. 규칙이 바뀌는 공지면 「지금 → 바뀐 뒤」 두 열 표 4) 첨부 파일 목록 — 파일 이름·크기와 [내려받기] 5) 아래 [이전 공지] [다음 공지] 화살표와 가운데 [목록으로] 6) 「관련 공지」 접기 — 같은 분류의 최근 공지 5개 7) 맨 아래 「이 공지에 관해 궁금한 점이 있나요?」와 [문의하기] 읽는 화면이다. 본문 폭을 좁혀 읽기 좋게 하고 표는 본문 폭을 넘겨서 넓게 써줘.
  ```

## 5. 화면 이동 흐름 (FLOW)
- `홈`(HO-01) —[매물 자세히 보기]→ `매물 상세`(SE-04)
- `홈`(HO-01) —[내 동네 설정]→ `내 동네 설정`(SE-01)
- `홈`(HO-01) —[카테고리 전체 보기]→ `카테고리 전체 보기`(HO-02)
- `홈`(HO-01) —[판매글 쓰기]→ `판매글 작성`(SL-01)
- `카테고리 전체 보기`(HO-02) —[이 분류 매물 보기]→ `매물 목록`(SE-02)
- `카테고리 전체 보기`(HO-02) —[거래 금지 품목 보기]→ `안전거래·이용안내`(HO-03)
- `안전거래·이용안내`(HO-03) —[안전결제 자세히 보기]→ `안전결제 시작·안내`(PA-01)
- `안전거래·이용안내`(HO-03) —[자주 묻는 질문]→ `자주 묻는 질문`(CS-01)
- `공지·이벤트 목록`(HO-04) —[공지 자세히 보기]→ `공지사항 상세`(CS-03)
- `공지·이벤트 목록`(HO-04) —[홈으로]→ `홈`(HO-01)
- `내 동네 설정`(SE-01) —[이 동네 매물 보기]→ `매물 목록`(SE-02)
- `내 동네 설정`(SE-01) —[홈으로]→ `홈`(HO-01)
- `매물 목록`(SE-02) —[매물 자세히 보기]→ `매물 상세`(SE-04)
- `매물 목록`(SE-02) —[검색으로]→ `검색`(SE-03)
- `매물 목록`(SE-02) —[알림 조건 저장]→ `관심 키워드·알림 설정`(MY-06)
- `매물 목록`(SE-02) —[결과 없을 때]→ `결과 없음`(SE-05)
- `검색`(SE-03) —[결과 전체 보기]→ `매물 목록`(SE-02)
- `검색`(SE-03) —[결과 없을 때]→ `결과 없음`(SE-05)
- `매물 상세`(SE-04) —[채팅하기]→ `채팅방`(CH-02)
- `매물 상세`(SE-04) —[안전결제로 사기]→ `안전결제 시작·안내`(PA-01)
- `매물 상세`(SE-04) —[판매자 프로필 보기]→ `내 프로필·매너 점수`(MY-01)
- `매물 상세`(SE-04) —[신고하기]→ `신고·차단`(CH-04)
- `결과 없음`(SE-05) —[조건 다시 잡기]→ `매물 목록`(SE-02)
- `결과 없음`(SE-05) —[알림 설정하기]→ `관심 키워드·알림 설정`(MY-06)
- `결과 없음`(SE-05) —[홈으로]→ `홈`(HO-01)
- `판매글 작성`(SL-01) —[사진 정리하기]→ `사진 등록·정리`(SL-02)
- `판매글 작성`(SL-01) —[얼마에 팔까 보기]→ `얼마에 팔까 — 시세 보기`(SL-03)
- `판매글 작성`(SL-01) —[등록하기]→ `등록 완료`(SL-04)
- `판매글 작성`(SL-01) —[금지 품목 보기]→ `안전거래·이용안내`(HO-03)
- `사진 등록·정리`(SL-02) —[판매글로 돌아가기]→ `판매글 작성`(SL-01)
- `사진 등록·정리`(SL-02) —[등록하기]→ `등록 완료`(SL-04)
- `얼마에 팔까 — 시세 보기`(SL-03) —[판매글로 돌아가기]→ `판매글 작성`(SL-01)
- `얼마에 팔까 — 시세 보기`(SL-03) —[같은 물건 매물 보기]→ `매물 목록`(SE-02)
- `등록 완료`(SL-04) —[내 판매글 관리]→ `내 판매글 관리`(SL-05)
- `등록 완료`(SL-04) —[끌올하기]→ `끌올하기`(BS-01)
- `등록 완료`(SL-04) —[판매글 더 쓰기]→ `판매글 작성`(SL-01)
- `내 판매글 관리`(SL-05) —[끌올·광고 보기]→ `끌올하기`(BS-01)
- `내 판매글 관리`(SL-05) —[후기 쓰기]→ `거래 후기 쓰기`(MY-04)
- `내 판매글 관리`(SL-05) —[판매글 쓰기]→ `판매글 작성`(SL-01)
- `끌올하기`(BS-01) —[광고 상품 자세히]→ `광고 상품·요금 안내`(BS-02)
- `끌올하기`(BS-01) —[끌올 내역 보기]→ `끌올·광고 내역`(BS-03)
- `끌올하기`(BS-01) —[내 판매글로]→ `내 판매글 관리`(SL-05)
- `광고 상품·요금 안내`(BS-02) —[끌올하러 가기]→ `끌올하기`(BS-01)
- `광고 상품·요금 안내`(BS-02) —[자주 묻는 질문]→ `자주 묻는 질문`(CS-01)
- `끌올·광고 내역`(BS-03) —[끌올하러 가기]→ `끌올하기`(BS-01)
- `끌올·광고 내역`(BS-03) —[내 판매글로]→ `내 판매글 관리`(SL-05)
- `채팅 목록`(CH-01) —[대화 열기]→ `채팅방`(CH-02)
- `채팅 목록`(CH-01) —[매물 보러 가기]→ `매물 목록`(SE-02)
- `채팅방`(CH-02) —[약속 잡기]→ `거래 약속 잡기`(CH-03)
- `채팅방`(CH-02) —[안전결제로 거래하기]→ `안전결제 시작·안내`(PA-01)
- `채팅방`(CH-02) —[신고·차단하기]→ `신고·차단`(CH-04)
- `채팅방`(CH-02) —[매물 보기]→ `매물 상세`(SE-04)
- `채팅방`(CH-02) —[채팅 목록으로]→ `채팅 목록`(CH-01)
- `거래 약속 잡기`(CH-03) —[채팅으로 돌아가기]→ `채팅방`(CH-02)
- `거래 약속 잡기`(CH-03) —[안전결제로 거래하기]→ `안전결제 시작·안내`(PA-01)
- `신고·차단`(CH-04) —[채팅으로 돌아가기]→ `채팅방`(CH-02)
- `신고·차단`(CH-04) —[안전거래 안내 보기]→ `안전거래·이용안내`(HO-03)
- `안전결제 시작·안내`(PA-01) —[결제하기]→ `결제`(PA-02)
- `안전결제 시작·안내`(PA-01) —[직거래로 하기]→ `거래 약속 잡기`(CH-03)
- `안전결제 시작·안내`(PA-01) —[안전거래 안내 보기]→ `안전거래·이용안내`(HO-03)
- `결제`(PA-02) —[거래 진행 상태 보기]→ `거래 진행 상태`(PA-05)
- `결제`(PA-02) —[안전결제 안내로]→ `안전결제 시작·안내`(PA-01)
- `발송 등록 (판매자)`(PA-03) —[거래 진행 상태 보기]→ `거래 진행 상태`(PA-05)
- `발송 등록 (판매자)`(PA-03) —[채팅으로]→ `채팅방`(CH-02)
- `수령 확인·구매확정`(PA-04) —[후기 남기기]→ `거래 후기 쓰기`(MY-04)
- `수령 확인·구매확정`(PA-04) —[거래 진행 상태 보기]→ `거래 진행 상태`(PA-05)
- `수령 확인·구매확정`(PA-04) —[신고하기]→ `신고·차단`(CH-04)
- `거래 진행 상태`(PA-05) —[수령 확인하기]→ `수령 확인·구매확정`(PA-04)
- `거래 진행 상태`(PA-05) —[발송 등록하기]→ `발송 등록 (판매자)`(PA-03)
- `거래 진행 상태`(PA-05) —[채팅으로]→ `채팅방`(CH-02)
- `내 프로필·매너 점수`(MY-01) —[받은 후기 보기]→ `받은 후기·매너 점수 상세`(MY-05)
- `내 프로필·매너 점수`(MY-01) —[내 판매글 관리]→ `내 판매글 관리`(SL-05)
- `내 프로필·매너 점수`(MY-01) —[찜한 물건 보기]→ `찜한 물건`(MY-03)
- `내 프로필·매너 점수`(MY-01) —[알림 설정]→ `관심 키워드·알림 설정`(MY-06)
- `구매 내역`(MY-02) —[후기 쓰기]→ `거래 후기 쓰기`(MY-04)
- `구매 내역`(MY-02) —[거래 진행 상태 보기]→ `거래 진행 상태`(PA-05)
- `구매 내역`(MY-02) —[매물 보러 가기]→ `매물 목록`(SE-02)
- `찜한 물건`(MY-03) —[매물 자세히 보기]→ `매물 상세`(SE-04)
- `찜한 물건`(MY-03) —[매물 보러 가기]→ `매물 목록`(SE-02)
- `거래 후기 쓰기`(MY-04) —[받은 후기 보기]→ `받은 후기·매너 점수 상세`(MY-05)
- `거래 후기 쓰기`(MY-04) —[구매 내역으로]→ `구매 내역`(MY-02)
- `받은 후기·매너 점수 상세`(MY-05) —[후기 쓰기]→ `거래 후기 쓰기`(MY-04)
- `받은 후기·매너 점수 상세`(MY-05) —[내 프로필로]→ `내 프로필·매너 점수`(MY-01)
- `관심 키워드·알림 설정`(MY-06) —[매물 보러 가기]→ `매물 목록`(SE-02)
- `관심 키워드·알림 설정`(MY-06) —[내 프로필로]→ `내 프로필·매너 점수`(MY-01)
- `신고 처리 대기열`(AD-01) —[신고 자세히 보기]→ `신고 상세·조치`(AD-02)
- `신고 처리 대기열`(AD-01) —[금지품목·규칙 관리]→ `금지품목·거래규칙 관리`(AD-03)
- `신고 상세·조치`(AD-02) —[대기열로 돌아가기]→ `신고 처리 대기열`(AD-01)
- `신고 상세·조치`(AD-02) —[회원 프로필 보기]→ `내 프로필·매너 점수`(MY-01)
- `금지품목·거래규칙 관리`(AD-03) —[신고 대기열로]→ `신고 처리 대기열`(AD-01)
- `금지품목·거래규칙 관리`(AD-03) —[이용안내 화면 보기]→ `안전거래·이용안내`(HO-03)
- `안전결제 정산·분쟁`(AD-04) —[신고 대기열로]→ `신고 처리 대기열`(AD-01)
- `안전결제 정산·분쟁`(AD-04) —[거래 진행 상태 보기]→ `거래 진행 상태`(PA-05)
- `운영자 로그인`(AD-05) —[신고 대기열로]→ `신고 처리 대기열`(AD-01)
- `운영자 로그인`(AD-05) —[정산·분쟁으로]→ `안전결제 정산·분쟁`(AD-04)
- `자주 묻는 질문`(CS-01) —[1:1 문의하기]→ `1:1 문의`(CS-02)
- `자주 묻는 질문`(CS-01) —[안전거래 안내 보기]→ `안전거래·이용안내`(HO-03)
- `1:1 문의`(CS-02) —[자주 묻는 질문]→ `자주 묻는 질문`(CS-01)
- `1:1 문의`(CS-02) —[거래 진행 상태 보기]→ `거래 진행 상태`(PA-05)
- `공지사항 상세`(CS-03) —[공지 목록으로]→ `공지·이벤트 목록`(HO-04)
- `공지사항 상세`(CS-03) —[문의하기]→ `1:1 문의`(CS-02)

## 6. 빌드 가이드
### 구조
- 각 **화면ID**를 하나의 라우트/페이지로 만드세요 (예: `PCHOME0110` → `/home`).
- 화면명은 페이지 제목·컴포넌트 이름의 기준으로 사용하세요.
- **2장 공통 요소**(헤더·GNB·푸터)를 공용 레이아웃으로 만들고 모든 화면이 감싸도록 하세요.
- **쓰는 사람이 다른 메뉴는 헤더를 나누세요.** 손님이 쓰는 메뉴(홈·매물 찾기·판매하기·끌올·광고)와 운영자가 쓰는 메뉴(운영자)를 **한 헤더에 나란히 놓지 마세요.** 둘은 사실상 다른 사이트입니다.
  · 손님 영역과 운영자 영역을 각각 다른 레이아웃(헤더·사이드바)으로 만들고, 오갈 때는 **따로 마련한 진입점** 하나로만 이동하세요(예: 우측 상단 `운영자 화면으로`, 운영자 화면에서는 `고객 화면 보기`).
  · 운영자 영역은 보통 좌측 사이드바 + 상단 바가 맞습니다. 손님 영역의 검색·카테고리 내비를 그대로 쓰지 마세요.
- **버튼 → 이동화면** 관계를 링크/네비게이션으로 연결하세요.
- **4장 각 화면의 `눌렀을 때 (이 화면 안에서 끝남)` 항목을 그대로 만드세요.** 적혀 있는 것은 빠짐없이 실제로 동작해야 합니다 — 누르면 값이 바뀌고, 다시 누르면 되돌아옵니다.
  · 그 항목에 없더라도 **누를 수 있게 생긴 것은 전부 눌리게** 만드세요. 적힌 것은 최소한이지 전부가 아닙니다.
- **누를 수 있게 보이는 것은 전부 눌리게 만드세요.** 두 종류로 나눠 처리합니다.
  · **화면 안에서 끝나는 조작은 진짜로 동작해야 합니다** — 탭 전환, 좌측 카테고리·필터 선택, 목록에서 항목 고르기(디자이너·옵션·시간), 펼치기·접기, 캘린더 이전/다음 달, 수량 ±, 체크·토글. 자바스크립트로 실제 상태를 바꾸고 화면을 다시 그리세요. 이걸 안내 문구로 때우지 마세요.
  · **서버가 있어야 하는 것만** 짧은 안내(토스트)로 대신합니다 — 저장·삭제·발송·결제·전화 걸기·이의 신청 등.
  · **이런 것은 토스트로 때우지 마세요** — `모두 보기`(펼치기), `선택`·`적용`(고른 상태로 바꾸기), `추가`(줄 하나 늘리기), `닫기`·`✕`(정말 닫기), `도움이 됐어요`(숫자 올리기), 탭·필터·정렬. 전부 화면 안에서 끝나는 일이라 실제로 되어야 합니다.
  · **모달·팝업은 뜨는 것만으로 끝이 아닙니다.** `취소`와 `✕`는 정말 닫히고, `확인`은 눌렀을 때 무엇이 달라지는지 보여야 합니다(다음 화면으로 가거나, 목록이 바뀌거나, 안내가 뜨거나). 닫히지 않는 팝업은 갇힌 화면이 됩니다.
  · 아무 것도 하지 않는 `<button>`을 남기지 마세요. 만들고 나서 **버튼을 하나씩 눌러 보고**, 반응이 없는 것이 있으면 위 둘 중 하나로 채우세요.
- **뒤로가기는 화면마다 정해져 있습니다. 4장 각 화면 스펙의 `뒤로가기` 항목을 그대로 만드세요.** 31개 화면에 붙고, 나머지 9개(메뉴 첫 화면)에는 없습니다. 좌측 상단에 `‹ 상위 화면 이름`과 현재 위치(예: `매장 탐색 › 매장 상세`)를 둡니다.
  · 여러 단계짜리 흐름(1→2→3)은 **이전 단계로 돌아갈 수 있게** 하세요. 되돌아갔을 때 앞서 고른 값이 남아 있어야 합니다.
  · **한 묶음으로 나란히 보여주는 것은 동일 계위입니다.** 탭 줄에 걸린 화면들, 같은 사이드바에 나란히 올라온 화면들이 그렇습니다. 그 안을 오가는 길이 이미 탭·사이드바이므로 뒤로가기를 겹쳐 두지 마세요 — 같은 줄에 있는 화면인데 어떤 건 있고 어떤 건 없어집니다.
  · 빈 화면·오류·마감처럼 막다른 화면에는 **다음에 할 일 버튼**을 반드시 하나 두세요(예: `목록으로`, `다시 시도`, `둘러보기`).
  · **전체 화면 목록으로 돌아가는 길**을 **146개 화면 «전부»에** 두세요 — 오른쪽 아래에 떠 있는 `화면 목록` 단추 하나면 됩니다. 누르면 `index.html`(전체 화면 목록)으로 갑니다. 접힌 패널 안에 `화면 정보` 같은 이름으로 넣으면 아무도 못 찾습니다.
  · 다 만든 뒤 **화면을 하나씩 열어 뒤로가기가 있는지 세어 보세요.** 하나라도 빠져 있으면 채웁니다.
### 화면 구현
- 각 화면의 **생성 프롬프트**를 그 화면 구현 지시로 그대로 사용하세요.
- 반복 UI(카드·리스트 행·폼·버튼)는 **공용 컴포넌트**로 만들어 재사용하세요.
- 필요하면 상태 화면(로딩·빈 결과·에러)도 함께 만드세요.
### 디자인
- **디자인 프리셋 파일을 함께 넣었다면 그것이 우선입니다.** 1장의 디자인 컨셉이나 화면별 프롬프트에 다른 색·톤이 적혀 있어도 프리셋을 따르세요. 충돌이 아니라 **프리셋이 정답**입니다.
- 프리셋의 **레이아웃 골격**(히어로·목록·내비·상세를 어떻게 놓을지)을 먼저 잡고 색을 입히세요. 색만 맞추면 어떤 테마로 만들어도 같은 화면이 나옵니다.
- 프리셋이 없다면 1장의 **디자인 컨셉**을 전역 스타일(색·톤·라운드·여백)로 토큰화해 일관되게 적용하세요.
- **글꼴은 프리셋이 없어도 한 벌로 고정하세요.** 기본은 `Paperlogy`이고, 못 불러오면 `Pretendard` → `Noto Sans KR` 순으로 물러납니다. CSS로는 `font-family: Paperlogy, Pretendard, 'Noto Sans KR', sans-serif` 한 줄이면 됩니다.
  · **글꼴은 한 벌만 씁니다.** 제목과 본문에 서로 다른 글꼴을 쓰지 마세요 — 두 벌이 되는 순간 어디에 무엇을 쓸지가 화면마다 갈립니다. 위계는 글꼴이 아니라 **크기와 굵기**로 만듭니다.
  · 굵기는 **400·600·700 셋만**, 크기는 본문 15 · 카드 제목 18 · 섹션 제목 22 · 페이지 제목 32을 기준으로 잡으세요.

#### 좁은 화면에서 넘치지 않게

폰 폭(**390px**)에서 **페이지가 좌우로 스크롤되면 안 됩니다.**
표·그림처럼 일부러 옆으로 넘기는 것은 «자기 상자 안에서만» 넘깁니다.

**왜 적어 두나** — 열 수(1024에서 2열, 720에서 1열)는 다들 맞추는데, 가로로 밀리는지는 잘 안 봅니다.
그리고 미는 것은 대개 표가 아닙니다. **표 밑의 페이지 번호 줄**과 「다음 프로그램 ›」 같은
이동 단추가 한 줄로 늘어서면서 밉니다 — 표는 감쌌는데 그 옆은 안 감싸기 때문입니다.

- 폰 폭(390px)에서 **페이지가 좌우로 스크롤되면 안 됩니다.** 표·그림처럼 일부러 옆으로 넘기는 것은 자기 상자 안에서만 넘깁니다.
- **페이지 번호 줄**은 줄바꿈하거나(`flex-wrap: wrap`) 자기 상자 안에서 옆으로 돌립니다(`overflow-x: auto`). 쪽수가 늘면 그만큼 길어지므로 한 줄로 두면 반드시 넘칩니다.
- **「이전 · 다음」 같은 이동 단추**는 줄바꿈을 허용하고, 글자가 길면 줄여 적습니다(`다음 프로그램 ›` → `다음 ›`).
- **표**는 `overflow-x: auto` 상자로 감쌉니다. 표 자체를 좁히지 않습니다 — 칸 글자가 세로로 접혀 읽을 수 없게 됩니다.
- **탭 줄·필터 줄**도 같습니다. 개수가 늘어나는 줄은 전부 줄바꿈이나 상자 안 스크롤 중 하나를 씁니다.
- ⛔ 옆으로 흘려보낼 때는 **다른 축을 반드시 막습니다** — `overflow: auto hidden`. `overflow-x` 만 열면 브라우저가 세로도 함께 열어, 테두리 1px 때문에 세로 스크롤바가 생깁니다.
- ⛔ **푸터는 화면 바닥에 붙입니다.** 내용이 짧은 화면(완료·빈 목록·오류)에서 푸터가 화면 가운데 떠 있으면 «덜 만들어진 화면»으로 보입니다. `body` 를 세로 flex 로 두고 푸터에 `margin-top: auto` 를 줍니다.
- ⛔ **`[hidden]` 을 쓸 때는 `[hidden] { display: none !important; }` 을 반드시 함께 넣습니다.** `[hidden]` 은 브라우저 기본값이라 `.row { display: flex }` 같은 규칙 하나에도 집니다. 그러면 «거르는 단추를 눌러도 목록이 안 줄어드는» 화면이 됩니다 — 숫자 안내만 바뀌고 목록은 그대로라, 눌러 보지 않으면 안 보입니다.
- ⛔ **그리드·플렉스 칸의 자식에는 `min-width: 0` 을 줍니다.** 기본값이 `auto` 라, 칸을 340px 로 정해도 내용이 그보다 넓으면 칸 밖으로 삐져나와 옆 칸을 덮습니다.
- ⛔ **여백을 지우는 클래스(`flush` 등)와 음수 마진을 «같이» 쓰지 않습니다.** 상쇄할 여백이 이미 없는데 음수 마진을 걸면 그만큼 넓어져 칸 밖으로 나갑니다.
- ⛔ **«세로» flex 로 바꾼 상자의 자식에도 `min-width: 0` 을 줍니다.** 푸터를 바닥에 붙이려고 `main` 이나 감싸개를 `flex-direction: column` 으로 바꾸면, 그 자식은 `min-width` 가 `auto` 라 «제 속내용보다 좁아지지 못합니다». 폭 360 짜리 폰에서 안쪽 상자가 364px 로 버티며 화면이 통째로 옆으로 밀립니다.
- ⛔ **폭을 잴 때는 390 과 360 을 «둘 다» 봅니다.** 바로 위의 세로 flex 밀림은 390 에서는 안 보이고 360 에서만 나옵니다. 한 폭만 재면 성한 줄 알고 넘어갑니다.

```css
/* 개수가 늘어나는 줄은 접거나, 자기 상자 안에서 돌린다 */
.pager { display: flex; flex-wrap: wrap; justify-content: center; gap: 4px; }
.tabs, .filters { display: flex; flex-wrap: wrap; gap: 8px; }
.tabs-scroll { overflow: auto hidden; }  /* 탭을 옆으로 흘릴 때도 마찬가지 */

/* 줄바꿈 대신 «옆으로 흘려보낼» 때는 반드시 두 축을 다 적는다 */
.scroll-x { overflow: auto hidden; }   /* overflow-x 만 쓰면 세로 스크롤바가 생긴다 */

/* 표는 좁히지 말고 감싼다 — 좁히면 칸 글자가 세로로 접혀 못 읽는다 */
.table-wrap { overflow: auto hidden; }   /* x 만 열면 세로 스크롤바가 딸려 온다 */
.table-wrap table { min-width: max-content; }

/* 좌우로 벌리는 줄에 긴 단추가 들어오면 내려보낸다 */
.row-between { display: flex; flex-wrap: wrap; align-items: center; justify-content: space-between; gap: 12px; }

/* 푸터는 «화면 바닥»에 붙인다 — 짧은 화면에서 가운데 떠 있으면 안 만들어진 것처럼 보인다 */
body { min-height: 100dvh; display: flex; flex-direction: column; }
body > main { flex: 1 0 auto; }
body > footer { margin-top: auto; }

/* ⛔⛔ 거르기가 목록을 못 줄이는 사고는 거의 다 여기서 난다.
   [hidden] 은 «브라우저 기본값»이라 .row{display:flex} 한 줄에도 진다.
   숫자 안내는 바뀌는데 목록만 그대로인 화면이 만들어진다. */
[hidden] { display: none !important; }

/* ⛔ 위 푸터 규칙은 «body 에만» 씁니다. 감싸개(.app 같은 것)에 옮겨 붙이지 마세요.
   사이드바형(감싸개가 grid 2열)에 flex-direction:column 을 덮었더니
   사이드바가 화면 폭을 다 먹고 본문이 그 아래로 밀렸습니다(2026-08-19에 실제로 그랬습니다).
   감싸개는 «제 짜임 그대로» 둡니다 — 사이드바형이면 grid 2열, 상단바형이면 세로 flex.
   짜임이 다른 화면 두 가지를 꼭 같이 열어 보세요. 한쪽에서 되는 것이 다른 쪽을 무너뜨립니다. */

/* 사진 위에 얹는 단추(카드 위 찜 하트·빠른 담기)는 사진에 덮여 안 눌립니다.
   «얹은 단추에만» 아래 둘을 주세요 — 줄 안에 서는 단추에 주면 자리가 튑니다. */
.card { position: relative; }              /* 얹을 자리의 기준점 */
.card > .overlay-btn { position: absolute; z-index: 2; }

/* 그리드·플렉스 자식은 min-width 를 0 으로 — 안 그러면 칸보다 넓은 내용이 삐져나온다 */
.split-r > *, .split-l > *, .app > * { min-width: 0; }

/* ⭐ «세로» flex 로 바꾼 상자의 자식에도 똑같이 걸어 주세요 — 여기가 제일 자주 빠집니다.
   푸터를 바닥에 붙이려고 main 이나 감싸개를 flex-direction:column 으로 바꾸면,
   그 자식은 min-width 가 auto 라 «제 속내용보다 좁아지지 못합니다».
   폭 360 짜리 폰에서 안쪽 상자가 364px 로 버티며 화면이 통째로 옆으로 밀립니다.
   ⚠ 폭 390 에서는 안 보이고 360 에서만 나옵니다 — 두 폭을 다 재세요.
   (2026-08-25 검수 · 판매팩 여덟 벌에서 같은 자리가 나왔습니다) */
main > *, .appmain > *, .edmain > * { min-width: 0; width: 100%; }
```

#### 읽기 폭 — 글을 한 단으로 좁힐 때

글이 주인공인 자리(상세 본문·안내·약관)는 **최대 760px** 로 좁히고 가운데 정렬합니다.
콘텐츠 영역(위의 최대 폭) **안에서** 좁히는 것입니다 — 바깥 폭을 바꾸는 게 아닙니다.

**왜 좁히나** — 한 줄이 길면 다음 줄 첫 글자를 눈이 못 찾습니다. 1440px 폭에 본문을 꽉 채우면
한 줄에 90자가 넘어가고, 읽는 사람은 두세 줄마다 같은 줄을 다시 읽게 됩니다.

- **이 값 하나만 씁니다.** 화면마다 680·720·760 처럼 다르게 잡지 마세요 — 저희도 그렇게 적혀 있었고,
  같은 팩 안의 상세 화면끼리 본문 폭이 달라 서로 다른 사이트처럼 보였습니다.
- **카드 격자에는 쓰지 않습니다.** 여기는 «글»을 좁히는 값입니다. 카드를 몇 열로 놓을지는 레이아웃이 정합니다.
- 사진·표·코드처럼 넓어야 읽히는 것은 이 폭을 넘겨도 됩니다. 좁히는 건 글줄입니다.

#### 줄어드는 지점 — 두 곳만 씁니다

화면이 좁아질 때 칸이 줄어드는 지점은 **1024px** 와 **720px**, 이 둘뿐입니다.
1024 은 태블릿 가로, 720 은 세로입니다. 720px 아래에서는 좌우 여백도 16px 로 줄입니다.

- **여기 없는 지점을 새로 만들지 마세요.** 560·860·520 처럼 지어낸 지점이 섞이면, 같은 팩으로 만든 화면끼리 줄어드는 자리가 달라 서로 다른 사이트처럼 보입니다.
- 더 잘게 나눠도 사람 눈에는 안 보입니다. 대신 **지킬 곳만 늘어납니다.**
- 두 지점에서 열이 어떻게 줄어드는지는 위 레이아웃 표에 적혀 있습니다(예: 3열 → 2열 → 1열).

#### 격자 — 카드를 늘어놓는 자리

카드·타일을 격자로 늘어놓을 때 **좌우와 위아래를 다른 값으로** 벌립니다.

| 자리 | 값 |
| --- | --- |
| **카드와 카드 사이** (좌우) | 위 눈금의 「카드와 카드 사이 (가로)」 |
| **줄과 줄 사이** (위아래) | 위 눈금의 「덩어리와 덩어리 사이」 |

**같은 값을 쓰면 줄이 안 갈립니다.** 카드에 상자가 있을 때는 테두리가 경계를 만들어 줬지만,
상자를 걷어낸 뒤로는 **위 카드의 가격과 아래 카드의 사진이 붙어** 한 덩어리로 읽힙니다.
위아래를 더 벌려야 "여기까지가 한 줄"이 보입니다.

| 나누는 것 | 정하는 곳 |
| --- | --- |
| 몇 열로 나눌지 | 레이아웃 프리셋 |
| **사이를 얼마나 벌릴지** | **위 간격 눈금 (좌우·위아래 따로)** |
| 카드가 어떻게 생겼는지 | 카드 모양 |

- **레이아웃 프리셋이 「넓게」라고 해도 값을 새로 만들지 마세요.** 레이아웃은 *몇 열로 무엇을 어디에*를
  말하는 층이고, *얼마나 벌릴지*는 이 눈금이 정합니다. 실제로 그 한 줄 때문에 40px을 쓴 격자가 있었고,
  같은 화면의 다른 격자는 16px이라 두 값이 나란히 놓였습니다.

**간격은 변수로 못 박고, 자리마다 그것만 씁니다.** CSS 맨 위에 이걸 그대로 붙여 넣으세요.

```css
:root{
  --wrap: 1440px;      /* 콘텐츠 폭 — 헤더·본문·푸터·고정바가 모두 이 값을 쓴다 */
  --pad-x: 24px;    /* 좌우 여백 */
  --card-gap:   16px;     /* 카드와 카드 사이 — 좌우 */
  --card-gap-y: 28px;     /* 카드 줄과 줄 사이 — 위아래 */
  --side-w:     260px;               /* 사이드바 폭 (사이드바 뼈대에서만) */
  --side-gap:   32px;                /* 사이드바와 본문 사이 */
  --row-gap:    14px;                /* 가로 행 카드 안: 썸네일과 글자 사이 */

  --card-pad:   24px;                /* 카드 안쪽 여백 */
  --row-h:      48px;                /* 표 한 줄 높이 — 안 정하면 줄마다 들쭉날쭉해진다 */
  --btn-gap:    8px;                 /* 나란히 놓은 버튼 사이 */
}
@media (max-width: 720px){
  :root{ --pad-x: 16px; --card-gap: 12px; }
}
.wrap { max-width: var(--wrap); margin-inline: auto; padding-inline: var(--pad-x); }
.cards{ display: grid; column-gap: var(--card-gap); row-gap: var(--card-gap-y); }

/* 지표 줄 — 화면 맨 위에 숫자 카드를 나란히 놓는 자리.
   카드 격자(.cards)와 다른 물건이므로 .cards 를 쓰지 않는다. 지표는 넷이 기본이다. */
.stats{ display: grid; grid-template-columns: repeat(4, 1fr);
        column-gap: var(--card-gap); row-gap: var(--card-gap-y); }
@media (max-width: 1024px){ .stats{ grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 720px){ .stats{ grid-template-columns: 1fr; } }

/* 모자이크 — 홈 첫 화면에 「큰 1장 + 작은 4장」을 놓는 자리.
   첫 장이 2칸×2줄, 나머지 넷이 옆을 채운다.
   어느 것을 크게 할지 class 로 정하지 않는다 — 맨 앞에 둔 것이 큰 것이다. */
.mosaic{ display: grid; grid-template-columns: repeat(4, 1fr);
         column-gap: var(--card-gap); row-gap: var(--card-gap-y); }
.mosaic > :first-child{ grid-column: span 2; grid-row: span 2;
                        display: flex; flex-direction: column; }
/* 큰 칸의 사진은 카드 비율을 따르지 않는다 — 두 줄 높이와 안 맞아 제목이 밀려난다.
   큰 칸에서는 사진이 남는 자리를 채운다. */
.mosaic > :first-child > .thumb{ aspect-ratio: auto; flex: 1; min-height: 0; }
@media (max-width: 1024px){
  .mosaic{ grid-template-columns: repeat(2, 1fr); }
  .mosaic > :first-child{ grid-row: auto; }   /* 2열에서 세로로 겹치면 빈칸이 생긴다 */
}
@media (max-width: 720px){
  .mosaic{ grid-template-columns: 1fr; }
  .mosaic > :first-child{ grid-column: auto; }
}

/* 상세 화면의 대표 사진 — 카드 비율을 그대로 쓰지 않는다.
   4:3 을 본문 폭 1392px 에 적용하면 세로가 1044px 이 되어 노트북 화면을 통째로 덮는다.
   가로로 눕히고(16:9) 세로를 420px 로 막는다 — 사진 아래 내용이 같이 보이는 선이다. */
.detail-img{ aspect-ratio: 16 / 9; max-height: 420px; width: 100%;
             border-radius: 12px; overflow: hidden; }
.detail-img > img{ width: 100%; height: 100%; object-fit: cover; display: block; }

/* 간격은 반드시 이 변수로 쓴다. gap:16px 처럼 숫자를 직접 적으면 자리마다 값이 갈린다. */
```

- **`--card-pad`·`--row-h`·`--btn-gap` 도 꼭 쓰세요.** 카드 안쪽 여백, 표 한 줄 높이,
  버튼 사이 간격입니다. 이 셋을 안 정해주면 화면마다 다르게 나옵니다 — 저희도 겪었습니다.
- **`.stats`·`.mosaic`·`.detail-img` 는 카드 격자와 다른 자리입니다.** 지표 숫자 줄,
  홈의 큰 사진 한 장 + 작은 넷, 상세 화면의 대표 사진. 카드 규칙을 여기 갖다 쓰면
  사진이 화면을 다 덮습니다.

- **격자에 `gap: 20px` 처럼 숫자를 직접 쓰지 마세요.** 한 번 쓰는 순간 그 자리만 달라지고,
  나중에 값을 바꿀 때 그 자리만 안 바뀝니다. 실제로 한 사이트에서 좌우가 3·8·16·20·24px
  다섯 가지로 갈라진 적이 있습니다 — 전부 `gap:` 에 숫자를 적어서 생긴 일입니다.
- **`gap` 한 줄로 합치지도 마세요.** 좌우와 위아래는 값이 다르므로 `column-gap`·`row-gap` 둘로 씁니다.
- **이름도 이대로 씁니다.** `--grid-x`·`--gap-col` 처럼 제 나름의 이름을 지으면, 나중에
  다른 사람이 볼 때 그게 같은 자리인지 알 수가 없습니다. 실제로 같은 규칙을 준 두 사이트가
  하나는 `--grid-x/--grid-y`, 하나는 `--gap-col/--gap-row` 를 써서 값이 따로 놀았습니다.
- 값을 바꾸고 싶으면 **`:root` 의 두 줄만** 고칩니다. 그러면 화면 전체가 한 번에 따라옵니다.
- 히어로의 좌우 2단처럼 **카드 격자가 아닌 자리**는 이 규칙 밖입니다. 거기는 화면 뼈대라 레이아웃이 정합니다.

#### 박스형 콘텐츠 — 몇 개를 늘어놓고 사진은 어떤 비율로

사진이 든 카드를 격자로 늘어놓을 때, **몇 개를 놓느냐가 사진 비율을 정합니다.**

| 한 줄에 | 사진 비율 | 왜 |
| --- | --- | --- |
| **2개** | **4:3** | 칸이 넓으니 가로로 넓은 사진 |
| **3개** | **4:3** | 위와 같음 |
| **4개** | **1:1** | 칸이 좁다. 정사각이 가장 얌전하다 |

4개짜리를 세로로 긴 3:4로 뒀더니 **카드 한 줄이 화면을 다 먹었습니다.** 칸이 좁을수록 세로로
길게 두고 싶어지지만, 그러면 한 줄만 보이고 다음 단락이 안 보입니다. 좁은 칸에는 정사각입니다.

- **한 줄에 하나만 두지 않습니다.** 마지막 줄에도 하나만 남기지 마세요 —
  **항목 수를 열 수에 맞춰** 3열이면 3·6·9개, 4열이면 4·8개로 둡니다.
- **한 사이트에서 카드 사진 비율은 하나입니다.** 위 표의 짝(2·3열↔4:3, 4열↔1:1) 밖의 비율은 만들지 마세요.
  같은 홈에 세로 카드와 16:9가 섞여 있어서 **사는 사람이 사진을 두 벌 준비해야 했던** 적이 있습니다.
- **배너·차트·영상·프로필은 이 규칙 밖입니다.** 가로로 긴 배너·영상(16:9)·프로필(1:1)은 제 나름의 비율이 있습니다.
- 상품 사진처럼 **정사각이 업계 표준인 분야**는 1:1을 써도 됩니다. 다만 그때도 한 사이트 안에서는 하나로 갑니다.
- **가로 배너 한 장으로 한 단락을 채우지 마세요.** 배너는 위 표 밖이지만 "한 줄에 하나만 두지 않는다"는 그대로 걸립니다.
  배너 한 장이 있던 자리는 **두 장으로 나누고** 4:3으로 맞추세요.
- **가로로 넘기는 줄(캐러셀)도 몇 개 보이느냐로 셉니다.** 뒤에 더 있어도 눈에 4개가 보이면 4개 — 비율도 1:1입니다.
  카드 폭을 `260px`처럼 박아 두면 화면 폭이 바뀔 때 3.4개씩 어정쩡하게 걸치니, **칸 수로 나눠서** 정하세요.
- **가로로 넘기는 줄에는 좌우 화살표를 두고, 아래 스크롤바는 감춥니다.** 넘길 게 있다는 건
  화살표가 알려 줍니다. 스크롤바까지 나오면 줄 밑에 회색 막대가 하나 더 생겨 지저분합니다.

```css
.carousel{ scrollbar-width:none; -ms-overflow-style:none; }
.carousel::-webkit-scrollbar{ display:none; }
```

  화살표가 **없는데** 스크롤바까지 감추면 넘길 게 있다는 걸 아무도 모릅니다. 둘은 한 벌입니다.

#### 분리형과 혼합형 — 글자를 사진 아래 두느냐 위에 얹느냐

박스형 콘텐츠는 두 가지입니다. **어느 쪽이든 위의 개수·비율 규칙은 똑같이 적용됩니다.**

| | **분리형** | **혼합형** |
| --- | --- | --- |
| 생김새 | 사진 아래에 글자 | 사진 위에 글자를 얹음 |
| 담을 수 있는 것 | 제목·설명·평점·가격까지 여러 줄 | 제목 + 한 줄 정도 |
| 사진이 하는 일 | 무엇인지 알려 준다 | 분위기를 만든다 |
| 쓰는 자리 | 상품·강의 목록처럼 **고르는** 자리 | 특집·기획전처럼 **끌어당기는** 자리 |

**분리형은 상자를 두르지 마세요.** 흰 배경에 테두리·그림자까지 두르면, 사진과 글자가 이미 한 덩어리인데
선을 한 번 더 치는 셈입니다. 격자로 늘어놓으면 그 선이 줄줄이 겹쳐 화면이 답답해집니다.
**모서리는 사진이 가집니다** — 상자를 없앤 자리에서 사진이 카드 노릇을 합니다.
(혼합형은 사진이 곧 배경이라 이 규칙 밖입니다.)

```css
/* 사진 + 글자로 된 카드 — 상자를 두르지 않는다 */
.card{
  background: none;   /* surface 나 흰색을 깔지 않는다 */
  border: 0;
  box-shadow: none;
}
.card .thumb{ border-radius: 12px; }  /* 모서리는 사진이 가진다 */
.card .body{ padding: 16px 0 0; }     /* 좌우 여백 0 — 상자가 없으니 안쪽 여백도 없다 */
```

**`background: var(--surface)` 나 `border: 1px solid` 를 카드에 쓰면 이 규칙을 어긴 것입니다.**
사진이 없는 상자(설정 패널·안내 상자·표를 감싼 것)는 이 규칙 밖입니다 — 거기는 배경이 있어야
덩어리로 읽힙니다. **사진과 글자로 된 카드**만 걷어냅니다.

상자를 걷어내면 두 가지가 따라옵니다.

- **글자가 흰색이 아니라 페이지 배경 위**에 놓입니다. 대비를 다시 재세요.
- **카드끼리 갈라 주던 테두리가 없어집니다.** 줄과 줄 사이를 더 벌려 그 몫을 대신하세요.

그리고 **카드 안에는 선을 긋지 마세요.** 가격 위에 선을 두면 상자가 없는 상태에서는 그 선이 카드를
가르는 것처럼 보여, 사진·제목과 가격이 따로 노는 두 덩어리로 읽힙니다.
**갈라야 할 것은 카드 안이 아니라 카드끼리**입니다.

혼합형을 쓸 때 반드시 지킬 것:

- **어둠막을 까세요.** 사진 위에 흰 글자를 그냥 얹으면 사진의 밝은 자리에서 글자가 사라집니다.
  **사진은 사는 분이 바꿀 것**이라 우리가 밝기를 미리 알 수 없습니다.
  글자 쪽에서 반대편으로 옅어지는 어둠막을 항상 깔아 두면 어떤 사진이 와도 읽힙니다.
- **글자 대비 4.5 이상**을 지키세요. 가장 밝은 사진(흰색)이 왔다고 치고 재세요.
- **글자를 짧게 쓰세요.** 제목 한 줄 + 설명 한 줄까지입니다. 그보다 길면 사진을 다 덮어 혼합형인 의미가 없습니다.
- **가격·평점처럼 정확히 읽어야 하는 것은 얹지 마세요.** 사진 위에서는 숫자가 잘 안 읽힙니다.
- **한 화면에 두 가지를 섞어도 됩니다.** 위는 혼합형 특집, 아래는 분리형 목록 — 하는 일이 다르므로 어수선하지 않습니다.

#### 글꼴은 한 벌입니다

**기본은 `Paperlogy`**, 못 불러오면 `Pretendard` → `Noto Sans KR` 순으로 물러납니다.
CSS로는 `font-family: Paperlogy, Pretendard, 'Noto Sans KR', sans-serif` 한 줄입니다.

- **제목과 본문에 다른 글꼴을 쓰지 마세요.** 두 벌이 되는 순간 어디에 무엇을 쓸지가
  화면마다 갈립니다. 처음엔 「제목만 다르게」로 시작해서, 나중엔 세 벌 네 벌이 됩니다.
- **위계는 글꼴이 아니라 크기와 굵기로 만듭니다.** 굵기는 400·600·700 셋만 쓰세요.
- 크기는 본문 15 · 카드 제목 18 · 섹션 제목 22 · 페이지 제목 32이 기준입니다.
- **숫자가 많은 표에는 고정폭 숫자**(`font-variant-numeric: tabular-nums`)를 켜세요.
  안 켜면 값이 바뀔 때 자릿수가 흔들려 칸이 들썩입니다.

#### 움직임 — 바뀌었다는 걸 눈이 알아채게

값이 바뀌었는데 **딱 하고 갈아 끼우면 사람은 그걸 못 봅니다.** 숫자가 12에서 3으로
바뀌어도, 눈이 그 자리를 안 보고 있었으면 바뀐 줄 모릅니다.
움직임은 꾸미개가 아니라 **「방금 여기가 바뀌었어요」라고 알려 주는 장치**입니다.

| 언제 | 얼마나 | 무엇이 |
| --- | --- | --- |
| 마우스를 올렸을 때 | **0.12초** | 색·테두리 |
| 눌러서 값이 바뀔 때 | **0.2초** | 사라짐·나타남·자리 옮김 |
| 접히고 펴질 때 | **0.25초** | 높이 |
| 화면 위에 뜰 때 (모달·시트) | **0.25초** | 옅어짐 + 살짝 올라옴 |
| 안내 문구(토스트) | **0.2초 등장 · 3초 머무름** | 옅어짐 |

- **가속은 하나로 씁니다.** `ease-out`(빨리 시작해 부드럽게 멈춤)이 기본입니다.
  자리마다 다른 가속을 쓰면 같은 사이트에서 두 사람이 만든 것처럼 보입니다.
- **0.3초를 넘기지 마세요.** 그보다 길면 기다리는 느낌이 듭니다. 목록이 다시 그려질 때는
  **0.15초**면 충분합니다.
- **자리를 밀어내며 움직이지 마세요.** 아래 내용이 출렁이면 읽던 곳을 잃습니다.
  옅어지거나 제자리에서 바뀌게 하세요.
- **한 번에 한 곳만 움직입니다.** 눌렀는데 화면 여기저기가 같이 움직이면 어디가 바뀐 건지 모릅니다.
- **끝없이 도는 움직임은 두지 않습니다.** 배너 자동 넘김·깜빡임은 읽는 걸 방해합니다.
  기다리는 동안 도는 것(로딩)만 예외입니다.
- **움직임을 꺼 둔 분을 존중하세요.** `prefers-reduced-motion` 이 켜져 있으면 전부 0초로 둡니다.
  어지럼증 때문에 꺼 두신 분들이 있습니다. 값은 바뀌되 움직이지만 않으면 됩니다.

#### 떠 있는 부품과 줄 안에 서는 부품은 다른 것입니다

사진 위에 **떠 있는** 동그란 찜 아이콘과, 버튼 줄 안에 **나란히 서는** 찜 버튼은
생김새가 비슷해 보여도 다른 부품입니다. 떠 있는 쪽은 자리를 빠져나가도록(`position:absolute`)
만들어져 있어서, 그 이름을 줄 안의 버튼에 붙이면 **줄을 빠져나가 엉뚱한 데 가서 앉습니다.**
실제로 찜 버튼이 가격 위에 겹쳐 앉은 적이 있습니다.

- **이름이 자리를 뜻하면 그 자리에서만 쓰세요.** 「사진 위 찜」과 「줄 안 아이콘 버튼」은
  이름을 나눠 두고, 섞어 쓰지 않습니다.
- **부품 이름과 실제 쓰임이 어긋나면 이름을 고치세요.** 「타임라인」이라 지어 놓고
  여섯 곳 모두 한 줄짜리 목록으로 쓰고 있던 적이 있습니다. 세로 점만 남아 글자 사이에 끼었습니다.
- **아이콘만으로 뜻이 통하면 글자를 빼세요.** 하트·공유·닫기가 그렇습니다.
  다만 **`aria-label`로 이름은 남깁니다** — 눈으로 못 보는 분에게는 그게 유일한 이름입니다.
- 반대로 **아이콘만으로 못 알아보는 것에는 글자를 붙이세요.** 「빼기」·「내보내기」처럼
  모양이 여러 뜻으로 읽히는 것이 그렇습니다.

#### 쓰는 화면이 있으면 보여주는 자리도 있어야 합니다

글을 쓰게 해 놓고 그 글이 나올 자리를 안 만드는 일이 잦습니다.
실제로 강사가 **1,240자짜리 강의 소개를 쓰는 화면**은 있는데, 상세 화면에는 그 글이
나올 자리가 없었습니다. 게다가 검수 화면은 "강의 소개 1,240자 — 확인됨"이라고까지 말하고 있었습니다.

- **입력 칸을 만들 때마다 "이건 어디에 보이나"를 한 번 물으세요.** 답이 없으면 둘 중 하나입니다 —
  보여줄 자리를 만들거나, 그 입력 칸을 빼거나.
- **에디터(굵게·목록·이미지 버튼이 붙은 칸)로 쓴 글**은 보여주는 쪽에도 그 모양이 살아야 합니다.
  제목·목록·굵게를 쓸 수 있게 해 놓고 보여줄 때 한 덩어리 문단으로 뭉개면, 쓴 사람이 헛수고합니다.
- **한 줄 요약과 본문 소개는 다른 것입니다.** 목록 카드에 들어가는 한 줄과, 상세에서 읽는 본문은
  자리도 길이도 다릅니다. 하나로 합치지 마세요.

#### 권장 크기는 실제 자리와 같아야 합니다

"권장 1280×720 (16:9)"이라고 적어 두고 옆의 자리는 4:3인 화면이 있었습니다.
**사는 분은 글자를 믿고 그 크기로 사진을 준비**하는데, 올려 보면 잘립니다.

- 안내 문구의 숫자와 **바로 옆 자리의 비율이 같은지** 확인하세요.
- 검수·확인 화면에 크기가 또 적힌다면 **거기도 같은 숫자**여야 합니다. 세 곳이 따로 놀기 쉽습니다.
- **영상만 16:9입니다.** 썸네일·카드 사진은 위 표(2·3개↔4:3, 4개↔1:1)를 따릅니다.

#### 차트 — 이미지가 아니라 데이터로 그립니다

추이·막대·도넛 같은 차트를 **이미지 자리로 두지 마세요.** `이미지 영역 (라인 차트 · 권장 1200×420)`
이라고 적어 두면 사는 분은 차트를 **그림 파일로 만들어 올려야 하는 줄** 압니다.
차트는 숫자가 바뀌면 따라 바뀌어야 하는 것이라, 사진 자리와 성격이 다릅니다.

- **가벼운 것은 라이브러리 없이 SVG로 그리세요.** 선 하나·막대 열두 개짜리는 30줄이면 됩니다.
  바깥에서 아무것도 안 불러오니 **인터넷 없이 폴더에서 열어도** 그대로 보입니다.

**대시보드를 만든다면 최소한 이 셋은 갖추세요.**

| 무엇 | 어디에 | 왜 |
| --- | --- | --- |
| **큰 추이 차트** | 화면에 한둘 | 기간을 놓고 흐름을 본다 |
| **스파크라인** (작은 추이선) | 지표 카드 안 | 숫자 옆에서 방향만 알려 준다 |
| **증감 표시** (▴ 812) | 지표 카드 안 | 좋아졌는지 나빠졌는지 |

숫자 하나만 두면 **좋은지 나쁜지를 못 말합니다.** "10,374명"만 있으면 늘고 있는지
줄고 있는지 알 수 없어서, 사는 분은 그 카드를 보고 아무 판단도 못 합니다.
지표 카드는 **숫자 + 증감 + 추이선**이 한 벌입니다.

- **본격적인 차트가 필요하면 무료(MIT) 라이브러리를 쓰세요.** 값을 치르지 않아도 되고 상업적으로 써도 됩니다.

| 라이브러리 | 크기 | 언제 |
| --- | --- | --- |
| **Chart.js** | ~200KB | 무난한 첫 선택. 자료가 많아 AI도 정확하게 짭니다 |
| **ApexCharts** | ~500KB | 기본 모양이 예쁘고 종류가 많을 때 |
| **uPlot** | ~40KB | 점이 수천 개라 가벼워야 할 때 |
| **Frappe Charts** | ~30KB | 작고 산뜻한 SVG면 충분할 때 |

- **색은 반드시 프리셋 색을 쓰세요.** 라이브러리 기본 팔레트(빨강·노랑·초록)를 그대로 두면
  그 자리만 다른 사이트처럼 보입니다.
- **눈금선은 옅게, 선은 진하게.** 눈금이 데이터보다 진하면 숫자가 안 읽힙니다.
- 라이브러리를 쓰면 **인터넷이 필요해집니다.** 오프라인으로 열어 볼 화면이라면 파일을 함께 받아 두거나 SVG로 그리세요.

#### 가격 표기 — 낼 돈부터

**판매가 · 정가 · 할인율** 순서로 한 줄에 세웁니다.

| 자리 | 모양 | 크기 |
| --- | --- | --- |
| **판매가** (낼 돈) | 진한 기본 글자색, 굵게 | 18 (상세는 28) |
| **정가** | 회색 + 취소선 | 13 |
| **할인율** | **강조색 글자**, 굵게 | 14 |

- **할인율을 앞에 두지 마세요.** 눈이 먼저 "20%"에 걸려 정작 얼마인지를 뒤늦게 찾습니다.
  사는 분이 알고 싶은 건 **낼 돈**입니다.
- **할인율은 배지가 아니라 색 글자입니다.** 배지로 두면 정가·판매가와 무게가 비슷해져 셋이 서로 다툽니다.
  배지 자리는 `입문`·`NEW` 같은 **꼬리표 몫**입니다.
- **셋의 크기를 벌려 두세요.** 비슷하면 무엇을 먼저 읽어야 할지 눈이 헤맵니다.
- **강조색 원색을 글자로 쓰지 마세요.** 주황·코랄 원색은 흰 배경에서 3:1을 못 넘깁니다.
  글자용으로 한 단계 어두운 색을 따로 두세요.
- 가격 묶음은 **한 곳에서 만들어 쓰세요.** 자리마다 손으로 짜면 순서가 갈립니다.

#### 버튼이냐 링크냐

**다른 화면으로 데려가면 링크, 이 화면 안에서 끝나면 버튼입니다.**

| 하는 일 | 무엇으로 | 눌렀을 때 |
| --- | --- | --- |
| 다른 화면으로 간다 | 링크 `<a href>` | 그 화면이 열린다 |
| 이 화면 안에서 끝난다 | 버튼 `<button>` | 안내 문구가 뜬다 |

- 필터 적용·정렬·검색·다음 차시·시간 지점 이동은 **화면이 바뀌지 않는 조작**입니다.
  링크로 만들면 **자기 자신으로 가는 링크**가 되어, 눌러도 같은 화면이 다시 열립니다.
  사는 분 눈에는 *"눌렀는데 아무 일도 안 나네"* 로 보입니다.
- **나란히 선 버튼끼리는 종류를 섞지 마세요.** 하나는 화면이 넘어가고 하나는 안 넘어가면
  규칙이 없어 보입니다.

**그리고 화면 안에서 끝나는 조작은 진짜로 값이 바뀌어야 합니다.**
눌러도 숫자가 그대로면, 사는 분 눈에는 **눌리지 않는 포스터**입니다.

| 눌렀을 때 | 바뀌어야 하는 것 |
| --- | --- |
| 기간 탭 (7일·30일·90일) | 차트와 그 옆 요약 숫자 |
| 쿠폰·옵션 고르기 | 결제 금액 줄과 합계 |
| 수량 늘리기 | 금액 |
| 정렬·필터 | 목록 순서나 개수 |

#### 눌러 봐야만 드러나는 것들 — 2026-08-19 LMS 두 팩 검수에서 나온 것

화면을 눈으로만 보면 멀쩡한데, 눌러 보면 어긋나는 자리가 있습니다. 실제로 나온 것들입니다.

**① 켜짐 표시와 실제 걸린 조건이 따로 놀지 않게.**
거르는 단추 여덟 개와 「전체 보기」가 서로 다른 상자에 담겨 있었습니다. 그래서
「디자인」을 눌러도 「전체 보기」가 안 꺼지고, 「전체 보기」를 눌러도 「디자인」이 안 꺼졌습니다.
**목록은 맞게 걸러지는데 표시만 손님을 속였습니다.**
- 같은 갈래의 단추는 **상자를 넘어** 한 벌로 갱신하세요. 상자 안에서만 끄면 밖에 있는 게 남습니다.
- 조건 칩의 ✕ 로 지웠을 때도 **왼쪽 단추의 켜짐이 같이 꺼져야** 합니다.

**② 쪽 번호는 결과를 따라가야 합니다.**
12개를 2개로 걸렀는데 쪽 번호가 「1 2 3 4 5」 그대로였고, 어느 쪽을 눌러도 같은 목록이 나왔습니다.
- 결과가 한 쪽에 다 들어가면 **쪽 번호를 감추세요.**
- 조건이 바뀌면 **1쪽으로 되돌리세요.** 3쪽을 보다 조건을 바꾸면 3쪽에 머물면 안 됩니다.

**③ 화면을 넘길 때 고른 값도 같이 넘기세요.**
결제 화면에서 「12개월 할부」를 골라 「매달 5,270원 × 12개월」까지 확인했는데,
완료 화면은 「신용카드 (일시불)」이었습니다. 고른 값이 다음 화면으로 안 넘어갔습니다.
- 앞 화면에서 고른 것이 뒤 화면에 다시 적힌다면 **둘을 이어 두세요**(sessionStorage 면 충분합니다).
- 이어 둘 수 없으면 **뒤 화면에 그 값을 적지 마세요.** 틀린 값보다 없는 게 낫습니다.

**④ 견본 날짜는 「오늘」에서 거꾸로 잡으세요.**
만든 날에 맞춰 날짜를 적어 두면 시간이 지나 여는 손님은 지난 날짜만 봅니다.
실제로 「D-3 · 마감 8월 10일」인데 그날은 8월 19일이었습니다 — 9일 지난 마감을 「3일 남음」이라 했습니다.
- **기준일 하나를 정해 두고, 화면을 열 때 오늘까지의 차이만큼 통째로 미세요.**
  날짜 사이 간격이 그대로라 「D-3」이나 「수강 기간 6개월」 같은 계산값은 손대지 않아도 맞습니다.
- 주문번호에 박힌 날짜(ORD-20260807-…)도 같이 밀어야 합니다.

**⑤ 숫자 하나를 고치면 그 숫자로 계산되는 값을 전부 따라가세요.**
차시 수를 30에서 18로 고치면서 딸린 값을 안 고쳤더니,
같은 강의 진도가 화면마다 67% 와 40% 로 갈라졌고 「12/18차시 · 남은 차시 18개」가 됐습니다.
- 차시 수를 바꿀 때 딸린 것: **총 재생 시간 · N/M차시 · 진도 % · 남은 차시 · 장별 완료 수.**
- 고친 뒤에는 그 값이 나오는 **모든 화면을 훑어** 한 벌로 맞았는지 보세요.

**⑥ 「비로그인」 화면은 헤더도 비로그인이어야 합니다.**
「홈 - 비로그인」인데 헤더에 알림 종과 프로필이 있고 로그인 단추가 없었습니다.
사이드바에는 「로그인·회원가입」이 있는데 헤더에는 「김수현님」이라, 한 화면에서 앞뒤가 안 맞았습니다.
- 로그인 전/후 화면을 나눠 만들었다면 **머리띠와 옆 메뉴도 같이 갈라야** 합니다.

**⑦ 견본이라 좁은 자리는 그 말을 화면에 적으세요.**
분야 카드는 「개발 284개」인데 눌러 거르면 2개가 나왔습니다. 견본이라 그런 건데 그 말이 없었습니다.
- 「전체 1,280개 가운데 12개만 보여 주는 견본입니다」 처럼 **한 줄로 밝히면** 어긋남이 사라집니다.
- 목록에서 어느 카드를 눌러도 같은 상세가 열린다면 **상세 화면에도** 그렇게 적으세요.

**⑧ 겹쳐 놓은 단추는 정말 눌리는지 확인하세요.**
카드 위 찜 하트가 사진에 덮여 있어서, 누르면 찜이 안 되고 강의 상세로 넘어갔습니다.
- 겹치는 단추에는 **z-index** 를 주고, `document.elementFromPoint` 로 그 자리 맨 위가 무엇인지 확인하세요.
- 링크 안에 단추를 넣었다면 `preventDefault` 만으로는 부족합니다 — **덮였는지부터** 보세요.

**⑨ 못 채운 곳으로 화면을 옮겨 주세요.**
「결제하기」는 오른쪽 위에 있고 「동의를 아직 채우지 않았어요」는 화면 아래에 떴습니다.
스크롤해야만 보이니 손님은 왜 안 되는지 모릅니다.
- 막혔으면 **못 채운 자리로 스크롤**하고, 채워지면 안내를 거두세요.

**⑩ 끝난 조작은 끝난 티를 내세요.**
쿠폰 「받기」를 몇 번이고 눌러도 「받았어요」가 또 나왔고, 과제를 내도 알림 한 줄 없이 화면이 넘어갔습니다.
- 한 번으로 끝나는 단추는 **글자를 바꾸고 다시 눌리지 않게** 하세요(받기 → 받음).
- 낸 뒤에는 **그 자리에서** 낸 것이 보여야 합니다.

- **미리 만들어 두고 바꿔 끼세요.** 차트 셋을 미리 그려 두고 탭에 따라 보여 주는 편이,
  누를 때마다 자바스크립트로 다시 그리는 것보다 낫습니다 — 그리는 규칙이 두 곳으로 갈라지지 않습니다.
- **계산은 한 곳에서만 하세요.** 합계를 화면마다 손으로 적어 두면 쿠폰을 바꿨을 때 한 군데만 바뀝니다.
- **탭에 갈 곳도 바꿀 것도 없으면 탭이 아닙니다.** 그냥 제목이거나, 아직 안 만든 자리입니다.

**같은 길을 두 번 두지 마세요 — 위 카테고리 줄과 왼쪽 LNB.**
둘이 같은 곳을 가리키면 위쪽이 두 줄로 두꺼워지고, 어느 쪽을 눌러야 할지 잠깐 멈칫합니다.

| 상태 | 왼쪽 LNB | 위 카테고리 줄 |
| --- | --- | --- |
| 맨 위 (LNB 보임) | 보인다 | **접힌다** |
| 스크롤해서 LNB가 위로 사라짐 | 안 보인다 | **내려온다** |
| LNB가 아예 없는 화면 | — | **늘 보인다** |

- **LNB가 있는 화면은 접힌 채로 시작하세요.** 펼쳐 두었다가 나중에 접으면 첫 화면에서 한 번 번쩍입니다.
- **LNB가 없는 화면에서는 절대 접지 마세요.** 접으면 갈 길이 통째로 사라집니다.
- **기준은 "LNB가 붙박이 머리말 아래로 사라지는 순간"입니다.** 머리말 높이만큼 위를 깎아서 재세요.
- 같은 규칙을 **LNB를 쓰는 모든 레이아웃에 똑같이** 거세요. 화면마다 다르면 위쪽 높이가 제멋대로 바뀌어 보입니다.

- **견본으로 가는 버튼을 화면 안에 두지 마세요.** `결과가 없을 때` `대화가 없을 때는 이렇게 보여요`
  처럼 예외 화면을 보여주려고 넣은 버튼·단락은 진짜 사이트에 없는 것입니다.
  검색창 옆에 그런 버튼이 서 있으면 사는 분은 그게 기능인 줄 압니다.
  빈 화면·오류 화면은 **화면 목록에 따로 한 장**으로 두세요 — 거기서 눌러 보면 됩니다.

#### 옮겨 다니며 봐야 드러나는 것들 — 2026-08-20 사장님 검수에서 나온 것

앞의 것들이 «눌러 봐야» 나오는 흠이라면, 이번 것은 **화면을 옮겨 다니고 끝까지 굴려야** 나옵니다.
한 장만 세워 놓고 보면 전부 멀쩡해 보입니다. 실제로 우리 팩 열넷에서 나온 것만 적습니다.

**① 화면을 옮길 때 본문이 좌우로 튀지 않게.**
세로 스크롤 막대는 «생길 때만» 15px 을 먹습니다. 그래서 목록이 짧은 화면에서 긴 화면으로 가면
본문이 통째로 15px 밀립니다. 한 장씩 보면 둘 다 멀쩡한데 오가면 화면이 흔들립니다.
```css
html { scrollbar-gutter: stable; }   /* 막대 자리를 늘 비워 둔다 */
```
- 화면 «안»에서 탭으로 목록을 거를 때도 같습니다. 3개 보이다 5개가 되면 막대가 생깁니다.

**② 콘텐츠 폭은 모든 화면에서 같아야 합니다.**
한 화면만 폭이 다르면 옮길 때마다 글이 좌우로 밀려 «따라다니는» 느낌이 납니다.
- 폭은 화면마다 적지 말고 **한 곳(`--wrap`)에 두고 모두 그것을 쓰세요.**

**③ 푸터가 본문에 맞붙지 않게.**
`margin-top: auto` 는 **자리가 남을 때만** 벌어집니다. 내용이 한 화면을 넘기면 0 이 되어
푸터 윗선이 버튼에 그대로 붙습니다. 실제로 「강사로 전환 신청」 바로 밑에 선이 붙었습니다.
```css
/* auto 는 그대로 두고(짧은 화면에서 바닥에 붙이는 일은 auto 만 합니다)
   푸터 «바로 앞 덩어리»에 최소 간격을 줍니다 */
:where(main, .page-wrap) > *:has(+ footer) { margin-bottom: 32px; }
```
- GNB 와 본문 사이도 마찬가지입니다. **16px 밑으로 붙지 않게** 하세요.

**④ 덩어리 사이 간격은 그 화면이 쓰는 «리듬»을 지키세요.**
다른 칸들은 24px 씩 벌어져 있는데 한 곳만 8px 이면, 0px 이 아니어도 **붙어 보입니다.**
- 새 묶음이 시작되는 자리(안내 상자 다음에 오는 소제목 같은 것)를 특히 보세요.
- 제목 바로 밑 부제는 **붙어 있는 것이 맞습니다.** 그건 한 덩어리입니다.

**⑤ 화면 «안» 탭에는 뒤로가기를 두지 마세요.**
탭처럼 생겼는데 실제로는 다른 장으로 넘어가고, 간 장에는 「‹ 돌아가기」가 붙어 있었습니다.
누를 때마다 없던 손잡이가 생기면서 **화면이 그만큼 아래로 밀립니다.**
- 탭으로 오갈 화면들은 **뒤로가기 유무를 통일**하세요. 탭이면 넷 다 없는 쪽이 맞습니다.
- 탭 하나가 «지금 보고 있는 그 화면»을 가리키고 있지는 않은지도 보세요. 눌러도 아무 일이 없습니다.

**⑥ 동그란 버튼을 가로로 늘이지 마세요.**
`border-radius: 50%` 인 34px 짜리 아이콘 버튼에 `width: auto` 를 얹으면 **타원**이 됩니다.
- 넓은 글자 버튼이 필요하면 **일반 버튼 모양**을 쓰고, 옆 버튼과 **키를 맞추세요.**
- 나란히 선 버튼끼리 키가 다르면 흠입니다. 다만 「큰 버튼 + 보통 버튼」을 **일부러** 짝지은 것은 괜찮습니다.

**⑦ 세로로 세운 flex 칸에서는 배지와 버튼이 늘어납니다.**
`display:flex; flex-direction:column` 에 `align-items` 를 안 주면 기본값이 `stretch` 라
배지도 버튼도 **칸 폭 그대로 늘어납니다.** 40px 이어야 할 배지가 767px 이 된 적이 있습니다.
```css
.promo .in > .badge, .promo .in > .btn { align-self: flex-start; }
```
- 글(제목·설명)은 넓게 두고 **배지와 버튼만** 제 몸에 맞추세요.

**⑧ 배지를 글 옆에 붙이려면 블록으로 만드세요.**
`14:31<div class="unread">2</div>` 에서 배지가 `inline-grid` 라 **시각 옆에 나란히** 섰습니다.
`margin-top` 을 준 걸 보면 원래는 밑에 놓을 셈이었는데, 인라인이라 안 먹었습니다.
```css
.unread { display: grid; width: max-content; margin: 4px 0 0 auto; }
```

**⑨ 좌우로 미는 줄에는 막대를 보이지 마세요.**
카드를 옆으로 미는 줄에 «‹ ›» 화살표를 붙여 놓고 막대까지 같이 보이면 밑에 회색 띠가 깔립니다.
```css
.rail { scrollbar-width: none; }
.rail::-webkit-scrollbar { display: none; }
```
- ⛔ **표에는 쓰지 마세요.** 표에는 화살표가 없어서 막대를 감추면 «넘길 수 있다는 것»을 알 길이 사라집니다.
  표는 애초에 **좁은 칸에 넣지 않는 것**이 답입니다.

**⑩ 나란한 썸네일은 크기가 같아야 합니다.**
한 줄에 선 카드의 사진 높이가 593 · 250 · 84px 로 제각각이면 목록이 무너져 보입니다.
- 목록 안 사진은 **비율(`aspect-ratio`)을 정해** 한 벌로 맞추세요.

**⑪ 지금 어느 메뉴에 와 있는지 «켜서» 보여 주세요.**
메뉴는 있는데 **지금 여기가 어디인지 표시가 없으면** 손님은 길을 잃습니다.
실제로 우리 팩에서 나왔습니다 — CSS 에 `.ednav-menu a.on { border-bottom-color: … }` 규칙까지 만들어 두고,
정작 `on` 을 **켜 주는 자리가 없어** 열세 화면의 상단 메뉴가 전부 꺼져 있었습니다.
- GNB 와 LNB 둘 다입니다. **켜진 것은 언제나 하나**여야 합니다 — 없어도, 둘이어도 틀린 것입니다.
- 상세 화면처럼 메뉴에 없는 쪽이면 **그 상위 메뉴**를 켜세요.
```js
// 화면을 열 때 지금 쪽에 해당하는 메뉴를 켠다
const 지금 = (document.body.dataset.page || '').slice(0, 2);
document.querySelectorAll('.gnb-menu a, .lnb a').forEach((a) => {
  const 갈곳 = (a.getAttribute('href') || '').split('/').pop() || '';
  if (갈곳.slice(0, 2) === 지금) { a.classList.add('on'); a.setAttribute('aria-current', 'page'); }
});
```

**⑫ 성격이 다른 상품은 «제 상세»로 보내세요.**
목록 카드가 여럿 있어도 **같은 종류라면 한 상세로 가도 됩니다** — 화면 목록이 이미 케이스를 보여 줍니다.

⛔ **다만 성격이 다르면 다릅니다.** 바로 구매 상품 · 경매 상품 · 예약 상품처럼
**상세에서 보여 주는 내용도 다르고 사는 과정도 다른 것**은 상세를 각각 만들어야 합니다.
한 사이트에 여러 케이스가 섞여 있을 때 특히 그렇습니다.
- 실제로 겪었습니다 — 여행 팩에 「상품 상세」와 「**패스** 상품 상세」가 따로 있는데,
  「오사카 주유패스」 카드가 **일반 상품 상세로** 가고 있었습니다. 카드는 제 갈래를 알고 있었는데
  링크만 늘 같은 곳을 가리켰습니다.
- 갈래가 다르면 **화면 목록에도 상세를 따로 세우고**, 카드가 그리로 가게 하세요.
- ⛔ `href="#"` 나 빈 `href` 로 두지 마세요. **눌리는데 아무 데도 안 가는 것**이 제일 나쁩니다.

#### 화면 목록으로 가는 길은 한 곳에만

만든 화면을 훑어보려면 목록이 필요합니다. 그런데 그 목록을 **화면 안에 늘어놓으면**
사는 분 눈에는 사이트의 일부로 보입니다.

```
이렇게 두지 마세요 — 화면 위쪽에 줄줄이
  [CO-01 · 강의 목록] [CO-02 · 검색 결과 없음] [CO-03 · 강의 상세] [CO-04 · 결제] …
```

**진짜 사이트에는 이런 줄이 없습니다.** `CO-02 · 검색 결과 없음` 은 만드는 사람의 말이지
쓰는 사람의 말이 아닙니다. 화면 ID 가 손님 눈에 보이는 것부터가 어긋납니다.

- **한 귀퉁이에 작게 하나만 둡니다.** 오른쪽 아래에 떠 있는 `화면 목록` 단추 하나면 됩니다.
  누르면 `화면목록.html` 로 갑니다 — index.html(홈)이 아닙니다.
- **그 단추는 화면 내용 위에 떠 있게** 두고, 본문 흐름에는 끼워 넣지 않습니다.
  본문에 들어가면 그것도 사이트의 일부가 됩니다.
- **화면 ID 를 손님이 보는 자리에 쓰지 않습니다.** 제목·탭·빵부스러기 어디에도요.
  ID 는 우리가 화면을 세는 이름이지 서비스의 말이 아닙니다.
- 화면끼리 오가는 길은 **그 서비스의 진짜 메뉴와 버튼**으로 만듭니다.
  「강의 목록 → 강의 상세」는 강의 카드를 눌러서 가지, 탭으로 가지 않습니다.

### 이미지 · 썸네일
- 아직 사진이 없으므로 이미지 자리는 **테마 색이 아니라 옅은 파스텔**로 채우세요. 테마 색으로 칠하면 화면이 그 색 덩어리로 뒤덮여 디자인이 안 보입니다.
- 배경 `#EEF2F7` · `#F2EFF7` · `#EDF4F1` · `#F7F1EC` · `#F2F4F7` 중 하나를 카드마다 돌려 쓰고, 테두리 `#E3E8F0` 1px, 글자색 `#8B94A6` 13px.
- 자리 안에 `이미지 영역 (무엇 · 권장 가로×세로)` 형식으로 적으세요. 예: `이미지 영역 (매장 대표 · 권장 1200×900)` · `이미지 영역 (프로필 · 권장 400×400)` · `이미지 영역 (배너 · 권장 1600×600)`
- 이미지 자리는 테마 색으로 칠하지 않는다. 옅은 파스텔 한 톤 + 1px 테두리로 둔다.
- 자리 안에 무엇이 들어갈 이미지인지와 권장 크기를 글자로 적는다.
- 적어 둔 크기의 **비율을 실제로 지킨다** — `400×400`이라 적었으면 정사각으로 보여야 한다(aspect-ratio).
- 특히 프로필·썸네일처럼 크기가 고정된 자리는, 가로로 나란히 놓아도 늘어나지 않게 한다. 목록·행 안에 넣을 때 `flex`가 잡아당겨 타원이 되는 일이 잦으니, 만든 뒤 실제로 열어 모양을 확인한다.
- 썸네일처럼 작아서 글자가 안 들어가면 크기만 적거나 아이콘 하나만 둔다.
- **「사진 위 겹침」 카드에서는 사진이 들어오기 전까지 어둠막을 걸지 않는다.** 옅은 자리표시자 위에 어두운 막을 덮으면 안에 적은 글이 묻혀 «깨진 그림»으로 보인다. 제목·태그는 자리표시자 아래에 본문 색으로 두었다가, 실제 사진을 넣을 때 위로 올린다.
- 실제 사진을 넣기 전까지 이 규칙을 유지한다 — 그래야 어디에 무엇을 넣어야 하는지 보인다.


### 6-끝. 다 만들었으면 «스스로 검수»하고 고친 뒤에 끝냈다고 하세요
> ⛔ **이 걸음을 건너뛰고 「다 만들었습니다」라고 하지 마세요.**
> 화면을 다 그렸어도 눌러서 안 움직이면 안 만든 것과 같습니다.

**AI 에게 그대로 시키시면 됩니다** — 아래 세 걸음은 사람 손이 필요 없습니다.

1. **파일부터** — 만든 폴더에서 `7-9-2` 의 글을 `검수.mjs` 로 저장하고 `node 검수.mjs .` 를 돌린다.
   끊어진 링크 · 없는 그림 · 빈 링크 · 외톨이 화면이 **0건**이 될 때까지 고친다.
2. **화면을** — `7-9` 의 글을 `화면검수-글.js` 로, `7-9-3` 의 글을 `화면검수.mjs` 로 저장하고
   `node 화면검수.mjs .` 를 돌린다. 화면이 몇 장이든 한 번에 잰다.
   간격 · 대비 · 넘침 · 늘어남이 **0건**이 될 때까지 고친다.
3. **눌러서** — 7장의 목록대로 직접 눌러 본다. 여기부터는 사람 눈이 필요하다.

⚠ **1·2 는 고치고 «다시» 돌립니다.** 한 번 돌리고 목록만 보고하면 안 됩니다 —
고친 것이 다른 곳을 깨뜨렸는지는 다시 돌려야 압니다.

⚠ 헤드리스 브라우저가 없다는 말이 나오면 그 글이 «무엇을 치면 되는지» 한 줄로 알려 줍니다.
깔기 어려우면 화면을 크롬으로 열고 **F12 → Console** 에 `7-9` 의 글을 붙여 넣으셔도 됩니다 —
재는 것은 똑같고, 대신 화면마다 한 번씩 붙이셔야 합니다.

> 📄 이 검수는 **`11_내사이트_검수하는_법.html`** 에 그림과 함께 다시 적어 두었습니다.
> 스펙팩을 안 여셔도 그 파일 하나만 보시면 됩니다.

## 7. 만들고 나서 — 눈으로 보고, 눌러 보고, 고치기
> **여기까지 해야 다 만든 것입니다.** 화면을 다 그렸어도 눌러서 안 움직이면 안 만든 것과 같습니다.
> **세 가지를 봅니다 — ① 화면연결·레이아웃·UI 를 눈으로 ② 뒤로가기·버튼·탭·썸네일·배너를 직접 눌러서 ③ 데이터가 화면끼리 맞는지.**
> 훑어보는 것이 아니라 **세어서** 확인하고, **나온 오류는 그 자리에서 고칩니다.** 하나라도 남으면 고치고 다시 셉니다.


### 7-0. 검수는 «두 갈래»입니다 — 둘 다 하세요
> **화면을 다 만들었으면 여기서 끝이 아닙니다.** 성격이 다른 두 가지를 각각 봅니다.

| | 무엇을 보나 | 어떻게 아나 | 어디서 |
|---|---|---|---|
| **① 사람처럼 보는 검수** | 보이는 것 — 레이아웃 · 눌러 본 반응 · 생김새 | **열고 눌러 보고 굴려야** 안다 | 아래 7-1 ~ 7-9 |
| **② 기계가 보는 검수** | 파일 · 값 · 문서끼리 앞뒤 | **열어 보지 않아도** 파일만 보면 안다 | 아래 7-9-2 |

⛔ **한 갈래만 하면 반드시 새어 나갑니다.**
보기에 멀쩡한데 링크가 죽어 있거나, 파일은 다 맞는데 화면이 무너져 있습니다.

**① 사람처럼 보는 검수 — 열다섯 가지**

| 갈래 | 무엇을 보나 |
|---|---|
| 레이아웃 | ① 콘텐츠 폭이 모든 화면에서 같은가 ② 콘텐츠 사이 간격이 균일한가 ③ GNB 상단·푸터 하단 고정 ④ GNB↔콘텐츠·푸터↔콘텐츠 간격 ⑤ GNB·LNB 가 지금 메뉴를 켜 주는가 ⑥ 화면 이동 시 흔들림이 없는가 |
| UX | ⑦ 뒤로가기가 적절한가(화면 안 탭에는 두지 않는다) ⑧ 배너·썸네일·버튼·탭이 정확히 반응하는가 |
| UI | ⑨ 배경 위 글자색이 적절한가 ⑩ 썸네일 크기가 적당한가 ⑪ 버튼 크기·모양이 균일한가 ⑫ 좌우로 넘기는 곳에 가로 막대가 드러나지 않는가 ⑬ UI 가 틀어지지 않았는가 ⑭ 배지 크기·모양이 균일한가 ⑮ 배지 위치가 적절한가 |

⭐ **이 열다섯 가지는 저희가 파는 팩을 재는 잣대 그대로입니다.**
아래 **7-9** 의 글을 화면에 붙여 넣으면 그 가운데 **열 가지(②③④⑤⑨⑩⑪⑫⑬⑭)** 를 기계가 대신 재 줍니다.
**①⑥⑦** 은 화면 하나로는 알 수 없어, 7-9 가 값을 내주면 **쪽끼리 모아 견주시면** 됩니다.
남는 **⑧⑮** 는 눌러 보고 눈으로 봐야 알 수 있습니다.

**② 기계가 보는 검수 — 화면을 안 열어도 아는 것**

- **글자 대비** — 본문 4.5 · 큰 글자 3.0 을 넘는가 (색을 고른 뒤 반드시 재세요)
- **끊어진 링크** — `href` 가 가리키는 화면이 실제로 있는가
- **없는 그림** — `src` 가 가리키는 파일이 실제로 있는가
- **외톨이 화면** — 만들어 놓고 아무 데서도 안 이어지는 화면이 있는가
- **화면 수가 맞는가** — 이 문서가 약속한 화면 수와 실제로 만든 수가 같은가
- **굳은 날짜** — 견본 날짜가 «오늘»에서 거꾸로 잡혀 있는가 (지난 날짜가 보이면 안 됩니다)

### 7-1. 화면연결 · 레이아웃 · UI 를 «눈으로» 봅니다
- **화면 목록에서 시작해 링크를 눌러 다닙니다.** 주소창에 직접 쳐 넣지 마세요 — 그러면 화면이 서로 이어지는지가 안 보입니다.
- 묶음마다 대표 화면을 열어 **무너진 곳 · 겹친 곳 · 잘린 글 · 화면 밖으로 나간 것**을 봅니다.
- 가로 스크롤바가 생기면 어딘가 넘친 것입니다. 넓은 화면에서도 한 번 봅니다.
- 사이드바가 있는 짜임이면 **본문이 사이드바에 가리지 않는지** 봅니다.

### 7-2. 눌러도 아무 일 없는 것이 0개여야 합니다
- `href="#"` · `href="javascript:void(0)"` · 빈 `href` 는 **하나도 남기지 않습니다.**
- **페이지 번호, 달력 날짜, 시간 칸, 이전·다음** — 여기가 제일 자주 빈 채로 남습니다. 실제로 눌러 보세요.
- 아직 만들 화면이 없으면 «지금은 안 되는 것»으로 보이게 두세요(흐리게 + 눌리지 않게). 눌리는데 아무 일도 없으면 고장으로 보입니다.
- 단추(`<button>`)도 같습니다. 눌렀을 때 **화면이 바뀌거나, 값이 바뀌거나, 안내가 뜨거나** 셋 중 하나는 일어나야 합니다.


### 7-3. 거르는 단추는 «목록이 실제로 줄어야» 합니다
- **필터 칩·탭·정렬**을 누르면 색만 바뀌는 것으로는 안 됩니다. **보이는 항목이 실제로 달라져야** 합니다.
- 칩에 개수를 적었으면(`기본 교육 2`) 누른 뒤 **그 수만큼만 보여야** 합니다. 적힌 숫자와 보이는 수가 다르면 그 자리는 거짓말이 됩니다.
- 거르면 하나도 안 남는 조건도 눌러 보세요. **「결과가 없습니다」 화면**이 나와야 합니다 — 빈 채로 두면 고장으로 보입니다.
- 정렬(최신순·낮은 요금순)도 같습니다. 순서가 **눈에 보이게** 바뀌어야 합니다.

⛔ **안 줄어들면 열에 아홉은 이것입니다** — `[hidden]` 은 브라우저 기본값이라 `.row { display: flex }` 한 줄에도 집니다. 「지금 2개 보고 있어요」로 숫자만 바뀌고 목록은 그대로면, CSS 에 `[hidden] { display: none !important; }` 이 있는지 보세요.


### 7-4. 썸네일 · 배너 · 탭도 «직접» 눌러 봅니다
- **썸네일과 배너**는 그림이라 눌러 보기를 잊기 쉽습니다. 눌러서 어디로 가는지 하나씩 확인하세요.
- 카드 사진 위에 얹은 단추(찜 하트·빠른 담기)는 **사진에 덮여 안 눌리는 일**이 잦습니다.
  겹쳐 놓은 단추에는 `z-index` 를 주고, 눌러서 **정말 그 단추가 눌리는지** 확인하세요.
  (그 자리 맨 위에 무엇이 있는지는 `document.elementFromPoint(x, y)` 로 알 수 있습니다.)
- **탭**을 눌렀을 때 내용이 화면 밖에 있으면 손님은 아무것도 안 바뀐 줄 압니다. 탭 줄이 화면에 보이게 옮겨 주세요.
- **쪽 번호**는 눌렀을 때 목록이 실제로 달라져야 합니다. 걸러서 한 쪽에 다 들어가면 쪽 번호를 감추고, 조건이 바뀌면 1쪽으로 되돌립니다.
- 한 번으로 끝나는 단추(쿠폰 받기·신청하기)는 누른 뒤 **글자가 바뀌고 다시 안 눌려야** 합니다. 몇 번이고 눌리면 받았는지 알 수 없습니다.

### 7-5. 뒤로가기는 4장에 적힌 그 화면으로 갑니다
- 31개 화면에 뒤로가기가 있고, 나머지 9개(메뉴 첫 화면)에는 **없어야** 합니다.
- 화면마다 4장의 `뒤로가기` 항목과 **하나씩 대 봅니다.** 「대충 상위 메뉴로」가 아니라 적힌 그 화면입니다.
- 여러 단계로 이어지는 화면(예약→결제→완료)에서 특히 어긋납니다. 완료 화면에서 뒤로 갔을 때 결제가 다시 시작되면 안 됩니다.

### 7-6. 화면 목록과 실제가 같은지 셉니다
- 만들어진 화면이 **40개** 인지 셉니다. 목록에는 있는데 안 만든 화면, 만들었는데 목록에 없는 화면 둘 다 0이어야 합니다.
- 화면끼리 잇는 링크가 **실제로 있는 화면**을 가리키는지 셉니다. 없는 화면을 가리키는 링크는 0이어야 합니다.
- **`화면목록.html` 이 40개를 «다» 가리키는지** 셉니다. 홈에서 닿는 것만 넣으면 나머지는 영영 못 봅니다.
- **화면 목록으로 가는 단추가 40개 화면 «전부»에 있는지** 셉니다. 한 화면이라도 빠지면 거기서 길이 끊깁니다.


### 7-7. 데이터가 화면끼리 «서로 맞는지» 봅니다
> 화면 하나만 보면 다 맞아 보입니다. **두 화면을 나란히 놓아야** 어긋난 것이 보입니다.

- **앞 화면에서 고른 것이 뒤 화면에 그대로 넘어가는지** — 수량·옵션·할부·기간을 골라 놓고 다음 화면에서 확인하세요.
  고른 값이 안 넘어가면 뒤 화면에 그 값을 아예 적지 마세요. **틀린 값보다 없는 게 낫습니다.**
- **같은 값이 여러 화면에 나오면 다 같은지** — 목록의 「12/18 · 67%」가 상세에서는 40%면 하나는 거짓말입니다.
- **합계가 항목의 합과 맞는지** — 「남은 일 59건」이 1+24+32+2 와 맞는지, 표의 인원 합이 위 카드의 총원과 맞는지.
- **N/M 과 「남은 것」이 맞는지** — 12/18 이면 남은 것은 6개입니다. 전체 수를 그대로 적어 두기 쉽습니다.
- **적어 둔 개수와 실제로 보이는 개수가 맞는지** — 「전체 284개」라고 해 놓고 걸러 보니 2개면, 그 말을 화면에 적으세요(「284개 가운데 8개만 보여 주는 견본입니다」).
- **날짜와 그 옆의 말이 맞는지** — 「이번 주」가 붙은 것이 정말 이번 주인지, 「D-3」이 그 날짜와 맞는지, 요일이 달력과 맞는지.

⛔ **숫자 하나를 고치면 그 숫자로 계산되는 값을 «전부» 따라가세요.**
차시 수를 30에서 18로 고치면서 진도와 남은 차시를 안 고치면, 같은 것이 화면마다 다르게 보입니다. **반쪽만 고치면 안 고친 것보다 나쁩니다.**

### 7-8. 폰 폭에서 다시 한 번
- 위 「좁은 화면에서 넘치지 않게」를 **다 만든 뒤에 다시** 확인합니다. 화면이 늘어나면서 새로 넘치는 곳이 생깁니다.


### 7-9. 기계로 재기 — 이 글을 화면마다 돌리세요
> **눈으로 세지 마세요.** 아래 글이 «옮겨 다니며 봐야 보이는 것»을 대신 재 줍니다 —
> 푸터가 본문에 맞붙었는지 · 덩어리 간격이 어긋났는지 · 버튼과 배지가 늘어났는지 ·
> 좌우로 미는 칸에 막대가 드러났는지 · 표가 제 칸을 넘쳤는지.

**돌리는 법** — 편한 것 하나만 고르세요.
- 브라우저 개발자도구 콘솔에 그대로 붙여 넣기
- 화면마다 <script> 로 잠깐 끼워 넣고 결과를 찍기
- 헤드리스 브라우저로 열어 결과를 document.title 에 담아 꺼내기

```js
(() => {
  const 흠 = [], 적기 = (칸, 말) => { const k = "[" + 칸 + "] " + 말; if (!흠.includes(k)) 흠.push(k); };
  const 보이나 = (el) => { const s = getComputedStyle(el);
    if (s.display === "none" || s.visibility === "hidden") return false;
    const r = el.getBoundingClientRect(); return r.width > 2 && r.height > 2; };
  const 이름 = (el) => el.tagName.toLowerCase() +
    (typeof el.className === "string" && el.className ? "." + el.className.trim().split(/ +/)[0] : "");
  const 반 = (n) => Math.round(n);
  const 본문 = document.querySelector("main") || document.body;
  const bs = getComputedStyle(본문);
  const 콘텐츠폭 = 반(본문.getBoundingClientRect().width - parseFloat(bs.paddingLeft) - parseFloat(bs.paddingRight));
  const 세로막대 = 반(window.innerWidth - document.documentElement.clientWidth);
  const 푸터 = document.querySelector("footer");

  // ① GNB 가 위에 붙어 있나
  /* ⚠ 아무 nav 나 상단바로 보면 안 된다 (2026-09-02). 로그인 같은 화면은 상단바가
     아예 없는데, 그 화면의 첫 nav 는 «길잡이»(빵부스러기)다. 길잡이는 굴러 올라가는
     것이 맞다 — 인테리어 팩 207장을 재다가 AU0201·AU0301 을 그렇게 헛짚었다.
     이름에 crumb·breadcrumb·path 가 든 것과, 화면 위쪽에 안 붙은 것은 건너뛴다. */
  const 상단바 = [...document.querySelectorAll("header, nav")].find(function (el) {
    if (/crumb|breadcrumb|path|back/.test(el.className || "")) return false;
    return el.getBoundingClientRect().top < 120;
  });
  if (상단바) { const p = getComputedStyle(상단바).position;
    if (p !== "sticky" && p !== "fixed") 적기("고정", 이름(상단바) + " 가 position:" + p + " — 굴리면 따라 올라갑니다"); }

  // ② 본문과 푸터 사이가 붙었나 (그릇이 아니라 «안 마지막 알맹이»까지 잰다)
  if (푸터 && 푸터.previousElementSibling) {
    let 끝 = 푸터.previousElementSibling;
    if (보이나(끝) && !/sticky|fixed/.test(getComputedStyle(끝).position)) {
      for (let i = 0; i < 6; i++) {
        const 안 = [...끝.children].filter((c) => 보이나(c) && getComputedStyle(c).position === "static");
        if (!안.length) break; 끝 = 안[안.length - 1];
      }
      const 틈 = 반(푸터.getBoundingClientRect().top - 끝.getBoundingClientRect().bottom);
      if (틈 >= 0 && 틈 < 16) 적기("간격", "본문과 푸터 사이가 " + 틈 + "px — " + 이름(끝) + " 바로 아래 푸터");
    }
  }

  // ③ 덩어리 사이 간격이 그 칸의 «리듬»을 벗어났나
  for (const 부모 of [본문, ...본문.querySelectorAll("*")]) {
    if (!보이나(부모) || 부모.closest("table, svg")) continue;
    const ps = getComputedStyle(부모);
    if (/flex|grid/.test(ps.display) && ps.flexDirection !== "column") continue;
    const 아이 = [...부모.children].filter((c) => 보이나(c) && getComputedStyle(c).position !== "absolute");
    if (아이.length < 3) continue;
    const 틈들 = [];
    for (let i = 1; i < 아이.length; i++) {
      const a = 아이[i - 1].getBoundingClientRect(), b = 아이[i].getBoundingClientRect();
      if (b.top < a.bottom - 2) continue;
      틈들.push({ v: 반(b.top - a.bottom), 앞: 아이[i - 1], 뒤: 아이[i] });
    }
    if (틈들.length < 3) continue;
    const 벌 = 틈들.filter((x) => x.v >= 8);
    if (벌.length < 틈들.length * 0.6) continue;
    const 리듬 = 틈들.map((x) => x.v).sort((a, b) => a - b)[Math.floor(틈들.length / 2)];
    for (const x of 틈들) {
      // 「리듬의 절반도 안 된다」만으로는 리듬이 클수록 헛짚는다. 섹션 사이가 80px 인 칸에
      // 「안내 띠 → 탭 → 탭 내용」이 24·16px 로 붙은 «한 묶음»이 들어 있으면 그건 묶음이다.
      // 눈에 붙어 보이는 것은 결국 절대값이 작을 때다.
      const 붙 = x.v <= 2, 좁 = 리듬 >= 20 && x.v < 리듬 * 0.45 && x.v <= 12;
      if (!붙 && !좁) continue;
      // 같은 «종류»가 이어지는 줄은 붙는 게 맞다. 뒤에 붙은 여백 클래스(mb3·total)까지
      // 견주면 "card mb3" 와 "card mb6" 를 서로 다른 것으로 봐서 헛짚는다.
      if ((x.앞.className || "").trim().split(/ +/)[0] === (x.뒤.className || "").trim().split(/ +/)[0]) continue;
      if (x.뒤 === 푸터) continue;
      // 누군가 «이 값으로» 정해 둔 자리는 흠이 아니다. 2px 은 이름 밑에 한 줄 소개를
      // 붙이려고 손으로 적은 값이지, 어쩌다 붙은 것이 아니다.
      if (붙 && (parseFloat(getComputedStyle(x.앞).marginBottom) || 0) +
                (parseFloat(getComputedStyle(x.뒤).marginTop) || 0) > 0) continue;
      // 뒤엣것이 «윗줄»이나 제 안여백을 가졌으면 그것이 곧 틈이다 — 카드 아랫단이 그렇다.
      if (붙) { const 뒤s = getComputedStyle(x.뒤);
        if (parseFloat(뒤s.borderTopWidth) > 0 || (parseFloat(뒤s.paddingTop) || 0) >= 8) continue; }
      // 길잡이(빵부스러기)·«‹ 뒤로»는 다음 덩어리와 한 덩어리다
      if (!붙 && (/crumb|breadcrumb|path|back/.test(x.앞.className || "") ||
                  x.앞.tagName.toLowerCase() === "nav")) continue;
      // «이름표와 값»은 한 덩어리다 — 어느 쪽이 위든 붙는 것이 맞다.
      // 제목→부제만 넘기면 「대표 시술 최저가 → 25,000원~」처럼 뒤집힌 짝을 흠이라 한다.
      const 잔글 = (el) => /t-sub|sub|desc|help|hint|caption|label/.test(el.className || "");
      const 큰글 = (el) => /^(h[1-4]|b|strong)$/.test(el.tagName.toLowerCase()) ||
                          /t-sec|t-card|t-page|lb|price|nm|name|field/.test(el.className || "");
      /* 큰 글자를 «감싼 칸»도 큰 글자로 본다 (2026-09-02). div.center 안에 30px
         제목을 넣어 둔 자리를 못 알아보고 그 아래 잔글씨와의 8px 을 흠이라 했다. */
      const 큰글감쌈 = (el) => 큰글(el) ||
        [...el.children].some(function (c) { return 큰글(c) || parseFloat(getComputedStyle(c).fontSize) >= 24; });
      /* ⭐ 2026-09-03 — 잔글(설명·캡션·힌트)은 «위든 아래든» 제 짝에 붙는 것이 맞다.
         앞서는 «잔글 ↔ 큰글»만 넘겼는데, 차트 밑 캡션·목록 위 설명처럼 짝이 큰글이
         아닌 자리를 열 번 넘게 헛짚었다. 이 규칙은 «칸 리듬보다 좁을 때»만 도므로,
         잔글이 제 짝에 바짝 붙은 것을 흠이라 할 일이 없다. */
      if (잔글(x.앞) || 잔글(x.뒤)) continue;
      /* 짧은 이름표 바로 «밑»의 입력 부품은 그 이름표의 것이다 — 한 덩어리다.
         「시·도」 아래 칩 묶음, 「연락처」 아래 입력칸이 그렇다. 이름이 아니라
         «짧은 글 한 줄 + 바로 아래 부품»이라는 사실로 가른다 (2026-09-03). */
      /* 이름표와 그 부품, 그리고 «나란한 부품 줄»은 한 덩어리다 — 칸 리듬과 견줄 것이
         아니다. 「인증번호 6자리」 밑 입력칸, 「전체 동의」 밑 약관 줄들이 그렇다.
         ⚠ 이름이 아니라 «사실»로 가른다 — 짧은 글이거나 부품을 담은 것이면 한 짝이다.
         (2026-09-03. 앞서 «자식이 없는 짧은 글»만 봤더니 「인증번호 6자리 · 남은 시간」
          처럼 두 조각으로 된 이름표 줄을 못 알아봤다.) */
      const 부품 = "input, select, textarea, button, .chip, .slot, .otp, .progress, progress";
      const 부품있나 = function (el) {
        return el.matches && (el.matches(부품) || !!el.querySelector(부품));
      };
      /* ⚠ 「\s」 의 역슬래시를 빠뜨리지 마라 (2026-09-07). /s+/ 는 «빈칸»이 아니라
         «영문 s 글자»를 찾는다. HTML 은 태그 사이에 줄바꿈과 들여쓰기가 그대로 들어
         있으므로, 빈칸을 못 접으면 「전체 동의」 같은 두 줄짜리 이름표가 마흔 자를
         넘겨 «짧은 글»이 아니게 되고, 바로 아래 부품과 한 덩어리라는 것을 못 알아본다.
         이 파일은 String.raw 라 역슬래시가 그대로 살아 있다 — 빼면 안 된다. */
      const 짧은글 = function (el) {
        const t = (el.textContent || "").replace(/\s+/g, " ").trim();
        return t.length > 0 && t.length <= 40;
      };
      if (부품있나(x.뒤) && (짧은글(x.앞) || 부품있나(x.앞))) continue;
      /* 단추 바로 밑 잔글씨는 «그 단추의 설명»이라 붙는 것이 맞다 (2026-09-02).
         「내 캘린더에 담기」 아래 「전날 오전에 확인 문자를 보내 드려요」가 그렇다 —
         눈으로 보면 한 덩어리인데 8px 이라고 짚었다. */
      if (잔글(x.뒤) && x.앞.matches &&
          (x.앞.matches("button, .btn, a.btn") || (x.앞.querySelector && x.앞.querySelector("button, .btn, a.btn")))) continue;
      적기("간격", 이름(x.뒤) + " 와 위 " + 이름(x.앞) + " 사이 " + x.v + "px (이 칸 리듬은 " + 리듬 + "px)");
    }
  }

  // ④ 버튼·배지가 늘어났나 / 동그란 버튼이 타원이 됐나
  const 부품 = [...document.querySelectorAll(".badge, .btn, button, [class*=badge], [class*=btn]")].filter((el) => {
    if (!보이나(el)) return false;
    // 버튼을 «담는 칸»(.btns 같은 것)은 부품이 아니다 — 넓은 게 맞다.
    // ⚠ 담긴 것이 .btn 이 아니라 그냥 <a> 일 수도 있다(「목록 보기 ›」). 그것도 칸이다.
    if (el.querySelector(".badge, .btn, button, a, [role=button], input, select, textarea, label")) return false;
    // 일부러 칸을 꽉 채우라고 시킨 것
    if (/block|full|wide/.test(el.className || "")) return false;
    // ⚠ 이름은 사람마다 다르게 짓는다(btn-w · w-full · is-block …). 이름 말고 «사실»로 본다 —
    //   제 스스로는 줄 안에 서는 것(inline·inline-flex·inline-block)이 칸을 꽉 채우고 있다면
    //   그건 만든 사람이 width 를 직접 준 것이다. 늘어난 것이 아니라 그렇게 시킨 것이다.
    {
      const s0 = getComputedStyle(el), p0 = el.parentElement;
      if (p0 && s0.display.indexOf("inline") === 0) {
        const ps0 = getComputedStyle(p0);
        const 속폭 = p0.getBoundingClientRect().width
          - parseFloat(ps0.paddingLeft) - parseFloat(ps0.paddingRight);
        if (el.getBoundingClientRect().width >= 속폭 - 1) return false;
      }
    }
    return true;
  });
  const 무리 = new Map();
  const 열쇠 = (el) => (el.className || "").trim().split(/ +/)[0] || el.tagName;
  for (const el of 부품) { const k = 열쇠(el); if (!무리.has(k)) 무리.set(k, []); 무리.get(k).push(el); }
  for (const el of 부품) {
    const s = getComputedStyle(el), r = el.getBoundingClientRect();
    if (s.borderTopLeftRadius.includes("%") && parseFloat(s.borderTopLeftRadius) >= 40 && r.width / r.height > 1.5)
      적기("버튼", 이름(el) + " 는 동그란 버튼인데 " + 반(r.width) + "x" + 반(r.height) + " 로 늘어나 타원이 됐습니다");
    const rg = document.createRange(); rg.selectNodeContents(el);
    const 글폭 = rg.getBoundingClientRect().width; if (!글폭) continue;
    const 또래 = 무리.get(열쇠(el)) || [];
    const 또래폭 = 또래.map((x) => x.getBoundingClientRect().width).sort((a, b) => a - b)[Math.floor(또래.length / 2)];
    if (또래.length >= 2 && r.width < 또래폭 * 2.5) continue;             // 다 같이 넓으면 일부러 그런 것
    // 스스로 «자라라»고 적힌 것은 늘어난 것이 아니다 — flex:1 1 0% 는 한 줄을 반씩
    // 나눠 쓰라는 뜻이다. 세로로 쌓은 단추 더미도 꽉 채우는 것이 제 모습이다.
    if (parseFloat(s.flexGrow) > 0) continue;
    const 부모 = el.parentElement; if (!부모) continue;
    const ps2 = getComputedStyle(부모);
    const 세로더미 = ps2.display.indexOf("flex") >= 0 && ps2.flexDirection.indexOf("column") === 0;
    if (세로더미 && r.width <= 480) continue;
    // 칸에 혼자 서 있으면 꽉 채우는 것이 제 모습이다 — 나란한 것이 없으니 견줄 것도 없다
    if ([...부모.children].filter(보이나).length === 1) continue;
    const 남 = 반(r.width - 글폭 - parseFloat(s.paddingLeft) - parseFloat(s.paddingRight));
    // ⚠ 까닭을 «지어내지» 않는다. 부모가 실제로 어떻게 서 있는지 읽어서 적는다.
    const 까닭 = ps2.display.indexOf("flex") >= 0
      ? 이름(부모) + " 가 flex(" + ps2.flexDirection + ") 인데 align-items 가 " + ps2.alignItems + " 입니다"
      : 이름(부모) + " 는 " + ps2.display + " 입니다";
    if (남 > 140) 적기("늘어남", 이름(el) + " 가 " + 반(r.width) + "px 인데 글은 " + 반(글폭) +
      "px — " + 까닭);
  }

  // ⑤ 좌우로 미는 칸에 «막대»가 드러났나
  for (const el of document.querySelectorAll("*")) {
    if (!보이나(el)) continue;
    const s = getComputedStyle(el);
    if (!/auto|scroll/.test(s.overflowX) || el.scrollWidth <= el.clientWidth + 2) continue;
    const 두께 = el.offsetHeight - el.clientHeight - parseFloat(s.borderTopWidth) - parseFloat(s.borderBottomWidth);
    if (두께 <= 2) continue;
    // 넘기는 길은 하나여야 한다 — «화살표가 있는데 띠도 깔린 것»이 흠이다.
    // ⚠ 화살표가 없으면 그 띠가 «넘길 수 있다»는 유일한 신호다. 표·간트가 그렇다.
    const 둘레 = el.parentElement || el;
    if (!둘레.querySelector(".car-nav, .nav, [aria-label=이전], [aria-label=다음], .prev, .next")) continue;
    적기("막대", 이름(el) + " 에 가로 막대가 " + 반(두께) + "px 드러납니다 — 화살표로 넘기게 하세요");
  }

  // ⑥ 표가 제 칸을 넘쳤나
  for (const t of document.querySelectorAll("table")) {
    if (!보이나(t) || !t.parentElement) continue;
    /* 감싼 칸이 «옆으로 밀 수» 있으면 넓은 표는 흠이 아니다 — 좁은 화면에서
       표를 뭉개지 않는 옳은 방법이다(2026-09-03). */
    const 밀림 = getComputedStyle(t.parentElement).overflowX;
    if (밀림 === "auto" || 밀림 === "scroll") continue;
    const 넘침 = 반(t.scrollWidth - t.parentElement.clientWidth);
    if (넘침 > 4) 적기("표", "표가 제 칸보다 " + 넘침 + "px 넓습니다");
  }

  // ⑦ 지금 보는 메뉴가 «켜져» 있나
  //    표시하는 방법은 사이트마다 다릅니다. 그래서 «클래스 이름»으로 찾지 않고,
  //    지금 쪽을 가리키는 링크가 옆 링크들과 «달라 보이는지»를 봅니다.
  for (const 메뉴 of document.querySelectorAll("nav, header, [role=navigation]")) {
    if (!보이나(메뉴)) continue;
    /* 바닥글의 링크 목록은 «지금 여기»를 표시하지 않는 것이 보통이다 — 둘러보는 길이지
       지금 어디 있나를 말하는 자리가 아니다(2026-09-03). */
    if (메뉴.closest("footer")) continue;
    const 고리 = [...메뉴.querySelectorAll("a[href]")].filter(보이나);
    if (고리.length < 3) continue;
    const 여기 = 고리.filter((a) => a.pathname === location.pathname);
    if (!여기.length) continue;
    const 다름 = (a) => {
      if (a.getAttribute("aria-current")) return true;
      const s = getComputedStyle(a);
      return 고리.some((b) => {
        if (b === a) return false;
        const t = getComputedStyle(b);
        return s.color !== t.color || s.fontWeight !== t.fontWeight ||
               s.borderBottomWidth !== t.borderBottomWidth || s.backgroundColor !== t.backgroundColor;
      });
    };
    const 켜진것 = 여기.filter(다름);
    if (!켜진것.length) 적기("메뉴", 이름(메뉴) + " 에 «지금 여기» 표시가 없습니다 — 링크 " + 고리.length + "개가 다 같아 보입니다");
    break;
  }

  // ⑧ 배경 위 글자가 읽히나 — 본문 4.5 · 큰 글자 3.0 (WCAG)
  //    사진 위 글자는 잴 수 없어 건너뜁니다. 눈으로 보셔야 합니다.
  //
  // ⛔ 색 글자에서 숫자만 뽑아 쓰지 마세요. 요즘 브라우저는 「lab(91.9 -0.6 5.1 / .85)」 처럼
  //    내주는 일이 잦은데, 숫자만 뽑으면 «밝은 회색»을 새까맣게 읽어 멀쩡한 화면이
  //    죄다 흠으로 잡힙니다. 캔버스에 한 점 찍어 브라우저가 직접 풀게 합니다.
  const 판 = document.createElement("canvas"); 판.width = 판.height = 1;
  const 붓 = 판.getContext("2d", { willReadFrequently: true });
  const 색값 = (s) => {
    try { 붓.clearRect(0, 0, 1, 1); 붓.fillStyle = s; 붓.fillRect(0, 0, 1, 1);
      const d = 붓.getImageData(0, 0, 1, 1).data;
      return [d[0], d[1], d[2], d[3] / 255];
    } catch (e) { return null; }
  };
  // 반투명한 것은 «아래 색 위에 얹어» 실제로 보이는 색을 만든다.
  const 겹치기 = (위, 아래) => {
    const a = 위[3] + 아래[3] * (1 - 위[3]);
    if (!a) return [0, 0, 0, 0];
    return [0, 1, 2].map((i) => (위[i] * 위[3] + 아래[i] * 아래[3] * (1 - 위[3])) / a).concat([a]);
  };
  const 광 = (c) => { const f = (v) => { v = v / 255; return v <= 0.03928 ? v / 12.92 : Math.pow((v + 0.055) / 1.055, 2.4); };
    return 0.2126 * f(c[0]) + 0.7152 * f(c[1]) + 0.0722 * f(c[2]); };
  const 바탕 = (el) => { let n = el, 쌓임 = null;
    for (let i = 0; n && i < 14; i++, n = n.parentElement) {
      const s = getComputedStyle(n);
      if (s.backgroundImage !== "none") return null;   // 사진·그러데이션 위는 못 잰다
      const c = 색값(s.backgroundColor);
      if (!c || c[3] === 0) continue;
      쌓임 = 쌓임 ? 겹치기(쌓임, c) : c;
      if (쌓임[3] >= 0.99) return 쌓임.slice(0, 3);
    }
    return 쌓임 ? 겹치기(쌓임, [255, 255, 255, 1]).slice(0, 3) : [255, 255, 255]; };
  let 대비셈 = 0;
  for (const el of document.querySelectorAll("p, a, li, h1, h2, h3, h4, button, td, th, label, span, strong")) {
    if (대비셈 >= 8 || !보이나(el)) continue;
    const 글 = (el.textContent || "").trim();
    if (!글 || 글.length > 120) continue;
    if ([...el.children].some((c) => (c.textContent || "").trim())) continue;  // 글을 «직접» 가진 것만
    // 꺼진 부품은 건너뛴다 — WCAG 1.4.3 이 «작동하지 않는 UI 부품»을 면제한다.
    // 못 고르는 날짜·잠긴 단추를 흐리게 두는 것은 «그렇게 보이라고» 한 것이다.
    if (el.closest("[disabled], [aria-disabled='true'], fieldset[disabled]")) continue;
    const s = getComputedStyle(el);
    const 글색 = 색값(s.color), 뒤 = 바탕(el);
    if (!글색 || !뒤 || 글색[3] === 0) continue;
    const 앞 = 글색[3] < 1 ? 겹치기(글색, 뒤.concat([1])) : 글색;   // 흐린 글자는 바탕에 얹어 잰다
    const a = 광(앞), b = 광(뒤);
    const 비 = (Math.max(a, b) + 0.05) / (Math.min(a, b) + 0.05);
    const 크기 = parseFloat(s.fontSize), 굵기 = parseInt(s.fontWeight) || 400;
    const 기준 = (크기 >= 24 || (크기 >= 18.66 && 굵기 >= 700)) ? 3 : 4.5;
    if (비 < 기준) { 대비셈++;
      적기("대비", 이름(el) + " «" + 글.slice(0, 14) + "» 대비 " + 비.toFixed(2) + " — " + 기준 + " 이상이어야 읽힙니다"); }
  }

  // ⑨ 나란히 놓인 사진 크기가 같은가 (윗변이 같은 것끼리만 견준다)
  for (const 부모 of document.querySelectorAll("main *, body > *")) {
    if (!보이나(부모)) continue;
    const 사진 = [...부모.children]
      .map((c) => (c.tagName === "IMG" ? c : c.querySelector ? c.querySelector("img") : null))
      .filter((x) => x && 보이나(x));
    if (사진.length < 3) continue;
    const 틀 = 사진.map((i) => i.getBoundingClientRect());
    if (new Set(틀.map((r) => 반(r.top))).size > 1) continue;
    const 높 = 틀.map((r) => 반(r.height));
    const 작 = Math.min.apply(null, 높), 큰 = Math.max.apply(null, 높);
    if (작 > 0 && 큰 - 작 > Math.max(8, 작 * 0.15))
      적기("사진", 이름(부모) + " 안에 나란한 사진 높이가 " + 작 + "~" + 큰 + "px 로 제각각입니다");
  }

  return JSON.stringify({
    화면: location.pathname.split("/").pop() || "(첫 화면)",
    콘텐츠폭, 세로막대,
    뒤로가기: !!document.querySelector(".back, [class*=back]"),
    탭이가는곳: [...document.querySelectorAll("[data-go], .tab[href], .tabs a")]
      .map((x) => x.getAttribute("data-go") || x.getAttribute("href")).filter(Boolean),
    흠,
  }, null, 1);
})()
```

**`흠` 에 담겨 나온 것은 그 화면에서 바로 고치세요.** 하나도 안 남을 때까지 다시 돌립니다.

⚠ **한 화면만 돌리고 끝내지 마세요.** 위의 「사람처럼 보는 검수 열다섯 가지」 가운데
이 글이 대신 재 주는 것은 **열 가지**입니다 — 나머지는 눈으로 보셔야 합니다.
**만든 화면을 하나씩 열어 가며 쪽마다 돌리세요.** 한 장만 보면 「이 화면끼리 폭이 다르다」를 못 잡습니다.

📱 **폰 폭에서 한 번 더 돌리세요.** 브라우저 개발자도구에서 폭을 **375px** 로 줄이고
같은 글을 다시 돌립니다. 넓은 화면에서 멀쩡하던 것이 여기서 무너지는 일이 가장 잦습니다.

**화면 «하나»로는 알 수 없는 것 셋** — 여러 화면의 결과를 모아 견주세요.
- **콘텐츠폭** 이 화면마다 다르면 옮길 때 글이 좌우로 밀립니다. 모든 화면이 한 값이어야 합니다.
- **세로막대** 가 어떤 화면은 0, 어떤 화면은 15 면 그만큼 화면이 좌우로 튑니다. `html { scrollbar-gutter: stable; }` 로 잡으세요.
- **뒤로가기** 가 `탭이가는곳` 으로 이어진 화면들 사이에서 갈리면 안 됩니다. 탭이면 넷 다 없는 쪽이 맞습니다.


### 7-9-2. 기계가 보는 검수 — 파일만 보고 재기
> 7-9 가 «그려 봐야 아는 것»을 잰다면, 이것은 **화면을 안 열어도 아는 것**을 잡습니다.
> 만든 폴더에서 한 번 돌리면 끊어진 링크·없는 그림·외톨이 화면이 한꺼번에 나옵니다.

⭐ **글자 대비는 아래 글이 아니라 `7-9` 의 글이 ⑧번으로 잽니다.**
파일만 봐서는 실제로 칠해진 색을 알 수 없어서입니다. 7-0 의 «② 기계가 보는 검수» 에서
여기로 오셨다면, 대비만은 **7-9** 를 함께 돌리세요.

```js
// node 로 돌리세요:  node 검수.mjs ./내사이트
// ⚠ «사이트 뿌리»를 주세요. 하위 폴더까지 훑습니다 —
//    pages/ 만 주면 한 층 위의 화면 목록(index.html)을 못 봐서
//    거기서만 이어지는 화면이 죄다 «외톨이»로 잘못 잡힙니다.
import { readdirSync, readFileSync, existsSync, statSync } from "node:fs";
import { join, resolve, dirname, relative } from "node:path";

const 뿌리 = process.argv[2] || ".";
const 쪽들 = [];
(function 훑기(방) {
  for (const e of readdirSync(방, { withFileTypes: true })) {
    if (e.name.startsWith(".") || e.name === "node_modules") continue;
    const 길 = join(방, e.name);
    if (e.isDirectory()) 훑기(길);
    else if (e.name.endsWith(".html")) 쪽들.push(길);
  }
})(뿌리);

const 흠 = [];
const 이어진곳 = new Set();

for (const 길 of 쪽들) {
  const 쪽 = relative(뿌리, 길);
  const s = readFileSync(길, "utf8");

  // ① 끊어진 링크
  for (const m of s.matchAll(/href="([^"]+)"/g)) {
    const v = m[1];
    if (!v || v.startsWith("#") || v.startsWith("http") || v.startsWith("mailto:")) continue;
    const 갈곳 = resolve(dirname(길), v.split("#")[0].split("?")[0]);
    이어진곳.add(갈곳);
    if (v.endsWith(".html") && !existsSync(갈곳)) 흠.push("[끊어진 링크] " + 쪽 + " -> " + v);
  }

  // ② 없는 그림
  for (const m of s.matchAll(/src="([^"]+)"/g)) {
    const v = m[1];
    if (!v || v.startsWith("http") || v.startsWith("data:")) continue;
    if (!existsSync(resolve(dirname(길), v.split("?")[0]))) 흠.push("[없는 그림] " + 쪽 + " -> " + v);
  }

  // ③ 눌러도 아무 데도 안 가는 것
  const 빈링크 = (s.match(/href="(#|javascript:void\(0\))"/g) || []).length;
  if (빈링크) 흠.push("[빈 링크] " + 쪽 + " 에 " + 빈링크 + "개 — 갈 곳이 있으면 잇고, 없으면 «안 눌리게» 두세요");

  // ④ 0바이트
  if (statSync(길).size === 0) 흠.push("[빈 파일] " + 쪽);
}

// ⑤ 외톨이 화면 — 만들어 놓고 아무 데서도 안 이어지는 쪽
for (const 길 of 쪽들) {
  const 쪽 = relative(뿌리, 길);
  if (/^(index|home)\./i.test(쪽)) continue;
  if (!이어진곳.has(resolve(길))) 흠.push("[외톨이] " + 쪽 + " 은 어느 화면에서도 이어지지 않습니다");
}

console.log("화면 " + 쪽들.length + "장 · 흠 " + 흠.length + "건");
for (const h of 흠) console.log("  " + h);
if (!흠.length) console.log("  깨끗합니다");
```

### 7-9-3. 화면이 많을 때 — 한 번에 전부 재기
> 화면이 스무 장만 넘어도 콘솔에 스무 번 붙이는 것은 못 할 일입니다.
> 이 글이 **화면을 전부 열어서** 7-9 와 «똑같은 것»을 잽니다.

**쓰는 법** — 두 파일을 같은 자리에 두고 한 줄만 치세요.

```bash
# 7-9 의 글을 화면검수-글.js 로, 아래 글을 화면검수.mjs 로 저장한 뒤
node 화면검수.mjs ./내사이트
```

```js
// node 로 돌리세요:  node 화면검수.mjs ./내사이트
//
// 화면 한 장씩 콘솔에 붙여 넣는 것이 힘드실 때 씁니다. 100장이면 100번이라
// 이 글이 대신 «전부» 열어서 잽니다. 재는 것은 콘솔에 붙이는 글과 똑같습니다.
//
// ⚠ 헤드리스 브라우저가 있어야 합니다. 없으면 이 글이 «무엇을 치면 되는지»
//   한 줄로 알려 주고 멈춥니다. 아무것도 망가뜨리지 않습니다.
import { readdirSync, readFileSync, writeFileSync } from "node:fs";
import { join, relative, resolve } from "node:path";
import { pathToFileURL } from "node:url";   // ⚠ node:path 가 아니다. 문법검사는 이걸 못 잡는다

const 뿌리 = process.argv[2] || ".";
const 재는글 = process.argv[3];  // 안 주면 이 파일 옆의 화면검수-글.js 를 찾습니다

let 열기;
try {
  ({ chromium: 열기 } = await import("playwright"));
} catch {
  try {
    const p = await import("puppeteer");
    열기 = { launch: (o) => p.default.launch(o) };
  } catch {
    console.log("헤드리스 브라우저가 없습니다. 아래 한 줄을 치신 뒤 다시 돌리세요.");
    console.log("");
    console.log("  npm i -D playwright && npx playwright install chromium");
    console.log("");
    console.log("깔기 싫으시면 화면을 크롬으로 열고 F12 → Console 에");
    console.log("스펙팩 7-9 의 글을 붙여 넣으셔도 됩니다. 재는 것은 같습니다.");
    process.exit(1);
  }
}

/* 잴 화면을 모은다 — 하위 폴더까지 훑는다. */
const 쪽들 = [];
(function 훑기(방) {
  for (const e of readdirSync(방, { withFileTypes: true })) {
    if (e.name.startsWith(".") || e.name === "node_modules") continue;
    const 길 = join(방, e.name);
    if (e.isDirectory()) 훑기(길);
    else if (e.name.endsWith(".html")) 쪽들.push(길);
  }
})(뿌리);
if (!쪽들.length) { console.log(뿌리 + " 안에 html 이 없습니다."); process.exit(1); }

/* 재는 글을 읽는다. 스펙팩 7-9 의 그 글을 파일로 저장해 두시면 됩니다. */
let 글;
try {
  글 = readFileSync(재는글 || "화면검수-글.js", "utf8");
} catch {
  console.log("재는 글을 못 찾았습니다.");
  console.log("스펙팩 7-9 의 자바스크립트를 «화면검수-글.js» 로 저장한 뒤 다시 돌리세요.");
  console.log("  node 화면검수.mjs ./내사이트 ./화면검수-글.js");
  process.exit(1);
}

const 브 = await 열기.launch();
const 쪽 = await 브.newPage();
/* ⛔ await 쪽.setViewportSize ? A : B 로 쓰지 마라 — (await 쪽.setViewportSize) ? A : B 로
   읽혀서 고른 쪽을 «안 기다린다». 둘은 이름이 다르다(playwright · puppeteer). */
const 창크기 = { width: 1440, height: 900 };
if (typeof 쪽.setViewportSize === "function") await 쪽.setViewportSize(창크기);
else if (typeof 쪽.setViewport === "function") await 쪽.setViewport(창크기);

let 흠셈 = 0;
const 줄들 = [];
for (const 길 of 쪽들) {
  const 이름 = relative(뿌리, 길);
  try {
    await 쪽.goto(pathToFileURL(resolve(길)).href, { waitUntil: "load" });
    const 답 = JSON.parse(await 쪽.evaluate(글));
    if (!답.흠.length) { 줄들.push("  ✓ " + 이름); continue; }
    흠셈 += 답.흠.length;
    줄들.push("  ⛔ " + 이름 + "  " + 답.흠.length + "건");
    for (const h of 답.흠) 줄들.push("       " + h);
  } catch (e) {
    줄들.push("  ⚠ " + 이름 + " — 못 열었습니다: " + String(e.message).split(String.fromCharCode(10))[0]);
  }
}
await 브.close();

const 보고 = ["화면 " + 쪽들.length + "장 · 흠 " + 흠셈 + "건", ...줄들].join(String.fromCharCode(10));
console.log(보고);
writeFileSync("화면검수_결과.txt", 보고, "utf8");
console.log("");
console.log("화면검수_결과.txt 에도 적어 두었습니다.");
process.exit(흠셈 ? 1 : 0);
```

결과는 화면에 찍히고 **`화면검수_결과.txt`** 에도 적힙니다. 흠이 있으면 1 로 끝나므로
`npm run` 이나 CI 에 걸어 두셔도 됩니다.

**글자 대비는 7-9 의 글이 ⑧번으로 잽니다** — 파일만 봐서는 실제로 칠해진 색을 알 수 없어서입니다.
본문 글자는 **4.5**, 24px 이상이거나 굵은 18.66px 이상은 **3.0** 을 넘어야 합니다.

### 7-10. 나온 오류는 «그 자리에서» 고칩니다
> 적어 두고 넘어가면 다음에 또 나옵니다. 찾은 김에 고치는 것이 제일 쌉니다.

- 고친 뒤에는 **그 화면을 다시 눌러** 정말 고쳐졌는지 봅니다. 고쳤다고 생각만 하고 넘어가는 일이 잦습니다.
- **한 화면에서 고친 것이 다른 화면을 깨뜨리지 않았는지** 봅니다. 공통 CSS·공통 스크립트를 고쳤다면 특히 그렇습니다.
  (짜임이 다른 화면 — 사이드바형과 상단바형 — 에 같은 규칙을 씌우면 한쪽이 무너집니다.)
- 같은 뿌리에서 난 것이면 **한 곳만 고치고 끝내지 말고** 같은 자리를 다 찾아 고칩니다.
- 고칠 수 없는 것(자료가 없어서, 화면이 아직 없어서)은 **화면에 솔직히 적습니다.** 「견본이라 어느 것을 눌러도 이 화면이 열립니다」처럼요.

### 7-11. 무엇을 확인했는지 적어 주세요
- 위 항목을 **숫자로** 적어 남깁니다 — 「눌러 본 화면 N개 · 빈 링크 0개 · 반응 없는 단추 0개 · 안 걸러지는 필터 0개 · 안 눌리는 썸네일·배너 0개 · 뒤로가기 어긋남 0개 · 화면 목록 단추 없는 화면 0개 · 화면끼리 안 맞는 숫자 0개 · 가로 넘침 0개 · **고친 것 N건**」.
- 숫자를 적을 수 없으면 아직 확인한 것이 아닙니다.

### 바꿀 값은 한 곳에
- **상호** · **주소** · **전화번호** · **이메일** · **영업시간** · **로고·대표 이미지** — 손님이 받아서 **제일 먼저 자기 것으로 바꾸는 값**입니다.
- 위 값은 **데이터 파일 한 곳**에 모으고, 화면은 전부 그 값을 불러 씁니다.
- 화면 코드·헤더·푸터는 물론 **페이지 제목(`<title>`)과 메타 설명까지** 그 값을 씁니다 — 여기가 제일 자주 빠집니다.
- 안내 문구 안에 상호가 섞여 있어도(예: 「내비게이션에 ‘○○’를 넣으세요」) 글자로 적지 말고 값을 끼워 넣습니다.
- 다 만든 뒤 **상호를 다른 이름으로 한 번 바꿔 보고** 옛 이름이 남는 곳이 있는지 찾아 보세요. 한 곳이라도 남으면 아직 안 끝난 것입니다.
### 파일을 어디에 두나

```
완성화면/
  index.html              홈 화면 (손님이 처음 보는 화면)
  화면목록.html            우리가 검수용으로 보는 «전체 화면 목록»
  pages/
    HO-01.html            화면 하나에 파일 하나 · 이름은 화면 ID 그대로
    CO-03.html
  assets/
    css/style.css         스타일 한 벌
    js/app.js             동작 한 벌
```

- **`pages/` 안에서는 `../assets/css/style.css` 로 부릅니다.** `assets/...` 라고 쓰면 `pages/assets/...` 를 찾다가 못 찾아서 **스타일이 통째로 안 붙습니다** — 글꼴도 여백도 격자도 전부 날아갑니다.
- ⭐ **`index.html` 은 «전체 화면 목록»입니다. 홈 화면이 아닙니다.** 이 폴더는 손님이 검수하는 **시안**이라, 열자마자 몇 개를 만들었는지 한눈에 보여야 합니다. 홈 화면은 `pages/` 안에 다른 화면들과 나란히 둡니다.
- **`index.html` 에는 화면을 «하나도 빼지 않고»** 메뉴별로 묶어 넣습니다. 화면 ID 와 이름을 함께 적어, 거기서 아무 화면이나 바로 열 수 있어야 합니다. 홈에서 닿는 것만 넣으면 나머지는 영영 못 봅니다.
- `index.html` 은 `완성화면/` 바로 아래 둡니다. 거기서는 `assets/...` 그대로 부르면 됩니다.
- **스타일과 동작은 각각 한 벌**입니다. 화면마다 따로 만들면 44장이 서로 달라집니다.
- 화면끼리 오갈 때는 `CO-03.html` 처럼 **같은 폴더 안 이름**으로 겁니다.
- 다 만든 뒤 **화면 하나를 브라우저로 직접 열어 보세요.** 글꼴이 기본 고딕으로 보이면 경로가 틀린 것입니다.

### 추천 스택
- 정적 다중 페이지(HTML/CSS) 또는 컴포넌트 기반(React·Next.js·Vue 등) 중 프로젝트 규모에 맞게 선택하세요. 화면이 많고 상태가 복잡하면 컴포넌트 기반을 권장합니다.
