# 동네 서비스 매칭 플랫폼 — AI 빌드 스펙팩

> 이 문서 하나로 사이트를 만들 수 있게 정리한 스펙이에요. 화면별 **생성 프롬프트**를 그대로 사용하세요.

## 1. 프로젝트 개요
- **컨셉**: 이사·청소·인테리어·과외·촬영처럼 사람을 직접 구해야 하는 일을 요청서 한 장으로 맡기는 동네 서비스 매칭 플랫폼. 손님은 무엇을·언제·어디서·예산이 얼마인지 단계별로 답해 요청서를 보내고, 조건에 맞는 고수들에게서 견적을 받아 가격·평점·응답 속도를 비교해 한 명을 고른다. 고수는 자기 분야와 활동 지역을 설정해 두고 들어온 요청에 견적을 보내며(보낼 때마다 크레딧 차감), 성사되면 채팅으로 조율하고 일을 마친 뒤 정산받는다.
- **디자인 컨셉**: 믿고 맡길 수 있다는 인상이 중요하다. 차분한 바탕에 포인트 한 색만 쓰고, 고수 프로필 사진과 평점·응답률 같은 신뢰 지표를 크게 보여준다. 요청서는 한 번에 한 질문씩 물어 부담을 줄인다.
- **디바이스**: 반응형
- **전체 일정**: 2026-08-10 ~ 2026-11-20

## 2. 공통 요소 (모든 화면 공통)
> 아래 요소는 **한 번만 만들어 모든 화면에서 재사용**하세요. 각 화면 스펙(4장)은 이 공통 요소 위에 올라가는 콘텐츠만 설명합니다.
- **영역이 둘입니다. 헤더를 각각 따로 만드세요.**
  - **손님 화면 GNB**: `홈` · `고수 찾기` · `요청서` · `받은 견적` · `채팅` · `결제·이용` · `내 요청` · `후기`
  - **운영자 화면 GNB**: `고수센터` · `요청 받기` · `진행 관리` — 좌측 사이드바 + 상단 바 형태가 맞습니다
  - **로그인·가입 화면**: `계정` — 헤더 없이 단독 화면으로 만드세요
  - 두 영역을 오가는 길은 **하나만** 둡니다 — 손님 화면 우측 상단에 운영자 영역으로 넘어가는 버튼 하나(예: `고수센터 화면으로`), 운영자 화면 우측 상단에 `고객 화면 보기`. 한 계정이 두 권한을 가지며(크몽·숨고와 같은 방식), 아직 권한이 없으면 전환 대신 **신청 화면으로 보냅니다.**
- **콘텐츠 영역**: 최대 1440px, 좌우 여백 24px — 헤더·본문·푸터·하단 고정 바가 모두 이 폭을 쓴다. 화면마다 따로 정하지 않는다. 화면마다 폭을 새로 정하면 위아래가 어긋납니다.
- **읽기 폭**: 글이 주인공인 자리(상세 본문·안내·약관)는 최대 760px로 좁혀 콘텐츠 영역 **안에서** 가운데 정렬 — 카드 격자에는 쓰지 않습니다. 아래 「읽기 폭」 참고.
- **헤더**: 로고 + 검색 + 상단 내비게이션(GNB)
- **푸터**: 저작권 · 기본 링크
- **재사용 컴포넌트**: 반복되는 UI(상품/항목 **카드**, 리스트 **행**, **폼 필드**, **버튼**)는 공용 컴포넌트로 만들어 화면마다 재사용하세요.

## 3. 메뉴 구조
- **[HO] 홈** (Home)
  - `HO-01` 홈 - 비로그인
  - `HO-02` 홈 - 손님 로그인
- **[SE] 고수 찾기** (Search)
  - `SE-01` 고수 목록
  - `SE-02` 고수 목록 - 결과 없음
  - `SE-03` 고수 상세
  - `SE-04` 고수 포트폴리오
  - `SE-05` 찜한 고수
- **[RQ] 요청서** (Request)
  - `RQ-01` 서비스 고르기
  - `RQ-02` 요청서 작성 - 단계별 질문
  - `RQ-03` 요청서 확인
  - `RQ-04` 요청 완료
  - `RQ-05` 요청 수정·취소
- **[QT] 받은 견적** (Quotes)
  - `QT-01` 견적 목록
  - `QT-02` 견적 비교
  - `QT-03` 견적 상세
  - `QT-04` 고수 선택 - 확인
- **[CH] 채팅** (Chat)
  - `CH-01` 채팅 목록
  - `CH-02` 채팅방
- **[PY] 결제·이용** (Payment)
  - `PY-01` 결제
  - `PY-02` 결제 완료
  - `PY-03` 결제 실패
- **[MY] 내 요청** (MyRequests)
  - `MY-01` 내 요청 목록
  - `MY-02` 요청 상세 - 진행 상황
  - `MY-03` 내 요청 - 비어 있음
- **[RV] 후기** (Review)
  - `RV-01` 후기 작성
  - `RV-02` 내 후기 목록
- **[PR] 고수센터** (ProCenter)
  - `PR-01` 고수센터 홈
  - `PR-02` 고수 등록 신청
  - `PR-03` 고수 등록 - 심사 중
  - `PR-04` 활동 설정
  - `PR-05` 프로필·포트폴리오 관리
- **[LD] 요청 받기** (Leads)
  - `LD-01` 새 요청 목록
  - `LD-02` 요청 상세
  - `LD-03` 견적 보내기
  - `LD-04` 크레딧 충전
- **[JB] 진행 관리** (Jobs)
  - `JB-01` 일감 목록
  - `JB-02` 일감 상세
  - `JB-03` 정산 내역
- **[AU] 계정** (Auth)
  - `AU-01` 로그인
  - `AU-02` 회원가입
  - `AU-03` 회원가입 - 휴대폰 인증
  - `AU-04` 회원가입 완료
  - `AU-05` 비밀번호 찾기

## 4. 화면별 스펙
### HO-01 · 홈 - 비로그인
- **소속 메뉴**: 홈
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-08-10 ~ 2026-08-13
- **기능정의**: 상단 히어로(핵심 카피 + '어떤 도움이 필요하세요?' 서비스 검색바) · 인기 서비스 카테고리 12종 아이콘 · 3단계 이용 안내(요청서 작성→견적 받기→고수 선택) · 실시간 요청 티커 · 분야별 인기 고수 카드 8개(사진·평점·응답률·시작가) · 최근 후기 슬라이드 6건 · 누적 매칭 수·평균 견적 도착 시간 지표 · 고수 지원 유도 배너 · 자주 묻는 질문 아코디언 · 하단 요청서 작성 CTA
- **버튼 → 이동화면**:
  - `요청서 작성` → `RQ-01` 서비스 고르기
  - `고수 찾기` → `SE-01` 고수 목록
  - `로그인` → `AU-01` 로그인
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `서비스 분야 아이콘` → 고른 분야에 표시가 남고 아래 인기 고수 목록이 그 분야만 남게 걸러진다
  - `「우리 동네」 지역 고르기` → 아래 고수 목록과 「N명」 숫자가 그 지역 것으로 바뀐다
  - `자주 묻는 질문` → 답이 펼쳐지고 다시 누르면 접힌다. 한 번에 하나만 열린다
  - `후기 캐러셀 좌우 화살표` → 카드가 한 칸씩 밀리고 끝에 닿으면 화살표가 흐려진다
- **생성 프롬프트**:

  ```
  동네 서비스 매칭 플랫폼의 비로그인 홈 화면을 만들어줘. 스크롤 순서대로 아래를 쌓아줘. 1) 상단 GNB(로고, 고수 찾기, 요청서 작성, 이용 안내, 고수 가입, 로그인) 2) 히어로 — "어떤 도움이 필요하세요?" 큰 검색바 하나. 아래에 인기 검색어 칩 5개 3) 인기 서비스 카테고리 12종 아이콘 그리드(이사·청소·인테리어·과외·촬영·수리 등) 4) 3단계 이용 안내 — 요청서 작성 → 견적 받기 → 고수 선택. 번호와 짧은 설명 5) 실시간 요청 티커("방금 강남구에서 이사 요청이 등록됐어요") 6) 분야별 인기 고수 카드 8개 — 프로필 사진, 활동명, 평점, 응답률, 시작가 7) 최근 후기 슬라이드 6건 8) 지표 3개 — 누적 매칭 수, 평균 견적 도착 시간, 활동 고수 수 9) 고수 지원 배너 10) 자주 묻는 질문 아코디언 11) 하단 요청서 작성 CTA와 푸터 신뢰가 중요한 서비스라 고수 사진과 평점·응답률을 크게. 모바일에서는 카드 1열.
  ```

### HO-02 · 홈 - 손님 로그인
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈 - 비로그인` — 누르면 `HO-01`로 간다
- **일정**: 2026-08-14 ~ 2026-08-19
- **기능정의**: 진행 중인 요청 카드 최상단 고정(받은 견적 수·남은 시간·다음 할 일) · 읽지 않은 채팅 알림 · 견적 마감 임박 안내 · 최근 본 고수 · 다시 맡기기(지난 고수 재요청) · 내 지역 인기 서비스 · 예정된 일정(확정된 방문일) · 후기 미작성 알림 · 추천 서비스 캐러셀 · 하단 새 요청서 작성 CTA
- **버튼 → 이동화면**:
  - `견적 비교` → `QT-02` 견적 비교
  - `채팅` → `CH-01` 채팅 목록
  - `내 요청` → `MY-01` 내 요청 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `진행 상태 탭(견적 대기·진행 중·완료)` → 아래 내 요청 목록과 탭 옆 숫자가 함께 바뀐다
  - `「새 견적」 알림 줄` → 그 줄이 읽음으로 바뀌고 위의 안 읽음 배지가 하나 줄어든다
  - `관심 분야 고르기` → 아래 추천 고수가 그에 맞게 바뀐다
- **생성 프롬프트**:

  ```
  손님이 로그인한 상태의 홈 화면을 만들어줘. 1) 맨 위에 진행 중인 요청 카드 — 서비스명, 받은 견적 3개, 마감까지 남은 시간, "견적 비교하기" 큰 버튼 2) 읽지 않은 채팅 알림 배지와 미리보기 2건 3) 예정된 일정 카드 — 확정된 방문일과 고수 이름, 연락 버튼 4) 후기를 안 쓴 완료 건 안내 5) "다시 맡기기" — 지난번에 이용한 고수 카드 3개, 바로 요청 버튼 6) 최근 본 고수 가로 스크롤 7) 내 지역에서 인기 있는 서비스 카테고리 8) 하단에 새 요청서 작성 CTA 할 일이 있는 것부터 위에 오게 해줘.
  ```

### SE-01 · 고수 목록
- **소속 메뉴**: 고수 찾기
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-08-20 ~ 2026-08-25
- **기능정의**: 서비스 카테고리·지역·가격대·평점 필터 · 정렬(추천순/평점순/응답 빠른순/가격 낮은순) · 고수 카드 목록(프로필 사진·활동명·한 줄 소개·평점·후기 수·응답률·평균 응답 시간·시작가·활동 지역) · 인증 배지(사업자·자격증·신원확인) · 찜하기 · 지도로 보기 전환 · 무한 스크롤
- **버튼 → 이동화면**:
  - `고수 상세` → `SE-03` 고수 상세
  - `결과 없음` → `SE-02` 고수 목록 - 결과 없음
  - `요청서 작성` → `RQ-01` 서비스 고르기
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `왼쪽 필터(지역·분야·가격대·평점)` → 고른 조건이 칩으로 쌓이고 고수 목록과 「N명」 숫자가 함께 바뀐다
  - `정렬(추천·평점·거래 많은 순·가까운 순)` → 카드 순서가 실제로 다시 늘어선다
  - `칩의 ✕` → 그 조건만 풀리고 목록이 다시 늘어난다
  - `「오늘 응답 가능만」 토글` → 켜면 오늘 답 주는 고수만 남고 숫자가 줄어든다
  - `카드의 찜 하트` → 채워지고 다시 누르면 비워진다
- **생성 프롬프트**:

  ```
  고수 목록 화면을 만들어줘. 좌측에 필터 패널(서비스 카테고리 체크박스, 활동 지역, 가격대 슬라이더, 평점 4.5 이상, 인증 여부), 우측에 고수 카드 목록. 카드 한 장에 왼쪽 프로필 사진(정사각), 오른쪽에 활동명·한 줄 소개·평점과 후기 수· 응답률·평균 응답 시간·활동 지역·시작가를 넣고, 사업자/자격증/신원확인 인증 배지를 작게 붙여줘. 카드 오른쪽 끝에 찜하기 하트와 "견적 요청" 버튼. 상단에 정렬 드롭다운과 결과 수, "지도로 보기" 전환 버튼.
  ```

### SE-02 · 고수 목록 - 결과 없음
- **소속 메뉴**: 고수 찾기
- **뒤로가기**: 좌측 상단에 `‹ 고수 목록` — 누르면 `SE-01`로 간다
- **일정**: 2026-08-26 ~ 2026-08-28
- **기능정의**: 검색어·필터를 포함한 안내 문구 · 적용된 필터 칩과 개별 해제 · 조건 완화 제안(지역 넓히기·가격대 올리기) · 요청서를 쓰면 고수가 먼저 연락한다는 안내 · 추천 고수 4명 · 요청서 작성 유도 버튼
- **버튼 → 이동화면**:
  - `요청서 작성` → `RQ-01` 서비스 고르기
  - `고수 목록` → `SE-01` 고수 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `걸린 조건 칩의 ✕` → 결과 수가 0에서 늘어나고 빈 화면 대신 목록이 나타난다
  - `「조건 모두 풀기」` → 칩이 전부 사라지고 전체 고수가 나온다
- **생성 프롬프트**:

  ```
  조건에 맞는 고수가 없을 때의 화면을 만들어줘. 아이콘과 "강남구에서 '피아노 과외' 조건에 맞는 고수가 없어요" 문구, 적용된 필터 칩을 보여주고 하나씩 뺄 수 있게, "이렇게 해보세요" 제안 두 개 — 지역을 서울 전체로 넓히기 / 가격대 올리기. 그 아래 강조 박스로 "요청서를 남기시면 고수가 먼저 연락드려요" 안내와 요청서 작성 버튼. 맨 아래 비슷한 분야 추천 고수 4명.
  ```

### SE-03 · 고수 상세
- **소속 메뉴**: 고수 찾기
- **뒤로가기**: 좌측 상단에 `‹ 고수 목록` — 누르면 `SE-01`로 간다
- **일정**: 2026-08-31 ~ 2026-09-03
- **기능정의**: 프로필 사진과 활동명·한 줄 소개 · 신뢰 지표(평점·후기 수·응답률·평균 응답 시간·매칭 성사 수·활동 기간) · 인증 배지와 확인된 항목 · 제공 서비스와 가격표 · 자기소개 상세 · 포트폴리오 사진 · 후기 목록과 별점 분포 · 활동 지역 지도 · 휴무·응답 가능 시간 · 하단 고정 견적 요청 버튼
- **버튼 → 이동화면**:
  - `포트폴리오` → `SE-04` 고수 포트폴리오
  - `요청서 작성` → `RQ-01` 서비스 고르기
  - `채팅 문의` → `CH-02` 채팅방
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `소개·포트폴리오·후기·가격 탭` → 아래 내용이 그 탭 것으로 바뀐다
  - `후기 별점 필터` → 후기 목록과 개수가 함께 걸러진다
  - `가격표 항목 펼치기` → 무엇이 포함되는지 펼쳐진다
  - `찜 하트` → 채워지고 다시 누르면 비워진다
- **생성 프롬프트**:

  ```
  고수 상세 화면을 만들어줘. 상단에 프로필 사진(원형 큰 것), 활동명, 한 줄 소개, 인증 배지 3개를 배치하고 그 아래 신뢰 지표를 숫자 카드 4개로 (평점, 응답률, 평균 응답 시간, 누적 매칭 수). 본문은 왼쪽에 제공 서비스와 가격표(표), 자기소개, 포트폴리오 사진 그리드, 후기 목록(별점 분포 막대 포함), 활동 지역 지도 자리. 오른쪽에 따라다니는 카드 — 시작가, 응답 가능 시간, "견적 요청하기" 큰 버튼, 찜하기와 공유. 화면 하단에도 고정 액션 바를 둬줘.
  ```

### SE-04 · 고수 포트폴리오
- **소속 메뉴**: 고수 찾기
- **뒤로가기**: 좌측 상단에 `‹ 고수 목록` — 누르면 `SE-01`로 간다
- **일정**: 2026-09-04 ~ 2026-09-08
- **기능정의**: 작업 사진 갤러리(격자 배치, 클릭 시 크게 보기) · 작업별 설명과 기간·비용대 · 분야별 필터 · 사진 확대 시 좌우 이동 · 이 작업으로 견적 요청 · 사진 신고 · 더 보기
- **버튼 → 이동화면**:
  - `고수 상세` → `SE-03` 고수 상세
  - `요청서 작성` → `RQ-01` 서비스 고르기
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `분야 필터` → 포트폴리오 사진이 그 분야 것만 남는다
  - `사진` → 크게 열리고 좌우로 넘길 수 있다. 작업 설명이 함께 뜬다
- **생성 프롬프트**:

  ```
  고수 포트폴리오 화면을 만들어줘. 상단에 고수 요약 줄(사진·활동명·평점)과 분야 필터 칩. 본문은 작업 사진 격자 3열. 사진 아래 작업명, 걸린 기간, 비용대를 적어줘. 사진을 누르면 크게 보이는 상태(어두운 배경에 좌우 이동 화살표, 작업 설명, "이 작업처럼 견적 요청" 버튼)도 함께 만들어줘. 사진 비율은 4:3으로 통일.
  ```

### SE-05 · 찜한 고수
- **소속 메뉴**: 고수 찾기
- **뒤로가기**: 좌측 상단에 `‹ 고수 목록` — 누르면 `SE-01`로 간다
- **일정**: 2026-09-09 ~ 2026-09-10
- **기능정의**: 찜한 고수 목록과 찜한 날짜 · 상태 변화 표시(가격 변동·새 후기·휴무 중) · 여러 명 골라 한 번에 견적 요청 · 찜 해제 · 비어 있을 때 안내와 고수 찾기 유도 · 최근 본 고수 목록
- **버튼 → 이동화면**:
  - `고수 상세` → `SE-03` 고수 상세
  - `요청서 작성` → `RQ-01` 서비스 고르기
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `찜 해제` → 그 카드가 목록에서 빠지고 「N명」이 줄어든다
  - `정렬 바꾸기` → 찜 목록 순서가 다시 늘어선다
- **생성 프롬프트**:

  ```
  찜한 고수 목록 화면을 만들어줘. 고수 카드마다 왼쪽에 체크박스를 두고, 여러 명을 골라 "고른 고수에게 한 번에 견적 요청"을 할 수 있게 해줘. 카드에는 찜한 날짜와 그 뒤 바뀐 것(가격이 내려갔어요, 새 후기 3건, 지금 휴무)을 작은 배지로 표시. 아래쪽에 최근 본 고수 가로 스크롤도 넣어줘.
  ```

### RQ-01 · 서비스 고르기
- **소속 메뉴**: 요청서
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-09-11 ~ 2026-09-15
- **기능정의**: 대분류 카테고리 12종 · 대분류를 고르면 소분류 목록 노출 · 검색으로 바로 찾기 · 최근 요청한 서비스 · 인기 서비스 표시 · 고른 서비스 확인과 다음 단계 이동 · 어떤 걸 골라야 할지 모를 때 안내
- **버튼 → 이동화면**:
  - `다음` → `RQ-02` 요청서 작성 - 단계별 질문
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `서비스 분야 고르기` → 고른 것에 표시가 남고 「다음」이 눌리게 켜진다
  - `세부 서비스 고르기` → 아래 예상 견적 범위가 그 서비스 것으로 바뀐다
  - `검색` → 치는 대로 분야 목록이 걸러진다
- **생성 프롬프트**:

  ```
  요청서 1단계, 어떤 서비스가 필요한지 고르는 화면을 만들어줘. 상단에 진행 표시(1 서비스 → 2 상세 질문 → 3 확인 → 4 완료). 큰 검색바 하나와 그 아래 대분류 카테고리 12개를 아이콘 카드로. 카테고리를 누르면 오른쪽(모바일은 아래)에 소분류 목록이 펼쳐지게 해줘. 소분류에는 인기 표시를 작게 붙이고, 맨 아래 "어떤 걸 골라야 할지 모르겠어요" 링크를 둬줘.
  ```

### RQ-02 · 요청서 작성 - 단계별 질문
- **소속 메뉴**: 요청서
- **뒤로가기**: 좌측 상단에 `‹ 서비스 고르기` — 누르면 `RQ-01`로 간다
- **일정**: 2026-09-16 ~ 2026-09-18
- **기능정의**: 한 화면에 한 질문씩 노출 · 질문 유형별 입력(객관식 선택·복수 선택·날짜·주소·금액대·자유 입력) · 진행률 표시와 남은 질문 수 · 이전 질문으로 돌아가기(고른 값 유지) · 건너뛸 수 있는 질문 표시 · 사진 첨부 · 임시 저장 안내
- **버튼 → 이동화면**:
  - `다음` → `RQ-03` 요청서 확인
  - `이전` → `RQ-01` 서비스 고르기
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `질문 답 고르기` → 다음 질문으로 넘어가고 위 진행 막대가 채워진다
  - `「이전」` → 앞 질문으로 돌아가고 골랐던 답이 그대로 남아 있다
  - `답을 안 고른 상태` → 「다음」이 눌리지 않는다
  - `사진 올리기·빼기` → 미리보기가 늘고 줄며 「N/5장」이 따라 바뀐다
- **생성 프롬프트**:

  ```
  요청서 2단계, 상세 질문 화면을 만들어줘. 한 화면에 질문 하나만 크게 보여줘. 상단에 진행률 막대와 "3 / 7"처럼 남은 질문 수. 질문 예시 — "이사 유형을 골라주세요"(객관식 카드 3개: 원룸/가정/사무실), 그 아래 "이전" 과 "다음" 버튼. 건너뛸 수 있는 질문에는 "건너뛰기" 링크. 날짜를 묻는 질문(달력), 주소를 묻는 질문(주소 검색), 예산을 묻는 질문(금액대 칩), 사진을 올리는 질문(드래그 앤 드롭)도 각각 어떻게 생겼는지 함께 보여줘. 아래에 "작성 중인 내용은 자동으로 저장돼요" 안내.
  ```

### RQ-03 · 요청서 확인
- **소속 메뉴**: 요청서
- **뒤로가기**: 좌측 상단에 `‹ 서비스 고르기` — 누르면 `RQ-01`로 간다
- **일정**: 2026-09-21 ~ 2026-09-23
- **기능정의**: 작성한 답을 항목별로 모아 보여주기 · 항목마다 고치기 버튼 · 연락 받을 방법 선택 · 요청서가 몇 명의 고수에게 전달되는지 안내 · 예상 견적 도착 시간 · 개인정보 제공 동의(고수에게 전달되는 항목 명시) · 요청서 보내기
- **버튼 → 이동화면**:
  - `보내기` → `RQ-04` 요청 완료
  - `고치기` → `RQ-02` 요청서 작성 - 단계별 질문
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `항목의 「수정」` → 그 줄이 입력 칸으로 바뀌고 고치면 요약이 따라 바뀐다
  - `「받고 싶은 견적 수」 바꾸기` → 아래 안내 문구가 그 수에 맞게 바뀐다
- **생성 프롬프트**:

  ```
  요청서 3단계, 보내기 전 확인 화면을 만들어줘. 작성한 답을 "무엇을 / 언제 / 어디서 / 예산 / 추가 요청"으로 묶어 보여주고 항목마다 오른쪽에 "고치기" 링크를 둬줘. 그 아래 강조 박스로 "이 요청서는 조건에 맞는 고수 24명에게 전달되고, 보통 30분 안에 첫 견적이 도착해요". 연락 받을 방법(앱 알림·문자·전화) 선택, 개인정보 제공 동의 — 고수에게 전달되는 항목(이름·연락처·주소)을 표로 명시. 맨 아래 큰 "요청서 보내기" 버튼.
  ```

### RQ-04 · 요청 완료
- **소속 메뉴**: 요청서
- **뒤로가기**: 좌측 상단에 `‹ 서비스 고르기` — 누르면 `RQ-01`로 간다
- **일정**: 2026-09-24 ~ 2026-09-28
- **기능정의**: 요청 접수 완료 메시지와 요청 번호 · 전달된 고수 수와 예상 견적 도착 시간 · 견적이 오면 알림으로 알려준다는 안내 · 기다리는 동안 볼 것(비슷한 고수 프로필) · 요청서 다시 보기 · 내 요청으로 이동 · 요청 취소 안내
- **버튼 → 이동화면**:
  - `내 요청` → `MY-01` 내 요청 목록
  - `받은 견적` → `QT-01` 견적 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `요청 번호` → 눌러서 복사되고 「복사했어요」가 뜬다
  - `「알림 받기」 토글` → 켜면 연락 방법 칸이 나타난다
- **생성 프롬프트**:

  ```
  요청서를 보낸 뒤 완료 화면을 만들어줘. 큰 체크 아이콘과 "요청서를 보냈어요" 문구, 요청 번호. 그 아래 카드로 "조건에 맞는 고수 24명에게 전달했어요· 첫 견적은 보통 30분 안에 도착해요". 견적이 오는 대로 알림으로 알려준다는 안내와 알림 설정 링크. "기다리는 동안" 섹션에 조건에 맞는 고수 프로필 4개. 아래에 "내 요청 보기" 큰 버튼과 "요청서 다시 보기" 링크.
  ```

### RQ-05 · 요청 수정·취소
- **소속 메뉴**: 요청서
- **뒤로가기**: 좌측 상단에 `‹ 서비스 고르기` — 누르면 `RQ-01`로 간다
- **일정**: 2026-09-29 ~ 2026-10-01
- **기능정의**: 수정 가능 여부 안내(견적이 오기 전까지 자유 수정) · 항목별 수정 · 수정하면 고수에게 다시 전달된다는 안내 · 이미 받은 견적이 있을 때 영향 설명 · 요청 취소 사유 선택 · 취소 시 받은 견적이 사라진다는 경고 · 최종 확인 모달
- **버튼 → 이동화면**:
  - `내 요청` → `MY-01` 내 요청 목록
  - `받은 견적` → `QT-01` 견적 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「수정」` → 요청 내용이 입력 칸으로 바뀌고 저장하면 되돌아온다
  - `취소 사유 고르기` → 고른 것에 표시가 남고 「취소」가 눌리게 켜진다
- **생성 프롬프트**:

  ```
  요청서 수정·취소 화면을 만들어줘. 상단에 현재 상태(견적 3개 받음) 안내 박스. 본문은 작성한 항목 목록과 각각 고치기. "내용을 고치면 고수들에게 다시 전달되고, 이미 받은 견적은 그대로 남아요" 안내. 아래쪽에 취소 영역 — 취소 사유 선택(다른 곳에서 해결/일정 변경/생각보다 비쌈/기타), "취소하면 받은 견적 3개가 모두 사라지고 되돌릴 수 없어요" 경고, 취소 확인 모달까지 만들어줘.
  ```

### QT-01 · 견적 목록
- **소속 메뉴**: 받은 견적
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-10-02 ~ 2026-10-06
- **기능정의**: 받은 견적 카드 목록(고수 사진·활동명·견적 금액·평점·응답 시간·한 줄 제안) · 새 견적 표시 · 정렬(낮은 가격순/평점순/빠른 응답순) · 견적 마감까지 남은 시간 · 여러 개 골라 비교하기 · 관심 없는 견적 숨기기 · 아직 견적이 없을 때 안내
- **버튼 → 이동화면**:
  - `견적 비교` → `QT-02` 견적 비교
  - `견적 상세` → `QT-03` 견적 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `정렬(낮은 금액·평점·빠른 응답)` → 견적 카드 순서가 다시 늘어선다
  - `카드의 「비교에 담기」` → 담은 수가 위에 뜨고 「비교하기」가 켜진다. 다시 누르면 빠진다
  - `「읽지 않음만」 토글` → 목록이 걸러지고 숫자가 바뀐다
- **생성 프롬프트**:

  ```
  받은 견적 목록 화면을 만들어줘. 상단에 요청 요약 줄(서비스명, 희망일, 지역)과 "견적 5개· 마감까지 2일 남음". 견적 카드마다 왼쪽에 고수 프로필 사진, 가운데 활동명·평점·응답 시간·한 줄 제안, 오른쪽에 견적 금액을 크게. 새로 온 견적에는 NEW 배지. 카드마다 체크박스를 둬서 여러 개를 고르면 하단 고정 바에 "3개 비교하기" 버튼이 뜨게 해줘. 정렬 드롭다운과 "관심 없어요" 숨기기도 넣어줘.
  ```

### QT-02 · 견적 비교
- **소속 메뉴**: 받은 견적
- **뒤로가기**: 좌측 상단에 `‹ 견적 목록` — 누르면 `QT-01`로 간다
- **일정**: 2026-10-07 ~ 2026-10-08
- **기능정의**: 고른 견적 2~4개를 표로 나란히 비교 · 비교 항목(금액·포함 내역·소요 시간·평점·후기 수·응답률·인증·취소 규정) · 가장 싼 값과 가장 높은 평점 강조 · 항목별 차이만 보기 · 비교에서 빼기 · 각 열에서 바로 선택하기
- **버튼 → 이동화면**:
  - `견적 상세` → `QT-03` 견적 상세
  - `고수 선택` → `QT-04` 고수 선택 - 확인
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「차이 항목만 보기」 토글` → 같은 값인 줄이 접히고 다른 줄만 남는다
  - `비교에서 견적 빼기` → 그 열이 표에서 사라지고 남은 것끼리 다시 비교된다
- **생성 프롬프트**:

  ```
  견적 비교 화면을 만들어줘. 고른 견적 3개를 세로 열로 나란히 놓는 표. 첫 열은 비교 항목 이름(견적 금액, 포함 내역, 예상 소요 시간, 평점, 후기 수, 응답률, 인증, 취소 규정), 나머지 열은 각 고수. 맨 위 열 머리에는 고수 프로필 사진과 활동명. 가장 싼 금액과 가장 높은 평점 칸을 색으로 강조하고 "최저가" "최고 평점" 배지를 붙여줘. "차이가 있는 항목만 보기" 토글, 각 열 아래에 "이 고수 선택" 버튼. 모바일에서는 표가 가로로 넘어가되 첫 열은 고정되게 해줘.
  ```

### QT-03 · 견적 상세
- **소속 메뉴**: 받은 견적
- **뒤로가기**: 좌측 상단에 `‹ 견적 목록` — 누르면 `QT-01`로 간다
- **일정**: 2026-10-09 ~ 2026-10-13
- **기능정의**: 고수가 보낸 제안 전문 · 금액 구성 내역(기본료·추가 항목·출장비) · 포함되는 것과 안 되는 것 · 예상 일정 · 고수 프로필 요약과 후기 바로가기 · 취소·환불 규정 · 궁금한 점 채팅으로 묻기 · 이 견적으로 선택하기
- **버튼 → 이동화면**:
  - `고수 선택` → `QT-04` 고수 선택 - 확인
  - `채팅 문의` → `CH-02` 채팅방
  - `고수 상세` → `SE-03` 고수 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `금액 항목 펼치기` → 무엇이 포함·불포함인지 펼쳐진다
  - `「추가 옵션」 체크` → 합계 금액이 그 자리에서 다시 계산된다
- **생성 프롬프트**:

  ```
  견적 상세 화면을 만들어줘. 상단에 고수 요약(사진·활동명·평점·응답률)과 견적 금액을 크게. 본문 왼쪽에 고수가 보낸 제안 글 전문, 금액 구성 표(기본료·추가 항목·출장비·합계), "포함되는 것 / 포함되지 않는 것"을 두 열로, 예상 일정 타임라인, 취소·환불 규정. 오른쪽에 따라다니는 카드 — 견적 금액, "이 고수 선택하기" 큰 버튼, "궁금한 점 물어보기"(채팅), 고수 프로필 보기.
  ```

### QT-04 · 고수 선택 - 확인
- **소속 메뉴**: 받은 견적
- **뒤로가기**: 좌측 상단에 `‹ 견적 목록` — 누르면 `QT-01`로 간다
- **일정**: 2026-10-14 ~ 2026-10-16
- **기능정의**: 고른 고수와 견적 요약 재확인 · 선택하면 연락처가 서로 공개된다는 안내 · 나머지 고수에게 자동으로 안내가 간다는 설명 · 결제는 일이 끝난 뒤인지 먼저인지 안내 · 선택 후에도 취소 가능한 조건 · 최종 확인 체크박스 · 선택 확정
- **버튼 → 이동화면**:
  - `채팅` → `CH-02` 채팅방
  - `내 요청` → `MY-01` 내 요청 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `약관 동의 체크` → 다 체크해야 「이 고수로 결정」이 켜진다
  - `「다시 생각해 볼게요」` → 이 화면이 닫히고 견적 목록으로 돌아간다
- **생성 프롬프트**:

  ```
  고수를 선택하기 전 확인 화면(모달)을 만들어줘. "이 고수에게 맡길까요?" 문구와 고수·견적 요약(사진, 활동명, 금액, 예상 일정). 그 아래 안내 목록 — 선택하면 서로 연락처가 공개돼요 / 나머지 고수 4명에게는 자동으로 안내가 가요 / 결제는 일이 끝난 뒤에 해요 / 방문 3일 전까지는 수수료 없이 취소할 수 있어요. "확인했어요" 체크박스를 체크해야 "선택하기" 버튼이 활성화되게 해줘.
  ```

### CH-01 · 채팅 목록
- **소속 메뉴**: 채팅
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-10-19 ~ 2026-10-21
- **기능정의**: 대화 목록(상대 사진·이름·마지막 메시지·시간·안 읽은 수) · 요청별 묶어 보기 · 진행 상태 배지(견적 문의·진행 중·완료) · 검색 · 알림 끄기 · 대화 나가기 · 대화가 없을 때 안내
- **버튼 → 이동화면**:
  - `채팅방` → `CH-02` 채팅방
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `전체·안 읽음 탭` → 목록과 탭 옆 숫자가 함께 바뀐다
  - `채팅방 줄` → 오른쪽에 그 대화가 열리고 안 읽음 배지가 사라진다
  - `검색` → 치는 대로 목록이 걸러진다
- **생성 프롬프트**:

  ```
  채팅 목록 화면을 만들어줘. 상단에 검색바와 필터 칩(전체·안 읽음·진행 중·완료). 목록 한 줄에 왼쪽 프로필 사진, 가운데 활동명과 마지막 메시지(한 줄로 잘림), 그 아래 어느 요청에 대한 대화인지 작게, 오른쪽에 시간과 안 읽은 개수 배지. 줄마다 진행 상태 배지(견적 문의 / 진행 중 / 완료)를 색으로 구분.
  ```

### CH-02 · 채팅방
- **소속 메뉴**: 채팅
- **뒤로가기**: 좌측 상단에 `‹ 채팅 목록` — 누르면 `CH-01`로 간다
- **일정**: 2026-10-22 ~ 2026-10-27
- **기능정의**: 말풍선 대화(보낸 것·받은 것 구분) · 사진·파일 보내기 · 견적서 카드가 대화에 끼어 보이기 · 일정 제안과 수락 · 안전 거래 안내(외부 송금 주의) · 상대 프로필 미리보기 · 신고·차단 · 읽음 표시와 상대 입력 중 표시
- **버튼 → 이동화면**:
  - `채팅 목록` → `CH-01` 채팅 목록
  - `견적 상세` → `QT-03` 견적 상세
  - `결제` → `PY-01` 결제
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `메시지 보내기` → 말풍선이 아래에 붙고 입력 칸이 비워지며 스크롤이 맨 아래로 간다
  - `파일·사진 올리기` → 미리보기 말풍선이 붙는다
  - `빠른 답변 문구` → 입력 칸에 그 말이 채워진다
- **생성 프롬프트**:

  ```
  채팅방 화면을 만들어줘. 상단 바에 상대 프로필 사진·활동명·평점, 오른쪽에 더보기(신고·차단·알림 끄기). 그 아래 얇은 안내 띠 — "앱 밖에서 송금하지 마세요. 문제가 생기면 도와드릴 수 없어요". 본문은 말풍선 대화. 내 것은 오른쪽 색 채움, 상대 것은 왼쪽 흰 배경. 중간에 견적서 카드(금액과 "견적 보기" 버튼)와 일정 제안 카드(날짜와 수락/다른 날 제안 버튼)가 대화 사이에 끼어 있는 모습도 넣어줘. 읽음 표시, 상대가 입력 중이라는 표시, 하단 입력창(사진·파일 첨부 아이콘 포함).
  ```

### PY-01 · 결제
- **소속 메뉴**: 결제·이용
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-10-28 ~ 2026-10-30
- **기능정의**: 이용 내역 요약(고수·서비스·일시·최종 금액) · 견적 금액과 달라졌을 때 사유 표시 · 쿠폰·포인트 적용 · 결제 수단 선택 · 안전 결제 안내(일이 끝난 뒤 결제됨) · 현금영수증 신청 · 결제 동의 · 결제하기
- **버튼 → 이동화면**:
  - `결제 완료` → `PY-02` 결제 완료
  - `결제 실패` → `PY-03` 결제 실패
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `결제 방식 고르기(전액·계약금)` → 결제 금액과 잔금 안내가 다시 계산된다
  - `쿠폰·포인트 적용` → 할인 줄과 최종 금액이 다시 계산된다
  - `약관 동의 체크` → 다 체크해야 「결제하기」가 켜진다
- **생성 프롬프트**:

  ```
  결제 화면을 만들어줘. 상단에 이용 내역 카드 — 고수 사진과 활동명, 서비스명, 방문 일시, 최종 금액. 견적 금액과 달라졌으면 "추가 작업으로 20,000원이 늘었어요"처럼 사유를 보여주고 원래 견적과 나란히 비교. 쿠폰·포인트 적용 영역, 결제 수단 선택(카드·간편결제·계좌이체), 현금영수증 신청, 오른쪽에 금액 요약 카드와 큰 "결제하기" 버튼. 안내 박스로 "일이 끝난 것을 확인한 뒤 결제됩니다".
  ```

### PY-02 · 결제 완료
- **소속 메뉴**: 결제·이용
- **뒤로가기**: 좌측 상단에 `‹ 결제` — 누르면 `PY-01`로 간다
- **일정**: 2026-11-02 ~ 2026-11-03
- **기능정의**: 결제 완료 메시지와 주문번호 · 결제 내역 요약과 영수증 · 후기 작성 유도(포인트 안내) · 이 고수 다시 맡기기 · 문제가 있었을 때 신고 안내 · 내 요청으로 이동
- **버튼 → 이동화면**:
  - `후기 작성` → `RV-01` 후기 작성
  - `내 요청` → `MY-01` 내 요청 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `결제 번호` → 눌러서 복사되고 「복사했어요」가 뜬다
  - `「영수증 보기」` → 내역이 그 자리에서 펼쳐진다
- **생성 프롬프트**:

  ```
  결제 완료 화면을 만들어줘. 큰 체크 아이콘과 "결제가 끝났어요", 주문번호와 결제 일시. 결제 내역 요약(서비스, 고수, 금액, 결제 수단)과 영수증 보기 링크. 그 아래 강조 카드로 "후기를 남기면 1,000 포인트를 드려요" + 후기 쓰기 버튼. "이 고수에게 다시 맡기기" 버튼과 "문제가 있었나요? 신고하기" 링크.
  ```

### PY-03 · 결제 실패
- **소속 메뉴**: 결제·이용
- **뒤로가기**: 좌측 상단에 `‹ 결제` — 누르면 `PY-01`로 간다
- **일정**: 2026-11-04 ~ 2026-11-06
- **기능정의**: 실패 사유를 쉬운 말로 안내 · 이용 내역은 그대로 유지된다는 안내 · 다시 시도 · 다른 결제 수단 선택 · 고수에게 상황 알리기 · 고객센터 문의 · 미결제 상태로 두면 어떻게 되는지 안내
- **버튼 → 이동화면**:
  - `다시 시도` → `PY-01` 결제
  - `채팅` → `CH-02` 채팅방
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `실패 사유 「자세히」` → 무엇이 문제였는지 펼쳐진다
  - `다른 결제 수단 고르기` → 표시가 옮겨 가고 「다시 시도」 문구가 바뀐다
- **생성 프롬프트**:

  ```
  결제 실패 화면을 만들어줘. 아이콘과 "결제가 되지 않았어요", 사유("카드 한도를 넘었습니다"). "아직 결제되지 않았고 이용 내역은 그대로 있어요" 안내. "이럴 때 이렇게 해보세요" 목록 3개(한도 확인·다른 카드·간편결제). 버튼 — 다시 시도하기 / 다른 결제 수단 / 고수에게 상황 알리기 / 고객센터. 아래 작은 글씨로 "3일 안에 결제하지 않으면 고수에게 안내가 갑니다".
  ```

### MY-01 · 내 요청 목록
- **소속 메뉴**: 내 요청
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-11-09 ~ 2026-11-10
- **기능정의**: 상태 탭(견적 대기·진행 중·완료·취소) · 요청 카드(서비스명·요청일·받은 견적 수·선택한 고수·다음 할 일) · 상태별 강조(견적 마감 임박·후기 미작성) · 검색과 기간 필터 · 요청 다시 보내기 · 비어 있을 때 안내
- **버튼 → 이동화면**:
  - `요청 상세` → `MY-02` 요청 상세 - 진행 상황
  - `비어 있음` → `MY-03` 내 요청 - 비어 있음
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `상태 탭(견적 대기·진행 중·완료·취소)` → 목록과 탭 옆 숫자가 함께 바뀐다
  - `기간 필터` → 목록이 그 기간 것으로 걸러진다
- **생성 프롬프트**:

  ```
  내 요청 목록 화면을 만들어줘. 상단에 상태 탭(견적 대기 2· 진행 중 1· 완료 5· 취소 1)과 검색·기간 필터. 요청 카드마다 서비스명, 요청한 날, 상태 배지, 그리고 상태에 따라 다른 정보 — 견적 대기면 "견적 3개 도착· 마감까지 2일", 진행 중이면 선택한 고수와 방문 예정일, 완료면 결제 금액과 후기 작성 여부. 카드 오른쪽에 다음에 할 일 버튼(견적 비교 / 채팅 / 후기 쓰기).
  ```

### MY-02 · 요청 상세 - 진행 상황
- **소속 메뉴**: 내 요청
- **뒤로가기**: 좌측 상단에 `‹ 내 요청 목록` — 누르면 `MY-01`로 간다
- **일정**: 2026-11-11 ~ 2026-11-13
- **기능정의**: 요청서 내용 전체 · 진행 상태 타임라인(요청→견적 도착→고수 선택→일정 확정→작업 완료→결제→후기) · 선택한 고수 정보와 연락 · 받은 견적 다시 보기 · 일정 변경 요청 · 취소·환불 요청 · 영수증 · 문제 신고
- **버튼 → 이동화면**:
  - `채팅` → `CH-02` 채팅방
  - `받은 견적` → `QT-01` 견적 목록
  - `후기 작성` → `RV-01` 후기 작성
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `진행 단계 줄` → 그 단계에서 무엇을 하는지 펼쳐진다
  - `「완료 확인」` → 단계 표시가 다음으로 넘어가고 아래 할 일이 바뀐다
- **생성 프롬프트**:

  ```
  요청 상세 화면을 만들어줘. 상단에 서비스명과 상태 배지, 요청 번호. 진행 상태를 세로 타임라인으로 — 요청 접수 → 견적 도착(5개) → 고수 선택 → 일정 확정 → 작업 완료 → 결제 → 후기. 지난 단계는 체크와 날짜, 현재 단계는 강조. 그 아래 선택한 고수 카드(사진·활동명·연락 버튼·채팅 버튼), 요청서에 쓴 내용 전체, 받은 견적 다시 보기 링크. 오른쪽에 액션 패널 — 일정 변경 요청, 취소·환불 요청, 영수증, 문제 신고.
  ```

### MY-03 · 내 요청 - 비어 있음
- **소속 메뉴**: 내 요청
- **뒤로가기**: 좌측 상단에 `‹ 내 요청 목록` — 누르면 `MY-01`로 간다
- **일정**: 2026-11-16 ~ 2026-11-17
- **기능정의**: 요청 내역이 없다는 안내와 일러스트 · 요청서 작성 유도 버튼 · 3단계 이용 안내 요약 · 첫 이용 할인 쿠폰 안내 · 인기 서비스 카테고리 · 고수 둘러보기 링크
- **버튼 → 이동화면**:
  - `요청서 작성` → `RQ-01` 서비스 고르기
  - `고수 찾기` → `SE-01` 고수 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「고수 둘러보기」` → 빈 화면 대신 추천 고수 목록이 그 자리에 나타난다
- **생성 프롬프트**:

  ```
  요청 내역이 없을 때의 화면을 만들어줘. 큰 아이콘과 "아직 보낸 요청이 없어요" 문구, "요청서 한 장이면 여러 고수가 값을 보내드려요" 설명, "요청서 작성하기" 큰 버튼과 "고수 둘러보기" 보조 버튼. 아래에 3단계 이용 안내를 가로 3칸으로 요약하고, 첫 이용 할인 쿠폰 카드와 인기 서비스 카테고리 그리드를 넣어줘.
  ```

### RV-01 · 후기 작성
- **소속 메뉴**: 후기
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-11-18 ~ 2026-11-20
- **기능정의**: 별점 선택(전체 만족도) · 항목별 별점(친절·시간 약속·마무리·가격) · 사진 첨부 · 후기 본문 · 다시 맡길 의향 · 비공개 피드백(고수에게만 보임) · 후기 작성 포인트 안내 · 익명으로 올리기 · 등록
- **버튼 → 이동화면**:
  - `내 요청` → `MY-01` 내 요청 목록
  - `내 후기` → `RV-02` 내 후기 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `별점` → 누른 별까지 채워지고 안내 문구가 바뀐다
  - `항목별 만족도(친절·실력·시간 약속)` → 고른 값이 남고 요약 점수가 다시 계산된다
  - `사진 올리기·빼기` → 미리보기가 늘고 줄며 「N/5장」이 따라 바뀐다
- **생성 프롬프트**:

  ```
  후기 작성 화면을 만들어줘. 상단에 이용 내역 요약(고수 사진·활동명·서비스명·이용일). 전체 만족도 별점을 크게 두고, 그 아래 항목별 별점 4개 (친절했나요 / 시간을 지켰나요 / 마무리가 깔끔했나요 / 값이 적절했나요). 사진 첨부 영역(드래그 앤 드롭, 최대 5장), 후기 본문 입력, "다시 맡기시겠어요?" 칩 3개, 그리고 "고수에게만 보이는 비공개 피드백" 입력칸. 익명으로 올리기 체크박스와 "후기를 남기면 1,000 포인트" 안내, 등록 버튼.
  ```

### RV-02 · 내 후기 목록
- **소속 메뉴**: 후기
- **뒤로가기**: 좌측 상단에 `‹ 후기 작성` — 누르면 `RV-01`로 간다
- **일정**: 2026-11-23 ~ 2026-11-25
- **기능정의**: 내가 쓴 후기 목록(고수·서비스·별점·작성일) · 고수 답글 표시 · 후기 수정·삭제(작성 후 30일 이내) · 아직 안 쓴 후기 안내와 바로 쓰기 · 받은 도움돼요 수 · 후기 신고 처리 상태
- **버튼 → 이동화면**:
  - `후기 작성` → `RV-01` 후기 작성
  - `고수 상세` → `SE-03` 고수 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「수정」` → 그 줄이 입력 칸으로 바뀌고 저장하면 되돌아온다
  - `「삭제」` → 확인을 한 번 물은 뒤 그 줄이 목록에서 사라진다
- **생성 프롬프트**:

  ```
  내가 쓴 후기 목록 화면을 만들어줘. 상단에 탭(작성한 후기 5· 작성 안 함 2)과 요약(평균 별점, 받은 도움돼요 수). 후기 카드마다 고수 사진·활동명·서비스명·이용일, 내가 준 별점과 본문, 첨부 사진. 고수가 답글을 달았으면 들여쓴 박스로 보여주고, 카드 오른쪽에 수정·삭제(30일 이내만) 버튼. "작성 안 함" 탭에는 아직 후기를 안 쓴 이용 건과 "후기 쓰기" 버튼.
  ```

### PR-01 · 고수센터 홈
- **소속 메뉴**: 고수센터
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-11-26 ~ 2026-11-30
- **기능정의**: 핵심 지표 4종(새 요청·보낸 견적·성사율·이번 달 수입) · 오늘 할 일(견적 보낼 요청·답장 안 한 채팅·오늘 방문 일정) · 남은 크레딧과 충전 유도 · 최근 30일 성사 추이 차트 · 내 순위와 노출 상태 · 최근 후기 · 응답률 경고(떨어지면 노출이 줄어든다) · 빠른 실행 버튼
- **버튼 → 이동화면**:
  - `새 요청` → `LD-01` 새 요청 목록
  - `진행 중 일감` → `JB-01` 일감 목록
  - `크레딧 충전` → `LD-04` 크레딧 충전
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `기간 탭(오늘·이번 주·이번 달)` → 지표 숫자와 추이 차트가 그 기간 것으로 바뀐다
  - `「처리할 일」 줄의 처리 버튼` → 그 줄이 목록에서 빠지고 위의 건수가 줄어든다
  - `「활동 중」 토글` → 끄면 새 요청이 안 오고 상단 배지가 「휴식 중」으로 바뀐다
- **생성 프롬프트**:

  ```
  고수가 로그인했을 때 보는 대시보드를 만들어줘. 1) 상단 지표 카드 4개 — 새 요청 12건, 보낸 견적 8건, 성사율 34%, 이번 달 수입 2) "오늘 할 일" 목록 — 견적 보낼 요청 5건, 답장 안 한 채팅 2건, 오늘 방문 1건 3) 남은 크레딧 카드와 충전 버튼(크레딧이 적으면 경고 색) 4) 최근 30일 성사 추이 라인 차트 5) 내 노출 상태 — "지금 강남구 이사 분야에서 4위" 와 순위를 올리는 방법 링크 6) 응답률이 기준 아래로 떨어지면 뜨는 경고 박스 7) 최근 받은 후기 3건 좌측 세로 사이드바로 이동하고, 지표부터 눈에 들어오게 해줘.
  ```

### PR-02 · 고수 등록 신청
- **소속 메뉴**: 고수센터
- **뒤로가기**: 좌측 상단에 `‹ 고수센터 홈` — 누르면 `PR-01`로 간다
- **일정**: 2026-12-01 ~ 2026-12-04
- **기능정의**: 고수가 하는 일과 수수료 구조 안내 · 활동명·소개·전문 분야 선택 · 경력과 자격 증빙 업로드 · 사업자 여부와 사업자등록증 · 신원 확인(휴대폰·신분증) · 활동 지역 설정 · 정산 계좌 등록 · 고수 이용약관 동의 · 심사 소요 기간 안내 · 임시 저장
- **버튼 → 이동화면**:
  - `심사 현황` → `PR-03` 고수 등록 - 심사 중
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `활동 분야 고르기(복수)` → 고른 것이 칩으로 쌓이고 다시 누르면 빠진다
  - `증빙 파일 올리기` → 파일 줄이 늘어나고 「N/5개」가 올라간다
  - `필수 칸이 빈 상태` → 「제출」이 눌리지 않고 어느 칸이 비었는지 표시된다
- **생성 프롬프트**:

  ```
  손님이 고수가 되겠다고 신청하는 화면을 만들어줘. 상단에 안내 카드 3개 — 무엇을 보나 / 얼마나 걸리나 / 어떻게 버나(수수료 구조). 활동명, 한 줄 소개, 전문 분야 선택(복수), 경력 입력과 자격·경력 증빙 파일 업로드, 사업자 여부 라디오와 사업자등록증 업로드, 신원 확인 영역(휴대폰 인증 완료 배지· 신분증 업로드), 활동 지역 선택(지도에서 구 단위로 고르기), 정산 계좌 등록(은행·계좌번호·예금주), 고수 이용약관 동의 체크박스. 오른쪽에 작성 현황 카드(항목별 완료 여부와 진행률), 임시 저장과 제출 버튼.
  ```

### PR-03 · 고수 등록 - 심사 중
- **소속 메뉴**: 고수센터
- **뒤로가기**: 좌측 상단에 `‹ 고수센터 홈` — 누르면 `PR-01`로 간다
- **일정**: 2026-12-07 ~ 2026-12-08
- **기능정의**: 심사 단계 표시(접수·서류 확인·신원 확인·승인) · 접수 일시와 예상 완료일 · 제출 내용 다시 보기 · 보완 요청 안내와 재제출 · 심사 중 활동 불가 안내 · 승인되면 무엇이 열리는지 · 신청 취소
- **버튼 → 이동화면**:
  - `고수센터` → `PR-01` 고수센터 홈
  - `다시 제출` → `PR-02` 고수 등록 신청
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「심사 단계」` → 각 단계에서 무엇을 보는지 펼쳐진다
- **생성 프롬프트**:

  ```
  고수 등록 심사 중 화면을 만들어줘. 가운데에 진행 단계 4개(접수 완료 → 서류 확인 → 신원 확인 → 승인), 현재 단계를 강조하고 접수 일시와 예상 완료일. 보완 요청이 온 상태도 만들어줘 — 주황 안내 박스에 요청 사유와 "다시 제출하기" 버튼. 아래에 제출한 내용 요약과 "지원서 다시 보기", "승인되면 이런 것이 열려요" 목록(요청 받기·견적 보내기·프로필 노출), 맨 아래 "신청 취소" 링크.
  ```

### PR-04 · 활동 설정
- **소속 메뉴**: 고수센터
- **뒤로가기**: 좌측 상단에 `‹ 고수센터 홈` — 누르면 `PR-01`로 간다
- **일정**: 2026-12-09 ~ 2026-12-11
- **기능정의**: 제공 서비스와 항목별 시작가 · 활동 지역(구 단위 다중 선택·출장 가능 거리) · 요청 받을 조건(예산 하한·최소 규모) · 응답 가능 시간과 휴무일 · 자동 응답 문구 · 휴식 모드(요청 받지 않기) · 저장 시 노출에 미치는 영향 안내
- **버튼 → 이동화면**:
  - `고수센터` → `PR-01` 고수센터 홈
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `활동 지역 고르기` → 고른 지역이 지도에 칠해지고 「예상 요청 수」가 다시 계산된다
  - `받고 싶은 금액대 슬라이더` → 예상 요청 수가 따라 바뀐다
  - `요일·시간 켜고 끄기` → 가능한 시간표가 칠해지고 지워진다
- **생성 프롬프트**:

  ```
  고수의 활동 설정 화면을 만들어줘. 1) 제공 서비스 — 항목마다 서비스명과 시작가를 넣는 줄, 추가·삭제 가능 2) 활동 지역 — 지도 자리 옆에 구 단위 체크박스 목록, 출장 가능 거리 슬라이더 3) 요청 받을 조건 — 예산 하한, 최소 규모, 받지 않을 유형 4) 응답 가능 시간 — 요일별 시작·종료 시각, 휴무일 지정 5) 자동 응답 문구 — 요청이 오면 자동으로 보낼 첫 인사 6) 휴식 모드 토글 — 켜면 새 요청을 받지 않는다는 경고와 함께 각 항목 아래에 "이 설정을 바꾸면 노출이 어떻게 달라지는지" 짧은 안내.
  ```

### PR-05 · 프로필·포트폴리오 관리
- **소속 메뉴**: 고수센터
- **뒤로가기**: 좌측 상단에 `‹ 고수센터 홈` — 누르면 `PR-01`로 간다
- **일정**: 2026-12-14 ~ 2026-12-17
- **기능정의**: 프로필 사진과 활동명·한 줄 소개 · 자기소개 상세 편집 · 포트폴리오 사진 업로드와 순서 변경 · 작업별 설명·기간·비용대 입력 · 자격증·인증 배지 관리 · 손님이 보는 화면 미리보기 · 프로필 완성도와 채우면 좋은 항목 안내
- **버튼 → 이동화면**:
  - `고수 상세` → `SE-03` 고수 상세
  - `고수센터` → `PR-01` 고수센터 홈
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `포트폴리오 순서 바꾸기` → 순서가 실제로 바뀌고 미리보기가 따라 바뀐다
  - `사진 올리기·빼기` → 장수가 늘고 준다
  - `「대표 사진으로」` → 그 사진이 맨 앞으로 오고 배지가 옮겨 붙는다
- **생성 프롬프트**:

  ```
  고수 프로필·포트폴리오 관리 화면을 만들어줘. 왼쪽 본문에 프로필 사진 업로드, 활동명, 한 줄 소개, 자기소개 리치 텍스트, 그 아래 포트폴리오 영역 — 사진 카드를 격자로 놓고 끌어서 순서를 바꾸며, 카드마다 작업명·기간·비용대를 입력하고 삭제할 수 있게. 자격증·인증 배지 관리 목록(승인됨/심사 중/반려). 오른쪽에 "프로필 완성도 72%" 카드와 채우면 좋은 항목 체크리스트, "손님이 보는 화면 미리보기" 버튼.
  ```

### LD-01 · 새 요청 목록
- **소속 메뉴**: 요청 받기
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-12-18 ~ 2026-12-22
- **기능정의**: 조건에 맞는 새 요청 카드(서비스·지역·희망일·예산대·요청 시각) · 남은 견적 자리 표시(선착순 마감) · 필터(분야·지역·예산·거리) · 정렬(최신순/마감 임박순/예산 높은순) · 견적 보내기 크레딧 비용 표시 · 관심 없음 처리 · 새 요청 실시간 표시
- **버튼 → 이동화면**:
  - `요청 상세` → `LD-02` 요청 상세
  - `견적 보내기` → `LD-03` 견적 보내기
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `분야·지역·금액대 필터` → 요청 목록과 「N건」 숫자가 함께 걸러진다
  - `정렬(최신·마감 임박·금액)` → 목록 순서가 다시 늘어선다
  - `「내 활동 지역만」 토글` → 목록이 걸러진다
- **생성 프롬프트**:

  ```
  고수에게 들어온 새 요청 목록 화면을 만들어줘. 상단에 필터 줄(분야·지역·예산대·거리)과 정렬, 그리고 "남은 크레딧 42". 요청 카드마다 서비스명, 지역, 희망일, 예산대, 몇 분 전에 올라왔는지, 그리고 "견적 자리 2 / 5 남음" 배지(선착순이라 급하다는 느낌). 카드 오른쪽에 "견적 보내기 (3크레딧)" 버튼과 "관심 없음" 링크. 새 요청이 실시간으로 올라오는 느낌의 상단 알림 띠도 넣어줘.
  ```

### LD-02 · 요청 상세
- **소속 메뉴**: 요청 받기
- **뒤로가기**: 좌측 상단에 `‹ 새 요청 목록` — 누르면 `LD-01`로 간다
- **일정**: 2026-12-23 ~ 2026-12-25
- **기능정의**: 손님이 쓴 요청서 전문(질문과 답을 항목별로) · 첨부 사진 · 손님 정보(가입 기간·이용 횟수·평균 평점, 연락처는 가림) · 이 요청에 이미 보낸 고수 수 · 예상 경쟁 강도 · 비슷한 요청의 평균 견적가 참고 · 견적 보내기 · 관심 없음
- **버튼 → 이동화면**:
  - `견적 보내기` → `LD-03` 견적 보내기
  - `새 요청` → `LD-01` 새 요청 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `요청 내용 펼치기` → 고객이 답한 질문과 사진이 펼쳐진다
  - `「비슷한 요청 이력」` → 지난 비슷한 건의 낙찰 금액대가 펼쳐진다
- **생성 프롬프트**:

  ```
  고수가 보는 요청 상세 화면을 만들어줘. 상단에 서비스명과 "견적 자리 2 / 5 남음", 올라온 시각. 본문에 손님이 쓴 요청서를 "질문 — 답" 형태로 항목별로 정리해 보여주고, 첨부 사진이 있으면 격자로. 그 아래 손님 정보 카드 — 가입 기간, 이용 횟수, 평균 평점(연락처는 선택 전까지 가림). 오른쪽 패널에 참고 정보 — 이 지역 비슷한 요청의 평균 견적가, 지금까지 견적 보낸 고수 3명, "견적 보내기 (3크레딧)" 버튼.
  ```

### LD-03 · 견적 보내기
- **소속 메뉴**: 요청 받기
- **뒤로가기**: 좌측 상단에 `‹ 새 요청 목록` — 누르면 `LD-01`로 간다
- **일정**: 2026-12-28 ~ 2026-12-30
- **기능정의**: 견적 금액 입력과 금액 구성(기본료·추가 항목·출장비) · 포함/불포함 항목 체크 · 예상 소요 시간 · 가능한 일정 제안(여러 날짜) · 제안 메시지 작성과 템플릿 불러오기 · 차감될 크레딧과 잔액 표시 · 미리보기 · 보내기
- **버튼 → 이동화면**:
  - `보낸 견적` → `JB-01` 일감 목록
  - `크레딧 충전` → `LD-04` 크레딧 충전
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `금액 입력` → 차감될 크레딧과 아래 「고객에게 보이는 모습」이 따라 바뀐다
  - `포함 항목 체크` → 견적 요약과 금액이 다시 계산된다
  - `크레딧이 모자란 상태` → 「보내기」가 잠기고 충전 안내가 뜬다
- **생성 프롬프트**:

  ```
  고수가 견적을 보내는 화면을 만들어줘. 왼쪽 본문 — 견적 금액 입력, 금액 구성(기본료·추가 항목 추가 가능·출장비), "포함되는 것 / 포함되지 않는 것" 체크박스 목록, 예상 소요 시간, 가능한 일정 제안(날짜를 여러 개 고를 수 있게), 제안 메시지 입력칸과 "자주 쓰는 문구 불러오기" 버튼. 오른쪽에 미리보기 카드 — 손님에게 이렇게 보인다는 것을 그대로 보여주고, 그 아래 "3크레딧이 차감돼요 (잔액 42 → 39)" 안내와 "견적 보내기" 버튼.
  ```

### LD-04 · 크레딧 충전
- **소속 메뉴**: 요청 받기
- **뒤로가기**: 좌측 상단에 `‹ 새 요청 목록` — 누르면 `LD-01`로 간다
- **일정**: 2026-12-31 ~ 2027-01-04
- **기능정의**: 현재 잔액과 이번 달 사용량 · 충전 상품 목록(수량별 할인) · 분야별 견적 1건당 크레딧 안내 · 자동 충전 설정 · 결제 수단 선택 · 사용 내역(차감·충전·환불) · 크레딧 유효기간 안내 · 환불 규정
- **버튼 → 이동화면**:
  - `고수센터` → `PR-01` 고수센터 홈
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `충전 금액 고르기` → 받는 크레딧과 보너스가 다시 계산된다
  - `결제 수단 고르기` → 표시가 옮겨 가고 카드면 할부 칸이 나타난다
- **생성 프롬프트**:

  ```
  고수의 크레딧 충전 화면을 만들어줘. 상단에 잔액 카드(남은 크레딧 42, 이번 달 사용 58)와 사용 추이 작은 차트. 충전 상품을 카드 4개로 — 50 / 100 / 300 / 500 크레딧, 많이 살수록 할인율 표시, 가운데 것에 "가장 많이 골라요" 배지. "잔액이 10 아래로 내려가면 자동으로 충전" 토글, 결제 수단 선택, 아래에 사용 내역 표(날짜·내용·증감·잔액). 옆에 분야별 견적 1건당 크레딧 안내 표와 유효기간·환불 규정.
  ```

### JB-01 · 일감 목록
- **소속 메뉴**: 진행 관리
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2027-01-05 ~ 2027-01-07
- **기능정의**: 상태 탭(보낸 견적·성사·진행 중·완료) · 일감 카드(손님·서비스·일정·금액·다음 할 일) · 오늘·이번 주 일정 강조 · 견적 마감 임박 표시 · 검색과 기간 필터 · 성사율과 평균 응답 시간 요약 · 비어 있을 때 안내
- **버튼 → 이동화면**:
  - `일감 상세` → `JB-02` 일감 상세
  - `정산` → `JB-03` 정산 내역
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `상태 탭(예정·진행 중·완료)` → 목록과 탭 옆 숫자가 함께 바뀐다
  - `일감 줄의 상태 바꾸기` → 배지가 바뀌고 위의 건수가 따라 바뀐다
- **생성 프롬프트**:

  ```
  고수의 일감 목록 화면을 만들어줘. 상단에 요약 지표 3개(성사율, 평균 응답 시간, 이번 주 방문 건수)와 상태 탭(보낸 견적 8· 성사 3· 진행 중 2· 완료 41). 일감 카드마다 손님 이름(가림 처리), 서비스명, 확정 일정, 금액, 상태에 따른 다음 할 일 버튼(답장하기 / 일정 확정 / 완료 처리 / 정산 확인). 오늘·이번 주 일정은 색으로 강조하고, 견적 마감이 임박한 건에는 배지.
  ```

### JB-02 · 일감 상세
- **소속 메뉴**: 진행 관리
- **뒤로가기**: 좌측 상단에 `‹ 일감 목록` — 누르면 `JB-01`로 간다
- **일정**: 2027-01-08 ~ 2027-01-12
- **기능정의**: 손님 요청 내용과 내가 보낸 견적 · 확정된 일정과 장소 · 진행 상태 타임라인 · 손님 연락처(성사 후 공개) · 채팅 바로가기 · 일정 변경 요청 · 추가 금액 요청(사유와 함께) · 완료 처리 · 취소 요청과 페널티 안내
- **버튼 → 이동화면**:
  - `채팅` → `CH-02` 채팅방
  - `일감 목록` → `JB-01` 일감 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `진행 단계 넘기기` → 단계 표시가 옮겨 가고 아래 할 일이 바뀐다
  - `「메모 남기기」` → 적는 칸이 펼쳐지고 저장하면 메모가 줄로 쌓인다
- **생성 프롬프트**:

  ```
  고수가 보는 일감 상세 화면을 만들어줘. 상단에 서비스명과 상태 배지, 확정 일정과 장소. 왼쪽 본문에 손님 요청 내용, 내가 보낸 견적 내역, 진행 상태 타임라인 (견적 발송 → 선택됨 → 일정 확정 → 방문 → 완료 → 정산). 오른쪽 액션 패널 — 손님 연락처(성사 후 공개된 상태), 채팅하기, 일정 변경 요청, 추가 금액 요청(사유 입력), "작업 완료 처리" 큰 버튼, 맨 아래 "취소 요청"과 취소 시 페널티 안내.
  ```

### JB-03 · 정산 내역
- **소속 메뉴**: 진행 관리
- **뒤로가기**: 좌측 상단에 `‹ 일감 목록` — 누르면 `JB-01`로 간다
- **일정**: 2027-01-13 ~ 2027-01-15
- **기능정의**: 정산 요약(정산 예정·완료·보류) · 건별 정산 표(일감·결제액·수수료·순정산액·상태·지급일) · 정산 주기 안내 · 기간 필터 · 정산 계좌 관리 · 세금계산서·명세서 내려받기 · 수수료율과 등급 안내 · 내역이 없을 때 안내
- **버튼 → 이동화면**:
  - `고수센터` → `PR-01` 고수센터 홈
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `기간 고르기` → 표와 위의 합계 금액이 그 기간 것으로 다시 계산된다
  - `표 머리글(날짜·금액)` → 그 기준으로 정렬되고 화살표 방향이 바뀐다
  - `정산 줄 펼치기` → 건별 내역과 수수료 계산이 펼쳐진다
- **생성 프롬프트**:

  ```
  고수의 정산 내역 화면을 만들어줘. 상단에 요약 카드 4개(정산 예정, 이번 달 정산 완료, 보류 금액, 내 수수료율). 아래 표에 건별 정산 — 일감명, 완료일, 결제액, 수수료, 순정산액, 상태 배지, 지급일. 표 아래 합계 행. 오른쪽 또는 아래에 정산 안내 — 정산 주기(완료 확인 후 5영업일), 수수료율이 등급에 따라 어떻게 달라지는지 표, 정산 계좌 관리와 세금계산서 발행 버튼.
  ```

### AU-01 · 로그인
- **소속 메뉴**: 계정
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2027-01-18 ~ 2027-01-20
- **기능정의**: 이메일·비밀번호 입력 · 소셜 로그인 3종(카카오·네이버·구글) · 로그인 상태 유지 · 비밀번호 찾기 · 회원가입 링크 · 입력 오류 안내 · 로그인 후 원래 보던 화면으로 · 고수 로그인도 같은 계정이라는 안내
- **버튼 → 이동화면**:
  - `회원가입` → `AU-02` 회원가입
  - `비밀번호 찾기` → `AU-05` 비밀번호 찾기
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「비밀번호 보기」 눈 아이콘` → 가려진 글자가 보이고 다시 누르면 가려진다
  - `빈 칸으로 로그인 시도` → 어느 칸이 비었는지 칸 아래에 안내가 뜬다
- **생성 프롬프트**:

  ```
  로그인 화면을 만들어줘. 헤더·GNB 없이 가운데 정렬된 단독 화면으로. 로고 아래 이메일과 비밀번호 입력칸, "로그인 상태 유지" 체크박스, 큰 로그인 버튼, 그 아래 비밀번호 찾기 링크. 구분선 아래 소셜 로그인 버튼 3개(카카오 노란색, 네이버 초록색, 구글 흰색)를 세로로, 맨 아래 "아직 회원이 아니신가요? 회원가입" 링크. 입력이 잘못됐을 때 뜨는 안내 문구 자리와, "고수도 같은 계정으로 로그인해요" 작은 안내를 넣어줘.
  ```

### AU-02 · 회원가입
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `AU-01`로 간다
- **일정**: 2027-01-21 ~ 2027-01-25
- **기능정의**: 이메일·비밀번호·비밀번호 확인 · 비밀번호 조건 실시간 표시 · 이름과 휴대폰 번호 · 활동 지역 선택 · 관심 서비스 선택 · 약관 동의(전체·필수 2종·선택 1종) · 소셜 간편 가입 · 이미 가입된 이메일 안내
- **버튼 → 이동화면**:
  - `다음` → `AU-03` 회원가입 - 휴대폰 인증
  - `로그인` → `AU-01` 로그인
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `비밀번호 입력` → 치는 대로 세기 막대가 약함→보통→강함으로 바뀐다
  - `비밀번호 확인 칸` → 위와 다르면 그 자리에서 안내가 뜬다
  - `약관 「전체 동의」` → 개별 체크가 한꺼번에 켜지고, 하나를 끄면 전체 동의가 풀린다
- **생성 프롬프트**:

  ```
  회원가입 화면을 만들어줘. 헤더 없이 단독 화면으로. 상단에 진행 표시(1 정보 입력 → 2 인증 → 3 완료). 이메일, 비밀번호, 비밀번호 확인 입력칸을 두고 비밀번호 조건(8자 이상·영문+숫자)을 입력하는 동안 체크 표시로 알려줘. 이름과 휴대폰 번호, 활동 지역 선택(시·구), 관심 서비스를 칩으로 고르는 영역(복수 선택), 약관 동의는 "전체 동의" 하나와 그 아래 이용약관(필수)·개인정보 처리방침(필수)· 마케팅 수신(선택)을 각각 체크박스로, 오른쪽에 전문 보기 링크. 하단에 다음 버튼, 위쪽에 소셜 간편 가입 버튼 3개.
  ```

### AU-03 · 회원가입 - 휴대폰 인증
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `AU-01`로 간다
- **일정**: 2027-01-26 ~ 2027-01-28
- **기능정의**: 휴대폰 번호 확인 표시 · 인증번호 6자리 입력 · 남은 시간 카운트다운 · 재발송 버튼과 대기 시간 · 번호 바꾸기 · 인증번호 불일치 안내 · 왜 인증이 필요한지 안내 · 인증 완료 시 다음 단계
- **버튼 → 이동화면**:
  - `다음` → `AU-04` 회원가입 완료
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「인증번호 받기」` → 입력 칸이 열리고 3분 타이머가 줄어들기 시작한다
  - `타이머가 0이 됨` → 칸이 잠기고 「다시 받기」가 켜진다
  - `인증번호 6자리를 다 넣음` → 「확인」이 눌리게 켜진다
- **생성 프롬프트**:

  ```
  회원가입 중 휴대폰 인증 단계 화면을 만들어줘. 진행 표시에서 2단계를 강조하고, "010-1234-5678로 인증번호를 보냈어요" 문구 아래 6칸짜리 인증번호 입력칸을 크게 두고, 오른쪽에 남은 시간(03:00 카운트다운), 아래에 "인증번호 다시 받기"(재발송 대기 중이면 남은 초 표시)와 "번호 바꾸기" 링크. 인증번호가 틀렸을 때 입력칸이 빨갛게 바뀌고 안내가 뜨는 상태도 만들어줘. 맨 아래 "왜 인증이 필요한가요?" — 실제 사람끼리 만나는 서비스라 필요하다는 안내.
  ```

### AU-04 · 회원가입 완료
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `AU-01`로 간다
- **일정**: 2027-01-29 ~ 2027-02-01
- **기능정의**: 가입 축하 문구와 이름 표시 · 첫 이용 할인 쿠폰 지급 안내 · 관심 서비스 기반 추천 고수 3명 · 바로 요청서 작성 버튼 · 고수 둘러보기 · 고수로 활동하기 안내 링크
- **버튼 → 이동화면**:
  - `요청서 작성` → `RQ-01` 서비스 고르기
  - `고수 등록` → `PR-02` 고수 등록 신청
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「관심 분야 고르기」` → 고른 것이 칩으로 쌓이고 아래 추천 고수가 그에 맞게 바뀐다
- **생성 프롬프트**:

  ```
  회원가입 완료 화면을 만들어줘. 헤더 없이 단독 화면으로. 큰 체크 아이콘과 "가입을 환영해요, 김하늘님" 문구, 아래에 "첫 이용 10,000원 할인 쿠폰을 드렸어요" 쿠폰 카드, "이런 고수는 어때요?" 추천 고수 3명(가입할 때 고른 관심 서비스 기준), 큰 "요청서 작성하기" 버튼과 그 아래 "고수 둘러보기" 링크, 맨 아래 옅게 "직접 일을 맡고 싶으세요? 고수로 등록하기" 링크.
  ```

### AU-05 · 비밀번호 찾기
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `AU-01`로 간다
- **일정**: 2027-02-02 ~ 2027-02-03
- **기능정의**: 가입한 이메일 입력 · 재설정 링크 보내기 · 보냈다는 안내와 재발송 · 등록되지 않은 이메일 안내 · 소셜 가입자 안내 · 새 비밀번호 입력과 조건 표시 · 재설정 완료 후 로그인 이동
- **버튼 → 이동화면**:
  - `로그인` → `AU-01` 로그인
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `찾는 방법 고르기(문자/이메일)` → 아래 입력 칸이 그 방법에 맞게 바뀐다
  - `형식에 안 맞는 값 입력` → 칸 아래에 무엇이 틀렸는지 뜬다
- **생성 프롬프트**:

  ```
  비밀번호 찾기 화면을 만들어줘. 헤더 없이 단독 화면으로. "가입하신 이메일을 알려주세요" 문구와 이메일 입력칸, "재설정 링크 보내기" 버튼. 보낸 뒤 상태(메일 아이콘 + "메일함을 확인해 주세요" + "다시 보내기" 링크)와 등록되지 않은 이메일일 때 안내, 소셜로 가입한 경우 안내도 같이 만들어줘. 링크를 눌러 들어온 새 비밀번호 입력 화면도 — 새 비밀번호, 확인 입력칸과 조건 체크 표시, "비밀번호 바꾸기" 버튼.
  ```

## 5. 화면 이동 흐름 (FLOW)
- `홈 - 비로그인`(HO-01) —[요청서 작성]→ `서비스 고르기`(RQ-01)
- `홈 - 비로그인`(HO-01) —[고수 찾기]→ `고수 목록`(SE-01)
- `홈 - 비로그인`(HO-01) —[로그인]→ `로그인`(AU-01)
- `홈 - 손님 로그인`(HO-02) —[견적 비교]→ `견적 비교`(QT-02)
- `홈 - 손님 로그인`(HO-02) —[채팅]→ `채팅 목록`(CH-01)
- `홈 - 손님 로그인`(HO-02) —[내 요청]→ `내 요청 목록`(MY-01)
- `고수 목록`(SE-01) —[고수 상세]→ `고수 상세`(SE-03)
- `고수 목록`(SE-01) —[결과 없음]→ `고수 목록 - 결과 없음`(SE-02)
- `고수 목록`(SE-01) —[요청서 작성]→ `서비스 고르기`(RQ-01)
- `고수 목록 - 결과 없음`(SE-02) —[요청서 작성]→ `서비스 고르기`(RQ-01)
- `고수 목록 - 결과 없음`(SE-02) —[고수 목록]→ `고수 목록`(SE-01)
- `고수 상세`(SE-03) —[포트폴리오]→ `고수 포트폴리오`(SE-04)
- `고수 상세`(SE-03) —[요청서 작성]→ `서비스 고르기`(RQ-01)
- `고수 상세`(SE-03) —[채팅 문의]→ `채팅방`(CH-02)
- `고수 포트폴리오`(SE-04) —[고수 상세]→ `고수 상세`(SE-03)
- `고수 포트폴리오`(SE-04) —[요청서 작성]→ `서비스 고르기`(RQ-01)
- `찜한 고수`(SE-05) —[고수 상세]→ `고수 상세`(SE-03)
- `찜한 고수`(SE-05) —[요청서 작성]→ `서비스 고르기`(RQ-01)
- `서비스 고르기`(RQ-01) —[다음]→ `요청서 작성 - 단계별 질문`(RQ-02)
- `요청서 작성 - 단계별 질문`(RQ-02) —[다음]→ `요청서 확인`(RQ-03)
- `요청서 작성 - 단계별 질문`(RQ-02) —[이전]→ `서비스 고르기`(RQ-01)
- `요청서 확인`(RQ-03) —[보내기]→ `요청 완료`(RQ-04)
- `요청서 확인`(RQ-03) —[고치기]→ `요청서 작성 - 단계별 질문`(RQ-02)
- `요청 완료`(RQ-04) —[내 요청]→ `내 요청 목록`(MY-01)
- `요청 완료`(RQ-04) —[받은 견적]→ `견적 목록`(QT-01)
- `요청 수정·취소`(RQ-05) —[내 요청]→ `내 요청 목록`(MY-01)
- `요청 수정·취소`(RQ-05) —[받은 견적]→ `견적 목록`(QT-01)
- `견적 목록`(QT-01) —[견적 비교]→ `견적 비교`(QT-02)
- `견적 목록`(QT-01) —[견적 상세]→ `견적 상세`(QT-03)
- `견적 비교`(QT-02) —[견적 상세]→ `견적 상세`(QT-03)
- `견적 비교`(QT-02) —[고수 선택]→ `고수 선택 - 확인`(QT-04)
- `견적 상세`(QT-03) —[고수 선택]→ `고수 선택 - 확인`(QT-04)
- `견적 상세`(QT-03) —[채팅 문의]→ `채팅방`(CH-02)
- `견적 상세`(QT-03) —[고수 상세]→ `고수 상세`(SE-03)
- `고수 선택 - 확인`(QT-04) —[채팅]→ `채팅방`(CH-02)
- `고수 선택 - 확인`(QT-04) —[내 요청]→ `내 요청 목록`(MY-01)
- `채팅 목록`(CH-01) —[채팅방]→ `채팅방`(CH-02)
- `채팅방`(CH-02) —[채팅 목록]→ `채팅 목록`(CH-01)
- `채팅방`(CH-02) —[견적 상세]→ `견적 상세`(QT-03)
- `채팅방`(CH-02) —[결제]→ `결제`(PY-01)
- `결제`(PY-01) —[결제 완료]→ `결제 완료`(PY-02)
- `결제`(PY-01) —[결제 실패]→ `결제 실패`(PY-03)
- `결제 완료`(PY-02) —[후기 작성]→ `후기 작성`(RV-01)
- `결제 완료`(PY-02) —[내 요청]→ `내 요청 목록`(MY-01)
- `결제 실패`(PY-03) —[다시 시도]→ `결제`(PY-01)
- `결제 실패`(PY-03) —[채팅]→ `채팅방`(CH-02)
- `내 요청 목록`(MY-01) —[요청 상세]→ `요청 상세 - 진행 상황`(MY-02)
- `내 요청 목록`(MY-01) —[비어 있음]→ `내 요청 - 비어 있음`(MY-03)
- `요청 상세 - 진행 상황`(MY-02) —[채팅]→ `채팅방`(CH-02)
- `요청 상세 - 진행 상황`(MY-02) —[받은 견적]→ `견적 목록`(QT-01)
- `요청 상세 - 진행 상황`(MY-02) —[후기 작성]→ `후기 작성`(RV-01)
- `내 요청 - 비어 있음`(MY-03) —[요청서 작성]→ `서비스 고르기`(RQ-01)
- `내 요청 - 비어 있음`(MY-03) —[고수 찾기]→ `고수 목록`(SE-01)
- `후기 작성`(RV-01) —[내 요청]→ `내 요청 목록`(MY-01)
- `후기 작성`(RV-01) —[내 후기]→ `내 후기 목록`(RV-02)
- `내 후기 목록`(RV-02) —[후기 작성]→ `후기 작성`(RV-01)
- `내 후기 목록`(RV-02) —[고수 상세]→ `고수 상세`(SE-03)
- `고수센터 홈`(PR-01) —[새 요청]→ `새 요청 목록`(LD-01)
- `고수센터 홈`(PR-01) —[진행 중 일감]→ `일감 목록`(JB-01)
- `고수센터 홈`(PR-01) —[크레딧 충전]→ `크레딧 충전`(LD-04)
- `고수 등록 신청`(PR-02) —[심사 현황]→ `고수 등록 - 심사 중`(PR-03)
- `고수 등록 - 심사 중`(PR-03) —[고수센터]→ `고수센터 홈`(PR-01)
- `고수 등록 - 심사 중`(PR-03) —[다시 제출]→ `고수 등록 신청`(PR-02)
- `활동 설정`(PR-04) —[고수센터]→ `고수센터 홈`(PR-01)
- `프로필·포트폴리오 관리`(PR-05) —[고수 상세]→ `고수 상세`(SE-03)
- `프로필·포트폴리오 관리`(PR-05) —[고수센터]→ `고수센터 홈`(PR-01)
- `새 요청 목록`(LD-01) —[요청 상세]→ `요청 상세`(LD-02)
- `새 요청 목록`(LD-01) —[견적 보내기]→ `견적 보내기`(LD-03)
- `요청 상세`(LD-02) —[견적 보내기]→ `견적 보내기`(LD-03)
- `요청 상세`(LD-02) —[새 요청]→ `새 요청 목록`(LD-01)
- `견적 보내기`(LD-03) —[보낸 견적]→ `일감 목록`(JB-01)
- `견적 보내기`(LD-03) —[크레딧 충전]→ `크레딧 충전`(LD-04)
- `크레딧 충전`(LD-04) —[고수센터]→ `고수센터 홈`(PR-01)
- `일감 목록`(JB-01) —[일감 상세]→ `일감 상세`(JB-02)
- `일감 목록`(JB-01) —[정산]→ `정산 내역`(JB-03)
- `일감 상세`(JB-02) —[채팅]→ `채팅방`(CH-02)
- `일감 상세`(JB-02) —[일감 목록]→ `일감 목록`(JB-01)
- `정산 내역`(JB-03) —[고수센터]→ `고수센터 홈`(PR-01)
- `로그인`(AU-01) —[회원가입]→ `회원가입`(AU-02)
- `로그인`(AU-01) —[비밀번호 찾기]→ `비밀번호 찾기`(AU-05)
- `회원가입`(AU-02) —[다음]→ `회원가입 - 휴대폰 인증`(AU-03)
- `회원가입`(AU-02) —[로그인]→ `로그인`(AU-01)
- `회원가입 - 휴대폰 인증`(AU-03) —[다음]→ `회원가입 완료`(AU-04)
- `회원가입 완료`(AU-04) —[요청서 작성]→ `서비스 고르기`(RQ-01)
- `회원가입 완료`(AU-04) —[고수 등록]→ `고수 등록 신청`(PR-02)
- `비밀번호 찾기`(AU-05) —[로그인]→ `로그인`(AU-01)

## 6. 빌드 가이드
### 구조
- 각 **화면ID**를 하나의 라우트/페이지로 만드세요 (예: `PCHOME0110` → `/home`).
- 화면명은 페이지 제목·컴포넌트 이름의 기준으로 사용하세요.
- **2장 공통 요소**(헤더·GNB·푸터)를 공용 레이아웃으로 만들고 모든 화면이 감싸도록 하세요.
- **쓰는 사람이 다른 메뉴는 헤더를 나누세요.** 손님이 쓰는 메뉴(홈·고수 찾기·요청서·받은 견적)와 운영자가 쓰는 메뉴(고수센터·요청 받기·진행 관리)를 **한 헤더에 나란히 놓지 마세요.** 둘은 사실상 다른 사이트입니다.
  · 손님 영역과 운영자 영역을 각각 다른 레이아웃(헤더·사이드바)으로 만들고, 오갈 때는 **따로 마련한 진입점** 하나로만 이동하세요(예: 우측 상단 `고수센터 화면으로`, 운영자 화면에서는 `고객 화면 보기`).
  · 운영자 영역은 보통 좌측 사이드바 + 상단 바가 맞습니다. 손님 영역의 검색·카테고리 내비를 그대로 쓰지 마세요.
- **버튼 → 이동화면** 관계를 링크/네비게이션으로 연결하세요.
- **4장 각 화면의 `눌렀을 때 (이 화면 안에서 끝남)` 항목을 그대로 만드세요.** 적혀 있는 것은 빠짐없이 실제로 동작해야 합니다 — 누르면 값이 바뀌고, 다시 누르면 되돌아옵니다.
  · 그 항목에 없더라도 **누를 수 있게 생긴 것은 전부 눌리게** 만드세요. 적힌 것은 최소한이지 전부가 아닙니다.
- **누를 수 있게 보이는 것은 전부 눌리게 만드세요.** 두 종류로 나눠 처리합니다.
  · **화면 안에서 끝나는 조작은 진짜로 동작해야 합니다** — 탭 전환, 좌측 카테고리·필터 선택, 목록에서 항목 고르기(디자이너·옵션·시간), 펼치기·접기, 캘린더 이전/다음 달, 수량 ±, 체크·토글. 자바스크립트로 실제 상태를 바꾸고 화면을 다시 그리세요. 이걸 안내 문구로 때우지 마세요.
  · **서버가 있어야 하는 것만** 짧은 안내(토스트)로 대신합니다 — 저장·삭제·발송·결제·전화 걸기·이의 신청 등.
  · **이런 것은 토스트로 때우지 마세요** — `모두 보기`(펼치기), `선택`·`적용`(고른 상태로 바꾸기), `추가`(줄 하나 늘리기), `닫기`·`✕`(정말 닫기), `도움이 됐어요`(숫자 올리기), 탭·필터·정렬. 전부 화면 안에서 끝나는 일이라 실제로 되어야 합니다.
  · **모달·팝업은 뜨는 것만으로 끝이 아닙니다.** `취소`와 `✕`는 정말 닫히고, `확인`은 눌렀을 때 무엇이 달라지는지 보여야 합니다(다음 화면으로 가거나, 목록이 바뀌거나, 안내가 뜨거나). 닫히지 않는 팝업은 갇힌 화면이 됩니다.
  · 아무 것도 하지 않는 `<button>`을 남기지 마세요. 만들고 나서 **버튼을 하나씩 눌러 보고**, 반응이 없는 것이 있으면 위 둘 중 하나로 채우세요.
- **뒤로가기는 화면마다 정해져 있습니다. 4장 각 화면 스펙의 `뒤로가기` 항목을 그대로 만드세요.** 31개 화면에 붙고, 나머지 12개(메뉴 첫 화면)에는 없습니다. 좌측 상단에 `‹ 상위 화면 이름`과 현재 위치(예: `매장 탐색 › 매장 상세`)를 둡니다.
  · 여러 단계짜리 흐름(1→2→3)은 **이전 단계로 돌아갈 수 있게** 하세요. 되돌아갔을 때 앞서 고른 값이 남아 있어야 합니다.
  · **한 묶음으로 나란히 보여주는 것은 동일 계위입니다.** 탭 줄에 걸린 화면들, 같은 사이드바에 나란히 올라온 화면들이 그렇습니다. 그 안을 오가는 길이 이미 탭·사이드바이므로 뒤로가기를 겹쳐 두지 마세요 — 같은 줄에 있는 화면인데 어떤 건 있고 어떤 건 없어집니다.
  · 빈 화면·오류·마감처럼 막다른 화면에는 **다음에 할 일 버튼**을 반드시 하나 두세요(예: `목록으로`, `다시 시도`, `둘러보기`).
  · **전체 화면 목록으로 돌아가는 길**을 모든 화면에 두되, 이름에 목적이 드러나게 하세요(`화면 목록`). 접힌 패널 안에 `화면 정보` 같은 이름으로 넣으면 아무도 못 찾습니다.
  · 다 만든 뒤 **화면을 하나씩 열어 뒤로가기가 있는지 세어 보세요.** 하나라도 빠져 있으면 채웁니다.
### 화면 구현
- 각 화면의 **생성 프롬프트**를 그 화면 구현 지시로 그대로 사용하세요.
- 반복 UI(카드·리스트 행·폼·버튼)는 **공용 컴포넌트**로 만들어 재사용하세요.
- 필요하면 상태 화면(로딩·빈 결과·에러)도 함께 만드세요.
### 디자인
- **디자인 프리셋 파일을 함께 넣었다면 그것이 우선입니다.** 1장의 디자인 컨셉이나 화면별 프롬프트에 다른 색·톤이 적혀 있어도 프리셋을 따르세요. 충돌이 아니라 **프리셋이 정답**입니다.
- 프리셋의 **레이아웃 골격**(히어로·목록·내비·상세를 어떻게 놓을지)을 먼저 잡고 색을 입히세요. 색만 맞추면 어떤 테마로 만들어도 같은 화면이 나옵니다.
- 프리셋이 없다면 1장의 **디자인 컨셉**을 전역 스타일(색·톤·라운드·여백)로 토큰화해 일관되게 적용하세요.
- **글꼴은 프리셋이 없어도 한 벌로 고정하세요.** 기본은 `Paperlogy`이고, 못 불러오면 `Pretendard` → `Noto Sans KR` 순으로 물러납니다. CSS로는 `font-family: Paperlogy, Pretendard, 'Noto Sans KR', sans-serif` 한 줄이면 됩니다.
  · **글꼴은 한 벌만 씁니다.** 제목과 본문에 서로 다른 글꼴을 쓰지 마세요 — 두 벌이 되는 순간 어디에 무엇을 쓸지가 화면마다 갈립니다. 위계는 글꼴이 아니라 **크기와 굵기**로 만듭니다.
  · 굵기는 **400·600·700 셋만**, 크기는 본문 15 · 카드 제목 18 · 섹션 제목 22 · 페이지 제목 32을 기준으로 잡으세요.

#### 읽기 폭 — 글을 한 단으로 좁힐 때

글이 주인공인 자리(상세 본문·안내·약관)는 **최대 760px** 로 좁히고 가운데 정렬합니다.
콘텐츠 영역(위의 최대 폭) **안에서** 좁히는 것입니다 — 바깥 폭을 바꾸는 게 아닙니다.

**왜 좁히나** — 한 줄이 길면 다음 줄 첫 글자를 눈이 못 찾습니다. 1440px 폭에 본문을 꽉 채우면
한 줄에 90자가 넘어가고, 읽는 사람은 두세 줄마다 같은 줄을 다시 읽게 됩니다.

- **이 값 하나만 씁니다.** 화면마다 680·720·760 처럼 다르게 잡지 마세요 — 저희도 그렇게 적혀 있었고,
  같은 팩 안의 상세 화면끼리 본문 폭이 달라 서로 다른 사이트처럼 보였습니다.
- **카드 격자에는 쓰지 않습니다.** 여기는 «글»을 좁히는 값입니다. 카드를 몇 열로 놓을지는 레이아웃이 정합니다.
- 사진·표·코드처럼 넓어야 읽히는 것은 이 폭을 넘겨도 됩니다. 좁히는 건 글줄입니다.

#### 격자 — 카드를 늘어놓는 자리

카드·타일을 격자로 늘어놓을 때 **좌우와 위아래를 다른 값으로** 벌립니다.

| 자리 | 값 |
| --- | --- |
| **카드와 카드 사이** (좌우) | 위 눈금의 「카드와 카드 사이 (가로)」 |
| **줄과 줄 사이** (위아래) | 위 눈금의 「덩어리와 덩어리 사이」 |

**같은 값을 쓰면 줄이 안 갈립니다.** 카드에 상자가 있을 때는 테두리가 경계를 만들어 줬지만,
상자를 걷어낸 뒤로는 **위 카드의 가격과 아래 카드의 사진이 붙어** 한 덩어리로 읽힙니다.
위아래를 더 벌려야 "여기까지가 한 줄"이 보입니다.

| 나누는 것 | 정하는 곳 |
| --- | --- |
| 몇 열로 나눌지 | 레이아웃 프리셋 |
| **사이를 얼마나 벌릴지** | **위 간격 눈금 (좌우·위아래 따로)** |
| 카드가 어떻게 생겼는지 | 카드 모양 |

- **레이아웃 프리셋이 「넓게」라고 해도 값을 새로 만들지 마세요.** 레이아웃은 *몇 열로 무엇을 어디에*를
  말하는 층이고, *얼마나 벌릴지*는 이 눈금이 정합니다. 실제로 그 한 줄 때문에 40px을 쓴 격자가 있었고,
  같은 화면의 다른 격자는 16px이라 두 값이 나란히 놓였습니다.

**간격은 변수로 못 박고, 자리마다 그것만 씁니다.** CSS 맨 위에 이걸 그대로 붙여 넣으세요.

```css
:root{
  --wrap: 1440px;      /* 콘텐츠 폭 — 헤더·본문·푸터·고정바가 모두 이 값을 쓴다 */
  --pad-x: 24px;    /* 좌우 여백 */
  --card-gap:   16px;     /* 카드와 카드 사이 — 좌우 */
  --card-gap-y: 28px;     /* 카드 줄과 줄 사이 — 위아래 */
  --side-w:     260px;               /* 사이드바 폭 (사이드바 뼈대에서만) */
  --side-gap:   32px;                /* 사이드바와 본문 사이 */
  --row-gap:    14px;                /* 가로 행 카드 안: 썸네일과 글자 사이 */

  --card-pad:   24px;                /* 카드 안쪽 여백 */
  --row-h:      48px;                /* 표 한 줄 높이 — 안 정하면 줄마다 들쭉날쭉해진다 */
  --btn-gap:    8px;                 /* 나란히 놓은 버튼 사이 */
}
@media (max-width: 720px){
  :root{ --pad-x: 16px; --card-gap: 12px; }
}
.wrap { max-width: var(--wrap); margin-inline: auto; padding-inline: var(--pad-x); }
.cards{ display: grid; column-gap: var(--card-gap); row-gap: var(--card-gap-y); }

/* 지표 줄 — 화면 맨 위에 숫자 카드를 나란히 놓는 자리.
   카드 격자(.cards)와 다른 물건이므로 .cards 를 쓰지 않는다. 지표는 넷이 기본이다. */
.stats{ display: grid; grid-template-columns: repeat(4, 1fr);
        column-gap: var(--card-gap); row-gap: var(--card-gap-y); }
@media (max-width: 1024px){ .stats{ grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 720px){ .stats{ grid-template-columns: 1fr; } }

/* 모자이크 — 홈 첫 화면에 「큰 1장 + 작은 4장」을 놓는 자리.
   첫 장이 2칸×2줄, 나머지 넷이 옆을 채운다.
   어느 것을 크게 할지 class 로 정하지 않는다 — 맨 앞에 둔 것이 큰 것이다. */
.mosaic{ display: grid; grid-template-columns: repeat(4, 1fr);
         column-gap: var(--card-gap); row-gap: var(--card-gap-y); }
.mosaic > :first-child{ grid-column: span 2; grid-row: span 2;
                        display: flex; flex-direction: column; }
/* 큰 칸의 사진은 카드 비율을 따르지 않는다 — 두 줄 높이와 안 맞아 제목이 밀려난다.
   큰 칸에서는 사진이 남는 자리를 채운다. */
.mosaic > :first-child > .thumb{ aspect-ratio: auto; flex: 1; min-height: 0; }
@media (max-width: 1024px){
  .mosaic{ grid-template-columns: repeat(2, 1fr); }
  .mosaic > :first-child{ grid-row: auto; }   /* 2열에서 세로로 겹치면 빈칸이 생긴다 */
}
@media (max-width: 720px){
  .mosaic{ grid-template-columns: 1fr; }
  .mosaic > :first-child{ grid-column: auto; }
}

/* 상세 화면의 대표 사진 — 카드 비율을 그대로 쓰지 않는다.
   4:3 을 본문 폭 1392px 에 적용하면 세로가 1044px 이 되어 노트북 화면을 통째로 덮는다.
   가로로 눕히고(16:9) 세로를 420px 로 막는다 — 사진 아래 내용이 같이 보이는 선이다. */
.detail-img{ aspect-ratio: 16 / 9; max-height: 420px; width: 100%;
             border-radius: 12px; overflow: hidden; }
.detail-img > img{ width: 100%; height: 100%; object-fit: cover; display: block; }

/* 간격은 반드시 이 변수로 쓴다. gap:16px 처럼 숫자를 직접 적으면 자리마다 값이 갈린다. */
```

- **`--card-pad`·`--row-h`·`--btn-gap` 도 꼭 쓰세요.** 카드 안쪽 여백, 표 한 줄 높이,
  버튼 사이 간격입니다. 이 셋을 안 정해주면 화면마다 다르게 나옵니다 — 저희도 겪었습니다.
- **`.stats`·`.mosaic`·`.detail-img` 는 카드 격자와 다른 자리입니다.** 지표 숫자 줄,
  홈의 큰 사진 한 장 + 작은 넷, 상세 화면의 대표 사진. 카드 규칙을 여기 갖다 쓰면
  사진이 화면을 다 덮습니다.

- **격자에 `gap: 20px` 처럼 숫자를 직접 쓰지 마세요.** 한 번 쓰는 순간 그 자리만 달라지고,
  나중에 값을 바꿀 때 그 자리만 안 바뀝니다. 실제로 한 사이트에서 좌우가 3·8·16·20·24px
  다섯 가지로 갈라진 적이 있습니다 — 전부 `gap:` 에 숫자를 적어서 생긴 일입니다.
- **`gap` 한 줄로 합치지도 마세요.** 좌우와 위아래는 값이 다르므로 `column-gap`·`row-gap` 둘로 씁니다.
- **이름도 이대로 씁니다.** `--grid-x`·`--gap-col` 처럼 제 나름의 이름을 지으면, 나중에
  다른 사람이 볼 때 그게 같은 자리인지 알 수가 없습니다. 실제로 같은 규칙을 준 두 사이트가
  하나는 `--grid-x/--grid-y`, 하나는 `--gap-col/--gap-row` 를 써서 값이 따로 놀았습니다.
- 값을 바꾸고 싶으면 **`:root` 의 두 줄만** 고칩니다. 그러면 화면 전체가 한 번에 따라옵니다.
- 히어로의 좌우 2단처럼 **카드 격자가 아닌 자리**는 이 규칙 밖입니다. 거기는 화면 뼈대라 레이아웃이 정합니다.

#### 박스형 콘텐츠 — 몇 개를 늘어놓고 사진은 어떤 비율로

사진이 든 카드를 격자로 늘어놓을 때, **몇 개를 놓느냐가 사진 비율을 정합니다.**

| 한 줄에 | 사진 비율 | 왜 |
| --- | --- | --- |
| **2개** | **4:3** | 칸이 넓으니 가로로 넓은 사진 |
| **3개** | **4:3** | 위와 같음 |
| **4개** | **1:1** | 칸이 좁다. 정사각이 가장 얌전하다 |

4개짜리를 세로로 긴 3:4로 뒀더니 **카드 한 줄이 화면을 다 먹었습니다.** 칸이 좁을수록 세로로
길게 두고 싶어지지만, 그러면 한 줄만 보이고 다음 단락이 안 보입니다. 좁은 칸에는 정사각입니다.

- **한 줄에 하나만 두지 않습니다.** 마지막 줄에도 하나만 남기지 마세요 —
  **항목 수를 열 수에 맞춰** 3열이면 3·6·9개, 4열이면 4·8개로 둡니다.
- **한 사이트에서 카드 사진 비율은 하나입니다.** 위 표의 짝(2·3열↔4:3, 4열↔1:1) 밖의 비율은 만들지 마세요.
  같은 홈에 세로 카드와 16:9가 섞여 있어서 **사는 사람이 사진을 두 벌 준비해야 했던** 적이 있습니다.
- **배너·차트·영상·프로필은 이 규칙 밖입니다.** 가로로 긴 배너·영상(16:9)·프로필(1:1)은 제 나름의 비율이 있습니다.
- 상품 사진처럼 **정사각이 업계 표준인 분야**는 1:1을 써도 됩니다. 다만 그때도 한 사이트 안에서는 하나로 갑니다.
- **가로 배너 한 장으로 한 단락을 채우지 마세요.** 배너는 위 표 밖이지만 "한 줄에 하나만 두지 않는다"는 그대로 걸립니다.
  배너 한 장이 있던 자리는 **두 장으로 나누고** 4:3으로 맞추세요.
- **가로로 넘기는 줄(캐러셀)도 몇 개 보이느냐로 셉니다.** 뒤에 더 있어도 눈에 4개가 보이면 4개 — 비율도 1:1입니다.
  카드 폭을 `260px`처럼 박아 두면 화면 폭이 바뀔 때 3.4개씩 어정쩡하게 걸치니, **칸 수로 나눠서** 정하세요.
- **가로로 넘기는 줄에는 좌우 화살표를 두고, 아래 스크롤바는 감춥니다.** 넘길 게 있다는 건
  화살표가 알려 줍니다. 스크롤바까지 나오면 줄 밑에 회색 막대가 하나 더 생겨 지저분합니다.

```css
.carousel{ scrollbar-width:none; -ms-overflow-style:none; }
.carousel::-webkit-scrollbar{ display:none; }
```

  화살표가 **없는데** 스크롤바까지 감추면 넘길 게 있다는 걸 아무도 모릅니다. 둘은 한 벌입니다.

#### 분리형과 혼합형 — 글자를 사진 아래 두느냐 위에 얹느냐

박스형 콘텐츠는 두 가지입니다. **어느 쪽이든 위의 개수·비율 규칙은 똑같이 적용됩니다.**

| | **분리형** | **혼합형** |
| --- | --- | --- |
| 생김새 | 사진 아래에 글자 | 사진 위에 글자를 얹음 |
| 담을 수 있는 것 | 제목·설명·평점·가격까지 여러 줄 | 제목 + 한 줄 정도 |
| 사진이 하는 일 | 무엇인지 알려 준다 | 분위기를 만든다 |
| 쓰는 자리 | 상품·강의 목록처럼 **고르는** 자리 | 특집·기획전처럼 **끌어당기는** 자리 |

**분리형은 상자를 두르지 마세요.** 흰 배경에 테두리·그림자까지 두르면, 사진과 글자가 이미 한 덩어리인데
선을 한 번 더 치는 셈입니다. 격자로 늘어놓으면 그 선이 줄줄이 겹쳐 화면이 답답해집니다.
**모서리는 사진이 가집니다** — 상자를 없앤 자리에서 사진이 카드 노릇을 합니다.
(혼합형은 사진이 곧 배경이라 이 규칙 밖입니다.)

```css
/* 사진 + 글자로 된 카드 — 상자를 두르지 않는다 */
.card{
  background: none;   /* surface 나 흰색을 깔지 않는다 */
  border: 0;
  box-shadow: none;
}
.card .thumb{ border-radius: 12px; }  /* 모서리는 사진이 가진다 */
.card .body{ padding: 16px 0 0; }     /* 좌우 여백 0 — 상자가 없으니 안쪽 여백도 없다 */
```

**`background: var(--surface)` 나 `border: 1px solid` 를 카드에 쓰면 이 규칙을 어긴 것입니다.**
사진이 없는 상자(설정 패널·안내 상자·표를 감싼 것)는 이 규칙 밖입니다 — 거기는 배경이 있어야
덩어리로 읽힙니다. **사진과 글자로 된 카드**만 걷어냅니다.

상자를 걷어내면 두 가지가 따라옵니다.

- **글자가 흰색이 아니라 페이지 배경 위**에 놓입니다. 대비를 다시 재세요.
- **카드끼리 갈라 주던 테두리가 없어집니다.** 줄과 줄 사이를 더 벌려 그 몫을 대신하세요.

그리고 **카드 안에는 선을 긋지 마세요.** 가격 위에 선을 두면 상자가 없는 상태에서는 그 선이 카드를
가르는 것처럼 보여, 사진·제목과 가격이 따로 노는 두 덩어리로 읽힙니다.
**갈라야 할 것은 카드 안이 아니라 카드끼리**입니다.

혼합형을 쓸 때 반드시 지킬 것:

- **어둠막을 까세요.** 사진 위에 흰 글자를 그냥 얹으면 사진의 밝은 자리에서 글자가 사라집니다.
  **사진은 사는 분이 바꿀 것**이라 우리가 밝기를 미리 알 수 없습니다.
  글자 쪽에서 반대편으로 옅어지는 어둠막을 항상 깔아 두면 어떤 사진이 와도 읽힙니다.
- **글자 대비 4.5 이상**을 지키세요. 가장 밝은 사진(흰색)이 왔다고 치고 재세요.
- **글자를 짧게 쓰세요.** 제목 한 줄 + 설명 한 줄까지입니다. 그보다 길면 사진을 다 덮어 혼합형인 의미가 없습니다.
- **가격·평점처럼 정확히 읽어야 하는 것은 얹지 마세요.** 사진 위에서는 숫자가 잘 안 읽힙니다.
- **한 화면에 두 가지를 섞어도 됩니다.** 위는 혼합형 특집, 아래는 분리형 목록 — 하는 일이 다르므로 어수선하지 않습니다.

#### 글꼴은 한 벌입니다

**기본은 `Paperlogy`**, 못 불러오면 `Pretendard` → `Noto Sans KR` 순으로 물러납니다.
CSS로는 `font-family: Paperlogy, Pretendard, 'Noto Sans KR', sans-serif` 한 줄입니다.

- **제목과 본문에 다른 글꼴을 쓰지 마세요.** 두 벌이 되는 순간 어디에 무엇을 쓸지가
  화면마다 갈립니다. 처음엔 「제목만 다르게」로 시작해서, 나중엔 세 벌 네 벌이 됩니다.
- **위계는 글꼴이 아니라 크기와 굵기로 만듭니다.** 굵기는 400·600·700 셋만 쓰세요.
- 크기는 본문 15 · 카드 제목 18 · 섹션 제목 22 · 페이지 제목 32이 기준입니다.
- **숫자가 많은 표에는 고정폭 숫자**(`font-variant-numeric: tabular-nums`)를 켜세요.
  안 켜면 값이 바뀔 때 자릿수가 흔들려 칸이 들썩입니다.

#### 움직임 — 바뀌었다는 걸 눈이 알아채게

값이 바뀌었는데 **딱 하고 갈아 끼우면 사람은 그걸 못 봅니다.** 숫자가 12에서 3으로
바뀌어도, 눈이 그 자리를 안 보고 있었으면 바뀐 줄 모릅니다.
움직임은 꾸미개가 아니라 **「방금 여기가 바뀌었어요」라고 알려 주는 장치**입니다.

| 언제 | 얼마나 | 무엇이 |
| --- | --- | --- |
| 마우스를 올렸을 때 | **0.12초** | 색·테두리 |
| 눌러서 값이 바뀔 때 | **0.2초** | 사라짐·나타남·자리 옮김 |
| 접히고 펴질 때 | **0.25초** | 높이 |
| 화면 위에 뜰 때 (모달·시트) | **0.25초** | 옅어짐 + 살짝 올라옴 |
| 안내 문구(토스트) | **0.2초 등장 · 3초 머무름** | 옅어짐 |

- **가속은 하나로 씁니다.** `ease-out`(빨리 시작해 부드럽게 멈춤)이 기본입니다.
  자리마다 다른 가속을 쓰면 같은 사이트에서 두 사람이 만든 것처럼 보입니다.
- **0.3초를 넘기지 마세요.** 그보다 길면 기다리는 느낌이 듭니다. 목록이 다시 그려질 때는
  **0.15초**면 충분합니다.
- **자리를 밀어내며 움직이지 마세요.** 아래 내용이 출렁이면 읽던 곳을 잃습니다.
  옅어지거나 제자리에서 바뀌게 하세요.
- **한 번에 한 곳만 움직입니다.** 눌렀는데 화면 여기저기가 같이 움직이면 어디가 바뀐 건지 모릅니다.
- **끝없이 도는 움직임은 두지 않습니다.** 배너 자동 넘김·깜빡임은 읽는 걸 방해합니다.
  기다리는 동안 도는 것(로딩)만 예외입니다.
- **움직임을 꺼 둔 분을 존중하세요.** `prefers-reduced-motion` 이 켜져 있으면 전부 0초로 둡니다.
  어지럼증 때문에 꺼 두신 분들이 있습니다. 값은 바뀌되 움직이지만 않으면 됩니다.

#### 떠 있는 부품과 줄 안에 서는 부품은 다른 것입니다

사진 위에 **떠 있는** 동그란 찜 아이콘과, 버튼 줄 안에 **나란히 서는** 찜 버튼은
생김새가 비슷해 보여도 다른 부품입니다. 떠 있는 쪽은 자리를 빠져나가도록(`position:absolute`)
만들어져 있어서, 그 이름을 줄 안의 버튼에 붙이면 **줄을 빠져나가 엉뚱한 데 가서 앉습니다.**
실제로 찜 버튼이 가격 위에 겹쳐 앉은 적이 있습니다.

- **이름이 자리를 뜻하면 그 자리에서만 쓰세요.** 「사진 위 찜」과 「줄 안 아이콘 버튼」은
  이름을 나눠 두고, 섞어 쓰지 않습니다.
- **부품 이름과 실제 쓰임이 어긋나면 이름을 고치세요.** 「타임라인」이라 지어 놓고
  여섯 곳 모두 한 줄짜리 목록으로 쓰고 있던 적이 있습니다. 세로 점만 남아 글자 사이에 끼었습니다.
- **아이콘만으로 뜻이 통하면 글자를 빼세요.** 하트·공유·닫기가 그렇습니다.
  다만 **`aria-label`로 이름은 남깁니다** — 눈으로 못 보는 분에게는 그게 유일한 이름입니다.
- 반대로 **아이콘만으로 못 알아보는 것에는 글자를 붙이세요.** 「빼기」·「내보내기」처럼
  모양이 여러 뜻으로 읽히는 것이 그렇습니다.

#### 쓰는 화면이 있으면 보여주는 자리도 있어야 합니다

글을 쓰게 해 놓고 그 글이 나올 자리를 안 만드는 일이 잦습니다.
실제로 강사가 **1,240자짜리 강의 소개를 쓰는 화면**은 있는데, 상세 화면에는 그 글이
나올 자리가 없었습니다. 게다가 검수 화면은 "강의 소개 1,240자 — 확인됨"이라고까지 말하고 있었습니다.

- **입력 칸을 만들 때마다 "이건 어디에 보이나"를 한 번 물으세요.** 답이 없으면 둘 중 하나입니다 —
  보여줄 자리를 만들거나, 그 입력 칸을 빼거나.
- **에디터(굵게·목록·이미지 버튼이 붙은 칸)로 쓴 글**은 보여주는 쪽에도 그 모양이 살아야 합니다.
  제목·목록·굵게를 쓸 수 있게 해 놓고 보여줄 때 한 덩어리 문단으로 뭉개면, 쓴 사람이 헛수고합니다.
- **한 줄 요약과 본문 소개는 다른 것입니다.** 목록 카드에 들어가는 한 줄과, 상세에서 읽는 본문은
  자리도 길이도 다릅니다. 하나로 합치지 마세요.

#### 권장 크기는 실제 자리와 같아야 합니다

"권장 1280×720 (16:9)"이라고 적어 두고 옆의 자리는 4:3인 화면이 있었습니다.
**사는 분은 글자를 믿고 그 크기로 사진을 준비**하는데, 올려 보면 잘립니다.

- 안내 문구의 숫자와 **바로 옆 자리의 비율이 같은지** 확인하세요.
- 검수·확인 화면에 크기가 또 적힌다면 **거기도 같은 숫자**여야 합니다. 세 곳이 따로 놀기 쉽습니다.
- **영상만 16:9입니다.** 썸네일·카드 사진은 위 표(2·3개↔4:3, 4개↔1:1)를 따릅니다.

#### 차트 — 이미지가 아니라 데이터로 그립니다

추이·막대·도넛 같은 차트를 **이미지 자리로 두지 마세요.** `이미지 영역 (라인 차트 · 권장 1200×420)`
이라고 적어 두면 사는 분은 차트를 **그림 파일로 만들어 올려야 하는 줄** 압니다.
차트는 숫자가 바뀌면 따라 바뀌어야 하는 것이라, 사진 자리와 성격이 다릅니다.

- **가벼운 것은 라이브러리 없이 SVG로 그리세요.** 선 하나·막대 열두 개짜리는 30줄이면 됩니다.
  바깥에서 아무것도 안 불러오니 **인터넷 없이 폴더에서 열어도** 그대로 보입니다.

**대시보드를 만든다면 최소한 이 셋은 갖추세요.**

| 무엇 | 어디에 | 왜 |
| --- | --- | --- |
| **큰 추이 차트** | 화면에 한둘 | 기간을 놓고 흐름을 본다 |
| **스파크라인** (작은 추이선) | 지표 카드 안 | 숫자 옆에서 방향만 알려 준다 |
| **증감 표시** (▴ 812) | 지표 카드 안 | 좋아졌는지 나빠졌는지 |

숫자 하나만 두면 **좋은지 나쁜지를 못 말합니다.** "10,374명"만 있으면 늘고 있는지
줄고 있는지 알 수 없어서, 사는 분은 그 카드를 보고 아무 판단도 못 합니다.
지표 카드는 **숫자 + 증감 + 추이선**이 한 벌입니다.

- **본격적인 차트가 필요하면 무료(MIT) 라이브러리를 쓰세요.** 값을 치르지 않아도 되고 상업적으로 써도 됩니다.

| 라이브러리 | 크기 | 언제 |
| --- | --- | --- |
| **Chart.js** | ~200KB | 무난한 첫 선택. 자료가 많아 AI도 정확하게 짭니다 |
| **ApexCharts** | ~500KB | 기본 모양이 예쁘고 종류가 많을 때 |
| **uPlot** | ~40KB | 점이 수천 개라 가벼워야 할 때 |
| **Frappe Charts** | ~30KB | 작고 산뜻한 SVG면 충분할 때 |

- **색은 반드시 프리셋 색을 쓰세요.** 라이브러리 기본 팔레트(빨강·노랑·초록)를 그대로 두면
  그 자리만 다른 사이트처럼 보입니다.
- **눈금선은 옅게, 선은 진하게.** 눈금이 데이터보다 진하면 숫자가 안 읽힙니다.
- 라이브러리를 쓰면 **인터넷이 필요해집니다.** 오프라인으로 열어 볼 화면이라면 파일을 함께 받아 두거나 SVG로 그리세요.

#### 가격 표기 — 낼 돈부터

**판매가 · 정가 · 할인율** 순서로 한 줄에 세웁니다.

| 자리 | 모양 | 크기 |
| --- | --- | --- |
| **판매가** (낼 돈) | 진한 기본 글자색, 굵게 | 18 (상세는 28) |
| **정가** | 회색 + 취소선 | 13 |
| **할인율** | **강조색 글자**, 굵게 | 14 |

- **할인율을 앞에 두지 마세요.** 눈이 먼저 "20%"에 걸려 정작 얼마인지를 뒤늦게 찾습니다.
  사는 분이 알고 싶은 건 **낼 돈**입니다.
- **할인율은 배지가 아니라 색 글자입니다.** 배지로 두면 정가·판매가와 무게가 비슷해져 셋이 서로 다툽니다.
  배지 자리는 `입문`·`NEW` 같은 **꼬리표 몫**입니다.
- **셋의 크기를 벌려 두세요.** 비슷하면 무엇을 먼저 읽어야 할지 눈이 헤맵니다.
- **강조색 원색을 글자로 쓰지 마세요.** 주황·코랄 원색은 흰 배경에서 3:1을 못 넘깁니다.
  글자용으로 한 단계 어두운 색을 따로 두세요.
- 가격 묶음은 **한 곳에서 만들어 쓰세요.** 자리마다 손으로 짜면 순서가 갈립니다.

#### 버튼이냐 링크냐

**다른 화면으로 데려가면 링크, 이 화면 안에서 끝나면 버튼입니다.**

| 하는 일 | 무엇으로 | 눌렀을 때 |
| --- | --- | --- |
| 다른 화면으로 간다 | 링크 `<a href>` | 그 화면이 열린다 |
| 이 화면 안에서 끝난다 | 버튼 `<button>` | 안내 문구가 뜬다 |

- 필터 적용·정렬·검색·다음 차시·시간 지점 이동은 **화면이 바뀌지 않는 조작**입니다.
  링크로 만들면 **자기 자신으로 가는 링크**가 되어, 눌러도 같은 화면이 다시 열립니다.
  사는 분 눈에는 *"눌렀는데 아무 일도 안 나네"* 로 보입니다.
- **나란히 선 버튼끼리는 종류를 섞지 마세요.** 하나는 화면이 넘어가고 하나는 안 넘어가면
  규칙이 없어 보입니다.

**그리고 화면 안에서 끝나는 조작은 진짜로 값이 바뀌어야 합니다.**
눌러도 숫자가 그대로면, 사는 분 눈에는 **눌리지 않는 포스터**입니다.

| 눌렀을 때 | 바뀌어야 하는 것 |
| --- | --- |
| 기간 탭 (7일·30일·90일) | 차트와 그 옆 요약 숫자 |
| 쿠폰·옵션 고르기 | 결제 금액 줄과 합계 |
| 수량 늘리기 | 금액 |
| 정렬·필터 | 목록 순서나 개수 |

- **미리 만들어 두고 바꿔 끼세요.** 차트 셋을 미리 그려 두고 탭에 따라 보여 주는 편이,
  누를 때마다 자바스크립트로 다시 그리는 것보다 낫습니다 — 그리는 규칙이 두 곳으로 갈라지지 않습니다.
- **계산은 한 곳에서만 하세요.** 합계를 화면마다 손으로 적어 두면 쿠폰을 바꿨을 때 한 군데만 바뀝니다.
- **탭에 갈 곳도 바꿀 것도 없으면 탭이 아닙니다.** 그냥 제목이거나, 아직 안 만든 자리입니다.

**같은 길을 두 번 두지 마세요 — 위 카테고리 줄과 왼쪽 LNB.**
둘이 같은 곳을 가리키면 위쪽이 두 줄로 두꺼워지고, 어느 쪽을 눌러야 할지 잠깐 멈칫합니다.

| 상태 | 왼쪽 LNB | 위 카테고리 줄 |
| --- | --- | --- |
| 맨 위 (LNB 보임) | 보인다 | **접힌다** |
| 스크롤해서 LNB가 위로 사라짐 | 안 보인다 | **내려온다** |
| LNB가 아예 없는 화면 | — | **늘 보인다** |

- **LNB가 있는 화면은 접힌 채로 시작하세요.** 펼쳐 두었다가 나중에 접으면 첫 화면에서 한 번 번쩍입니다.
- **LNB가 없는 화면에서는 절대 접지 마세요.** 접으면 갈 길이 통째로 사라집니다.
- **기준은 "LNB가 붙박이 머리말 아래로 사라지는 순간"입니다.** 머리말 높이만큼 위를 깎아서 재세요.
- 같은 규칙을 **LNB를 쓰는 모든 레이아웃에 똑같이** 거세요. 화면마다 다르면 위쪽 높이가 제멋대로 바뀌어 보입니다.

- **견본으로 가는 버튼을 화면 안에 두지 마세요.** `결과가 없을 때` `대화가 없을 때는 이렇게 보여요`
  처럼 예외 화면을 보여주려고 넣은 버튼·단락은 진짜 사이트에 없는 것입니다.
  검색창 옆에 그런 버튼이 서 있으면 사는 분은 그게 기능인 줄 압니다.
  빈 화면·오류 화면은 **화면 목록에 따로 한 장**으로 두세요 — 거기서 눌러 보면 됩니다.

#### 화면 목록으로 가는 길은 한 곳에만

만든 화면을 훑어보려면 목록이 필요합니다. 그런데 그 목록을 **화면 안에 늘어놓으면**
사는 분 눈에는 사이트의 일부로 보입니다.

```
이렇게 두지 마세요 — 화면 위쪽에 줄줄이
  [CO-01 · 강의 목록] [CO-02 · 검색 결과 없음] [CO-03 · 강의 상세] [CO-04 · 결제] …
```

**진짜 사이트에는 이런 줄이 없습니다.** `CO-02 · 검색 결과 없음` 은 만드는 사람의 말이지
쓰는 사람의 말이 아닙니다. 화면 ID 가 손님 눈에 보이는 것부터가 어긋납니다.

- **한 귀퉁이에 작게 하나만 둡니다.** 오른쪽 아래에 떠 있는 `화면 목록` 단추 하나면 됩니다.
  누르면 `index.html` 로 갑니다.
- **그 단추는 화면 내용 위에 떠 있게** 두고, 본문 흐름에는 끼워 넣지 않습니다.
  본문에 들어가면 그것도 사이트의 일부가 됩니다.
- **화면 ID 를 손님이 보는 자리에 쓰지 않습니다.** 제목·탭·빵부스러기 어디에도요.
  ID 는 우리가 화면을 세는 이름이지 서비스의 말이 아닙니다.
- 화면끼리 오가는 길은 **그 서비스의 진짜 메뉴와 버튼**으로 만듭니다.
  「강의 목록 → 강의 상세」는 강의 카드를 눌러서 가지, 탭으로 가지 않습니다.

### 이미지 · 썸네일
- 아직 사진이 없으므로 이미지 자리는 **테마 색이 아니라 옅은 파스텔**로 채우세요. 테마 색으로 칠하면 화면이 그 색 덩어리로 뒤덮여 디자인이 안 보입니다.
- 배경 `#EEF2F7` · `#F2EFF7` · `#EDF4F1` · `#F7F1EC` · `#F2F4F7` 중 하나를 카드마다 돌려 쓰고, 테두리 `#E3E8F0` 1px, 글자색 `#8B94A6` 13px.
- 자리 안에 `이미지 영역 (무엇 · 권장 가로×세로)` 형식으로 적으세요. 예: `이미지 영역 (매장 대표 · 권장 1200×900)` · `이미지 영역 (프로필 · 권장 400×400)` · `이미지 영역 (배너 · 권장 1600×600)`
- 이미지 자리는 테마 색으로 칠하지 않는다. 옅은 파스텔 한 톤 + 1px 테두리로 둔다.
- 자리 안에 무엇이 들어갈 이미지인지와 권장 크기를 글자로 적는다.
- 적어 둔 크기의 **비율을 실제로 지킨다** — `400×400`이라 적었으면 정사각으로 보여야 한다(aspect-ratio).
- 특히 프로필·썸네일처럼 크기가 고정된 자리는, 가로로 나란히 놓아도 늘어나지 않게 한다. 목록·행 안에 넣을 때 `flex`가 잡아당겨 타원이 되는 일이 잦으니, 만든 뒤 실제로 열어 모양을 확인한다.
- 썸네일처럼 작아서 글자가 안 들어가면 크기만 적거나 아이콘 하나만 둔다.
- 실제 사진을 넣기 전까지 이 규칙을 유지한다 — 그래야 어디에 무엇을 넣어야 하는지 보인다.
### 파일을 어디에 두나

```
완성화면/
  index.html              전체 화면 목록 (여기서 화면으로 들어간다)
  pages/
    HO-01.html            화면 하나에 파일 하나 · 이름은 화면 ID 그대로
    CO-03.html
  assets/
    css/style.css         스타일 한 벌
    js/app.js             동작 한 벌
```

- **`pages/` 안에서는 `../assets/css/style.css` 로 부릅니다.** `assets/...` 라고 쓰면 `pages/assets/...` 를 찾다가 못 찾아서 **스타일이 통째로 안 붙습니다** — 글꼴도 여백도 격자도 전부 날아갑니다.
- **`index.html` 은 `완성화면/` 바로 아래**에 둡니다. 거기서는 `assets/...` 그대로 부르면 됩니다.
- **스타일과 동작은 각각 한 벌**입니다. 화면마다 따로 만들면 44장이 서로 달라집니다.
- 화면끼리 오갈 때는 `CO-03.html` 처럼 **같은 폴더 안 이름**으로 겁니다.
- 다 만든 뒤 **화면 하나를 브라우저로 직접 열어 보세요.** 글꼴이 기본 고딕으로 보이면 경로가 틀린 것입니다.

### 추천 스택
- 정적 다중 페이지(HTML/CSS) 또는 컴포넌트 기반(React·Next.js·Vue 등) 중 프로젝트 규모에 맞게 선택하세요. 화면이 많고 상태가 복잡하면 컴포넌트 기반을 권장합니다.
