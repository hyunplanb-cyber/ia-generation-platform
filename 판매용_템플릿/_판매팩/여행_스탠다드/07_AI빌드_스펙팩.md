# 해외 투어·티켓 예약 플랫폼 — AI 빌드 스펙팩

> 이 문서 하나로 사이트를 만들 수 있게 정리한 스펙이에요. 화면별 **생성 프롬프트**를 그대로 사용하세요.

## 1. 프로젝트 개요
- **컨셉**: 해외 여행지의 투어·입장권·패스를 파는 예약 플랫폼. 여행자는 여행지별로 상품을 둘러보고 날짜와 인원을 골라 예약한 뒤, 현지에서 쓸 바우처를 받는다. 예약 확인·취소·환불부터 바우처 사용, 후기와 알림까지 여행자가 쓰는 화면 전체를 담는다.
- **디자인 컨셉**: 여행 사진이 주인공이 되는 밝은 화이트 배경, 포인트 컬러는 딥 오션 블루(#0B6FA4) 하나. 상품은 큰 사진 카드로, 가격과 할인율은 눈에 띄게
- **디바이스**: 반응형
- **전체 일정**: 2026-08-03 ~ 2026-10-16

## 2. 공통 요소 (모든 화면 공통)
> 아래 요소는 **한 번만 만들어 모든 화면에서 재사용**하세요. 각 화면 스펙(4장)은 이 공통 요소 위에 올라가는 콘텐츠만 설명합니다.
- **상단 내비게이션(GNB)**: `홈` · `상품` · `장바구니` · `예약·결제` · `바우처` · `마이페이지` · `고객센터` — 모든 화면 상단에 고정
- **콘텐츠 영역**: 최대 1440px, 좌우 여백 24px — 헤더·본문·푸터·하단 고정 바가 모두 이 폭을 쓴다. 화면마다 따로 정하지 않는다. 화면마다 폭을 새로 정하면 위아래가 어긋납니다.
- **읽기 폭**: 글이 주인공인 자리(상세 본문·안내·약관)는 최대 760px로 좁혀 콘텐츠 영역 **안에서** 가운데 정렬 — 카드 격자에는 쓰지 않습니다. 아래 「읽기 폭」 참고.
- **헤더**: 로고 + 검색 + 상단 내비게이션(GNB)
- **푸터**: 저작권 · 기본 링크
- **재사용 컴포넌트**: 반복되는 UI(상품/항목 **카드**, 리스트 **행**, **폼 필드**, **버튼**)는 공용 컴포넌트로 만들어 화면마다 재사용하세요.

## 3. 메뉴 구조
- **[HO] 홈** (Home)
  - `HO-01` 홈
  - `HO-02` 여행지 홈
  - `HO-03` 검색 결과
  - `HO-04` 검색 결과 없음
  - `HO-05` 테마 기획전
- **[PR] 상품** (Products)
  - `PR-01` 상품 목록
  - `PR-02` 상품 상세
  - `PR-03` 날짜·옵션 선택
  - `PR-04` 패스 상품 상세
  - `PR-05` 상품 비교
  - `PR-06` 예약 마감·품절
  - `PR-07` 후기 전체보기
  - `PR-08` 찜한 상품
- **[CT] 장바구니** (Cart)
  - `CT-01` 장바구니
  - `CT-02` 장바구니 비어 있음
- **[BK] 예약·결제** (Booking)
  - `BK-01` 여행자 정보 입력
  - `BK-02` 결제
  - `BK-03` 예약 완료
  - `BK-04` 결제 실패
  - `BK-05` 예약 확정 대기
  - `BK-06` 최소 인원 미달 안내
- **[VC] 바우처** (Voucher)
  - `VC-01` 바우처 목록
  - `VC-02` 바우처 상세
  - `VC-03` 바우처 만료
  - `VC-04` 바우처 재발급 요청
- **[MY] 마이페이지** (MyPage)
  - `MY-01` 로그인
  - `MY-02` 회원가입
  - `MY-03` 예약 내역
  - `MY-04` 예약 내역 없음
  - `MY-05` 예약 상세
  - `MY-06` 예약 취소 신청
  - `MY-07` 취소 불가 안내
  - `MY-08` 후기 작성
  - `MY-09` 쿠폰함
  - `MY-10` 내 여정
  - `MY-11` 내 후기
  - `MY-12` 알림함
  - `MY-13` 계정 설정
- **[CS] 고객센터** (Support)
  - `CS-01` 자주 묻는 질문
  - `CS-02` 1:1 문의 작성
  - `CS-03` 문의 내역
  - `CS-04` 공지사항
  - `CS-05` 취소·환불 규정 안내

## 4. 화면별 스펙
### HO-01 · 홈
- **소속 메뉴**: 홈
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-08-03 ~ 2026-08-06
- **기능정의**: 여행지 검색창이 얹힌 히어로 배너 · 인기 여행지 바로가기 6곳 · 이번 주 인기 상품 8개 카드 · 특가·마감임박 상품 캐러셀 · 패스 상품 안내 배너 · 테마별 추천 탭(투어/입장권/교통) · 실제 후기 슬라이드 5개 · 현지 라운지·고객센터 안내 · 여행 팁 콘텐츠 4개 · 하단 앱·카톡 채널 안내
- **버튼 → 이동화면**:
  - `여행지 보기` → `HO-02` 여행지 홈
  - `검색` → `HO-03` 검색 결과
  - `상품 상세` → `PR-02` 상품 상세
  - `패스 보기` → `PR-04` 패스 상품 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `지역·기간 검색바` → 고른 값이 칩으로 남고 아래 추천 상품이 그에 맞게 바뀐다
  - `테마 카테고리` → 고른 테마에 표시가 남고 아래 목록이 걸러진다
  - `가로 목록 좌우 화살표` → 카드가 한 칸씩 밀리고 끝에 닿으면 화살표가 흐려진다
  - `자주 묻는 질문` → 답이 펼쳐지고 다시 누르면 접힌다
- **생성 프롬프트**:

  ```
  해외 투어·티켓 예약 사이트의 홈 화면을 만들어줘. 스크롤 순서대로 아래 섹션을 위에서 아래로 쌓아줘. 1) 상단 GNB(로고, 여행지, 투어·티켓, 패스, 고객센터, 로그인, 장바구니 아이콘) 2) 히어로 배너 — 큰 여행 사진 위에 "어디로 떠나세요?" 검색창(여행지·날짜 입력)과 누적 이용자 수 지표 3) 인기 여행지 바로가기 6곳 — 도시 사진 원형 카드(뉴욕, LA, 칸쿤 등) 4) 이번 주 인기 상품 8개 카드 — 사진, 상품명, 별점과 후기 수, 할인율, 가격 5) 특가·마감임박 상품 가로 캐러셀 — 남은 좌석 수를 붉게 표시 6) 패스 상품 안내 배너 — "주요 명소 최대 70% 할인" 강조 7) 테마별 추천 — 투어 / 입장권 / 교통 탭으로 전환되는 섹션 8) 실제 후기 슬라이드 5개 — 사진, 별점, 한 줄 평, 작성자 9) 현지 라운지 안내 — 주소, 운영시간, 지도 썸네일 10) 여행 팁 콘텐츠 4개 카드 11) 하단 푸터 — 회사 정보, 고객센터 번호, 카톡 채널, SNS 여행 사진이 주인공이 되도록 사진은 크게, 글자는 사진 위에 겹치지 않게 아래로 배치해줘.
  ```

### HO-02 · 여행지 홈
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈` — 누르면 `HO-01`로 간다
- **일정**: 2026-08-07 ~ 2026-08-11
- **기능정의**: 여행지 대표 사진과 한 줄 소개 · 이 도시 인기 상품 8개 · 카테고리 필터(투어/입장권/교통/패스) · 이 도시 전용 패스 안내 · 추천 일정 3코스 · 현지 쿠폰 제휴처 목록 · 날씨·시차·팁 정보 카드 · 이 도시 후기 모음
- **버튼 → 이동화면**:
  - `상품 목록` → `PR-01` 상품 목록
  - `패스 상세` → `PR-04` 패스 상품 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `여행지 안의 지역 탭` → 아래 상품·명소 목록이 그 지역 것으로 바뀐다
  - `「지도로 보기」` → 같은 자리가 목록에서 지도로 바뀌고 버튼 문구가 「목록 보기」가 된다
- **생성 프롬프트**:

  ```
  특정 여행지(예: 뉴욕) 전용 홈 화면을 만들어줘. 1) 상단에 도시 대표 사진과 도시명, 한 줄 소개 2) 카테고리 필터 칩 — 전체 / 투어 / 입장권 / 교통 / 패스 3) 이 도시 인기 상품 8개 카드 그리드 4) 이 도시 전용 패스 안내 박스 — 포함 명소 목록과 절약 금액 5) 추천 일정 3코스 — "2박3일 첫 방문 코스" 같은 카드 6) 현지 쿠폰 제휴처 목록 — 가게명, 혜택(음료 무료, 10% 할인) 7) 여행 정보 카드 — 현재 날씨, 한국과의 시차, 팁 문화 안내 8) 이 도시 후기 모음 6개
  ```

### HO-03 · 검색 결과
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈` — 누르면 `HO-01`로 간다
- **일정**: 2026-08-12 ~ 2026-08-14
- **기능정의**: 검색어와 결과 수 표시 · 필터(여행지·카테고리·가격대·평점·소요시간) · 정렬(인기순/낮은가격순/평점순) · 상품 카드 목록 · 지도 보기 전환 · 무한 스크롤
- **버튼 → 이동화면**:
  - `상품 상세` → `PR-02` 상품 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `왼쪽 필터(가격·기간·테마·평점)` → 고른 조건이 칩으로 쌓이고 결과 수와 목록이 함께 바뀐다
  - `정렬(인기·낮은 가격·평점·출발 임박)` → 카드 순서가 다시 늘어선다
  - `칩의 ✕` → 그 조건만 풀리고 결과가 다시 늘어난다
  - `「오늘 출발 가능만」 토글` → 켜면 오늘 자리 있는 상품만 남고 숫자가 줄어든다
- **생성 프롬프트**:

  ```
  상품 검색 결과 화면을 만들어줘. 상단에 검색어와 "총 42개 상품" 표시, 왼쪽에 필터 패널(여행지, 카테고리, 가격대 슬라이더, 평점, 소요시간), 오른쪽에 상품 카드 목록(사진, 상품명, 소요시간, 별점, 후기 수, 할인 전/후 가격). 상단에 정렬 드롭다운(인기순·낮은 가격순·평점순)과 "지도로 보기" 전환 버튼. 모바일은 필터를 하단 시트로 열리게 해줘.
  ```

### HO-04 · 검색 결과 없음
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈` — 누르면 `HO-01`로 간다
- **일정**: 2026-08-17 ~ 2026-08-18
- **기능정의**: 검색어를 포함한 안내 문구 · 필터 초기화 버튼 · 추천 상품 4개 · 인기 검색어 칩
- **버튼 → 이동화면**:
  - `필터 초기화` → `HO-03` 검색 결과
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `걸린 조건 칩의 ✕` → 결과 수가 0에서 늘어나고 빈 화면 대신 목록이 나타난다
  - `「조건 모두 풀기」` → 칩이 전부 사라지고 전체 상품이 나온다
- **생성 프롬프트**:

  ```
  검색 결과가 없을 때의 화면을 만들어줘. 중앙에 일러스트와 "'{검색어}'에 맞는 상품을 찾지 못했어요" 문구, "필터를 조금 넓혀보세요" 안내와 [필터 초기화] 버튼, 아래에 "이런 상품은 어때요?" 추천 4개 카드와 인기 검색어 칩을 넣어줘.
  ```

### HO-05 · 테마 기획전
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈` — 누르면 `HO-01`로 간다
- **일정**: 2026-08-19 ~ 2026-08-21
- **기능정의**: 기획전 대표 배너(테마명·기간·혜택 문구) · 테마 탭(가족여행·미식·야경·액티비티·근교 당일) · 기획전 소개 문구와 큐레이터 코멘트 · 남은 기간 카운트다운 · 기획전 전용 쿠폰 다운로드 · 대상 상품 카드 그리드와 기획전가 표시 · 여행지별 묶음 보기 · 정렬(추천순·낮은 가격순·할인율순) · 함께 보면 좋은 기획전 3개 · 종료된 기획전 안내와 다음 기획전 예고
- **버튼 → 이동화면**:
  - `상품 상세` → `PR-02` 상품 상세
  - `전체 상품` → `PR-01` 상품 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `기획전 안 카테고리 탭` → 아래 상품 묶음이 그 카테고리 것으로 바뀐다
  - `「남은 시간」` → 마감까지 남은 시간이 계속 줄어든다
- **생성 프롬프트**:

  ```
  테마·지역별로 상품을 묶어 보여주는 기획전 화면을 만들어줘. 1) 상단 기획전 대표 배너 — 큰 여행 사진 위에 테마명("여름 야경 특집"), 진행 기간, "최대 35% 할인" 혜택 문구 2) 남은 기간 카운트다운 — "종료까지 3일 12시간" 을 배너 아래 띠로 3) 테마 탭 — 가족여행 / 미식 / 야경 / 액티비티 / 근교 당일치기 4) 기획전 소개 문구와 큐레이터 한 줄 코멘트("현지 밤 풍경이 가장 예쁜 시기예요") 5) 기획전 전용 쿠폰 — 할인율, 최소 결제 금액과 [받기] 버튼, 받으면 [받음] 으로 바뀌게 6) 대상 상품 카드 그리드 — 사진, 상품명, 별점, 평소 가격 취소선과 기획전가, "기획전가" 배지를 눈에 띄게 7) 여행지별 묶음 보기 — 도시별로 접었다 펼 수 있는 섹션 8) 정렬 드롭다운 — 추천순 / 낮은 가격순 / 할인율순 9) 하단에 "함께 보면 좋은 기획전" 3개 카드 10) 이미 끝난 기획전이면 배너를 흐리게 하고 "종료된 기획전이에요" 와 다음 기획전 예고를 보여줘 사진이 주인공이 되도록 카드 사진을 크게 쓰고, 할인 표시는 붉은 계열로 강조해줘.
  ```

### PR-01 · 상품 목록
- **소속 메뉴**: 상품
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-08-24 ~ 2026-08-26
- **기능정의**: 카테고리 탭 · 필터(가격·평점·소요시간·언어·즉시확정) · 정렬 · 상품 카드 그리드 · 마감임박 배지 · 찜하기 · 페이지네이션
- **버튼 → 이동화면**:
  - `상품 상세` → `PR-02` 상품 상세
  - `마감 상품` → `PR-06` 예약 마감·품절
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `필터·정렬` → 목록과 결과 수가 함께 바뀐다
  - `카드의 찜 하트` → 채워지고 다시 누르면 비워진다
  - `「더 보기」` → 상품이 더 붙고 남은 개수가 줄어든다
- **생성 프롬프트**:

  ```
  투어·티켓 상품 목록 화면을 만들어줘. 상단에 카테고리 탭(전체·투어·입장권·교통·패스), 그 아래 필터 칩 (가격대, 평점 4.5 이상, 소요시간, 한국어 가이드, 즉시확정). 상품 카드 3열 그리드 — 사진, 상품명, 소요시간 배지, 별점과 후기 수, "즉시확정" 배지, 할인 전 가격(취소선)과 판매가. 잔여 좌석이 적으면 "마감임박 3자리" 를 붉게, 카드 우상단에 찜하기 하트. 모바일은 2열로 바꿔줘.
  ```

### PR-02 · 상품 상세
- **소속 메뉴**: 상품
- **뒤로가기**: 좌측 상단에 `‹ 상품 목록` — 누르면 `PR-01`로 간다
- **일정**: 2026-08-27 ~ 2026-09-01
- **기능정의**: 사진 갤러리 · 상품명과 별점·후기 수 · 핵심 정보 배지(소요시간·언어·즉시확정·미팅장소) · 가격과 할인율 · 상품 소개 본문 · 코스 일정 타임라인 · 포함/불포함 사항 · 미팅 장소 지도 · 취소·환불 규정 · 후기 요약과 최신 후기 5개 · 자주 묻는 질문 · 하단 고정 예약 바
- **버튼 → 이동화면**:
  - `날짜 선택` → `PR-03` 날짜·옵션 선택
  - `후기 전체` → `PR-07` 후기 전체보기
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `일정·포함·불포함·후기 탭` → 아래 내용이 그 탭 것으로 바뀐다
  - `사진 갤러리` → 크게 열리고 좌우로 넘길 수 있다
  - `일정 날짜별 줄` → 그날 일정이 펼쳐지고 다시 누르면 접힌다
  - `찜 하트` → 채워지고 다시 누르면 비워진다
- **생성 프롬프트**:

  ```
  투어 상품 상세 화면을 만들어줘. 1) 상단에 사진 갤러리(대표 사진 + 썸네일 4장, 클릭 시 확대) 2) 상품명, 별점과 후기 수, 찜하기·공유 버튼 3) 핵심 정보 배지 — 소요시간 8시간, 한국어 가이드, 즉시확정, 미팅 장소 4) 가격 영역 — 할인 전 가격 취소선, 할인율, 판매가를 크게 5) 상품 소개 본문 6) 코스 일정 타임라인 — 시간대별로 방문지와 머무는 시간 7) 포함 사항 / 불포함 사항을 두 칸으로 나눠 체크·엑스 아이콘과 함께 8) 미팅 장소 지도와 찾아가는 길 안내 9) 취소·환불 규정 표 — 출발 며칠 전이냐에 따른 수수료율 10) 후기 요약(평점 분포 막대)과 최신 후기 5개 11) 자주 묻는 질문 아코디언 12) 하단 고정 바 — 가격과 [날짜 선택하기] 버튼 모바일에서는 하단 고정 바가 항상 보이게 해줘.
  ```

### PR-03 · 날짜·옵션 선택
- **소속 메뉴**: 상품
- **뒤로가기**: 좌측 상단에 `‹ 상품 목록` — 누르면 `PR-01`로 간다
- **일정**: 2026-09-02 ~ 2026-09-04
- **기능정의**: 달력에서 출발일 선택(예약 가능일만 활성) · 시간대 선택 · 인원 선택(성인·아동·유아) · 옵션 선택(픽업 여부·좌석 등급) · 실시간 잔여 좌석 표시 · 총 금액 자동 계산
- **버튼 → 이동화면**:
  - `장바구니 담기` → `CT-01` 장바구니
  - `바로 예약` → `BK-01` 여행자 정보 입력
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `달력 날짜` → 고른 날에 표시가 남고 아래 옵션·잔여 좌석이 그 날 것으로 바뀐다
  - `인원 늘리고 줄이기` → 합계 금액이 그 자리에서 다시 계산된다
  - `옵션 고르기(픽업·좌석 등급)` → 합계 금액과 아래 요약이 따라 바뀐다
  - `잔여가 0인 날짜` → 눌리지 않고 흐리게 남는다
- **생성 프롬프트**:

  ```
  투어 날짜와 옵션을 고르는 화면을 만들어줘. 1) 달력 — 예약 가능한 날짜만 진하게, 휴무일과 마감일은 흐리게 하고 클릭되지 않게 2) 날짜를 고르면 그날의 출발 시간대 목록이 나오고, 각 시간대 옆에 남은 좌석 수 표시 3) 인원 선택 — 성인·아동(3~12세)·유아 각각 플러스 마이너스 버튼, 나이 기준 안내 4) 옵션 선택 — 호텔 픽업 여부, 좌석 등급 등을 라디오로 5) 우측(모바일은 하단)에 실시간으로 바뀌는 금액 요약 — 항목별 금액과 총액 6) [장바구니 담기]와 [바로 예약하기] 버튼 잔여 좌석보다 많은 인원을 고르면 그 자리에서 "남은 좌석은 4자리예요" 안내를 띄워줘.
  ```

### PR-04 · 패스 상품 상세
- **소속 메뉴**: 상품
- **뒤로가기**: 좌측 상단에 `‹ 상품 목록` — 누르면 `PR-01`로 간다
- **일정**: 2026-09-07 ~ 2026-09-09
- **기능정의**: 패스 소개와 절약 금액 강조 · 포함 명소 목록과 개별 정가 · 이용 기간과 사용 규칙 · 패스 종류별 가격표(2일권·3일권) · 사용 방법 3단계 · 주의사항 · 하단 고정 구매 바
- **버튼 → 이동화면**:
  - `날짜 선택` → `PR-03` 날짜·옵션 선택
  - `명소 상세` → `PR-02` 상품 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `패스 기간 고르기(1일·3일·7일)` → 가격과 「포함되는 곳」 목록이 그 기간 것으로 바뀐다
  - `포함 시설 줄` → 이용 조건이 펼쳐진다
- **생성 프롬프트**:

  ```
  여러 명소를 묶어 할인하는 패스 상품 상세 화면을 만들어줘. 1) 상단에 패스 이름과 "개별 구매 대비 68% 절약" 을 크게 2) 포함 명소 목록 — 명소 사진, 이름, 개별 정가, 합계와 패스 가격 비교 표 3) 패스 종류별 가격표 — 2일권 / 3일권 / 5일권을 카드로 나란히, 추천 배지 4) 이용 기간 안내 — "첫 사용일로부터 연속 N일" 규칙을 예시와 함께 5) 사용 방법 3단계 — 구매 → 바우처 받기 → 입구에서 QR 제시 6) 주의사항 — 일부 명소 사전 예약 필수, 재입장 불가 등 7) 하단 고정 바 — 선택한 패스 종류와 [구매하기]
  ```

### PR-05 · 상품 비교
- **소속 메뉴**: 상품
- **뒤로가기**: 좌측 상단에 `‹ 상품 목록` — 누르면 `PR-01`로 간다
- **일정**: 2026-09-10 ~ 2026-09-11
- **기능정의**: 선택한 상품 2~3개를 표로 나란히 비교 · 비교 항목(가격·소요시간·포함사항·미팅장소·취소규정·평점) · 차이나는 항목 강조 · 각 열에서 바로 예약
- **버튼 → 이동화면**:
  - `날짜 선택` → `PR-03` 날짜·옵션 선택
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「차이 항목만 보기」 토글` → 같은 값인 줄이 접히고 다른 줄만 남는다
  - `비교 목록에서 상품 빼기` → 그 열이 표에서 사라지고 남은 상품끼리 다시 비교된다
- **생성 프롬프트**:

  ```
  상품 2~3개를 나란히 비교하는 화면을 만들어줘. 표의 각 열이 상품 하나이고, 맨 위에 사진과 상품명, 가격. 행에는 소요시간, 한국어 가이드 여부, 픽업 포함, 미팅 장소, 취소 가능 시점, 평점과 후기 수를 넣어줘. 값이 서로 다른 행은 배경을 옅게 칠해 눈에 띄게 하고, 각 열 아래에 [예약하기] 버튼을 넣어줘.
  ```

### PR-06 · 예약 마감·품절
- **소속 메뉴**: 상품
- **뒤로가기**: 좌측 상단에 `‹ 상품 목록` — 누르면 `PR-01`로 간다
- **일정**: 2026-09-14 ~ 2026-09-15
- **기능정의**: 마감 사유 안내(좌석 소진·판매 종료·시즌 종료) · 다음 예약 가능일 안내 · 재입고 알림 신청 · 비슷한 상품 4개 추천
- **버튼 → 이동화면**:
  - `비슷한 상품` → `PR-01` 상품 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「알림 신청」 토글` → 켜면 연락 방법 칸이 나타난다
  - `「다른 날짜 보기」` → 달력이 자리 있는 날로 옮겨 가고 잔여가 다시 그려진다
- **생성 프롬프트**:

  ```
  상품이 마감되었을 때의 화면을 만들어줘. 상단에 회색 배너로 "이 날짜는 예약이 마감되었어요" 와 마감 사유 (좌석 소진 / 판매 종료 / 시즌 종료)를 표시. "가장 빠른 예약 가능일은 9월 3일이에요" 안내와 [그 날짜로 보기] 버튼, [자리가 나면 알려주세요] 알림 신청 버튼, 아래에 "비슷한 상품" 4개 카드를 넣어줘.
  ```

### PR-07 · 후기 전체보기
- **소속 메뉴**: 상품
- **뒤로가기**: 좌측 상단에 `‹ 상품 목록` — 누르면 `PR-01`로 간다
- **일정**: 2026-09-16 ~ 2026-09-17
- **기능정의**: 평점 분포 막대그래프 · 필터(별점·사진후기만·최근순) · 후기 카드 목록(사진·별점·본문·여행일·동행유형) · 도움돼요 버튼 · 더보기
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `별점 필터·「사진 있는 후기만」` → 목록과 개수가 함께 걸러진다
  - `「도움돼요」` → 숫자가 하나 오르고 다시 누르면 되돌아간다
  - `긴 후기의 「더 보기」` → 접힌 뒷부분이 펼쳐진다
- **생성 프롬프트**:

  ```
  상품 후기 전체보기 화면을 만들어줘. 상단에 평균 별점과 5점부터 1점까지 분포 막대그래프, 필터 칩(전체 / 사진 후기만 / 5점 / 4점 이하)과 정렬(최신순·도움순). 후기 카드에는 작성자 닉네임, 별점, 여행 날짜, 동행 유형(가족·친구·혼자), 본문, 첨부 사진 썸네일, [도움돼요] 버튼과 카운트를 넣어줘.
  ```

### PR-08 · 찜한 상품
- **소속 메뉴**: 상품
- **뒤로가기**: 좌측 상단에 `‹ 상품 목록` — 누르면 `PR-01`로 간다
- **일정**: 2026-09-18 ~ 2026-09-23
- **기능정의**: 찜한 상품 카드 목록(사진·상품명·별점·가격) · 여행지·카테고리 필터 · 정렬(담은 순·낮은 가격순·할인율순) · 가격 인하 배지와 이전 가격 비교 · 마감임박·품절 상태 표시 · 개별 찜 해제와 선택 삭제 · 장바구니에 담기 · 상품 비교에 추가 · 가격 알림 신청 · 찜한 상품 없을 때 추천 상품 4개
- **버튼 → 이동화면**:
  - `상품 상세` → `PR-02` 상품 상세
  - `날짜 선택` → `PR-03` 날짜·옵션 선택
  - `상품 비교` → `PR-05` 상품 비교
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `찜 해제` → 그 카드가 목록에서 빠지고 「N개」가 줄어든다
  - `정렬·필터` → 찜 목록 순서와 개수가 바뀐다
- **생성 프롬프트**:

  ```
  찜해둔 상품을 모아 보는 화면을 만들어줘. 1) 상단에 "찜한 상품 12개" 와 [선택 삭제] [전체 비우기] 버튼 2) 필터 칩 — 여행지, 카테고리(투어·입장권·교통·패스), 예약 가능한 것만 보기 3) 정렬 드롭다운 — 담은 순 / 낮은 가격순 / 할인율순 4) 상품 카드 — 사진, 상품명, 별점과 후기 수, 가격, 우상단에 채워진 하트(누르면 해제) 5) 담아둔 뒤 가격이 내려간 상품은 "5,000원 내려갔어요" 배지와 이전 가격을 함께 보여줘 6) 마감되었거나 시즌이 끝난 상품은 카드를 흐리게 하고 "예약 마감" 배지와 [비슷한 상품 보기] 링크 7) 카드마다 [날짜 선택] [장바구니 담기] [비교에 추가] 버튼 8) "가격이 내려가면 알려드릴게요" 알림 신청 토글 9) 찜한 상품이 하나도 없으면 일러스트와 "마음에 드는 상품에 하트를 눌러보세요" 문구, 추천 상품 4개 카드를 보여줘 찜 해제는 실수로 누를 수 있으니 [되돌리기] 토스트를 잠깐 띄워줘.
  ```

### CT-01 · 장바구니
- **소속 메뉴**: 장바구니
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-09-24 ~ 2026-09-28
- **기능정의**: 담은 상품 목록(사진·상품명·날짜·인원·옵션·금액) · 개별 선택 체크박스 · 수량·옵션 수정 · 삭제 · 예약 불가 항목 경고 · 금액 요약과 결제 버튼
- **버튼 → 이동화면**:
  - `예약하기` → `BK-01` 여행자 정보 입력
  - `날짜 변경` → `PR-03` 날짜·옵션 선택
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `수량 늘리고 줄이기` → 그 줄 금액과 아래 합계가 다시 계산된다
  - `줄 체크 풀기` → 합계에서 빠지고 「선택한 N개」가 줄어든다
  - `줄 삭제` → 장바구니에서 빠지고 합계가 다시 계산된다
  - `쿠폰 고르기` → 할인 줄과 최종 금액이 다시 계산된다
- **생성 프롬프트**:

  ```
  장바구니 화면을 만들어줘. 담은 상품마다 사진, 상품명, 선택한 날짜와 시간, 인원 구성, 옵션, 금액을 한 줄 카드로. 각 카드에 선택 체크박스, [수정]과 [삭제] 버튼. 날짜가 지났거나 마감된 항목은 카드를 흐리게 하고 "이 날짜는 마감되었어요. 날짜를 바꿔주세요" 경고와 [날짜 변경] 버튼을 넣어줘. 우측(모바일은 하단 고정)에 금액 요약 — 상품 금액, 할인, 총 결제 금액과 [예약하기] 버튼.
  ```

### CT-02 · 장바구니 비어 있음
- **소속 메뉴**: 장바구니
- **뒤로가기**: 좌측 상단에 `‹ 장바구니` — 누르면 `CT-01`로 간다
- **일정**: 2026-09-29 ~ 2026-09-29
- **기능정의**: 빈 상태 일러스트와 안내 문구 · 인기 상품 둘러보기 버튼 · 최근 본 상품 4개
- **버튼 → 이동화면**:
  - `둘러보기` → `PR-01` 상품 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「찜한 상품에서 담기」` → 빈 화면 대신 찜 목록이 그 자리에 펼쳐진다
- **생성 프롬프트**:

  ```
  장바구니에 담은 상품이 없을 때의 화면을 만들어줘. 중앙에 일러스트와 "아직 담은 상품이 없어요" 문구, [인기 상품 둘러보기] 큰 버튼, 아래에 "최근 본 상품" 4개 카드를 가로로 넣어줘.
  ```

### BK-01 · 여행자 정보 입력
- **소속 메뉴**: 예약·결제
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-09-30 ~ 2026-10-01
- **기능정의**: 예약 상품 요약 · 대표 예약자 정보(이름·영문명·연락처·이메일) · 인원별 여행자 정보(영문명·생년월일) · 픽업 호텔 입력 · 요청사항 · 약관 동의
- **버튼 → 이동화면**:
  - `결제하기` → `BK-02` 결제
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `여행자 수 늘리기` → 입력 칸 묶음이 그 수만큼 늘어난다
  - `「대표 여행자와 같음」 체크` → 아래 칸이 자동으로 채워지고 잠긴다. 풀면 다시 입력할 수 있다
  - `여권 번호 입력` → 형식이 맞으면 확인 표시, 틀리면 칸 아래 안내가 뜬다
- **생성 프롬프트**:

  ```
  예약자 정보 입력 화면을 만들어줘. 1) 상단에 예약할 상품 요약 카드 — 상품명, 날짜와 시간, 인원, 금액 2) 대표 예약자 — 한글 이름, 여권 영문명, 휴대폰, 이메일 입력 3) 여행자 정보 — 인원 수만큼 영문명과 생년월일 입력칸을 반복해서 보여주고, "대표 예약자와 동일" 체크박스로 첫 칸은 자동 입력되게 4) 호텔 픽업을 선택했다면 호텔명과 주소 입력칸 5) 요청사항 자유 입력 6) 약관 동의 — 취소·환불 규정 동의(필수), 개인정보 제3자 제공 동의(필수) 영문명은 여권과 같아야 한다는 안내를 입력칸 아래에 작게 넣어줘.
  ```

### BK-02 · 결제
- **소속 메뉴**: 예약·결제
- **뒤로가기**: 좌측 상단에 `‹ 여행자 정보 입력` — 누르면 `BK-01`로 간다
- **일정**: 2026-10-02 ~ 2026-10-06
- **기능정의**: 예약 내역 요약 · 쿠폰·포인트 적용 · 결제 수단 선택(카드·간편결제·해외결제) · 최종 금액 계산 · 환율 안내 · 결제 진행
- **버튼 → 이동화면**:
  - `결제 완료` → `BK-03` 예약 완료
  - `결제 실패` → `BK-04` 결제 실패
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `쿠폰·적립금 적용` → 할인 줄과 최종 결제 금액이 다시 계산된다
  - `결제 수단 고르기` → 고른 것에 표시가 남고 카드면 할부 칸이 나타난다
  - `약관 동의 체크` → 다 체크해야 「결제하기」가 켜진다
- **생성 프롬프트**:

  ```
  결제 화면을 만들어줘. 1) 좌측에 예약 내역 요약 — 상품명, 날짜, 인원, 여행자 이름 2) 쿠폰 선택 드롭다운과 포인트 사용 입력칸, 적용 시 할인 금액이 바로 반영 3) 결제 수단 — 신용카드, 간편결제(카카오페이·네이버페이), 해외 발행 카드 탭 4) 우측(모바일은 하단)에 금액 요약 — 상품 금액, 쿠폰 할인, 포인트 사용, 최종 결제 금액 5) 원화 결제 금액과 함께 "현지 통화 기준 $128" 을 작게 병기 6) [결제하기] 큰 버튼과 결제 진행 중 로딩 상태 결제 버튼을 누른 뒤에는 중복 결제되지 않도록 버튼이 비활성화되게 해줘.
  ```

### BK-03 · 예약 완료
- **소속 메뉴**: 예약·결제
- **뒤로가기**: 좌측 상단에 `‹ 여행자 정보 입력` — 누르면 `BK-01`로 간다
- **일정**: 2026-10-07 ~ 2026-10-08
- **기능정의**: 완료 아이콘과 예약번호 · 예약 상품 요약 · 바우처 발급 안내 · 미팅 장소와 시간 재안내 · 준비물 체크리스트 · 예약 내역 바로가기
- **버튼 → 이동화면**:
  - `바우처 보기` → `VC-02` 바우처 상세
  - `예약 내역` → `MY-03` 예약 내역
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `예약 번호` → 눌러서 복사되고 「복사했어요」가 뜬다
  - `「일정표 보기」` → 날짜별 일정이 그 자리에서 펼쳐진다
- **생성 프롬프트**:

  ```
  예약 완료 화면을 만들어줘. 중앙에 체크 아이콘과 "예약이 확정되었어요", 예약번호를 크게. 아래에 예약 상품 요약 카드(상품명, 날짜와 시간, 인원, 결제 금액), "바우처는 마이페이지에서 바로 확인할 수 있어요" 안내와 [바우처 보기] 버튼, 미팅 장소와 시간을 다시 한 번 박스로 강조, "출발 전 준비물" 체크리스트(여권, 바우처 화면 또는 인쇄물, 편한 신발), 하단에 [예약 내역 보기]와 [다른 상품 둘러보기].
  ```

### BK-04 · 결제 실패
- **소속 메뉴**: 예약·결제
- **뒤로가기**: 좌측 상단에 `‹ 여행자 정보 입력` — 누르면 `BK-01`로 간다
- **일정**: 2026-10-09 ~ 2026-10-12
- **기능정의**: 실패 아이콘과 사유를 쉬운 말로 안내 · 자주 있는 원인 3가지 · 다시 시도 버튼 · 다른 결제 수단 안내 · 고객센터 연결
- **버튼 → 이동화면**:
  - `다시 시도` → `BK-02` 결제
  - `고객센터` → `CS-02` 1:1 문의 작성
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `실패 사유 「자세히」` → 무엇이 문제였는지 펼쳐진다
  - `다른 결제 수단 고르기` → 표시가 옮겨 가고 「다시 시도」 문구가 바뀐다
- **생성 프롬프트**:

  ```
  결제가 실패했을 때의 화면을 만들어줘. 경고 아이콘과 "결제가 완료되지 않았어요" 문구, 실패 사유를 쉬운 문장으로 표시(한도 초과 / 해외결제 차단 / 카드사 승인 거절). "이런 경우가 많아요" 로 원인 3가지를 안내하고, [다시 시도하기] 버튼과 "다른 결제 수단으로 해보기" 링크, 아래에 고객센터 연결 버튼. 결제가 되었는지 불안하지 않도록 "요금은 청구되지 않았습니다" 를 명확히 적어줘.
  ```

### BK-05 · 예약 확정 대기
- **소속 메뉴**: 예약·결제
- **뒤로가기**: 좌측 상단에 `‹ 여행자 정보 입력` — 누르면 `BK-01`로 간다
- **일정**: 2026-10-13 ~ 2026-10-14
- **기능정의**: 대기 상태 안내와 예상 확정 시각 · 왜 대기가 필요한지 설명 · 확정 알림 방법 안내 · 대기 중 취소 시 전액 환불 안내 · 예약 내역 바로가기
- **버튼 → 이동화면**:
  - `예약 내역` → `MY-03` 예약 내역
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「확정 절차」` → 언제 확정되는지 단계가 펼쳐진다
- **생성 프롬프트**:

  ```
  현지 확인이 필요해 예약이 바로 확정되지 않는 상품의 대기 화면을 만들어줘. 시계 아이콘과 "현지 확인 중이에요" 문구, "보통 24시간 안에 확정돼요. 늦어도 8월 3일 오후 6시까지 알려드릴게요" 안내, 왜 대기가 필요한지 한 줄 설명(현지 업체의 좌석 확인이 필요한 상품), 확정되면 카카오톡과 이메일로 알려준다는 안내, "확정 전에 취소하시면 전액 환불돼요" 를 눈에 띄게, [예약 내역 보기] 버튼.
  ```

### BK-06 · 최소 인원 미달 안내
- **소속 메뉴**: 예약·결제
- **뒤로가기**: 좌측 상단에 `‹ 여행자 정보 입력` — 누르면 `BK-01`로 간다
- **일정**: 2026-10-15 ~ 2026-10-16
- **기능정의**: 출발 확정 인원과 현재 인원 표시 · 확정 마감 시각 안내 · 미달 시 전액 환불 안내 · 대체 상품 추천 · 친구 초대 공유
- **버튼 → 이동화면**:
  - `다른 날짜` → `PR-03` 날짜·옵션 선택
  - `비슷한 상품` → `PR-01` 상품 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「알림 신청」 토글` → 켜면 연락 방법 칸이 나타난다
  - `「다른 날짜 보기」` → 달력이 인원이 찬 날로 옮겨 간다
- **생성 프롬프트**:

  ```
  최소 출발 인원을 채우지 못했을 때의 안내 화면을 만들어줘. "아직 출발이 확정되지 않았어요" 문구와 함께 현재 인원 / 최소 출발 인원을 진행 막대로 보여줘(예: 4명 / 6명). "8월 1일 18시까지 인원이 차지 않으면 자동 취소되고 전액 환불돼요" 를 박스로 강조, [친구에게 공유하기] 버튼(같이 가면 확정될 수 있다는 안내), 아래에 "확정된 다른 날짜" 와 "비슷한 상품" 추천을 넣어줘.
  ```

### VC-01 · 바우처 목록
- **소속 메뉴**: 바우처
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-10-19 ~ 2026-10-20
- **기능정의**: 사용 예정·사용 완료·만료 탭 · 바우처 카드(상품명·사용일·QR 미리보기·상태 배지) · 오프라인 저장 안내 · 인쇄·이미지 저장 버튼
- **버튼 → 이동화면**:
  - `바우처 상세` → `VC-02` 바우처 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `사용 전·사용 완료·만료 탭` → 목록과 탭 옆 숫자가 함께 바뀐다
  - `바우처 카드` → QR이 그 자리에서 커진다
- **생성 프롬프트**:

  ```
  내 바우처 목록 화면을 만들어줘. 상단에 탭 — 사용 예정 / 사용 완료 / 만료. 바우처 카드에는 상품 사진, 상품명, 사용 날짜와 시간, 인원, 상태 배지(사용 예정·사용 완료·만료), QR 코드 작은 미리보기. 상단에 "현지에서 인터넷이 안 될 수 있어요. 미리 저장해두세요" 안내 배너와 [이미지로 저장] [인쇄하기] 버튼을 넣어줘.
  ```

### VC-02 · 바우처 상세
- **소속 메뉴**: 바우처
- **뒤로가기**: 좌측 상단에 `‹ 바우처 목록` — 누르면 `VC-01`로 간다
- **일정**: 2026-10-21 ~ 2026-10-22
- **기능정의**: 큰 QR 코드와 예약번호 · 상품명과 이용 일시 · 미팅 장소 지도와 길찾기 · 현지 연락처 · 이용 안내와 주의사항 · 화면 밝기 자동 최대 안내 · 저장·인쇄
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「이용 조건」` → 조건이 펼쳐지고 다시 누르면 접힌다
  - `QR 코드` → 크게 열리고 화면 밝기가 올라간다
- **생성 프롬프트**:

  ```
  현지에서 제시하는 바우처 상세 화면을 만들어줘. 1) 화면 가운데 큰 QR 코드와 그 아래 예약번호(길게 눌러 복사 가능) 2) 상품명, 이용 날짜와 시간, 인원, 대표 예약자 영문명 3) 미팅 장소 — 주소, 지도 썸네일, [길찾기] 버튼 4) 현지 비상 연락처 — 전화 걸기 버튼 5) 이용 안내 — 15분 전 도착, 여권 지참 등 주의사항 6) 하단에 [이미지로 저장] [인쇄하기] QR이 잘 읽히도록 이 화면에서는 배경을 흰색으로 하고, "화면 밝기를 최대로 올려주세요" 안내를 작게 넣어줘.
  ```

### VC-03 · 바우처 만료
- **소속 메뉴**: 바우처
- **뒤로가기**: 좌측 상단에 `‹ 바우처 목록` — 누르면 `VC-01`로 간다
- **일정**: 2026-10-23 ~ 2026-10-26
- **기능정의**: 만료 안내와 만료일 표시 · 만료 사유 설명 · 재발급 가능 여부 안내 · 고객센터 문의 버튼 · 같은 상품 다시 예약
- **버튼 → 이동화면**:
  - `문의하기` → `CS-02` 1:1 문의 작성
  - `다시 예약` → `PR-02` 상품 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「재발급 조건」` → 무엇이 필요한지 펼쳐진다
- **생성 프롬프트**:

  ```
  바우처 사용 기간이 지났을 때의 화면을 만들어줘. 회색 처리된 QR 위에 "만료됨" 도장 느낌의 표시, "이 바우처는 2026년 8월 20일에 만료되었어요" 안내, 왜 만료되었는지 한 줄 설명(이용 기한이 지남 / 이미 사용됨), "기한 연장이나 재발급이 필요하시면 도와드릴게요" 와 [고객센터 문의] 버튼, 아래에 [같은 상품 다시 예약하기] 버튼을 넣어줘.
  ```

### VC-04 · 바우처 재발급 요청
- **소속 메뉴**: 바우처
- **뒤로가기**: 좌측 상단에 `‹ 바우처 목록` — 누르면 `VC-01`로 간다
- **일정**: 2026-10-27 ~ 2026-10-28
- **기능정의**: 재발급 사유 선택 · 예약번호 확인 · 받을 이메일 입력 · 처리 소요시간 안내 · 요청 완료 안내
- **버튼 → 이동화면**:
  - `바우처 목록` → `VC-01` 바우처 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `사유 고르기` → 고른 것에 표시가 남고, 「기타」면 직접 적는 칸이 나타난다
  - `사유를 안 고른 상태` → 「요청」이 눌리지 않는다
- **생성 프롬프트**:

  ```
  바우처 재발급을 요청하는 화면을 만들어줘. 예약 정보를 위에 요약해 보여주고, 재발급 사유를 라디오로 선택(메일을 못 받았어요 / 실수로 지웠어요 / 정보가 잘못됐어요), 사유가 "정보가 잘못됐어요" 면 수정할 내용 입력칸이 펼쳐지게, 받을 이메일 주소 입력(기본값은 가입 이메일), "보통 10분 안에 다시 보내드려요" 안내와 [재발급 요청] 버튼.
  ```

### MY-01 · 로그인
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-10-29 ~ 2026-10-30
- **기능정의**: 이메일·비밀번호 입력 · 소셜 로그인(카카오·구글·애플) · 자동 로그인 · 비밀번호 찾기 · 비회원 예약 조회 링크
- **버튼 → 이동화면**:
  - `회원가입` → `MY-02` 회원가입
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「비밀번호 보기」` → 가려진 글자가 보이고 다시 누르면 가려진다
  - `빈 칸으로 로그인 시도` → 어느 칸이 비었는지 안내가 뜬다
- **생성 프롬프트**:

  ```
  로그인 화면을 만들어줘. 중앙 정렬 카드에 이메일과 비밀번호 입력칸, [로그인] 버튼, 그 아래 소셜 로그인 버튼 3개(카카오·구글·애플)를 각 브랜드 색으로, "자동 로그인" 체크박스와 "비밀번호를 잊으셨나요?" 링크, 맨 아래 "예약번호로 조회하기"(비회원용) 링크를 넣어줘.
  ```

### MY-02 · 회원가입
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `MY-01`로 간다
- **일정**: 2026-11-02 ~ 2026-11-03
- **기능정의**: 이메일·비밀번호·이름·영문명 입력 · 비밀번호 조건 실시간 표시 · 약관 동의(필수·선택 구분) · 마케팅 수신 동의 · 가입 혜택 안내
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `비밀번호 입력` → 세기 막대가 약함→강함으로 바뀐다
  - `약관 「전체 동의」` → 개별 체크가 한꺼번에 켜지고, 하나를 끄면 전체 동의가 풀린다
- **생성 프롬프트**:

  ```
  회원가입 화면을 만들어줘. 이메일, 비밀번호, 비밀번호 확인, 이름, 여권 영문명 입력칸. 비밀번호는 8자 이상·영문과 숫자 포함 조건을 입력하는 동안 체크 표시로 알려주고, 약관 동의는 전체 동의 체크박스와 개별 항목(이용약관 필수, 개인정보 필수, 마케팅 수신 선택)으로 나눠줘. 상단에 "가입하면 첫 예약 10% 쿠폰을 드려요" 배너를 넣어줘.
  ```

### MY-03 · 예약 내역
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `MY-01`로 간다
- **일정**: 2026-11-04 ~ 2026-11-05
- **기능정의**: 상태 탭(예정·완료·취소) · 예약 카드(상품명·날짜·인원·상태 배지·금액) · D-day 표시 · 바우처 바로가기 · 후기 쓰기 유도
- **버튼 → 이동화면**:
  - `예약 상세` → `MY-05` 예약 상세
  - `바우처` → `VC-02` 바우처 상세
  - `후기 쓰기` → `MY-08` 후기 작성
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `예정·완료·취소 탭` → 목록과 탭 옆 숫자가 함께 바뀐다
  - `기간 필터` → 목록이 그 기간 것으로 걸러진다
- **생성 프롬프트**:

  ```
  예약 내역 목록 화면을 만들어줘. 상단 탭 — 이용 예정 / 이용 완료 / 취소. 예약 카드에 상품 사진, 상품명, 이용 날짜와 시간, 인원, 결제 금액, 상태 배지(확정·대기·취소), 출발이 다가오면 "D-3" 를 눈에 띄게. 이용 예정 카드에는 [바우처 보기] [예약 취소] 버튼, 이용 완료 카드에는 [후기 쓰기] 버튼(안 쓴 경우 강조)을 넣어줘.
  ```

### MY-04 · 예약 내역 없음
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `MY-01`로 간다
- **일정**: 2026-11-06 ~ 2026-11-06
- **기능정의**: 빈 상태 일러스트와 안내 · 인기 여행지 바로가기 · 첫 예약 쿠폰 안내
- **버튼 → 이동화면**:
  - `둘러보기` → `HO-01` 홈
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `기간 바꾸기` → 내역이 있으면 빈 화면 대신 목록이 나타난다
- **생성 프롬프트**:

  ```
  예약 내역이 하나도 없을 때의 화면을 만들어줘. 일러스트와 "아직 예약한 여행이 없어요" 문구, "첫 예약에 쓸 수 있는 10% 쿠폰이 있어요" 안내 박스, [인기 여행지 둘러보기] 버튼을 넣어줘.
  ```

### MY-05 · 예약 상세
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `MY-01`로 간다
- **일정**: 2026-11-09 ~ 2026-11-11
- **기능정의**: 예약번호와 상태 · 상품·일정 정보 · 여행자 명단 · 결제 내역(금액·수단·영수증) · 미팅 장소 · 취소·환불 규정 요약 · 취소하기·문의하기 버튼
- **버튼 → 이동화면**:
  - `예약 취소` → `MY-06` 예약 취소 신청
  - `문의하기` → `CS-02` 1:1 문의 작성
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「일정 자세히」` → 날짜별 일정이 펼쳐진다
  - `「여행자 정보」` → 동행자 목록이 펼쳐진다
- **생성 프롬프트**:

  ```
  예약 한 건의 상세 화면을 만들어줘. 1) 상단에 예약번호와 상태 배지, 복사 버튼 2) 상품 정보 — 사진, 상품명, 이용 일시, 소요시간 3) 여행자 명단 — 영문명과 생년월일 목록 4) 결제 내역 — 상품 금액, 할인, 결제 금액, 결제 수단, [영수증 보기] 5) 미팅 장소와 지도, 현지 연락처 6) 취소·환불 규정 요약 — "지금 취소하면 90% 환불" 처럼 현재 기준으로 계산해서 표시 7) 하단에 [예약 취소] [문의하기] [바우처 보기] 버튼
  ```

### MY-06 · 예약 취소 신청
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `MY-01`로 간다
- **일정**: 2026-11-12 ~ 2026-11-13
- **기능정의**: 출발일 기준 환불 수수료 자동 계산 · 환불 예정 금액 표시 · 취소 사유 선택 · 환불 계좌·수단 확인 · 취소 확인 재확인
- **버튼 → 이동화면**:
  - `취소 불가` → `MY-07` 취소 불가 안내
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `취소 사유 고르기` → 고른 것에 표시가 남고 아래 환불 예상 금액이 다시 계산된다
  - `사유를 안 고른 상태` → 「취소 신청」이 눌리지 않는다
- **생성 프롬프트**:

  ```
  예약 취소 신청 화면을 만들어줘. 1) 상단에 취소할 예약 요약 2) 환불 규정 표를 보여주되, 지금 날짜가 어디에 해당하는지 그 줄을 강조 (예: 출발 7일 전까지 100% / 3일 전까지 50% / 이후 환불 불가) 3) 환불 예정 금액을 크게 — 결제 금액, 취소 수수료, 실제 환불 금액을 각 줄로 4) 취소 사유 선택 — 일정 변경 / 다른 상품 예약 / 단순 변심 / 기타(입력) 5) 환불 수단 안내 — "결제하신 카드로 3~5영업일 안에 환불돼요" 6) [취소 신청하기] 버튼을 누르면 "정말 취소하시겠어요?" 확인 창을 한 번 더 띄워줘
  ```

### MY-07 · 취소 불가 안내
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `MY-01`로 간다
- **일정**: 2026-11-16 ~ 2026-11-17
- **기능정의**: 취소 불가 사유와 기준일 안내 · 규정 원문 표시 · 예외 요청 안내 · 고객센터 연결
- **버튼 → 이동화면**:
  - `문의하기` → `CS-02` 1:1 문의 작성
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「규정 보기」` → 왜 못 하는지와 기준일이 펼쳐진다
- **생성 프롬프트**:

  ```
  환불 기한이 지나 취소할 수 없을 때의 화면을 만들어줘. 안내 아이콘과 "이 예약은 취소가 어려워요" 문구, "출발 3일 전(8월 1일)까지만 취소할 수 있어요" 처럼 기준을 명확히 적고, 취소·환불 규정 원문을 접었다 펼 수 있게, "천재지변이나 현지 사정이라면 도와드릴 수 있어요" 안내와 [고객센터 문의] 버튼, 강한 어조 대신 미안한 톤으로 써줘.
  ```

### MY-08 · 후기 작성
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `MY-01`로 간다
- **일정**: 2026-11-18 ~ 2026-11-19
- **기능정의**: 별점 선택 · 세부 항목 평가(가이드·일정·가격만족도) · 본문 입력(글자수 안내) · 사진 첨부 최대 5장 · 동행 유형 선택 · 포인트 지급 안내
- **버튼 → 이동화면**:
  - `예약 내역` → `MY-03` 예약 내역
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `별점` → 누른 별까지 채워지고 안내 문구가 바뀐다
  - `사진 올리기·빼기` → 미리보기가 늘고 줄며 「N/5장」이 따라 바뀐다
  - `항목별 만족도` → 고른 값이 남고 요약 점수가 다시 계산된다
- **생성 프롬프트**:

  ```
  후기 작성 화면을 만들어줘. 1) 상단에 후기를 쓸 상품 요약 2) 전체 별점을 큰 별 5개로 선택 3) 세부 평가 — 가이드, 일정 구성, 가격 대비 만족도를 각각 별점으로 4) 본문 입력 — "어떤 점이 좋았나요?" 안내 문구와 최소 20자 안내 5) 사진 첨부 — 최대 5장, 미리보기 썸네일과 삭제 버튼 6) 동행 유형 선택 — 혼자 / 친구 / 연인 / 가족 7) "사진 후기를 남기면 500포인트를 드려요" 안내와 [등록하기] 버튼
  ```

### MY-09 · 쿠폰함
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `MY-01`로 간다
- **일정**: 2026-11-20 ~ 2026-11-23
- **기능정의**: 사용 가능·사용 완료·만료 탭 · 쿠폰 카드(할인율·최소금액·유효기간·적용 대상) · 만료 임박 강조 · 쿠폰 코드 등록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `사용 가능·사용 완료·만료 탭` → 목록과 숫자가 함께 바뀐다
  - `쿠폰 코드 등록` → 목록 맨 위에 한 장이 늘고 「사용 가능 N장」이 오른다
- **생성 프롬프트**:

  ```
  쿠폰함 화면을 만들어줘. 탭 — 사용 가능 / 사용 완료 / 만료. 쿠폰 카드는 왼쪽에 할인율(10%)이나 금액(5,000원)을 크게, 오른쪽에 쿠폰 이름, 최소 결제 금액, 유효기간, 적용 가능 상품 조건. 유효기간이 7일 이내면 "D-3" 를 붉게 표시하고 카드 테두리를 강조해줘. 상단에 쿠폰 코드 입력칸과 [등록] 버튼을 넣어줘.
  ```

### MY-10 · 내 여정
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `MY-01`로 간다
- **일정**: 2026-11-24 ~ 2026-11-27
- **기능정의**: 여행 단위 카드(도시·기간·예약 건수) · 날짜별 타임라인에 예약을 시간순 배치 · 시간이 겹치는 예약 경고 · 앞뒤 일정 사이 이동 시간 여유 안내 · 현지 시각과 한국 시차 표시 · 빈 시간대에 추천 상품 노출 · 일정별 바우처·예약 상세 바로가기 · 당일 날씨와 준비물 체크 · 동행자에게 일정 공유 · 일정표 이미지·PDF 저장
- **버튼 → 이동화면**:
  - `바우처 보기` → `VC-02` 바우처 상세
  - `예약 상세` → `MY-05` 예약 상세
  - `상품 둘러보기` → `PR-01` 상품 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `날짜 탭` → 그날 일정이 아래에 펼쳐진다
  - `일정 줄 순서 바꾸기` → 순서가 실제로 바뀌고 이동 시간이 다시 계산된다
- **생성 프롬프트**:

  ```
  예약한 상품을 날짜순 일정표로 보여주는 "내 여정" 화면을 만들어줘. 1) 상단에 여행 단위 카드 — 도시 사진, 도시명, 여행 기간(8월 3일~8월 7일), 예약 건수, 여행까지 남은 D-day 2) 그 아래 날짜별 타임라인 — 왼쪽에 시간 축, 오른쪽에 예약 블록을 시작 시각과 소요시간만큼의 길이로 배치 3) 예약 블록에는 상품명, 시간, 집합 장소, 인원, 상태 배지(확정·대기) 4) 시간이 겹치는 예약은 붉은 테두리와 "일정이 겹쳐요" 경고 5) 앞 일정과 뒤 일정 사이가 촉박하면 "이동에 40분 걸려요. 조금 빠듯해요" 안내 6) 날짜 헤더에 현지 시각 기준 표기와 "한국보다 13시간 느려요" 시차 안내, 그날의 날씨 아이콘 7) 비어 있는 시간대에는 옅은 카드로 "이 시간에 갈 만한 곳" 추천 상품 2개 8) 예약 블록을 누르면 [바우처 보기] [예약 상세] 로 이동 9) 하단에 [동행자에게 공유] [일정표 저장(이미지·PDF)] 버튼 여행이 없으면 "예정된 여정이 없어요" 빈 상태로 보여주고, 타임라인은 모바일에서 세로 한 줄로 접히게 해줘.
  ```

### MY-11 · 내 후기
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `MY-01`로 간다
- **일정**: 2026-11-30 ~ 2026-12-02
- **기능정의**: 작성 가능·작성 완료 탭 · 미작성 후기 카드(상품·이용일·작성 기한 D-day) · 작성한 후기 목록(별점·본문·사진·작성일) · 후기 수정과 삭제 · 판매자 답변 표시 · 받은 도움돼요 수 · 후기로 받은 포인트 합계 · 작성 기한 만료 안내 · 후기가 없을 때 빈 상태 · 상품 상세로 이동
- **버튼 → 이동화면**:
  - `후기 쓰기` → `MY-08` 후기 작성
  - `상품 상세` → `PR-02` 상품 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「수정」` → 그 줄이 입력 칸으로 바뀌고 저장하면 되돌아온다
  - `「삭제」` → 확인을 한 번 물은 뒤 그 줄이 목록에서 사라진다
- **생성 프롬프트**:

  ```
  내가 쓴 후기와 쓸 수 있는 후기를 모아 보는 화면을 만들어줘. 1) 상단 탭 — 작성 가능 (3) / 작성 완료 (11) 2) 상단 요약 띠 — "후기로 모은 포인트 2,500P" 와 받은 도움돼요 총 개수 3) 작성 가능 탭 — 이용을 마쳤는데 후기를 안 쓴 예약 카드 (상품 사진, 상품명, 이용일, "작성 기한 D-5", [후기 쓰기] 버튼) 4) 기한이 3일 이내면 D-day를 붉게 표시하고 카드 테두리를 강조 5) 작성 완료 탭 — 내가 쓴 후기 카드(별점, 본문 미리보기, 첨부 사진 썸네일, 작성일, 받은 도움돼요 수) 6) 각 후기에 [수정] [삭제] 버튼, 수정은 작성 후 30일까지만 가능하다고 안내 7) 판매자 답변이 달렸으면 후기 아래에 들여쓴 말풍선으로 표시 8) 작성 기한이 지난 예약은 "작성 기한이 지났어요" 회색 안내로 구분 9) 후기가 하나도 없으면 일러스트와 "첫 후기를 남기면 500포인트를 드려요" 안내 후기 본문은 3줄까지만 보여주고 [더보기] 로 펼치게 해줘.
  ```

### MY-12 · 알림함
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `MY-01`로 간다
- **일정**: 2026-12-03 ~ 2026-12-08
- **기능정의**: 전체·예약·혜택·공지 탭 · 알림 카드(아이콘·제목·본문 요약·받은 시각) · 읽지 않음 점 표시와 모두 읽음 처리 · 알림별 관련 화면 바로가기 · 출발 임박·확정 알림 상단 고정 · 현지 이슈 알림 붉게 강조 · 개별 삭제와 전체 비우기 · 알림 수신 설정 바로가기 · 30일 지난 알림 자동 삭제 안내 · 알림이 없을 때 빈 상태
- **버튼 → 이동화면**:
  - `예약 상세` → `MY-05` 예약 상세
  - `알림 설정` → `MY-13` 계정 설정
  - `공지사항` → `CS-04` 공지사항
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `전체·읽지 않음 탭` → 목록과 숫자가 바뀐다
  - `「모두 읽음」` → 안 읽음 표시가 전부 사라지고 배지 숫자가 0이 된다
- **생성 프롬프트**:

  ```
  받은 알림을 모아 보는 알림함 화면을 만들어줘. 1) 상단 탭 — 전체 / 예약 / 혜택 / 공지, 탭마다 읽지 않은 개수 배지 2) 오른쪽 위에 [모두 읽음] 과 [설정] 아이콘 3) 알림 카드 — 유형 아이콘, 제목, 본문 한두 줄 요약, 받은 시각("2시간 전"), 읽지 않았으면 왼쪽에 파란 점과 배경을 옅게 4) 예약 알림 예시 — "예약이 확정되었어요", "내일 출발이에요. 집합 장소를 확인하세요", "바우처가 발급되었어요" 5) 혜택 알림 예시 — "찜한 상품이 20% 할인 중이에요", "쿠폰이 3일 뒤 만료돼요" 6) 공지·현지 이슈 알림은 붉은 아이콘으로 강조하고 상단에 고정 7) 알림을 누르면 관련 화면(예약 상세, 바우처, 공지)으로 바로 이동 8) 카드를 옆으로 밀면 [삭제], 하단에 [전체 비우기] 9) 맨 아래에 "30일이 지난 알림은 자동으로 사라져요" 안내를 작게 10) 알림이 없으면 일러스트와 "새로운 알림이 없어요" 문구 알림이 많아도 답답하지 않도록 카드 사이 여백을 넉넉히 잡아줘.
  ```

### MY-13 · 계정 설정
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `MY-01`로 간다
- **일정**: 2026-12-09 ~ 2026-12-11
- **기능정의**: 프로필 정보 수정(이름·여권 영문명·연락처) · 이메일 변경과 인증 메일 발송 · 비밀번호 변경(현재·새 비밀번호·조건 표시) · 소셜 계정 연결과 해제 · 알림 수신 설정(카톡·이메일·앱 푸시 개별 토글) · 마케팅 수신 동의와 변경 이력 · 표시 언어와 결제 통화 설정 · 자주 쓰는 여행자 정보 저장·관리 · 내 개인정보 내려받기 · 회원 탈퇴와 유의사항
- **버튼 → 이동화면**:
  - `알림함` → `MY-12` 알림함
  - `문의하기` → `CS-02` 1:1 문의 작성
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「수정」` → 읽는 모양이 입력 칸으로 바뀌고 저장하면 되돌아온다
  - `알림 받기 토글` → 켜고 끈 것이 그대로 남는다
- **생성 프롬프트**:

  ```
  계정 설정 화면을 만들어줘. 왼쪽에 설정 항목 목록, 오른쪽에 내용을 두고 모바일에서는 항목을 세로로 쌓아줘. 1) 프로필 — 이름, 여권 영문명, 휴대폰 번호 수정과 [저장] 버튼, "예약에 쓰인 영문명은 여기서 바꿔도 이미 한 예약에는 반영되지 않아요" 안내 2) 이메일 변경 — 새 이메일 입력 후 인증 메일 발송, 인증 전에는 "확인 대기" 배지 3) 비밀번호 변경 — 현재 비밀번호, 새 비밀번호, 확인, 조건 충족 체크 표시 4) 소셜 계정 연결 — 카카오·구글·애플 각각 연결 여부와 [연결] [해제] 버튼, 마지막 로그인 수단은 해제되지 않게 안내 5) 알림 수신 설정 — 예약 안내 / 혜택·특가 / 공지를 카톡·이메일·앱 푸시별로 토글 6) 마케팅 수신 동의 — 동의 여부와 최근 변경일 표시 7) 표시 설정 — 언어(한국어·영어)와 결제 통화(KRW·USD) 8) 저장된 여행자 정보 — 자주 쓰는 동행자 영문명·생년월일 목록과 추가·삭제 9) 개인정보 내려받기 — 요청하면 메일로 보내준다는 안내 10) 맨 아래에 [회원 탈퇴] 를 작고 옅은 글씨로 두고, 누르면 남은 예약·보유 쿠폰·포인트가 사라진다는 유의사항과 확인 창을 띄워줘
  ```

### CS-01 · 자주 묻는 질문
- **소속 메뉴**: 고객센터
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-12-14 ~ 2026-12-15
- **기능정의**: 카테고리 탭(예약·결제·취소환불·바우처·현지이용) · 검색창 · 아코디언 질문 목록 · 도움됐어요 버튼 · 문의하기 유도
- **버튼 → 이동화면**:
  - `문의하기` → `CS-02` 1:1 문의 작성
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `분류 탭` → 질문 목록이 그 분류 것으로 바뀐다
  - `질문 줄` → 답이 펼쳐지고 다시 누르면 접힌다
  - `검색` → 치는 대로 목록이 걸러진다
- **생성 프롬프트**:

  ```
  자주 묻는 질문 화면을 만들어줘. 상단에 질문 검색창, 카테고리 탭 — 예약 / 결제 / 취소·환불 / 바우처 / 현지 이용. 질문은 아코디언으로 펼쳐지게 하고, 답변 아래에 [도움이 됐어요] [아니요] 버튼을 넣어줘. 맨 아래에 "원하는 답을 못 찾으셨나요?" 와 [1:1 문의하기] 버튼.
  ```

### CS-02 · 1:1 문의 작성
- **소속 메뉴**: 고객센터
- **뒤로가기**: 좌측 상단에 `‹ 자주 묻는 질문` — 누르면 `CS-01`로 간다
- **일정**: 2026-12-16 ~ 2026-12-18
- **기능정의**: 문의 유형 선택 · 관련 예약 선택 · 제목·내용 입력 · 파일 첨부 · 답변 받을 방법 선택 · 예상 답변 시간 안내
- **버튼 → 이동화면**:
  - `문의 내역` → `CS-03` 문의 내역
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `문의 유형 고르기` → 아래 입력 칸이 그 유형에 맞게 바뀐다
  - `파일 올리기·빼기` → 파일 줄이 늘고 준다
  - `필수 칸이 빈 상태` → 「등록」이 눌리지 않는다
- **생성 프롬프트**:

  ```
  1:1 문의 작성 화면을 만들어줘. 문의 유형 선택(예약 변경 / 취소·환불 / 바우처 / 현지 문제 / 기타), "어떤 예약에 대한 문의인가요?" 예약 선택 드롭다운(선택 사항), 제목과 내용 입력, 이미지 파일 첨부(최대 3장), 답변 받을 방법 선택(이메일 / 카카오톡), "보통 영업일 기준 1일 안에 답변드려요. 현지에서 급하신 경우 전화주세요" 안내와 현지 긴급 연락처를 눈에 띄게 넣어줘.
  ```

### CS-03 · 문의 내역
- **소속 메뉴**: 고객센터
- **뒤로가기**: 좌측 상단에 `‹ 자주 묻는 질문` — 누르면 `CS-01`로 간다
- **일정**: 2026-12-21 ~ 2026-12-22
- **기능정의**: 문의 목록(제목·유형·작성일·상태 배지) · 답변 완료 여부 표시 · 문의 상세 펼치기 · 추가 질문 이어쓰기
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `답변 완료·대기 탭` → 목록과 숫자가 바뀐다
  - `문의 줄` → 주고받은 내용이 펼쳐진다
- **생성 프롬프트**:

  ```
  내 문의 내역 화면을 만들어줘. 문의 목록에 제목, 문의 유형 배지, 작성일, 상태(답변 대기 / 답변 완료). 항목을 누르면 문의 내용과 답변이 대화 형태로 펼쳐지고, 아래에 추가 질문을 이어서 쓸 수 있는 입력칸을 넣어줘. 답변이 달린 항목은 목록에서 점 표시로 구분해줘.
  ```

### CS-04 · 공지사항
- **소속 메뉴**: 고객센터
- **뒤로가기**: 좌측 상단에 `‹ 자주 묻는 질문` — 누르면 `CS-01`로 간다
- **일정**: 2026-12-23 ~ 2026-12-24
- **기능정의**: 중요 공지 상단 고정 · 공지 목록(제목·작성일·카테고리) · 상세 펼치기 · 현지 이슈 알림 배너
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `공지 줄` → 본문이 펼쳐지고 다시 누르면 접힌다
  - `분류 필터` → 목록이 걸러진다
- **생성 프롬프트**:

  ```
  공지사항 화면을 만들어줘. 상단에 현재 진행 중인 중요 공지(예: "칸쿤 지역 기상 악화로 일부 투어가 지연되고 있어요")를 눈에 띄는 배너로 고정, 아래에 공지 목록 — 제목, 카테고리 배지(운영·이벤트·현지소식), 작성일. 항목을 누르면 본문이 펼쳐지게 해줘.
  ```

### CS-05 · 취소·환불 규정 안내
- **소속 메뉴**: 고객센터
- **뒤로가기**: 좌측 상단에 `‹ 자주 묻는 질문` — 누르면 `CS-01`로 간다
- **일정**: 2026-12-25 ~ 2026-12-30
- **기능정의**: 상품 유형별 환불 규정 표(투어·입장권·패스) · 출발일 기준 수수료율 단계 · 무료 취소 기한 계산 예시 · 현지 시각 기준이라는 안내 · 기상·천재지변 취소 처리 · 최소 인원 미달 자동 취소와 전액 환불 · 노쇼와 부분 사용 처리 기준 · 환불 소요 기간과 결제 수단별 차이 · 자주 묻는 취소 질문 5개 · 내 예약의 규정 바로 확인
- **버튼 → 이동화면**:
  - `예약 내역` → `MY-03` 예약 내역
  - `자주 묻는 질문` → `CS-01` 자주 묻는 질문
  - `문의하기` → `CS-02` 1:1 문의 작성
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `상품 종류 탭` → 환불 규정 표가 그 종류 것으로 바뀐다
  - `「내 예약으로 계산해 보기」` → 출발일까지 남은 날에 따른 환불액이 계산돼 뜬다
- **생성 프롬프트**:

  ```
  취소와 환불 규정을 한 곳에서 설명하는 안내 화면을 만들어줘. 1) 상단에 "언제까지 취소하면 얼마를 돌려받나요?" 한 줄 요약과 상품 유형 탭 — 투어 / 입장권 / 패스 2) 유형별 환불 규정 표 — 출발 며칠 전이냐에 따른 환불율 (7일 전까지 100% / 3일 전까지 50% / 이후 환불 불가)을 단계 막대와 함께 3) 계산 예시 박스 — "8월 10일 출발이면 8월 3일 밤 12시까지 무료 취소" 처럼 날짜를 넣어 구체적으로 4) "기준 시각은 현지 시각이에요" 를 눈에 띄게 안내 5) 특별한 경우 — 기상 악화·천재지변으로 현지에서 취소되면 전액 환불, 최소 출발 인원 미달로 자동 취소되면 전액 환불 6) 노쇼(예약하고 안 나타남)와 일부 인원만 이용한 경우의 처리 기준 7) 환불 소요 기간 — 카드 3~5영업일, 간편결제 1~3영업일, 해외 카드는 더 걸릴 수 있음 8) 자주 묻는 취소 질문 5개 아코디언 9) 하단에 [내 예약의 규정 확인하기] 와 [1:1 문의하기] 버튼 규정 문서처럼 딱딱하지 않게, 표와 예시를 앞세워 쉽게 읽히도록 써줘.
  ```

## 5. 화면 이동 흐름 (FLOW)
- `홈`(HO-01) —[여행지 보기]→ `여행지 홈`(HO-02)
- `홈`(HO-01) —[검색]→ `검색 결과`(HO-03)
- `홈`(HO-01) —[상품 상세]→ `상품 상세`(PR-02)
- `홈`(HO-01) —[패스 보기]→ `패스 상품 상세`(PR-04)
- `여행지 홈`(HO-02) —[상품 목록]→ `상품 목록`(PR-01)
- `여행지 홈`(HO-02) —[패스 상세]→ `패스 상품 상세`(PR-04)
- `검색 결과`(HO-03) —[상품 상세]→ `상품 상세`(PR-02)
- `검색 결과 없음`(HO-04) —[필터 초기화]→ `검색 결과`(HO-03)
- `테마 기획전`(HO-05) —[상품 상세]→ `상품 상세`(PR-02)
- `테마 기획전`(HO-05) —[전체 상품]→ `상품 목록`(PR-01)
- `상품 목록`(PR-01) —[상품 상세]→ `상품 상세`(PR-02)
- `상품 목록`(PR-01) —[마감 상품]→ `예약 마감·품절`(PR-06)
- `상품 상세`(PR-02) —[날짜 선택]→ `날짜·옵션 선택`(PR-03)
- `상품 상세`(PR-02) —[후기 전체]→ `후기 전체보기`(PR-07)
- `날짜·옵션 선택`(PR-03) —[장바구니 담기]→ `장바구니`(CT-01)
- `날짜·옵션 선택`(PR-03) —[바로 예약]→ `여행자 정보 입력`(BK-01)
- `패스 상품 상세`(PR-04) —[날짜 선택]→ `날짜·옵션 선택`(PR-03)
- `패스 상품 상세`(PR-04) —[명소 상세]→ `상품 상세`(PR-02)
- `상품 비교`(PR-05) —[날짜 선택]→ `날짜·옵션 선택`(PR-03)
- `예약 마감·품절`(PR-06) —[비슷한 상품]→ `상품 목록`(PR-01)
- `찜한 상품`(PR-08) —[상품 상세]→ `상품 상세`(PR-02)
- `찜한 상품`(PR-08) —[날짜 선택]→ `날짜·옵션 선택`(PR-03)
- `찜한 상품`(PR-08) —[상품 비교]→ `상품 비교`(PR-05)
- `장바구니`(CT-01) —[예약하기]→ `여행자 정보 입력`(BK-01)
- `장바구니`(CT-01) —[날짜 변경]→ `날짜·옵션 선택`(PR-03)
- `장바구니 비어 있음`(CT-02) —[둘러보기]→ `상품 목록`(PR-01)
- `여행자 정보 입력`(BK-01) —[결제하기]→ `결제`(BK-02)
- `결제`(BK-02) —[결제 완료]→ `예약 완료`(BK-03)
- `결제`(BK-02) —[결제 실패]→ `결제 실패`(BK-04)
- `예약 완료`(BK-03) —[바우처 보기]→ `바우처 상세`(VC-02)
- `예약 완료`(BK-03) —[예약 내역]→ `예약 내역`(MY-03)
- `결제 실패`(BK-04) —[다시 시도]→ `결제`(BK-02)
- `결제 실패`(BK-04) —[고객센터]→ `1:1 문의 작성`(CS-02)
- `예약 확정 대기`(BK-05) —[예약 내역]→ `예약 내역`(MY-03)
- `최소 인원 미달 안내`(BK-06) —[다른 날짜]→ `날짜·옵션 선택`(PR-03)
- `최소 인원 미달 안내`(BK-06) —[비슷한 상품]→ `상품 목록`(PR-01)
- `바우처 목록`(VC-01) —[바우처 상세]→ `바우처 상세`(VC-02)
- `바우처 만료`(VC-03) —[문의하기]→ `1:1 문의 작성`(CS-02)
- `바우처 만료`(VC-03) —[다시 예약]→ `상품 상세`(PR-02)
- `바우처 재발급 요청`(VC-04) —[바우처 목록]→ `바우처 목록`(VC-01)
- `로그인`(MY-01) —[회원가입]→ `회원가입`(MY-02)
- `예약 내역`(MY-03) —[예약 상세]→ `예약 상세`(MY-05)
- `예약 내역`(MY-03) —[바우처]→ `바우처 상세`(VC-02)
- `예약 내역`(MY-03) —[후기 쓰기]→ `후기 작성`(MY-08)
- `예약 내역 없음`(MY-04) —[둘러보기]→ `홈`(HO-01)
- `예약 상세`(MY-05) —[예약 취소]→ `예약 취소 신청`(MY-06)
- `예약 상세`(MY-05) —[문의하기]→ `1:1 문의 작성`(CS-02)
- `예약 취소 신청`(MY-06) —[취소 불가]→ `취소 불가 안내`(MY-07)
- `취소 불가 안내`(MY-07) —[문의하기]→ `1:1 문의 작성`(CS-02)
- `후기 작성`(MY-08) —[예약 내역]→ `예약 내역`(MY-03)
- `내 여정`(MY-10) —[바우처 보기]→ `바우처 상세`(VC-02)
- `내 여정`(MY-10) —[예약 상세]→ `예약 상세`(MY-05)
- `내 여정`(MY-10) —[상품 둘러보기]→ `상품 목록`(PR-01)
- `내 후기`(MY-11) —[후기 쓰기]→ `후기 작성`(MY-08)
- `내 후기`(MY-11) —[상품 상세]→ `상품 상세`(PR-02)
- `알림함`(MY-12) —[예약 상세]→ `예약 상세`(MY-05)
- `알림함`(MY-12) —[알림 설정]→ `계정 설정`(MY-13)
- `알림함`(MY-12) —[공지사항]→ `공지사항`(CS-04)
- `계정 설정`(MY-13) —[알림함]→ `알림함`(MY-12)
- `계정 설정`(MY-13) —[문의하기]→ `1:1 문의 작성`(CS-02)
- `자주 묻는 질문`(CS-01) —[문의하기]→ `1:1 문의 작성`(CS-02)
- `1:1 문의 작성`(CS-02) —[문의 내역]→ `문의 내역`(CS-03)
- `취소·환불 규정 안내`(CS-05) —[예약 내역]→ `예약 내역`(MY-03)
- `취소·환불 규정 안내`(CS-05) —[자주 묻는 질문]→ `자주 묻는 질문`(CS-01)
- `취소·환불 규정 안내`(CS-05) —[문의하기]→ `1:1 문의 작성`(CS-02)

## 6. 빌드 가이드
### 구조
- 각 **화면ID**를 하나의 라우트/페이지로 만드세요 (예: `PCHOME0110` → `/home`).
- 화면명은 페이지 제목·컴포넌트 이름의 기준으로 사용하세요.
- **2장 공통 요소**(헤더·GNB·푸터)를 공용 레이아웃으로 만들고 모든 화면이 감싸도록 하세요.
- **쓰는 사람이 다른 메뉴는 헤더를 나누세요.** 손님이 쓰는 메뉴(둘러보기·내 내역)와 운영자가 쓰는 메뉴(운영 관리·정산)를 **한 헤더에 나란히 놓지 마세요.** 둘은 사실상 다른 사이트입니다.
  · 손님 영역과 운영자 영역을 각각 다른 레이아웃(헤더·사이드바)으로 만들고, 오갈 때는 **따로 마련한 진입점** 하나로만 이동하세요(예: 우측 상단 `운영 화면으로`, 운영자 화면에서는 `고객 화면 보기`).
  · 운영자 영역은 보통 좌측 사이드바 + 상단 바가 맞습니다. 손님 영역의 검색·카테고리 내비를 그대로 쓰지 마세요.
- **버튼 → 이동화면** 관계를 링크/네비게이션으로 연결하세요.
- **4장 각 화면의 `눌렀을 때 (이 화면 안에서 끝남)` 항목을 그대로 만드세요.** 적혀 있는 것은 빠짐없이 실제로 동작해야 합니다 — 누르면 값이 바뀌고, 다시 누르면 되돌아옵니다.
  · 그 항목에 없더라도 **누를 수 있게 생긴 것은 전부 눌리게** 만드세요. 적힌 것은 최소한이지 전부가 아닙니다.
- **누를 수 있게 보이는 것은 전부 눌리게 만드세요.** 두 종류로 나눠 처리합니다.
  · **화면 안에서 끝나는 조작은 진짜로 동작해야 합니다** — 탭 전환, 좌측 카테고리·필터 선택, 목록에서 항목 고르기(디자이너·옵션·시간), 펼치기·접기, 캘린더 이전/다음 달, 수량 ±, 체크·토글. 자바스크립트로 실제 상태를 바꾸고 화면을 다시 그리세요. 이걸 안내 문구로 때우지 마세요.
  · **서버가 있어야 하는 것만** 짧은 안내(토스트)로 대신합니다 — 저장·삭제·발송·결제·전화 걸기·이의 신청 등.
  · **이런 것은 토스트로 때우지 마세요** — `모두 보기`(펼치기), `선택`·`적용`(고른 상태로 바꾸기), `추가`(줄 하나 늘리기), `닫기`·`✕`(정말 닫기), `도움이 됐어요`(숫자 올리기), 탭·필터·정렬. 전부 화면 안에서 끝나는 일이라 실제로 되어야 합니다.
  · **모달·팝업은 뜨는 것만으로 끝이 아닙니다.** `취소`와 `✕`는 정말 닫히고, `확인`은 눌렀을 때 무엇이 달라지는지 보여야 합니다(다음 화면으로 가거나, 목록이 바뀌거나, 안내가 뜨거나). 닫히지 않는 팝업은 갇힌 화면이 됩니다.
  · 아무 것도 하지 않는 `<button>`을 남기지 마세요. 만들고 나서 **버튼을 하나씩 눌러 보고**, 반응이 없는 것이 있으면 위 둘 중 하나로 채우세요.
- **뒤로가기는 화면마다 정해져 있습니다. 4장 각 화면 스펙의 `뒤로가기` 항목을 그대로 만드세요.** 36개 화면에 붙고, 나머지 7개(메뉴 첫 화면)에는 없습니다. 좌측 상단에 `‹ 상위 화면 이름`과 현재 위치(예: `매장 탐색 › 매장 상세`)를 둡니다.
  · 여러 단계짜리 흐름(1→2→3)은 **이전 단계로 돌아갈 수 있게** 하세요. 되돌아갔을 때 앞서 고른 값이 남아 있어야 합니다.
  · **한 묶음으로 나란히 보여주는 것은 동일 계위입니다.** 탭 줄에 걸린 화면들, 같은 사이드바에 나란히 올라온 화면들이 그렇습니다. 그 안을 오가는 길이 이미 탭·사이드바이므로 뒤로가기를 겹쳐 두지 마세요 — 같은 줄에 있는 화면인데 어떤 건 있고 어떤 건 없어집니다.
  · 빈 화면·오류·마감처럼 막다른 화면에는 **다음에 할 일 버튼**을 반드시 하나 두세요(예: `목록으로`, `다시 시도`, `둘러보기`).
  · **전체 화면 목록으로 돌아가는 길**을 모든 화면에 두되, 이름에 목적이 드러나게 하세요(`화면 목록`). 접힌 패널 안에 `화면 정보` 같은 이름으로 넣으면 아무도 못 찾습니다.
  · 다 만든 뒤 **화면을 하나씩 열어 뒤로가기가 있는지 세어 보세요.** 하나라도 빠져 있으면 채웁니다.
### 화면 구현
- 각 화면의 **생성 프롬프트**를 그 화면 구현 지시로 그대로 사용하세요.
- 반복 UI(카드·리스트 행·폼·버튼)는 **공용 컴포넌트**로 만들어 재사용하세요.
- 필요하면 상태 화면(로딩·빈 결과·에러)도 함께 만드세요.
### 디자인
- **디자인 프리셋 파일을 함께 넣었다면 그것이 우선입니다.** 1장의 디자인 컨셉이나 화면별 프롬프트에 다른 색·톤이 적혀 있어도 프리셋을 따르세요. 충돌이 아니라 **프리셋이 정답**입니다.
- 프리셋의 **레이아웃 골격**(히어로·목록·내비·상세를 어떻게 놓을지)을 먼저 잡고 색을 입히세요. 색만 맞추면 어떤 테마로 만들어도 같은 화면이 나옵니다.
- 프리셋이 없다면 1장의 **디자인 컨셉**을 전역 스타일(색·톤·라운드·여백)로 토큰화해 일관되게 적용하세요.
- **글꼴은 프리셋이 없어도 한 벌로 고정하세요.** 기본은 `Paperlogy`이고, 못 불러오면 `Pretendard` → `Noto Sans KR` 순으로 물러납니다. CSS로는 `font-family: Paperlogy, Pretendard, 'Noto Sans KR', sans-serif` 한 줄이면 됩니다.
  · **글꼴은 한 벌만 씁니다.** 제목과 본문에 서로 다른 글꼴을 쓰지 마세요 — 두 벌이 되는 순간 어디에 무엇을 쓸지가 화면마다 갈립니다. 위계는 글꼴이 아니라 **크기와 굵기**로 만듭니다.
  · 굵기는 **400·600·700 셋만**, 크기는 본문 15 · 카드 제목 18 · 섹션 제목 22 · 페이지 제목 32을 기준으로 잡으세요.

#### 읽기 폭 — 글을 한 단으로 좁힐 때

글이 주인공인 자리(상세 본문·안내·약관)는 **최대 760px** 로 좁히고 가운데 정렬합니다.
콘텐츠 영역(위의 최대 폭) **안에서** 좁히는 것입니다 — 바깥 폭을 바꾸는 게 아닙니다.

**왜 좁히나** — 한 줄이 길면 다음 줄 첫 글자를 눈이 못 찾습니다. 1440px 폭에 본문을 꽉 채우면
한 줄에 90자가 넘어가고, 읽는 사람은 두세 줄마다 같은 줄을 다시 읽게 됩니다.

- **이 값 하나만 씁니다.** 화면마다 680·720·760 처럼 다르게 잡지 마세요 — 저희도 그렇게 적혀 있었고,
  같은 팩 안의 상세 화면끼리 본문 폭이 달라 서로 다른 사이트처럼 보였습니다.
- **카드 격자에는 쓰지 않습니다.** 여기는 «글»을 좁히는 값입니다. 카드를 몇 열로 놓을지는 레이아웃이 정합니다.
- 사진·표·코드처럼 넓어야 읽히는 것은 이 폭을 넘겨도 됩니다. 좁히는 건 글줄입니다.

#### 격자 — 카드를 늘어놓는 자리

카드·타일을 격자로 늘어놓을 때 **좌우와 위아래를 다른 값으로** 벌립니다.

| 자리 | 값 |
| --- | --- |
| **카드와 카드 사이** (좌우) | 위 눈금의 「카드와 카드 사이 (가로)」 |
| **줄과 줄 사이** (위아래) | 위 눈금의 「덩어리와 덩어리 사이」 |

**같은 값을 쓰면 줄이 안 갈립니다.** 카드에 상자가 있을 때는 테두리가 경계를 만들어 줬지만,
상자를 걷어낸 뒤로는 **위 카드의 가격과 아래 카드의 사진이 붙어** 한 덩어리로 읽힙니다.
위아래를 더 벌려야 "여기까지가 한 줄"이 보입니다.

| 나누는 것 | 정하는 곳 |
| --- | --- |
| 몇 열로 나눌지 | 레이아웃 프리셋 |
| **사이를 얼마나 벌릴지** | **위 간격 눈금 (좌우·위아래 따로)** |
| 카드가 어떻게 생겼는지 | 카드 모양 |

- **레이아웃 프리셋이 「넓게」라고 해도 값을 새로 만들지 마세요.** 레이아웃은 *몇 열로 무엇을 어디에*를
  말하는 층이고, *얼마나 벌릴지*는 이 눈금이 정합니다. 실제로 그 한 줄 때문에 40px을 쓴 격자가 있었고,
  같은 화면의 다른 격자는 16px이라 두 값이 나란히 놓였습니다.

**간격은 변수로 못 박고, 자리마다 그것만 씁니다.** CSS 맨 위에 이걸 그대로 붙여 넣으세요.

```css
:root{
  --wrap: 1440px;      /* 콘텐츠 폭 — 헤더·본문·푸터·고정바가 모두 이 값을 쓴다 */
  --pad-x: 24px;    /* 좌우 여백 */
  --card-gap:   16px;     /* 카드와 카드 사이 — 좌우 */
  --card-gap-y: 28px;     /* 카드 줄과 줄 사이 — 위아래 */
  --side-w:     260px;               /* 사이드바 폭 (사이드바 뼈대에서만) */
  --side-gap:   32px;                /* 사이드바와 본문 사이 */
  --row-gap:    14px;                /* 가로 행 카드 안: 썸네일과 글자 사이 */

  --card-pad:   24px;                /* 카드 안쪽 여백 */
  --row-h:      48px;                /* 표 한 줄 높이 — 안 정하면 줄마다 들쭉날쭉해진다 */
  --btn-gap:    8px;                 /* 나란히 놓은 버튼 사이 */
}
@media (max-width: 720px){
  :root{ --pad-x: 16px; --card-gap: 12px; }
}
.wrap { max-width: var(--wrap); margin-inline: auto; padding-inline: var(--pad-x); }
.cards{ display: grid; column-gap: var(--card-gap); row-gap: var(--card-gap-y); }

/* 지표 줄 — 화면 맨 위에 숫자 카드를 나란히 놓는 자리.
   카드 격자(.cards)와 다른 물건이므로 .cards 를 쓰지 않는다. 지표는 넷이 기본이다. */
.stats{ display: grid; grid-template-columns: repeat(4, 1fr);
        column-gap: var(--card-gap); row-gap: var(--card-gap-y); }
@media (max-width: 1024px){ .stats{ grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 720px){ .stats{ grid-template-columns: 1fr; } }

/* 모자이크 — 홈 첫 화면에 「큰 1장 + 작은 4장」을 놓는 자리.
   첫 장이 2칸×2줄, 나머지 넷이 옆을 채운다.
   어느 것을 크게 할지 class 로 정하지 않는다 — 맨 앞에 둔 것이 큰 것이다. */
.mosaic{ display: grid; grid-template-columns: repeat(4, 1fr);
         column-gap: var(--card-gap); row-gap: var(--card-gap-y); }
.mosaic > :first-child{ grid-column: span 2; grid-row: span 2;
                        display: flex; flex-direction: column; }
/* 큰 칸의 사진은 카드 비율을 따르지 않는다 — 두 줄 높이와 안 맞아 제목이 밀려난다.
   큰 칸에서는 사진이 남는 자리를 채운다. */
.mosaic > :first-child > .thumb{ aspect-ratio: auto; flex: 1; min-height: 0; }
@media (max-width: 1024px){
  .mosaic{ grid-template-columns: repeat(2, 1fr); }
  .mosaic > :first-child{ grid-row: auto; }   /* 2열에서 세로로 겹치면 빈칸이 생긴다 */
}
@media (max-width: 720px){
  .mosaic{ grid-template-columns: 1fr; }
  .mosaic > :first-child{ grid-column: auto; }
}

/* 상세 화면의 대표 사진 — 카드 비율을 그대로 쓰지 않는다.
   4:3 을 본문 폭 1392px 에 적용하면 세로가 1044px 이 되어 노트북 화면을 통째로 덮는다.
   가로로 눕히고(16:9) 세로를 420px 로 막는다 — 사진 아래 내용이 같이 보이는 선이다. */
.detail-img{ aspect-ratio: 16 / 9; max-height: 420px; width: 100%;
             border-radius: 12px; overflow: hidden; }
.detail-img > img{ width: 100%; height: 100%; object-fit: cover; display: block; }

/* 간격은 반드시 이 변수로 쓴다. gap:16px 처럼 숫자를 직접 적으면 자리마다 값이 갈린다. */
```

- **`--card-pad`·`--row-h`·`--btn-gap` 도 꼭 쓰세요.** 카드 안쪽 여백, 표 한 줄 높이,
  버튼 사이 간격입니다. 이 셋을 안 정해주면 화면마다 다르게 나옵니다 — 저희도 겪었습니다.
- **`.stats`·`.mosaic`·`.detail-img` 는 카드 격자와 다른 자리입니다.** 지표 숫자 줄,
  홈의 큰 사진 한 장 + 작은 넷, 상세 화면의 대표 사진. 카드 규칙을 여기 갖다 쓰면
  사진이 화면을 다 덮습니다.

- **격자에 `gap: 20px` 처럼 숫자를 직접 쓰지 마세요.** 한 번 쓰는 순간 그 자리만 달라지고,
  나중에 값을 바꿀 때 그 자리만 안 바뀝니다. 실제로 한 사이트에서 좌우가 3·8·16·20·24px
  다섯 가지로 갈라진 적이 있습니다 — 전부 `gap:` 에 숫자를 적어서 생긴 일입니다.
- **`gap` 한 줄로 합치지도 마세요.** 좌우와 위아래는 값이 다르므로 `column-gap`·`row-gap` 둘로 씁니다.
- **이름도 이대로 씁니다.** `--grid-x`·`--gap-col` 처럼 제 나름의 이름을 지으면, 나중에
  다른 사람이 볼 때 그게 같은 자리인지 알 수가 없습니다. 실제로 같은 규칙을 준 두 사이트가
  하나는 `--grid-x/--grid-y`, 하나는 `--gap-col/--gap-row` 를 써서 값이 따로 놀았습니다.
- 값을 바꾸고 싶으면 **`:root` 의 두 줄만** 고칩니다. 그러면 화면 전체가 한 번에 따라옵니다.
- 히어로의 좌우 2단처럼 **카드 격자가 아닌 자리**는 이 규칙 밖입니다. 거기는 화면 뼈대라 레이아웃이 정합니다.

#### 박스형 콘텐츠 — 몇 개를 늘어놓고 사진은 어떤 비율로

사진이 든 카드를 격자로 늘어놓을 때, **몇 개를 놓느냐가 사진 비율을 정합니다.**

| 한 줄에 | 사진 비율 | 왜 |
| --- | --- | --- |
| **2개** | **4:3** | 칸이 넓으니 가로로 넓은 사진 |
| **3개** | **4:3** | 위와 같음 |
| **4개** | **1:1** | 칸이 좁다. 정사각이 가장 얌전하다 |

4개짜리를 세로로 긴 3:4로 뒀더니 **카드 한 줄이 화면을 다 먹었습니다.** 칸이 좁을수록 세로로
길게 두고 싶어지지만, 그러면 한 줄만 보이고 다음 단락이 안 보입니다. 좁은 칸에는 정사각입니다.

- **한 줄에 하나만 두지 않습니다.** 마지막 줄에도 하나만 남기지 마세요 —
  **항목 수를 열 수에 맞춰** 3열이면 3·6·9개, 4열이면 4·8개로 둡니다.
- **한 사이트에서 카드 사진 비율은 하나입니다.** 위 표의 짝(2·3열↔4:3, 4열↔1:1) 밖의 비율은 만들지 마세요.
  같은 홈에 세로 카드와 16:9가 섞여 있어서 **사는 사람이 사진을 두 벌 준비해야 했던** 적이 있습니다.
- **배너·차트·영상·프로필은 이 규칙 밖입니다.** 가로로 긴 배너·영상(16:9)·프로필(1:1)은 제 나름의 비율이 있습니다.
- 상품 사진처럼 **정사각이 업계 표준인 분야**는 1:1을 써도 됩니다. 다만 그때도 한 사이트 안에서는 하나로 갑니다.
- **가로 배너 한 장으로 한 단락을 채우지 마세요.** 배너는 위 표 밖이지만 "한 줄에 하나만 두지 않는다"는 그대로 걸립니다.
  배너 한 장이 있던 자리는 **두 장으로 나누고** 4:3으로 맞추세요.
- **가로로 넘기는 줄(캐러셀)도 몇 개 보이느냐로 셉니다.** 뒤에 더 있어도 눈에 4개가 보이면 4개 — 비율도 1:1입니다.
  카드 폭을 `260px`처럼 박아 두면 화면 폭이 바뀔 때 3.4개씩 어정쩡하게 걸치니, **칸 수로 나눠서** 정하세요.
- **가로로 넘기는 줄에는 좌우 화살표를 두고, 아래 스크롤바는 감춥니다.** 넘길 게 있다는 건
  화살표가 알려 줍니다. 스크롤바까지 나오면 줄 밑에 회색 막대가 하나 더 생겨 지저분합니다.

```css
.carousel{ scrollbar-width:none; -ms-overflow-style:none; }
.carousel::-webkit-scrollbar{ display:none; }
```

  화살표가 **없는데** 스크롤바까지 감추면 넘길 게 있다는 걸 아무도 모릅니다. 둘은 한 벌입니다.

#### 분리형과 혼합형 — 글자를 사진 아래 두느냐 위에 얹느냐

박스형 콘텐츠는 두 가지입니다. **어느 쪽이든 위의 개수·비율 규칙은 똑같이 적용됩니다.**

| | **분리형** | **혼합형** |
| --- | --- | --- |
| 생김새 | 사진 아래에 글자 | 사진 위에 글자를 얹음 |
| 담을 수 있는 것 | 제목·설명·평점·가격까지 여러 줄 | 제목 + 한 줄 정도 |
| 사진이 하는 일 | 무엇인지 알려 준다 | 분위기를 만든다 |
| 쓰는 자리 | 상품·강의 목록처럼 **고르는** 자리 | 특집·기획전처럼 **끌어당기는** 자리 |

**분리형은 상자를 두르지 마세요.** 흰 배경에 테두리·그림자까지 두르면, 사진과 글자가 이미 한 덩어리인데
선을 한 번 더 치는 셈입니다. 격자로 늘어놓으면 그 선이 줄줄이 겹쳐 화면이 답답해집니다.
**모서리는 사진이 가집니다** — 상자를 없앤 자리에서 사진이 카드 노릇을 합니다.
(혼합형은 사진이 곧 배경이라 이 규칙 밖입니다.)

```css
/* 사진 + 글자로 된 카드 — 상자를 두르지 않는다 */
.card{
  background: none;   /* surface 나 흰색을 깔지 않는다 */
  border: 0;
  box-shadow: none;
}
.card .thumb{ border-radius: 12px; }  /* 모서리는 사진이 가진다 */
.card .body{ padding: 16px 0 0; }     /* 좌우 여백 0 — 상자가 없으니 안쪽 여백도 없다 */
```

**`background: var(--surface)` 나 `border: 1px solid` 를 카드에 쓰면 이 규칙을 어긴 것입니다.**
사진이 없는 상자(설정 패널·안내 상자·표를 감싼 것)는 이 규칙 밖입니다 — 거기는 배경이 있어야
덩어리로 읽힙니다. **사진과 글자로 된 카드**만 걷어냅니다.

상자를 걷어내면 두 가지가 따라옵니다.

- **글자가 흰색이 아니라 페이지 배경 위**에 놓입니다. 대비를 다시 재세요.
- **카드끼리 갈라 주던 테두리가 없어집니다.** 줄과 줄 사이를 더 벌려 그 몫을 대신하세요.

그리고 **카드 안에는 선을 긋지 마세요.** 가격 위에 선을 두면 상자가 없는 상태에서는 그 선이 카드를
가르는 것처럼 보여, 사진·제목과 가격이 따로 노는 두 덩어리로 읽힙니다.
**갈라야 할 것은 카드 안이 아니라 카드끼리**입니다.

혼합형을 쓸 때 반드시 지킬 것:

- **어둠막을 까세요.** 사진 위에 흰 글자를 그냥 얹으면 사진의 밝은 자리에서 글자가 사라집니다.
  **사진은 사는 분이 바꿀 것**이라 우리가 밝기를 미리 알 수 없습니다.
  글자 쪽에서 반대편으로 옅어지는 어둠막을 항상 깔아 두면 어떤 사진이 와도 읽힙니다.
- **글자 대비 4.5 이상**을 지키세요. 가장 밝은 사진(흰색)이 왔다고 치고 재세요.
- **글자를 짧게 쓰세요.** 제목 한 줄 + 설명 한 줄까지입니다. 그보다 길면 사진을 다 덮어 혼합형인 의미가 없습니다.
- **가격·평점처럼 정확히 읽어야 하는 것은 얹지 마세요.** 사진 위에서는 숫자가 잘 안 읽힙니다.
- **한 화면에 두 가지를 섞어도 됩니다.** 위는 혼합형 특집, 아래는 분리형 목록 — 하는 일이 다르므로 어수선하지 않습니다.

#### 글꼴은 한 벌입니다

**기본은 `Paperlogy`**, 못 불러오면 `Pretendard` → `Noto Sans KR` 순으로 물러납니다.
CSS로는 `font-family: Paperlogy, Pretendard, 'Noto Sans KR', sans-serif` 한 줄입니다.

- **제목과 본문에 다른 글꼴을 쓰지 마세요.** 두 벌이 되는 순간 어디에 무엇을 쓸지가
  화면마다 갈립니다. 처음엔 「제목만 다르게」로 시작해서, 나중엔 세 벌 네 벌이 됩니다.
- **위계는 글꼴이 아니라 크기와 굵기로 만듭니다.** 굵기는 400·600·700 셋만 쓰세요.
- 크기는 본문 15 · 카드 제목 18 · 섹션 제목 22 · 페이지 제목 32이 기준입니다.
- **숫자가 많은 표에는 고정폭 숫자**(`font-variant-numeric: tabular-nums`)를 켜세요.
  안 켜면 값이 바뀔 때 자릿수가 흔들려 칸이 들썩입니다.

#### 움직임 — 바뀌었다는 걸 눈이 알아채게

값이 바뀌었는데 **딱 하고 갈아 끼우면 사람은 그걸 못 봅니다.** 숫자가 12에서 3으로
바뀌어도, 눈이 그 자리를 안 보고 있었으면 바뀐 줄 모릅니다.
움직임은 꾸미개가 아니라 **「방금 여기가 바뀌었어요」라고 알려 주는 장치**입니다.

| 언제 | 얼마나 | 무엇이 |
| --- | --- | --- |
| 마우스를 올렸을 때 | **0.12초** | 색·테두리 |
| 눌러서 값이 바뀔 때 | **0.2초** | 사라짐·나타남·자리 옮김 |
| 접히고 펴질 때 | **0.25초** | 높이 |
| 화면 위에 뜰 때 (모달·시트) | **0.25초** | 옅어짐 + 살짝 올라옴 |
| 안내 문구(토스트) | **0.2초 등장 · 3초 머무름** | 옅어짐 |

- **가속은 하나로 씁니다.** `ease-out`(빨리 시작해 부드럽게 멈춤)이 기본입니다.
  자리마다 다른 가속을 쓰면 같은 사이트에서 두 사람이 만든 것처럼 보입니다.
- **0.3초를 넘기지 마세요.** 그보다 길면 기다리는 느낌이 듭니다. 목록이 다시 그려질 때는
  **0.15초**면 충분합니다.
- **자리를 밀어내며 움직이지 마세요.** 아래 내용이 출렁이면 읽던 곳을 잃습니다.
  옅어지거나 제자리에서 바뀌게 하세요.
- **한 번에 한 곳만 움직입니다.** 눌렀는데 화면 여기저기가 같이 움직이면 어디가 바뀐 건지 모릅니다.
- **끝없이 도는 움직임은 두지 않습니다.** 배너 자동 넘김·깜빡임은 읽는 걸 방해합니다.
  기다리는 동안 도는 것(로딩)만 예외입니다.
- **움직임을 꺼 둔 분을 존중하세요.** `prefers-reduced-motion` 이 켜져 있으면 전부 0초로 둡니다.
  어지럼증 때문에 꺼 두신 분들이 있습니다. 값은 바뀌되 움직이지만 않으면 됩니다.

#### 떠 있는 부품과 줄 안에 서는 부품은 다른 것입니다

사진 위에 **떠 있는** 동그란 찜 아이콘과, 버튼 줄 안에 **나란히 서는** 찜 버튼은
생김새가 비슷해 보여도 다른 부품입니다. 떠 있는 쪽은 자리를 빠져나가도록(`position:absolute`)
만들어져 있어서, 그 이름을 줄 안의 버튼에 붙이면 **줄을 빠져나가 엉뚱한 데 가서 앉습니다.**
실제로 찜 버튼이 가격 위에 겹쳐 앉은 적이 있습니다.

- **이름이 자리를 뜻하면 그 자리에서만 쓰세요.** 「사진 위 찜」과 「줄 안 아이콘 버튼」은
  이름을 나눠 두고, 섞어 쓰지 않습니다.
- **부품 이름과 실제 쓰임이 어긋나면 이름을 고치세요.** 「타임라인」이라 지어 놓고
  여섯 곳 모두 한 줄짜리 목록으로 쓰고 있던 적이 있습니다. 세로 점만 남아 글자 사이에 끼었습니다.
- **아이콘만으로 뜻이 통하면 글자를 빼세요.** 하트·공유·닫기가 그렇습니다.
  다만 **`aria-label`로 이름은 남깁니다** — 눈으로 못 보는 분에게는 그게 유일한 이름입니다.
- 반대로 **아이콘만으로 못 알아보는 것에는 글자를 붙이세요.** 「빼기」·「내보내기」처럼
  모양이 여러 뜻으로 읽히는 것이 그렇습니다.

#### 쓰는 화면이 있으면 보여주는 자리도 있어야 합니다

글을 쓰게 해 놓고 그 글이 나올 자리를 안 만드는 일이 잦습니다.
실제로 강사가 **1,240자짜리 강의 소개를 쓰는 화면**은 있는데, 상세 화면에는 그 글이
나올 자리가 없었습니다. 게다가 검수 화면은 "강의 소개 1,240자 — 확인됨"이라고까지 말하고 있었습니다.

- **입력 칸을 만들 때마다 "이건 어디에 보이나"를 한 번 물으세요.** 답이 없으면 둘 중 하나입니다 —
  보여줄 자리를 만들거나, 그 입력 칸을 빼거나.
- **에디터(굵게·목록·이미지 버튼이 붙은 칸)로 쓴 글**은 보여주는 쪽에도 그 모양이 살아야 합니다.
  제목·목록·굵게를 쓸 수 있게 해 놓고 보여줄 때 한 덩어리 문단으로 뭉개면, 쓴 사람이 헛수고합니다.
- **한 줄 요약과 본문 소개는 다른 것입니다.** 목록 카드에 들어가는 한 줄과, 상세에서 읽는 본문은
  자리도 길이도 다릅니다. 하나로 합치지 마세요.

#### 권장 크기는 실제 자리와 같아야 합니다

"권장 1280×720 (16:9)"이라고 적어 두고 옆의 자리는 4:3인 화면이 있었습니다.
**사는 분은 글자를 믿고 그 크기로 사진을 준비**하는데, 올려 보면 잘립니다.

- 안내 문구의 숫자와 **바로 옆 자리의 비율이 같은지** 확인하세요.
- 검수·확인 화면에 크기가 또 적힌다면 **거기도 같은 숫자**여야 합니다. 세 곳이 따로 놀기 쉽습니다.
- **영상만 16:9입니다.** 썸네일·카드 사진은 위 표(2·3개↔4:3, 4개↔1:1)를 따릅니다.

#### 차트 — 이미지가 아니라 데이터로 그립니다

추이·막대·도넛 같은 차트를 **이미지 자리로 두지 마세요.** `이미지 영역 (라인 차트 · 권장 1200×420)`
이라고 적어 두면 사는 분은 차트를 **그림 파일로 만들어 올려야 하는 줄** 압니다.
차트는 숫자가 바뀌면 따라 바뀌어야 하는 것이라, 사진 자리와 성격이 다릅니다.

- **가벼운 것은 라이브러리 없이 SVG로 그리세요.** 선 하나·막대 열두 개짜리는 30줄이면 됩니다.
  바깥에서 아무것도 안 불러오니 **인터넷 없이 폴더에서 열어도** 그대로 보입니다.

**대시보드를 만든다면 최소한 이 셋은 갖추세요.**

| 무엇 | 어디에 | 왜 |
| --- | --- | --- |
| **큰 추이 차트** | 화면에 한둘 | 기간을 놓고 흐름을 본다 |
| **스파크라인** (작은 추이선) | 지표 카드 안 | 숫자 옆에서 방향만 알려 준다 |
| **증감 표시** (▴ 812) | 지표 카드 안 | 좋아졌는지 나빠졌는지 |

숫자 하나만 두면 **좋은지 나쁜지를 못 말합니다.** "10,374명"만 있으면 늘고 있는지
줄고 있는지 알 수 없어서, 사는 분은 그 카드를 보고 아무 판단도 못 합니다.
지표 카드는 **숫자 + 증감 + 추이선**이 한 벌입니다.

- **본격적인 차트가 필요하면 무료(MIT) 라이브러리를 쓰세요.** 값을 치르지 않아도 되고 상업적으로 써도 됩니다.

| 라이브러리 | 크기 | 언제 |
| --- | --- | --- |
| **Chart.js** | ~200KB | 무난한 첫 선택. 자료가 많아 AI도 정확하게 짭니다 |
| **ApexCharts** | ~500KB | 기본 모양이 예쁘고 종류가 많을 때 |
| **uPlot** | ~40KB | 점이 수천 개라 가벼워야 할 때 |
| **Frappe Charts** | ~30KB | 작고 산뜻한 SVG면 충분할 때 |

- **색은 반드시 프리셋 색을 쓰세요.** 라이브러리 기본 팔레트(빨강·노랑·초록)를 그대로 두면
  그 자리만 다른 사이트처럼 보입니다.
- **눈금선은 옅게, 선은 진하게.** 눈금이 데이터보다 진하면 숫자가 안 읽힙니다.
- 라이브러리를 쓰면 **인터넷이 필요해집니다.** 오프라인으로 열어 볼 화면이라면 파일을 함께 받아 두거나 SVG로 그리세요.

#### 가격 표기 — 낼 돈부터

**판매가 · 정가 · 할인율** 순서로 한 줄에 세웁니다.

| 자리 | 모양 | 크기 |
| --- | --- | --- |
| **판매가** (낼 돈) | 진한 기본 글자색, 굵게 | 18 (상세는 28) |
| **정가** | 회색 + 취소선 | 13 |
| **할인율** | **강조색 글자**, 굵게 | 14 |

- **할인율을 앞에 두지 마세요.** 눈이 먼저 "20%"에 걸려 정작 얼마인지를 뒤늦게 찾습니다.
  사는 분이 알고 싶은 건 **낼 돈**입니다.
- **할인율은 배지가 아니라 색 글자입니다.** 배지로 두면 정가·판매가와 무게가 비슷해져 셋이 서로 다툽니다.
  배지 자리는 `입문`·`NEW` 같은 **꼬리표 몫**입니다.
- **셋의 크기를 벌려 두세요.** 비슷하면 무엇을 먼저 읽어야 할지 눈이 헤맵니다.
- **강조색 원색을 글자로 쓰지 마세요.** 주황·코랄 원색은 흰 배경에서 3:1을 못 넘깁니다.
  글자용으로 한 단계 어두운 색을 따로 두세요.
- 가격 묶음은 **한 곳에서 만들어 쓰세요.** 자리마다 손으로 짜면 순서가 갈립니다.

#### 버튼이냐 링크냐

**다른 화면으로 데려가면 링크, 이 화면 안에서 끝나면 버튼입니다.**

| 하는 일 | 무엇으로 | 눌렀을 때 |
| --- | --- | --- |
| 다른 화면으로 간다 | 링크 `<a href>` | 그 화면이 열린다 |
| 이 화면 안에서 끝난다 | 버튼 `<button>` | 안내 문구가 뜬다 |

- 필터 적용·정렬·검색·다음 차시·시간 지점 이동은 **화면이 바뀌지 않는 조작**입니다.
  링크로 만들면 **자기 자신으로 가는 링크**가 되어, 눌러도 같은 화면이 다시 열립니다.
  사는 분 눈에는 *"눌렀는데 아무 일도 안 나네"* 로 보입니다.
- **나란히 선 버튼끼리는 종류를 섞지 마세요.** 하나는 화면이 넘어가고 하나는 안 넘어가면
  규칙이 없어 보입니다.

**그리고 화면 안에서 끝나는 조작은 진짜로 값이 바뀌어야 합니다.**
눌러도 숫자가 그대로면, 사는 분 눈에는 **눌리지 않는 포스터**입니다.

| 눌렀을 때 | 바뀌어야 하는 것 |
| --- | --- |
| 기간 탭 (7일·30일·90일) | 차트와 그 옆 요약 숫자 |
| 쿠폰·옵션 고르기 | 결제 금액 줄과 합계 |
| 수량 늘리기 | 금액 |
| 정렬·필터 | 목록 순서나 개수 |

- **미리 만들어 두고 바꿔 끼세요.** 차트 셋을 미리 그려 두고 탭에 따라 보여 주는 편이,
  누를 때마다 자바스크립트로 다시 그리는 것보다 낫습니다 — 그리는 규칙이 두 곳으로 갈라지지 않습니다.
- **계산은 한 곳에서만 하세요.** 합계를 화면마다 손으로 적어 두면 쿠폰을 바꿨을 때 한 군데만 바뀝니다.
- **탭에 갈 곳도 바꿀 것도 없으면 탭이 아닙니다.** 그냥 제목이거나, 아직 안 만든 자리입니다.

**같은 길을 두 번 두지 마세요 — 위 카테고리 줄과 왼쪽 LNB.**
둘이 같은 곳을 가리키면 위쪽이 두 줄로 두꺼워지고, 어느 쪽을 눌러야 할지 잠깐 멈칫합니다.

| 상태 | 왼쪽 LNB | 위 카테고리 줄 |
| --- | --- | --- |
| 맨 위 (LNB 보임) | 보인다 | **접힌다** |
| 스크롤해서 LNB가 위로 사라짐 | 안 보인다 | **내려온다** |
| LNB가 아예 없는 화면 | — | **늘 보인다** |

- **LNB가 있는 화면은 접힌 채로 시작하세요.** 펼쳐 두었다가 나중에 접으면 첫 화면에서 한 번 번쩍입니다.
- **LNB가 없는 화면에서는 절대 접지 마세요.** 접으면 갈 길이 통째로 사라집니다.
- **기준은 "LNB가 붙박이 머리말 아래로 사라지는 순간"입니다.** 머리말 높이만큼 위를 깎아서 재세요.
- 같은 규칙을 **LNB를 쓰는 모든 레이아웃에 똑같이** 거세요. 화면마다 다르면 위쪽 높이가 제멋대로 바뀌어 보입니다.

- **견본으로 가는 버튼을 화면 안에 두지 마세요.** `결과가 없을 때` `대화가 없을 때는 이렇게 보여요`
  처럼 예외 화면을 보여주려고 넣은 버튼·단락은 진짜 사이트에 없는 것입니다.
  검색창 옆에 그런 버튼이 서 있으면 사는 분은 그게 기능인 줄 압니다.
  빈 화면·오류 화면은 **화면 목록에 따로 한 장**으로 두세요 — 거기서 눌러 보면 됩니다.

#### 화면 목록으로 가는 길은 한 곳에만

만든 화면을 훑어보려면 목록이 필요합니다. 그런데 그 목록을 **화면 안에 늘어놓으면**
사는 분 눈에는 사이트의 일부로 보입니다.

```
이렇게 두지 마세요 — 화면 위쪽에 줄줄이
  [CO-01 · 강의 목록] [CO-02 · 검색 결과 없음] [CO-03 · 강의 상세] [CO-04 · 결제] …
```

**진짜 사이트에는 이런 줄이 없습니다.** `CO-02 · 검색 결과 없음` 은 만드는 사람의 말이지
쓰는 사람의 말이 아닙니다. 화면 ID 가 손님 눈에 보이는 것부터가 어긋납니다.

- **한 귀퉁이에 작게 하나만 둡니다.** 오른쪽 아래에 떠 있는 `화면 목록` 단추 하나면 됩니다.
  누르면 `index.html` 로 갑니다.
- **그 단추는 화면 내용 위에 떠 있게** 두고, 본문 흐름에는 끼워 넣지 않습니다.
  본문에 들어가면 그것도 사이트의 일부가 됩니다.
- **화면 ID 를 손님이 보는 자리에 쓰지 않습니다.** 제목·탭·빵부스러기 어디에도요.
  ID 는 우리가 화면을 세는 이름이지 서비스의 말이 아닙니다.
- 화면끼리 오가는 길은 **그 서비스의 진짜 메뉴와 버튼**으로 만듭니다.
  「강의 목록 → 강의 상세」는 강의 카드를 눌러서 가지, 탭으로 가지 않습니다.

### 이미지 · 썸네일
- 아직 사진이 없으므로 이미지 자리는 **테마 색이 아니라 옅은 파스텔**로 채우세요. 테마 색으로 칠하면 화면이 그 색 덩어리로 뒤덮여 디자인이 안 보입니다.
- 배경 `#EEF2F7` · `#F2EFF7` · `#EDF4F1` · `#F7F1EC` · `#F2F4F7` 중 하나를 카드마다 돌려 쓰고, 테두리 `#E3E8F0` 1px, 글자색 `#8B94A6` 13px.
- 자리 안에 `이미지 영역 (무엇 · 권장 가로×세로)` 형식으로 적으세요. 예: `이미지 영역 (매장 대표 · 권장 1200×900)` · `이미지 영역 (프로필 · 권장 400×400)` · `이미지 영역 (배너 · 권장 1600×600)`
- 이미지 자리는 테마 색으로 칠하지 않는다. 옅은 파스텔 한 톤 + 1px 테두리로 둔다.
- 자리 안에 무엇이 들어갈 이미지인지와 권장 크기를 글자로 적는다.
- 적어 둔 크기의 **비율을 실제로 지킨다** — `400×400`이라 적었으면 정사각으로 보여야 한다(aspect-ratio).
- 특히 프로필·썸네일처럼 크기가 고정된 자리는, 가로로 나란히 놓아도 늘어나지 않게 한다. 목록·행 안에 넣을 때 `flex`가 잡아당겨 타원이 되는 일이 잦으니, 만든 뒤 실제로 열어 모양을 확인한다.
- 썸네일처럼 작아서 글자가 안 들어가면 크기만 적거나 아이콘 하나만 둔다.
- 실제 사진을 넣기 전까지 이 규칙을 유지한다 — 그래야 어디에 무엇을 넣어야 하는지 보인다.
### 파일을 어디에 두나

```
완성화면/
  index.html              전체 화면 목록 (여기서 화면으로 들어간다)
  pages/
    HO-01.html            화면 하나에 파일 하나 · 이름은 화면 ID 그대로
    CO-03.html
  assets/
    css/style.css         스타일 한 벌
    js/app.js             동작 한 벌
```

- **`pages/` 안에서는 `../assets/css/style.css` 로 부릅니다.** `assets/...` 라고 쓰면 `pages/assets/...` 를 찾다가 못 찾아서 **스타일이 통째로 안 붙습니다** — 글꼴도 여백도 격자도 전부 날아갑니다.
- **`index.html` 은 `완성화면/` 바로 아래**에 둡니다. 거기서는 `assets/...` 그대로 부르면 됩니다.
- **스타일과 동작은 각각 한 벌**입니다. 화면마다 따로 만들면 44장이 서로 달라집니다.
- 화면끼리 오갈 때는 `CO-03.html` 처럼 **같은 폴더 안 이름**으로 겁니다.
- 다 만든 뒤 **화면 하나를 브라우저로 직접 열어 보세요.** 글꼴이 기본 고딕으로 보이면 경로가 틀린 것입니다.

### 추천 스택
- 정적 다중 페이지(HTML/CSS) 또는 컴포넌트 기반(React·Next.js·Vue 등) 중 프로젝트 규모에 맞게 선택하세요. 화면이 많고 상태가 복잡하면 컴포넌트 기반을 권장합니다.
