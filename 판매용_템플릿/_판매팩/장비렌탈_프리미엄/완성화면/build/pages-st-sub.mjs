/* ST 정산 — 3뎁스 잎사귀 15장 */
import * as U from './ui.mjs';
import { 잎 } from './sub.mjs';
import { GEAR, 정산항목, 면책액, 연체료, SITE } from './data.mjs';

const P = {};
export default P;
const G = GEAR[0];
const 보증금 = 200_000;
const 청구 = 정산항목.reduce((a, x) => a + x.amt, 0) - 면책액;
const 환급 = 보증금 - 청구;
const 점검 = ['ST0101', '점검 중'];
const 정산 = ['ST0201', '정산 내역'];
const 환급화면 = ['ST0301', '보증금 환급'];
const 완료 = ['ST0401', '환급 완료'];

P['ST0102'] = () => 잎({
  부모: 점검, 제목: '단계마다 무엇을 보나',
  설명: '점검이 «무엇을 하는 시간»인지 알려드립니다.',
  본문: `${U.card('', U.accordion([
  { q: '<span class="grow row-b"><span><b>① 반납 접수</b></span><span class="badge b-ok">끝</span></span>',
    a: '<p class="t-sub">돌려주신 시각을 기록합니다. <b>이 시각으로 연체 여부가 정해집니다.</b> (8/18 09:14 접수 — 기한보다 1일 늦음)</p>' },
  { q: '<span class="grow row-b"><span><b>② 구성품 확인</b></span><span class="badge b-ok">끝</span></span>',
    a: '<p class="t-sub">목록대로 다 있는지 하나씩 셉니다. 폴대 1개가 없는 것으로 확인됐습니다. 나중에 찾아 가져오시면 30일 안에는 돌려드립니다.</p>' },
  { q: '<span class="grow row-b"><span><b>③ 상태 점검</b></span><span class="badge b-warn">진행 중</span></span>',
    a: `<p class="t-sub">찢어짐·부러짐·오염을 봅니다. <b>받으실 때 사진과 견줍니다.</b>
      미리 신고해 주신 부분은 따로 표시하고, 수리로 될 것 같으면 수리비만 받습니다.</p>` },
  { q: '<span class="grow row-b"><span><b>④ 정산 확정</b></span><span class="badge b-mut">아직</span></span>',
    a: '<p class="t-sub">청구할 금액을 확정하고 보증금에서 뺍니다. 확정 뒤 <b>7일 동안</b> 이의를 제기하실 수 있습니다.</p>' },
], 2))}
${U.banner('info', '👀', '<b>점검하는 사람도 사진을 보고 판단합니다.</b> 받으실 때 사진을 남겨 두셨으면 이 단계가 훨씬 빠르고 정확해요.', { cls: 'mt6' })}`,
  다루는것: [
    ['각 단계', '무엇을 보는지 · 지금 결과가 어떤지'],
    ['끝난 단계', '시각과 결과를 함께 남긴다'],
    ['진행 중', '예상 완료 시각을 보여준다'],
    ['왜 알리나', '「기다리라」만 하면 불안하다'],
  ],
});

P['ST0103'] = () => 잎({
  부모: 점검, 제목: '점검이 늦어질 때',
  설명: '저희가 늦은 것을 손님이 기다리실 이유는 없습니다.',
  본문: `${U.banner('warn', '⏳', '<b>점검이 예정보다 늦어지고 있습니다.</b> 죄송합니다 — 연휴 뒤라 돌아온 장비가 많습니다.')}
${U.card('', `<div class="row-b mb3"><span>자동 처리까지</span><b class="num pri">14시간 남음</b></div>
  ${U.progress(70)}
  <p class="t-sub mt4">접수 8/18 09:14 → 자동 처리 8/20 09:14</p>`, { cls: 'mt6' })}
${U.card('2영업일이 넘으면', `${U.banner('ok', '🤝', '<b>자동으로 「이상 없음」 처리되고 보증금이 전액 환급됩니다.</b>')}
  <p class="t-sub mt4">저희 사정으로 늦어진 것인데 손님이 기다리시게 두는 것은 맞지 않습니다.
    그래서 <b>2영업일</b>을 넘기면 저희가 손해를 보더라도 넘어갑니다.</p>
  <p class="t-sub mt3">다만 <b>파손이 이미 확인된 건</b>은 자동 처리되지 않습니다. 그때는 개별로 연락드려요.</p>`, { cls: 'mt6' })}
${U.card('지금 하실 수 있는 것', `<div class="btns">
  ${U.btn('그냥 기다릴게요', { cls: 'btn-pri', attr: ' data-toast="끝나면 문자로 알려드릴게요"' })}
  ${U.btn('전화로 물어보기', { attr: ` data-toast="${SITE.tel}"` })}
  ${U.btn('내 대여로', { href: 'MY0201' })}
</div>`, { cls: 'mt6' })}`,
  다루는것: [
    ['자동 처리', '2영업일 초과 시 이상 없음으로 넘긴다'],
    ['왜', '저희 사정으로 늦은 것을 손님이 부담하면 안 된다'],
    ['예외', '파손이 이미 확인된 건은 개별 연락'],
    ['남은 시간', '언제 자동 처리되는지 막대로'],
  ],
});

P['ST0104'] = () => 잎({
  부모: 점검, 제목: '아무 문제가 없을 때',
  설명: '가장 많은 경우입니다. 짧고 분명하게 알려드립니다.',
  본문: `${U.banner('ok', '✓', '<b>이상 없습니다.</b> 구성품도 다 있고 상태도 깨끗했어요. 고맙습니다.')}
${U.card('', `<div style="text-align:center;padding:var(--sp-block) 0">
  <div style="font-size:44px">🎉</div>
  <div class="t-sec mt4">추가로 내실 돈이 없습니다</div>
  <div class="t-page pri mt4 num">${U.won(보증금)}</div>
  <div class="t-sub mt2">보증금 전액을 돌려드립니다</div>
  <div class="btns mt8" style="justify-content:center">${U.btn('환급 받기', { cls: 'btn-pri', lg: true, href: 'ST0301' })}</div>
</div>`, { cls: 'mt6' })}
${U.card('점검 결과', U.table(['본 것', { t: '결과', w: '132px', cls: 'r' }], [
  ['반납 시각', { t: '<b class="pri">기한 안</b>', cls: 'r' }],
  ['구성품', { t: '<b class="pri">모두 있음</b>', cls: 'r' }],
  ['상태', { t: '<b class="pri">이상 없음</b>', cls: 'r' }],
  ['세척 필요', { t: '<b class="pri">없음</b>', cls: 'r' }],
]), { cls: 'mt6' })}
${U.card('단골까지', `<div class="row-b mb3"><span>3회 중 <b class="pri">3회</b> 완료</span><b class="num">100%</b></div>
  ${U.progress(100)}
  ${U.banner('ok', '🎁', '<b>다음부터 보증금을 받지 않습니다.</b> 세 번 모두 깨끗하게 돌려주셨어요.', { cls: 'mt4' })}`, { cls: 'mt6' })}`,
  다루는것: [
    ['가장 흔한 경우', '짧고 분명하게 — 좋은 소식은 길 필요가 없다'],
    ['고맙다는 말', '깨끗하게 돌려주신 것에 대한 인사'],
    ['바로 다음', '환급 화면으로 큰 버튼 하나'],
    ['단골 진행', '이번으로 조건을 채웠으면 그것을 알린다'],
  ],
});

P['ST0202'] = () => 잎({
  부모: 정산, 제목: '근거를 펼쳐 볼 때',
  설명: '금액만 적으면 납득하지 못합니다. 사진과 계산식을 함께 둡니다.',
  본문: `${U.card('', U.accordion(정산항목.map((x) => ({
  q: `<span class="grow row-b" style="gap:var(--sp-item)"><span>${x.nm}</span><span class="num strong">${U.won(x.amt)}</span></span>`,
  a: `<div class="g2">
    <div>${U.kv([['계산식', x.calc], ['근거', x.근거], ['점검자', '박** (매장)'], ['확인 시각', '<span class="num">2026-08-18 17:40</span>']], { cls: 'left' })}
      <div class="mt4">${U.btn('이 항목에 이의', { sm: true, href: 'ST0205' })}</div></div>
    <div>${U.ph(['근거 사진', 800, 600], { seed: x.nm })}</div>
  </div>`,
})), 0))}
${U.banner('info', '📷', '<b>사진마다 「받을 때」와 「돌려줄 때」를 함께 볼 수 있습니다.</b> 원래 그랬던 것인지 확인하실 수 있어요.', { cls: 'mt6' })}
${U.card('근거가 없으면', `<p class="t-sub">사진이나 기록이 없는 항목은 <b>청구하지 않습니다.</b>
  「눈으로 봤다」만으로는 증명이 안 되니까요.</p>
  <p class="t-sub mt3">그래서 점검할 때 반드시 사진을 남깁니다 — 저희를 위해서가 아니라 <b>손님이 확인하실 수 있게</b>요.</p>`, { cls: 'mt6' })}`,
  다루는것: [
    ['펼치면', '계산식 · 근거 · 점검자 · 시각 · 사진'],
    ['항목마다 이의', '전체가 아니라 그 항목만 걸 수 있게'],
    ['견주기', '수령 사진과 반납 사진을 함께 본다'],
    ['원칙', '근거가 없으면 청구하지 않는다'],
  ],
});

P['ST0203'] = () => 잎({
  부모: 정산, 제목: '보험이 덮은 몫',
  설명: '면책된 금액을 마이너스 줄로 분명히 보여드립니다.',
  본문: `${U.card('', U.table(['항목', { t: '금액', w: '132px', cls: 'r nowrap' }, '메모'], [
  ['연체료 1일', { t: `<span class="num">${U.won(정산항목[0].amt)}</span>`, cls: 'r nowrap' }, '보험 대상 아님'],
  ['폴대 1개 파손', { t: `<span class="num">${U.won(정산항목[1].amt)}</span>`, cls: 'r nowrap' }, '보험 대상'],
  ['<span class="pri">파손 면책 보험 적용</span>', { t: `<b class="num pri">-${U.num(면책액)}원</b>`, cls: 'r nowrap' }, '한도 200,000원 중 15,000원 사용'],
  ['세척비', { t: `<span class="num">${U.won(정산항목[2].amt)}</span>`, cls: 'r nowrap' }, '보험 대상 아님'],
], { foot: ['<b>청구 합계</b>', { t: `<b class="num">${U.won(청구)}</b>`, cls: 'r nowrap' }, ''] }))}
${U.banner('ok', '🛡', `보험료 <b>9,000원</b>을 내시고 <b>${U.won(면책액)}</b>을 덮었습니다. 이번엔 이득이셨어요.`, { cls: 'mt6' })}
${U.card('무엇이 보험 대상인가요', U.table(['항목', { t: '보험', w: '104px', cls: 'c' }, '왜'], [
  ['파손·고장', { t: '<b class="pri">○</b>', cls: 'c' }, '쓰다가 생긴 사고'],
  ['연체료', { t: '<b class="dan">✕</b>', cls: 'c' }, '늦게 돌려주신 것은 사고가 아닙니다'],
  ['세척·건조비', { t: '<b class="dan">✕</b>', cls: 'c' }, '상태를 안 맞춰 돌려주신 것'],
  ['구성품 부족', { t: '<b class="dan">✕</b>', cls: 'c' }, '분실은 면책 제외'],
]), { cls: 'mt6' })}
${U.card('남은 한도', `<div class="row-b mb3"><span>200,000원 중 15,000원 사용</span><b class="num">7%</b></div>
  ${U.progress(7)}
  <p class="t-sub mt3">이번 대여가 끝나면 한도도 함께 끝납니다. 다음 대여에는 다시 가입하셔야 해요.</p>`, { cls: 'mt6' })}`,
  다루는것: [
    ['마이너스 줄', '면책액을 따로 한 줄로 빼서 보여준다'],
    ['무엇이 대상인가', '파손만 — 연체·세척·분실은 아니다'],
    ['이득이었나', '보험료와 덮은 금액을 견줘 준다'],
    ['한도', '대여마다 새로 시작된다는 것을 알린다'],
  ],
});

P['ST0204'] = () => 잎({
  부모: 정산, 제목: '보증금을 넘었을 때',
  설명: '넘은 만큼만 카드로 청구합니다.',
  본문: `${U.banner('dan', '⚠', '<b>청구액이 보증금을 넘었습니다.</b> 차액 <b class="num">42,000원</b>이 등록하신 카드로 청구됩니다.')}
${U.card('', `${U.barSplit(200_000, 0)}
  <div class="mt6">${U.sumRows([
    ['청구 합계', '<span class="num">242,000원</span>'],
    ['보증금에서', '<span class="num">-200,000원</span>'],
  ], ['추가로 내실 돈', '<span class="num dan">42,000원</span>'])}</div>`, { cls: 'mt6' })}
${U.card('무엇 때문에 넘었나', U.table(['항목', { t: '금액', w: '132px', cls: 'r nowrap' }], [
  ['연체료 5일', { t: `<span class="num">${U.won(G.day * 2 * 2)}</span>`, cls: 'r nowrap' }],
  ['원단 찢어짐', { t: '<span class="num">40,000원</span>', cls: 'r nowrap' }],
  ['보관가방 분실', { t: '<span class="num">12,000원</span>', cls: 'r nowrap' }],
  ['세척비', { t: '<span class="num">20,000원</span>', cls: 'r nowrap' }],
]), { cls: 'mt6' })}
${U.card('언제 청구되나요', `<ul class="stack t-sub">
  <li>· <b>정산 확정 후 3영업일</b> 뒤에 청구합니다 — 그 사이에 이의를 제기하실 수 있게요</li>
  <li>· 청구 전날 <b>문자로 미리</b> 알려드립니다</li>
  <li>· 카드 결제가 안 되면 <b>계좌 안내</b>를 드립니다</li>
  <li>· 금액이 크면 <b>나눠 내시는 것</b>도 상의할 수 있습니다</li>
</ul>
<div class="btns mt6">${U.btn('이의 제기', { cls: 'btn-pri', href: 'ST0205' })}${U.btn('나눠 내기 문의', { href: 'CS0201' })}</div>`, { cls: 'mt6' })}`,
  다루는것: [
    ['넘은 만큼만', '보증금을 다 쓰고 차액만 청구한다'],
    ['3영업일 유예', '이의 제기할 시간을 준다'],
    ['미리 알림', '청구 전날 문자'],
    ['분납', '금액이 크면 상의할 수 있다고 알린다'],
  ],
});

P['ST0205'] = () => 잎({
  부모: 정산, 제목: '이의를 제기할 때',
  설명: '접수하시면 정산과 환급이 함께 멈춥니다.',
  본문: `${U.banner('warn', '⏳', '<b>정산 확정 후 7일 안에만</b> 제기하실 수 있습니다. 남은 기간 <b class="num">5일</b> (8월 25일까지).')}
${U.card('', `${U.field('어떤 항목이 다른가요', `<div class="stack">
    ${정산항목.map((x, i) => U.check(`${x.nm} <span class="t-sub">— ${U.won(x.amt)}</span>`, { on: i === 2, none: true })).join('')}
  </div>`)}
  ${U.field('어떤 점이 다른가요', U.textarea({ v: '흙 오염으로 세척비 20,000원이 잡혔는데, 받을 때 이미 바닥에 흙이 묻어 있었습니다. 수령 사진 4번째를 봐 주시겠어요?' }), { req: true })}
  ${U.field('사진을 붙이시면 빠릅니다', `<div class="g2">
    <div>${U.ph(['수령 사진 (자동으로 붙었습니다)', 800, 600], { seed: '바닥' })}<div class="mt2">${U.badge('자동 첨부', 'b-ok')}</div></div>
    <div>${U.ph(['추가 사진', 800, 600], { seed: 'extra' })}<div class="btns mt2">${U.btn('붙이기', { sm: true, w: true })}</div></div>
  </div>`)}
  <div class="btns mt6">${U.btn('이의 접수하기', { cls: 'btn-pri', lg: true, attr: ' data-toast="접수했어요. 정산이 보류되고 2영업일 안에 답변드립니다" data-toast-kind="ok"' })}</div>`, { cls: 'mt6' })}
${U.card('접수하면 어떻게 되나요', U.timeline([
  { k: 'on', t: '접수 — 정산이 보류됩니다', d: '환급도 함께 멈춥니다' },
  { k: '', t: '다시 확인 (2영업일)', d: '수령·반납 사진을 다시 봅니다' },
  { k: '', t: '결과 안내', d: '조정되면 그만큼 더 돌려드립니다' },
]), { cls: 'mt6' })}
${U.card('결과는 어떻게 되나요', `<ul class="stack t-sub">
  <li>· <b>인정되면</b> 그 항목을 빼고 다시 정산합니다</li>
  <li>· <b>인정 안 되면</b> 왜 그런지 근거와 함께 알려드립니다</li>
  <li>· 그래도 납득이 안 되시면 <b>소비자원</b>에 조정을 신청하실 수 있습니다 — 저희가 절차를 안내해 드려요</li>
</ul>`, { cls: 'mt6' })}`,
  다루는것: [
    ['항목별로', '전체가 아니라 문제되는 항목만 고른다'],
    ['사진 자동 첨부', '그 항목의 근거 사진을 미리 붙여 준다'],
    ['접수하면', '정산과 환급이 함께 멈춘다'],
    ['안 되면', '소비자원 절차까지 안내한다'],
  ],
});

P['ST0206'] = () => 잎({
  부모: 정산, 제목: '정산을 확정할 때',
  설명: '되돌릴 수 없다는 것을 한 번 더 말씀드립니다.',
  본문: `${U.card('', `<div style="text-align:center;padding:var(--sp-block) 0">
    <div class="t-sub">확정할 금액</div>
    <div class="t-page mt2 num">${U.won(청구)}</div>
    <div class="t-sub mt3">보증금 ${U.num(보증금)}원에서 빼고 <b class="pri">${U.won(환급)}</b>을 돌려드립니다</div>
  </div>`)}
${U.card('확정 전에 확인해 주세요', `<div class="stack">
  ${U.check('<b>근거 사진을 확인했습니다</b>', { sub: '항목마다 펼쳐 사진을 보셨나요?', attr: ' data-agree' })}
  ${U.check('<b>계산식을 확인했습니다</b>', { sub: '연체료 계산이 맞는지 보셨나요?', attr: ' data-agree' })}
  <div class="mt4">${U.check('<b>위 내용을 모두 확인했습니다</b>', { attr: ' data-unlock="doFix"' })}</div>
</div>
<div class="mt6">${U.btn('정산 확정하기', { cls: 'btn-pri', lg: true, w: true, id: 'doFix', off: true, href: 'ST0301' })}</div>`, { cls: 'mt6' })}
${U.banner('info', 'ℹ', '<b>확정 후에도 7일 안에는 이의를 제기하실 수 있습니다.</b> 확정이 곧 포기는 아니에요.', { cls: 'mt6' })}
${U.card('확정하지 않으면', `<p class="t-sub"><b>7일 뒤에 저절로 확정됩니다.</b> 그동안은 환급이 시작되지 않아요.</p>
  <p class="t-sub mt3">빨리 돌려받고 싶으시면 확정해 주시는 편이 좋습니다.
  <b>확정해도 이의 기간은 그대로 7일</b>입니다.</p>`, { cls: 'mt6' })}`,
  다루는것: [
    ['확인 절차', '사진과 계산식을 보셨는지 묻고 잠금을 푼다'],
    ['확정 ≠ 포기', '확정 후에도 7일간 이의 가능'],
    ['안 하면', '7일 뒤 자동 확정 · 그동안 환급 안 됨'],
    ['권유', '확정하면 환급이 빨라진다'],
  ],
});

P['ST0302'] = () => 잎({
  부모: 환급화면, 제목: '어떻게 받으실지 고를 때',
  설명: '세 가지 중 하나를 고르시면 됩니다.',
  본문: `${U.card('', `<div class="stack">
  ${U.check('<b>카드 한도 보류를 풀기</b> <span class="badge b-solid">권합니다</span> <span class="t-sub">— 1~3영업일</span>',
    { on: true, sub: '실제로 빠져나간 돈이 없어서 «입금»이 아니라 «한도 해제»입니다. 통장에는 아무것도 안 들어옵니다.' })}
  ${U.check('<b>계좌로 받기</b> <span class="t-sub">— 3~5영업일</span>', { sub: '한도 보류를 풀고 계좌로 입금해 드립니다. 조금 더 걸립니다.' })}
  ${U.check(`<b>적립금으로 받기</b> <span class="badge b-acc">5% 더</span> <span class="t-sub">— 바로</span>`,
    { sub: `${U.won(환급)} 대신 ${U.won(Math.round(환급 * 1.05))}을 적립금으로 드립니다. 1년 안에 쓰시면 이득이에요.` })}
</div>`)}
${U.card('견줘 보세요', U.table(['', { t: '한도 해제', w: '124px', cls: 'r' }, { t: '계좌 입금', w: '124px', cls: 'r' }, { t: '적립금', w: '124px', cls: 'r' }], [
  ['받는 금액', { t: `<span class="num">${U.won(환급)}</span>`, cls: 'r' }, { t: `<span class="num">${U.won(환급)}</span>`, cls: 'r' }, { t: `<b class="num pri">${U.won(Math.round(환급 * 1.05))}</b>`, cls: 'r' }],
  ['걸리는 기간', { t: '1~3영업일', cls: 'r' }, { t: '3~5영업일', cls: 'r' }, { t: '<b class="pri">바로</b>', cls: 'r' }],
  ['어디로', { t: '카드 한도', cls: 'r' }, { t: '통장', cls: 'r' }, { t: '내 적립금', cls: 'r' }],
  ['쓸 수 있는 곳', { t: '어디나', cls: 'r' }, { t: '어디나', cls: 'r' }, { t: '<span class="warn">여기서만</span>', cls: 'r' }],
]), { cls: 'mt6' })}
${U.banner('info', '💡', '<b>또 빌리실 생각이면 적립금이 이득입니다.</b> 아니면 한도 해제가 가장 빠릅니다.', { cls: 'mt6' })}`,
  다루는것: [
    ['세 가지', '한도 해제 · 계좌 입금 · 적립금'],
    ['견주기', '금액 · 기간 · 쓸 수 있는 곳을 표로'],
    ['적립금 주의', '여기서만 쓸 수 있다는 것을 숨기지 않는다'],
    ['권유', '또 빌리실 분께만 적립금을 권한다'],
  ],
});

P['ST0303'] = () => 잎({
  부모: 환급화면, 제목: '「한도 해제」가 무슨 뜻인가요',
  설명: '가장 많이 물어보시는 것이라 따로 화면을 뒀습니다.',
  본문: `${U.card('', `<div class="g2">
  ${U.box(`<div class="strong mb3">보증금을 «걸 때»</div>
    <p class="t-sub">카드사에 이렇게 요청합니다 —<br><br>
    <b>「이 사람 한도 중 20만원은 잠깐 못 쓰게 해 주세요」</b><br><br>
    돈이 나가지 않습니다. 명세서에도 안 찍힙니다.
    다만 <b>쓸 수 있는 한도가 20만원 줄어듭니다.</b></p>`)}
  ${U.box(`<div class="strong mb3">보증금을 «풀 때»</div>
    <p class="t-sub">카드사에 이렇게 요청합니다 —<br><br>
    <b>「이제 풀어 주세요」</b><br><br>
    한도가 원래대로 돌아옵니다.
    <b>입금이 아니라 «해제»</b>라서 통장에 돈이 들어오지 않습니다.</p>`)}
</div>`)}
${U.banner('warn', '❓', '<b>「환급됐다는데 통장에 안 들어왔어요」</b> — 가장 많이 듣는 말씀입니다. 카드 한도를 확인해 보시면 늘어나 있을 거예요.', { cls: 'mt6' })}
${U.card('어디서 확인하나요', U.table(['카드사', '어디를 보면 되나'], [
  ['신한·삼성·현대', '앱 → 내 카드 → 이용 가능 한도'],
  ['국민·롯데', '앱 → 카드 → 한도 조회'],
  ['카카오뱅크·토스', '앱 → 카드 → 이용 한도'],
]), { cls: 'mt6' })}
${U.card('실제로 결제됐던 경우는', `<p class="t-sub">체크카드나 계좌이체로 보증금을 내셨으면 <b>실제로 돈이 나갔습니다.</b>
  그 경우에는 <b>통장으로 돌아옵니다.</b> 3~5영업일 걸려요.</p>
  <p class="t-sub mt3">내 대여의 보증금 방식은 <b>「카드 한도 보류」</b>였습니다.</p>`, { cls: 'mt6' })}`,
  다루는것: [
    ['핵심', '입금이 아니라 해제라는 것'],
    ['왜 따로 화면인가', '문의의 가장 큰 몫이 이것이다'],
    ['확인 방법', '카드사별로 어디를 보면 되는지'],
    ['예외', '체크카드·계좌이체는 실제로 돌아온다'],
  ],
});

P['ST0304'] = () => 잎({
  부모: 환급화면, 제목: '환급이 처리되는 동안',
  설명: '어디까지 갔는지 보여드립니다.',
  본문: `${U.card('', U.steps([['정산 확정', '8/18 18:30'], ['환급 요청', '8/18 18:31'], ['카드사 처리', '진행 중'], ['완료', '']], 2))}
${U.card('지금 상태', U.kv([
  ['환급 금액', `<b class="num">${U.won(환급)}</b>`],
  ['방법', '신한카드 한도 보류 해제'],
  ['요청한 때', '<span class="num">2026-08-18 18:31</span>'],
  ['예상 완료', '<b>2026-08-21</b> (3영업일)'],
  ['상태', U.stBadge('보류중')],
]), { cls: 'mt6' })}
${U.card('무엇을 기다리나요', `<p class="t-sub">저희가 카드사에 <b>해제 요청</b>을 보냈습니다. 카드사가 처리하는 데 1~3영업일 걸립니다.</p>
  <p class="t-sub mt3">저희 쪽에서 더 할 일은 없고, <b>카드사의 처리를 기다리는 중</b>입니다.
  주말과 공휴일은 영업일에 안 들어갑니다.</p>`, { cls: 'mt6' })}
${U.banner('info', '📱', '<b>해제되면 문자로 알려드립니다.</b> 이 화면을 다시 열어 보지 않으셔도 돼요.', { cls: 'mt6' })}`,
  다루는것: [
    ['단계', '확정 → 요청 → 카드사 처리 → 완료'],
    ['누가 하고 있나', '지금은 카드사 차례라는 것을 밝힌다'],
    ['영업일', '주말·공휴일은 안 센다는 것을 알린다'],
    ['알림', '끝나면 문자 — 다시 들어와 보지 않게'],
  ],
});

P['ST0305'] = () => 잎({
  부모: 환급화면, 제목: '환급이 늦어질 때',
  설명: '예정일이 지나면 저희가 먼저 나섭니다.',
  본문: `${U.banner('dan', '⏳', '<b>예상 완료일(8월 21일)이 지났습니다.</b> 아직 한도가 안 풀리셨나요?')}
${U.card('먼저 확인해 주세요', `<ol class="stack t-sub">
  <li>1. 카드사 앱에서 <b>이용 가능 한도</b>를 확인해 주세요</li>
  <li>2. 늘어나 있으면 <b>이미 풀린 것</b>입니다 — 통장에는 안 들어옵니다</li>
  <li>3. 그대로면 아래 버튼을 눌러 주세요</li>
</ol>
<div class="btns mt6">${U.btn('한도가 그대로예요', { cls: 'btn-pri', attr: ' data-toast="접수했어요. 저희가 카드사에 직접 확인하겠습니다" data-toast-kind="ok"' })}${U.btn('풀렸어요', { attr: ' data-toast="다행입니다. 이용해 주셔서 고맙습니다" data-toast-kind="ok"' })}</div>`, { cls: 'mt6' })}
${U.card('저희가 하는 일', U.timeline([
  { k: 'on', t: '카드사에 직접 확인 (1영업일)', d: '해제 요청이 제대로 갔는지 봅니다' },
  { k: '', t: '재요청', d: '누락됐으면 다시 보냅니다' },
  { k: '', t: '그래도 안 되면 — 계좌로 입금', d: '카드사 문제라도 손님이 기다리시게 두지 않습니다' },
]), { cls: 'mt6' })}
${U.card('계좌로 바꿔 드릴까요', `<p class="t-sub">기다리시기 어려우시면 <b>계좌 입금으로 바꿔</b> 드릴 수 있습니다.
  한도 해제와 중복되지 않게 저희가 확인하고 처리합니다.</p>
  <div class="btns mt4">${U.btn('계좌로 받을게요', { href: 'ST0301' })}${U.btn('전화로 문의', { attr: ` data-toast="${SITE.tel}"` })}</div>`, { cls: 'mt6' })}`,
  다루는것: [
    ['먼저 확인', '이미 풀렸는데 모르시는 경우가 가장 많다'],
    ['접수하면', '저희가 카드사에 직접 확인한다'],
    ['마지막 수단', '카드사 문제라도 계좌로 입금해 드린다'],
    ['원칙', '「카드사 사정입니다」로 끝내지 않는다'],
  ],
});

P['ST0402'] = () => 잎({
  부모: 완료, 제목: '이번 대여를 한 장으로',
  설명: '얼마 내고 얼마 돌려받았는지 한 표에 담습니다.',
  본문: `${U.card('', U.table(['', { t: '', w: '164px', cls: 'r nowrap' }], [
  ['빌린 것', { t: `${G.nm} × 2개`, cls: 'r' }],
  ['기간', { t: '8/15 ~ 8/17 (2박 3일)', cls: 'r' }],
  ['실제 반납', { t: '<span class="dan">8/18 09:14 (1일 늦음)</span>', cls: 'r' }],
  ['받는 방법', { t: '매장 방문', cls: 'r' }],
  ['대여료', { t: '<span class="num">240,000원</span>', cls: 'r nowrap' }],
  ['보증금', { t: `<span class="num">${U.won(보증금)}</span>`, cls: 'r nowrap' }],
  ['정산 (연체·파손·세척)', { t: `<span class="num">-${U.num(청구)}원</span>`, cls: 'r nowrap' }],
], { foot: ['<b>돌려받은 돈</b>', { t: `<b class="num pri">${U.won(환급)}</b>`, cls: 'r nowrap' }] }))}
${U.card('서류 받기', `<div class="btns">
  ${U.btn('영수증', { attr: ' data-toast="이메일로 보냈어요" data-toast-kind="ok"' })}
  ${U.btn('정산서', { attr: ' data-toast="이메일로 보냈어요" data-toast-kind="ok"' })}
  ${U.btn('계약서', { href: 'BK0501' })}
  ${U.btn('세금계산서 요청', { attr: ' data-toast="사업자 정보를 입력해 주세요"' })}
</div>
<p class="t-sub mt4">모든 서류는 마이페이지에서 <b>언제든</b> 다시 받으실 수 있습니다.</p>`, { cls: 'mt6' })}
${U.card('실제로 든 돈', `<p class="t-sub">대여료 240,000원 + 정산 ${U.num(청구)}원 = <b>${U.won(240_000 + 청구)}</b>이 이번에 실제로 드신 돈입니다.
  보증금은 돌려받으셨으니 비용이 아니에요.</p>
  <p class="t-sub mt3">사서 쓰셨다면 텐트 2개에 <b>590,000원</b>이었을 겁니다.</p>`, { cls: 'mt6' })}`,
  다루는것: [
    ['한 표로', '빌린 것부터 돌려받은 돈까지'],
    ['실제 비용', '보증금은 비용이 아니라는 것을 계산해 준다'],
    ['견주기', '사서 쓸 때와 견줘 준다'],
    ['서류', '네 가지를 한자리에서'],
  ],
});

P['ST0403'] = () => 잎({
  부모: 완료, 제목: '단골까지 얼마나 남았나',
  설명: '연체가 있어도 정산을 끝내시면 횟수는 쌓입니다.',
  본문: `${U.card('', `<div class="row-b mb3"><span>3회 중 <b class="pri">2회</b> 완료</span><b class="num">66%</b></div>
  ${U.progress(66)}
  <p class="t-sub mt4">한 번 더 이용하시면 <b>다음부터 보증금을 받지 않습니다.</b></p>`)}
${U.card('내 이용 기록', U.table(['언제', '무엇을', { t: '연체', w: '84px', cls: 'c' }, { t: '파손', w: '84px', cls: 'c' }, { t: '횟수', w: '76px', cls: 'c' }], [
  ['<span class="num sub">2026-07-03</span>', '코베아 화로대', { t: '<span class="pri">없음</span>', cls: 'c' }, { t: '<span class="pri">없음</span>', cls: 'c' }, { t: '<b class="pri">✓ 1</b>', cls: 'c' }],
  ['<span class="num sub">2026-07-22</span>', '소니 A7M4', { t: '<span class="pri">없음</span>', cls: 'c' }, { t: '<span class="pri">없음</span>', cls: 'c' }, { t: '<b class="pri">✓ 2</b>', cls: 'c' }],
  ['<span class="num sub">2026-08-15</span>', '카즈미 텐트 2개', { t: '<b class="dan">1일</b>', cls: 'c' }, { t: '<span class="warn">폴대</span>', cls: 'c' }, { t: '<span class="muted">보류</span>', cls: 'c' }],
]), { cls: 'mt6' })}
${U.banner('warn', '⚠', '<b>이번 건은 연체와 파손이 있어 횟수에 안 들어갑니다.</b> 정산은 끝내셨지만 「깨끗한 이용」 조건은 못 채우셨어요.', { cls: 'mt6' })}
${U.card('조건을 다시 정리하면', `<ul class="stack t-sub">
  <li>· <b>3회</b> 이상 이용 — 연체·파손 없이</li>
  <li>· 연체나 파손이 한 번 생기면 <b>그 건은 안 세어집니다</b></li>
  <li>· 이미 쌓은 횟수는 <b>없어지지 않습니다</b> — 처음부터 다시가 아니에요</li>
  <li>· 100만원 넘는 장비는 면제 대상이 아닙니다</li>
</ul>`, { cls: 'mt6' })}`,
  다루는것: [
    ['진행률', '몇 회 중 몇 회인지 막대로'],
    ['기록', '언제 무엇을 빌렸고 문제가 있었는지'],
    ['이번 건', '연체·파손이 있으면 안 세어진다는 것을 분명히'],
    ['쌓은 것은 유지', '처음부터 다시가 아니라는 안심'],
  ],
});

P['ST0404'] = () => 잎({
  부모: 완료, 제목: '다음에 또 빌리시라고',
  설명: '끝나는 화면에서 다음으로 이어 줍니다.',
  본문: `${U.card('이 장비 또 빌리기', `<div class="row wrap-row" style="gap:var(--sp-block)">
  <div style="width:160px">${U.phGear(G.id)}</div>
  <div class="grow"><div class="t-card">${G.nm}</div>
    <p class="t-sub mt2">지난번과 같은 장비입니다. 쓰시던 것이라 설명이 필요 없어요.</p>
    <p class="mt4"><b class="num" style="font-size:22px">${U.num(G.day)}</b><span class="t-sub">원 / 1일</span></p></div>
  ${U.btn('날짜 고르기', { cls: 'btn-pri', href: 'PD0301' })}
</div>`)}
${U.card('쿠폰이 나왔어요', `<div class="row-b wrap-row">
  <div><div class="t-card">재이용 10% 할인</div>
    <div class="t-sub mt1">9월 20일까지 · 5만원 이상 · 쿠폰함에 넣어 뒀습니다</div></div>
  ${U.badge('받았어요', 'b-ok')}
</div>`, { cls: 'mt6' })}
${U.card('다음 시즌에 필요하실 것', `<div class="cards">${GEAR.slice(1, 3).map((g) => U.gearCard(g, { left: 5, href: 'PD0201' })).join('')}</div>
<p class="t-sub mt4">가을에는 침낭 등급을 한 단계 올리시는 편이 좋습니다. 밤에 생각보다 춥습니다.</p>`, { cls: 'mt6' })}
${U.card('미리 알려드릴까요', `<div class="stack">
  <div class="row-b"><span>기획전이 열리면</span>${U.toggle(true, '기획전 알림을 켰어요')}</div>
  <div class="row-b"><span>이 장비가 특가가 되면</span>${U.toggle(true, '특가 알림을 켰어요')}</div>
  <div class="row-b"><span>연휴 2주 전에</span>${U.toggle(false, '연휴 알림을 켰어요')}</div>
</div>`, { cls: 'mt6' })}`,
  다루는것: [
    ['같은 장비', '쓰시던 것이라 설명이 필요 없다 — 가장 쉬운 재이용'],
    ['쿠폰', '자동으로 발급하고 유효기간을 알린다'],
    ['다음 시즌', '계절에 따라 필요한 것을 권한다'],
    ['알림', '기획전 · 특가 · 연휴 세 가지'],
  ],
});
