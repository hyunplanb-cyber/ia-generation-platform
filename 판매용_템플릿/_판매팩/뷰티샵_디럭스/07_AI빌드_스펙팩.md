# 뷰티샵 예약 플랫폼 — AI 빌드 스펙팩

> 이 문서 하나로 사이트를 만들 수 있게 정리한 스펙이에요. 화면별 **생성 프롬프트**를 그대로 사용하세요.

## 1. 프로젝트 개요
- **컨셉**: 미용실·네일·왁싱·피부관리 매장을 한 곳에서 찾아 예약하는 뷰티 예약 플랫폼. 고객은 지역과 시술로 매장을 찾아 담당 디자이너와 시간을 골라 예약하고, 시술 후 사진 리뷰를 남긴다. 매장은 들어온 예약을 확정·관리하고 시술 메뉴와 디자이너 근무 일정을 운영하며 정산 내역을 확인한다.
- **디자인 컨셉**: 부드러운 뉴트럴 베이지(#F5EFE9) 배경에 딥 플럼(#7A3B5D) 포인트, 시술 사진이 돋보이는 큼직한 카드형 레이아웃
- **디바이스**: 반응형
- **전체 일정**: 2026-08-03 ~ 2026-11-06

## 2. 공통 요소 (모든 화면 공통)
> 아래 요소는 **한 번만 만들어 모든 화면에서 재사용**하세요. 각 화면 스펙(4장)은 이 공통 요소 위에 올라가는 콘텐츠만 설명합니다.
- **영역이 둘입니다. 헤더를 각각 따로 만드세요.**
  - **손님 화면 GNB**: `홈` · `매장 탐색` · `예약하기` · `내 예약` · `리뷰` · `내 정보`
  - **운영자 화면 GNB**: `예약 관리` · `매장 운영` · `매장 홈` · `정산` — 좌측 사이드바 + 상단 바 형태가 맞습니다
  - **로그인·가입 화면**: `계정` — 헤더 없이 단독 화면으로 만드세요
  - 두 영역을 오가는 길은 **하나만** 둡니다 — 손님 화면 우측 상단에 운영자 영역으로 넘어가는 버튼 하나(예: `예약 관리 화면으로`), 운영자 화면 우측 상단에 `고객 화면 보기`. 한 계정이 두 권한을 가지며(크몽·숨고와 같은 방식), 아직 권한이 없으면 전환 대신 **신청 화면으로 보냅니다.**
- **콘텐츠 영역**: 최대 1440px, 좌우 여백 24px — 헤더·본문·푸터·하단 고정 바가 모두 이 폭을 쓴다. 화면마다 따로 정하지 않는다. 화면마다 폭을 새로 정하면 위아래가 어긋납니다.
- **읽기 폭**: 글이 주인공인 자리(상세 본문·안내·약관)는 최대 760px로 좁혀 콘텐츠 영역 **안에서** 가운데 정렬 — 카드 격자에는 쓰지 않습니다. 아래 「읽기 폭」 참고.
- **헤더**: 로고 + 검색 + 상단 내비게이션(GNB)
- **푸터**: 저작권 · 기본 링크
- **재사용 컴포넌트**: 반복되는 UI(상품/항목 **카드**, 리스트 **행**, **폼 필드**, **버튼**)는 공용 컴포넌트로 만들어 화면마다 재사용하세요.

## 3. 메뉴 구조
- **[HO] 홈** (Home)
  - `HO-01` 홈 - 비로그인
  - `HO-02` 홈 - 고객 로그인
- **[SE] 매장 탐색** (Search)
  - `SE-01` 매장 목록
  - `SE-02` 매장 목록 - 검색 결과 없음
  - `SE-03` 매장 지도
  - `SE-04` 매장 상세
  - `SE-05` 시술 메뉴 상세
  - `SE-06` 디자이너 소개
- **[RE] 예약하기** (Reserve)
  - `RE-01` 예약 1단계 - 시술 선택
  - `RE-02` 예약 2단계 - 디자이너 선택
  - `RE-03` 예약 3단계 - 날짜·시간 선택
  - `RE-04` 예약 4단계 - 확인 및 결제
  - `RE-05` 예약 3단계 - 가능한 시간 없음
  - `RE-06` 예약 완료
  - `RE-07` 예약 - 결제 실패
  - `RE-08` 대기 신청 완료
- **[MY] 내 예약** (Mybooking)
  - `MY-01` 내 예약 - 예정
  - `MY-02` 내 예약 - 비어 있음
  - `MY-03` 예약 상세
  - `MY-04` 예약 변경
  - `MY-05` 예약 취소 - 확인
  - `MY-06` 예약 변경·취소 - 마감됨
- **[RV] 리뷰** (Review)
  - `RV-01` 리뷰 작성
  - `RV-02` 매장 리뷰 목록
  - `RV-03` 내 리뷰 목록
  - `RV-04` 리뷰 작성 - 기간 지남
- **[ST] 예약 관리** (Store)
  - `ST-01` 오늘 예약
  - `ST-02` 예약 캘린더
  - `ST-03` 예약 목록 - 예약 없음
  - `ST-04` 예약 상세 - 매장
  - `ST-05` 예약 접수 대기
  - `ST-06` 노쇼 처리 - 확인
- **[MG] 매장 운영** (Manage)
  - `MG-01` 시술 메뉴 관리
  - `MG-02` 디자이너 관리
  - `MG-03` 근무 일정 관리
  - `MG-04` 휴무·예약 정책 설정
  - `MG-05` 사진 업로드 - 실패
- **[AC] 내 정보** (Mypage)
  - `AC-01` 내 정보
  - `AC-02` 쿠폰·적립금
- **[PT] 매장 홈** (Partner)
  - `PT-01` 매장 홈
  - `PT-02` 매장 입점 신청
  - `PT-03` 입점 심사 중
- **[SA] 정산** (Settlement)
  - `SA-01` 매장 정산 내역
  - `SA-02` 정산 내역 - 내역 없음
- **[AU] 계정** (Auth)
  - `AU-01` 로그인
  - `AU-02` 회원가입
  - `AU-03` 회원가입 - 휴대폰 인증
  - `AU-04` 회원가입 완료
  - `AU-05` 비밀번호 찾기

## 4. 화면별 스펙
### HO-01 · 홈 - 비로그인
- **소속 메뉴**: 홈
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-08-03 ~ 2026-08-06
- **기능정의**: 상단 히어로(카피 + 지역·시술 통합 검색바) · 시술 카테고리 바로가기 8종(헤어컷·펌·염색·네일·왁싱·피부관리·속눈썹·두피케어) · 내 주변 인기 매장 8개 카드 · 이번 주 할인 기획전 배너 2단 · 실시간 예약 현황 티커 · 시술 사진 리뷰 갤러리 12장 · 신규 입점 매장 캐러셀 · 매장 입점 안내 배너 · 자주 묻는 질문 아코디언 · 하단 회원가입 유도 CTA
- **버튼 → 이동화면**:
  - `매장 찾기` → `SE-01` 매장 목록
  - `로그인` → `AU-01` 로그인
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `시술 카테고리 아이콘` → 고른 카테고리가 선택 표시되고, 아래 인기 매장 목록이 그 시술만 남게 걸러진다
  - `자주 묻는 질문 항목` → 답이 펼쳐지고 다시 누르면 접힌다. 한 번에 하나만 열린다
  - `신규 입점 캐러셀 좌우 화살표` → 카드가 한 칸씩 밀린다. 끝에 닿으면 그쪽 화살표가 흐려진다
  - `리뷰 갤러리 사진` → 그 자리에서 큰 사진과 시술명·매장명이 겹쳐 뜬다. 좌우로 넘길 수 있다
- **생성 프롬프트**:

  ```
  뷰티 예약 플랫폼의 비로그인 홈 화면을 만들어줘. 스크롤 순서대로 아래 섹션을 쌓아줘. 1) 상단 GNB(로고, 매장 찾기, 이벤트, 매장 입점, 로그인, 회원가입) 2) 히어로 — 카피와 함께 "지역 + 시술" 통합 검색바(예: 강남구 / 네일) 3) 시술 카테고리 8종 아이콘 그리드 4) 내 주변 인기 매장 8개 카드(대표사진, 매장명, 지역, 별점, 리뷰수, 최저가) 5) 이번 주 할인 기획전 배너 2단 6) 실시간 예약 현황 티커("방금 강남 OO샵 예약") 7) 시술 사진 리뷰 갤러리 12장 그리드 8) 신규 입점 매장 가로 캐러셀 9) 매장 입점 안내 배너 10) 자주 묻는 질문 아코디언 11) 하단 회원가입 CTA와 푸터 시술 사진이 크게 보이는 카드형. 모바일에서는 카드 1~2열, 캐러셀은 스와이프.
  ```

### HO-02 · 홈 - 고객 로그인
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈 - 비로그인` — 누르면 `HO-01`로 간다
- **일정**: 2026-08-07 ~ 2026-08-11
- **기능정의**: 다가오는 예약 카드 상단 고정(매장·시술·일시·남은 기간) · 이번 달 방문 요약 지표 3종 · 자주 가는 매장 바로 예약 · 리뷰 미작성 알림 · 관심 매장 신규 시술 소식 · 내 주변 추천 매장 · 지난 시술 이력 재예약 · 쿠폰·적립금 현황 · 하단 이벤트 배너
- **버튼 → 이동화면**:
  - `예약 상세` → `MY-03` 예약 상세
  - `내 예약` → `MY-01` 내 예약 - 예정
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `방문 요약 카드(방문·적립금·쿠폰)` → 그 아래 목록이 해당 내역으로 바뀐다
  - `「같은 시술 다시 예약」의 시술 카드` → 고른 시술이 선택 표시되고 예약 버튼 문구가 그 시술명으로 바뀐다
  - `리뷰 미작성 알림의 「나중에」` → 그 줄이 목록에서 사라지고 남은 건수가 하나 줄어든다
- **생성 프롬프트**:

  ```
  고객이 로그인한 상태의 홈 화면을 만들어줘. 스크롤 순서대로 배치해줘. 1) 인사말과 "다가오는 예약" 대형 카드 — 매장명, 시술명, 담당 디자이너, 일시, D-day 배지, 상세보기 버튼 2) 이번 달 방문 요약 3종(방문 횟수, 적립금, 사용 가능 쿠폰) 숫자 크게 3) "자주 가는 매장" 바로 예약 카드 목록 4) 리뷰 미작성 알림 리스트(시술 완료했는데 리뷰 안 쓴 건) 5) 관심 매장의 신규 시술 소식 6) 내 주변 추천 매장 캐러셀 7) 지난 시술 이력에서 "같은 시술 다시 예약" 섹션 8) 하단 이벤트 배너 예약 D-day와 적립금은 딥 플럼 색으로 강조해줘.
  ```

### SE-01 · 매장 목록
- **소속 메뉴**: 매장 탐색
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-08-12 ~ 2026-08-17
- **기능정의**: 지역·시술·가격대·평점 필터 · 정렬 드롭다운(거리순/인기순/평점순/최저가순) · 매장 카드 그리드(대표사진, 매장명, 지역, 별점, 리뷰수, 대표 시술 최저가) · 지도 보기 전환 버튼 · 오늘 예약 가능 매장만 보기 토글 · 무한 스크롤
- **버튼 → 이동화면**:
  - `매장 상세` → `SE-04` 매장 상세
  - `지도 보기` → `SE-03` 매장 지도
  - `검색` → `SE-02` 매장 목록 - 검색 결과 없음
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `지역·시술·가격대·평점 필터` → 고른 조건이 위에 칩으로 쌓이고, 매장 카드 목록과 「총 N개」 숫자가 함께 바뀐다
  - `정렬 드롭다운(거리순/인기순/평점순/최저가순)` → 카드 순서가 실제로 다시 늘어선다
  - `「오늘 예약 가능만」 토글` → 켜면 오늘 자리가 있는 매장만 남고 숫자가 줄어든다. 끄면 되돌아온다
  - `칩의 ✕` → 그 조건만 풀리고 목록이 다시 늘어난다
  - `「지도 보기」` → 같은 자리가 목록에서 지도로 바뀌고 버튼 문구가 「목록 보기」가 된다
  - `카드의 하트` → 채워지고 다시 누르면 비워진다
- **생성 프롬프트**:

  ```
  매장 목록 화면을 만들어줘. 좌측에 필터 사이드바(지역 선택, 시술 카테고리 체크박스, 가격대 슬라이더, 최소 평점, "오늘 예약 가능" 토글), 우측에 매장 카드 그리드 3열. 카드에는 대표 시술 사진, 매장명, 지역, 별점과 리뷰 수, 대표 시술 최저가를 넣어줘. 상단에 검색바, 정렬 드롭다운(거리순·인기순·평점순·최저가순), "지도로 보기" 전환 버튼. 모바일에서는 필터를 하단 시트로 전환하고 카드 1열.
  ```

### SE-02 · 매장 목록 - 검색 결과 없음
- **소속 메뉴**: 매장 탐색
- **뒤로가기**: 좌측 상단에 `‹ 매장 목록` — 누르면 `SE-01`로 간다
- **일정**: 2026-08-18 ~ 2026-08-19
- **기능정의**: 검색 조건을 포함한 안내 문구 · 적용된 필터 칩과 개별 해제 · 필터 초기화 버튼 · 조건을 넓힌 추천 매장 4개 · 인근 지역 제안 링크
- **버튼 → 이동화면**:
  - `필터 초기화` → `SE-01` 매장 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `걸린 조건 칩의 ✕` → 그 조건이 풀리면서 결과 수가 0에서 늘어나고, 빈 화면 대신 목록이 나타난다
  - `「조건 모두 풀기」` → 칩이 전부 사라지고 전체 매장이 나온다
- **생성 프롬프트**:

  ```
  매장 검색 결과가 없을 때의 화면을 만들어줘. 중앙에 아이콘과 "'강남구· 왁싱' 조건에 맞는 매장이 없어요" 문구, 적용된 필터를 칩으로 보여주고 각각 X로 제거 가능하게, "필터 초기화" 버튼과 "이런 매장은 어때요?" 추천 매장 4개 카드, "인근 서초구에서 찾아보기" 같은 지역 확장 링크도 넣어줘.
  ```

### SE-03 · 매장 지도
- **소속 메뉴**: 매장 탐색
- **뒤로가기**: 좌측 상단에 `‹ 매장 목록` — 누르면 `SE-01`로 간다
- **일정**: 2026-08-20 ~ 2026-08-21
- **기능정의**: 지도에 매장 위치 핀 표시 · 핀 클릭 시 하단 미리보기 카드 · 현재 위치 기준 재검색 버튼 · 지도 이동 시 이 지역 재검색 · 목록 보기 전환 · 예약 가능 매장 핀 색상 구분
- **버튼 → 이동화면**:
  - `매장 상세` → `SE-04` 매장 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `지도 위 핀` → 그 매장 미리보기 카드가 아래에 뜨고, 핀이 커진다. 다른 핀을 누르면 카드가 바뀐다
  - `지도 확대·축소` → 보이는 범위가 바뀌고 「이 지역 매장 N개」 숫자가 따라 바뀐다
  - `「목록 보기」` → 지도가 목록으로 바뀐다
- **생성 프롬프트**:

  ```
  매장 지도 화면을 만들어줘. 화면 대부분을 지도가 차지하고 매장 위치에 핀을 표시. 핀을 누르면 하단에 매장 미리보기 카드(사진, 매장명, 별점, 최저가, 예약하기 버튼)가 슬라이드로 올라와줘. 상단에 검색바와 "현재 위치에서 찾기" 버튼, 지도를 움직이면 "이 지역에서 재검색" 버튼이 뜨게. 우측 상단에 "목록으로 보기" 전환. 오늘 예약 가능한 매장은 핀 색을 다르게 해줘.
  ```

### SE-04 · 매장 상세
- **소속 메뉴**: 매장 탐색
- **뒤로가기**: 좌측 상단에 `‹ 매장 목록` — 누르면 `SE-01`로 간다
- **일정**: 2026-08-24 ~ 2026-08-27
- **기능정의**: 대표 사진 갤러리 슬라이드 · 매장 기본 정보(주소·영업시간·휴무일·전화) · 시술 메뉴와 가격표 · 디자이너 소개 카드 · 시술 사진 리뷰와 별점 분포 · 편의시설 아이콘 · 지도와 오시는 길 · 하단 고정 예약하기 버튼
- **버튼 → 이동화면**:
  - `예약하기` → `RE-01` 예약 1단계 - 시술 선택
  - `디자이너 보기` → `SE-06` 디자이너 소개
  - `시술 메뉴` → `SE-05` 시술 메뉴 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `시술·디자이너·리뷰·정보 탭` → 아래 내용이 그 탭 것으로 바뀐다. 선택된 탭에 밑줄이 남는다
  - `대표 사진` → 사진이 크게 열리고 좌우로 넘길 수 있다
  - `시술 메뉴 줄` → 펼쳐져서 소요 시간·설명·가격이 보이고, 다시 누르면 접힌다
  - `하트(찜)` → 채워지고 다시 누르면 비워진다
  - `「영업시간 더 보기」` → 요일별 시간표가 펼쳐진다
- **생성 프롬프트**:

  ```
  매장 상세 화면을 만들어줘. 상단에 대표 사진 갤러리 슬라이드, 그 아래 매장명·별점·리뷰수·지역과 찜하기 버튼. 본문 탭(시술 메뉴, 디자이너, 리뷰, 매장 정보). 시술 메뉴는 카테고리별 아코디언으로 시술명·소요시간·가격을 보여주고 각 항목에 선택 버튼. 리뷰는 시술 사진이 함께 보이는 카드형, 별점 분포 막대도 넣어줘. 하단(모바일) 또는 우측(PC)에 "예약하기" 스티키 버튼.
  ```

### SE-05 · 시술 메뉴 상세
- **소속 메뉴**: 매장 탐색
- **뒤로가기**: 좌측 상단에 `‹ 매장 목록` — 누르면 `SE-01`로 간다
- **일정**: 2026-08-28 ~ 2026-08-31
- **기능정의**: 시술 대표 사진과 설명 · 소요 시간과 가격(옵션별 차등) · 추가 옵션 선택(길이·디자인 등) · 시술 전후 사진 예시 · 주의사항과 준비물 안내 · 이 시술 리뷰만 모아보기 · 예약하기 버튼
- **버튼 → 이동화면**:
  - `예약하기` → `RE-01` 예약 1단계 - 시술 선택
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `옵션 고르기(길이·제품·추가 케어)` → 고른 옵션에 따라 예상 금액과 소요 시간이 다시 계산된다
  - `전후 사진 좌우 손잡이` → 끌면 시술 전/후 사진이 갈리는 지점이 움직인다
- **생성 프롬프트**:

  ```
  시술 메뉴 상세 화면을 만들어줘. 상단에 시술 대표 사진과 시술명, 소요 시간(예: 약 90분)과 기본 가격, 옵션별 추가 요금 표(길이·디자인 난이도 등). 시술 설명, 시술 전후 사진 예시 갤러리, 주의사항과 준비물 안내 아코디언, 이 시술에 대한 리뷰만 모아 보여주는 섹션. 하단에 "이 시술로 예약하기" 버튼.
  ```

### SE-06 · 디자이너 소개
- **소속 메뉴**: 매장 탐색
- **뒤로가기**: 좌측 상단에 `‹ 매장 목록` — 누르면 `SE-01`로 간다
- **일정**: 2026-09-01 ~ 2026-09-02
- **기능정의**: 디자이너 프로필 사진과 직급 · 전문 시술 태그 · 경력 소개 · 대표 작업 사진 포트폴리오 그리드 · 이 디자이너 리뷰와 평점 · 예약 가능 요일 안내 · 이 디자이너로 예약하기 버튼
- **버튼 → 이동화면**:
  - `예약하기` → `RE-01` 예약 1단계 - 시술 선택
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `디자이너 목록의 이름` → 오른쪽 소개·경력·시술 사진이 그 사람 것으로 바뀐다
  - `「이 디자이너 시술 사진 더 보기」` → 사진이 12장 더 붙는다
- **생성 프롬프트**:

  ```
  디자이너 소개 화면을 만들어줘. 상단에 프로필 사진, 이름과 직급(원장·실장 등), 평점. 전문 시술 태그 칩(예: 발레아쥬, 볼륨매직), 경력 소개 문단. 대표 작업 사진 포트폴리오를 3열 그리드로 크게 보여주고, 이 디자이너를 지정한 리뷰만 모아 보여줘. 예약 가능 요일을 요일 칩으로 표시하고 하단에 "이 디자이너로 예약하기" 버튼.
  ```

### RE-01 · 예약 1단계 - 시술 선택
- **소속 메뉴**: 예약하기
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-09-03 ~ 2026-09-04
- **기능정의**: 예약 진행 단계 표시(4단계) · 카테고리별 시술 목록과 가격 · 시술 다중 선택과 옵션 지정 · 선택 시술 합계 금액과 총 소요시간 자동 계산 · 선택 항목 요약 바 고정 · 다음 단계 버튼 활성 조건 안내
- **버튼 → 이동화면**:
  - `다음 단계` → `RE-02` 예약 2단계 - 디자이너 선택
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `시술 고르기` → 고른 시술에 표시가 남고, 아래 합계 금액과 예상 소요 시간이 바뀐다
  - `추가 옵션 체크` → 합계 금액이 그 자리에서 다시 계산된다
  - `시술을 하나도 안 고른 상태` → 「다음」 버튼이 눌리지 않게 흐려져 있다. 하나 고르면 켜진다
- **생성 프롬프트**:

  ```
  예약 1단계 시술 선택 화면을 만들어줘. 상단에 진행 스텝(1 시술 → 2 디자이너 → 3 날짜·시간 → 4 확인). 카테고리별 아코디언에 시술 목록(시술명, 소요시간, 가격, 선택 체크박스). 시술을 고르면 옵션(길이·디자인)을 고르는 영역이 펼쳐지게. 하단에 고정 바로 "선택 2건· 총 120분· 85,000원"과 다음 단계 버튼. 아무것도 선택하지 않으면 버튼은 비활성.
  ```

### RE-02 · 예약 2단계 - 디자이너 선택
- **소속 메뉴**: 예약하기
- **뒤로가기**: 좌측 상단에 `‹ 예약 1단계 - 시술 선택` — 누르면 `RE-01`로 간다
- **일정**: 2026-09-07 ~ 2026-09-08
- **기능정의**: 디자이너 카드 목록(사진·직급·평점·전문 시술) · 지정 안 함(아무나) 옵션 · 디자이너별 추가 지명료 표시 · 선택 시술 가능 여부 표시 · 이번 주 예약 여유 표시 · 이전/다음 단계 이동
- **버튼 → 이동화면**:
  - `다음 단계` → `RE-03` 예약 3단계 - 날짜·시간 선택
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `디자이너 카드` → 고른 사람에 테두리가 생기고, 아래 요약의 담당자 이름이 바뀐다
  - `「아무나 괜찮아요」` → 디자이너 선택이 풀리고 요약이 「지정 안 함」으로 바뀐다
- **생성 프롬프트**:

  ```
  예약 2단계 디자이너 선택 화면을 만들어줘. 진행 스텝 유지. 맨 위에 "디자이너 지정 안 함(빠른 예약)" 옵션 카드, 그 아래 디자이너 카드 목록(프로필 사진, 이름·직급, 평점, 전문 시술 태그, 지명료). 선택한 시술을 할 수 없는 디자이너는 흐리게 처리하고 "이 시술 불가" 표시. 각 카드에 이번 주 예약 여유(여유·보통·마감임박)를 배지로 보여줘. 하단에 이전/다음 버튼.
  ```

### RE-03 · 예약 3단계 - 날짜·시간 선택
- **소속 메뉴**: 예약하기
- **뒤로가기**: 좌측 상단에 `‹ 예약 1단계 - 시술 선택` — 누르면 `RE-01`로 간다
- **일정**: 2026-09-09 ~ 2026-09-11
- **기능정의**: 월 단위 캘린더와 예약 가능 날짜 표시 · 휴무일·마감일 비활성 처리 · 선택 날짜의 시간대 슬롯 목록 · 소요시간 기준 연속 슬롯 확보 여부 반영 · 잔여 슬롯 수 표시 · 다른 날짜 추천 · 이전/다음 단계
- **버튼 → 이동화면**:
  - `다음 단계` → `RE-04` 예약 4단계 - 확인 및 결제
  - `가능 시간 없음` → `RE-05` 예약 3단계 - 가능한 시간 없음
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `달력 날짜` → 고른 날에 표시가 남고, 아래 시간 슬롯이 그 날 것으로 다시 그려진다
  - `달력 ‹ ›` → 달이 바뀐다. 지난 날짜는 눌리지 않는다
  - `시간 슬롯` → 고른 시간에 표시가 남고, 아래 요약의 일시가 바뀐다
  - `이미 찬 시간 슬롯` → 눌리지 않고 흐리게 남는다
- **생성 프롬프트**:

  ```
  예약 3단계 날짜·시간 선택 화면을 만들어줘. 진행 스텝 유지. 좌측(모바일은 상단)에 월 단위 캘린더 — 예약 가능한 날은 진하게, 매장 휴무일과 마감된 날은 회색 비활성, 선택한 날은 딥 플럼으로 강조. 우측(모바일은 하단)에 선택한 날짜의 시간 슬롯을 30분 단위 칩으로 나열하고, 선택한 시술의 총 소요시간만큼 연속으로 비어 있는 슬롯만 선택 가능하게 해줘. 마감된 슬롯은 취소선 처리하고, 각 슬롯에 잔여 여부를 표시. 하단에 이전/다음 버튼.
  ```

### RE-04 · 예약 4단계 - 확인 및 결제
- **소속 메뉴**: 예약하기
- **뒤로가기**: 좌측 상단에 `‹ 예약 1단계 - 시술 선택` — 누르면 `RE-01`로 간다
- **일정**: 2026-09-14 ~ 2026-09-16
- **기능정의**: 예약 내용 요약(매장·시술·디자이너·일시·소요시간) · 요청사항 입력 · 쿠폰 적용과 적립금 사용 · 예약금 결제 수단 선택 · 최종 금액 계산 표시 · 취소·환불 규정 동의 체크박스 · 노쇼 정책 안내
- **버튼 → 이동화면**:
  - `예약 확정` → `RE-06` 예약 완료
  - `결제 실패` → `RE-07` 예약 - 결제 실패
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `쿠폰 고르기` → 할인 줄과 최종 결제 금액이 그 자리에서 다시 계산된다
  - `적립금 입력` → 쓰는 만큼 최종 금액이 줄어든다. 가진 것보다 많이 넣으면 최대치로 맞춰진다
  - `결제 수단 고르기` → 고른 것에 표시가 남고, 카드면 할부 개월 수 칸이 나타난다
  - `약관 동의 체크` → 다 체크해야 「결제하기」가 눌리게 켜진다
- **생성 프롬프트**:

  ```
  예약 4단계 확인·결제 화면을 만들어줘. 진행 스텝 유지. 좌측에 예약 내용 요약 카드(매장명, 시술 목록, 담당 디자이너, 예약 일시, 총 소요시간), 요청사항 입력 텍스트 영역(예: 앞머리는 짧게 해주세요), 쿠폰 선택과 적립금 사용 입력. 우측에 금액 요약(시술 금액, 지명료, 쿠폰 할인, 예약금, 현장 결제 예정액)을 구분해 표시. 하단에 취소·환불 규정과 노쇼 정책 동의 체크박스, 동의 전에는 결제 버튼 비활성.
  ```

### RE-05 · 예약 3단계 - 가능한 시간 없음
- **소속 메뉴**: 예약하기
- **뒤로가기**: 좌측 상단에 `‹ 예약 1단계 - 시술 선택` — 누르면 `RE-01`로 간다
- **일정**: 2026-09-17 ~ 2026-09-18
- **기능정의**: 선택 날짜에 가능한 시간이 없다는 안내 · 소요시간 때문임을 설명 · 가장 빠른 예약 가능 일시 제안 3개 · 다른 디자이너로 찾기 링크 · 대기 신청 등록 버튼 · 날짜 다시 고르기
- **버튼 → 이동화면**:
  - `다른 날짜` → `RE-03` 예약 3단계 - 날짜·시간 선택
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「다른 날짜 보기」` → 달력이 다음 자리 있는 날로 옮겨 가고 시간 슬롯이 다시 그려진다
  - `「대기 신청」 토글` → 켜면 알림 받을 연락 방법 칸이 나타난다
- **생성 프롬프트**:

  ```
  선택한 날짜에 예약 가능한 시간이 없을 때의 화면을 만들어줘. 시간 슬롯 영역에 아이콘과 "9월 12일에는 120분 연속으로 비는 시간이 없어요" 문구, "가장 빠른 예약 가능 시간" 3개를 카드로 제안(9/13 14:00 등), "다른 디자이너로 찾아보기" 링크와 "자리 나면 알림 받기(대기 신청)" 버튼, "다른 날짜 고르기" 버튼도 함께 넣어줘.
  ```

### RE-06 · 예약 완료
- **소속 메뉴**: 예약하기
- **뒤로가기**: 좌측 상단에 `‹ 예약 1단계 - 시술 선택` — 누르면 `RE-01`로 간다
- **일정**: 2026-09-21 ~ 2026-09-23
- **기능정의**: 예약 완료 메시지와 예약번호 · 예약 내용 요약 카드 · 매장 위치와 길찾기 · 캘린더에 추가 버튼 · 취소 가능 기한 안내 · 내 예약으로 이동 · 예약 변경·취소 링크
- **버튼 → 이동화면**:
  - `내 예약` → `MY-01` 내 예약 - 예정
  - `예약 상세` → `MY-03` 예약 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「캘린더에 추가」` → 일정이 담겼다는 안내가 뜬다
  - `예약 번호` → 눌러서 복사되고 「복사했어요」가 뜬다
- **생성 프롬프트**:

  ```
  예약 완료 화면을 만들어줘. 중앙에 성공 체크 아이콘과 "예약이 확정됐어요" 문구, 예약번호와 예약 내용 요약 카드(매장, 시술, 디자이너, 일시). 매장 위치 미니 지도와 "길찾기" 버튼, "내 캘린더에 추가" 버튼. "9월 11일 18시까지 무료 취소할 수 있어요" 안내 문구를 눈에 띄게, 하단에 "내 예약 보기" 버튼과 예약 변경·취소 링크.
  ```

### RE-07 · 예약 - 결제 실패
- **소속 메뉴**: 예약하기
- **뒤로가기**: 좌측 상단에 `‹ 예약 1단계 - 시술 선택` — 누르면 `RE-01`로 간다
- **일정**: 2026-09-24 ~ 2026-09-25
- **기능정의**: 실패 사유를 사람이 이해할 문장으로 안내 · 예약 내용은 유지된다는 안내 · 다시 시도 버튼 · 다른 결제 수단 선택 · 시간 선점 만료까지 남은 시간 표시 · 고객센터 문의 링크
- **버튼 → 이동화면**:
  - `다시 시도` → `RE-04` 예약 4단계 - 확인 및 결제
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `실패 사유 「자세히」` → 무엇이 문제였는지 설명이 펼쳐진다
  - `다른 결제 수단 고르기` → 고른 것에 표시가 남고 「다시 시도」 버튼 문구가 그 수단 이름으로 바뀐다
- **생성 프롬프트**:

  ```
  예약 결제 실패 화면을 만들어줘. 경고 아이콘과 "결제가 완료되지 않았어요" 문구, 실패 사유를 쉬운 문장으로 표시(예: 카드 한도를 초과했어요). "선택한 시간은 10분간 유지돼요" 남은 시간 카운트다운을 보여주고, "다시 시도" 버튼과 "다른 결제 수단 선택" 버튼, 하단에 고객센터 문의 링크. 예약 내용은 그대로 유지된다는 안내도 넣어줘.
  ```

### RE-08 · 대기 신청 완료
- **소속 메뉴**: 예약하기
- **뒤로가기**: 좌측 상단에 `‹ 예약 1단계 - 시술 선택` — 누르면 `RE-01`로 간다
- **일정**: 2026-09-28 ~ 2026-09-29
- **기능정의**: 대기 신청 완료 안내 · 신청한 조건 요약(매장·시술·희망 기간) · 알림 받을 방법 표시 · 현재 대기 순번 안내 · 대기 취소 버튼 · 다른 매장 둘러보기
- **버튼 → 이동화면**:
  - `매장 목록` → `SE-01` 매장 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「알림 방법」 고르기` → 문자/앱 알림 중 고른 것에 표시가 남는다
  - `「대기 취소」` → 확인을 한 번 물은 뒤 이 화면이 취소된 상태로 바뀐다
- **생성 프롬프트**:

  ```
  예약 대기 신청 완료 화면을 만들어줘. 체크 아이콘과 "자리가 나면 알려드릴게요" 문구, 신청 조건 요약(매장, 시술, 희망 날짜 범위, 지정 디자이너), "현재 대기 2번째" 안내와 알림 수신 방법(앱 푸시·문자) 표시, "대기 취소" 보조 버튼과 "다른 매장 둘러보기" 링크.
  ```

### MY-01 · 내 예약 - 예정
- **소속 메뉴**: 내 예약
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-09-30 ~ 2026-10-02
- **기능정의**: 예정/완료/취소 탭 구분 · 예약 카드(매장·시술·디자이너·일시·상태 배지) · D-day 표시 · 변경·취소 버튼과 가능 여부 · 길찾기 바로가기 · 지난 예약 재예약 버튼
- **버튼 → 이동화면**:
  - `예약 상세` → `MY-03` 예약 상세
  - `예약 변경` → `MY-04` 예약 변경
  - `예약 취소` → `MY-05` 예약 취소 - 확인
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `예정·완료·취소 탭` → 아래 목록이 그 상태 것으로 바뀌고 탭 옆 숫자도 함께 바뀐다
  - `예약 카드 접기/펴기` → 시술 상세와 매장 연락처가 펼쳐진다
- **생성 프롬프트**:

  ```
  내 예약 목록 화면을 만들어줘. 상단 탭(예정, 완료, 취소). 예약 카드에 매장 대표사진, 매장명, 시술명, 담당 디자이너, 예약 일시, D-day 배지, 상태 배지(확정·접수 대기·변경 요청 중)를 표시. 각 카드에 "변경", "취소", "길찾기" 버튼을 두되, 취소 마감이 지난 건은 취소 버튼을 비활성하고 "취소 마감" 툴팁을 보여줘. 완료 탭의 카드에는 "리뷰 쓰기"와 "같은 시술 재예약" 버튼.
  ```

### MY-02 · 내 예약 - 비어 있음
- **소속 메뉴**: 내 예약
- **뒤로가기**: 좌측 상단에 `‹ 내 예약 - 예정` — 누르면 `MY-01`로 간다
- **일정**: 2026-10-05 ~ 2026-10-06
- **기능정의**: 예정된 예약이 없다는 안내 · 일러스트 노출 · 매장 찾기 유도 버튼 · 최근 본 매장 4개 · 첫 예약 할인 쿠폰 안내
- **버튼 → 이동화면**:
  - `매장 찾기` → `SE-01` 매장 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「지난 예약 보기」` → 빈 화면 대신 지난 예약 목록이 그 자리에 나타난다
- **생성 프롬프트**:

  ```
  예정된 예약이 하나도 없을 때의 화면을 만들어줘. 일러스트와 "예정된 예약이 없어요" 문구, 큰 CTA "매장 둘러보기", 그 아래 최근 본 매장 4개 카드와 "첫 예약 20% 할인 쿠폰" 안내 배너를 넣어줘.
  ```

### MY-03 · 예약 상세
- **소속 메뉴**: 내 예약
- **뒤로가기**: 좌측 상단에 `‹ 내 예약 - 예정` — 누르면 `MY-01`로 간다
- **일정**: 2026-10-07 ~ 2026-10-09
- **기능정의**: 예약번호와 상태 타임라인(접수→확정→방문→완료) · 예약 내용 전체 · 결제 내역(예약금·현장 결제 예정) · 요청사항 확인 · 매장 연락처와 길찾기 · 취소 가능 기한과 수수료 안내 · 변경·취소 버튼
- **버튼 → 이동화면**:
  - `예약 변경` → `MY-04` 예약 변경
  - `예약 취소` → `MY-05` 예약 취소 - 확인
  - `리뷰 쓰기` → `RV-01` 리뷰 작성
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「예약 내용 더 보기」` → 고른 옵션·요청사항·결제 내역이 펼쳐진다
  - `「매장 위치」` → 지도가 그 자리에서 펼쳐진다
- **생성 프롬프트**:

  ```
  예약 상세 화면을 만들어줘. 상단에 예약번호와 진행 상태 타임라인 (접수 → 확정 → 방문 예정 → 완료)을 가로 스텝으로 표시. 예약 내용 전체(매장, 시술 목록과 각 소요시간, 디자이너, 일시, 요청사항), 결제 내역(예약금 결제 완료, 현장 결제 예정액 구분), 매장 전화 걸기와 길찾기 버튼, "9월 11일 18시 이후 취소 시 예약금이 환불되지 않아요" 같은 취소 규정 안내, 하단에 변경·취소 버튼.
  ```

### MY-04 · 예약 변경
- **소속 메뉴**: 내 예약
- **뒤로가기**: 좌측 상단에 `‹ 내 예약 - 예정` — 누르면 `MY-01`로 간다
- **일정**: 2026-10-12 ~ 2026-10-14
- **기능정의**: 현재 예약 내용 표시 · 변경 가능 항목 안내(일시·디자이너) · 새 날짜·시간 선택 · 변경 시 금액 차이 표시 · 변경 가능 횟수 남은 수 안내 · 매장 승인 필요 여부 안내 · 변경 요청 버튼
- **버튼 → 이동화면**:
  - `변경 요청` → `MY-03` 예약 상세
  - `변경 불가` → `MY-06` 예약 변경·취소 - 마감됨
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `달력 날짜·시간 슬롯` → 고른 것에 표시가 남고, 아래 「바뀌는 내용」이 이전 → 이후로 다시 쓰인다
  - `바꾼 게 없는 상태` → 「변경 신청」 버튼이 흐려져 눌리지 않는다
- **생성 프롬프트**:

  ```
  예약 변경 화면을 만들어줘. 상단에 현재 예약 내용을 회색 카드로 보여주고, 그 아래 변경할 날짜·시간을 고르는 캘린더와 시간 슬롯(예약 3단계와 같은 방식). 디자이너 변경 옵션도 함께 제공. 변경으로 금액이 달라지면 차액을 표시하고, "변경은 1회 더 가능해요", "매장 확인 후 확정돼요" 안내 문구를 넣어줘. 하단에 변경 요청 버튼.
  ```

### MY-05 · 예약 취소 - 확인
- **소속 메뉴**: 내 예약
- **뒤로가기**: 좌측 상단에 `‹ 내 예약 - 예정` — 누르면 `MY-01`로 간다
- **일정**: 2026-10-15 ~ 2026-10-16
- **기능정의**: 취소 대상 예약 요약 · 취소 사유 선택 · 환불 예정 금액과 수수료 계산 표시 · 되돌릴 수 없다는 경고 · 노쇼와의 차이 안내 · 최종 확인 체크박스 · 취소 확정 버튼
- **버튼 → 이동화면**:
  - `내 예약` → `MY-01` 내 예약 - 예정
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `취소 사유 고르기` → 고른 것에 표시가 남고, 「기타」를 고르면 직접 적는 칸이 나타난다
  - `사유를 안 고른 상태` → 「취소하기」가 눌리지 않는다
- **생성 프롬프트**:

  ```
  예약 취소 확인 화면(모달)을 만들어줘. 취소할 예약 요약과 "정말 취소하시겠어요?" 문구, 취소 사유 선택(일정 변경, 단순 변심, 매장 사정, 기타), 환불 예정 금액을 계산해 표시(예약금 20,000원 중 수수료 0원 차감 → 20,000원 환불), "취소 후에는 되돌릴 수 없어요" 경고를 눈에 띄게, 최종 확인 체크박스를 체크해야 "예약 취소하기" 버튼이 활성화되게 해줘.
  ```

### MY-06 · 예약 변경·취소 - 마감됨
- **소속 메뉴**: 내 예약
- **뒤로가기**: 좌측 상단에 `‹ 내 예약 - 예정` — 누르면 `MY-01`로 간다
- **일정**: 2026-10-19 ~ 2026-10-20
- **기능정의**: 변경·취소 가능 시간이 지났다는 안내와 마감 일시 · 입력 폼 비활성 처리 · 노쇼 시 불이익 안내 · 매장에 직접 연락하기 버튼 · 예약 상세로 돌아가기
- **버튼 → 이동화면**:
  - `예약 상세` → `MY-03` 예약 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「매장에 문의」` → 연락 방법이 펼쳐진다
- **생성 프롬프트**:

  ```
  예약 변경·취소 마감 화면을 만들어줘. 상단에 회색 안내 배너 "변경·취소 가능 시간이 지났어요 (마감: 9월 11일 18:00)", 예약 내용은 읽기 전용으로 보여주되 변경 폼은 비활성 처리, "방문하지 않으면 노쇼로 기록되어 다음 예약이 제한될 수 있어요" 주의 안내, "매장에 전화하기" 버튼과 예약 상세로 돌아가는 링크를 제공해줘.
  ```

### RV-01 · 리뷰 작성
- **소속 메뉴**: 리뷰
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-10-21 ~ 2026-10-23
- **기능정의**: 시술 정보 자동 표시 · 항목별 별점 입력(시술 만족도·친절도·청결도) · 시술 사진 첨부(최대 5장, 드래그앤드롭) · 리뷰 본문 입력과 최소 글자수 안내 · 재방문 의사 선택 · 작성 시 적립금 안내 · 임시 저장
- **버튼 → 이동화면**:
  - `등록` → `RV-03` 내 리뷰 목록
  - `작성 기간 지남` → `RV-04` 리뷰 작성 - 기간 지남
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `별점` → 누른 별까지 채워지고, 그에 맞는 안내 문구가 바뀐다
  - `사진 올리기` → 미리보기 칸이 늘어나고 「N/5장」 숫자가 올라간다
  - `미리보기의 ✕` → 그 사진이 빠지고 숫자가 줄어든다
  - `시술 만족도 항목(친절·실력·청결)` → 고른 값이 남고 아래 요약 점수가 다시 계산된다
- **생성 프롬프트**:

  ```
  리뷰 작성 화면을 만들어줘. 상단에 시술 정보(매장명, 시술명, 담당 디자이너, 방문일)를 자동으로 표시. 항목별 별점 입력 3종(시술 만족도, 친절도, 청결도)을 큰 별 아이콘으로, 시술 사진 첨부 영역(드래그앤드롭, 최대 5장, 썸네일 미리보기와 삭제), 리뷰 본문 텍스트 영역(최소 20자 안내와 현재 글자수), "재방문 의사가 있나요?" 예/아니오 선택, "사진 리뷰 작성 시 적립금 2,000원" 안내 배너, 하단에 임시 저장과 등록 버튼.
  ```

### RV-02 · 매장 리뷰 목록
- **소속 메뉴**: 리뷰
- **뒤로가기**: 좌측 상단에 `‹ 리뷰 작성` — 누르면 `RV-01`로 간다
- **일정**: 2026-10-26 ~ 2026-10-28
- **기능정의**: 전체 별점과 항목별 평균 · 별점 분포 막대 그래프 · 사진 리뷰만 보기 필터 · 시술별 필터 · 정렬(최신순/평점순/도움순) · 리뷰 카드(사진·별점·본문·시술명) · 매장 답글 강조 표시 · 도움이 됐어요 버튼
- **버튼 → 이동화면**:
  - `매장 상세` → `SE-04` 매장 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `별점 필터(전체/5점/4점/3점 이하)` → 리뷰 목록과 개수가 함께 걸러진다
  - `「사진 있는 리뷰만」 토글` → 켜면 사진 있는 것만 남는다
  - `「도움돼요」` → 숫자가 하나 오르고 다시 누르면 되돌아간다
  - `긴 리뷰의 「더 보기」` → 접혀 있던 뒷부분이 펼쳐진다
- **생성 프롬프트**:

  ```
  매장 리뷰 목록 화면을 만들어줘. 상단에 전체 평점 큰 숫자와 항목별 평균 3종, 별점 분포 막대 그래프(5점부터 1점까지). 필터 칩(사진 리뷰만, 시술별)과 정렬 드롭다운(최신순·평점순·도움순). 리뷰 카드에 작성자 닉네임, 별점, 시술명, 시술 사진 썸네일, 본문, 매장 답글은 들여쓰고 배경색을 다르게 해서 구분, 각 카드에 "도움이 됐어요" 버튼과 카운트를 넣어줘.
  ```

### RV-03 · 내 리뷰 목록
- **소속 메뉴**: 리뷰
- **뒤로가기**: 좌측 상단에 `‹ 리뷰 작성` — 누르면 `RV-01`로 간다
- **일정**: 2026-10-29 ~ 2026-10-30
- **기능정의**: 작성한 리뷰 목록과 작성일 · 매장 답글 여부 배지 · 받은 도움 수 표시 · 수정·삭제 버튼과 가능 기한 · 작성 가능한 리뷰 알림 영역 · 적립금 지급 내역 표시
- **버튼 → 이동화면**:
  - `리뷰 작성` → `RV-01` 리뷰 작성
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「수정」` → 그 줄이 입력 칸으로 바뀌고, 저장하면 다시 읽는 모양으로 돌아간다
  - `「삭제」` → 확인을 한 번 물은 뒤 그 줄이 목록에서 사라진다
- **생성 프롬프트**:

  ```
  내가 쓴 리뷰 목록 화면을 만들어줘. 상단에 "작성 가능한 리뷰 2건" 알림 영역(시술 완료했는데 아직 안 쓴 건, 바로 쓰기 버튼). 그 아래 작성한 리뷰 카드 목록(매장명, 시술명, 별점, 사진 썸네일, 본문 일부, 작성일), 매장 답글이 달린 리뷰에는 "답글 있음" 배지, 받은 "도움이 됐어요" 수와 지급받은 적립금 표시, 각 카드에 수정·삭제 버튼(작성 후 30일 이내만 가능하다고 안내).
  ```

### RV-04 · 리뷰 작성 - 기간 지남
- **소속 메뉴**: 리뷰
- **뒤로가기**: 좌측 상단에 `‹ 리뷰 작성` — 누르면 `RV-01`로 간다
- **일정**: 2026-11-02 ~ 2026-11-03
- **기능정의**: 작성 가능 기간이 지났다는 안내와 기한 표시 · 입력 폼 비활성 처리 · 기간 정책 안내 · 다른 작성 가능 리뷰 목록으로 이동 · 매장에 문의하기 링크
- **버튼 → 이동화면**:
  - `내 리뷰` → `RV-03` 내 리뷰 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「기간 안내」` → 왜 못 쓰는지 설명이 펼쳐진다
- **생성 프롬프트**:

  ```
  리뷰 작성 기간이 지났을 때의 화면을 만들어줘. 회색 안내 배너 "리뷰 작성 기간이 지났어요 (방문일로부터 30일)", 시술 정보는 읽기 전용으로 보여주되 별점과 본문 입력은 비활성 처리, "방문일로부터 30일 이내에만 작성할 수 있어요" 정책 안내, "작성 가능한 다른 리뷰 보기" 버튼과 매장 문의 링크를 제공해줘.
  ```

### ST-01 · 오늘 예약
- **소속 메뉴**: 예약 관리
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-11-04 ~ 2026-11-06
- **기능정의**: 오늘 날짜와 총 예약 건수 · 시간순 예약 목록(시간·고객명·시술·디자이너·상태) · 상태별 색상 구분 · 방문 완료 체크 · 노쇼 처리 버튼 · 고객 연락처 바로 걸기 · 날짜 이동 화살표
- **버튼 → 이동화면**:
  - `예약 상세` → `ST-04` 예약 상세 - 매장
  - `캘린더` → `ST-02` 예약 캘린더
  - `노쇼 처리` → `ST-06` 노쇼 처리 - 확인
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `시간대 필터(오전/오후/전체)` → 아래 예약 줄이 그 시간대만 남는다
  - `예약 줄의 「접수」` → 그 줄의 상태 배지가 「대기」에서 「확정」으로 바뀌고, 위 요약의 대기 건수가 하나 줄어든다
  - `예약 줄 펼치기` → 고객 연락처·요청사항·결제 내역이 펼쳐진다
- **생성 프롬프트**:

  ```
  매장의 오늘 예약 화면을 만들어줘. 상단에 오늘 날짜와 좌우 날짜 이동 화살표, "오늘 예약 14건· 접수 대기 2건" 요약. 시간순 예약 목록을 타임라인 형태로 — 각 행에 시간, 고객명, 시술명, 담당 디자이너, 상태 배지(확정·대기·완료·노쇼)를 색상으로 구분해 표시. 각 행에 "방문 완료" 체크 버튼, "전화 걸기" 아이콘, 더보기 메뉴(노쇼 처리, 예약 상세). 상태별 색: 확정 초록, 대기 노랑, 완료 회색, 노쇼 빨강.
  ```

### ST-02 · 예약 캘린더
- **소속 메뉴**: 예약 관리
- **뒤로가기**: 좌측 상단에 `‹ 오늘 예약` — 누르면 `ST-01`로 간다
- **일정**: 2026-11-09 ~ 2026-11-11
- **기능정의**: 주/월 보기 전환 · 디자이너별 행으로 나뉜 타임 그리드 · 예약 블록에 고객명·시술 표시 · 블록 드래그로 시간 이동 · 휴무·휴가 구간 회색 처리 · 빈 시간 클릭 시 예약 추가 · 디자이너 필터
- **버튼 → 이동화면**:
  - `예약 상세` → `ST-04` 예약 상세 - 매장
  - `예약 없음` → `ST-03` 예약 목록 - 예약 없음
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `달력 ‹ ›` → 주·월이 바뀌고 예약 칸이 다시 그려진다
  - `일/주/월 보기 전환` → 달력 모양이 바뀐다
  - `달력 안의 예약 칸` → 그 자리에서 미리보기가 뜬다
  - `디자이너 필터` → 고른 사람의 예약만 달력에 남는다
- **생성 프롬프트**:

  ```
  매장 예약 캘린더 화면을 만들어줘. 상단에 주/월 보기 전환과 디자이너 필터. 주 보기는 가로축이 날짜, 세로축이 시간인 타임 그리드로 만들고 예약을 색상 블록으로 표시(블록 안에 고객명과 시술명, 블록 높이는 소요시간에 비례). 디자이너별로 색을 다르게 하고, 매장 휴무일과 디자이너 휴가 구간은 회색 빗금 처리. 빈 시간을 클릭하면 예약 추가 팝업이 열리게, 블록은 드래그로 시간 이동 가능하게 해줘.
  ```

### ST-03 · 예약 목록 - 예약 없음
- **소속 메뉴**: 예약 관리
- **뒤로가기**: 좌측 상단에 `‹ 오늘 예약` — 누르면 `ST-01`로 간다
- **일정**: 2026-11-12 ~ 2026-11-13
- **기능정의**: 해당 기간에 예약이 없다는 안내 · 선택 기간 표시 · 다른 날짜 보기 버튼 · 예약이 없는 이유 점검 안내(휴무 설정·시술 메뉴 미등록) · 매장 노출 상태 확인 링크
- **버튼 → 이동화면**:
  - `시술 메뉴` → `MG-01` 시술 메뉴 관리
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `기간 바꾸기` → 빈 화면 대신 그 기간의 예약이 있으면 목록이 나타난다
- **생성 프롬프트**:

  ```
  예약이 없을 때의 화면을 만들어줘. 아이콘과 "9월 12일에는 예약이 없어요" 문구, "다른 날짜 보기" 버튼과 함께, "혹시 이런 설정을 확인해 보세요" 체크리스트 (휴무일로 설정되어 있지 않은지, 시술 메뉴가 등록되어 있는지, 매장이 노출 중인지)를 각각 해당 설정 화면 링크와 함께 보여줘.
  ```

### ST-04 · 예약 상세 - 매장
- **소속 메뉴**: 예약 관리
- **뒤로가기**: 좌측 상단에 `‹ 오늘 예약` — 누르면 `ST-01`로 간다
- **일정**: 2026-11-16 ~ 2026-11-18
- **기능정의**: 고객 정보와 방문 이력 요약 · 예약 내용 전체와 요청사항 · 이전 시술 기록과 사용 제품 메모 · 결제 상태(예약금·현장 결제) · 상태 변경 버튼(확정·완료·노쇼) · 내부 메모 작성 · 고객에게 메시지 보내기
- **버튼 → 이동화면**:
  - `예약 접수` → `ST-05` 예약 접수 대기
  - `노쇼 처리` → `ST-06` 노쇼 처리 - 확인
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「메모 남기기」` → 적는 칸이 펼쳐지고, 저장하면 메모가 줄로 쌓인다
  - `상태 바꾸기(확정/완료/노쇼)` → 배지가 바뀌고 아래 이력에 한 줄이 쌓인다
- **생성 프롬프트**:

  ```
  매장이 보는 예약 상세 화면을 만들어줘. 상단에 고객 정보(이름, 연락처, 총 방문 횟수, 최근 방문일, 노쇼 이력). 예약 내용(시술 목록, 담당 디자이너, 일시, 총 소요시간)과 고객 요청사항을 강조해서 표시. "이전 시술 기록" 섹션에 지난 방문의 시술과 사용 제품 메모를 보여줘. 결제 상태(예약금 20,000원 결제 완료, 현장 결제 예정 65,000원), 우측에 상태 변경 버튼(예약 확정, 방문 완료, 노쇼 처리)과 내부 메모 입력, "고객에게 메시지 보내기" 버튼.
  ```

### ST-05 · 예약 접수 대기
- **소속 메뉴**: 예약 관리
- **뒤로가기**: 좌측 상단에 `‹ 오늘 예약` — 누르면 `ST-01`로 간다
- **일정**: 2026-11-19 ~ 2026-11-23
- **기능정의**: 접수 대기 예약 목록과 요청 시각 · 남은 응답 시간 표시 · 요청 시간대 예약 가능 여부 자동 표시 · 확정·거절 버튼 · 거절 사유 선택 · 대체 시간 제안 기능 · 일괄 확정
- **버튼 → 이동화면**:
  - `예약 상세` → `ST-04` 예약 상세 - 매장
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「접수」·「거절」` → 그 줄이 목록에서 빠지고 위의 대기 건수가 줄어든다
  - `거절 사유 고르기` → 고른 것에 표시가 남고 「거절」이 눌리게 켜진다
- **생성 프롬프트**:

  ```
  예약 접수 대기 화면을 만들어줘. 상단에 "접수 대기 3건"과 일괄 확정 버튼. 각 카드에 고객명, 요청 시술, 희망 일시, 요청이 들어온 시각과 "2시간 내 응답 필요" 남은 시간 배지. 해당 시간대가 비어 있는지 자동으로 확인해 "예약 가능"/"다른 예약과 겹침"을 표시. 각 카드에 "확정", "거절", "다른 시간 제안" 버튼을 두고, 거절을 누르면 사유 선택(예약 마감, 디자이너 부재, 시술 불가)이 뜨게 해줘.
  ```

### ST-06 · 노쇼 처리 - 확인
- **소속 메뉴**: 예약 관리
- **뒤로가기**: 좌측 상단에 `‹ 오늘 예약` — 누르면 `ST-01`로 간다
- **일정**: 2026-11-24 ~ 2026-11-25
- **기능정의**: 대상 예약과 고객 정보 확인 · 노쇼 처리 시 영향 안내(고객 페널티·예약금 처리) · 고객의 기존 노쇼 횟수 표시 · 되돌릴 수 없다는 경고 · 사유 메모 입력 · 최종 확인 체크박스
- **버튼 → 이동화면**:
  - `오늘 예약` → `ST-01` 오늘 예약
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `노쇼 사유 고르기` → 고른 것에 표시가 남고, 아래 「고객에게 갈 안내」 문구가 그에 맞게 바뀐다
- **생성 프롬프트**:

  ```
  노쇼 처리 확인 화면(모달)을 만들어줘. "이 예약을 노쇼로 처리할까요?" 문구와 예약 요약(고객명, 시술, 일시), "이 고객의 노쇼 이력: 1회" 표시, 처리 시 영향 안내(예약금은 환불되지 않고 매장에 정산됩니다 / 고객은 30일간 예약이 제한됩니다), "노쇼 처리 후에는 되돌릴 수 없어요" 경고, 사유 메모 입력란과 최종 확인 체크박스를 체크해야 버튼이 활성화되게 해줘.
  ```

### MG-01 · 시술 메뉴 관리
- **소속 메뉴**: 매장 운영
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-11-26 ~ 2026-11-30
- **기능정의**: 카테고리 추가와 순서 변경 · 카테고리별 시술 목록 · 시술 추가·수정(이름·소요시간·가격·설명) · 옵션 항목 설정(길이·난이도별 추가금) · 대표 사진 업로드 · 노출/숨김 토글 · 드래그로 순서 변경
- **버튼 → 이동화면**:
  - `디자이너 관리` → `MG-02` 디자이너 관리
  - `사진 업로드` → `MG-05` 사진 업로드 - 실패
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `메뉴 줄의 「공개」 토글` → 켜고 끄면 배지가 「공개/비공개」로 바뀌고 위의 공개 메뉴 수가 따라 바뀐다
  - `「+ 메뉴 추가」` → 빈 줄이 하나 늘어나고 커서가 그 칸에 간다
  - `줄의 ↑↓` → 메뉴 순서가 실제로 바뀐다
  - `가격·소요 시간 입력` → 아래 미리보기 카드의 값이 따라 바뀐다
- **생성 프롬프트**:

  ```
  시술 메뉴 관리 화면을 만들어줘. 이 화면이 매장 운영의 핵심이야. 좌측에 카테고리 목록(헤어, 네일, 왁싱 등)과 카테고리 추가 버튼, 드래그로 순서 변경. 우측에 선택한 카테고리의 시술 목록 — 각 행에 시술명, 소요시간, 가격, 대표 사진 썸네일, 노출/숨김 토글, 수정·삭제 버튼. 시술을 추가하거나 수정하면 패널이 열려 이름, 소요시간(분), 기본 가격, 옵션별 추가금 표(길이·난이도), 설명, 사진 업로드를 입력하게 해줘. 시술 순서도 드래그로 바꿀 수 있게.
  ```

### MG-02 · 디자이너 관리
- **소속 메뉴**: 매장 운영
- **뒤로가기**: 좌측 상단에 `‹ 시술 메뉴 관리` — 누르면 `MG-01`로 간다
- **일정**: 2026-12-01 ~ 2026-12-03
- **기능정의**: 디자이너 목록(사진·이름·직급·상태) · 디자이너 추가와 정보 수정 · 담당 가능 시술 지정 · 지명료 설정 · 포트폴리오 사진 관리 · 재직/휴직 상태 전환 · 예약 노출 순서 변경
- **버튼 → 이동화면**:
  - `근무 일정` → `MG-03` 근무 일정 관리
  - `포트폴리오` → `MG-05` 사진 업로드 - 실패
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `디자이너 줄의 「근무 중」 토글` → 배지가 바뀌고 예약 받을 수 있는 사람 수가 따라 바뀐다
  - `「+ 디자이너 추가」` → 입력 줄이 하나 늘어난다
  - `담당 시술 고르기(복수)` → 고른 것들이 칩으로 쌓이고 다시 누르면 빠진다
- **생성 프롬프트**:

  ```
  디자이너 관리 화면을 만들어줘. 상단에 "디자이너 추가" 버튼. 디자이너 카드 목록에 프로필 사진, 이름, 직급, 상태 배지(재직·휴직), 이번 달 예약 건수와 평점을 표시. 각 카드에 수정·근무일정·포트폴리오 버튼. 수정 패널에서는 이름, 직급, 소개, 지명료, 담당 가능 시술 체크박스, 프로필 사진을 설정하게 하고, 예약 화면에 보이는 순서도 드래그로 바꿀 수 있게 해줘.
  ```

### MG-03 · 근무 일정 관리
- **소속 메뉴**: 매장 운영
- **뒤로가기**: 좌측 상단에 `‹ 시술 메뉴 관리` — 누르면 `MG-01`로 간다
- **일정**: 2026-12-04 ~ 2026-12-08
- **기능정의**: 디자이너별 주간 근무 시간 설정 · 요일별 근무/휴무 지정 · 휴게 시간 설정 · 특정 날짜 예외 등록(휴가·조퇴) · 반복 일정 적용 · 일정 충돌 경고 · 변경 시 기존 예약 영향 표시
- **버튼 → 이동화면**:
  - `휴무 설정` → `MG-04` 휴무·예약 정책 설정
  - `예약 캘린더` → `ST-02` 예약 캘린더
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `달력 칸 끌기` → 근무 시간이 그만큼 칠해진다. 다시 끌면 지워진다
  - `「이 주 복사」` → 다음 주 칸이 같은 모양으로 채워진다
  - `디자이너 탭` → 달력이 그 사람 일정으로 바뀐다
- **생성 프롬프트**:

  ```
  디자이너 근무 일정 관리 화면을 만들어줘. 상단에 디자이너 선택 탭. 요일별 근무 시간을 설정하는 표(월~일, 각 행에 근무 여부 토글, 시작·종료 시간, 휴게 시간). "매주 반복" 기본 일정과 별개로, 특정 날짜의 예외를 등록하는 영역 (날짜 선택 + 휴가/조퇴/추가근무 구분). 일정을 바꿨을 때 이미 잡힌 예약과 겹치면 경고 배너로 "이 변경으로 9월 14일 예약 2건이 영향을 받아요"를 보여주고 해당 예약 목록도 함께.
  ```

### MG-04 · 휴무·예약 정책 설정
- **소속 메뉴**: 매장 운영
- **뒤로가기**: 좌측 상단에 `‹ 시술 메뉴 관리` — 누르면 `MG-01`로 간다
- **일정**: 2026-12-09 ~ 2026-12-11
- **기능정의**: 매장 정기 휴무일 지정 · 임시 휴무 날짜 등록 · 예약 가능 시작 시점(며칠 전부터) · 예약 마감 시점(몇 시간 전까지) · 취소 가능 기한과 수수료율 · 노쇼 정책 · 예약금 비율 설정 · 자동 확정 여부
- **버튼 → 이동화면**:
  - `매장 운영` → `MG-01` 시술 메뉴 관리
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `휴무일 고르기` → 달력에 표시가 남고 「이번 달 휴무 N일」이 바뀐다
  - `예약 정책 값(최소 예약 시간·취소 마감)` → 바꾸면 아래 「고객에게 이렇게 보입니다」 문구가 그 값으로 다시 쓰인다
- **생성 프롬프트**:

  ```
  휴무·예약 정책 설정 화면을 만들어줘. 섹션을 나눠 구성해줘. 1) 휴무 설정 — 정기 휴무 요일 선택, 임시 휴무 날짜를 캘린더에서 다중 선택 2) 예약 접수 — 며칠 전부터 예약 가능한지, 몇 시간 전까지 예약 받을지 숫자 입력 3) 취소 정책 — 무료 취소 가능 기한(시간), 기한 이후 수수료율(%) 슬라이더 4) 노쇼 정책 — 노쇼 시 제한 기간(일), 예약금 처리 방식 5) 예약금 — 시술 금액의 몇 %를 예약금으로 받을지 6) 자동 확정 — 예약을 자동으로 확정할지, 매장이 직접 확인할지 라디오 설정을 바꾸면 "이렇게 적용돼요" 예시 문장이 실시간으로 갱신되게 해줘.
  ```

### MG-05 · 사진 업로드 - 실패
- **소속 메뉴**: 매장 운영
- **뒤로가기**: 좌측 상단에 `‹ 시술 메뉴 관리` — 누르면 `MG-01`로 간다
- **일정**: 2026-12-14 ~ 2026-12-15
- **기능정의**: 실패 사유 표시(용량 초과·형식 불일치·개수 초과) · 실패한 파일만 선택적 재업로드 · 성공한 파일 유지 안내 · 권장 사이즈와 형식 재안내 · 이미지 자동 압축 안내
- **버튼 → 이동화면**:
  - `시술 메뉴` → `MG-01` 시술 메뉴 관리
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `실패한 파일 줄의 「다시 시도」` → 그 줄만 다시 올라가고 상태가 바뀐다
  - `「실패한 것만 보기」 토글` → 목록이 걸러진다
- **생성 프롬프트**:

  ```
  매장·포트폴리오 사진 업로드 실패 화면을 만들어줘. 업로드한 파일 목록을 보여주고 실패한 파일은 빨간 테두리로 구분, 각각 실패 사유를 명확한 문장으로 표시 (예: 파일 용량이 10MB를 초과했어요 / 지원하지 않는 형식이에요 / 최대 20장까지 올릴 수 있어요), "실패한 파일만 다시 올리기" 버튼, 성공한 파일은 유지된다는 안내와 권장 사이즈(1200x1200 이상, JPG·PNG) 재안내.
  ```

### AC-01 · 내 정보
- **소속 메뉴**: 내 정보
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-12-16 ~ 2026-12-17
- **기능정의**: 프로필 사진과 닉네임 수정 · 연락처 인증과 변경 · 알림 수신 설정(예약 알림·마케팅) · 관심 지역과 관심 시술 설정 · 매장 입점 신청 버튼 · 회원 탈퇴 링크
- **버튼 → 이동화면**:
  - `쿠폰·적립금` → `AC-02` 쿠폰·적립금
  - `매장 입점` → `PT-02` 매장 입점 신청
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「수정」` → 읽는 모양이던 칸이 입력 칸으로 바뀌고, 저장하면 되돌아온다
  - `알림 받기 토글(예약·이벤트·마케팅)` → 켜고 끄는 것이 그대로 남는다
- **생성 프롬프트**:

  ```
  마이페이지 내 정보 화면을 만들어줘. 좌측에 사이드 메뉴 (내 정보, 쿠폰·적립금, 내 예약, 내 리뷰, 알림 설정). 우측 본문에 프로필 사진 업로드와 닉네임, 연락처(인증 버튼 포함), 이메일, 알림 수신 토글(예약 확정·리마인더·마케팅 수신 구분), 관심 지역과 관심 시술 태그 선택, 하단에 "매장 입점 신청" 버튼과 회색 작은 글씨로 회원 탈퇴 링크.
  ```

### AC-02 · 쿠폰·적립금
- **소속 메뉴**: 내 정보
- **뒤로가기**: 좌측 상단에 `‹ 내 정보` — 누르면 `AC-01`로 간다
- **일정**: 2026-12-18 ~ 2026-12-22
- **기능정의**: 보유 적립금 잔액 강조 표시 · 적립·사용 내역 목록 · 사용 가능 쿠폰 목록과 만료일 · 만료 임박 쿠폰 강조 · 쿠폰 코드 등록 입력 · 사용 완료·만료 탭 구분
- **버튼 → 이동화면**:
  - `정산 내역` → `SA-01` 매장 정산 내역
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `쿠폰·적립금 탭` → 아래 목록이 바뀌고 상단 숫자도 함께 바뀐다
  - `「사용 가능만 보기」 토글` → 만료·사용된 것이 목록에서 빠진다
  - `쿠폰 코드 넣고 「등록」` → 목록 맨 위에 한 장이 늘어나고 「사용 가능 N장」이 올라간다
- **생성 프롬프트**:

  ```
  쿠폰·적립금 화면을 만들어줘. 상단에 보유 적립금 잔액을 큰 숫자로, 그 아래 탭(사용 가능, 사용 완료, 만료). 쿠폰 카드는 티켓 모양으로 — 할인 금액 또는 할인율, 사용 조건, 만료일 표시, 만료가 7일 이내면 빨간 배지로 "곧 만료" 강조. 하단에 쿠폰 코드 직접 등록 입력창과 등록 버튼, 적립금 적립·사용 내역 목록(날짜, 내용, 증감액)도 함께 보여줘.
  ```

### PT-01 · 매장 홈
- **소속 메뉴**: 매장 홈
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-12-23 ~ 2026-12-25
- **기능정의**: 오늘 예약 요약 카드 4종(오늘 예약·대기 접수·이번 달 매출·평균 별점) · 처리가 필요한 일 목록(접수 대기·변경 요청·노쇼 확인) · 오늘 시간대별 예약 타임라인 · 최근 7일 예약 추이 차트 · 디자이너별 예약 현황 · 최근 리뷰 5건 · 미답변 리뷰 알림 · 이번 달 정산 예정 요약 · 빠른 실행 버튼 3종
- **버튼 → 이동화면**:
  - `예약 접수` → `ST-05` 예약 접수 대기
  - `오늘 예약` → `ST-01` 오늘 예약
  - `시술 메뉴` → `MG-01` 시술 메뉴 관리
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `기간 탭(오늘/이번 주/이번 달)` → 아래 지표 숫자와 추이 차트가 그 기간 것으로 바뀐다
  - `「처리할 일」 줄의 처리 버튼` → 그 줄이 목록에서 빠지고 위의 건수가 줄어든다
- **생성 프롬프트**:

  ```
  매장이 로그인한 상태의 홈(대시보드) 화면을 만들어줘. 스크롤 순서대로 배치해줘. 1) 요약 지표 카드 4개 — 오늘 예약 건수, 접수 대기, 이번 달 매출, 평균 별점(숫자 크게) 2) "처리가 필요한 일" — 접수 대기 예약, 변경 요청, 노쇼 확인 건수와 바로가기 3) 오늘 시간대별 예약 타임라인(가로 축이 시간, 디자이너별 행) 4) 최근 7일 예약 추이 라인 차트 5) 디자이너별 예약 현황 막대 6) 최근 리뷰 5건(별점, 한 줄, 시술명) 7) 미답변 리뷰 알림 8) 이번 달 정산 예정 요약 카드 9) 빠른 실행 버튼(예약 접수, 시술 메뉴 관리, 근무 일정) 지표 숫자는 딥 플럼으로 강조.
  ```

### PT-02 · 매장 입점 신청
- **소속 메뉴**: 매장 홈
- **뒤로가기**: 좌측 상단에 `‹ 매장 홈` — 누르면 `PT-01`로 간다
- **일정**: 2026-12-28 ~ 2026-12-30
- **기능정의**: 매장 기본 정보 입력(상호·주소·연락처) · 사업자등록증 첨부 · 대표 시술 카테고리 선택 · 매장 사진 업로드 · 영업시간 입력 · 정산 계좌 등록 · 입점 심사 절차 안내 · 약관 동의
- **버튼 → 이동화면**:
  - `신청 완료` → `AC-01` 내 정보
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `시술 분야 고르기(복수)` → 고른 것이 칩으로 쌓이고 다시 누르면 빠진다
  - `사업자등록번호 입력` → 형식이 맞으면 초록 확인 표시, 틀리면 빨간 안내가 칸 아래에 뜬다
  - `서류 끌어다 놓기` → 파일 줄이 늘어나고 「N/5개」가 올라간다
  - `필수 칸이 빈 상태` → 「제출」이 눌리지 않고, 어느 칸이 비었는지 표시된다
- **생성 프롬프트**:

  ```
  매장 입점 신청 화면을 만들어줘. 상단에 진행 스텝(1 매장정보 → 2 서류 → 3 심사). 매장 기본 정보 입력(상호, 주소 검색, 연락처, 대표자명), 대표 시술 카테고리 다중 선택, 영업시간 입력(요일별 시작·종료), 매장 사진 업로드(최소 3장 안내), 사업자등록증 파일 첨부, 정산 계좌 등록. 우측에 "입점 심사는 3~5영업일이 걸려요" 절차 안내 카드, 하단에 입점 약관 동의 체크박스와 신청 버튼.
  ```

### PT-03 · 입점 심사 중
- **소속 메뉴**: 매장 홈
- **뒤로가기**: 좌측 상단에 `‹ 매장 홈` — 누르면 `PT-01`로 간다
- **일정**: 2026-12-31 ~ 2027-01-01
- **기능정의**: 심사 진행 단계 표시(접수·서류 확인·승인) · 제출한 정보 요약 · 보완 요청 안내 영역 · 예상 소요 안내 · 담당자 문의 · 신청 취소 · 심사 중에도 볼 수 있는 준비 안내
- **버튼 → 이동화면**:
  - `매장 홈` → `PT-01` 매장 홈
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「심사 단계」` → 각 단계에 무엇을 보는지 설명이 펼쳐진다
- **생성 프롬프트**:

  ```
  매장 입점 심사가 진행 중일 때의 화면을 만들어줘. 상단에 3단계 진행 표시(접수 완료 → 서류 확인 중 → 승인)에서 현재 단계를 강조하고, 제출한 매장 정보 요약 카드(상호, 주소, 대표 시술, 첨부 서류 목록)를 보여줘. 보완이 필요하면 무엇을 다시 올려야 하는지 안내 박스로 띄우고, "보통 3~5영업일이 걸려요" 안내와 담당자 문의 버튼, 하단에 신청 취소 링크. 심사를 기다리는 동안 미리 해둘 일(시술 메뉴 정리, 매장 사진 준비) 체크리스트도 함께.
  ```

### SA-01 · 매장 정산 내역
- **소속 메뉴**: 정산
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2027-01-04 ~ 2027-01-06
- **기능정의**: 월별 정산 요약 카드 3종(예상 정산액·누적 정산액·정산 예정일) · 예약 건별 정산 내역 테이블 · 플랫폼 수수료 공제 내역 · 노쇼 예약금 정산 구분 표시 · 정산 상태 배지 · 세금계산서 발행 · 정산 계좌 관리
- **버튼 → 이동화면**:
  - `내역 없음` → `SA-02` 정산 내역 - 내역 없음
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `기간 고르기` → 표와 위의 합계 금액이 그 기간 것으로 다시 계산된다
  - `표 머리글(날짜·금액)` → 그 기준으로 정렬되고 화살표 방향이 바뀐다
  - `정산 줄 펼치기` → 건별 내역과 수수료 계산이 펼쳐진다
- **생성 프롬프트**:

  ```
  매장 정산 내역 화면을 만들어줘. 상단에 요약 카드 3개 (이번 달 예상 정산액, 누적 정산액, 다음 정산 예정일). 월 선택 드롭다운과 함께 예약 건별 정산 테이블 (예약일, 고객명, 시술, 결제 금액, 플랫폼 수수료, 실 정산액, 상태 배지). 노쇼로 처리되어 예약금만 정산되는 건은 별도 배지로 구분, 하단에 합계 행과 세금계산서 발행 버튼, 우측 상단에 정산 계좌 관리 링크.
  ```

### SA-02 · 정산 내역 - 내역 없음
- **소속 메뉴**: 정산
- **뒤로가기**: 좌측 상단에 `‹ 매장 정산 내역` — 누르면 `SA-01`로 간다
- **일정**: 2027-01-07 ~ 2027-01-08
- **기능정의**: 선택 기간에 정산 내역이 없다는 안내 · 다른 기간 선택 유도 · 정산 주기 안내 · 첫 정산 시점 안내 · 예약 받기 위한 점검 항목 링크
- **버튼 → 이동화면**:
  - `시술 메뉴` → `MG-01` 시술 메뉴 관리
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `기간 바꾸기` → 내역이 있으면 빈 화면 대신 표가 나타난다
- **생성 프롬프트**:

  ```
  정산 내역이 없을 때의 화면을 만들어줘. 아이콘과 "2026년 8월에는 정산 내역이 없어요" 문구, "다른 기간 보기" 버튼과 정산 주기 안내("매월 10일에 전월 정산액을 지급해요"), 아직 예약이 없는 매장을 위해 "예약을 받으려면 확인하세요" 체크리스트 (시술 메뉴 등록, 디자이너 등록, 근무 일정 설정)를 링크와 함께 보여줘.
  ```

### AU-01 · 로그인
- **소속 메뉴**: 계정
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2027-01-11 ~ 2027-01-13
- **기능정의**: 이메일·비밀번호 입력 · 소셜 로그인 3종(카카오·네이버·애플) · 로그인 상태 유지 체크 · 비밀번호 찾기 링크 · 회원가입 링크 · 입력 오류 안내 문구 자리 · 로그인 후 원래 보던 화면으로 돌아가기
- **버튼 → 이동화면**:
  - `회원가입` → `AU-02` 회원가입
  - `비밀번호 찾기` → `AU-05` 비밀번호 찾기
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「비밀번호 보기」 눈 아이콘` → 가려져 있던 글자가 보이고 다시 누르면 가려진다
  - `빈 칸으로 로그인 시도` → 어느 칸이 비었는지 칸 아래에 빨간 안내가 뜬다
- **생성 프롬프트**:

  ```
  로그인 화면을 만들어줘. 헤더·GNB 없이 가운데 정렬된 단독 화면으로. 로고 아래 이메일과 비밀번호 입력칸, "로그인 상태 유지" 체크박스, 큰 로그인 버튼, 그 아래 비밀번호 찾기 링크. 구분선 아래 소셜 로그인 버튼 3개(카카오 노란색, 네이버 초록색, 애플 검정)를 세로로, 맨 아래 "아직 회원이 아니신가요? 회원가입" 링크. 입력이 잘못됐을 때 뜨는 안내 문구 자리도 만들어줘.
  ```

### AU-02 · 회원가입
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `AU-01`로 간다
- **일정**: 2027-01-14 ~ 2027-01-18
- **기능정의**: 이메일·비밀번호·비밀번호 확인 입력 · 비밀번호 조건 실시간 표시 · 닉네임 입력 · 휴대폰 번호 입력 · 약관 동의(전체·필수 2종·선택 1종) · 소셜로 간편 가입 · 이미 가입된 이메일 안내
- **버튼 → 이동화면**:
  - `다음` → `AU-03` 회원가입 - 휴대폰 인증
  - `로그인` → `AU-01` 로그인
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `비밀번호 입력` → 치는 대로 아래 세기 막대가 약함→보통→강함으로 바뀐다
  - `비밀번호 확인 칸` → 위와 다르면 그 자리에서 빨간 안내가 뜬다
  - `약관 「전체 동의」` → 아래 개별 체크가 한꺼번에 켜지고, 하나를 끄면 전체 동의도 풀린다
- **생성 프롬프트**:

  ```
  회원가입 화면을 만들어줘. 헤더 없이 단독 화면으로. 상단에 진행 표시(1 정보 입력 → 2 인증 → 3 완료). 이메일, 비밀번호, 비밀번호 확인 입력칸을 두고 비밀번호 조건(8자 이상·영문+숫자)을 입력하는 동안 체크 표시로 알려줘. 닉네임과 휴대폰 번호 입력칸, 약관 동의 영역은 "전체 동의" 하나와 그 아래 이용약관(필수), 개인정보 처리방침(필수), 마케팅 수신(선택)을 각각 체크박스로, 각 항목 오른쪽에 전문 보기 링크. 하단에 다음 버튼, 위쪽에 "소셜 계정으로 간편 가입" 버튼 3개. 이미 가입된 이메일일 때 뜨는 안내 문구 자리도 만들어줘.
  ```

### AU-03 · 회원가입 - 휴대폰 인증
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `AU-01`로 간다
- **일정**: 2027-01-19 ~ 2027-01-21
- **기능정의**: 휴대폰 번호 확인 표시 · 인증번호 6자리 입력 · 남은 시간 카운트다운 · 재발송 버튼(대기 시간 표시) · 번호 바꾸기 · 인증번호 불일치 안내 · 인증 완료 시 다음 단계
- **버튼 → 이동화면**:
  - `인증 완료` → `AU-04` 회원가입 완료
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「인증번호 받기」` → 입력 칸이 열리고 3분 타이머가 줄어들기 시작한다
  - `타이머가 0이 됨` → 칸이 잠기고 「다시 받기」가 켜진다
  - `인증번호 6자리를 다 넣음` → 「확인」이 눌리게 켜진다
- **생성 프롬프트**:

  ```
  회원가입 중 휴대폰 인증 단계 화면을 만들어줘. 진행 표시에서 2단계를 강조하고, "010-1234-5678로 인증번호를 보냈어요" 문구 아래 6칸짜리 인증번호 입력칸을 크게 두고, 오른쪽에 남은 시간(03:00 카운트다운), 아래에 "인증번호 다시 받기" 버튼(재발송 대기 중이면 남은 초 표시)과 "번호 바꾸기" 링크. 인증번호가 틀렸을 때 입력칸이 빨갛게 바뀌고 안내가 뜨는 상태도 만들어줘.
  ```

### AU-04 · 회원가입 완료
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `AU-01`로 간다
- **일정**: 2027-01-22 ~ 2027-01-25
- **기능정의**: 가입 축하 메시지와 닉네임 표시 · 신규 가입 쿠폰 지급 안내 · 관심 지역·시술 고르기 유도 · 첫 예약 하러 가기 · 매장 운영자라면 입점 신청 안내 · 앱 알림 허용 안내
- **버튼 → 이동화면**:
  - `매장 둘러보기` → `SE-01` 매장 목록
  - `입점 신청` → `PT-02` 매장 입점 신청
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「관심 시술 고르기」` → 고른 것이 칩으로 쌓이고, 아래 추천 매장이 그에 맞게 바뀐다
- **생성 프롬프트**:

  ```
  회원가입 완료 화면을 만들어줘. 헤더 없이 가운데 정렬. 큰 체크 아이콘과 "○○님, 가입을 환영해요" 문구, 그 아래 신규 가입 쿠폰 카드(할인 금액과 사용 기한)를 눈에 띄게. "관심 지역과 시술을 고르면 딱 맞는 매장을 추천해드려요" 안내와 함께 지역·시술 칩을 몇 개 고를 수 있게 두고, 큰 버튼으로 "매장 둘러보기", 그 아래 작은 링크로 "매장을 운영하시나요? 입점 신청하기". 맨 아래 알림 허용 안내 문구.
  ```

### AU-05 · 비밀번호 찾기
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `AU-01`로 간다
- **일정**: 2027-01-26 ~ 2027-01-27
- **기능정의**: 가입 이메일 입력 · 재설정 메일 발송 안내 · 메일이 안 올 때 안내(스팸함·오타) · 다시 보내기 · 가입 이력 없음 안내 · 소셜 가입 계정 안내 · 로그인으로 돌아가기
- **버튼 → 이동화면**:
  - `로그인` → `AU-01` 로그인
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `찾는 방법 고르기(문자/이메일)` → 아래 입력 칸이 그 방법에 맞게 바뀐다
  - `형식에 안 맞는 값 입력` → 칸 아래에 무엇이 틀렸는지 뜬다
- **생성 프롬프트**:

  ```
  비밀번호 찾기 화면을 만들어줘. 헤더 없이 단독 화면으로. "가입하신 이메일을 알려주시면 재설정 링크를 보내드려요" 문구와 이메일 입력칸, 큰 "재설정 메일 보내기" 버튼. 보낸 뒤 상태도 함께 만들어줘 — 메일 아이콘과 "○○@메일로 보냈어요", "메일이 오지 않았나요? 스팸함을 확인하거나 다시 보내기"(60초 대기 표시). 가입 이력이 없을 때와, 소셜로 가입한 계정일 때 ("카카오로 가입하신 계정이에요") 각각의 안내 문구 자리도 만들어줘. 맨 아래 로그인으로 돌아가기 링크.
  ```

## 5. 화면 이동 흐름 (FLOW)
- `홈 - 비로그인`(HO-01) —[매장 찾기]→ `매장 목록`(SE-01)
- `홈 - 비로그인`(HO-01) —[로그인]→ `로그인`(AU-01)
- `홈 - 고객 로그인`(HO-02) —[예약 상세]→ `예약 상세`(MY-03)
- `홈 - 고객 로그인`(HO-02) —[내 예약]→ `내 예약 - 예정`(MY-01)
- `매장 목록`(SE-01) —[매장 상세]→ `매장 상세`(SE-04)
- `매장 목록`(SE-01) —[지도 보기]→ `매장 지도`(SE-03)
- `매장 목록`(SE-01) —[검색]→ `매장 목록 - 검색 결과 없음`(SE-02)
- `매장 목록 - 검색 결과 없음`(SE-02) —[필터 초기화]→ `매장 목록`(SE-01)
- `매장 지도`(SE-03) —[매장 상세]→ `매장 상세`(SE-04)
- `매장 상세`(SE-04) —[예약하기]→ `예약 1단계 - 시술 선택`(RE-01)
- `매장 상세`(SE-04) —[디자이너 보기]→ `디자이너 소개`(SE-06)
- `매장 상세`(SE-04) —[시술 메뉴]→ `시술 메뉴 상세`(SE-05)
- `시술 메뉴 상세`(SE-05) —[예약하기]→ `예약 1단계 - 시술 선택`(RE-01)
- `디자이너 소개`(SE-06) —[예약하기]→ `예약 1단계 - 시술 선택`(RE-01)
- `예약 1단계 - 시술 선택`(RE-01) —[다음 단계]→ `예약 2단계 - 디자이너 선택`(RE-02)
- `예약 2단계 - 디자이너 선택`(RE-02) —[다음 단계]→ `예약 3단계 - 날짜·시간 선택`(RE-03)
- `예약 3단계 - 날짜·시간 선택`(RE-03) —[다음 단계]→ `예약 4단계 - 확인 및 결제`(RE-04)
- `예약 3단계 - 날짜·시간 선택`(RE-03) —[가능 시간 없음]→ `예약 3단계 - 가능한 시간 없음`(RE-05)
- `예약 4단계 - 확인 및 결제`(RE-04) —[예약 확정]→ `예약 완료`(RE-06)
- `예약 4단계 - 확인 및 결제`(RE-04) —[결제 실패]→ `예약 - 결제 실패`(RE-07)
- `예약 3단계 - 가능한 시간 없음`(RE-05) —[다른 날짜]→ `예약 3단계 - 날짜·시간 선택`(RE-03)
- `예약 완료`(RE-06) —[내 예약]→ `내 예약 - 예정`(MY-01)
- `예약 완료`(RE-06) —[예약 상세]→ `예약 상세`(MY-03)
- `예약 - 결제 실패`(RE-07) —[다시 시도]→ `예약 4단계 - 확인 및 결제`(RE-04)
- `대기 신청 완료`(RE-08) —[매장 목록]→ `매장 목록`(SE-01)
- `내 예약 - 예정`(MY-01) —[예약 상세]→ `예약 상세`(MY-03)
- `내 예약 - 예정`(MY-01) —[예약 변경]→ `예약 변경`(MY-04)
- `내 예약 - 예정`(MY-01) —[예약 취소]→ `예약 취소 - 확인`(MY-05)
- `내 예약 - 비어 있음`(MY-02) —[매장 찾기]→ `매장 목록`(SE-01)
- `예약 상세`(MY-03) —[예약 변경]→ `예약 변경`(MY-04)
- `예약 상세`(MY-03) —[예약 취소]→ `예약 취소 - 확인`(MY-05)
- `예약 상세`(MY-03) —[리뷰 쓰기]→ `리뷰 작성`(RV-01)
- `예약 변경`(MY-04) —[변경 요청]→ `예약 상세`(MY-03)
- `예약 변경`(MY-04) —[변경 불가]→ `예약 변경·취소 - 마감됨`(MY-06)
- `예약 취소 - 확인`(MY-05) —[내 예약]→ `내 예약 - 예정`(MY-01)
- `예약 변경·취소 - 마감됨`(MY-06) —[예약 상세]→ `예약 상세`(MY-03)
- `리뷰 작성`(RV-01) —[등록]→ `내 리뷰 목록`(RV-03)
- `리뷰 작성`(RV-01) —[작성 기간 지남]→ `리뷰 작성 - 기간 지남`(RV-04)
- `매장 리뷰 목록`(RV-02) —[매장 상세]→ `매장 상세`(SE-04)
- `내 리뷰 목록`(RV-03) —[리뷰 작성]→ `리뷰 작성`(RV-01)
- `리뷰 작성 - 기간 지남`(RV-04) —[내 리뷰]→ `내 리뷰 목록`(RV-03)
- `오늘 예약`(ST-01) —[예약 상세]→ `예약 상세 - 매장`(ST-04)
- `오늘 예약`(ST-01) —[캘린더]→ `예약 캘린더`(ST-02)
- `오늘 예약`(ST-01) —[노쇼 처리]→ `노쇼 처리 - 확인`(ST-06)
- `예약 캘린더`(ST-02) —[예약 상세]→ `예약 상세 - 매장`(ST-04)
- `예약 캘린더`(ST-02) —[예약 없음]→ `예약 목록 - 예약 없음`(ST-03)
- `예약 목록 - 예약 없음`(ST-03) —[시술 메뉴]→ `시술 메뉴 관리`(MG-01)
- `예약 상세 - 매장`(ST-04) —[예약 접수]→ `예약 접수 대기`(ST-05)
- `예약 상세 - 매장`(ST-04) —[노쇼 처리]→ `노쇼 처리 - 확인`(ST-06)
- `예약 접수 대기`(ST-05) —[예약 상세]→ `예약 상세 - 매장`(ST-04)
- `노쇼 처리 - 확인`(ST-06) —[오늘 예약]→ `오늘 예약`(ST-01)
- `시술 메뉴 관리`(MG-01) —[디자이너 관리]→ `디자이너 관리`(MG-02)
- `시술 메뉴 관리`(MG-01) —[사진 업로드]→ `사진 업로드 - 실패`(MG-05)
- `디자이너 관리`(MG-02) —[근무 일정]→ `근무 일정 관리`(MG-03)
- `디자이너 관리`(MG-02) —[포트폴리오]→ `사진 업로드 - 실패`(MG-05)
- `근무 일정 관리`(MG-03) —[휴무 설정]→ `휴무·예약 정책 설정`(MG-04)
- `근무 일정 관리`(MG-03) —[예약 캘린더]→ `예약 캘린더`(ST-02)
- `휴무·예약 정책 설정`(MG-04) —[매장 운영]→ `시술 메뉴 관리`(MG-01)
- `사진 업로드 - 실패`(MG-05) —[시술 메뉴]→ `시술 메뉴 관리`(MG-01)
- `내 정보`(AC-01) —[쿠폰·적립금]→ `쿠폰·적립금`(AC-02)
- `내 정보`(AC-01) —[매장 입점]→ `매장 입점 신청`(PT-02)
- `쿠폰·적립금`(AC-02) —[정산 내역]→ `매장 정산 내역`(SA-01)
- `매장 홈`(PT-01) —[예약 접수]→ `예약 접수 대기`(ST-05)
- `매장 홈`(PT-01) —[오늘 예약]→ `오늘 예약`(ST-01)
- `매장 홈`(PT-01) —[시술 메뉴]→ `시술 메뉴 관리`(MG-01)
- `매장 입점 신청`(PT-02) —[신청 완료]→ `내 정보`(AC-01)
- `입점 심사 중`(PT-03) —[매장 홈]→ `매장 홈`(PT-01)
- `매장 정산 내역`(SA-01) —[내역 없음]→ `정산 내역 - 내역 없음`(SA-02)
- `정산 내역 - 내역 없음`(SA-02) —[시술 메뉴]→ `시술 메뉴 관리`(MG-01)
- `로그인`(AU-01) —[회원가입]→ `회원가입`(AU-02)
- `로그인`(AU-01) —[비밀번호 찾기]→ `비밀번호 찾기`(AU-05)
- `회원가입`(AU-02) —[다음]→ `회원가입 - 휴대폰 인증`(AU-03)
- `회원가입`(AU-02) —[로그인]→ `로그인`(AU-01)
- `회원가입 - 휴대폰 인증`(AU-03) —[인증 완료]→ `회원가입 완료`(AU-04)
- `회원가입 완료`(AU-04) —[매장 둘러보기]→ `매장 목록`(SE-01)
- `회원가입 완료`(AU-04) —[입점 신청]→ `매장 입점 신청`(PT-02)
- `비밀번호 찾기`(AU-05) —[로그인]→ `로그인`(AU-01)

## 6. 빌드 가이드
### 구조
- 각 **화면ID**를 하나의 라우트/페이지로 만드세요 (예: `PCHOME0110` → `/home`).
- 화면명은 페이지 제목·컴포넌트 이름의 기준으로 사용하세요.
- **2장 공통 요소**(헤더·GNB·푸터)를 공용 레이아웃으로 만들고 모든 화면이 감싸도록 하세요.
- **쓰는 사람이 다른 메뉴는 헤더를 나누세요.** 손님이 쓰는 메뉴(홈·매장 탐색·예약하기·내 예약)와 운영자가 쓰는 메뉴(예약 관리·매장 운영·매장 홈·정산)를 **한 헤더에 나란히 놓지 마세요.** 둘은 사실상 다른 사이트입니다.
  · 손님 영역과 운영자 영역을 각각 다른 레이아웃(헤더·사이드바)으로 만들고, 오갈 때는 **따로 마련한 진입점** 하나로만 이동하세요(예: 우측 상단 `예약 관리 화면으로`, 운영자 화면에서는 `고객 화면 보기`).
  · 운영자 영역은 보통 좌측 사이드바 + 상단 바가 맞습니다. 손님 영역의 검색·카테고리 내비를 그대로 쓰지 마세요.
- **버튼 → 이동화면** 관계를 링크/네비게이션으로 연결하세요.
- **4장 각 화면의 `눌렀을 때 (이 화면 안에서 끝남)` 항목을 그대로 만드세요.** 적혀 있는 것은 빠짐없이 실제로 동작해야 합니다 — 누르면 값이 바뀌고, 다시 누르면 되돌아옵니다.
  · 그 항목에 없더라도 **누를 수 있게 생긴 것은 전부 눌리게** 만드세요. 적힌 것은 최소한이지 전부가 아닙니다.
- **누를 수 있게 보이는 것은 전부 눌리게 만드세요.** 두 종류로 나눠 처리합니다.
  · **화면 안에서 끝나는 조작은 진짜로 동작해야 합니다** — 탭 전환, 좌측 카테고리·필터 선택, 목록에서 항목 고르기(디자이너·옵션·시간), 펼치기·접기, 캘린더 이전/다음 달, 수량 ±, 체크·토글. 자바스크립트로 실제 상태를 바꾸고 화면을 다시 그리세요. 이걸 안내 문구로 때우지 마세요.
  · **서버가 있어야 하는 것만** 짧은 안내(토스트)로 대신합니다 — 저장·삭제·발송·결제·전화 걸기·이의 신청 등.
  · **이런 것은 토스트로 때우지 마세요** — `모두 보기`(펼치기), `선택`·`적용`(고른 상태로 바꾸기), `추가`(줄 하나 늘리기), `닫기`·`✕`(정말 닫기), `도움이 됐어요`(숫자 올리기), 탭·필터·정렬. 전부 화면 안에서 끝나는 일이라 실제로 되어야 합니다.
  · **모달·팝업은 뜨는 것만으로 끝이 아닙니다.** `취소`와 `✕`는 정말 닫히고, `확인`은 눌렀을 때 무엇이 달라지는지 보여야 합니다(다음 화면으로 가거나, 목록이 바뀌거나, 안내가 뜨거나). 닫히지 않는 팝업은 갇힌 화면이 됩니다.
  · 아무 것도 하지 않는 `<button>`을 남기지 마세요. 만들고 나서 **버튼을 하나씩 눌러 보고**, 반응이 없는 것이 있으면 위 둘 중 하나로 채우세요.
- **뒤로가기는 화면마다 정해져 있습니다. 4장 각 화면 스펙의 `뒤로가기` 항목을 그대로 만드세요.** 38개 화면에 붙고, 나머지 11개(메뉴 첫 화면)에는 없습니다. 좌측 상단에 `‹ 상위 화면 이름`과 현재 위치(예: `매장 탐색 › 매장 상세`)를 둡니다.
  · 여러 단계짜리 흐름(1→2→3)은 **이전 단계로 돌아갈 수 있게** 하세요. 되돌아갔을 때 앞서 고른 값이 남아 있어야 합니다.
  · **한 묶음으로 나란히 보여주는 것은 동일 계위입니다.** 탭 줄에 걸린 화면들, 같은 사이드바에 나란히 올라온 화면들이 그렇습니다. 그 안을 오가는 길이 이미 탭·사이드바이므로 뒤로가기를 겹쳐 두지 마세요 — 같은 줄에 있는 화면인데 어떤 건 있고 어떤 건 없어집니다.
  · 빈 화면·오류·마감처럼 막다른 화면에는 **다음에 할 일 버튼**을 반드시 하나 두세요(예: `목록으로`, `다시 시도`, `둘러보기`).
  · **전체 화면 목록으로 돌아가는 길**을 모든 화면에 두되, 이름에 목적이 드러나게 하세요(`화면 목록`). 접힌 패널 안에 `화면 정보` 같은 이름으로 넣으면 아무도 못 찾습니다.
  · 다 만든 뒤 **화면을 하나씩 열어 뒤로가기가 있는지 세어 보세요.** 하나라도 빠져 있으면 채웁니다.
### 화면 구현
- 각 화면의 **생성 프롬프트**를 그 화면 구현 지시로 그대로 사용하세요.
- 반복 UI(카드·리스트 행·폼·버튼)는 **공용 컴포넌트**로 만들어 재사용하세요.
- 필요하면 상태 화면(로딩·빈 결과·에러)도 함께 만드세요.
### 디자인
- **디자인 프리셋 파일을 함께 넣었다면 그것이 우선입니다.** 1장의 디자인 컨셉이나 화면별 프롬프트에 다른 색·톤이 적혀 있어도 프리셋을 따르세요. 충돌이 아니라 **프리셋이 정답**입니다.
- 프리셋의 **레이아웃 골격**(히어로·목록·내비·상세를 어떻게 놓을지)을 먼저 잡고 색을 입히세요. 색만 맞추면 어떤 테마로 만들어도 같은 화면이 나옵니다.
- 프리셋이 없다면 1장의 **디자인 컨셉**을 전역 스타일(색·톤·라운드·여백)로 토큰화해 일관되게 적용하세요.
- **글꼴은 프리셋이 없어도 한 벌로 고정하세요.** 기본은 `Paperlogy`이고, 못 불러오면 `Pretendard` → `Noto Sans KR` 순으로 물러납니다. CSS로는 `font-family: Paperlogy, Pretendard, 'Noto Sans KR', sans-serif` 한 줄이면 됩니다.
  · **글꼴은 한 벌만 씁니다.** 제목과 본문에 서로 다른 글꼴을 쓰지 마세요 — 두 벌이 되는 순간 어디에 무엇을 쓸지가 화면마다 갈립니다. 위계는 글꼴이 아니라 **크기와 굵기**로 만듭니다.
  · 굵기는 **400·600·700 셋만**, 크기는 본문 15 · 카드 제목 18 · 섹션 제목 22 · 페이지 제목 32을 기준으로 잡으세요.

#### 읽기 폭 — 글을 한 단으로 좁힐 때

글이 주인공인 자리(상세 본문·안내·약관)는 **최대 760px** 로 좁히고 가운데 정렬합니다.
콘텐츠 영역(위의 최대 폭) **안에서** 좁히는 것입니다 — 바깥 폭을 바꾸는 게 아닙니다.

**왜 좁히나** — 한 줄이 길면 다음 줄 첫 글자를 눈이 못 찾습니다. 1440px 폭에 본문을 꽉 채우면
한 줄에 90자가 넘어가고, 읽는 사람은 두세 줄마다 같은 줄을 다시 읽게 됩니다.

- **이 값 하나만 씁니다.** 화면마다 680·720·760 처럼 다르게 잡지 마세요 — 저희도 그렇게 적혀 있었고,
  같은 팩 안의 상세 화면끼리 본문 폭이 달라 서로 다른 사이트처럼 보였습니다.
- **카드 격자에는 쓰지 않습니다.** 여기는 «글»을 좁히는 값입니다. 카드를 몇 열로 놓을지는 레이아웃이 정합니다.
- 사진·표·코드처럼 넓어야 읽히는 것은 이 폭을 넘겨도 됩니다. 좁히는 건 글줄입니다.

#### 격자 — 카드를 늘어놓는 자리

카드·타일을 격자로 늘어놓을 때 **좌우와 위아래를 다른 값으로** 벌립니다.

| 자리 | 값 |
| --- | --- |
| **카드와 카드 사이** (좌우) | 위 눈금의 「카드와 카드 사이 (가로)」 |
| **줄과 줄 사이** (위아래) | 위 눈금의 「덩어리와 덩어리 사이」 |

**같은 값을 쓰면 줄이 안 갈립니다.** 카드에 상자가 있을 때는 테두리가 경계를 만들어 줬지만,
상자를 걷어낸 뒤로는 **위 카드의 가격과 아래 카드의 사진이 붙어** 한 덩어리로 읽힙니다.
위아래를 더 벌려야 "여기까지가 한 줄"이 보입니다.

| 나누는 것 | 정하는 곳 |
| --- | --- |
| 몇 열로 나눌지 | 레이아웃 프리셋 |
| **사이를 얼마나 벌릴지** | **위 간격 눈금 (좌우·위아래 따로)** |
| 카드가 어떻게 생겼는지 | 카드 모양 |

- **레이아웃 프리셋이 「넓게」라고 해도 값을 새로 만들지 마세요.** 레이아웃은 *몇 열로 무엇을 어디에*를
  말하는 층이고, *얼마나 벌릴지*는 이 눈금이 정합니다. 실제로 그 한 줄 때문에 40px을 쓴 격자가 있었고,
  같은 화면의 다른 격자는 16px이라 두 값이 나란히 놓였습니다.

**간격은 변수로 못 박고, 자리마다 그것만 씁니다.** CSS 맨 위에 이걸 그대로 붙여 넣으세요.

```css
:root{
  --wrap: 1440px;      /* 콘텐츠 폭 — 헤더·본문·푸터·고정바가 모두 이 값을 쓴다 */
  --pad-x: 24px;    /* 좌우 여백 */
  --card-gap:   16px;     /* 카드와 카드 사이 — 좌우 */
  --card-gap-y: 28px;     /* 카드 줄과 줄 사이 — 위아래 */
  --side-w:     260px;               /* 사이드바 폭 (사이드바 뼈대에서만) */
  --side-gap:   32px;                /* 사이드바와 본문 사이 */
  --row-gap:    14px;                /* 가로 행 카드 안: 썸네일과 글자 사이 */

  --card-pad:   24px;                /* 카드 안쪽 여백 */
  --row-h:      48px;                /* 표 한 줄 높이 — 안 정하면 줄마다 들쭉날쭉해진다 */
  --btn-gap:    8px;                 /* 나란히 놓은 버튼 사이 */
}
@media (max-width: 720px){
  :root{ --pad-x: 16px; --card-gap: 12px; }
}
.wrap { max-width: var(--wrap); margin-inline: auto; padding-inline: var(--pad-x); }
.cards{ display: grid; column-gap: var(--card-gap); row-gap: var(--card-gap-y); }

/* 지표 줄 — 화면 맨 위에 숫자 카드를 나란히 놓는 자리.
   카드 격자(.cards)와 다른 물건이므로 .cards 를 쓰지 않는다. 지표는 넷이 기본이다. */
.stats{ display: grid; grid-template-columns: repeat(4, 1fr);
        column-gap: var(--card-gap); row-gap: var(--card-gap-y); }
@media (max-width: 1024px){ .stats{ grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 720px){ .stats{ grid-template-columns: 1fr; } }

/* 모자이크 — 홈 첫 화면에 「큰 1장 + 작은 4장」을 놓는 자리.
   첫 장이 2칸×2줄, 나머지 넷이 옆을 채운다.
   어느 것을 크게 할지 class 로 정하지 않는다 — 맨 앞에 둔 것이 큰 것이다. */
.mosaic{ display: grid; grid-template-columns: repeat(4, 1fr);
         column-gap: var(--card-gap); row-gap: var(--card-gap-y); }
.mosaic > :first-child{ grid-column: span 2; grid-row: span 2;
                        display: flex; flex-direction: column; }
/* 큰 칸의 사진은 카드 비율을 따르지 않는다 — 두 줄 높이와 안 맞아 제목이 밀려난다.
   큰 칸에서는 사진이 남는 자리를 채운다. */
.mosaic > :first-child > .thumb{ aspect-ratio: auto; flex: 1; min-height: 0; }
@media (max-width: 1024px){
  .mosaic{ grid-template-columns: repeat(2, 1fr); }
  .mosaic > :first-child{ grid-row: auto; }   /* 2열에서 세로로 겹치면 빈칸이 생긴다 */
}
@media (max-width: 720px){
  .mosaic{ grid-template-columns: 1fr; }
  .mosaic > :first-child{ grid-column: auto; }
}

/* 상세 화면의 대표 사진 — 카드 비율을 그대로 쓰지 않는다.
   4:3 을 본문 폭 1392px 에 적용하면 세로가 1044px 이 되어 노트북 화면을 통째로 덮는다.
   가로로 눕히고(16:9) 세로를 420px 로 막는다 — 사진 아래 내용이 같이 보이는 선이다. */
.detail-img{ aspect-ratio: 16 / 9; max-height: 420px; width: 100%;
             border-radius: 12px; overflow: hidden; }
.detail-img > img{ width: 100%; height: 100%; object-fit: cover; display: block; }

/* 간격은 반드시 이 변수로 쓴다. gap:16px 처럼 숫자를 직접 적으면 자리마다 값이 갈린다. */
```

- **`--card-pad`·`--row-h`·`--btn-gap` 도 꼭 쓰세요.** 카드 안쪽 여백, 표 한 줄 높이,
  버튼 사이 간격입니다. 이 셋을 안 정해주면 화면마다 다르게 나옵니다 — 저희도 겪었습니다.
- **`.stats`·`.mosaic`·`.detail-img` 는 카드 격자와 다른 자리입니다.** 지표 숫자 줄,
  홈의 큰 사진 한 장 + 작은 넷, 상세 화면의 대표 사진. 카드 규칙을 여기 갖다 쓰면
  사진이 화면을 다 덮습니다.

- **격자에 `gap: 20px` 처럼 숫자를 직접 쓰지 마세요.** 한 번 쓰는 순간 그 자리만 달라지고,
  나중에 값을 바꿀 때 그 자리만 안 바뀝니다. 실제로 한 사이트에서 좌우가 3·8·16·20·24px
  다섯 가지로 갈라진 적이 있습니다 — 전부 `gap:` 에 숫자를 적어서 생긴 일입니다.
- **`gap` 한 줄로 합치지도 마세요.** 좌우와 위아래는 값이 다르므로 `column-gap`·`row-gap` 둘로 씁니다.
- **이름도 이대로 씁니다.** `--grid-x`·`--gap-col` 처럼 제 나름의 이름을 지으면, 나중에
  다른 사람이 볼 때 그게 같은 자리인지 알 수가 없습니다. 실제로 같은 규칙을 준 두 사이트가
  하나는 `--grid-x/--grid-y`, 하나는 `--gap-col/--gap-row` 를 써서 값이 따로 놀았습니다.
- 값을 바꾸고 싶으면 **`:root` 의 두 줄만** 고칩니다. 그러면 화면 전체가 한 번에 따라옵니다.
- 히어로의 좌우 2단처럼 **카드 격자가 아닌 자리**는 이 규칙 밖입니다. 거기는 화면 뼈대라 레이아웃이 정합니다.

#### 박스형 콘텐츠 — 몇 개를 늘어놓고 사진은 어떤 비율로

사진이 든 카드를 격자로 늘어놓을 때, **몇 개를 놓느냐가 사진 비율을 정합니다.**

| 한 줄에 | 사진 비율 | 왜 |
| --- | --- | --- |
| **2개** | **4:3** | 칸이 넓으니 가로로 넓은 사진 |
| **3개** | **4:3** | 위와 같음 |
| **4개** | **1:1** | 칸이 좁다. 정사각이 가장 얌전하다 |

4개짜리를 세로로 긴 3:4로 뒀더니 **카드 한 줄이 화면을 다 먹었습니다.** 칸이 좁을수록 세로로
길게 두고 싶어지지만, 그러면 한 줄만 보이고 다음 단락이 안 보입니다. 좁은 칸에는 정사각입니다.

- **한 줄에 하나만 두지 않습니다.** 마지막 줄에도 하나만 남기지 마세요 —
  **항목 수를 열 수에 맞춰** 3열이면 3·6·9개, 4열이면 4·8개로 둡니다.
- **한 사이트에서 카드 사진 비율은 하나입니다.** 위 표의 짝(2·3열↔4:3, 4열↔1:1) 밖의 비율은 만들지 마세요.
  같은 홈에 세로 카드와 16:9가 섞여 있어서 **사는 사람이 사진을 두 벌 준비해야 했던** 적이 있습니다.
- **배너·차트·영상·프로필은 이 규칙 밖입니다.** 가로로 긴 배너·영상(16:9)·프로필(1:1)은 제 나름의 비율이 있습니다.
- 상품 사진처럼 **정사각이 업계 표준인 분야**는 1:1을 써도 됩니다. 다만 그때도 한 사이트 안에서는 하나로 갑니다.
- **가로 배너 한 장으로 한 단락을 채우지 마세요.** 배너는 위 표 밖이지만 "한 줄에 하나만 두지 않는다"는 그대로 걸립니다.
  배너 한 장이 있던 자리는 **두 장으로 나누고** 4:3으로 맞추세요.
- **가로로 넘기는 줄(캐러셀)도 몇 개 보이느냐로 셉니다.** 뒤에 더 있어도 눈에 4개가 보이면 4개 — 비율도 1:1입니다.
  카드 폭을 `260px`처럼 박아 두면 화면 폭이 바뀔 때 3.4개씩 어정쩡하게 걸치니, **칸 수로 나눠서** 정하세요.
- **가로로 넘기는 줄에는 좌우 화살표를 두고, 아래 스크롤바는 감춥니다.** 넘길 게 있다는 건
  화살표가 알려 줍니다. 스크롤바까지 나오면 줄 밑에 회색 막대가 하나 더 생겨 지저분합니다.

```css
.carousel{ scrollbar-width:none; -ms-overflow-style:none; }
.carousel::-webkit-scrollbar{ display:none; }
```

  화살표가 **없는데** 스크롤바까지 감추면 넘길 게 있다는 걸 아무도 모릅니다. 둘은 한 벌입니다.

#### 분리형과 혼합형 — 글자를 사진 아래 두느냐 위에 얹느냐

박스형 콘텐츠는 두 가지입니다. **어느 쪽이든 위의 개수·비율 규칙은 똑같이 적용됩니다.**

| | **분리형** | **혼합형** |
| --- | --- | --- |
| 생김새 | 사진 아래에 글자 | 사진 위에 글자를 얹음 |
| 담을 수 있는 것 | 제목·설명·평점·가격까지 여러 줄 | 제목 + 한 줄 정도 |
| 사진이 하는 일 | 무엇인지 알려 준다 | 분위기를 만든다 |
| 쓰는 자리 | 상품·강의 목록처럼 **고르는** 자리 | 특집·기획전처럼 **끌어당기는** 자리 |

**분리형은 상자를 두르지 마세요.** 흰 배경에 테두리·그림자까지 두르면, 사진과 글자가 이미 한 덩어리인데
선을 한 번 더 치는 셈입니다. 격자로 늘어놓으면 그 선이 줄줄이 겹쳐 화면이 답답해집니다.
**모서리는 사진이 가집니다** — 상자를 없앤 자리에서 사진이 카드 노릇을 합니다.
(혼합형은 사진이 곧 배경이라 이 규칙 밖입니다.)

```css
/* 사진 + 글자로 된 카드 — 상자를 두르지 않는다 */
.card{
  background: none;   /* surface 나 흰색을 깔지 않는다 */
  border: 0;
  box-shadow: none;
}
.card .thumb{ border-radius: 12px; }  /* 모서리는 사진이 가진다 */
.card .body{ padding: 16px 0 0; }     /* 좌우 여백 0 — 상자가 없으니 안쪽 여백도 없다 */
```

**`background: var(--surface)` 나 `border: 1px solid` 를 카드에 쓰면 이 규칙을 어긴 것입니다.**
사진이 없는 상자(설정 패널·안내 상자·표를 감싼 것)는 이 규칙 밖입니다 — 거기는 배경이 있어야
덩어리로 읽힙니다. **사진과 글자로 된 카드**만 걷어냅니다.

상자를 걷어내면 두 가지가 따라옵니다.

- **글자가 흰색이 아니라 페이지 배경 위**에 놓입니다. 대비를 다시 재세요.
- **카드끼리 갈라 주던 테두리가 없어집니다.** 줄과 줄 사이를 더 벌려 그 몫을 대신하세요.

그리고 **카드 안에는 선을 긋지 마세요.** 가격 위에 선을 두면 상자가 없는 상태에서는 그 선이 카드를
가르는 것처럼 보여, 사진·제목과 가격이 따로 노는 두 덩어리로 읽힙니다.
**갈라야 할 것은 카드 안이 아니라 카드끼리**입니다.

혼합형을 쓸 때 반드시 지킬 것:

- **어둠막을 까세요.** 사진 위에 흰 글자를 그냥 얹으면 사진의 밝은 자리에서 글자가 사라집니다.
  **사진은 사는 분이 바꿀 것**이라 우리가 밝기를 미리 알 수 없습니다.
  글자 쪽에서 반대편으로 옅어지는 어둠막을 항상 깔아 두면 어떤 사진이 와도 읽힙니다.
- **글자 대비 4.5 이상**을 지키세요. 가장 밝은 사진(흰색)이 왔다고 치고 재세요.
- **글자를 짧게 쓰세요.** 제목 한 줄 + 설명 한 줄까지입니다. 그보다 길면 사진을 다 덮어 혼합형인 의미가 없습니다.
- **가격·평점처럼 정확히 읽어야 하는 것은 얹지 마세요.** 사진 위에서는 숫자가 잘 안 읽힙니다.
- **한 화면에 두 가지를 섞어도 됩니다.** 위는 혼합형 특집, 아래는 분리형 목록 — 하는 일이 다르므로 어수선하지 않습니다.

#### 글꼴은 한 벌입니다

**기본은 `Paperlogy`**, 못 불러오면 `Pretendard` → `Noto Sans KR` 순으로 물러납니다.
CSS로는 `font-family: Paperlogy, Pretendard, 'Noto Sans KR', sans-serif` 한 줄입니다.

- **제목과 본문에 다른 글꼴을 쓰지 마세요.** 두 벌이 되는 순간 어디에 무엇을 쓸지가
  화면마다 갈립니다. 처음엔 「제목만 다르게」로 시작해서, 나중엔 세 벌 네 벌이 됩니다.
- **위계는 글꼴이 아니라 크기와 굵기로 만듭니다.** 굵기는 400·600·700 셋만 쓰세요.
- 크기는 본문 15 · 카드 제목 18 · 섹션 제목 22 · 페이지 제목 32이 기준입니다.
- **숫자가 많은 표에는 고정폭 숫자**(`font-variant-numeric: tabular-nums`)를 켜세요.
  안 켜면 값이 바뀔 때 자릿수가 흔들려 칸이 들썩입니다.

#### 움직임 — 바뀌었다는 걸 눈이 알아채게

값이 바뀌었는데 **딱 하고 갈아 끼우면 사람은 그걸 못 봅니다.** 숫자가 12에서 3으로
바뀌어도, 눈이 그 자리를 안 보고 있었으면 바뀐 줄 모릅니다.
움직임은 꾸미개가 아니라 **「방금 여기가 바뀌었어요」라고 알려 주는 장치**입니다.

| 언제 | 얼마나 | 무엇이 |
| --- | --- | --- |
| 마우스를 올렸을 때 | **0.12초** | 색·테두리 |
| 눌러서 값이 바뀔 때 | **0.2초** | 사라짐·나타남·자리 옮김 |
| 접히고 펴질 때 | **0.25초** | 높이 |
| 화면 위에 뜰 때 (모달·시트) | **0.25초** | 옅어짐 + 살짝 올라옴 |
| 안내 문구(토스트) | **0.2초 등장 · 3초 머무름** | 옅어짐 |

- **가속은 하나로 씁니다.** `ease-out`(빨리 시작해 부드럽게 멈춤)이 기본입니다.
  자리마다 다른 가속을 쓰면 같은 사이트에서 두 사람이 만든 것처럼 보입니다.
- **0.3초를 넘기지 마세요.** 그보다 길면 기다리는 느낌이 듭니다. 목록이 다시 그려질 때는
  **0.15초**면 충분합니다.
- **자리를 밀어내며 움직이지 마세요.** 아래 내용이 출렁이면 읽던 곳을 잃습니다.
  옅어지거나 제자리에서 바뀌게 하세요.
- **한 번에 한 곳만 움직입니다.** 눌렀는데 화면 여기저기가 같이 움직이면 어디가 바뀐 건지 모릅니다.
- **끝없이 도는 움직임은 두지 않습니다.** 배너 자동 넘김·깜빡임은 읽는 걸 방해합니다.
  기다리는 동안 도는 것(로딩)만 예외입니다.
- **움직임을 꺼 둔 분을 존중하세요.** `prefers-reduced-motion` 이 켜져 있으면 전부 0초로 둡니다.
  어지럼증 때문에 꺼 두신 분들이 있습니다. 값은 바뀌되 움직이지만 않으면 됩니다.

#### 떠 있는 부품과 줄 안에 서는 부품은 다른 것입니다

사진 위에 **떠 있는** 동그란 찜 아이콘과, 버튼 줄 안에 **나란히 서는** 찜 버튼은
생김새가 비슷해 보여도 다른 부품입니다. 떠 있는 쪽은 자리를 빠져나가도록(`position:absolute`)
만들어져 있어서, 그 이름을 줄 안의 버튼에 붙이면 **줄을 빠져나가 엉뚱한 데 가서 앉습니다.**
실제로 찜 버튼이 가격 위에 겹쳐 앉은 적이 있습니다.

- **이름이 자리를 뜻하면 그 자리에서만 쓰세요.** 「사진 위 찜」과 「줄 안 아이콘 버튼」은
  이름을 나눠 두고, 섞어 쓰지 않습니다.
- **부품 이름과 실제 쓰임이 어긋나면 이름을 고치세요.** 「타임라인」이라 지어 놓고
  여섯 곳 모두 한 줄짜리 목록으로 쓰고 있던 적이 있습니다. 세로 점만 남아 글자 사이에 끼었습니다.
- **아이콘만으로 뜻이 통하면 글자를 빼세요.** 하트·공유·닫기가 그렇습니다.
  다만 **`aria-label`로 이름은 남깁니다** — 눈으로 못 보는 분에게는 그게 유일한 이름입니다.
- 반대로 **아이콘만으로 못 알아보는 것에는 글자를 붙이세요.** 「빼기」·「내보내기」처럼
  모양이 여러 뜻으로 읽히는 것이 그렇습니다.

#### 쓰는 화면이 있으면 보여주는 자리도 있어야 합니다

글을 쓰게 해 놓고 그 글이 나올 자리를 안 만드는 일이 잦습니다.
실제로 강사가 **1,240자짜리 강의 소개를 쓰는 화면**은 있는데, 상세 화면에는 그 글이
나올 자리가 없었습니다. 게다가 검수 화면은 "강의 소개 1,240자 — 확인됨"이라고까지 말하고 있었습니다.

- **입력 칸을 만들 때마다 "이건 어디에 보이나"를 한 번 물으세요.** 답이 없으면 둘 중 하나입니다 —
  보여줄 자리를 만들거나, 그 입력 칸을 빼거나.
- **에디터(굵게·목록·이미지 버튼이 붙은 칸)로 쓴 글**은 보여주는 쪽에도 그 모양이 살아야 합니다.
  제목·목록·굵게를 쓸 수 있게 해 놓고 보여줄 때 한 덩어리 문단으로 뭉개면, 쓴 사람이 헛수고합니다.
- **한 줄 요약과 본문 소개는 다른 것입니다.** 목록 카드에 들어가는 한 줄과, 상세에서 읽는 본문은
  자리도 길이도 다릅니다. 하나로 합치지 마세요.

#### 권장 크기는 실제 자리와 같아야 합니다

"권장 1280×720 (16:9)"이라고 적어 두고 옆의 자리는 4:3인 화면이 있었습니다.
**사는 분은 글자를 믿고 그 크기로 사진을 준비**하는데, 올려 보면 잘립니다.

- 안내 문구의 숫자와 **바로 옆 자리의 비율이 같은지** 확인하세요.
- 검수·확인 화면에 크기가 또 적힌다면 **거기도 같은 숫자**여야 합니다. 세 곳이 따로 놀기 쉽습니다.
- **영상만 16:9입니다.** 썸네일·카드 사진은 위 표(2·3개↔4:3, 4개↔1:1)를 따릅니다.

#### 차트 — 이미지가 아니라 데이터로 그립니다

추이·막대·도넛 같은 차트를 **이미지 자리로 두지 마세요.** `이미지 영역 (라인 차트 · 권장 1200×420)`
이라고 적어 두면 사는 분은 차트를 **그림 파일로 만들어 올려야 하는 줄** 압니다.
차트는 숫자가 바뀌면 따라 바뀌어야 하는 것이라, 사진 자리와 성격이 다릅니다.

- **가벼운 것은 라이브러리 없이 SVG로 그리세요.** 선 하나·막대 열두 개짜리는 30줄이면 됩니다.
  바깥에서 아무것도 안 불러오니 **인터넷 없이 폴더에서 열어도** 그대로 보입니다.

**대시보드를 만든다면 최소한 이 셋은 갖추세요.**

| 무엇 | 어디에 | 왜 |
| --- | --- | --- |
| **큰 추이 차트** | 화면에 한둘 | 기간을 놓고 흐름을 본다 |
| **스파크라인** (작은 추이선) | 지표 카드 안 | 숫자 옆에서 방향만 알려 준다 |
| **증감 표시** (▴ 812) | 지표 카드 안 | 좋아졌는지 나빠졌는지 |

숫자 하나만 두면 **좋은지 나쁜지를 못 말합니다.** "10,374명"만 있으면 늘고 있는지
줄고 있는지 알 수 없어서, 사는 분은 그 카드를 보고 아무 판단도 못 합니다.
지표 카드는 **숫자 + 증감 + 추이선**이 한 벌입니다.

- **본격적인 차트가 필요하면 무료(MIT) 라이브러리를 쓰세요.** 값을 치르지 않아도 되고 상업적으로 써도 됩니다.

| 라이브러리 | 크기 | 언제 |
| --- | --- | --- |
| **Chart.js** | ~200KB | 무난한 첫 선택. 자료가 많아 AI도 정확하게 짭니다 |
| **ApexCharts** | ~500KB | 기본 모양이 예쁘고 종류가 많을 때 |
| **uPlot** | ~40KB | 점이 수천 개라 가벼워야 할 때 |
| **Frappe Charts** | ~30KB | 작고 산뜻한 SVG면 충분할 때 |

- **색은 반드시 프리셋 색을 쓰세요.** 라이브러리 기본 팔레트(빨강·노랑·초록)를 그대로 두면
  그 자리만 다른 사이트처럼 보입니다.
- **눈금선은 옅게, 선은 진하게.** 눈금이 데이터보다 진하면 숫자가 안 읽힙니다.
- 라이브러리를 쓰면 **인터넷이 필요해집니다.** 오프라인으로 열어 볼 화면이라면 파일을 함께 받아 두거나 SVG로 그리세요.

#### 가격 표기 — 낼 돈부터

**판매가 · 정가 · 할인율** 순서로 한 줄에 세웁니다.

| 자리 | 모양 | 크기 |
| --- | --- | --- |
| **판매가** (낼 돈) | 진한 기본 글자색, 굵게 | 18 (상세는 28) |
| **정가** | 회색 + 취소선 | 13 |
| **할인율** | **강조색 글자**, 굵게 | 14 |

- **할인율을 앞에 두지 마세요.** 눈이 먼저 "20%"에 걸려 정작 얼마인지를 뒤늦게 찾습니다.
  사는 분이 알고 싶은 건 **낼 돈**입니다.
- **할인율은 배지가 아니라 색 글자입니다.** 배지로 두면 정가·판매가와 무게가 비슷해져 셋이 서로 다툽니다.
  배지 자리는 `입문`·`NEW` 같은 **꼬리표 몫**입니다.
- **셋의 크기를 벌려 두세요.** 비슷하면 무엇을 먼저 읽어야 할지 눈이 헤맵니다.
- **강조색 원색을 글자로 쓰지 마세요.** 주황·코랄 원색은 흰 배경에서 3:1을 못 넘깁니다.
  글자용으로 한 단계 어두운 색을 따로 두세요.
- 가격 묶음은 **한 곳에서 만들어 쓰세요.** 자리마다 손으로 짜면 순서가 갈립니다.

#### 버튼이냐 링크냐

**다른 화면으로 데려가면 링크, 이 화면 안에서 끝나면 버튼입니다.**

| 하는 일 | 무엇으로 | 눌렀을 때 |
| --- | --- | --- |
| 다른 화면으로 간다 | 링크 `<a href>` | 그 화면이 열린다 |
| 이 화면 안에서 끝난다 | 버튼 `<button>` | 안내 문구가 뜬다 |

- 필터 적용·정렬·검색·다음 차시·시간 지점 이동은 **화면이 바뀌지 않는 조작**입니다.
  링크로 만들면 **자기 자신으로 가는 링크**가 되어, 눌러도 같은 화면이 다시 열립니다.
  사는 분 눈에는 *"눌렀는데 아무 일도 안 나네"* 로 보입니다.
- **나란히 선 버튼끼리는 종류를 섞지 마세요.** 하나는 화면이 넘어가고 하나는 안 넘어가면
  규칙이 없어 보입니다.

**그리고 화면 안에서 끝나는 조작은 진짜로 값이 바뀌어야 합니다.**
눌러도 숫자가 그대로면, 사는 분 눈에는 **눌리지 않는 포스터**입니다.

| 눌렀을 때 | 바뀌어야 하는 것 |
| --- | --- |
| 기간 탭 (7일·30일·90일) | 차트와 그 옆 요약 숫자 |
| 쿠폰·옵션 고르기 | 결제 금액 줄과 합계 |
| 수량 늘리기 | 금액 |
| 정렬·필터 | 목록 순서나 개수 |

- **미리 만들어 두고 바꿔 끼세요.** 차트 셋을 미리 그려 두고 탭에 따라 보여 주는 편이,
  누를 때마다 자바스크립트로 다시 그리는 것보다 낫습니다 — 그리는 규칙이 두 곳으로 갈라지지 않습니다.
- **계산은 한 곳에서만 하세요.** 합계를 화면마다 손으로 적어 두면 쿠폰을 바꿨을 때 한 군데만 바뀝니다.
- **탭에 갈 곳도 바꿀 것도 없으면 탭이 아닙니다.** 그냥 제목이거나, 아직 안 만든 자리입니다.

**같은 길을 두 번 두지 마세요 — 위 카테고리 줄과 왼쪽 LNB.**
둘이 같은 곳을 가리키면 위쪽이 두 줄로 두꺼워지고, 어느 쪽을 눌러야 할지 잠깐 멈칫합니다.

| 상태 | 왼쪽 LNB | 위 카테고리 줄 |
| --- | --- | --- |
| 맨 위 (LNB 보임) | 보인다 | **접힌다** |
| 스크롤해서 LNB가 위로 사라짐 | 안 보인다 | **내려온다** |
| LNB가 아예 없는 화면 | — | **늘 보인다** |

- **LNB가 있는 화면은 접힌 채로 시작하세요.** 펼쳐 두었다가 나중에 접으면 첫 화면에서 한 번 번쩍입니다.
- **LNB가 없는 화면에서는 절대 접지 마세요.** 접으면 갈 길이 통째로 사라집니다.
- **기준은 "LNB가 붙박이 머리말 아래로 사라지는 순간"입니다.** 머리말 높이만큼 위를 깎아서 재세요.
- 같은 규칙을 **LNB를 쓰는 모든 레이아웃에 똑같이** 거세요. 화면마다 다르면 위쪽 높이가 제멋대로 바뀌어 보입니다.

- **견본으로 가는 버튼을 화면 안에 두지 마세요.** `결과가 없을 때` `대화가 없을 때는 이렇게 보여요`
  처럼 예외 화면을 보여주려고 넣은 버튼·단락은 진짜 사이트에 없는 것입니다.
  검색창 옆에 그런 버튼이 서 있으면 사는 분은 그게 기능인 줄 압니다.
  빈 화면·오류 화면은 **화면 목록에 따로 한 장**으로 두세요 — 거기서 눌러 보면 됩니다.

#### 화면 목록으로 가는 길은 한 곳에만

만든 화면을 훑어보려면 목록이 필요합니다. 그런데 그 목록을 **화면 안에 늘어놓으면**
사는 분 눈에는 사이트의 일부로 보입니다.

```
이렇게 두지 마세요 — 화면 위쪽에 줄줄이
  [CO-01 · 강의 목록] [CO-02 · 검색 결과 없음] [CO-03 · 강의 상세] [CO-04 · 결제] …
```

**진짜 사이트에는 이런 줄이 없습니다.** `CO-02 · 검색 결과 없음` 은 만드는 사람의 말이지
쓰는 사람의 말이 아닙니다. 화면 ID 가 손님 눈에 보이는 것부터가 어긋납니다.

- **한 귀퉁이에 작게 하나만 둡니다.** 오른쪽 아래에 떠 있는 `화면 목록` 단추 하나면 됩니다.
  누르면 `index.html` 로 갑니다.
- **그 단추는 화면 내용 위에 떠 있게** 두고, 본문 흐름에는 끼워 넣지 않습니다.
  본문에 들어가면 그것도 사이트의 일부가 됩니다.
- **화면 ID 를 손님이 보는 자리에 쓰지 않습니다.** 제목·탭·빵부스러기 어디에도요.
  ID 는 우리가 화면을 세는 이름이지 서비스의 말이 아닙니다.
- 화면끼리 오가는 길은 **그 서비스의 진짜 메뉴와 버튼**으로 만듭니다.
  「강의 목록 → 강의 상세」는 강의 카드를 눌러서 가지, 탭으로 가지 않습니다.

### 이미지 · 썸네일
- 아직 사진이 없으므로 이미지 자리는 **테마 색이 아니라 옅은 파스텔**로 채우세요. 테마 색으로 칠하면 화면이 그 색 덩어리로 뒤덮여 디자인이 안 보입니다.
- 배경 `#EEF2F7` · `#F2EFF7` · `#EDF4F1` · `#F7F1EC` · `#F2F4F7` 중 하나를 카드마다 돌려 쓰고, 테두리 `#E3E8F0` 1px, 글자색 `#8B94A6` 13px.
- 자리 안에 `이미지 영역 (무엇 · 권장 가로×세로)` 형식으로 적으세요. 예: `이미지 영역 (매장 대표 · 권장 1200×900)` · `이미지 영역 (프로필 · 권장 400×400)` · `이미지 영역 (배너 · 권장 1600×600)`
- 이미지 자리는 테마 색으로 칠하지 않는다. 옅은 파스텔 한 톤 + 1px 테두리로 둔다.
- 자리 안에 무엇이 들어갈 이미지인지와 권장 크기를 글자로 적는다.
- 적어 둔 크기의 **비율을 실제로 지킨다** — `400×400`이라 적었으면 정사각으로 보여야 한다(aspect-ratio).
- 특히 프로필·썸네일처럼 크기가 고정된 자리는, 가로로 나란히 놓아도 늘어나지 않게 한다. 목록·행 안에 넣을 때 `flex`가 잡아당겨 타원이 되는 일이 잦으니, 만든 뒤 실제로 열어 모양을 확인한다.
- 썸네일처럼 작아서 글자가 안 들어가면 크기만 적거나 아이콘 하나만 둔다.
- 실제 사진을 넣기 전까지 이 규칙을 유지한다 — 그래야 어디에 무엇을 넣어야 하는지 보인다.
### 파일을 어디에 두나

```
완성화면/
  index.html              전체 화면 목록 (여기서 화면으로 들어간다)
  pages/
    HO-01.html            화면 하나에 파일 하나 · 이름은 화면 ID 그대로
    CO-03.html
  assets/
    css/style.css         스타일 한 벌
    js/app.js             동작 한 벌
```

- **`pages/` 안에서는 `../assets/css/style.css` 로 부릅니다.** `assets/...` 라고 쓰면 `pages/assets/...` 를 찾다가 못 찾아서 **스타일이 통째로 안 붙습니다** — 글꼴도 여백도 격자도 전부 날아갑니다.
- **`index.html` 은 `완성화면/` 바로 아래**에 둡니다. 거기서는 `assets/...` 그대로 부르면 됩니다.
- **스타일과 동작은 각각 한 벌**입니다. 화면마다 따로 만들면 44장이 서로 달라집니다.
- 화면끼리 오갈 때는 `CO-03.html` 처럼 **같은 폴더 안 이름**으로 겁니다.
- 다 만든 뒤 **화면 하나를 브라우저로 직접 열어 보세요.** 글꼴이 기본 고딕으로 보이면 경로가 틀린 것입니다.

### 추천 스택
- 정적 다중 페이지(HTML/CSS) 또는 컴포넌트 기반(React·Next.js·Vue 등) 중 프로젝트 규모에 맞게 선택하세요. 화면이 많고 상태가 복잡하면 컴포넌트 기반을 권장합니다.
