# 인테리어 시공 견적·시공관리 — AI 빌드 스펙팩

> 이 문서 하나로 사이트를 만들 수 있게 정리한 스펙이에요. 화면별 **생성 프롬프트**를 그대로 사용하세요.

## 1. 프로젝트 개요
- **컨셉**: 아파트 리모델링·상가 인테리어를 맡아 하는 시공 업체의 사이트. 집주인은 시공 사례를 둘러보고 평수·공간·마감 등급을 넣어 예상 견적을 받은 뒤, 방문 실측을 예약한다. 실측 결과로 확정된 견적에 계약금을 걸면 공사가 시작되고, 철거부터 준공까지 공정표와 현장 사진 일지로 진행을 본다. 도중에 생기는 추가공사는 변경 견적을 받아 승인해야 진행되고, 돈은 계약금·중도금·잔금으로 나누어 낸다. 준공 검수를 마친 뒤에도 하자보수를 접수할 수 있다. 업체 쪽은 현장별 공정·자재 원가·기성 청구를 한 화면에서 굴린다.
- **디자인 컨셉**: 공간 사진이 주인공이라 색을 절제한다. 바탕은 #F0EFEB, 카드는 흰색에 1px 구분선(#E5E5E5)만 두고 그림자를 쓰지 않는다. 주요 버튼은 검정(#111111), 강조는 오렌지 하나(#D97757 — 배경·배지·큰 글자용, 본문 글자에는 #A84B28). 크기보다 굵기와 여백으로 위계를 만든다. 비포·애프터 사진과 공정표가 가장 크고, 금액은 표로 또렷하게
- **디바이스**: 반응형
- **전체 일정**: 2026-08-10 ~ 2026-10-30

## 2. 공통 요소 (모든 화면 공통)
> 아래 요소는 **한 번만 만들어 모든 화면에서 재사용**하세요. 각 화면 스펙(4장)은 이 공통 요소 위에 올라가는 콘텐츠만 설명합니다.
- **영역이 둘입니다. 헤더를 각각 따로 만드세요.**
  - **손님 화면 GNB**: `홈` · `시공사례` · `견적` · `실측 예약` · `계약·결제` · `공사 진행` · `하자보수`
  - **운영자 화면 GNB**: `현장 관리` — 좌측 사이드바 + 상단 바 형태가 맞습니다
  - **로그인·가입 화면**: `계정` — 헤더 없이 단독 화면으로 만드세요
  - 두 영역을 오가는 길은 **하나만** 둡니다 — 손님 화면 우측 상단에 운영자 영역으로 넘어가는 버튼 하나(예: `현장 관리 화면으로`), 운영자 화면 우측 상단에 `고객 화면 보기`. 한 계정이 두 권한을 가지며(크몽·숨고와 같은 방식), 아직 권한이 없으면 전환 대신 **신청 화면으로 보냅니다.**
- **콘텐츠 영역**: 최대 1440px, 좌우 여백 24px — 헤더·본문·푸터·하단 고정 바가 모두 이 폭을 쓴다. 화면마다 따로 정하지 않는다. 화면마다 폭을 새로 정하면 위아래가 어긋납니다.
- **읽기 폭**: 글이 주인공인 자리(상세 본문·안내·약관)는 최대 760px로 좁혀 콘텐츠 영역 **안에서** 가운데 정렬 — 카드 격자에는 쓰지 않습니다. 아래 「읽기 폭」 참고.
- **헤더**: 로고 + 검색 + 상단 내비게이션(GNB)
- **푸터**: 저작권 · 기본 링크
- **재사용 컴포넌트**: 반복되는 UI(상품/항목 **카드**, 리스트 **행**, **폼 필드**, **버튼**)는 공용 컴포넌트로 만들어 화면마다 재사용하세요.

## 3. 메뉴 구조
- **[HO] 홈** (Home)
  - `HO-01` 홈
  - `HO-02` 시공 분야 홈
  - `HO-03` 비용 안내
- **[CS] 시공사례** (Cases)
  - `CS-01` 시공사례 목록
  - `CS-02` 시공사례 상세
  - `CS-03` 시공사례 - 결과 없음
  - `CS-04` 자재·마감 둘러보기
- **[ES] 견적** (Estimate)
  - `ES-01` 견적 조건 입력 - 단계별
  - `ES-02` 견적 결과
  - `ES-03` 견적 항목 상세
  - `ES-04` 견적 저장 완료
  - `ES-05` 견적 견주기
- **[VS] 실측 예약** (Visit)
  - `VS-01` 방문 실측 예약
  - `VS-02` 예약 내용 확인
  - `VS-03` 예약 완료
  - `VS-04` 예약 변경·취소
- **[CT] 계약·결제** (Contract)
  - `CT-01` 계약서 확인·서명
  - `CT-02` 계약금 결제
  - `CT-03` 결제 완료 - 계약 성사
  - `CT-04` 결제 실패
- **[PR] 공사 진행** (Progress)
  - `PR-01` 공정표
  - `PR-02` 오늘 현장
  - `PR-03` 현장 사진 일지
  - `PR-04` 추가공사 변경 견적 승인
  - `PR-05` 준공 검수 체크리스트
  - `PR-06` 공사 진행 - 진행 중 없음
- **[AS] 하자보수** (AfterService)
  - `AS-01` 하자보수 접수
  - `AS-02` 하자보수 접수 내역
  - `AS-03` 하자보수 처리 현황
- **[OW] 현장 관리** (Sites)
  - `OW-01` 현장 대시보드
  - `OW-02` 견적 요청함
  - `OW-03` 현장 상세 - 공정 편집
  - `OW-04` 자재·원가
  - `OW-05` 기성 청구·수금
  - `OW-06` 일정 캘린더
  - `OW-07` 업체 정보·직원 관리
- **[AU] 계정** (Account)
  - `AU-01` 로그인
  - `AU-02` 회원가입
  - `AU-03` 비밀번호 찾기
  - `AU-04` 내 정보

## 4. 화면별 스펙
### HO-01 · 홈
- **소속 메뉴**: 홈
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-08-10 ~ 2026-08-13
- **기능정의**: 대표 시공 사진을 크게 쓴 히어로와 「우리 집은 얼마나 나올까요?」 견적 시작 버튼 · 평수 칩(10평대~40평대 이상)을 누르면 바뀌는 예상 비용 띠 · 시공 분야 6종 아이콘(아파트 전체·주방·욕실·상업공간·부분 시공·베란다) · 최근 시공 사례 6개 카드(비포·애프터 손잡이) · 공사가 어떻게 굴러가는지 5단계 안내(상담→실측→계약→시공→준공) · 우리 업체 숫자 3개(누적 현장 수·평균 공사 기간·하자보수 대응일) · 자재·마감 브랜드 로고 띠 · 실제 고객 후기 4건 · 자주 묻는 질문 아코디언 · 하단 상담 신청 CTA
- **버튼 → 이동화면**:
  - `예상 견적 내기` → `ES-01` 견적 조건 입력 - 단계별
  - `시공사례 보기` → `CS-01` 시공사례 목록
  - `분야별 홈` → `HO-02` 시공 분야 홈
  - `비용 안내` → `HO-03` 비용 안내
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 아코디언 — 누르면 그 자리에서 펴지고 접힙니다. 다른 화면으로 가지 않습니다
- **생성 프롬프트**:

  ```
  인테리어 시공 업체 사이트의 홈 화면을 만들어줘. 스크롤 순서대로 아래 섹션을 위에서 아래로 쌓아줘. 1) 상단 GNB(로고, 시공사례, 견적내기, 시공과정, 회사소개, 상담예약, 로그인) 2) 히어로 — 준공 사진 한 장을 크게 깔고 그 위에 "우리 집은 얼마나 나올까요?"와 [1분 예상 견적] 버튼. 사진 위 글자는 어둠막을 깔아 읽히게 3) 평수 칩 띠 — 10평대 / 20평대 / 30평대 / 40평대 이상. 고르면 바로 아래 "30평대 아파트 전체 시공 평균 3,200만원 ~ 4,600만원" 처럼 숫자가 바뀐다. ⚠ 여기서 고른 평수를 [1분 예상 견적] 이 «들고 가야» 한다 — 견적 화면 1단계(평수)가 그 값으로 채워진 채 열린다. 처음부터 골라져 있는 칸(30평대)도 «고른 것»이다. 손님이 칩을 건드리지 않고 버튼만 눌러도 그 평수가 넘어가야 한다. ⚠ 값만 넘기고 «단계를 건너뛰지는» 마라. 1단계부터 열되 값이 채워져 있는 것이다. 4) 시공 분야 6종 아이콘 그리드 — 아파트 전체·주방·욕실·상업공간·부분 시공·베란다 5) 최근 시공 사례 6개 카드 — 사진, 평수와 지역, 공사 기간, 총 금액대. 카드 사진은 가운데 세로 손잡이를 끌면 공사 전/후가 갈라져 보이는 비교 사진 6) 공사가 어떻게 굴러가나 5단계 — 상담 → 방문 실측 → 계약 → 시공 → 준공. 번호와 한 줄 설명, 각 단계에 걸리는 날수 7) 우리 업체 숫자 3개 — 누적 현장 수, 평균 공사 기간, 하자 접수 후 방문까지 걸린 날 8) 쓰는 자재·마감 브랜드 로고 띠 9) 실제 고객 후기 4건 — 사진과 평수, 한 줄 인용 10) 자주 묻는 질문 아코디언 5개 11) 하단 상담 신청 CTA와 푸터 공간 사진이 주인공이다. 색은 절제하고 사진을 크게, 금액은 표처럼 또렷하게 써줘.
  ```

### HO-02 · 시공 분야 홈
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈` — 누르면 `HO-01`로 간다
- **일정**: 2026-08-14 ~ 2026-08-18
- **기능정의**: 분야 대표 사진과 한 줄 소개(예: 욕실 시공) · 이 분야만의 공사 범위 탭(전체 교체·부분 보수) · 마감 등급 3단(기본·고급·프리미엄)별 평당 단가 표 · 이 분야 시공 사례 6개 · 이 분야에서 자주 놓치는 것 안내 카드(방수·배관 구배 등) · 공정 순서와 걸리는 날수 표 · 이 분야 자주 묻는 질문 4개 · 이 분야로 견적 시작 CTA
- **버튼 → 이동화면**:
  - `시공사례` → `CS-01` 시공사례 목록
  - `예상 견적 내기` → `ES-01` 견적 조건 입력 - 단계별
  - `비용 안내` → `HO-03` 비용 안내
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
- **생성 프롬프트**:

  ```
  특정 시공 분야(예: 욕실 시공) 전용 홈 화면을 만들어줘. 1) 상단에 분야 대표 사진과 이름, 한 줄 소개 2) 공사 범위 탭 — 전체 교체 / 부분 보수. 탭과 그 아래 몸통은 반드시 같은 상자 안에 두고, 탭을 누르면 아래 내용이 실제로 바뀌게 3) 마감 등급 3단 비교 표 — 기본 / 고급 / 프리미엄. 행은 평당 단가, 쓰는 자재, 걸리는 날수, 보증 기간. 등급을 고르면 표에 표시가 남는다 4) 이 분야 시공 사례 6개 카드 그리드 5) "이 공사에서 자주 놓치는 것" 카드 3장 — 방수 두 번 치기, 배관 구배, 환기 6) 공정 순서 표 — 철거 1일 → 방수 2일 → 타일 2일 → 도기·수전 1일. 합계 날수 7) 이 분야 자주 묻는 질문 4개 아코디언 8) 하단 [이 분야로 견적 내기] 버튼 사진과 표가 번갈아 나오게 짜서 지루하지 않게 해줘.
  ```

### HO-03 · 비용 안내
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈` — 누르면 `HO-01`로 간다
- **일정**: 2026-08-19 ~ 2026-08-24
- **기능정의**: 「값이 왜 이렇게 나오는지」를 먼저 밝히는 안내 화면 · 평수와 공사 범위를 고르는 바 · 총액 범위 막대(최소~최대)와 그 안에서 무엇이 값을 움직이는지 · 공정별 비용 비중 표(철거·설비·목공·타일·도배·마루·조명) · 마감 등급별 차이 비교 표 · 추가로 붙을 수 있는 비용 목록(폐기물·엘리베이터 사용료·야간 작업) · 계약금·중도금·잔금 분할 비율 안내 · 다른 업체와 견적을 견줄 때 볼 것 체크리스트 · 견적서 예시 내려받기
- **버튼 → 이동화면**:
  - `예상 견적 내기` → `ES-01` 견적 조건 입력 - 단계별
  - `실측 예약` → `VS-01` 방문 실측 예약
  - `자주 묻는 질문` → `HO-01` 홈
- **생성 프롬프트**:

  ```
  인테리어 비용이 어떻게 정해지는지 알려 주는 안내 화면을 만들어줘. 1) 맨 위에 "값을 숨기지 않습니다" 한 줄과 평수·공사 범위를 고르는 바 2) 총액 범위 가로 막대 — 최소 2,800만원 ~ 최대 4,900만원 처럼 폭으로 보여 주고, "무엇이 이 폭을 만드나"를 옆에 세 줄로 3) 공정별 비용 비중 표 — 철거 8% / 설비 12% / 목공 22% / 타일 14% / 도배 9% / 마루 11% / 조명·전기 10% / 기타 14%. 비중은 가로 막대로도 같이 4) 마감 등급별 차이 비교 표 — 기본 / 고급 / 프리미엄. 항목을 누르면 세부 내역이 펼쳐진다 5) "이런 비용이 더 붙을 수 있어요" 목록 — 폐기물 처리, 엘리베이터 사용료, 야간·주말 작업, 누수 발견 시 배관 교체 6) 돈 내는 차례 — 계약금 10% / 착공 30% / 중도 40% / 준공 20%. 가로 단계 막대 7) "다른 곳 견적과 견줄 때 이것만은 보세요" 체크리스트 5개 8) [견적서 예시 보기] 버튼 숫자가 많은 화면이니 표와 막대를 섞고, 표 한 줄 높이를 일정하게 유지해줘.
  ```

### CS-01 · 시공사례 목록
- **소속 메뉴**: 시공사례
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-08-25 ~ 2026-08-27
- **기능정의**: 결과 수와 정렬(최신순·금액 낮은순·평수순) · 필터 패널(평수 구간·시공 분야·예산 구간·스타일·지역) · 사례 카드 격자(대표 사진·평수와 지역·공사 기간·총 금액대·스타일 꼬리표) · 카드 위 비포·애프터 비교 손잡이 · 고른 조건 칩 줄과 전체 지우기 · 더 보기 페이지네이션 · 조건에 맞는 것이 없을 때로 가는 길
- **버튼 → 이동화면**:
  - `사례 상세` → `CS-02` 시공사례 상세
  - `결과 없음` → `CS-03` 시공사례 - 결과 없음
  - `자재 둘러보기` → `CS-04` 자재·마감 둘러보기
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 필터 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
  - 정렬 — 고른 기준으로 **순서를 실제로 바꿔** 다시 그립니다
  - 더 보기 — 누르면 그 자리에서 펴지고 접힙니다. 다른 화면으로 가지 않습니다
  - 페이지네이션 — 누른 쪽의 목록으로 **내용이 바뀌고**, 지금 쪽이 표시됩니다
- **생성 프롬프트**:

  ```
  시공 사례 목록 화면을 만들어줘. 1) 상단에 제목과 "지금까지 148개 현장" 같은 총 수 2) 좌측 필터 패널 — 평수 구간 체크박스, 시공 분야, 예산 구간 슬라이더, 스타일(모던·내추럴·클래식·인더스트리얼), 지역 3) 필터 위에 고른 조건 칩 줄과 [전체 지우기] 4) 우측 상단에 결과 수("조건에 맞는 사례 24개")와 정렬 드롭다운 5) 사례 카드 3열 격자 — 대표 사진, "32평· 성동구", 공사 기간 "24일", 총 금액대 "3,800만원대", 스타일 꼬리표. 사진에는 가운데 세로 손잡이를 두어 끌면 공사 전 사진이 드러난다 6) 하단 [더 보기] 버튼과 남은 수 사진이 주인공이니 카드는 사진을 크게 쓰고 글자는 아래에 작게 붙여줘. 카드 비율은 4:3으로 통일해서 줄이 들쭉날쭉하지 않게.
  ```

### CS-02 · 시공사례 상세
- **소속 메뉴**: 시공사례
- **뒤로가기**: 좌측 상단에 `‹ 시공사례 목록` — 누르면 `CS-01`로 간다
- **일정**: 2026-08-28 ~ 2026-09-02
- **기능정의**: 현장 한 줄 소개(평수·지역·가족 구성·요구사항) · 공간 탭(거실·주방·욕실·침실·현관) · 공간별 비포·애프터 비교 사진과 사진 여러 장 · 이 현장에 쓴 자재 표(부위·제품명·색상·등급) · 실제 공정표와 걸린 날수 · 총 공사비와 공정별 비중 · 집주인이 남긴 후기 · 시공 중 생겼던 문제와 어떻게 풀었는지 · 비슷한 사례 3개 · 「이 사례처럼 견적 내기」 버튼(조건이 채워진 채로 견적으로 간다)
- **버튼 → 이동화면**:
  - `이 사례처럼 견적 내기` → `ES-01` 견적 조건 입력 - 단계별
  - `실측 예약` → `VS-01` 방문 실측 예약
  - `목록으로` → `CS-01` 시공사례 목록
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
- **생성 프롬프트**:

  ```
  시공 사례 한 건의 상세 화면을 만들어줘. 1) 맨 위에 대표 사진 크게 하나와 "32평 아파트 전체 시공· 성동구· 24일· 3,800만원" 2) 현장 소개 — 살던 사람 이야기와 요구사항 3줄 3) 공간 탭 — 거실 / 주방 / 욕실 / 침실 / 현관. ⚠ 탭과 그 아래 몸통은 반드시 같은 상자 안에 두어 탭을 누르면 몸통이 실제로 바뀌게 4) 각 공간마다 — 비포·애프터 비교 사진 하나(손잡이로 갈라짐)와 그 공간 사진 4장 격자 5) 이 현장에 쓴 자재 표 — 부위 / 제품명 / 색상 / 등급. 행을 누르면 단가가 펼쳐진다 6) 실제 공정표 — 철거 2일 → 설비 3일 → 목공 6일 → 타일 3일 → 도배 2일 → 마루 2일 → 조명·마무리 2일. 가로 막대 일정표로 7) 총 공사비와 공정별 비중 도넛 또는 가로 막대 8) 집주인 후기 인용과 별점 9) "시공 중에 이런 일이 있었어요" — 누수 발견 후 배관을 바꾼 이야기와 추가 비용 10) 비슷한 사례 3개 카드 11) 하단 [이 사례처럼 견적 내기] 버튼 — 누르면 평수·분야가 채워진 채 견적 화면으로 사진이 크고 표가 또렷한 화면이다. 사진 세로 길이가 화면을 다 덮지 않게 비율을 잡아줘.
  ```

### CS-03 · 시공사례 - 결과 없음
- **소속 메뉴**: 시공사례
- **뒤로가기**: 좌측 상단에 `‹ 시공사례 목록` — 누르면 `CS-01`로 간다
- **일정**: 2026-09-03 ~ 2026-09-07
- **기능정의**: 고른 조건을 그대로 보여 주는 칩 줄과 「이 조건에 맞는 사례가 아직 없어요」 안내 · 어느 조건이 결과를 0으로 만들었는지 짚어 주는 문장 · 조건을 하나씩 지우는 버튼 · 조건을 살짝 넓힌 추천 3개(예: 예산 구간 한 칸 넓히기) · 그래도 없으면 상담으로 가는 길 · 인기 사례 6개
- **버튼 → 이동화면**:
  - `조건 지우고 보기` → `CS-01` 시공사례 목록
  - `상담 예약` → `VS-01` 방문 실측 예약
  - `예상 견적 내기` → `ES-01` 견적 조건 입력 - 단계별
- **생성 프롬프트**:

  ```
  시공 사례 필터 결과가 하나도 없을 때의 화면을 만들어줘. 1) 가운데에 그림 하나와 "이 조건에 맞는 사례가 아직 없어요" 2) 바로 아래 고른 조건 칩 줄 — 각 칩에 X 를 두어 하나씩 지울 수 있게 3) "예산 구간을 좁게 잡으신 것 같아요" 처럼 어느 조건이 0을 만들었는지 짚어 주는 한 줄 4) 조건을 살짝 넓힌 추천 3개 — "예산 한 칸 넓히기(사례 8개)" 처럼 결과 수를 미리 보여 준다 5) [전체 조건 지우기] 버튼 6) "찾는 사례가 없어도 상담은 됩니다" 안내와 [상담 예약] 버튼 7) 하단에 인기 사례 6개 카드 막다른 길로 두지 않는 것이 이 화면의 일이다. 나가는 문을 세 개 이상 두어줘.
  ```

### CS-04 · 자재·마감 둘러보기
- **소속 메뉴**: 시공사례
- **뒤로가기**: 좌측 상단에 `‹ 시공사례 목록` — 누르면 `CS-01`로 간다
- **일정**: 2026-09-08 ~ 2026-09-10
- **기능정의**: 부위 탭(바닥·벽·주방·욕실·창호) · 자재 카드 격자(제품 사진·브랜드·등급·평당 추가금) · 색상 견본 줄과 누르면 바뀌는 미리보기 사진 · 등급 필터(기본 포함·추가금 있음) · 자재별 장단점과 관리법 짧은 글 · 내가 고른 자재 담아두기 패널과 예상 추가금 합계 · 「고른 자재로 견적 내기」 버튼
- **버튼 → 이동화면**:
  - `고른 자재로 견적 내기` → `ES-01` 견적 조건 입력 - 단계별
  - `시공사례` → `CS-01` 시공사례 목록
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 필터 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
- **생성 프롬프트**:

  ```
  시공에 쓰는 자재·마감을 골라 보는 화면을 만들어줘. 1) 상단에 미리보기 — 방 사진 한 장. 아래에서 자재를 고르면 이 사진의 바닥·벽 색이 바뀐다 2) 부위 탭 — 바닥 / 벽 / 주방 / 욕실 / 창호. 탭과 몸통은 같은 상자 안에 3) 부위마다 자재 카드 격자 — 제품 사진, 브랜드, 등급 배지(기본 포함 / +8만원/평), 색상 견본 동그라미 줄 4) 등급 필터 — 기본 포함만 보기 / 추가금 있는 것도 보기 5) 카드를 누르면 아래에 그 자재의 장점·단점·관리법이 3줄로 펼쳐진다 6) 오른쪽에 "내가 고른 자재" 패널 — 부위별로 고른 것이 쌓이고 맨 아래에 "기본 견적 대비 +142만원" 합계 7) 하단 [고른 자재로 견적 내기] 버튼 색을 고르는 화면이니 우리 강조색은 최소로 쓰고 자재 색이 돋보이게 해줘.
  ```

### ES-01 · 견적 조건 입력 - 단계별
- **소속 메뉴**: 견적
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-09-11 ~ 2026-09-16
- **기능정의**: 한 번에 한 가지만 묻는 단계별 입력(6단계) — 한 화면 안에서 여섯 단계가 앞뒤로 오간다 · 1단계 평수와 ㎡ 환산 · 2단계 공사 범위(전체·부분) · 3단계 손볼 공간 체크(거실·주방·욕실·침실·베란다·현관) · 4단계 마감 등급 3단 · 5단계 거주 여부와 희망 착공 시기 · 6단계 연락처 · 위쪽 진행 막대는 «끝낸 만큼»만 찬다 · 오른쪽에 실시간으로 오르내리는 예상 금액 미리보기 · 이전으로 돌아가도 답이 남는 것 · 「나중에 이어서 하기」 임시저장
- **버튼 → 이동화면**:
  - `견적 결과 보기` → `ES-02` 견적 결과
  - `자재 둘러보기` → `CS-04` 자재·마감 둘러보기
- **생성 프롬프트**:

  ```
  견적 조건을 단계별로 묻는 화면을 만들어줘. 한 화면에 한 질문만 크게 나오되, 여섯 단계가 «모두 그 화면 안에» 들어 있고 [이전] [다음] 으로 오간다. ⚠ 3단계 같은 «중간 한 장면만» 그려 놓고 끝내지 마라. 나머지 다섯 단계가 없으면 손님은 그 화면에서 앞으로도 뒤로도 못 간다. 1) 맨 위에 진행 막대와 "6단계 중 1번째· 대략 2분이면 끝나요" — 처음엔 «1번째»에서 시작한다. 단계를 넘길 때마다 이 숫자와 막대가 같이 움직인다. ⚠ 막대는 «끝낸 만큼»만 채운다. 4단계에 서 있으면 3칸(50%)이 찬 것이다 — 지금 서 있는 칸까지 칠하면 이미 끝낸 것처럼 보인다. 2) 그 아래 단계 이름 띠 — 평수 › 공사 범위 › 공간 › 마감 등급 › 거주 여부 › 연락처. 지나온 단계는 진하게, 지금 단계는 표시만, 아직 안 온 단계는 흐리게. 3) 여섯 단계는 이 차례다 — 1단계 평수: 숫자 입력칸 옆에 "32평 = 105.8㎡" 환산을 같이 보여 준다. ⚠ 홈 히어로에서 평수 구간을 고르고 들어온 손님은 이 칸이 «이미 채워진 채» 열려야 한다. 고른 것을 버리고 처음부터 다시 묻지 마라. 2단계 공사 범위: 전체 시공 / 부분 시공 둘 중 하나 3단계 손볼 공간: 거실·주방·욕실·침실·베란다·현관 체크박스 카드(여러 개 고름) 4단계 마감 등급: 기본 / 고급 / 프리미엄 — 각각 공사 날수와 보증 기간도 같이 5단계 거주 여부(거주 중·비어 있음·짐만 있음)와 희망 착공 시기 6단계 연락처: 이름·전화번호 4) 질문마다 아래에 짧은 도움말 한 줄 — "여러 개 고를 수 있어요" 5) 오른쪽(모바일에서는 아래)에 「지금까지 고른 조건」 — 물어본 차례대로 답이 쌓이고, 맨 아래 예상 금액이 고를 때마다 "2,400만원 ~ 3,100만원" 처럼 오르내린다 6) 아래에 [이전] [다음] 버튼. 이전으로 가도 적은 답이 그대로 남아 있어야 한다. 1단계에서는 [이전] 이 잠기고, 마지막 단계에서만 [견적 결과 보기] 가 뜬다. 7) [나중에 이어서 하기] — 누르면 임시저장됐다는 알림 부담을 줄이는 것이 이 화면의 목적이다. 한 번에 질문 하나만, 여백을 넉넉하게.
  ```

### ES-02 · 견적 결과
- **소속 메뉴**: 견적
- **뒤로가기**: 좌측 상단에 `‹ 견적 조건 입력 - 단계별` — 누르면 `ES-01`로 간다
- **일정**: 2026-09-17 ~ 2026-09-22
- **기능정의**: 총액 범위를 큰 글자로(최소~최대)와 「실측 후 확정됩니다」 못 박는 안내 · 넣은 조건 요약 칩 줄과 고치기 버튼 · 마감 등급 3단 전환 스위치(바꾸면 총액이 바로 바뀐다) · 공정별 항목 표(공정·수량·단가·금액·켜고 끄기) · 항목을 펼치면 나오는 자재비·인건비 갈래 · 포함되지 않은 것 목록 · 돈 내는 차례(계약금·중도금·잔금) 미리보기 · 예상 공사 기간 막대 · 「이 견적으로 실측 예약」과 「견적서 저장·내려받기」
- **버튼 → 이동화면**:
  - `항목 상세` → `ES-03` 견적 항목 상세
  - `실측 예약` → `VS-01` 방문 실측 예약
  - `견적 저장 완료` → `ES-04` 견적 저장 완료
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 수량 — 누를 때마다 **숫자와 합계가 같이** 바뀝니다
  - 펼치 — 누르면 그 자리에서 펴지고 접힙니다. 다른 화면으로 가지 않습니다
  - 끄기 — 누르면 **켜짐·꺼짐이 눈에 보이게** 바뀌고 그대로 남습니다
- **생성 프롬프트**:

  ```
  계산된 견적 결과 화면을 만들어줘. 이 팩에서 가장 중요한 화면 중 하나다. 1) 맨 위에 총액을 아주 크게 — "3,240만원 ~ 3,980만원". 바로 아래 작은 글씨로 "방문 실측 뒤에 확정됩니다. 지금 금액은 평균값 기준이에요" 2) 넣은 조건 요약 칩 줄 — "32평· 전체 시공· 고급 마감· 9월 착공" 과 [조건 고치기] 3) 마감 등급 전환 스위치 3단 — 기본 / 고급 / 프리미엄. 바꾸면 위 총액과 아래 표 금액이 실제로 다시 계산된다 4) 공정별 항목 표 — 공정 / 수량 / 단가 / 금액 / 포함 여부 토글. 행: 철거·폐기물, 설비·배관, 전기·조명, 목공, 타일, 도배, 마루, 도장, 청소. 토글을 끄면 그 행에 취소선이 생기고 총액이 줄어든다 5) 행을 누르면 아래에 자재비 / 인건비 / 철거비 갈래가 작은 표로 펼쳐진다 6) "이건 포함되어 있지 않아요" 목록 — 가구, 가전, 이사비, 입주청소 7) 돈 내는 차례 미리보기 — 계약금 324만 / 착공 972만 / 중도 1,296만 / 잔금 648만 8) 예상 공사 기간 가로 막대 — "22일 (주말 제외)" 9) 하단 고정 바에 [이 견적으로 실측 예약] [견적서 저장하기] 숫자가 주인공이다. 표 한 줄 높이를 일정하게, 금액은 오른쪽 정렬로 자릿수를 맞춰줘.
  ```

### ES-03 · 견적 항목 상세
- **소속 메뉴**: 견적
- **뒤로가기**: 좌측 상단에 `‹ 견적 조건 입력 - 단계별` — 누르면 `ES-01`로 간다
- **일정**: 2026-09-23 ~ 2026-09-25
- **기능정의**: 공정 탭(철거·설비·전기·목공·타일·도배·마루·도장) · 공정별 세부 항목 표(품명·규격·수량·단위·단가·금액) · 자재 등급을 그 줄에서 바로 바꾸는 드롭다운 · 수량을 직접 고치는 칸 · 공정 합계와 전체 합계가 아래에 붙어 따라 오르는 것 · 이 공정에 걸리는 날수와 앞뒤 공정 · 「왜 이 값인가」 설명 카드 · 표 내려받기
- **버튼 → 이동화면**:
  - `견적 결과로` → `ES-02` 견적 결과
  - `실측 예약` → `VS-01` 방문 실측 예약
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
  - 수량 — 누를 때마다 **숫자와 합계가 같이** 바뀝니다
- **생성 프롬프트**:

  ```
  견적 항목을 공정별로 파고드는 화면을 만들어줘. 1) 상단에 총액과 "지금 보고 계신 것: 목공 공정" 표시 2) 공정 탭 가로 줄 — 철거 / 설비 / 전기 / 목공 / 타일 / 도배 / 마루 / 도장. ⚠ 탭과 아래 표는 반드시 같은 상자 안에 두어야 탭이 실제로 먹는다 3) 그 공정의 세부 표 — 품명 / 규격 / 수량 / 단위 / 단가 / 금액. 수량 칸은 직접 고칠 수 있고 고치면 금액이 곧바로 다시 곱해진다 4) 자재가 걸린 줄에는 등급 드롭다운(기본/고급/프리미엄)을 두어 바꾸면 단가가 바뀐다 5) 표 맨 아래에 공정 합계, 그 아래 회색 띠로 전체 합계 6) 오른쪽에 "이 공정은요" 카드 — 걸리는 날수, 앞에 끝나야 하는 공정, 뒤에 이어지는 공정 7) "왜 이 값인가" 설명 카드 — 목공 인건비가 지역·층수에 따라 달라지는 이유 8) [표 내려받기] 버튼 숫자 표가 주인공인 화면이다. 금액은 오른쪽 정렬, 고칠 수 있는 칸은 테두리로 구분해줘.
  ```

### ES-04 · 견적 저장 완료
- **소속 메뉴**: 견적
- **뒤로가기**: 좌측 상단에 `‹ 견적 조건 입력 - 단계별` — 누르면 `ES-01`로 간다
- **일정**: 2026-09-28 ~ 2026-09-30
- **기능정의**: 저장됐다는 체크 표시와 견적 번호 · 저장한 견적 요약 카드(조건·총액·유효기간) · 견적서 이름 고치기 · PDF 내려받기·문자로 받기·링크 복사 · 「이 견적은 30일 동안 유효해요」 안내와 값이 바뀔 수 있는 까닭 · 다음에 할 일 3개 카드(실측 예약·자재 고르기·사례 더 보기) · 내 견적 목록으로 가는 길
- **버튼 → 이동화면**:
  - `실측 예약` → `VS-01` 방문 실측 예약
  - `다른 견적과 견주기` → `ES-05` 견적 견주기
  - `자재 둘러보기` → `CS-04` 자재·마감 둘러보기
  - `홈으로` → `HO-01` 홈
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 더 보기 — 누르면 그 자리에서 펴지고 접힙니다. 다른 화면으로 가지 않습니다
- **생성 프롬프트**:

  ```
  견적서를 저장한 직후의 완료 화면을 만들어줘. 1) 가운데 큰 체크 표시와 "견적서를 저장했어요"· 견적 번호 EST-20260817-0042 2) 저장한 견적 요약 카드 — 조건 칩, 총액, "2026년 9월 16일까지 유효" 3) 견적서 이름을 고치는 입력칸 — 기본값 "성동구 32평 전체시공" 4) 버튼 세 개 — [PDF 내려받기] [문자로 받기] [링크 복사] 5) 안내 박스 — "이 견적은 30일 동안 유효해요. 자재 값과 인건비가 오르면 금액이 달라질 수 있어서예요" 6) "이제 뭘 하면 되나요?" 카드 3장 — 방문 실측 예약하기 / 자재 미리 골라두기 / 비슷한 사례 더 보기 7) 하단에 [내 견적 목록 보기] 완료 화면이지만 막다른 길이 되지 않게 다음 행동 버튼을 크게 두어줘. ⚠ 보여 줄 것이 많은 화면이다. 로그인 상자처럼 «가운데 좁은 카드»에 몰아넣지 마라 — 요약은 제 폭으로 펴고, 다음에 할 일은 옆(모바일에서는 아래)에 둔다. 가운데 정렬은 «끝났다는 체크 표시»에만 쓴다.
  ```

### ES-05 · 견적 견주기
- **소속 메뉴**: 견적
- **뒤로가기**: 좌측 상단에 `‹ 견적 조건 입력 - 단계별` — 누르면 `ES-01`로 간다
- **일정**: 2026-10-01 ~ 2026-10-05
- **기능정의**: 내가 저장한 견적 목록에서 두 개를 고르는 줄 · 좌우로 나란히 놓인 두 견적 표 · 다른 칸에 표시(색 배경)가 생기는 것 · 「다른 것만 보기」 토글 · 총액 차이를 맨 위에 큰 글자로 · 공정별 금액 차이 막대 · 어느 쪽이 어떤 점에서 나은지 짚어 주는 한 줄 · 고른 견적으로 실측 예약하기
- **버튼 → 이동화면**:
  - `실측 예약` → `VS-01` 방문 실측 예약
  - `견적 결과` → `ES-02` 견적 결과
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 토글 — 누르면 **켜짐·꺼짐이 눈에 보이게** 바뀌고 그대로 남습니다
- **생성 프롬프트**:

  ```
  저장해 둔 견적 두 개를 나란히 견주는 화면을 만들어줘. 1) 상단에 내 견적 카드 줄 — 저장한 견적들 중 두 개를 고른다. 세 개째를 고르면 안내가 뜬다 2) 맨 위에 총액 차이를 크게 — "고급 마감이 640만원 더 듭니다" 3) 좌우 2단 비교 표 — 왼쪽 견적 A, 오른쪽 견적 B. 행: 총액, 마감 등급, 공사 기간, 공정별 금액 9줄, 포함 안 된 것 4) 값이 다른 줄은 배경에 옅은 색을 깔아 눈에 띄게. 같은 줄은 그대로 5) [다른 것만 보기] 토글 — 켜면 같은 값인 줄이 숨는다 6) 공정별 차이 가로 막대 — 어느 공정에서 차이가 벌어지는지 7) 아래에 "A 는 기간이 4일 짧고, B 는 마루·타일 등급이 한 칸 높아요" 한 줄 정리 8) 각 열 아래에 [이 견적으로 실측 예약] 버튼 2단이 좁아지면 모바일에서는 위아래로 접히게 하고, 표가 짜부라지지 않게 해줘.
  ```

### VS-01 · 방문 실측 예약
- **소속 메뉴**: 실측 예약
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-10-06 ~ 2026-10-08
- **기능정의**: 가능한 날짜를 보여 주는 달력(꽉 찬 날은 흐리게, 오늘 이전은 안 눌림) · 고른 날의 시간대 칸(오전·오후 2시간 단위, 남은 자리 수 표시) · 주소와 동·호수 입력 · 현장 상황 체크(거주 중·공실·짐 있음) · 실측에 걸리는 시간과 준비할 것 안내 · 오른쪽 예약 요약 카드(날짜·시간·주소·담당) · 연결할 견적 고르기 · 방문비 무료 안내와 취소 규정
- **버튼 → 이동화면**:
  - `예약 확인` → `VS-02` 예약 내용 확인
  - `견적 결과` → `ES-02` 견적 결과
- **생성 프롬프트**:

  ```
  방문 실측 날짜를 잡는 예약 화면을 만들어줘. 1) 왼쪽에 달력 — 이번 달과 다음 달. 예약이 꽉 찬 날은 흐리게, 오늘 이전은 눌리지 않게, 자리가 적게 남은 날은 "1자리" 배지 2) 날짜를 누르면 아래에 시간대 칸이 뜬다 — 09:00 / 11:00 / 14:00 / 16:00. 각 칸에 "2자리 남음", 꽉 찬 칸은 흐리게 하고 눌리지 않게 3) 오른쪽에 예약 요약 카드 — 고른 날짜·시간이 채워지고, 아래로 주소 입력(우편번호 찾기 + 상세주소), 동·호수, 연락처 4) 현장 상황 체크 — 지금 살고 계신가요? (거주 중 / 비어 있음 / 짐만 있음), 엘리베이터 있음, 주차 가능 5) 연결할 견적 고르기 드롭다운 — 저장해 둔 견적 목록 6) 안내 박스 — "실측은 40분에서 1시간 걸려요. 방문비는 받지 않습니다. 하루 전까지 연락 주시면 바꿔 드려요" 7) 하단 [예약하기] 버튼 달력이 이 화면의 주인공이다. 날짜 칸을 크게 하고 상태를 색이 아니라 배지와 흐림으로도 구분되게 해줘.
  ```

### VS-02 · 예약 내용 확인
- **소속 메뉴**: 실측 예약
- **뒤로가기**: 좌측 상단에 `‹ 방문 실측 예약` — 누르면 `VS-01`로 간다
- **일정**: 2026-10-09 ~ 2026-10-13
- **기능정의**: 예약 내용을 한 장에 모아 보여 주는 확인 표(날짜·시간·주소·연락처·현장 상황·연결한 견적) · 줄마다 고치기 버튼 · 방문하는 담당자 카드(사진·이름·경력) · 실측 때 무엇을 하는지 3단계 · 준비해 주실 것 목록 · 개인정보 수집 동의 체크(체크해야 확정 버튼이 열린다) · 취소·변경 규정 · 예약 확정 버튼
- **버튼 → 이동화면**:
  - `예약 완료` → `VS-03` 예약 완료
  - `예약 화면으로` → `VS-01` 방문 실측 예약
- **생성 프롬프트**:

  ```
  실측 예약을 확정하기 전 내용을 확인하는 화면을 만들어줘. 1) 상단에 "이대로 예약할까요?" 와 예약 내용 표 — 날짜·시간 / 주소 / 연락처 / 현장 상황 / 연결한 견적. 줄마다 오른쪽에 [고치기] 2) 방문하는 담당자 카드 — 사진, 이름, "현장 경력 11년", 한 줄 인사 3) "실측 때 이런 걸 해요" 3단계 — 치수 재기 / 배관·전기 상태 확인 / 요구사항 듣기 4) "준비해 주시면 좋아요" 목록 — 도면이 있으면 미리 보여 주기, 바꾸고 싶은 곳 사진 찍어 두기, 예산 범위 정리해 두기 5) 개인정보 수집·이용 동의 체크박스와 [전문 보기]. ⚠ 이 체크를 하기 전에는 아래 [예약 확정] 버튼이 눌리지 않아야 한다. 버튼은 반드시 button 요소로 만들고 disabled 를 쓰되, 체크하면 실제로 열리게 여는 장치를 붙여줘(a 태그로 만들면 잠기지 않는다) 6) 취소·변경 규정 3줄 7) 하단 [예약 확정] 버튼 — 처음엔 잠겨 있다 "동의하면 열립니다"라고 써 놓고 여는 길이 없으면 거짓말이 된다. 실제로 열리게 해줘.
  ```

### VS-03 · 예약 완료
- **소속 메뉴**: 실측 예약
- **뒤로가기**: 좌측 상단에 `‹ 방문 실측 예약` — 누르면 `VS-01`로 간다
- **일정**: 2026-10-14 ~ 2026-10-16
- **기능정의**: 예약됐다는 체크 표시와 예약 번호 · 날짜·시간을 큰 글자로 · 담당자 연락처와 문자 안내 · 내 캘린더에 담기 버튼 · 실측 전날 알림이 간다는 안내 · 실측 뒤 무엇이 이어지는지 3단계(확정 견적→계약→착공) · 예약 변경·취소로 가는 길 · 기다리는 동안 볼 것(사례·자재)
- **버튼 → 이동화면**:
  - `예약 변경·취소` → `VS-04` 예약 변경·취소
  - `시공사례` → `CS-01` 시공사례 목록
  - `홈으로` → `HO-01` 홈
- **생성 프롬프트**:

  ```
  실측 예약이 끝난 완료 화면을 만들어줘. 1) 가운데 큰 체크와 "실측 예약이 잡혔어요"· 예약번호 VST-20260817-0031 2) 날짜와 시간을 아주 크게 — "9월 3일 (수) 오후 2시" 3) 담당자 카드 — 사진, 이름, 연락처, [문자 보내기] 4) [내 캘린더에 담기] 버튼과 "전날 오전에 확인 문자를 보내 드려요" 안내 5) "실측 뒤에는 이렇게 이어져요" 3단계 — 확정 견적서 받기(2일 안) → 계약서 검토·서명 → 착공일 잡기 6) [예약 바꾸기] [예약 취소] 작은 버튼 두 개 7) "기다리는 동안" 카드 2장 — 비슷한 사례 보기 / 자재 미리 골라두기 8) 접혀 있는 [오시는 길] — 누르면 지도 썸네일과 주차 안내가 펼쳐진다 완료 화면이 막다른 길이 되지 않게 다음에 할 일을 분명히 적어줘. ⚠ 보여 줄 것이 많은 화면이다. 로그인 상자처럼 «가운데 좁은 카드»에 몰아넣지 마라 — 요약은 제 폭으로 펴고, 다음에 할 일은 옆(모바일에서는 아래)에 둔다. 가운데 정렬은 «끝났다는 체크 표시»에만 쓴다.
  ```

### VS-04 · 예약 변경·취소
- **소속 메뉴**: 실측 예약
- **뒤로가기**: 좌측 상단에 `‹ 방문 실측 예약` — 누르면 `VS-01`로 간다
- **일정**: 2026-10-19 ~ 2026-10-21
- **기능정의**: 지금 잡힌 예약 요약 카드 · 「날짜만 바꾸기」와 「아예 취소하기」 두 갈래 · 날짜 바꾸기를 고르면 펼쳐지는 달력과 남은 자리 · 취소를 고르면 나오는 사유 선택과 자유 입력 · 하루 전까지는 그냥 되고 당일에는 연락이 필요하다는 안내 · 바뀐 내용 미리보기 · 확정 버튼(되돌릴 수 없다는 확인)
- **버튼 → 이동화면**:
  - `예약 완료` → `VS-03` 예약 완료
  - `홈으로` → `HO-01` 홈
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 선택 — 고른 개수를 **숫자로 보여주고**, 하나도 안 골랐으면 일괄 단추를 눌리지 않게 둡니다
- **생성 프롬프트**:

  ```
  잡아 둔 실측 예약을 바꾸거나 취소하는 화면을 만들어줘. 1) 상단에 지금 예약 요약 카드 — "9월 3일 (수) 오후 2시· 성동구 ○○아파트 101동 1203호" 2) 갈래 두 개를 큰 카드로 — [날짜만 바꾸기] / [예약 취소하기]. 고른 쪽 아래가 펼쳐진다 3) 날짜 바꾸기를 고르면 — 달력이 펼쳐지고 새 날짜·시간을 고른다. 아래에 "9월 3일 오후 2시 → 9월 8일 오전 11시" 바뀐 내용 미리보기 4) 취소하기를 고르면 — 사유 라디오(일정이 안 맞아요 / 공사를 미루기로 했어요 / 다른 업체로 정했어요 / 기타)와 자유 입력칸. 사유에 따라 아래 안내 문구가 바뀐다 5) 안내 박스 — "하루 전까지는 여기서 바로 바꾸실 수 있어요. 방문 당일에는 전화로 연락 주세요" 6) 하단 [이대로 바꾸기] 또는 [예약 취소하기] — 취소는 되돌릴 수 없다는 확인 한 줄 취소를 어렵게 숨기지 않는 것이 신뢰다. 두 갈래를 같은 크기로 두어줘.
  ```

### CT-01 · 계약서 확인·서명
- **소속 메뉴**: 계약·결제
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-10-22 ~ 2026-10-26
- **기능정의**: 실측으로 확정된 견적과 처음 견적의 차이를 맨 위에 밝히는 표 · 계약서 조항 목록(공사 범위·기간·금액·지급 시기·지체상금·하자보수·해지) · 조항마다 펼쳐 보는 전문과 「쉽게 말하면」 한 줄 · 첨부 도면과 자재 목록 · 손으로 그리는 서명란과 지우기 · 동의 체크 3개(계약 조건·개인정보·전자서명) · 셋을 다 해야 열리는 계약 버튼 · 계약서 PDF 내려받기
- **버튼 → 이동화면**:
  - `계약금 결제` → `CT-02` 계약금 결제
  - `견적 결과` → `ES-02` 견적 결과
- **생성 프롬프트**:

  ```
  인테리어 공사 계약서를 확인하고 서명하는 화면을 만들어줘. 1) 맨 위에 "실측으로 금액이 이렇게 바뀌었어요" 비교 표 — 처음 견적 3,240만원 / 확정 견적 3,410만원 / 차이 +170만원. 차이가 난 항목 3줄(배관 노후로 교체, 단열 보강, 폐기물 증가) 2) 계약서 조항 아코디언 — 공사 범위 / 공사 기간 / 계약 금액 / 대금 지급 시기 / 지체상금 / 하자보수 / 계약 해지. 조항마다 전문이 펼쳐지고, 그 위에 회색 띠로 "쉽게 말하면" 한 줄 요약 3) 첨부 — 평면 도면 썸네일, 자재 목록 표, 공정표. 각각 [크게 보기] 4) 대금 지급 표 — 계약금 341만(오늘) / 착공금 1,023만(9/10) / 중도금 1,364만(9/24) / 잔금 682만(준공 후) 5) 서명란 — 손가락이나 마우스로 그리는 네모 칸과 [지우기]. 그리면 아래에 서명한 날짜가 자동으로 찍힌다 6) 동의 체크 3개 — 계약 조건에 동의 / 개인정보 처리에 동의 / 전자서명에 동의. ⚠ 셋을 다 체크해야 아래 [계약하고 계약금 결제] 버튼이 열린다. 버튼은 button 요소로 만들고 실제로 열리는 장치를 붙여줘 7) [계약서 PDF 내려받기] 돈이 큰 화면이다. 차이가 난 금액을 숨기지 말고 맨 위에 두는 것이 핵심이다.
  ```

### CT-02 · 계약금 결제
- **소속 메뉴**: 계약·결제
- **뒤로가기**: 좌측 상단에 `‹ 계약서 확인·서명` — 누르면 `CT-01`로 간다
- **일정**: 2026-10-27 ~ 2026-10-29
- **기능정의**: 이번에 낼 금액을 큰 글자로와 전체 대금 중 몇 번째인지 단계 막대 · 결제 수단 탭(카드·계좌이체·무통장) · 수단별 입력칸 · 할부 개월과 월 납입액 계산 · 현금영수증·세금계산서 발급 정보 · 결제 전 마지막 확인 요약(현장·금액·다음 낼 날) · 안전거래 안내 · 결제 버튼
- **버튼 → 이동화면**:
  - `결제 완료` → `CT-03` 결제 완료 - 계약 성사
  - `결제 실패` → `CT-04` 결제 실패
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
- **생성 프롬프트**:

  ```
  계약금을 결제하는 화면을 만들어줘. 1) 맨 위에 이번에 낼 금액을 크게 — "계약금 3,410,000원" 2) 그 아래 대금 단계 막대 4칸 — 계약금(지금) / 착공금 / 중도금 / 잔금. 지금 칸에 표시가 있고 나머지는 흐리게, 각 칸 아래 예정일 3) 결제 수단 탭 — 신용카드 / 계좌이체 / 무통장입금. ⚠ 탭과 아래 입력칸은 같은 상자 안에 두어 탭이 실제로 먹게 4) 카드 탭 — 카드사 선택, 할부 개월 드롭다운(고르면 옆에 "월 568,333원" 계산), 무이자 안내 5) 현금영수증 / 세금계산서 라디오와 체크하면 펼쳐지는 발급 정보 입력칸 6) 오른쪽에 확인 요약 카드 — 현장 주소, 총 계약금액, 이번 결제액, "다음 결제는 9월 10일 착공금 10,230,000원" 7) 안전 안내 — "공사 전 계약금은 10%를 넘지 않습니다" 8) 하단 [3,410,000원 결제하기] 금액을 크게, 다음에 낼 돈까지 미리 보여 주는 것이 이 화면의 신뢰다.
  ```

### CT-03 · 결제 완료 - 계약 성사
- **소속 메뉴**: 계약·결제
- **뒤로가기**: 좌측 상단에 `‹ 계약서 확인·서명` — 누르면 `CT-01`로 간다
- **일정**: 2026-10-30 ~ 2026-11-03
- **기능정의**: 결제됐다는 체크와 계약 번호 · 계약이 성사됐다는 것과 이제 무엇이 시작되는지 · 착공일 고르기(달력) · 남은 대금 일정 표 · 계약서·영수증 내려받기 · 담당 현장소장 배정 안내와 연락처 · 착공 전 준비할 것 목록(이사·짐 정리·관리사무소 신고) · 공사 진행 화면으로 가는 큰 버튼
- **버튼 → 이동화면**:
  - `공사 진행 보기` → `PR-01` 공정표
  - `홈으로` → `HO-01` 홈
- **생성 프롬프트**:

  ```
  계약금 결제가 끝나 계약이 성사된 완료 화면을 만들어줘. 1) 가운데 큰 체크와 "계약이 성사됐어요"· 계약번호 CTR-20260817-0018 2) "이제 공사가 시작됩니다" 한 줄과 착공 예정일 3) 착공일 고르기 — 달력을 열어 원하는 날을 고른다. 고르면 위 문구의 날짜가 바뀐다. "착공일 5일 전까지는 바꾸실 수 있어요" 4) 남은 대금 일정 표 — 착공금 / 중도금 / 잔금. 각각 금액과 예정일, 상태 배지 5) [계약서 내려받기] [영수증 보기] — 영수증은 누르면 상세 내역이 표로 펼쳐진다 6) 담당 현장소장 카드 — 사진, 이름, 연락처, "매일 저녁 사진으로 알려 드려요" 7) "착공 전에 준비해 주세요" 체크 목록 — 짐 빼기, 관리사무소에 공사 신고, 엘리베이터 사용 예약, 이웃에 인사 8) 하단 큰 버튼 [공사 진행 보기] 여기서부터 손님의 관심은 "언제 어떻게 되나"로 옮겨 간다. 그 길을 크게 열어줘. ⚠ 보여 줄 것이 많은 화면이다. 로그인 상자처럼 «가운데 좁은 카드»에 몰아넣지 마라 — 요약은 제 폭으로 펴고, 다음에 할 일은 옆(모바일에서는 아래)에 둔다. 가운데 정렬은 «끝났다는 체크 표시»에만 쓴다.
  ```

### CT-04 · 결제 실패
- **소속 메뉴**: 계약·결제
- **뒤로가기**: 좌측 상단에 `‹ 계약서 확인·서명` — 누르면 `CT-01`로 간다
- **일정**: 2026-11-04 ~ 2026-11-06
- **기능정의**: 실패했다는 것과 「돈은 빠져나가지 않았습니다」를 먼저 못 박기 · 실패 사유와 코드 · 사유별로 무엇을 하면 되는지 안내 · 다시 시도하기와 다른 수단으로 내기 버튼 · 계약은 아직 살아 있고 며칠까지 결제하면 되는지 · 무통장으로 돌리기 · 전화 상담 연결
- **버튼 → 이동화면**:
  - `다시 결제하기` → `CT-02` 계약금 결제
  - `계약서로` → `CT-01` 계약서 확인·서명
- **생성 프롬프트**:

  ```
  계약금 결제가 실패했을 때의 화면을 만들어줘. 1) 맨 위에 "결제가 되지 않았어요"와 바로 아래 굵게 "결제 금액은 빠져나가지 않았습니다" 2) 실패 사유 카드 — "카드 한도를 넘었습니다 (코드 F-2201)". [자세히] 를 누르면 무슨 뜻인지 설명이 펼쳐진다 3) "이렇게 해 보세요" 목록 3개 — 한도를 확인하고 다시 시도 / 다른 카드로 결제 / 계좌이체로 결제 4) 버튼 두 개 — [다시 시도하기] [다른 수단으로 내기] 5) 안내 박스 — "계약은 아직 살아 있어요. 8월 20일까지 결제하시면 됩니다" 6) [무통장입금으로 바꾸기] — 계좌번호와 입금자명 안내 7) 하단에 [전화 상담] 버튼과 운영 시간 실패 화면에서 가장 중요한 것은 돈이 안 빠졌다는 안심이다. 그것을 맨 위에 크게.
  ```

### PR-01 · 공정표
- **소속 메뉴**: 공사 진행
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-11-09 ~ 2026-11-12
- **기능정의**: 전체 진행률과 남은 날수를 맨 위에 · 공정별 가로 막대 일정표(철거·설비·전기·목공·타일·도배·마루·조명·청소)와 오늘 선 · 공정 상태 배지(끝남·하는 중·기다림·밀림) · 막대를 누르면 펼쳐지는 세부 일정과 담당 팀 · 앞 공정이 밀리면 뒤가 어떻게 밀리는지 표시 · 주 보기와 월 보기 전환 · 이번 주에 있을 일 목록 · 공정별 진행 사진으로 가는 길
- **버튼 → 이동화면**:
  - `오늘 현장` → `PR-02` 오늘 현장
  - `사진 일지` → `PR-03` 현장 사진 일지
  - `추가공사 승인` → `PR-04` 추가공사 변경 견적 승인
  - `준공 검수` → `PR-05` 준공 검수 체크리스트
  - `진행 중 공사 없음` → `PR-06` 공사 진행 - 진행 중 없음
- **생성 프롬프트**:

  ```
  공사 공정표(간트) 화면을 만들어줘. 이 팩에서 가장 중요한 화면이다. 1) 맨 위에 진행률 큰 막대 — "62%· 22일 중 14일 지남· 준공 예정 9월 30일" 2) 그 아래 공정별 가로 막대 일정표 — 왼쪽에 공정 이름 세로 목록(철거 / 설비·배관 / 전기 / 목공 / 타일 / 도배 / 마루 / 조명·마무리 / 입주청소), 오른쪽에 날짜 눈금과 막대. 오늘 위치에 세로 선을 긋고 "오늘" 표시 3) 막대마다 상태 배지 — 끝남(체크) / 하는 중 / 기다림 / 밀림(빨강). 밀린 공정은 막대에 빗금을 넣고, 그 뒤 공정 막대에 "2일 밀림" 꼬리표 4) 막대를 누르면 아래에 그 공정 상세가 펼쳐진다 — 담당 팀, 실제 시작·종료일, 오늘 한 일, 남은 일 5) 오른쪽 위에 [주 보기] [월 보기] 전환과 [오늘로 가기] 6) 일정표 아래에 "이번 주에 있을 일" 목록 — 날짜와 공정, 집주인이 할 일 (9/22 타일 색 최종 확인 필요) 은 표시를 달리해서 7) 하단에 [오늘 현장 보기] [사진 일지 보기] 일정표가 가로로 길다. 좁은 화면에서는 가로 스크롤이 되게 하되 공정 이름 열은 왼쪽에 고정해줘. 화면 전체가 가로로 밀리면 안 된다.
  ```

### PR-02 · 오늘 현장
- **소속 메뉴**: 공사 진행
- **뒤로가기**: 좌측 상단에 `‹ 공정표` — 누르면 `PR-01`로 간다
- **일정**: 2026-11-13 ~ 2026-11-18
- **기능정의**: 오늘 날짜와 며칠째인지 · 오늘 진행 중인 공정과 현장에 온 팀 · 오늘 찍은 사진 줄 · 현장소장이 남긴 오늘의 한마디 · 오늘 끝난 일과 내일 할 일 목록 · 집주인이 오늘 정해 줘야 할 것(있으면 위로 올라온다) · 소장에게 바로 묻는 말풍선 · 날씨와 그 때문에 밀릴 수 있는 공정 안내 · 어제·내일로 넘기는 화살표
- **버튼 → 이동화면**:
  - `공정표` → `PR-01` 공정표
  - `사진 일지` → `PR-03` 현장 사진 일지
  - `자재 고르기` → `CS-04` 자재·마감 둘러보기
- **생성 프롬프트**:

  ```
  오늘 현장에서 무슨 일이 있었는지 보여 주는 화면을 만들어줘. 1) 맨 위에 "9월 22일 (월)· 공사 14일째" 와 좌우 화살표(어제 / 내일) 2) 오늘 공정 카드 — "타일 시공 2일차" 와 현장에 온 팀(타일 2명, 설비 1명) 3) 집주인이 정해 줘야 할 것이 있으면 맨 위에 눈에 띄게 — "욕실 바닥 타일 색을 오늘까지 정해 주세요" 와 [고르러 가기] 버튼 4) 오늘 찍은 사진 가로 줄 6장 — 누르면 크게 보기가 뜨고 좌우로 넘긴다. 사진마다 찍은 시각과 공간 이름 5) 현장소장 한마디 카드 — 프로필 사진과 함께 "욕실 방수 두 번째 치기 끝냈습니다. 내일 하루 말리고 모레 타일 들어갑니다" 6) 오늘 끝난 일 / 내일 할 일 두 목록 — 체크 표시로 7) 날씨 카드 — "내일 비· 베란다 도장은 하루 미룰 수 있어요" 8) 하단 고정 말풍선 입력칸 — [소장에게 묻기]. 보낸 말이 아래에 쌓인다 매일 들어와 보는 화면이다. 사진과 한마디가 주인공이고 나머지는 작게.
  ```

### PR-03 · 현장 사진 일지
- **소속 메뉴**: 공사 진행
- **뒤로가기**: 좌측 상단에 `‹ 공정표` — 누르면 `PR-01`로 간다
- **일정**: 2026-11-19 ~ 2026-11-24
- **기능정의**: 날짜별로 묶인 사진 일지(최신이 위) · 공정 필터 칩과 공간 필터 칩 · 날짜 묶음마다 그날 공정·팀·사진 장수 · 사진 격자와 찍은 시각·공간 꼬리표 · 사진에 달린 소장 설명 · 같은 자리를 날짜별로 견주어 보는 「이 자리 변해 온 것」 · 전체 사진 내려받기 · 준공 앨범으로 묶기
- **버튼 → 이동화면**:
  - `오늘 현장` → `PR-02` 오늘 현장
  - `공정표` → `PR-01` 공정표
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 필터 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
- **생성 프롬프트**:

  ```
  공사 현장 사진을 날짜별로 모아 보는 일지 화면을 만들어줘. 1) 상단에 필터 칩 두 줄 — 공정별(철거·설비·목공·타일·도배·마루) / 공간별(거실·주방·욕실·침실·베란다). 고른 칩에 표시가 남는다 2) 총 사진 수와 [전체 내려받기] 버튼 3) 날짜별 묶음 목록 — 최신 날짜가 위. 묶음 머리에 "9월 22일 (월)· 공사 14일째· 타일 시공· 사진 8장"과 접기 화살표 4) 묶음 안에 사진 격자 4열 — 사진마다 찍은 시각과 공간 꼬리표. 사진 아래에 소장이 남긴 한 줄이 있으면 같이 5) 특별한 묶음 하나 — "이 자리가 변해 온 것" — 같은 각도로 찍은 거실 사진을 날짜순으로 가로로 늘어놓고 끌면 넘어가게 6) 하단 [준공 앨범으로 묶기] 버튼 사진이 많은 화면이다. 격자 칸 비율을 4:3으로 통일하고 줄과 줄 사이 간격을 일정하게 유지해줘.
  ```

### PR-04 · 추가공사 변경 견적 승인
- **소속 메뉴**: 공사 진행
- **뒤로가기**: 좌측 상단에 `‹ 공정표` — 누르면 `PR-01`로 간다
- **일정**: 2026-11-25 ~ 2026-11-27
- **기능정의**: 왜 추가공사가 생겼는지 먼저 설명(현장 사진과 함께) · 추가 항목 표(내용·수량·단가·금액·왜 필요한지) · 항목마다 승인·거절을 따로 고르는 것 · 거절하면 어떻게 되는지 경고 · 승인하면 공사 기간이 며칠 늘어나는지 · 바뀐 총액과 남은 대금 일정 재계산 · 승인 체크와 서명 · 「나중에 정하기」와 그 경우 공사가 멈추는 자리 안내 · 소장에게 묻기
- **버튼 → 이동화면**:
  - `공정표` → `PR-01` 공정표
  - `오늘 현장` → `PR-02` 오늘 현장
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 수량 — 누를 때마다 **숫자와 합계가 같이** 바뀝니다
- **생성 프롬프트**:

  ```
  공사 도중 생긴 추가공사의 변경 견적을 승인하는 화면을 만들어줘. 이 화면이 이 팩에서 쇼핑몰로는 만들 수 없다는 증거다. 1) 맨 위에 "추가 공사가 필요합니다" 와 요청한 날짜, 소장 이름 2) 왜 생겼는지 카드 — 현장 사진 2장(누수 흔적)과 설명 3줄. "철거하고 보니 배관 이음새가 삭아 있었습니다. 그대로 두면 도배 뒤에 물이 새어 다시 뜯어야 합니다" 3) 추가 항목 표 — 내용 / 수량 / 단위 / 단가 / 금액 / 판단. 행: 온수 배관 교체 12m, 배관 자재, 방수 재시공, 벽체 보수. 행마다 오른쪽에 [승인] [거절] 라디오. 고르면 아래 합계가 다시 계산된다 4) 행을 누르면 "왜 필요한가"가 펼쳐진다 — 안 하면 생길 일과 나중에 들 비용 5) 거절한 항목이 있으면 경고 띠 — "배관 교체를 빼면 하자보수 보증에서 이 부분은 빠집니다" 6) 오른쪽 요약 카드 — 기존 계약 3,410만 / 추가 +186만 / 바뀐 총액 3,596만. 공사 기간 "22일 → 25일 (3일 늘어남)". 남은 대금 일정이 다시 계산된 표 7) 승인 체크박스와 서명란. ⚠ 체크해야 아래 [승인하고 진행하기] 버튼이 열린다. button 요소로 만들고 실제로 열리는 장치를 붙여줘 8) 아래에 [나중에 정하기] — 옆에 작게 "정해 주실 때까지 이 자리 공사가 멈춥니다" 9) [소장에게 물어보기] 버튼 돈이 더 나가는 화면이다. 왜 필요한지를 사진과 함께 먼저 보여 주고, 거절할 자유도 같은 크기로 두는 것이 신뢰다.
  ```

### PR-05 · 준공 검수 체크리스트
- **소속 메뉴**: 공사 진행
- **뒤로가기**: 좌측 상단에 `‹ 공정표` — 누르면 `PR-01`로 간다
- **일정**: 2026-11-30 ~ 2026-12-02
- **기능정의**: 공간 탭(거실·주방·욕실·침실·베란다·공용) · 공간별 검수 항목 체크 목록(마감·수평·문 여닫힘·수전 누수·콘센트·조명) · 항목마다 「괜찮음·문제 있음」 두 갈래 · 문제 있음을 고르면 열리는 사진 올리기와 메모 · 검수 진행 숫자(48개 중 41개 확인) · 문제로 잡힌 것 모아 보기 · 재시공 요청과 처리 기한 안내 · 다 끝나면 열리는 준공 승인 버튼 · 잔금 결제로 이어지는 길
- **버튼 → 이동화면**:
  - `하자보수 접수` → `AS-01` 하자보수 접수
  - `공정표` → `PR-01` 공정표
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
- **생성 프롬프트**:

  ```
  준공 검수 체크리스트 화면을 만들어줘. 집주인이 현장을 돌며 하나씩 확인한다. 1) 맨 위에 진행 숫자 — "48개 중 41개 확인· 문제로 잡힌 것 3개"와 진행 막대 2) 공간 탭 — 거실 / 주방 / 욕실 / 침실 / 베란다 / 공용. 탭마다 "8개 중 6개" 처럼 진행이 배지로 붙는다. ⚠ 탭과 아래 목록은 같은 상자 안에 3) 공간별 검수 항목 목록 — 줄마다 항목 이름, 설명 한 줄, 오른쪽에 [괜찮음] [문제 있음] 두 버튼. 예: 벽지 이음새 / 걸레받이 마감 / 문 여닫힘 / 콘센트 작동 / 조명 스위치 / 수전 누수 / 배수 속도 / 실리콘 마감 4) [문제 있음] 을 고르면 그 줄 아래가 펼쳐진다 — 사진 올리기 칸(여러 장), 메모 입력, 급한 정도 고르기 5) 오른쪽에 "문제로 잡힌 것" 패널 — 3건이 목록으로 쌓이고 각각 공간과 항목 6) 하단 안내 — "재시공은 접수 후 3일 안에 시작합니다" 7) 맨 아래 [준공 승인하고 잔금 결제] 버튼 — ⚠ 모든 항목을 확인해야 열린다. 안 끝났으면 "7개가 남았어요"라고 알려 준다. button 요소로 만들고 실제로 열리는 장치를 붙여줘 현장에서 폰으로 보는 화면이다. 좁은 화면에서 버튼이 손가락에 닿게 크게 잡아줘.
  ```

### PR-06 · 공사 진행 - 진행 중 없음
- **소속 메뉴**: 공사 진행
- **뒤로가기**: 좌측 상단에 `‹ 공정표` — 누르면 `PR-01`로 간다
- **일정**: 2026-12-03 ~ 2026-12-07
- **기능정의**: 지금 진행 중인 공사가 없다는 안내 · 왜 비어 있는지(아직 계약 전이거나 다 끝났거나)에 따라 다른 문구 · 계약 전이라면 견적·실측으로 가는 길 · 끝난 공사가 있으면 준공 앨범과 하자보수로 가는 길 · 지난 공사 목록 접어 두기 · 하자보수 보증 기간이 얼마나 남았는지
- **버튼 → 이동화면**:
  - `예상 견적 내기` → `ES-01` 견적 조건 입력 - 단계별
  - `하자보수 접수` → `AS-01` 하자보수 접수
  - `홈으로` → `HO-01` 홈
- **생성 프롬프트**:

  ```
  진행 중인 공사가 하나도 없을 때의 화면을 만들어줘. 1) 가운데 그림 하나와 "지금 진행 중인 공사가 없어요" 2) 아래에 상황에 맞는 안내 두 갈래를 카드로 — 왼쪽: "아직 계약 전이신가요?" → [예상 견적 내기] [실측 예약] 오른쪽: "공사가 끝나셨나요?" → [준공 앨범 보기] [하자보수 접수] 3) 접혀 있는 [지난 공사 보기] — 누르면 끝난 현장 카드가 펼쳐진다. 카드마다 주소, 준공일, 총 금액, [앨범] [하자보수] 버튼 4) 하자보수 보증 안내 띠 — "성동구 ○○아파트· 보증 기간 2027년 9월 30일까지 (남은 기간 13개월)" 5) 하단에 시공 사례 6개 빈 화면이지만 막다른 길이 아니게, 나가는 문을 네 개 두어줘.
  ```

### AS-01 · 하자보수 접수
- **소속 메뉴**: 하자보수
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-12-08 ~ 2026-12-11
- **기능정의**: 보증 기간이 얼마나 남았는지 맨 위에 · 어느 현장인지 고르기(여러 곳이면) · 부위 고르기(도배·바닥·타일·설비·전기·창호·목공)와 부위에 맞게 바뀌는 증상 목록 · 사진·영상 올리기 · 자세한 상황 적는 칸 · 급한 정도 3단(급함·보통·천천히)과 그에 따른 예상 방문일 · 방문 가능한 요일·시간대 고르기 · 보증에 들어가는 것과 안 들어가는 것 안내 · 접수 버튼
- **버튼 → 이동화면**:
  - `접수 내역` → `AS-02` 하자보수 접수 내역
  - `공사 진행` → `PR-01` 공정표
- **생성 프롬프트**:

  ```
  하자보수를 접수하는 화면을 만들어줘. 1) 맨 위에 보증 안내 띠 — "성동구 ○○아파트 101동 1203호· 보증 기간 2027년 9월 30일까지· 남은 기간 13개월" 2) 부위 고르기 — 도배 / 바닥 / 타일 / 설비·수전 / 전기·조명 / 창호 / 목공. 큼직한 아이콘 카드로. 고르면 아래 증상 목록이 그 부위 것으로 바뀐다 3) 증상 고르기 — 예: 타일을 고르면 "줄눈이 갈라짐 / 타일이 들뜸 / 깨짐 / 물이 스밈" 이 나온다. 여러 개 고를 수 있게 4) 사진·영상 올리기 — 끌어다 놓는 칸. 올린 것은 썸네일로 쌓이고 각각 [지우기]. "가까이 찍은 것과 멀리서 찍은 것을 같이 올려 주시면 빨라요" 도움말 5) 자세한 상황 적는 칸 — "언제부터 그랬는지 적어 주시면 도움이 됩니다" 6) 급한 정도 3단 — 급함(물이 새요) / 보통 / 천천히. 고르면 옆에 "예상 방문 9월 24일" 처럼 날짜가 바뀐다 7) 방문 가능한 요일·시간대 체크 8) 접혀 있는 안내 — "보증에 들어가는 것 / 들어가지 않는 것" 두 목록 9) 하단 [접수하기] 불편해서 들어온 사람이 쓰는 화면이다. 단계를 줄이고 사진 올리기를 크게 두어줘.
  ```

### AS-02 · 하자보수 접수 내역
- **소속 메뉴**: 하자보수
- **뒤로가기**: 좌측 상단에 `‹ 하자보수 접수` — 누르면 `AS-01`로 간다
- **일정**: 2026-12-14 ~ 2026-12-16
- **기능정의**: 상태 탭(전체·접수됨·방문 예정·처리 중·완료) · 접수 건 목록(접수일·부위·증상·상태 배지·예상 방문일) · 줄을 펼치면 나오는 진행 단계 막대와 사진 · 급한 건은 위로 · 처리가 늦어지는 건에 붙는 표시 · 접수 건이 없을 때 안내 · 새로 접수하기 버튼 · 보증 기간 남은 날
- **버튼 → 이동화면**:
  - `처리 현황` → `AS-03` 하자보수 처리 현황
  - `새로 접수` → `AS-01` 하자보수 접수
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
  - 펼치 — 누르면 그 자리에서 펴지고 접힙니다. 다른 화면으로 가지 않습니다
- **생성 프롬프트**:

  ```
  하자보수 접수 내역 목록 화면을 만들어줘. 1) 상단에 보증 기간 띠와 [새로 접수하기] 버튼 2) 상태 탭 — 전체(5) / 접수됨(1) / 방문 예정(1) / 처리 중(1) / 완료(2). ⚠ 탭과 아래 목록은 같은 상자 안에 3) 접수 건 목록 — 줄마다 접수일, 부위 아이콘, 증상 요약, 상태 배지, 예상 방문일 또는 처리 완료일. 급하다고 접수한 건은 맨 위에 두고 표시를 달리 4) 접수 후 사흘이 지났는데 방문일이 안 잡힌 건에는 "확인 중입니다" 띠 5) 줄을 누르면 아래가 펼쳐진다 — 진행 단계 막대(접수 → 확인 → 방문 → 처리 → 완료), 올린 사진 썸네일, 담당자 메모 6) 목록이 비어 있으면 "아직 접수하신 하자가 없어요"와 [접수하기] 표가 짜부라지지 않게, 좁은 화면에서는 표 대신 카드로 접히게 해줘.
  ```

### AS-03 · 하자보수 처리 현황
- **소속 메뉴**: 하자보수
- **뒤로가기**: 좌측 상단에 `‹ 하자보수 접수` — 누르면 `AS-01`로 간다
- **일정**: 2026-12-17 ~ 2026-12-22
- **기능정의**: 접수 번호와 지금 어느 단계인지 큰 막대 · 접수 내용 요약(부위·증상·올린 사진) · 담당 기사 카드와 방문 예정 일시 · 처리 기록 시간순(접수·확인·방문·처리·완료) · 처리 전·후 사진 견주기 · 원인과 무엇을 했는지 설명 · 이번 처리가 보증에 들어갔는지 비용이 붙었는지 · 처리 확인 체크와 완료 버튼 · 「다시 봐 주세요」 재접수 · 처리 만족도
- **버튼 → 이동화면**:
  - `접수 내역` → `AS-02` 하자보수 접수 내역
  - `새로 접수` → `AS-01` 하자보수 접수
- **생성 프롬프트**:

  ```
  하자보수 한 건의 처리 현황 상세 화면을 만들어줘. 1) 맨 위에 접수번호 AS-20260924-0007 과 진행 단계 가로 막대 5칸 — 접수 → 확인 → 방문 → 처리 → 완료. 지금 칸에 표시, 각 칸 아래 날짜 2) 접수 내용 요약 카드 — 부위·증상, 접수 때 올린 사진 썸네일, 적었던 글 3) 담당 기사 카드 — 사진, 이름, 연락처, "9월 24일 (수) 오후 2시 방문 예정" 4) 처리 기록 시간순 목록 — 날짜·시각과 무엇이 있었는지. "9/22 14:10 접수 / 9/22 17:30 담당 배정 / 9/24 14:00 방문 / 9/24 16:20 처리 완료" 5) 처리 전·후 사진 견주기 — 좌우 두 장을 나란히, 누르면 크게 보기 6) "무엇이 문제였고 무엇을 했나" 설명 카드 — 원인과 처리 방법 3줄 7) 비용 카드 — "보증 기간 안이라 비용이 들지 않았습니다" 또는 보증 밖이면 항목별 금액 표 8) 처리 확인 체크박스와 [처리 완료 확인] 버튼 — 체크해야 열린다 9) 옆에 [다시 봐 주세요] — 누르면 재접수 사유 입력칸이 펼쳐진다 10) 완료 뒤에는 별점과 한 줄 평 남기는 칸 처리 전·후 사진이 이 화면의 증거다. 두 장을 같은 크기로 나란히 두어줘.
  ```

### OW-01 · 현장 대시보드
- **소속 메뉴**: 현장 관리
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-12-23 ~ 2026-12-25
- **기능정의**: 숫자 카드 넷(진행 중 현장·이달 매출·미수금·밀린 공정) · 기간 바꾸기와 그에 따라 다시 계산되는 숫자 · 오늘 해야 할 일 목록(기성 청구·자재 발주·검수 방문) · 진행 중 현장 카드 목록(주소·공정·진행률·다음 일정·밀림 표시) · 이번 주 일정 가로 띠 · 새 견적 요청 알림 · 매출 그래프(월별)
- **버튼 → 이동화면**:
  - `견적 요청함` → `OW-02` 견적 요청함
  - `현장 상세` → `OW-03` 현장 상세 - 공정 편집
  - `청구·수금` → `OW-05` 기성 청구·수금
- **생성 프롬프트**:

  ```
  인테리어 업체가 여러 현장을 한눈에 보는 관리자 대시보드를 만들어줘. 왼쪽에 사이드바 내비(대시보드 / 견적 요청함 / 현장 / 자재·원가 / 청구·수금 / 일정 / 설정)를 두고 오른쪽이 본문이다. 1) 맨 위 숫자 카드 4개 — 진행 중 현장 7곳 / 이달 매출 2억 1,400만원 / 미수금 3,820만원 / 밀린 공정 2건. ⚠ 이 숫자들은 아래 목록·그래프와 반드시 아귀가 맞아야 한다. 사이드바 배지 수와 화면 안 건수가 따로 놀면 사는 사람이 먼저 알아챈다 2) 기간 드롭다운(이번 달 / 지난 달 / 올해) — 바꾸면 위 숫자와 아래 그래프가 바뀐다 3) "오늘 할 일" 목록 — 기성 청구 2건, 자재 발주 1건, 준공 검수 방문 1건. 각 줄에 [처리하러 가기] 4) 진행 중 현장 카드 목록 — 주소, 지금 공정, 진행률 막대, 다음 일정, 밀린 현장은 빨간 꼬리표 5) 이번 주 일정 가로 띠 — 요일별로 어느 현장에 어느 팀이 가는지 6) 새 견적 요청 알림 카드 — "어제 들어온 견적 요청 3건" 과 [보러 가기] 7) 아래에 월별 매출 막대 그래프 — 오른쪽 끝 값이 위 "이달 매출" 카드와 같아야 한다 정보 밀도가 높은 화면이다. 표 한 줄 높이를 일정하게 하고 사이드바(2단) 안에 또 2단을 넣지 마라 — 표가 짜부라진다.
  ```

### OW-02 · 견적 요청함
- **소속 메뉴**: 현장 관리
- **뒤로가기**: 좌측 상단에 `‹ 현장 대시보드` — 누르면 `OW-01`로 간다
- **일정**: 2026-12-28 ~ 2026-12-30
- **기능정의**: 상태 탭(새 요청·확인 중·실측 잡힘·계약됨·놓침) · 요청 목록(들어온 시각·평수·분야·자동 계산 금액·연락처 가림) · 새 요청은 표시가 다르게 · 줄을 펼치면 나오는 손님이 넣은 조건 전부 · 담당자 배정 드롭다운 · 몇 시간 안에 연락했는지 응답 시간 표시 · 놓친 요청과 그 까닭 적기 · 실측 예약으로 바로 넘기기 · 요청 수와 계약 성사율
- **버튼 → 이동화면**:
  - `현장 대시보드` → `OW-01` 현장 대시보드
  - `현장 상세` → `OW-03` 현장 상세 - 공정 편집
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
  - 펼치 — 누르면 그 자리에서 펴지고 접힙니다. 다른 화면으로 가지 않습니다
- **생성 프롬프트**:

  ```
  손님이 넣은 견적 요청이 쌓이는 관리자 화면을 만들어줘. 1) 상단에 이번 달 요청 수와 계약 성사율 — "요청 34건· 실측 19건· 계약 7건 (20.6%)". 세 숫자는 아래 탭 배지와 아귀가 맞아야 한다 2) 상태 탭 — 새 요청(5) / 확인 중(8) / 실측 잡힘(4) / 계약됨(7) / 놓침(10) 3) 요청 목록 표 — 들어온 시각 / 평수·분야 / 자동 계산 금액 / 희망 착공 / 담당자 / 상태. 새 요청 줄은 배경을 달리하고 "3시간 전" 처럼 지난 시간을 표시 4) 4시간 넘게 연락 안 한 요청에는 경고 꼬리표 — "응답이 늦어지고 있어요" 5) 줄을 누르면 아래가 펼쳐진다 — 손님이 단계별로 넣은 조건 전부, 자동 계산된 항목 표, 남긴 메모 6) 줄마다 [담당자 배정] 드롭다운과 [실측 예약 잡기] 버튼 7) 놓친 요청에는 사유 적는 칸 — 다음에 왜 놓쳤는지 보려고 표가 주인공인 화면이다. 금액은 오른쪽 정렬, 상태 배지는 색만이 아니라 글자로도 구분되게 해줘.
  ```

### OW-03 · 현장 상세 - 공정 편집
- **소속 메뉴**: 현장 관리
- **뒤로가기**: 좌측 상단에 `‹ 현장 대시보드` — 누르면 `OW-01`로 간다
- **일정**: 2026-12-31 ~ 2027-01-05
- **기능정의**: 현장 머리(주소·평수·계약금액·진행률·담당 소장) · 탭(공정·자재·사진·정산) · 공정 탭의 편집 가능한 일정표(막대를 끌어 날짜 바꾸기) · 앞 공정을 밀면 뒤가 따라 밀리는 것 · 공정별 팀 배정과 일정 충돌 경고 · 공정 추가·지우기 · 실제 진행률 입력 · 손님에게 보이는 것과 안 보이는 것 갈라 두기 · 변경 이력
- **버튼 → 이동화면**:
  - `자재·원가` → `OW-04` 자재·원가
  - `청구·수금` → `OW-05` 기성 청구·수금
  - `일정 캘린더` → `OW-06` 일정 캘린더
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
- **생성 프롬프트**:

  ```
  업체가 한 현장의 공정을 편집하는 화면을 만들어줘. 1) 현장 머리 — 주소, 32평, 계약금액 3,596만원, 진행률 62%, 담당 소장. 오른쪽에 [손님 화면으로 보기] 2) 탭 — 공정 / 자재 / 사진 / 정산. ⚠ 탭과 몸통은 반드시 같은 상자 안에 두어야 탭이 실제로 먹는다 3) 공정 탭 — 편집 가능한 가로 막대 일정표. 막대를 끌면 시작·끝 날짜가 바뀌고, 앞 공정을 밀면 뒤 공정이 따라 밀린다. 밀린 결과 준공일이 바뀌면 위에 "준공 9/30 → 10/2" 경고 띠 4) 공정마다 오른쪽에 팀 배정 드롭다운 — 고른 팀이 그 기간에 다른 현장에 가 있으면 "김목수팀은 9/24 성수 현장에 있습니다" 경고 5) 공정 줄 끝에 진행률 입력칸(%)과 [공정 추가] [지우기] 6) 공정마다 눈 아이콘 — 손님 화면에 보일지 말지. 내부 공정은 숨긴다 7) 아래에 변경 이력 — 누가 언제 무엇을 바꿨는지 일정표가 가로로 길다. 공정 이름 열은 왼쪽 고정, 나머지만 가로 스크롤되게 해줘.
  ```

### OW-04 · 자재·원가
- **소속 메뉴**: 현장 관리
- **뒤로가기**: 좌측 상단에 `‹ 현장 대시보드` — 누르면 `OW-01`로 간다
- **일정**: 2027-01-06 ~ 2027-01-08
- **기능정의**: 현장별 자재 목록(공정·품명·규격·수량·견적 단가·실제 단가·차액) · 실제 단가를 넣으면 계산되는 차액과 마진 · 발주 상태 배지(발주 전·발주함·입고됨·반품) · 발주 안 한 것 수를 위에 · 납품 예정일과 늦어지는 것 표시 · 공정별 필터 · 원가 합계와 계약금액 대비 마진율 · 자재 추가와 거래처 · 발주서 내보내기
- **버튼 → 이동화면**:
  - `현장 상세` → `OW-03` 현장 상세 - 공정 편집
  - `청구·수금` → `OW-05` 기성 청구·수금
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 필터 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
  - 수량 — 누를 때마다 **숫자와 합계가 같이** 바뀝니다
- **생성 프롬프트**:

  ```
  현장의 자재와 원가를 관리하는 화면을 만들어줘. 1) 맨 위 숫자 카드 4개 — 자재 원가 1,842만 / 계약금액 3,596만 / 마진 1,754만 / 마진율 48.8%. 마진율은 아래 표 합계와 반드시 맞아야 한다 2) 경고 띠 — "발주 안 한 자재 4건· 그중 2건은 3일 안에 써야 합니다" 3) 공정 필터 칩 — 전체 / 철거 / 설비 / 전기 / 목공 / 타일 / 도배 / 마루 4) 자재 표 — 공정 / 품명 / 규격 / 수량 / 단위 / 견적 단가 / 실제 단가 / 차액 / 발주 상태 / 납품 예정일. 실제 단가 칸은 직접 넣을 수 있고, 넣으면 차액이 곧바로 계산되고 색으로 넘침·아낌이 갈린다 5) 발주 상태 드롭다운 — 발주 전 / 발주함 / 입고됨 / 반품. 바꾸면 위 경고 띠 숫자가 줄어든다 6) 납품 예정일이 지났는데 입고가 안 된 줄에는 빨간 꼬리표 7) 표 아래 합계 줄 — 견적 합계 / 실제 합계 / 차액 합계 8) 오른쪽 위 [자재 추가] [발주서 내보내기] 숫자 표가 빽빽한 화면이다. 금액은 오른쪽 정렬하고 자릿수를 맞춰줘. 합계 줄은 배경을 달리해 눈에 띄게.
  ```

### OW-05 · 기성 청구·수금
- **소속 메뉴**: 현장 관리
- **뒤로가기**: 좌측 상단에 `‹ 현장 대시보드` — 누르면 `OW-01`로 간다
- **일정**: 2027-01-11 ~ 2027-01-14
- **기능정의**: 미수금 총액과 이달 수금액을 맨 위에 · 현장별·회차별 청구 목록(계약금·착공금·중도금·잔금) · 회차마다 예정일·금액·상태(예정·청구함·수금 완료·연체) · 청구서 만들기와 문자로 보내기 · 입금 확인 체크 · 연체된 건 위로 올리고 며칠 늦었는지 · 세금계산서 발행 상태 · 현장별 필터 · 수금 예정 달력
- **버튼 → 이동화면**:
  - `현장 대시보드` → `OW-01` 현장 대시보드
  - `자재·원가` → `OW-04` 자재·원가
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 필터 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
- **생성 프롬프트**:

  ```
  기성(공정 진행에 따라 나누어 받는 돈)을 청구하고 수금을 확인하는 화면을 만들어줘. 1) 맨 위 숫자 카드 3개 — 미수금 3,820만원 / 이달 수금 1억 240만원 / 연체 2건 620만원. 아래 표 합계와 아귀가 맞아야 한다 2) 현장 필터 드롭다운과 상태 탭 — 전체 / 예정 / 청구함 / 수금 완료 / 연체 3) 청구 표 — 현장 / 회차 / 금액 / 예정일 / 청구일 / 입금일 / 상태 / 세금계산서. 회차는 계약금·착공금·중도금·잔금 4) 연체된 줄은 맨 위로 올리고 "7일 늦음" 꼬리표 5) 예정 상태인 줄에는 [청구서 만들기] 버튼 — 누르면 상태가 「청구함」으로 바뀌고 [문자로 보내기] 가 나타난다 6) 청구함 상태인 줄에는 [입금 확인] 체크 — 체크하면 「수금 완료」로 바뀌고 위 미수금 숫자가 실제로 줄어든다 7) 세금계산서 열 — 발행 전 / 발행함 배지와 [발행하기] 8) 오른쪽에 수금 예정 달력 — 이번 달 어느 날 얼마가 들어올 예정인지 돈을 다루는 표다. 숫자끼리 아귀를 맞추는 것이 가장 중요하다 — 위 카드 3개와 표 합계, 달력 합계가 모두 같아야 한다.
  ```

### OW-06 · 일정 캘린더
- **소속 메뉴**: 현장 관리
- **뒤로가기**: 좌측 상단에 `‹ 현장 대시보드` — 누르면 `OW-01`로 간다
- **일정**: 2027-01-15 ~ 2027-01-20
- **기능정의**: 여러 현장의 일정을 한 달력에 겹쳐 보기 · 현장별 색과 팀별 필터 · 일정을 끌어 다른 날로 옮기기 · 같은 팀이 두 곳에 잡히면 뜨는 겹침 경고 · 주 보기·월 보기 전환 · 날짜를 누르면 옆에 열리는 그 날 일정 목록 · 실측 방문·검수 방문도 함께 · 공휴일과 아파트 공사 금지일 표시 · 일정 추가
- **버튼 → 이동화면**:
  - `현장 상세` → `OW-03` 현장 상세 - 공정 편집
  - `현장 대시보드` → `OW-01` 현장 대시보드
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 필터 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
- **생성 프롬프트**:

  ```
  업체가 여러 현장 일정을 한 달력에서 보는 화면을 만들어줘. 1) 상단에 [주 보기] [월 보기] 전환과 오늘로 가기, [일정 추가] 2) 필터 줄 — 현장별 체크(색 동그라미와 함께) / 팀별 체크 (목공팀·타일팀·설비팀·도배팀·전기팀) 3) 달력 본문 — 날짜 칸마다 그 날 일정이 색 띠로 쌓인다. 띠에는 현장 줄임 이름과 공정, 팀 이름 4) 띠를 끌어 다른 날로 옮길 수 있고, 같은 팀이 같은 날 두 현장에 잡히면 빨간 경고 띠가 뜬다 — "타일팀이 9/24 에 두 현장에 잡혔습니다" 5) 실측 방문·준공 검수 방문은 다른 모양으로 구분(테두리만 있는 띠) 6) 공휴일과 아파트 공사 금지일(일요일·공휴일)은 칸 배경을 흐리게 7) 날짜를 누르면 오른쪽에 그 날 일정 목록 패널이 열린다 — 시각, 현장, 공정, 팀, 연락처 8) 아래에 이번 주 팀별 가동률 막대 — 목공팀 90% 처럼 달력 칸이 좁아지면 일정 띠가 뭉개진다. 한 칸에 셋까지만 보이고 넘으면 "+2건" 으로 접히게 해줘.
  ```

### OW-07 · 업체 정보·직원 관리
- **소속 메뉴**: 현장 관리
- **뒤로가기**: 좌측 상단에 `‹ 현장 대시보드` — 누르면 `OW-01`로 간다
- **일정**: 2027-01-21 ~ 2027-01-25
- **기능정의**: 업체 기본 정보(상호·사업자번호·대표·주소·전화)와 고치기 · 손님 화면에 보이는 소개 글과 대표 사진 · 직원 계정 목록과 권한 3단(대표·소장·경리) · 권한마다 볼 수 있는 화면 표 · 직원 부르기(문자 초대) · 거래처 목록(자재상·인력팀·연락처·거래 조건) · 알림 받을 사람 고르기(견적 요청·기성 청구·하자 접수 각각) · 사업자 서류 올리기(사업자등록증·통장사본) · 세금계산서 기본 정보
- **버튼 → 이동화면**:
  - `현장 대시보드` → `OW-01` 현장 대시보드
  - `자재·원가` → `OW-04` 자재·원가
- **생성 프롬프트**:

  ```
  시공 업체가 «제 회사 정보»를 다루는 화면을 만들어줘. ⚠ 이것은 손님용 「내 정보」와 «다른 화면»이다. 손님 것은 내 현장·저장한 견적·관심 분야를 다루고, 이것은 사업자 정보·직원·거래처를 다룬다. 둘을 한 화면에 섞지 마라. 1) 맨 위 업체 기본 정보 카드 — 상호, 사업자등록번호, 대표자, 주소, 대표 전화. 오른쪽에 [고치기]. 사업자등록번호는 한 번 넣으면 잠기고 「바꾸려면 문의해 주세요」라고 적는다 2) 손님에게 보이는 것 — 업체 소개 글(여러 줄), 대표 사진 자리, 「손님 화면에서 이렇게 보여요」 미리보기 조각 3) 직원 계정 표 — 이름, 연락처, 권한(대표·소장·경리), 마지막 접속, [권한 바꾸기]. 권한을 바꾸면 그 아래에 «무엇이 달라지는지» 한 줄로 뜬다 — 예: "경리로 바꾸면 청구·수금만 보이고 현장 사진은 안 보입니다" 4) 권한별로 볼 수 있는 화면 표 — 세로에 화면(현장 대시보드·견적 요청함· 현장 상세·자재 원가·청구 수금·일정), 가로에 권한 3단, 칸에 O/X 5) [직원 부르기] — 연락처를 넣으면 문자로 초대가 간다는 안내 6) 거래처 목록 — 자재상·인력팀 이름, 담당자, 연락처, 거래 조건(월말 결제 등), [거래처 추가]. 자재·원가 화면의 발주서에서 여기 거래처를 고른다고 적어 준다 7) 알림 받을 사람 — 견적 요청 / 기성 청구 / 하자 접수 각각에 대해 누구에게 갈지 고르는 줄 8) 사업자 서류 — 사업자등록증, 통장 사본 올리는 자리와 「세금계산서에 쓰입니다」 표가 많은 관리자 화면이다. 좁은 가운데 카드에 몰아넣지 말고 제 폭으로 펴 줘.
  ```

### AU-01 · 로그인
- **소속 메뉴**: 계정
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2027-01-26 ~ 2027-01-28
- **기능정의**: 휴대폰 번호 또는 아이디와 비밀번호 입력 · 비밀번호 보기 눈 아이콘 · 로그인 상태 유지 · 틀렸을 때 입력칸 아래에 뜨는 안내(어느 칸이 틀렸는지) · 간편 로그인 3종 · 회원가입과 비밀번호 찾기로 가는 길 · 업체 관리자로 들어가는 작은 길 · 로그인 없이 견적만 내보기
- **버튼 → 이동화면**:
  - `회원가입` → `AU-02` 회원가입
  - `비밀번호 찾기` → `AU-03` 비밀번호 찾기
  - `홈으로` → `HO-01` 홈
- **생성 프롬프트**:

  ```
  로그인 화면을 만들어줘. 1) 가운데 좁은 상자에 로고와 "다시 오셨네요" 2) 입력칸 두 개 — 휴대폰 번호 또는 아이디 / 비밀번호(눈 아이콘으로 보기) 3) 로그인 상태 유지 체크박스와 [비밀번호 찾기] 링크를 한 줄에 좌우로 4) [로그인] 버튼 — 폭을 꽉 채우게 5) 틀렸을 때 — 입력칸 아래에 빨간 글씨로 "비밀번호가 맞지 않아요"가 뜨고 그 칸 테두리 색이 바뀐다. 어느 칸이 문제인지 분명히 6) 가운데 "또는" 구분선과 간편 로그인 버튼 3개(카카오·네이버·구글) 7) 아래에 "처음 오셨나요? [회원가입]" 8) 맨 아래 작게 [업체 관리자 로그인] 과 [로그인 없이 견적만 내보기] 좁은 상자 하나만 있는 화면이다. 여백을 넉넉히 두고 버튼 폭을 꽉 채워줘.
  ```

### AU-02 · 회원가입
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `AU-01`로 간다
- **일정**: 2027-01-29 ~ 2027-02-02
- **기능정의**: 이름·휴대폰 번호와 인증번호 받기 · 남은 시간과 다시 받기 · 비밀번호와 조건 세 줄이 하나씩 채워지는 표시 · 비밀번호 확인 · 관심 시공 분야 고르기(선택) · 약관 동의(전체 동의와 항목별) · 필수를 다 해야 열리는 가입 버튼 · 이미 가입된 번호일 때 안내
- **버튼 → 이동화면**:
  - `가입 완료` → `AU-04` 내 정보
  - `로그인` → `AU-01` 로그인
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 선택 — 고른 개수를 **숫자로 보여주고**, 하나도 안 골랐으면 일괄 단추를 눌리지 않게 둡니다
- **생성 프롬프트**:

  ```
  회원가입 화면을 만들어줘. 1) 위에 단계 표시 — "1. 정보 입력 / 2. 인증 / 3. 완료" 중 지금 자리 2) 이름 입력칸 3) 휴대폰 번호 입력칸과 오른쪽에 [인증번호 받기]. 누르면 아래에 인증번호 칸과 "2:58" 남은 시간, [다시 받기] 가 나타난다 4) 비밀번호 입력칸과 아래에 조건 세 줄 — 8자 이상 / 영문과 숫자 섞기 / 특수문자 하나 이상. 조건을 채울 때마다 그 줄이 하나씩 표시가 바뀐다 5) 비밀번호 확인칸 — 다르면 바로 아래에 안내 6) 관심 시공 분야 칩(선택) — 아파트 전체 / 주방 / 욕실 / 상업공간 7) 약관 동의 — [전체 동의] 체크 하나와 그 아래 항목별 (이용약관 필수 / 개인정보 필수 / 마케팅 수신 선택). 각 줄에 [보기] 8) ⚠ 필수 항목을 다 채워야 [가입하기] 버튼이 열린다. button 요소로 만들고 실제로 열리는 장치를 붙여줘 9) 이미 가입된 번호를 넣으면 "이미 가입된 번호예요. [로그인하러 가기]" 단계가 많아 지루하다. 한 줄에 하나씩, 채워질 때마다 표시가 바뀌게 해줘.
  ```

### AU-03 · 비밀번호 찾기
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `AU-01`로 간다
- **일정**: 2027-02-03 ~ 2027-02-05
- **기능정의**: 찾는 방법 탭(휴대폰 인증·이메일) · 방법별 입력칸 · 인증번호 받기와 남은 시간 · 인증이 끝나면 나오는 새 비밀번호 입력 · 조건 세 줄 표시 · 바꾸고 나면 바로 로그인으로 · 가입한 적 없는 번호일 때 안내 · 그래도 안 되면 전화 상담
- **버튼 → 이동화면**:
  - `로그인` → `AU-01` 로그인
  - `회원가입` → `AU-02` 회원가입
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
- **생성 프롬프트**:

  ```
  비밀번호를 찾는 화면을 만들어줘. 1) 가운데 좁은 상자에 "비밀번호를 잊으셨나요?" 2) 찾는 방법 탭 — 휴대폰 인증 / 이메일. ⚠ 탭과 아래 입력칸은 같은 상자 안에 두어 탭이 실제로 먹게 3) 휴대폰 탭 — 이름, 휴대폰 번호, [인증번호 받기]. 누르면 인증번호 칸과 남은 시간이 나타난다 4) 인증이 끝나면 그 아래가 펼쳐진다 — 새 비밀번호, 확인, 조건 세 줄(8자 이상 / 영문·숫자 / 특수문자) 5) 가입한 적 없는 번호면 "가입된 정보가 없어요. [회원가입하러 가기]" 6) 맨 아래 작게 "그래도 안 되시면 1588-0000 로 전화 주세요" 단계가 위에서 아래로 하나씩 열리는 화면이다. 아직 안 열린 부분은 흐리게 두어줘.
  ```

### AU-04 · 내 정보
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `AU-01`로 간다
- **일정**: 2027-02-08 ~ 2027-02-10
- **기능정의**: 프로필 사진과 이름·연락처 고치기 · 내 현장 목록(진행 중·끝남)과 각각으로 가는 길 · 저장한 견적 목록 · 관심 시공 분야 칩 고치기 · 알림 설정 스위치(공사 진행·추가공사·청구·하자보수) · 알림 받을 방법(문자·카톡·앱) · 비밀번호 바꾸기 · 회원 탈퇴와 그 전에 알려 줄 것(보증 기간이 남았다는 것)
- **버튼 → 이동화면**:
  - `공사 진행` → `PR-01` 공정표
  - `저장한 견적 견주기` → `ES-05` 견적 견주기
  - `하자보수 접수` → `AS-01` 하자보수 접수
  - `홈으로` → `HO-01` 홈
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 알림 설정 — 누르면 **켜짐·꺼짐이 눈에 보이게** 바뀌고 그대로 남습니다
- **생성 프롬프트**:

  ```
  내 정보 화면을 만들어줘. 1) 위에 프로필 — 사진, 이름, 휴대폰 번호. 각각 [고치기] 2) "내 현장" 카드 목록 — 진행 중 1곳(주소, 진행률, [공사 진행 보기]), 끝난 곳 1개(주소, 준공일, 보증 남은 기간, [하자보수 접수]) 3) "저장한 견적" 목록 — 견적 이름, 총액, 유효기간, [보기] [지우기]. 유효기간이 지난 것은 흐리게 4) 관심 시공 분야 칩 — 누르면 켜고 꺼진다 5) 알림 설정 — 스위치 목록(공사 진행 알림 / 추가공사 요청 / 대금 청구 / 하자보수 처리). 스위치 아래에 무엇을 언제 보내는지 한 줄 설명 6) 알림 받을 방법 라디오 — 문자 / 카카오톡 / 앱 알림 7) [비밀번호 바꾸기] 8) 맨 아래 작게 [회원 탈퇴] — 누르면 "보증 기간이 13개월 남아 있어요. 탈퇴하시면 하자보수 접수가 어려워집니다" 확인이 먼저 뜬다 설정이 많은 화면이다. 덩어리를 카드로 갈라 두고 사이 간격을 일정하게 해줘.
  ```

## 5. 화면 이동 흐름 (FLOW)
- `홈`(HO-01) —[예상 견적 내기]→ `견적 조건 입력 - 단계별`(ES-01)
- `홈`(HO-01) —[시공사례 보기]→ `시공사례 목록`(CS-01)
- `홈`(HO-01) —[분야별 홈]→ `시공 분야 홈`(HO-02)
- `홈`(HO-01) —[비용 안내]→ `비용 안내`(HO-03)
- `시공 분야 홈`(HO-02) —[시공사례]→ `시공사례 목록`(CS-01)
- `시공 분야 홈`(HO-02) —[예상 견적 내기]→ `견적 조건 입력 - 단계별`(ES-01)
- `시공 분야 홈`(HO-02) —[비용 안내]→ `비용 안내`(HO-03)
- `비용 안내`(HO-03) —[예상 견적 내기]→ `견적 조건 입력 - 단계별`(ES-01)
- `비용 안내`(HO-03) —[실측 예약]→ `방문 실측 예약`(VS-01)
- `비용 안내`(HO-03) —[자주 묻는 질문]→ `홈`(HO-01)
- `시공사례 목록`(CS-01) —[사례 상세]→ `시공사례 상세`(CS-02)
- `시공사례 목록`(CS-01) —[결과 없음]→ `시공사례 - 결과 없음`(CS-03)
- `시공사례 목록`(CS-01) —[자재 둘러보기]→ `자재·마감 둘러보기`(CS-04)
- `시공사례 상세`(CS-02) —[이 사례처럼 견적 내기]→ `견적 조건 입력 - 단계별`(ES-01)
- `시공사례 상세`(CS-02) —[실측 예약]→ `방문 실측 예약`(VS-01)
- `시공사례 상세`(CS-02) —[목록으로]→ `시공사례 목록`(CS-01)
- `시공사례 - 결과 없음`(CS-03) —[조건 지우고 보기]→ `시공사례 목록`(CS-01)
- `시공사례 - 결과 없음`(CS-03) —[상담 예약]→ `방문 실측 예약`(VS-01)
- `시공사례 - 결과 없음`(CS-03) —[예상 견적 내기]→ `견적 조건 입력 - 단계별`(ES-01)
- `자재·마감 둘러보기`(CS-04) —[고른 자재로 견적 내기]→ `견적 조건 입력 - 단계별`(ES-01)
- `자재·마감 둘러보기`(CS-04) —[시공사례]→ `시공사례 목록`(CS-01)
- `견적 조건 입력 - 단계별`(ES-01) —[견적 결과 보기]→ `견적 결과`(ES-02)
- `견적 조건 입력 - 단계별`(ES-01) —[자재 둘러보기]→ `자재·마감 둘러보기`(CS-04)
- `견적 결과`(ES-02) —[항목 상세]→ `견적 항목 상세`(ES-03)
- `견적 결과`(ES-02) —[실측 예약]→ `방문 실측 예약`(VS-01)
- `견적 결과`(ES-02) —[견적 저장 완료]→ `견적 저장 완료`(ES-04)
- `견적 항목 상세`(ES-03) —[견적 결과로]→ `견적 결과`(ES-02)
- `견적 항목 상세`(ES-03) —[실측 예약]→ `방문 실측 예약`(VS-01)
- `견적 저장 완료`(ES-04) —[실측 예약]→ `방문 실측 예약`(VS-01)
- `견적 저장 완료`(ES-04) —[다른 견적과 견주기]→ `견적 견주기`(ES-05)
- `견적 저장 완료`(ES-04) —[자재 둘러보기]→ `자재·마감 둘러보기`(CS-04)
- `견적 저장 완료`(ES-04) —[홈으로]→ `홈`(HO-01)
- `견적 견주기`(ES-05) —[실측 예약]→ `방문 실측 예약`(VS-01)
- `견적 견주기`(ES-05) —[견적 결과]→ `견적 결과`(ES-02)
- `방문 실측 예약`(VS-01) —[예약 확인]→ `예약 내용 확인`(VS-02)
- `방문 실측 예약`(VS-01) —[견적 결과]→ `견적 결과`(ES-02)
- `예약 내용 확인`(VS-02) —[예약 완료]→ `예약 완료`(VS-03)
- `예약 내용 확인`(VS-02) —[예약 화면으로]→ `방문 실측 예약`(VS-01)
- `예약 완료`(VS-03) —[예약 변경·취소]→ `예약 변경·취소`(VS-04)
- `예약 완료`(VS-03) —[시공사례]→ `시공사례 목록`(CS-01)
- `예약 완료`(VS-03) —[홈으로]→ `홈`(HO-01)
- `예약 변경·취소`(VS-04) —[예약 완료]→ `예약 완료`(VS-03)
- `예약 변경·취소`(VS-04) —[홈으로]→ `홈`(HO-01)
- `계약서 확인·서명`(CT-01) —[계약금 결제]→ `계약금 결제`(CT-02)
- `계약서 확인·서명`(CT-01) —[견적 결과]→ `견적 결과`(ES-02)
- `계약금 결제`(CT-02) —[결제 완료]→ `결제 완료 - 계약 성사`(CT-03)
- `계약금 결제`(CT-02) —[결제 실패]→ `결제 실패`(CT-04)
- `결제 완료 - 계약 성사`(CT-03) —[공사 진행 보기]→ `공정표`(PR-01)
- `결제 완료 - 계약 성사`(CT-03) —[홈으로]→ `홈`(HO-01)
- `결제 실패`(CT-04) —[다시 결제하기]→ `계약금 결제`(CT-02)
- `결제 실패`(CT-04) —[계약서로]→ `계약서 확인·서명`(CT-01)
- `공정표`(PR-01) —[오늘 현장]→ `오늘 현장`(PR-02)
- `공정표`(PR-01) —[사진 일지]→ `현장 사진 일지`(PR-03)
- `공정표`(PR-01) —[추가공사 승인]→ `추가공사 변경 견적 승인`(PR-04)
- `공정표`(PR-01) —[준공 검수]→ `준공 검수 체크리스트`(PR-05)
- `공정표`(PR-01) —[진행 중 공사 없음]→ `공사 진행 - 진행 중 없음`(PR-06)
- `오늘 현장`(PR-02) —[공정표]→ `공정표`(PR-01)
- `오늘 현장`(PR-02) —[사진 일지]→ `현장 사진 일지`(PR-03)
- `오늘 현장`(PR-02) —[자재 고르기]→ `자재·마감 둘러보기`(CS-04)
- `현장 사진 일지`(PR-03) —[오늘 현장]→ `오늘 현장`(PR-02)
- `현장 사진 일지`(PR-03) —[공정표]→ `공정표`(PR-01)
- `추가공사 변경 견적 승인`(PR-04) —[공정표]→ `공정표`(PR-01)
- `추가공사 변경 견적 승인`(PR-04) —[오늘 현장]→ `오늘 현장`(PR-02)
- `준공 검수 체크리스트`(PR-05) —[하자보수 접수]→ `하자보수 접수`(AS-01)
- `준공 검수 체크리스트`(PR-05) —[공정표]→ `공정표`(PR-01)
- `공사 진행 - 진행 중 없음`(PR-06) —[예상 견적 내기]→ `견적 조건 입력 - 단계별`(ES-01)
- `공사 진행 - 진행 중 없음`(PR-06) —[하자보수 접수]→ `하자보수 접수`(AS-01)
- `공사 진행 - 진행 중 없음`(PR-06) —[홈으로]→ `홈`(HO-01)
- `하자보수 접수`(AS-01) —[접수 내역]→ `하자보수 접수 내역`(AS-02)
- `하자보수 접수`(AS-01) —[공사 진행]→ `공정표`(PR-01)
- `하자보수 접수 내역`(AS-02) —[처리 현황]→ `하자보수 처리 현황`(AS-03)
- `하자보수 접수 내역`(AS-02) —[새로 접수]→ `하자보수 접수`(AS-01)
- `하자보수 처리 현황`(AS-03) —[접수 내역]→ `하자보수 접수 내역`(AS-02)
- `하자보수 처리 현황`(AS-03) —[새로 접수]→ `하자보수 접수`(AS-01)
- `현장 대시보드`(OW-01) —[견적 요청함]→ `견적 요청함`(OW-02)
- `현장 대시보드`(OW-01) —[현장 상세]→ `현장 상세 - 공정 편집`(OW-03)
- `현장 대시보드`(OW-01) —[청구·수금]→ `기성 청구·수금`(OW-05)
- `견적 요청함`(OW-02) —[현장 대시보드]→ `현장 대시보드`(OW-01)
- `견적 요청함`(OW-02) —[현장 상세]→ `현장 상세 - 공정 편집`(OW-03)
- `현장 상세 - 공정 편집`(OW-03) —[자재·원가]→ `자재·원가`(OW-04)
- `현장 상세 - 공정 편집`(OW-03) —[청구·수금]→ `기성 청구·수금`(OW-05)
- `현장 상세 - 공정 편집`(OW-03) —[일정 캘린더]→ `일정 캘린더`(OW-06)
- `자재·원가`(OW-04) —[현장 상세]→ `현장 상세 - 공정 편집`(OW-03)
- `자재·원가`(OW-04) —[청구·수금]→ `기성 청구·수금`(OW-05)
- `기성 청구·수금`(OW-05) —[현장 대시보드]→ `현장 대시보드`(OW-01)
- `기성 청구·수금`(OW-05) —[자재·원가]→ `자재·원가`(OW-04)
- `일정 캘린더`(OW-06) —[현장 상세]→ `현장 상세 - 공정 편집`(OW-03)
- `일정 캘린더`(OW-06) —[현장 대시보드]→ `현장 대시보드`(OW-01)
- `업체 정보·직원 관리`(OW-07) —[현장 대시보드]→ `현장 대시보드`(OW-01)
- `업체 정보·직원 관리`(OW-07) —[자재·원가]→ `자재·원가`(OW-04)
- `로그인`(AU-01) —[회원가입]→ `회원가입`(AU-02)
- `로그인`(AU-01) —[비밀번호 찾기]→ `비밀번호 찾기`(AU-03)
- `로그인`(AU-01) —[홈으로]→ `홈`(HO-01)
- `회원가입`(AU-02) —[가입 완료]→ `내 정보`(AU-04)
- `회원가입`(AU-02) —[로그인]→ `로그인`(AU-01)
- `비밀번호 찾기`(AU-03) —[로그인]→ `로그인`(AU-01)
- `비밀번호 찾기`(AU-03) —[회원가입]→ `회원가입`(AU-02)
- `내 정보`(AU-04) —[공사 진행]→ `공정표`(PR-01)
- `내 정보`(AU-04) —[저장한 견적 견주기]→ `견적 견주기`(ES-05)
- `내 정보`(AU-04) —[하자보수 접수]→ `하자보수 접수`(AS-01)
- `내 정보`(AU-04) —[홈으로]→ `홈`(HO-01)

## 6. 빌드 가이드
### 구조
- 각 **화면ID**를 하나의 라우트/페이지로 만드세요 (예: `PCHOME0110` → `/home`).
- 화면명은 페이지 제목·컴포넌트 이름의 기준으로 사용하세요.
- **2장 공통 요소**(헤더·GNB·푸터)를 공용 레이아웃으로 만들고 모든 화면이 감싸도록 하세요.
- **쓰는 사람이 다른 메뉴는 헤더를 나누세요.** 손님이 쓰는 메뉴(홈·시공사례·견적·실측 예약)와 운영자가 쓰는 메뉴(현장 관리)를 **한 헤더에 나란히 놓지 마세요.** 둘은 사실상 다른 사이트입니다.
  · 손님 영역과 운영자 영역을 각각 다른 레이아웃(헤더·사이드바)으로 만들고, 오갈 때는 **따로 마련한 진입점** 하나로만 이동하세요(예: 우측 상단 `현장 관리 화면으로`, 운영자 화면에서는 `고객 화면 보기`).
  · 운영자 영역은 보통 좌측 사이드바 + 상단 바가 맞습니다. 손님 영역의 검색·카테고리 내비를 그대로 쓰지 마세요.
- **버튼 → 이동화면** 관계를 링크/네비게이션으로 연결하세요.
- **4장 각 화면의 `눌렀을 때 (이 화면 안에서 끝남)` 항목을 그대로 만드세요.** 적혀 있는 것은 빠짐없이 실제로 동작해야 합니다 — 누르면 값이 바뀌고, 다시 누르면 되돌아옵니다.
  · 그 항목에 없더라도 **누를 수 있게 생긴 것은 전부 눌리게** 만드세요. 적힌 것은 최소한이지 전부가 아닙니다.
- **누를 수 있게 보이는 것은 전부 눌리게 만드세요.** 두 종류로 나눠 처리합니다.
  · **화면 안에서 끝나는 조작은 진짜로 동작해야 합니다** — 탭 전환, 좌측 카테고리·필터 선택, 목록에서 항목 고르기(디자이너·옵션·시간), 펼치기·접기, 캘린더 이전/다음 달, 수량 ±, 체크·토글. 자바스크립트로 실제 상태를 바꾸고 화면을 다시 그리세요. 이걸 안내 문구로 때우지 마세요.
  · **서버가 있어야 하는 것만** 짧은 안내(토스트)로 대신합니다 — 저장·삭제·발송·결제·전화 걸기·이의 신청 등.
  · **이런 것은 토스트로 때우지 마세요** — `모두 보기`(펼치기), `선택`·`적용`(고른 상태로 바꾸기), `추가`(줄 하나 늘리기), `닫기`·`✕`(정말 닫기), `도움이 됐어요`(숫자 올리기), 탭·필터·정렬. 전부 화면 안에서 끝나는 일이라 실제로 되어야 합니다.
  · **모달·팝업은 뜨는 것만으로 끝이 아닙니다.** `취소`와 `✕`는 정말 닫히고, `확인`은 눌렀을 때 무엇이 달라지는지 보여야 합니다(다음 화면으로 가거나, 목록이 바뀌거나, 안내가 뜨거나). 닫히지 않는 팝업은 갇힌 화면이 됩니다.
  · 아무 것도 하지 않는 `<button>`을 남기지 마세요. 만들고 나서 **버튼을 하나씩 눌러 보고**, 반응이 없는 것이 있으면 위 둘 중 하나로 채우세요.
- **뒤로가기는 화면마다 정해져 있습니다. 4장 각 화면 스펙의 `뒤로가기` 항목을 그대로 만드세요.** 31개 화면에 붙고, 나머지 9개(메뉴 첫 화면)에는 없습니다. 좌측 상단에 `‹ 상위 화면 이름`과 현재 위치(예: `매장 탐색 › 매장 상세`)를 둡니다.
  · 여러 단계짜리 흐름(1→2→3)은 **이전 단계로 돌아갈 수 있게** 하세요. 되돌아갔을 때 앞서 고른 값이 남아 있어야 합니다.
  · **한 묶음으로 나란히 보여주는 것은 동일 계위입니다.** 탭 줄에 걸린 화면들, 같은 사이드바에 나란히 올라온 화면들이 그렇습니다. 그 안을 오가는 길이 이미 탭·사이드바이므로 뒤로가기를 겹쳐 두지 마세요 — 같은 줄에 있는 화면인데 어떤 건 있고 어떤 건 없어집니다.
  · 빈 화면·오류·마감처럼 막다른 화면에는 **다음에 할 일 버튼**을 반드시 하나 두세요(예: `목록으로`, `다시 시도`, `둘러보기`).
  · **전체 화면 목록으로 돌아가는 길**을 **146개 화면 «전부»에** 두세요 — 오른쪽 아래에 떠 있는 `화면 목록` 단추 하나면 됩니다. 누르면 `index.html`(전체 화면 목록)으로 갑니다. 접힌 패널 안에 `화면 정보` 같은 이름으로 넣으면 아무도 못 찾습니다.
  · 다 만든 뒤 **화면을 하나씩 열어 뒤로가기가 있는지 세어 보세요.** 하나라도 빠져 있으면 채웁니다.
### 화면 구현
- 각 화면의 **생성 프롬프트**를 그 화면 구현 지시로 그대로 사용하세요.
- 반복 UI(카드·리스트 행·폼·버튼)는 **공용 컴포넌트**로 만들어 재사용하세요.
- 필요하면 상태 화면(로딩·빈 결과·에러)도 함께 만드세요.
### 디자인
- **디자인 프리셋 파일을 함께 넣었다면 그것이 우선입니다.** 1장의 디자인 컨셉이나 화면별 프롬프트에 다른 색·톤이 적혀 있어도 프리셋을 따르세요. 충돌이 아니라 **프리셋이 정답**입니다.
- 프리셋의 **레이아웃 골격**(히어로·목록·내비·상세를 어떻게 놓을지)을 먼저 잡고 색을 입히세요. 색만 맞추면 어떤 테마로 만들어도 같은 화면이 나옵니다.
- 프리셋이 없다면 1장의 **디자인 컨셉**을 전역 스타일(색·톤·라운드·여백)로 토큰화해 일관되게 적용하세요.
- **글꼴은 프리셋이 없어도 한 벌로 고정하세요.** 기본은 `Paperlogy`이고, 못 불러오면 `Pretendard` → `Noto Sans KR` 순으로 물러납니다. CSS로는 `font-family: Paperlogy, Pretendard, 'Noto Sans KR', sans-serif` 한 줄이면 됩니다.
  · **글꼴은 한 벌만 씁니다.** 제목과 본문에 서로 다른 글꼴을 쓰지 마세요 — 두 벌이 되는 순간 어디에 무엇을 쓸지가 화면마다 갈립니다. 위계는 글꼴이 아니라 **크기와 굵기**로 만듭니다.
  · 굵기는 **400·600·700 셋만**, 크기는 본문 15 · 카드 제목 18 · 섹션 제목 22 · 페이지 제목 32을 기준으로 잡으세요.

#### 좁은 화면에서 넘치지 않게

폰 폭(**390px**)에서 **페이지가 좌우로 스크롤되면 안 됩니다.**
표·그림처럼 일부러 옆으로 넘기는 것은 «자기 상자 안에서만» 넘깁니다.

**왜 적어 두나** — 열 수(1024에서 2열, 720에서 1열)는 다들 맞추는데, 가로로 밀리는지는 잘 안 봅니다.
그리고 미는 것은 대개 표가 아닙니다. **표 밑의 페이지 번호 줄**과 「다음 프로그램 ›」 같은
이동 단추가 한 줄로 늘어서면서 밉니다 — 표는 감쌌는데 그 옆은 안 감싸기 때문입니다.

- 폰 폭(390px)에서 **페이지가 좌우로 스크롤되면 안 됩니다.** 표·그림처럼 일부러 옆으로 넘기는 것은 자기 상자 안에서만 넘깁니다.
- **페이지 번호 줄**은 줄바꿈하거나(`flex-wrap: wrap`) 자기 상자 안에서 옆으로 돌립니다(`overflow-x: auto`). 쪽수가 늘면 그만큼 길어지므로 한 줄로 두면 반드시 넘칩니다.
- **「이전 · 다음」 같은 이동 단추**는 줄바꿈을 허용하고, 글자가 길면 줄여 적습니다(`다음 프로그램 ›` → `다음 ›`).
- **표**는 `overflow-x: auto` 상자로 감쌉니다. 표 자체를 좁히지 않습니다 — 칸 글자가 세로로 접혀 읽을 수 없게 됩니다.
- **탭 줄·필터 줄**도 같습니다. 개수가 늘어나는 줄은 전부 줄바꿈이나 상자 안 스크롤 중 하나를 씁니다.
- ⛔ 옆으로 흘려보낼 때는 **다른 축을 반드시 막습니다** — `overflow: auto hidden`. `overflow-x` 만 열면 브라우저가 세로도 함께 열어, 테두리 1px 때문에 세로 스크롤바가 생깁니다.
- ⛔ **푸터는 화면 바닥에 붙입니다.** 내용이 짧은 화면(완료·빈 목록·오류)에서 푸터가 화면 가운데 떠 있으면 «덜 만들어진 화면»으로 보입니다. `body` 를 세로 flex 로 두고 푸터에 `margin-top: auto` 를 줍니다.
- ⛔ **`[hidden]` 을 쓸 때는 `[hidden] { display: none !important; }` 을 반드시 함께 넣습니다.** `[hidden]` 은 브라우저 기본값이라 `.row { display: flex }` 같은 규칙 하나에도 집니다. 그러면 «거르는 단추를 눌러도 목록이 안 줄어드는» 화면이 됩니다 — 숫자 안내만 바뀌고 목록은 그대로라, 눌러 보지 않으면 안 보입니다.
- ⛔ **그리드·플렉스 칸의 자식에는 `min-width: 0` 을 줍니다.** 기본값이 `auto` 라, 칸을 340px 로 정해도 내용이 그보다 넓으면 칸 밖으로 삐져나와 옆 칸을 덮습니다.
- ⛔ **여백을 지우는 클래스(`flush` 등)와 음수 마진을 «같이» 쓰지 않습니다.** 상쇄할 여백이 이미 없는데 음수 마진을 걸면 그만큼 넓어져 칸 밖으로 나갑니다.

```css
/* 개수가 늘어나는 줄은 접거나, 자기 상자 안에서 돌린다 */
.pager { display: flex; flex-wrap: wrap; justify-content: center; gap: 4px; }
.tabs, .filters { display: flex; flex-wrap: wrap; gap: 8px; }
.tabs-scroll { overflow: auto hidden; }  /* 탭을 옆으로 흘릴 때도 마찬가지 */

/* 줄바꿈 대신 «옆으로 흘려보낼» 때는 반드시 두 축을 다 적는다 */
.scroll-x { overflow: auto hidden; }   /* overflow-x 만 쓰면 세로 스크롤바가 생긴다 */

/* 표는 좁히지 말고 감싼다 — 좁히면 칸 글자가 세로로 접혀 못 읽는다 */
.table-wrap { overflow: auto hidden; }   /* x 만 열면 세로 스크롤바가 딸려 온다 */
.table-wrap table { min-width: max-content; }

/* 좌우로 벌리는 줄에 긴 단추가 들어오면 내려보낸다 */
.row-between { display: flex; flex-wrap: wrap; align-items: center; justify-content: space-between; gap: 12px; }

/* 푸터는 «화면 바닥»에 붙인다 — 짧은 화면에서 가운데 떠 있으면 안 만들어진 것처럼 보인다 */
body { min-height: 100dvh; display: flex; flex-direction: column; }
body > main { flex: 1 0 auto; }
body > footer { margin-top: auto; }

/* ⛔⛔ 거르기가 목록을 못 줄이는 사고는 거의 다 여기서 난다.
   [hidden] 은 «브라우저 기본값»이라 .row{display:flex} 한 줄에도 진다.
   숫자 안내는 바뀌는데 목록만 그대로인 화면이 만들어진다. */
[hidden] { display: none !important; }

/* ⛔ 위 푸터 규칙은 «body 에만» 씁니다. 감싸개(.app 같은 것)에 옮겨 붙이지 마세요.
   사이드바형(감싸개가 grid 2열)에 flex-direction:column 을 덮었더니
   사이드바가 화면 폭을 다 먹고 본문이 그 아래로 밀렸습니다(2026-08-19에 실제로 그랬습니다).
   감싸개는 «제 짜임 그대로» 둡니다 — 사이드바형이면 grid 2열, 상단바형이면 세로 flex.
   짜임이 다른 화면 두 가지를 꼭 같이 열어 보세요. 한쪽에서 되는 것이 다른 쪽을 무너뜨립니다. */

/* 사진 위에 얹는 단추(카드 위 찜 하트·빠른 담기)는 사진에 덮여 안 눌립니다.
   «얹은 단추에만» 아래 둘을 주세요 — 줄 안에 서는 단추에 주면 자리가 튑니다. */
.card { position: relative; }              /* 얹을 자리의 기준점 */
.card > .overlay-btn { position: absolute; z-index: 2; }

/* 그리드·플렉스 자식은 min-width 를 0 으로 — 안 그러면 칸보다 넓은 내용이 삐져나온다 */
.split-r > *, .split-l > *, .app > * { min-width: 0; }
```

#### 읽기 폭 — 글을 한 단으로 좁힐 때

글이 주인공인 자리(상세 본문·안내·약관)는 **최대 760px** 로 좁히고 가운데 정렬합니다.
콘텐츠 영역(위의 최대 폭) **안에서** 좁히는 것입니다 — 바깥 폭을 바꾸는 게 아닙니다.

**왜 좁히나** — 한 줄이 길면 다음 줄 첫 글자를 눈이 못 찾습니다. 1440px 폭에 본문을 꽉 채우면
한 줄에 90자가 넘어가고, 읽는 사람은 두세 줄마다 같은 줄을 다시 읽게 됩니다.

- **이 값 하나만 씁니다.** 화면마다 680·720·760 처럼 다르게 잡지 마세요 — 저희도 그렇게 적혀 있었고,
  같은 팩 안의 상세 화면끼리 본문 폭이 달라 서로 다른 사이트처럼 보였습니다.
- **카드 격자에는 쓰지 않습니다.** 여기는 «글»을 좁히는 값입니다. 카드를 몇 열로 놓을지는 레이아웃이 정합니다.
- 사진·표·코드처럼 넓어야 읽히는 것은 이 폭을 넘겨도 됩니다. 좁히는 건 글줄입니다.

#### 줄어드는 지점 — 두 곳만 씁니다

화면이 좁아질 때 칸이 줄어드는 지점은 **1024px** 와 **720px**, 이 둘뿐입니다.
1024 은 태블릿 가로, 720 은 세로입니다. 720px 아래에서는 좌우 여백도 16px 로 줄입니다.

- **여기 없는 지점을 새로 만들지 마세요.** 560·860·520 처럼 지어낸 지점이 섞이면, 같은 팩으로 만든 화면끼리 줄어드는 자리가 달라 서로 다른 사이트처럼 보입니다.
- 더 잘게 나눠도 사람 눈에는 안 보입니다. 대신 **지킬 곳만 늘어납니다.**
- 두 지점에서 열이 어떻게 줄어드는지는 위 레이아웃 표에 적혀 있습니다(예: 3열 → 2열 → 1열).

#### 격자 — 카드를 늘어놓는 자리

카드·타일을 격자로 늘어놓을 때 **좌우와 위아래를 다른 값으로** 벌립니다.

| 자리 | 값 |
| --- | --- |
| **카드와 카드 사이** (좌우) | 위 눈금의 「카드와 카드 사이 (가로)」 |
| **줄과 줄 사이** (위아래) | 위 눈금의 「덩어리와 덩어리 사이」 |

**같은 값을 쓰면 줄이 안 갈립니다.** 카드에 상자가 있을 때는 테두리가 경계를 만들어 줬지만,
상자를 걷어낸 뒤로는 **위 카드의 가격과 아래 카드의 사진이 붙어** 한 덩어리로 읽힙니다.
위아래를 더 벌려야 "여기까지가 한 줄"이 보입니다.

| 나누는 것 | 정하는 곳 |
| --- | --- |
| 몇 열로 나눌지 | 레이아웃 프리셋 |
| **사이를 얼마나 벌릴지** | **위 간격 눈금 (좌우·위아래 따로)** |
| 카드가 어떻게 생겼는지 | 카드 모양 |

- **레이아웃 프리셋이 「넓게」라고 해도 값을 새로 만들지 마세요.** 레이아웃은 *몇 열로 무엇을 어디에*를
  말하는 층이고, *얼마나 벌릴지*는 이 눈금이 정합니다. 실제로 그 한 줄 때문에 40px을 쓴 격자가 있었고,
  같은 화면의 다른 격자는 16px이라 두 값이 나란히 놓였습니다.

**간격은 변수로 못 박고, 자리마다 그것만 씁니다.** CSS 맨 위에 이걸 그대로 붙여 넣으세요.

```css
:root{
  --wrap: 1440px;      /* 콘텐츠 폭 — 헤더·본문·푸터·고정바가 모두 이 값을 쓴다 */
  --pad-x: 24px;    /* 좌우 여백 */
  --card-gap:   16px;     /* 카드와 카드 사이 — 좌우 */
  --card-gap-y: 28px;     /* 카드 줄과 줄 사이 — 위아래 */
  --side-w:     260px;               /* 사이드바 폭 (사이드바 뼈대에서만) */
  --side-gap:   32px;                /* 사이드바와 본문 사이 */
  --row-gap:    14px;                /* 가로 행 카드 안: 썸네일과 글자 사이 */

  --card-pad:   24px;                /* 카드 안쪽 여백 */
  --row-h:      48px;                /* 표 한 줄 높이 — 안 정하면 줄마다 들쭉날쭉해진다 */
  --btn-gap:    8px;                 /* 나란히 놓은 버튼 사이 */
}
@media (max-width: 720px){
  :root{ --pad-x: 16px; --card-gap: 12px; }
}
.wrap { max-width: var(--wrap); margin-inline: auto; padding-inline: var(--pad-x); }
.cards{ display: grid; column-gap: var(--card-gap); row-gap: var(--card-gap-y); }

/* 지표 줄 — 화면 맨 위에 숫자 카드를 나란히 놓는 자리.
   카드 격자(.cards)와 다른 물건이므로 .cards 를 쓰지 않는다. 지표는 넷이 기본이다. */
.stats{ display: grid; grid-template-columns: repeat(4, 1fr);
        column-gap: var(--card-gap); row-gap: var(--card-gap-y); }
@media (max-width: 1024px){ .stats{ grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 720px){ .stats{ grid-template-columns: 1fr; } }

/* 모자이크 — 홈 첫 화면에 「큰 1장 + 작은 4장」을 놓는 자리.
   첫 장이 2칸×2줄, 나머지 넷이 옆을 채운다.
   어느 것을 크게 할지 class 로 정하지 않는다 — 맨 앞에 둔 것이 큰 것이다. */
.mosaic{ display: grid; grid-template-columns: repeat(4, 1fr);
         column-gap: var(--card-gap); row-gap: var(--card-gap-y); }
.mosaic > :first-child{ grid-column: span 2; grid-row: span 2;
                        display: flex; flex-direction: column; }
/* 큰 칸의 사진은 카드 비율을 따르지 않는다 — 두 줄 높이와 안 맞아 제목이 밀려난다.
   큰 칸에서는 사진이 남는 자리를 채운다. */
.mosaic > :first-child > .thumb{ aspect-ratio: auto; flex: 1; min-height: 0; }
@media (max-width: 1024px){
  .mosaic{ grid-template-columns: repeat(2, 1fr); }
  .mosaic > :first-child{ grid-row: auto; }   /* 2열에서 세로로 겹치면 빈칸이 생긴다 */
}
@media (max-width: 720px){
  .mosaic{ grid-template-columns: 1fr; }
  .mosaic > :first-child{ grid-column: auto; }
}

/* 상세 화면의 대표 사진 — 카드 비율을 그대로 쓰지 않는다.
   4:3 을 본문 폭 1392px 에 적용하면 세로가 1044px 이 되어 노트북 화면을 통째로 덮는다.
   가로로 눕히고(16:9) 세로를 420px 로 막는다 — 사진 아래 내용이 같이 보이는 선이다. */
.detail-img{ aspect-ratio: 16 / 9; max-height: 420px; width: 100%;
             border-radius: 12px; overflow: hidden; }
.detail-img > img{ width: 100%; height: 100%; object-fit: cover; display: block; }

/* 간격은 반드시 이 변수로 쓴다. gap:16px 처럼 숫자를 직접 적으면 자리마다 값이 갈린다. */
```

- **`--card-pad`·`--row-h`·`--btn-gap` 도 꼭 쓰세요.** 카드 안쪽 여백, 표 한 줄 높이,
  버튼 사이 간격입니다. 이 셋을 안 정해주면 화면마다 다르게 나옵니다 — 저희도 겪었습니다.
- **`.stats`·`.mosaic`·`.detail-img` 는 카드 격자와 다른 자리입니다.** 지표 숫자 줄,
  홈의 큰 사진 한 장 + 작은 넷, 상세 화면의 대표 사진. 카드 규칙을 여기 갖다 쓰면
  사진이 화면을 다 덮습니다.

- **격자에 `gap: 20px` 처럼 숫자를 직접 쓰지 마세요.** 한 번 쓰는 순간 그 자리만 달라지고,
  나중에 값을 바꿀 때 그 자리만 안 바뀝니다. 실제로 한 사이트에서 좌우가 3·8·16·20·24px
  다섯 가지로 갈라진 적이 있습니다 — 전부 `gap:` 에 숫자를 적어서 생긴 일입니다.
- **`gap` 한 줄로 합치지도 마세요.** 좌우와 위아래는 값이 다르므로 `column-gap`·`row-gap` 둘로 씁니다.
- **이름도 이대로 씁니다.** `--grid-x`·`--gap-col` 처럼 제 나름의 이름을 지으면, 나중에
  다른 사람이 볼 때 그게 같은 자리인지 알 수가 없습니다. 실제로 같은 규칙을 준 두 사이트가
  하나는 `--grid-x/--grid-y`, 하나는 `--gap-col/--gap-row` 를 써서 값이 따로 놀았습니다.
- 값을 바꾸고 싶으면 **`:root` 의 두 줄만** 고칩니다. 그러면 화면 전체가 한 번에 따라옵니다.
- 히어로의 좌우 2단처럼 **카드 격자가 아닌 자리**는 이 규칙 밖입니다. 거기는 화면 뼈대라 레이아웃이 정합니다.

#### 박스형 콘텐츠 — 몇 개를 늘어놓고 사진은 어떤 비율로

사진이 든 카드를 격자로 늘어놓을 때, **몇 개를 놓느냐가 사진 비율을 정합니다.**

| 한 줄에 | 사진 비율 | 왜 |
| --- | --- | --- |
| **2개** | **4:3** | 칸이 넓으니 가로로 넓은 사진 |
| **3개** | **4:3** | 위와 같음 |
| **4개** | **1:1** | 칸이 좁다. 정사각이 가장 얌전하다 |

4개짜리를 세로로 긴 3:4로 뒀더니 **카드 한 줄이 화면을 다 먹었습니다.** 칸이 좁을수록 세로로
길게 두고 싶어지지만, 그러면 한 줄만 보이고 다음 단락이 안 보입니다. 좁은 칸에는 정사각입니다.

- **한 줄에 하나만 두지 않습니다.** 마지막 줄에도 하나만 남기지 마세요 —
  **항목 수를 열 수에 맞춰** 3열이면 3·6·9개, 4열이면 4·8개로 둡니다.
- **한 사이트에서 카드 사진 비율은 하나입니다.** 위 표의 짝(2·3열↔4:3, 4열↔1:1) 밖의 비율은 만들지 마세요.
  같은 홈에 세로 카드와 16:9가 섞여 있어서 **사는 사람이 사진을 두 벌 준비해야 했던** 적이 있습니다.
- **배너·차트·영상·프로필은 이 규칙 밖입니다.** 가로로 긴 배너·영상(16:9)·프로필(1:1)은 제 나름의 비율이 있습니다.
- 상품 사진처럼 **정사각이 업계 표준인 분야**는 1:1을 써도 됩니다. 다만 그때도 한 사이트 안에서는 하나로 갑니다.
- **가로 배너 한 장으로 한 단락을 채우지 마세요.** 배너는 위 표 밖이지만 "한 줄에 하나만 두지 않는다"는 그대로 걸립니다.
  배너 한 장이 있던 자리는 **두 장으로 나누고** 4:3으로 맞추세요.
- **가로로 넘기는 줄(캐러셀)도 몇 개 보이느냐로 셉니다.** 뒤에 더 있어도 눈에 4개가 보이면 4개 — 비율도 1:1입니다.
  카드 폭을 `260px`처럼 박아 두면 화면 폭이 바뀔 때 3.4개씩 어정쩡하게 걸치니, **칸 수로 나눠서** 정하세요.
- **가로로 넘기는 줄에는 좌우 화살표를 두고, 아래 스크롤바는 감춥니다.** 넘길 게 있다는 건
  화살표가 알려 줍니다. 스크롤바까지 나오면 줄 밑에 회색 막대가 하나 더 생겨 지저분합니다.

```css
.carousel{ scrollbar-width:none; -ms-overflow-style:none; }
.carousel::-webkit-scrollbar{ display:none; }
```

  화살표가 **없는데** 스크롤바까지 감추면 넘길 게 있다는 걸 아무도 모릅니다. 둘은 한 벌입니다.

#### 분리형과 혼합형 — 글자를 사진 아래 두느냐 위에 얹느냐

박스형 콘텐츠는 두 가지입니다. **어느 쪽이든 위의 개수·비율 규칙은 똑같이 적용됩니다.**

| | **분리형** | **혼합형** |
| --- | --- | --- |
| 생김새 | 사진 아래에 글자 | 사진 위에 글자를 얹음 |
| 담을 수 있는 것 | 제목·설명·평점·가격까지 여러 줄 | 제목 + 한 줄 정도 |
| 사진이 하는 일 | 무엇인지 알려 준다 | 분위기를 만든다 |
| 쓰는 자리 | 상품·강의 목록처럼 **고르는** 자리 | 특집·기획전처럼 **끌어당기는** 자리 |

**분리형은 상자를 두르지 마세요.** 흰 배경에 테두리·그림자까지 두르면, 사진과 글자가 이미 한 덩어리인데
선을 한 번 더 치는 셈입니다. 격자로 늘어놓으면 그 선이 줄줄이 겹쳐 화면이 답답해집니다.
**모서리는 사진이 가집니다** — 상자를 없앤 자리에서 사진이 카드 노릇을 합니다.
(혼합형은 사진이 곧 배경이라 이 규칙 밖입니다.)

```css
/* 사진 + 글자로 된 카드 — 상자를 두르지 않는다 */
.card{
  background: none;   /* surface 나 흰색을 깔지 않는다 */
  border: 0;
  box-shadow: none;
}
.card .thumb{ border-radius: 12px; }  /* 모서리는 사진이 가진다 */
.card .body{ padding: 16px 0 0; }     /* 좌우 여백 0 — 상자가 없으니 안쪽 여백도 없다 */
```

**`background: var(--surface)` 나 `border: 1px solid` 를 카드에 쓰면 이 규칙을 어긴 것입니다.**
사진이 없는 상자(설정 패널·안내 상자·표를 감싼 것)는 이 규칙 밖입니다 — 거기는 배경이 있어야
덩어리로 읽힙니다. **사진과 글자로 된 카드**만 걷어냅니다.

상자를 걷어내면 두 가지가 따라옵니다.

- **글자가 흰색이 아니라 페이지 배경 위**에 놓입니다. 대비를 다시 재세요.
- **카드끼리 갈라 주던 테두리가 없어집니다.** 줄과 줄 사이를 더 벌려 그 몫을 대신하세요.

그리고 **카드 안에는 선을 긋지 마세요.** 가격 위에 선을 두면 상자가 없는 상태에서는 그 선이 카드를
가르는 것처럼 보여, 사진·제목과 가격이 따로 노는 두 덩어리로 읽힙니다.
**갈라야 할 것은 카드 안이 아니라 카드끼리**입니다.

혼합형을 쓸 때 반드시 지킬 것:

- **어둠막을 까세요.** 사진 위에 흰 글자를 그냥 얹으면 사진의 밝은 자리에서 글자가 사라집니다.
  **사진은 사는 분이 바꿀 것**이라 우리가 밝기를 미리 알 수 없습니다.
  글자 쪽에서 반대편으로 옅어지는 어둠막을 항상 깔아 두면 어떤 사진이 와도 읽힙니다.
- **글자 대비 4.5 이상**을 지키세요. 가장 밝은 사진(흰색)이 왔다고 치고 재세요.
- **글자를 짧게 쓰세요.** 제목 한 줄 + 설명 한 줄까지입니다. 그보다 길면 사진을 다 덮어 혼합형인 의미가 없습니다.
- **가격·평점처럼 정확히 읽어야 하는 것은 얹지 마세요.** 사진 위에서는 숫자가 잘 안 읽힙니다.
- **한 화면에 두 가지를 섞어도 됩니다.** 위는 혼합형 특집, 아래는 분리형 목록 — 하는 일이 다르므로 어수선하지 않습니다.

#### 글꼴은 한 벌입니다

**기본은 `Paperlogy`**, 못 불러오면 `Pretendard` → `Noto Sans KR` 순으로 물러납니다.
CSS로는 `font-family: Paperlogy, Pretendard, 'Noto Sans KR', sans-serif` 한 줄입니다.

- **제목과 본문에 다른 글꼴을 쓰지 마세요.** 두 벌이 되는 순간 어디에 무엇을 쓸지가
  화면마다 갈립니다. 처음엔 「제목만 다르게」로 시작해서, 나중엔 세 벌 네 벌이 됩니다.
- **위계는 글꼴이 아니라 크기와 굵기로 만듭니다.** 굵기는 400·600·700 셋만 쓰세요.
- 크기는 본문 15 · 카드 제목 18 · 섹션 제목 22 · 페이지 제목 32이 기준입니다.
- **숫자가 많은 표에는 고정폭 숫자**(`font-variant-numeric: tabular-nums`)를 켜세요.
  안 켜면 값이 바뀔 때 자릿수가 흔들려 칸이 들썩입니다.

#### 움직임 — 바뀌었다는 걸 눈이 알아채게

값이 바뀌었는데 **딱 하고 갈아 끼우면 사람은 그걸 못 봅니다.** 숫자가 12에서 3으로
바뀌어도, 눈이 그 자리를 안 보고 있었으면 바뀐 줄 모릅니다.
움직임은 꾸미개가 아니라 **「방금 여기가 바뀌었어요」라고 알려 주는 장치**입니다.

| 언제 | 얼마나 | 무엇이 |
| --- | --- | --- |
| 마우스를 올렸을 때 | **0.12초** | 색·테두리 |
| 눌러서 값이 바뀔 때 | **0.2초** | 사라짐·나타남·자리 옮김 |
| 접히고 펴질 때 | **0.25초** | 높이 |
| 화면 위에 뜰 때 (모달·시트) | **0.25초** | 옅어짐 + 살짝 올라옴 |
| 안내 문구(토스트) | **0.2초 등장 · 3초 머무름** | 옅어짐 |

- **가속은 하나로 씁니다.** `ease-out`(빨리 시작해 부드럽게 멈춤)이 기본입니다.
  자리마다 다른 가속을 쓰면 같은 사이트에서 두 사람이 만든 것처럼 보입니다.
- **0.3초를 넘기지 마세요.** 그보다 길면 기다리는 느낌이 듭니다. 목록이 다시 그려질 때는
  **0.15초**면 충분합니다.
- **자리를 밀어내며 움직이지 마세요.** 아래 내용이 출렁이면 읽던 곳을 잃습니다.
  옅어지거나 제자리에서 바뀌게 하세요.
- **한 번에 한 곳만 움직입니다.** 눌렀는데 화면 여기저기가 같이 움직이면 어디가 바뀐 건지 모릅니다.
- **끝없이 도는 움직임은 두지 않습니다.** 배너 자동 넘김·깜빡임은 읽는 걸 방해합니다.
  기다리는 동안 도는 것(로딩)만 예외입니다.
- **움직임을 꺼 둔 분을 존중하세요.** `prefers-reduced-motion` 이 켜져 있으면 전부 0초로 둡니다.
  어지럼증 때문에 꺼 두신 분들이 있습니다. 값은 바뀌되 움직이지만 않으면 됩니다.

#### 떠 있는 부품과 줄 안에 서는 부품은 다른 것입니다

사진 위에 **떠 있는** 동그란 찜 아이콘과, 버튼 줄 안에 **나란히 서는** 찜 버튼은
생김새가 비슷해 보여도 다른 부품입니다. 떠 있는 쪽은 자리를 빠져나가도록(`position:absolute`)
만들어져 있어서, 그 이름을 줄 안의 버튼에 붙이면 **줄을 빠져나가 엉뚱한 데 가서 앉습니다.**
실제로 찜 버튼이 가격 위에 겹쳐 앉은 적이 있습니다.

- **이름이 자리를 뜻하면 그 자리에서만 쓰세요.** 「사진 위 찜」과 「줄 안 아이콘 버튼」은
  이름을 나눠 두고, 섞어 쓰지 않습니다.
- **부품 이름과 실제 쓰임이 어긋나면 이름을 고치세요.** 「타임라인」이라 지어 놓고
  여섯 곳 모두 한 줄짜리 목록으로 쓰고 있던 적이 있습니다. 세로 점만 남아 글자 사이에 끼었습니다.
- **아이콘만으로 뜻이 통하면 글자를 빼세요.** 하트·공유·닫기가 그렇습니다.
  다만 **`aria-label`로 이름은 남깁니다** — 눈으로 못 보는 분에게는 그게 유일한 이름입니다.
- 반대로 **아이콘만으로 못 알아보는 것에는 글자를 붙이세요.** 「빼기」·「내보내기」처럼
  모양이 여러 뜻으로 읽히는 것이 그렇습니다.

#### 쓰는 화면이 있으면 보여주는 자리도 있어야 합니다

글을 쓰게 해 놓고 그 글이 나올 자리를 안 만드는 일이 잦습니다.
실제로 강사가 **1,240자짜리 강의 소개를 쓰는 화면**은 있는데, 상세 화면에는 그 글이
나올 자리가 없었습니다. 게다가 검수 화면은 "강의 소개 1,240자 — 확인됨"이라고까지 말하고 있었습니다.

- **입력 칸을 만들 때마다 "이건 어디에 보이나"를 한 번 물으세요.** 답이 없으면 둘 중 하나입니다 —
  보여줄 자리를 만들거나, 그 입력 칸을 빼거나.
- **에디터(굵게·목록·이미지 버튼이 붙은 칸)로 쓴 글**은 보여주는 쪽에도 그 모양이 살아야 합니다.
  제목·목록·굵게를 쓸 수 있게 해 놓고 보여줄 때 한 덩어리 문단으로 뭉개면, 쓴 사람이 헛수고합니다.
- **한 줄 요약과 본문 소개는 다른 것입니다.** 목록 카드에 들어가는 한 줄과, 상세에서 읽는 본문은
  자리도 길이도 다릅니다. 하나로 합치지 마세요.

#### 권장 크기는 실제 자리와 같아야 합니다

"권장 1280×720 (16:9)"이라고 적어 두고 옆의 자리는 4:3인 화면이 있었습니다.
**사는 분은 글자를 믿고 그 크기로 사진을 준비**하는데, 올려 보면 잘립니다.

- 안내 문구의 숫자와 **바로 옆 자리의 비율이 같은지** 확인하세요.
- 검수·확인 화면에 크기가 또 적힌다면 **거기도 같은 숫자**여야 합니다. 세 곳이 따로 놀기 쉽습니다.
- **영상만 16:9입니다.** 썸네일·카드 사진은 위 표(2·3개↔4:3, 4개↔1:1)를 따릅니다.

#### 차트 — 이미지가 아니라 데이터로 그립니다

추이·막대·도넛 같은 차트를 **이미지 자리로 두지 마세요.** `이미지 영역 (라인 차트 · 권장 1200×420)`
이라고 적어 두면 사는 분은 차트를 **그림 파일로 만들어 올려야 하는 줄** 압니다.
차트는 숫자가 바뀌면 따라 바뀌어야 하는 것이라, 사진 자리와 성격이 다릅니다.

- **가벼운 것은 라이브러리 없이 SVG로 그리세요.** 선 하나·막대 열두 개짜리는 30줄이면 됩니다.
  바깥에서 아무것도 안 불러오니 **인터넷 없이 폴더에서 열어도** 그대로 보입니다.

**대시보드를 만든다면 최소한 이 셋은 갖추세요.**

| 무엇 | 어디에 | 왜 |
| --- | --- | --- |
| **큰 추이 차트** | 화면에 한둘 | 기간을 놓고 흐름을 본다 |
| **스파크라인** (작은 추이선) | 지표 카드 안 | 숫자 옆에서 방향만 알려 준다 |
| **증감 표시** (▴ 812) | 지표 카드 안 | 좋아졌는지 나빠졌는지 |

숫자 하나만 두면 **좋은지 나쁜지를 못 말합니다.** "10,374명"만 있으면 늘고 있는지
줄고 있는지 알 수 없어서, 사는 분은 그 카드를 보고 아무 판단도 못 합니다.
지표 카드는 **숫자 + 증감 + 추이선**이 한 벌입니다.

- **본격적인 차트가 필요하면 무료(MIT) 라이브러리를 쓰세요.** 값을 치르지 않아도 되고 상업적으로 써도 됩니다.

| 라이브러리 | 크기 | 언제 |
| --- | --- | --- |
| **Chart.js** | ~200KB | 무난한 첫 선택. 자료가 많아 AI도 정확하게 짭니다 |
| **ApexCharts** | ~500KB | 기본 모양이 예쁘고 종류가 많을 때 |
| **uPlot** | ~40KB | 점이 수천 개라 가벼워야 할 때 |
| **Frappe Charts** | ~30KB | 작고 산뜻한 SVG면 충분할 때 |

- **색은 반드시 프리셋 색을 쓰세요.** 라이브러리 기본 팔레트(빨강·노랑·초록)를 그대로 두면
  그 자리만 다른 사이트처럼 보입니다.
- **눈금선은 옅게, 선은 진하게.** 눈금이 데이터보다 진하면 숫자가 안 읽힙니다.
- 라이브러리를 쓰면 **인터넷이 필요해집니다.** 오프라인으로 열어 볼 화면이라면 파일을 함께 받아 두거나 SVG로 그리세요.

#### 가격 표기 — 낼 돈부터

**판매가 · 정가 · 할인율** 순서로 한 줄에 세웁니다.

| 자리 | 모양 | 크기 |
| --- | --- | --- |
| **판매가** (낼 돈) | 진한 기본 글자색, 굵게 | 18 (상세는 28) |
| **정가** | 회색 + 취소선 | 13 |
| **할인율** | **강조색 글자**, 굵게 | 14 |

- **할인율을 앞에 두지 마세요.** 눈이 먼저 "20%"에 걸려 정작 얼마인지를 뒤늦게 찾습니다.
  사는 분이 알고 싶은 건 **낼 돈**입니다.
- **할인율은 배지가 아니라 색 글자입니다.** 배지로 두면 정가·판매가와 무게가 비슷해져 셋이 서로 다툽니다.
  배지 자리는 `입문`·`NEW` 같은 **꼬리표 몫**입니다.
- **셋의 크기를 벌려 두세요.** 비슷하면 무엇을 먼저 읽어야 할지 눈이 헤맵니다.
- **강조색 원색을 글자로 쓰지 마세요.** 주황·코랄 원색은 흰 배경에서 3:1을 못 넘깁니다.
  글자용으로 한 단계 어두운 색을 따로 두세요.
- 가격 묶음은 **한 곳에서 만들어 쓰세요.** 자리마다 손으로 짜면 순서가 갈립니다.

#### 버튼이냐 링크냐

**다른 화면으로 데려가면 링크, 이 화면 안에서 끝나면 버튼입니다.**

| 하는 일 | 무엇으로 | 눌렀을 때 |
| --- | --- | --- |
| 다른 화면으로 간다 | 링크 `<a href>` | 그 화면이 열린다 |
| 이 화면 안에서 끝난다 | 버튼 `<button>` | 안내 문구가 뜬다 |

- 필터 적용·정렬·검색·다음 차시·시간 지점 이동은 **화면이 바뀌지 않는 조작**입니다.
  링크로 만들면 **자기 자신으로 가는 링크**가 되어, 눌러도 같은 화면이 다시 열립니다.
  사는 분 눈에는 *"눌렀는데 아무 일도 안 나네"* 로 보입니다.
- **나란히 선 버튼끼리는 종류를 섞지 마세요.** 하나는 화면이 넘어가고 하나는 안 넘어가면
  규칙이 없어 보입니다.

**그리고 화면 안에서 끝나는 조작은 진짜로 값이 바뀌어야 합니다.**
눌러도 숫자가 그대로면, 사는 분 눈에는 **눌리지 않는 포스터**입니다.

| 눌렀을 때 | 바뀌어야 하는 것 |
| --- | --- |
| 기간 탭 (7일·30일·90일) | 차트와 그 옆 요약 숫자 |
| 쿠폰·옵션 고르기 | 결제 금액 줄과 합계 |
| 수량 늘리기 | 금액 |
| 정렬·필터 | 목록 순서나 개수 |

#### 눌러 봐야만 드러나는 것들 — 2026-08-19 LMS 두 팩 검수에서 나온 것

화면을 눈으로만 보면 멀쩡한데, 눌러 보면 어긋나는 자리가 있습니다. 실제로 나온 것들입니다.

**① 켜짐 표시와 실제 걸린 조건이 따로 놀지 않게.**
거르는 단추 여덟 개와 「전체 보기」가 서로 다른 상자에 담겨 있었습니다. 그래서
「디자인」을 눌러도 「전체 보기」가 안 꺼지고, 「전체 보기」를 눌러도 「디자인」이 안 꺼졌습니다.
**목록은 맞게 걸러지는데 표시만 손님을 속였습니다.**
- 같은 갈래의 단추는 **상자를 넘어** 한 벌로 갱신하세요. 상자 안에서만 끄면 밖에 있는 게 남습니다.
- 조건 칩의 ✕ 로 지웠을 때도 **왼쪽 단추의 켜짐이 같이 꺼져야** 합니다.

**② 쪽 번호는 결과를 따라가야 합니다.**
12개를 2개로 걸렀는데 쪽 번호가 「1 2 3 4 5」 그대로였고, 어느 쪽을 눌러도 같은 목록이 나왔습니다.
- 결과가 한 쪽에 다 들어가면 **쪽 번호를 감추세요.**
- 조건이 바뀌면 **1쪽으로 되돌리세요.** 3쪽을 보다 조건을 바꾸면 3쪽에 머물면 안 됩니다.

**③ 화면을 넘길 때 고른 값도 같이 넘기세요.**
결제 화면에서 「12개월 할부」를 골라 「매달 5,270원 × 12개월」까지 확인했는데,
완료 화면은 「신용카드 (일시불)」이었습니다. 고른 값이 다음 화면으로 안 넘어갔습니다.
- 앞 화면에서 고른 것이 뒤 화면에 다시 적힌다면 **둘을 이어 두세요**(sessionStorage 면 충분합니다).
- 이어 둘 수 없으면 **뒤 화면에 그 값을 적지 마세요.** 틀린 값보다 없는 게 낫습니다.

**④ 견본 날짜는 「오늘」에서 거꾸로 잡으세요.**
만든 날에 맞춰 날짜를 적어 두면 시간이 지나 여는 손님은 지난 날짜만 봅니다.
실제로 「D-3 · 마감 8월 10일」인데 그날은 8월 19일이었습니다 — 9일 지난 마감을 「3일 남음」이라 했습니다.
- **기준일 하나를 정해 두고, 화면을 열 때 오늘까지의 차이만큼 통째로 미세요.**
  날짜 사이 간격이 그대로라 「D-3」이나 「수강 기간 6개월」 같은 계산값은 손대지 않아도 맞습니다.
- 주문번호에 박힌 날짜(ORD-20260807-…)도 같이 밀어야 합니다.

**⑤ 숫자 하나를 고치면 그 숫자로 계산되는 값을 전부 따라가세요.**
차시 수를 30에서 18로 고치면서 딸린 값을 안 고쳤더니,
같은 강의 진도가 화면마다 67% 와 40% 로 갈라졌고 「12/18차시 · 남은 차시 18개」가 됐습니다.
- 차시 수를 바꿀 때 딸린 것: **총 재생 시간 · N/M차시 · 진도 % · 남은 차시 · 장별 완료 수.**
- 고친 뒤에는 그 값이 나오는 **모든 화면을 훑어** 한 벌로 맞았는지 보세요.

**⑥ 「비로그인」 화면은 헤더도 비로그인이어야 합니다.**
「홈 - 비로그인」인데 헤더에 알림 종과 프로필이 있고 로그인 단추가 없었습니다.
사이드바에는 「로그인·회원가입」이 있는데 헤더에는 「김수현님」이라, 한 화면에서 앞뒤가 안 맞았습니다.
- 로그인 전/후 화면을 나눠 만들었다면 **머리띠와 옆 메뉴도 같이 갈라야** 합니다.

**⑦ 견본이라 좁은 자리는 그 말을 화면에 적으세요.**
분야 카드는 「개발 284개」인데 눌러 거르면 2개가 나왔습니다. 견본이라 그런 건데 그 말이 없었습니다.
- 「전체 1,280개 가운데 12개만 보여 주는 견본입니다」 처럼 **한 줄로 밝히면** 어긋남이 사라집니다.
- 목록에서 어느 카드를 눌러도 같은 상세가 열린다면 **상세 화면에도** 그렇게 적으세요.

**⑧ 겹쳐 놓은 단추는 정말 눌리는지 확인하세요.**
카드 위 찜 하트가 사진에 덮여 있어서, 누르면 찜이 안 되고 강의 상세로 넘어갔습니다.
- 겹치는 단추에는 **z-index** 를 주고, `document.elementFromPoint` 로 그 자리 맨 위가 무엇인지 확인하세요.
- 링크 안에 단추를 넣었다면 `preventDefault` 만으로는 부족합니다 — **덮였는지부터** 보세요.

**⑨ 못 채운 곳으로 화면을 옮겨 주세요.**
「결제하기」는 오른쪽 위에 있고 「동의를 아직 채우지 않았어요」는 화면 아래에 떴습니다.
스크롤해야만 보이니 손님은 왜 안 되는지 모릅니다.
- 막혔으면 **못 채운 자리로 스크롤**하고, 채워지면 안내를 거두세요.

**⑩ 끝난 조작은 끝난 티를 내세요.**
쿠폰 「받기」를 몇 번이고 눌러도 「받았어요」가 또 나왔고, 과제를 내도 알림 한 줄 없이 화면이 넘어갔습니다.
- 한 번으로 끝나는 단추는 **글자를 바꾸고 다시 눌리지 않게** 하세요(받기 → 받음).
- 낸 뒤에는 **그 자리에서** 낸 것이 보여야 합니다.

- **미리 만들어 두고 바꿔 끼세요.** 차트 셋을 미리 그려 두고 탭에 따라 보여 주는 편이,
  누를 때마다 자바스크립트로 다시 그리는 것보다 낫습니다 — 그리는 규칙이 두 곳으로 갈라지지 않습니다.
- **계산은 한 곳에서만 하세요.** 합계를 화면마다 손으로 적어 두면 쿠폰을 바꿨을 때 한 군데만 바뀝니다.
- **탭에 갈 곳도 바꿀 것도 없으면 탭이 아닙니다.** 그냥 제목이거나, 아직 안 만든 자리입니다.

**같은 길을 두 번 두지 마세요 — 위 카테고리 줄과 왼쪽 LNB.**
둘이 같은 곳을 가리키면 위쪽이 두 줄로 두꺼워지고, 어느 쪽을 눌러야 할지 잠깐 멈칫합니다.

| 상태 | 왼쪽 LNB | 위 카테고리 줄 |
| --- | --- | --- |
| 맨 위 (LNB 보임) | 보인다 | **접힌다** |
| 스크롤해서 LNB가 위로 사라짐 | 안 보인다 | **내려온다** |
| LNB가 아예 없는 화면 | — | **늘 보인다** |

- **LNB가 있는 화면은 접힌 채로 시작하세요.** 펼쳐 두었다가 나중에 접으면 첫 화면에서 한 번 번쩍입니다.
- **LNB가 없는 화면에서는 절대 접지 마세요.** 접으면 갈 길이 통째로 사라집니다.
- **기준은 "LNB가 붙박이 머리말 아래로 사라지는 순간"입니다.** 머리말 높이만큼 위를 깎아서 재세요.
- 같은 규칙을 **LNB를 쓰는 모든 레이아웃에 똑같이** 거세요. 화면마다 다르면 위쪽 높이가 제멋대로 바뀌어 보입니다.

- **견본으로 가는 버튼을 화면 안에 두지 마세요.** `결과가 없을 때` `대화가 없을 때는 이렇게 보여요`
  처럼 예외 화면을 보여주려고 넣은 버튼·단락은 진짜 사이트에 없는 것입니다.
  검색창 옆에 그런 버튼이 서 있으면 사는 분은 그게 기능인 줄 압니다.
  빈 화면·오류 화면은 **화면 목록에 따로 한 장**으로 두세요 — 거기서 눌러 보면 됩니다.

#### 옮겨 다니며 봐야 드러나는 것들 — 2026-08-20 사장님 검수에서 나온 것

앞의 것들이 «눌러 봐야» 나오는 흠이라면, 이번 것은 **화면을 옮겨 다니고 끝까지 굴려야** 나옵니다.
한 장만 세워 놓고 보면 전부 멀쩡해 보입니다. 실제로 우리 팩 열넷에서 나온 것만 적습니다.

**① 화면을 옮길 때 본문이 좌우로 튀지 않게.**
세로 스크롤 막대는 «생길 때만» 15px 을 먹습니다. 그래서 목록이 짧은 화면에서 긴 화면으로 가면
본문이 통째로 15px 밀립니다. 한 장씩 보면 둘 다 멀쩡한데 오가면 화면이 흔들립니다.
```css
html { scrollbar-gutter: stable; }   /* 막대 자리를 늘 비워 둔다 */
```
- 화면 «안»에서 탭으로 목록을 거를 때도 같습니다. 3개 보이다 5개가 되면 막대가 생깁니다.

**② 콘텐츠 폭은 모든 화면에서 같아야 합니다.**
한 화면만 폭이 다르면 옮길 때마다 글이 좌우로 밀려 «따라다니는» 느낌이 납니다.
- 폭은 화면마다 적지 말고 **한 곳(`--wrap`)에 두고 모두 그것을 쓰세요.**

**③ 푸터가 본문에 맞붙지 않게.**
`margin-top: auto` 는 **자리가 남을 때만** 벌어집니다. 내용이 한 화면을 넘기면 0 이 되어
푸터 윗선이 버튼에 그대로 붙습니다. 실제로 「강사로 전환 신청」 바로 밑에 선이 붙었습니다.
```css
/* auto 는 그대로 두고(짧은 화면에서 바닥에 붙이는 일은 auto 만 합니다)
   푸터 «바로 앞 덩어리»에 최소 간격을 줍니다 */
:where(main, .page-wrap) > *:has(+ footer) { margin-bottom: 32px; }
```
- GNB 와 본문 사이도 마찬가지입니다. **16px 밑으로 붙지 않게** 하세요.

**④ 덩어리 사이 간격은 그 화면이 쓰는 «리듬»을 지키세요.**
다른 칸들은 24px 씩 벌어져 있는데 한 곳만 8px 이면, 0px 이 아니어도 **붙어 보입니다.**
- 새 묶음이 시작되는 자리(안내 상자 다음에 오는 소제목 같은 것)를 특히 보세요.
- 제목 바로 밑 부제는 **붙어 있는 것이 맞습니다.** 그건 한 덩어리입니다.

**⑤ 화면 «안» 탭에는 뒤로가기를 두지 마세요.**
탭처럼 생겼는데 실제로는 다른 장으로 넘어가고, 간 장에는 「‹ 돌아가기」가 붙어 있었습니다.
누를 때마다 없던 손잡이가 생기면서 **화면이 그만큼 아래로 밀립니다.**
- 탭으로 오갈 화면들은 **뒤로가기 유무를 통일**하세요. 탭이면 넷 다 없는 쪽이 맞습니다.
- 탭 하나가 «지금 보고 있는 그 화면»을 가리키고 있지는 않은지도 보세요. 눌러도 아무 일이 없습니다.

**⑥ 동그란 버튼을 가로로 늘이지 마세요.**
`border-radius: 50%` 인 34px 짜리 아이콘 버튼에 `width: auto` 를 얹으면 **타원**이 됩니다.
- 넓은 글자 버튼이 필요하면 **일반 버튼 모양**을 쓰고, 옆 버튼과 **키를 맞추세요.**
- 나란히 선 버튼끼리 키가 다르면 흠입니다. 다만 「큰 버튼 + 보통 버튼」을 **일부러** 짝지은 것은 괜찮습니다.

**⑦ 세로로 세운 flex 칸에서는 배지와 버튼이 늘어납니다.**
`display:flex; flex-direction:column` 에 `align-items` 를 안 주면 기본값이 `stretch` 라
배지도 버튼도 **칸 폭 그대로 늘어납니다.** 40px 이어야 할 배지가 767px 이 된 적이 있습니다.
```css
.promo .in > .badge, .promo .in > .btn { align-self: flex-start; }
```
- 글(제목·설명)은 넓게 두고 **배지와 버튼만** 제 몸에 맞추세요.

**⑧ 배지를 글 옆에 붙이려면 블록으로 만드세요.**
`14:31<div class="unread">2</div>` 에서 배지가 `inline-grid` 라 **시각 옆에 나란히** 섰습니다.
`margin-top` 을 준 걸 보면 원래는 밑에 놓을 셈이었는데, 인라인이라 안 먹었습니다.
```css
.unread { display: grid; width: max-content; margin: 4px 0 0 auto; }
```

**⑨ 좌우로 미는 줄에는 막대를 보이지 마세요.**
카드를 옆으로 미는 줄에 «‹ ›» 화살표를 붙여 놓고 막대까지 같이 보이면 밑에 회색 띠가 깔립니다.
```css
.rail { scrollbar-width: none; }
.rail::-webkit-scrollbar { display: none; }
```
- ⛔ **표에는 쓰지 마세요.** 표에는 화살표가 없어서 막대를 감추면 «넘길 수 있다는 것»을 알 길이 사라집니다.
  표는 애초에 **좁은 칸에 넣지 않는 것**이 답입니다.

**⑩ 나란한 썸네일은 크기가 같아야 합니다.**
한 줄에 선 카드의 사진 높이가 593 · 250 · 84px 로 제각각이면 목록이 무너져 보입니다.
- 목록 안 사진은 **비율(`aspect-ratio`)을 정해** 한 벌로 맞추세요.

**⑪ 지금 어느 메뉴에 와 있는지 «켜서» 보여 주세요.**
메뉴는 있는데 **지금 여기가 어디인지 표시가 없으면** 손님은 길을 잃습니다.
실제로 우리 팩에서 나왔습니다 — CSS 에 `.ednav-menu a.on { border-bottom-color: … }` 규칙까지 만들어 두고,
정작 `on` 을 **켜 주는 자리가 없어** 열세 화면의 상단 메뉴가 전부 꺼져 있었습니다.
- GNB 와 LNB 둘 다입니다. **켜진 것은 언제나 하나**여야 합니다 — 없어도, 둘이어도 틀린 것입니다.
- 상세 화면처럼 메뉴에 없는 쪽이면 **그 상위 메뉴**를 켜세요.
```js
// 화면을 열 때 지금 쪽에 해당하는 메뉴를 켠다
const 지금 = (document.body.dataset.page || '').slice(0, 2);
document.querySelectorAll('.gnb-menu a, .lnb a').forEach((a) => {
  const 갈곳 = (a.getAttribute('href') || '').split('/').pop() || '';
  if (갈곳.slice(0, 2) === 지금) { a.classList.add('on'); a.setAttribute('aria-current', 'page'); }
});
```

**⑫ 성격이 다른 상품은 «제 상세»로 보내세요.**
목록 카드가 여럿 있어도 **같은 종류라면 한 상세로 가도 됩니다** — 화면 목록이 이미 케이스를 보여 줍니다.

⛔ **다만 성격이 다르면 다릅니다.** 바로 구매 상품 · 경매 상품 · 예약 상품처럼
**상세에서 보여 주는 내용도 다르고 사는 과정도 다른 것**은 상세를 각각 만들어야 합니다.
한 사이트에 여러 케이스가 섞여 있을 때 특히 그렇습니다.
- 실제로 겪었습니다 — 여행 팩에 「상품 상세」와 「**패스** 상품 상세」가 따로 있는데,
  「오사카 주유패스」 카드가 **일반 상품 상세로** 가고 있었습니다. 카드는 제 갈래를 알고 있었는데
  링크만 늘 같은 곳을 가리켰습니다.
- 갈래가 다르면 **화면 목록에도 상세를 따로 세우고**, 카드가 그리로 가게 하세요.
- ⛔ `href="#"` 나 빈 `href` 로 두지 마세요. **눌리는데 아무 데도 안 가는 것**이 제일 나쁩니다.

#### 화면 목록으로 가는 길은 한 곳에만

만든 화면을 훑어보려면 목록이 필요합니다. 그런데 그 목록을 **화면 안에 늘어놓으면**
사는 분 눈에는 사이트의 일부로 보입니다.

```
이렇게 두지 마세요 — 화면 위쪽에 줄줄이
  [CO-01 · 강의 목록] [CO-02 · 검색 결과 없음] [CO-03 · 강의 상세] [CO-04 · 결제] …
```

**진짜 사이트에는 이런 줄이 없습니다.** `CO-02 · 검색 결과 없음` 은 만드는 사람의 말이지
쓰는 사람의 말이 아닙니다. 화면 ID 가 손님 눈에 보이는 것부터가 어긋납니다.

- **한 귀퉁이에 작게 하나만 둡니다.** 오른쪽 아래에 떠 있는 `화면 목록` 단추 하나면 됩니다.
  누르면 `화면목록.html` 로 갑니다 — index.html(홈)이 아닙니다.
- **그 단추는 화면 내용 위에 떠 있게** 두고, 본문 흐름에는 끼워 넣지 않습니다.
  본문에 들어가면 그것도 사이트의 일부가 됩니다.
- **화면 ID 를 손님이 보는 자리에 쓰지 않습니다.** 제목·탭·빵부스러기 어디에도요.
  ID 는 우리가 화면을 세는 이름이지 서비스의 말이 아닙니다.
- 화면끼리 오가는 길은 **그 서비스의 진짜 메뉴와 버튼**으로 만듭니다.
  「강의 목록 → 강의 상세」는 강의 카드를 눌러서 가지, 탭으로 가지 않습니다.

### 이미지 · 썸네일
- 아직 사진이 없으므로 이미지 자리는 **테마 색이 아니라 옅은 파스텔**로 채우세요. 테마 색으로 칠하면 화면이 그 색 덩어리로 뒤덮여 디자인이 안 보입니다.
- 배경 `#EEF2F7` · `#F2EFF7` · `#EDF4F1` · `#F7F1EC` · `#F2F4F7` 중 하나를 카드마다 돌려 쓰고, 테두리 `#E3E8F0` 1px, 글자색 `#8B94A6` 13px.
- 자리 안에 `이미지 영역 (무엇 · 권장 가로×세로)` 형식으로 적으세요. 예: `이미지 영역 (매장 대표 · 권장 1200×900)` · `이미지 영역 (프로필 · 권장 400×400)` · `이미지 영역 (배너 · 권장 1600×600)`
- 이미지 자리는 테마 색으로 칠하지 않는다. 옅은 파스텔 한 톤 + 1px 테두리로 둔다.
- 자리 안에 무엇이 들어갈 이미지인지와 권장 크기를 글자로 적는다.
- 적어 둔 크기의 **비율을 실제로 지킨다** — `400×400`이라 적었으면 정사각으로 보여야 한다(aspect-ratio).
- 특히 프로필·썸네일처럼 크기가 고정된 자리는, 가로로 나란히 놓아도 늘어나지 않게 한다. 목록·행 안에 넣을 때 `flex`가 잡아당겨 타원이 되는 일이 잦으니, 만든 뒤 실제로 열어 모양을 확인한다.
- 썸네일처럼 작아서 글자가 안 들어가면 크기만 적거나 아이콘 하나만 둔다.
- **「사진 위 겹침」 카드에서는 사진이 들어오기 전까지 어둠막을 걸지 않는다.** 옅은 자리표시자 위에 어두운 막을 덮으면 안에 적은 글이 묻혀 «깨진 그림»으로 보인다. 제목·태그는 자리표시자 아래에 본문 색으로 두었다가, 실제 사진을 넣을 때 위로 올린다.
- 실제 사진을 넣기 전까지 이 규칙을 유지한다 — 그래야 어디에 무엇을 넣어야 하는지 보인다.


## 7. 만들고 나서 — 눈으로 보고, 눌러 보고, 고치기
> **여기까지 해야 다 만든 것입니다.** 화면을 다 그렸어도 눌러서 안 움직이면 안 만든 것과 같습니다.
> **세 가지를 봅니다 — ① 화면연결·레이아웃·UI 를 눈으로 ② 뒤로가기·버튼·탭·썸네일·배너를 직접 눌러서 ③ 데이터가 화면끼리 맞는지.**
> 훑어보는 것이 아니라 **세어서** 확인하고, **나온 오류는 그 자리에서 고칩니다.** 하나라도 남으면 고치고 다시 셉니다.


### 7-0. 검수는 «두 갈래»입니다 — 둘 다 하세요
> **화면을 다 만들었으면 여기서 끝이 아닙니다.** 성격이 다른 두 가지를 각각 봅니다.

| | 무엇을 보나 | 어떻게 아나 | 어디서 |
|---|---|---|---|
| **① 사람처럼 보는 검수** | 보이는 것 — 레이아웃 · 눌러 본 반응 · 생김새 | **열고 눌러 보고 굴려야** 안다 | 아래 7-1 ~ 7-9 |
| **② 기계가 보는 검수** | 파일 · 값 · 문서끼리 앞뒤 | **열어 보지 않아도** 파일만 보면 안다 | 아래 7-9-2 |

⛔ **한 갈래만 하면 반드시 새어 나갑니다.**
보기에 멀쩡한데 링크가 죽어 있거나, 파일은 다 맞는데 화면이 무너져 있습니다.

**① 사람처럼 보는 검수 — 열다섯 가지**

| 갈래 | 무엇을 보나 |
|---|---|
| 레이아웃 | ① 콘텐츠 폭이 모든 화면에서 같은가 ② 콘텐츠 사이 간격이 균일한가 ③ GNB 상단·푸터 하단 고정 ④ GNB↔콘텐츠·푸터↔콘텐츠 간격 ⑤ GNB·LNB 가 지금 메뉴를 켜 주는가 ⑥ 화면 이동 시 흔들림이 없는가 |
| UX | ⑦ 뒤로가기가 적절한가(화면 안 탭에는 두지 않는다) ⑧ 배너·썸네일·버튼·탭이 정확히 반응하는가 |
| UI | ⑨ 배경 위 글자색이 적절한가 ⑩ 썸네일 크기가 적당한가 ⑪ 버튼 크기·모양이 균일한가 ⑫ 좌우로 넘기는 곳에 가로 막대가 드러나지 않는가 ⑬ UI 가 틀어지지 않았는가 ⑭ 배지 크기·모양이 균일한가 ⑮ 배지 위치가 적절한가 |

**② 기계가 보는 검수 — 화면을 안 열어도 아는 것**

- **글자 대비** — 본문 4.5 · 큰 글자 3.0 을 넘는가 (색을 고른 뒤 반드시 재세요)
- **끊어진 링크** — `href` 가 가리키는 화면이 실제로 있는가
- **없는 그림** — `src` 가 가리키는 파일이 실제로 있는가
- **외톨이 화면** — 만들어 놓고 아무 데서도 안 이어지는 화면이 있는가
- **화면 수가 맞는가** — 이 문서가 약속한 화면 수와 실제로 만든 수가 같은가
- **굳은 날짜** — 견본 날짜가 «오늘»에서 거꾸로 잡혀 있는가 (지난 날짜가 보이면 안 됩니다)

### 7-1. 화면연결 · 레이아웃 · UI 를 «눈으로» 봅니다
- **화면 목록에서 시작해 링크를 눌러 다닙니다.** 주소창에 직접 쳐 넣지 마세요 — 그러면 화면이 서로 이어지는지가 안 보입니다.
- 묶음마다 대표 화면을 열어 **무너진 곳 · 겹친 곳 · 잘린 글 · 화면 밖으로 나간 것**을 봅니다.
- 가로 스크롤바가 생기면 어딘가 넘친 것입니다. 넓은 화면에서도 한 번 봅니다.
- 사이드바가 있는 짜임이면 **본문이 사이드바에 가리지 않는지** 봅니다.

### 7-2. 눌러도 아무 일 없는 것이 0개여야 합니다
- `href="#"` · `href="javascript:void(0)"` · 빈 `href` 는 **하나도 남기지 않습니다.**
- **페이지 번호, 달력 날짜, 시간 칸, 이전·다음** — 여기가 제일 자주 빈 채로 남습니다. 실제로 눌러 보세요.
- 아직 만들 화면이 없으면 «지금은 안 되는 것»으로 보이게 두세요(흐리게 + 눌리지 않게). 눌리는데 아무 일도 없으면 고장으로 보입니다.
- 단추(`<button>`)도 같습니다. 눌렀을 때 **화면이 바뀌거나, 값이 바뀌거나, 안내가 뜨거나** 셋 중 하나는 일어나야 합니다.


### 7-3. 거르는 단추는 «목록이 실제로 줄어야» 합니다
- **필터 칩·탭·정렬**을 누르면 색만 바뀌는 것으로는 안 됩니다. **보이는 항목이 실제로 달라져야** 합니다.
- 칩에 개수를 적었으면(`기본 교육 2`) 누른 뒤 **그 수만큼만 보여야** 합니다. 적힌 숫자와 보이는 수가 다르면 그 자리는 거짓말이 됩니다.
- 거르면 하나도 안 남는 조건도 눌러 보세요. **「결과가 없습니다」 화면**이 나와야 합니다 — 빈 채로 두면 고장으로 보입니다.
- 정렬(최신순·낮은 요금순)도 같습니다. 순서가 **눈에 보이게** 바뀌어야 합니다.

⛔ **안 줄어들면 열에 아홉은 이것입니다** — `[hidden]` 은 브라우저 기본값이라 `.row { display: flex }` 한 줄에도 집니다. 「지금 2개 보고 있어요」로 숫자만 바뀌고 목록은 그대로면, CSS 에 `[hidden] { display: none !important; }` 이 있는지 보세요.


### 7-4. 썸네일 · 배너 · 탭도 «직접» 눌러 봅니다
- **썸네일과 배너**는 그림이라 눌러 보기를 잊기 쉽습니다. 눌러서 어디로 가는지 하나씩 확인하세요.
- 카드 사진 위에 얹은 단추(찜 하트·빠른 담기)는 **사진에 덮여 안 눌리는 일**이 잦습니다.
  겹쳐 놓은 단추에는 `z-index` 를 주고, 눌러서 **정말 그 단추가 눌리는지** 확인하세요.
  (그 자리 맨 위에 무엇이 있는지는 `document.elementFromPoint(x, y)` 로 알 수 있습니다.)
- **탭**을 눌렀을 때 내용이 화면 밖에 있으면 손님은 아무것도 안 바뀐 줄 압니다. 탭 줄이 화면에 보이게 옮겨 주세요.
- **쪽 번호**는 눌렀을 때 목록이 실제로 달라져야 합니다. 걸러서 한 쪽에 다 들어가면 쪽 번호를 감추고, 조건이 바뀌면 1쪽으로 되돌립니다.
- 한 번으로 끝나는 단추(쿠폰 받기·신청하기)는 누른 뒤 **글자가 바뀌고 다시 안 눌려야** 합니다. 몇 번이고 눌리면 받았는지 알 수 없습니다.

### 7-5. 뒤로가기는 4장에 적힌 그 화면으로 갑니다
- 31개 화면에 뒤로가기가 있고, 나머지 9개(메뉴 첫 화면)에는 **없어야** 합니다.
- 화면마다 4장의 `뒤로가기` 항목과 **하나씩 대 봅니다.** 「대충 상위 메뉴로」가 아니라 적힌 그 화면입니다.
- 여러 단계로 이어지는 화면(예약→결제→완료)에서 특히 어긋납니다. 완료 화면에서 뒤로 갔을 때 결제가 다시 시작되면 안 됩니다.

### 7-6. 화면 목록과 실제가 같은지 셉니다
- 만들어진 화면이 **40개** 인지 셉니다. 목록에는 있는데 안 만든 화면, 만들었는데 목록에 없는 화면 둘 다 0이어야 합니다.
- 화면끼리 잇는 링크가 **실제로 있는 화면**을 가리키는지 셉니다. 없는 화면을 가리키는 링크는 0이어야 합니다.
- **`화면목록.html` 이 40개를 «다» 가리키는지** 셉니다. 홈에서 닿는 것만 넣으면 나머지는 영영 못 봅니다.
- **화면 목록으로 가는 단추가 40개 화면 «전부»에 있는지** 셉니다. 한 화면이라도 빠지면 거기서 길이 끊깁니다.


### 7-7. 데이터가 화면끼리 «서로 맞는지» 봅니다
> 화면 하나만 보면 다 맞아 보입니다. **두 화면을 나란히 놓아야** 어긋난 것이 보입니다.

- **앞 화면에서 고른 것이 뒤 화면에 그대로 넘어가는지** — 수량·옵션·할부·기간을 골라 놓고 다음 화면에서 확인하세요.
  고른 값이 안 넘어가면 뒤 화면에 그 값을 아예 적지 마세요. **틀린 값보다 없는 게 낫습니다.**
- **같은 값이 여러 화면에 나오면 다 같은지** — 목록의 「12/18 · 67%」가 상세에서는 40%면 하나는 거짓말입니다.
- **합계가 항목의 합과 맞는지** — 「남은 일 59건」이 1+24+32+2 와 맞는지, 표의 인원 합이 위 카드의 총원과 맞는지.
- **N/M 과 「남은 것」이 맞는지** — 12/18 이면 남은 것은 6개입니다. 전체 수를 그대로 적어 두기 쉽습니다.
- **적어 둔 개수와 실제로 보이는 개수가 맞는지** — 「전체 284개」라고 해 놓고 걸러 보니 2개면, 그 말을 화면에 적으세요(「284개 가운데 8개만 보여 주는 견본입니다」).
- **날짜와 그 옆의 말이 맞는지** — 「이번 주」가 붙은 것이 정말 이번 주인지, 「D-3」이 그 날짜와 맞는지, 요일이 달력과 맞는지.

⛔ **숫자 하나를 고치면 그 숫자로 계산되는 값을 «전부» 따라가세요.**
차시 수를 30에서 18로 고치면서 진도와 남은 차시를 안 고치면, 같은 것이 화면마다 다르게 보입니다. **반쪽만 고치면 안 고친 것보다 나쁩니다.**

### 7-8. 폰 폭에서 다시 한 번
- 위 「좁은 화면에서 넘치지 않게」를 **다 만든 뒤에 다시** 확인합니다. 화면이 늘어나면서 새로 넘치는 곳이 생깁니다.


### 7-9. 기계로 재기 — 이 글을 화면마다 돌리세요
> **눈으로 세지 마세요.** 아래 글이 «옮겨 다니며 봐야 보이는 것»을 대신 재 줍니다 —
> 푸터가 본문에 맞붙었는지 · 덩어리 간격이 어긋났는지 · 버튼과 배지가 늘어났는지 ·
> 좌우로 미는 칸에 막대가 드러났는지 · 표가 제 칸을 넘쳤는지.

**돌리는 법** — 편한 것 하나만 고르세요.
- 브라우저 개발자도구 콘솔에 그대로 붙여 넣기
- 화면마다 <script> 로 잠깐 끼워 넣고 결과를 찍기
- 헤드리스 브라우저로 열어 결과를 document.title 에 담아 꺼내기

```js
(() => {
  const 흠 = [], 적기 = (칸, 말) => { const k = "[" + 칸 + "] " + 말; if (!흠.includes(k)) 흠.push(k); };
  const 보이나 = (el) => { const s = getComputedStyle(el);
    if (s.display === "none" || s.visibility === "hidden") return false;
    const r = el.getBoundingClientRect(); return r.width > 2 && r.height > 2; };
  const 이름 = (el) => el.tagName.toLowerCase() +
    (typeof el.className === "string" && el.className ? "." + el.className.trim().split(/ +/)[0] : "");
  const 반 = (n) => Math.round(n);
  const 본문 = document.querySelector("main") || document.body;
  const bs = getComputedStyle(본문);
  const 콘텐츠폭 = 반(본문.getBoundingClientRect().width - parseFloat(bs.paddingLeft) - parseFloat(bs.paddingRight));
  const 세로막대 = 반(window.innerWidth - document.documentElement.clientWidth);
  const 푸터 = document.querySelector("footer");

  // ① GNB 가 위에 붙어 있나
  const 상단바 = document.querySelector("header, nav");
  if (상단바) { const p = getComputedStyle(상단바).position;
    if (p !== "sticky" && p !== "fixed") 적기("고정", 이름(상단바) + " 가 position:" + p + " — 굴리면 따라 올라갑니다"); }

  // ② 본문과 푸터 사이가 붙었나 (그릇이 아니라 «안 마지막 알맹이»까지 잰다)
  if (푸터 && 푸터.previousElementSibling) {
    let 끝 = 푸터.previousElementSibling;
    if (보이나(끝) && !/sticky|fixed/.test(getComputedStyle(끝).position)) {
      for (let i = 0; i < 6; i++) {
        const 안 = [...끝.children].filter((c) => 보이나(c) && getComputedStyle(c).position === "static");
        if (!안.length) break; 끝 = 안[안.length - 1];
      }
      const 틈 = 반(푸터.getBoundingClientRect().top - 끝.getBoundingClientRect().bottom);
      if (틈 >= 0 && 틈 < 16) 적기("간격", "본문과 푸터 사이가 " + 틈 + "px — " + 이름(끝) + " 바로 아래 푸터");
    }
  }

  // ③ 덩어리 사이 간격이 그 칸의 «리듬»을 벗어났나
  for (const 부모 of [본문, ...본문.querySelectorAll("*")]) {
    if (!보이나(부모) || 부모.closest("table, svg")) continue;
    const ps = getComputedStyle(부모);
    if (/flex|grid/.test(ps.display) && ps.flexDirection !== "column") continue;
    const 아이 = [...부모.children].filter((c) => 보이나(c) && getComputedStyle(c).position !== "absolute");
    if (아이.length < 3) continue;
    const 틈들 = [];
    for (let i = 1; i < 아이.length; i++) {
      const a = 아이[i - 1].getBoundingClientRect(), b = 아이[i].getBoundingClientRect();
      if (b.top < a.bottom - 2) continue;
      틈들.push({ v: 반(b.top - a.bottom), 앞: 아이[i - 1], 뒤: 아이[i] });
    }
    if (틈들.length < 3) continue;
    const 벌 = 틈들.filter((x) => x.v >= 8);
    if (벌.length < 틈들.length * 0.6) continue;
    const 리듬 = 틈들.map((x) => x.v).sort((a, b) => a - b)[Math.floor(틈들.length / 2)];
    for (const x of 틈들) {
      const 붙 = x.v <= 2, 좁 = 리듬 >= 20 && x.v < 리듬 * 0.45;
      if (!붙 && !좁) continue;
      // 같은 «종류»가 이어지는 줄은 붙는 게 맞다. 뒤에 붙은 여백 클래스(mb3·total)까지
      // 견주면 "card mb3" 와 "card mb6" 를 서로 다른 것으로 봐서 헛짚는다.
      if ((x.앞.className || "").trim().split(/ +/)[0] === (x.뒤.className || "").trim().split(/ +/)[0]) continue;
      if (x.뒤 === 푸터) continue;
      if (!붙 && /^h[1-4]$/.test(x.앞.tagName.toLowerCase())) continue;  // 제목 밑 부제는 붙는 게 맞다
      적기("간격", 이름(x.뒤) + " 와 위 " + 이름(x.앞) + " 사이 " + x.v + "px (이 칸 리듬은 " + 리듬 + "px)");
    }
  }

  // ④ 버튼·배지가 늘어났나 / 동그란 버튼이 타원이 됐나
  const 부품 = [...document.querySelectorAll(".badge, .btn, button, [class*=badge], [class*=btn]")].filter((el) => {
    if (!보이나(el)) return false;
    // 버튼을 «담는 칸»(.btns 같은 것)은 부품이 아니다 — 넓은 게 맞다
    if (el.querySelector(".badge, .btn, button")) return false;
    // 일부러 칸을 꽉 채우라고 시킨 것
    if (/block|full|wide/.test(el.className || "")) return false;
    return true;
  });
  const 무리 = new Map();
  const 열쇠 = (el) => (el.className || "").trim().split(/ +/)[0] || el.tagName;
  for (const el of 부품) { const k = 열쇠(el); if (!무리.has(k)) 무리.set(k, []); 무리.get(k).push(el); }
  for (const el of 부품) {
    const s = getComputedStyle(el), r = el.getBoundingClientRect();
    if (s.borderTopLeftRadius.includes("%") && parseFloat(s.borderTopLeftRadius) >= 40 && r.width / r.height > 1.5)
      적기("버튼", 이름(el) + " 는 동그란 버튼인데 " + 반(r.width) + "x" + 반(r.height) + " 로 늘어나 타원이 됐습니다");
    const rg = document.createRange(); rg.selectNodeContents(el);
    const 글폭 = rg.getBoundingClientRect().width; if (!글폭) continue;
    const 또래 = 무리.get(열쇠(el)) || [];
    const 또래폭 = 또래.map((x) => x.getBoundingClientRect().width).sort((a, b) => a - b)[Math.floor(또래.length / 2)];
    if (또래.length >= 2 && r.width < 또래폭 * 2.5) continue;             // 다 같이 넓으면 일부러 그런 것
    const 남 = 반(r.width - 글폭 - parseFloat(s.paddingLeft) - parseFloat(s.paddingRight));
    if (남 > 140) 적기("늘어남", 이름(el) + " 가 " + 반(r.width) + "px 인데 글은 " + 반(글폭) +
      "px — 부모가 flex 세로칸이면 align-items 를 주세요");
  }

  // ⑤ 좌우로 미는 칸에 «막대»가 드러났나
  for (const el of document.querySelectorAll("*")) {
    if (!보이나(el)) continue;
    const s = getComputedStyle(el);
    if (!/auto|scroll/.test(s.overflowX) || el.scrollWidth <= el.clientWidth + 2) continue;
    const 두께 = el.offsetHeight - el.clientHeight - parseFloat(s.borderTopWidth) - parseFloat(s.borderBottomWidth);
    if (두께 > 2) 적기("막대", 이름(el) + " 에 가로 막대가 " + 반(두께) + "px 드러납니다");
  }

  // ⑥ 표가 제 칸을 넘쳤나
  for (const t of document.querySelectorAll("table")) {
    if (!보이나(t) || !t.parentElement) continue;
    const 넘침 = 반(t.scrollWidth - t.parentElement.clientWidth);
    if (넘침 > 4) 적기("표", "표가 제 칸보다 " + 넘침 + "px 넓습니다");
  }

  return JSON.stringify({
    화면: location.pathname.split("/").pop() || "(첫 화면)",
    콘텐츠폭, 세로막대,
    뒤로가기: !!document.querySelector(".back, [class*=back]"),
    탭이가는곳: [...document.querySelectorAll("[data-go], .tab[href], .tabs a")]
      .map((x) => x.getAttribute("data-go") || x.getAttribute("href")).filter(Boolean),
    흠,
  }, null, 1);
})()
```

**`흠` 에 담겨 나온 것은 그 화면에서 바로 고치세요.** 하나도 안 남을 때까지 다시 돌립니다.

**화면 «하나»로는 알 수 없는 것 셋** — 여러 화면의 결과를 모아 견주세요.
- **콘텐츠폭** 이 화면마다 다르면 옮길 때 글이 좌우로 밀립니다. 모든 화면이 한 값이어야 합니다.
- **세로막대** 가 어떤 화면은 0, 어떤 화면은 15 면 그만큼 화면이 좌우로 튑니다. `html { scrollbar-gutter: stable; }` 로 잡으세요.
- **뒤로가기** 가 `탭이가는곳` 으로 이어진 화면들 사이에서 갈리면 안 됩니다. 탭이면 넷 다 없는 쪽이 맞습니다.


### 7-9-2. 기계가 보는 검수 — 파일만 보고 재기
> 7-9 가 «그려 봐야 아는 것»을 잰다면, 이것은 **화면을 안 열어도 아는 것**을 잡습니다.
> 만든 폴더에서 한 번 돌리면 끊어진 링크·없는 그림·외톨이 화면이 한꺼번에 나옵니다.

```js
// node 로 돌리세요:  node 검수.mjs ./내사이트
import { readdirSync, readFileSync, existsSync, statSync } from "node:fs";
import { join, resolve, dirname } from "node:path";

const 방 = process.argv[2] || ".";
const 쪽들 = readdirSync(방).filter((f) => f.endsWith(".html"));
const 흠 = [];
const 이어진곳 = new Set();

for (const 쪽 of 쪽들) {
  const 길 = join(방, 쪽);
  const s = readFileSync(길, "utf8");

  // ① 끊어진 링크 — 같은 폴더의 .html 만 본다
  for (const m of s.matchAll(/href="([^"]+)"/g)) {
    const v = m[1];
    if (!v || v.startsWith("#") || v.startsWith("http") || v.startsWith("mailto:")) continue;
    const 갈곳 = resolve(dirname(길), v.split("#")[0].split("?")[0]);
    이어진곳.add(갈곳);
    if (v.endsWith(".html") && !existsSync(갈곳)) 흠.push("[끊어진 링크] " + 쪽 + " -> " + v);
  }

  // ② 없는 그림
  for (const m of s.matchAll(/src="([^"]+)"/g)) {
    const v = m[1];
    if (!v || v.startsWith("http") || v.startsWith("data:")) continue;
    if (!existsSync(resolve(dirname(길), v.split("?")[0]))) 흠.push("[없는 그림] " + 쪽 + " -> " + v);
  }

  // ③ 눌러도 아무 데도 안 가는 것
  const 빈링크 = (s.match(/href="(#|javascript:void\(0\))"/g) || []).length;
  if (빈링크) 흠.push("[빈 링크] " + 쪽 + " 에 " + 빈링크 + "개 — 아직 없는 화면이면 «흐리게 + 안 눌리게» 두세요");

  // ④ 0바이트
  if (statSync(길).size === 0) 흠.push("[빈 파일] " + 쪽);
}

// ⑤ 외톨이 화면 — 만들어 놓고 아무 데서도 안 이어지는 쪽
for (const 쪽 of 쪽들) {
  if (/^(index|home)\./i.test(쪽)) continue;
  if (!이어진곳.has(resolve(방, 쪽))) 흠.push("[외톨이] " + 쪽 + " 은 어느 화면에서도 이어지지 않습니다");
}

console.log("화면 " + 쪽들.length + "장 · 흠 " + 흠.length + "건");
for (const h of 흠) console.log("  " + h);
if (!흠.length) console.log("  깨끗합니다");
```

**글자 대비는 브라우저에서 잽니다** — 7-9 의 글과 같은 자리에 붙여 넣으세요.
본문 글자는 **4.5**, 24px 이상이거나 굵은 18.66px 이상은 **3.0** 을 넘어야 합니다.

### 7-10. 나온 오류는 «그 자리에서» 고칩니다
> 적어 두고 넘어가면 다음에 또 나옵니다. 찾은 김에 고치는 것이 제일 쌉니다.

- 고친 뒤에는 **그 화면을 다시 눌러** 정말 고쳐졌는지 봅니다. 고쳤다고 생각만 하고 넘어가는 일이 잦습니다.
- **한 화면에서 고친 것이 다른 화면을 깨뜨리지 않았는지** 봅니다. 공통 CSS·공통 스크립트를 고쳤다면 특히 그렇습니다.
  (짜임이 다른 화면 — 사이드바형과 상단바형 — 에 같은 규칙을 씌우면 한쪽이 무너집니다.)
- 같은 뿌리에서 난 것이면 **한 곳만 고치고 끝내지 말고** 같은 자리를 다 찾아 고칩니다.
- 고칠 수 없는 것(자료가 없어서, 화면이 아직 없어서)은 **화면에 솔직히 적습니다.** 「견본이라 어느 것을 눌러도 이 화면이 열립니다」처럼요.

### 7-11. 무엇을 확인했는지 적어 주세요
- 위 항목을 **숫자로** 적어 남깁니다 — 「눌러 본 화면 N개 · 빈 링크 0개 · 반응 없는 단추 0개 · 안 걸러지는 필터 0개 · 안 눌리는 썸네일·배너 0개 · 뒤로가기 어긋남 0개 · 화면 목록 단추 없는 화면 0개 · 화면끼리 안 맞는 숫자 0개 · 가로 넘침 0개 · **고친 것 N건**」.
- 숫자를 적을 수 없으면 아직 확인한 것이 아닙니다.

### 바꿀 값은 한 곳에
- **상호** · **주소** · **전화번호** · **이메일** · **영업시간** · **로고·대표 이미지** — 손님이 받아서 **제일 먼저 자기 것으로 바꾸는 값**입니다.
- 위 값은 **데이터 파일 한 곳**에 모으고, 화면은 전부 그 값을 불러 씁니다.
- 화면 코드·헤더·푸터는 물론 **페이지 제목(`<title>`)과 메타 설명까지** 그 값을 씁니다 — 여기가 제일 자주 빠집니다.
- 안내 문구 안에 상호가 섞여 있어도(예: 「내비게이션에 ‘○○’를 넣으세요」) 글자로 적지 말고 값을 끼워 넣습니다.
- 다 만든 뒤 **상호를 다른 이름으로 한 번 바꿔 보고** 옛 이름이 남는 곳이 있는지 찾아 보세요. 한 곳이라도 남으면 아직 안 끝난 것입니다.
### 파일을 어디에 두나

```
완성화면/
  index.html              홈 화면 (손님이 처음 보는 화면)
  화면목록.html            우리가 검수용으로 보는 «전체 화면 목록»
  pages/
    HO-01.html            화면 하나에 파일 하나 · 이름은 화면 ID 그대로
    CO-03.html
  assets/
    css/style.css         스타일 한 벌
    js/app.js             동작 한 벌
```

- **`pages/` 안에서는 `../assets/css/style.css` 로 부릅니다.** `assets/...` 라고 쓰면 `pages/assets/...` 를 찾다가 못 찾아서 **스타일이 통째로 안 붙습니다** — 글꼴도 여백도 격자도 전부 날아갑니다.
- ⭐ **`index.html` 은 «전체 화면 목록»입니다. 홈 화면이 아닙니다.** 이 폴더는 손님이 검수하는 **시안**이라, 열자마자 몇 개를 만들었는지 한눈에 보여야 합니다. 홈 화면은 `pages/` 안에 다른 화면들과 나란히 둡니다.
- **`index.html` 에는 화면을 «하나도 빼지 않고»** 메뉴별로 묶어 넣습니다. 화면 ID 와 이름을 함께 적어, 거기서 아무 화면이나 바로 열 수 있어야 합니다. 홈에서 닿는 것만 넣으면 나머지는 영영 못 봅니다.
- `index.html` 은 `완성화면/` 바로 아래 둡니다. 거기서는 `assets/...` 그대로 부르면 됩니다.
- **스타일과 동작은 각각 한 벌**입니다. 화면마다 따로 만들면 44장이 서로 달라집니다.
- 화면끼리 오갈 때는 `CO-03.html` 처럼 **같은 폴더 안 이름**으로 겁니다.
- 다 만든 뒤 **화면 하나를 브라우저로 직접 열어 보세요.** 글꼴이 기본 고딕으로 보이면 경로가 틀린 것입니다.

### 추천 스택
- 정적 다중 페이지(HTML/CSS) 또는 컴포넌트 기반(React·Next.js·Vue 등) 중 프로젝트 규모에 맞게 선택하세요. 화면이 많고 상태가 복잡하면 컴포넌트 기반을 권장합니다.
