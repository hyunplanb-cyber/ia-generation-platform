# 온라인 강의 플랫폼(LMS) — 상세 IA — AI 빌드 스펙팩

> 이 문서 하나로 사이트를 만들 수 있게 정리한 스펙이에요. 화면별 **생성 프롬프트**를 그대로 사용하세요.

## 1. 프로젝트 개요
- **컨셉**: 직장인과 학원 수강생을 위한 온라인 강의 플랫폼(LMS). 수강생은 강의를 신청해 영상으로 학습하고 과제를 제출하며 수료증을 받는다. 강사는 커리큘럼을 편성하고 영상을 올리며, 수강생의 진도·출결·과제를 관리하고 정산 내역을 확인한다.
- **디자인 컨셉**: 신뢰감 있는 네이비(#1B2A4A) + 포인트 오렌지(#FF7A30 — 배경·배지 전용, 글자에는 #A84B28), 카드형 레이아웃, 진도율은 막대 그래프로 시각화
- **디바이스**: 반응형
- **전체 일정**: 2026-08-03 ~ 2026-10-23

## 2. 공통 요소 (모든 화면 공통)
> 아래 요소는 **한 번만 만들어 모든 화면에서 재사용**하세요. 각 화면 스펙(4장)은 이 공통 요소 위에 올라가는 콘텐츠만 설명합니다.
- **영역이 둘입니다. 헤더를 각각 따로 만드세요.**
  - **손님 화면 GNB**: `홈` · `강의 탐색` · `내 강의실` · `마이페이지`
  - **운영자 화면 GNB**: `강사센터` · `수업 편성` · `학생 관리` · `출결·평가` — 좌측 사이드바 + 상단 바 형태가 맞습니다
  - **로그인·가입 화면**: `계정` — 헤더 없이 단독 화면으로 만드세요
  - 두 영역을 오가는 길은 **하나만** 둡니다 — 손님 화면 우측 상단에 운영자 영역으로 넘어가는 버튼 하나(예: `강사센터 화면으로`), 운영자 화면 우측 상단에 `고객 화면 보기`. 한 계정이 두 권한을 가지며(크몽·숨고와 같은 방식), 아직 권한이 없으면 전환 대신 **신청 화면으로 보냅니다.**
- **콘텐츠 영역**: 최대 1440px, 좌우 여백 24px — 헤더·본문·푸터·하단 고정 바가 모두 이 폭을 쓴다. 화면마다 따로 정하지 않는다. 화면마다 폭을 새로 정하면 위아래가 어긋납니다.
- **읽기 폭**: 글이 주인공인 자리(상세 본문·안내·약관)는 최대 760px로 좁혀 콘텐츠 영역 **안에서** 가운데 정렬 — 카드 격자에는 쓰지 않습니다. 아래 「읽기 폭」 참고.
- **헤더**: 로고 + 검색 + 상단 내비게이션(GNB)
- **푸터**: 저작권 · 기본 링크
- **재사용 컴포넌트**: 반복되는 UI(상품/항목 **카드**, 리스트 **행**, **폼 필드**, **버튼**)는 공용 컴포넌트로 만들어 화면마다 재사용하세요.

## 3. 메뉴 구조
- **[HO] 홈** (Home)
  - `HO0101` 홈 - 비로그인
  - `HO0102` 홈 - 비로그인 > 분야별 추천 탭
  - `HO0103` 홈 - 비로그인 > 검색어 자동완성
  - `HO0104` 홈 - 비로그인 > FAQ 아코디언 펼침
  - `HO0201` 홈 - 수강생 로그인
  - `HO0202` 홈 - 수강생 로그인 > 이어보기 없음
  - `HO0203` 홈 - 수강생 로그인 > 마감 임박 과제 알림
  - `HO0204` 홈 - 수강생 로그인 > 학습 습관 캘린더
- **[TC] 강사센터** (TeacherCenter)
  - `TC0101` 강사센터 홈
  - `TC0102` 강사센터 홈 > 수강 신청 추이 차트
  - `TC0103` 강사센터 홈 > 처리할 일 없음
  - `TC0201` 강사 지원
  - `TC0301` 강사 지원 - 심사 중
- **[CO] 강의 탐색** (Courses)
  - `CO0101` 강의 목록
  - `CO0102` 강의 목록 > 필터 적용 상태
  - `CO0103` 강의 목록 > 목록 로딩
  - `CO0104` 강의 목록 > 모바일 필터 시트
  - `CO0201` 강의 목록 - 검색 결과 없음
  - `CO0202` 강의 목록 - 검색 결과 없음 > 필터 과다 안내
  - `CO0301` 강의 상세
  - `CO0302` 강의 상세 > 커리큘럼 탭
  - `CO0303` 강의 상세 > 수강 후기 탭
  - `CO0304` 강의 상세 > 강사 소개 탭
  - `CO0305` 강의 상세 > 미리보기 재생 모달
  - `CO0306` 강의 상세 > 이미 수강 중 상태
  - `CO0401` 수강 신청 - 결제
  - `CO0402` 수강 신청 - 결제 > 쿠폰 적용 실패
  - `CO0403` 수강 신청 - 결제 > 환불 규정 미동의
  - `CO0404` 수강 신청 - 결제 > 결제 수단 확장
  - `CO0405` 수강 신청 - 결제 > 결제 진행 중
  - `CO0501` 수강 신청 - 완료
  - `CO0502` 수강 신청 - 완료 > 영수증 보기
  - `CO0601` 수강 신청 - 결제 실패
  - `CO0602` 수강 신청 - 결제 실패 > 사유별 변형
  - `CO0603` 수강 신청 - 결제 실패 > 중복 결제 감지
- **[CL] 내 강의실** (Classroom)
  - `CL0101` 내 강의실 - 수강 중
  - `CL0102` 내 강의실 - 수강 중 > 수강 완료 탭
  - `CL0103` 내 강의실 - 수강 중 > 수강 기간 만료 임박
  - `CL0104` 내 강의실 - 수강 중 > 수강 기간 만료
  - `CL0201` 내 강의실 - 비어 있음
  - `CL0202` 내 강의실 - 비어 있음 > 환불로 비워진 상태
  - `CL0301` 강의 재생
  - `CL0302` 강의 재생 > 강의 노트 탭
  - `CL0303` 강의 재생 > 첨부 자료 탭
  - `CL0304` 강의 재생 > 재생 설정
  - `CL0305` 강의 재생 > 차시 완료 처리
  - `CL0306` 강의 재생 > 동시 접속 차단
  - `CL0401` 강의 재생 - 오류
  - `CL0402` 강의 재생 - 오류 > 인코딩 미완료
  - `CL0403` 강의 재생 - 오류 > 재생 권한 없음
  - `CL0501` 과제 제출
  - `CL0502` 과제 제출 > 파일 용량 초과
  - `CL0503` 과제 제출 > 임시 저장 상태
  - `CL0504` 과제 제출 > 재제출 상태
  - `CL0505` 과제 제출 > 제출 완료
  - `CL0601` 과제 제출 - 마감됨
  - `CL0602` 과제 제출 - 마감됨 > 지각 제출 허용
  - `CL0701` 질문 게시판
  - `CL0702` 질문 게시판 > 질문 작성 폼
  - `CL0703` 질문 게시판 > 미답변만 보기
  - `CL0704` 질문 게시판 > 질문 없음
  - `CL0801` 수료증 발급
  - `CL0802` 수료증 발급 > 수료 조건 미충족
  - `CL0803` 수료증 발급 > 공유·다운로드
- **[CU] 수업 편성** (Curriculum)
  - `CU0101` 내 강의 목록
  - `CU0102` 내 강의 목록 > 상태별 필터
  - `CU0103` 내 강의 목록 > 개설 강의 없음
  - `CU0201` 강의 개설 - 기본 정보
  - `CU0202` 강의 개설 - 기본 정보 > 썸네일 업로드
  - `CU0203` 강의 개설 - 기본 정보 > 필수값 누락
  - `CU0204` 강의 개설 - 기본 정보 > 가격·할인 설정
  - `CU0301` 커리큘럼 편성
  - `CU0302` 커리큘럼 편성 > 순서 변경
  - `CU0303` 커리큘럼 편성 > 영상 미연결 경고
  - `CU0304` 커리큘럼 편성 > 차시 삭제 확인
  - `CU0305` 커리큘럼 편성 > 총 시간 요약
  - `CU0401` 영상 업로드
  - `CU0402` 영상 업로드 > 업로드 진행 중
  - `CU0403` 영상 업로드 > 인코딩 대기
  - `CU0501` 영상 업로드 - 실패
  - `CU0502` 영상 업로드 - 실패 > 실패 파일 부분 재시도
  - `CU0601` 강의 게시 - 검수 대기
  - `CU0602` 강의 게시 - 검수 대기 > 반려됨
- **[ST] 학생 관리** (Students)
  - `ST0101` 수강생 목록
  - `ST0102` 수강생 목록 > 진도 구간 필터
  - `ST0103` 수강생 목록 > 엑셀 다운로드
  - `ST0104` 수강생 목록 > 수강생 없음
  - `ST0201` 수강생 목록 - 검색 결과 없음
  - `ST0202` 수강생 목록 - 검색 결과 없음 > 검색 팁
  - `ST0301` 수강생 상세 - 진도 현황
  - `ST0302` 수강생 상세 - 진도 현황 > 과제 이력 탭
  - `ST0303` 수강생 상세 - 진도 현황 > 질문 이력 탭
  - `ST0304` 수강생 상세 - 진도 현황 > 학습 시간 추이
  - `ST0401` 진도 미달자 관리
  - `ST0402` 진도 미달자 관리 > 기준 설정
  - `ST0403` 진도 미달자 관리 > 대상자 없음
  - `ST0501` 알림 발송
  - `ST0502` 알림 발송 > 미리보기
  - `ST0503` 알림 발송 > 일부 발송 실패
- **[GR] 출결·평가** (Grading)
  - `GR0101` 출석 현황
  - `GR0102` 출석 현황 > 기간 필터
  - `GR0103` 출석 현황 > 출석 수정 모달
  - `GR0201` 과제 채점 목록
  - `GR0202` 과제 채점 목록 > 채점 완료 탭
  - `GR0203` 과제 채점 목록 > 채점 대기 없음
  - `GR0301` 과제 채점 - 상세
  - `GR0302` 과제 채점 - 상세 > 루브릭 점수 입력
  - `GR0303` 과제 채점 - 상세 > 첨부 파일 미리보기
  - `GR0304` 과제 채점 - 상세 > 미제출 학생
  - `GR0401` 수료 기준 설정
  - `GR0402` 수료 기준 설정 > 영향 인원 시뮬레이션
  - `GR0403` 수료 기준 설정 > 게시 후 기준 변경 경고
  - `GR0501` 수료 처리 - 확인
  - `GR0502` 수료 처리 - 확인 > 처리 실패
- **[MY] 마이페이지** (Mypage)
  - `MY0101` 내 정보
  - `MY0102` 내 정보 > 비밀번호 변경
  - `MY0103` 내 정보 > 강사 전환 신청
  - `MY0104` 내 정보 > 회원 탈퇴 확인
  - `MY0201` 수강 이력
  - `MY0202` 수강 이력 > 환불 신청
  - `MY0203` 수강 이력 > 환불 불가 상태
  - `MY0204` 수강 이력 > 기간 필터
  - `MY0301` 정산 내역
  - `MY0302` 정산 내역 > 정산 계좌 관리
  - `MY0303` 정산 내역 > 세금계산서 발행
  - `MY0401` 정산 내역 - 내역 없음
  - `MY0402` 정산 내역 - 내역 없음 > 첫 정산 예정 안내
- **[AU] 계정** (Auth)
  - `AU0101` 로그인
  - `AU0201` 회원가입
  - `AU0301` 회원가입 - 휴대폰 인증
  - `AU0401` 회원가입 완료
  - `AU0501` 비밀번호 찾기

## 4. 화면별 스펙
### HO0101 · 홈 - 비로그인
- **소속 메뉴**: 홈
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-08-03 ~ 2026-08-05
- **기능정의**: 상단 히어로 배너(핵심 카피 + 강의 검색창 + 수강생 수 지표) · 카테고리 바로가기 8종 아이콘 · 이번 주 인기 강의 8개 카드 · 신규 오픈 강의 캐러셀 · 기획전 배너 2단(신규 할인·패키지) · 분야별 추천 강의 탭 섹션 · 수강 후기 슬라이드 5개 · 강사 지원 유도 배너 · 자주 묻는 질문 아코디언 · 하단 회원가입 유도 CTA
- **버튼 → 이동화면**:
  - `강의 둘러보기` → `CO0101` 강의 목록
  - `로그인` → `MY0101` 내 정보
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `분야 카테고리` → 고른 분야에 표시가 남고, 아래 인기 강의 목록이 그 분야만 남게 걸러진다
  - `인기 검색어 칩` → 검색창에 그 말이 채워지고 아래 추천이 그에 맞게 바뀐다
  - `자주 묻는 질문` → 답이 펼쳐지고 다시 누르면 접힌다. 한 번에 하나만 열린다
  - `가로 목록 좌우 화살표` → 카드가 한 칸씩 밀린다. 끝에 닿으면 그쪽 화살표가 흐려진다
- **생성 프롬프트**:

  ```
  온라인 강의 플랫폼의 비로그인 홈 화면을 만들어줘. 스크롤 순서대로 아래 섹션을 위에서 아래로 쌓아줘. 1) 상단 GNB(로고, 강의 둘러보기, 강사센터, 로그인, 회원가입) 2) 히어로 배너 — 핵심 카피, 강의 검색창, "누적 수강생 3만 명" 같은 신뢰 지표 3) 카테고리 바로가기 8종 아이콘 그리드 4) 이번 주 인기 강의 8개 카드(썸네일, 강의명, 강사명, 평점, 수강생 수, 가격) 5) 신규 오픈 강의 가로 캐러셀 6) 기획전 배너 2단(신규 가입 할인 / 패키지 강의) 7) 분야별 추천 강의 — 탭으로 분야 전환되는 섹션 8) 수강 후기 슬라이드 5개(프로필, 별점, 한 줄 후기) 9) 강사 지원 유도 배너 10) 자주 묻는 질문 아코디언 11) 하단 회원가입 유도 CTA와 푸터 카드형 레이아웃. 모바일에서는 카드 1열, 캐러셀은 스와이프.
  ```

### HO0102 · 홈 - 비로그인 > 분야별 추천 탭
- **소속 메뉴**: 홈
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-08-06 ~ 2026-08-06
- **기능정의**: 탭 전환 시 추천 강의 목록 갱신 · 탭별 인기 강의 6개 · 더보기로 목록 이동 · 선택한 탭 스크롤 위치 유지 · 신규 분야 NEW 배지
- **생성 프롬프트**:

  ```
  '홈 - 비로그인' 화면의 '분야별 추천 탭' 탭을 만들어줘. 탭 전환 시 추천 강의 목록 갱신· 탭별 인기 강의 6개· 더보기로 목록 이동· 선택한 탭 스크롤 위치 유지· 신규 분야 NEW 배지. 상위 화면(홈 - 비로그인)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### HO0103 · 홈 - 비로그인 > 검색어 자동완성
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈 - 비로그인` — 누르면 `HO0101`로 간다
- **일정**: 2026-08-07 ~ 2026-08-10
- **기능정의**: 히어로 검색창 입력 중 강의명·강사명 제안 · 인기 검색어 노출 · 최근 검색어 개별 삭제 · 엔터 시 목록 화면 이동 · 결과 없음 문구
- **생성 프롬프트**:

  ```
  '홈 - 비로그인' 화면의 '검색어 자동완성' 상태·세부 화면을 만들어줘. 히어로 검색창 입력 중 강의명·강사명 제안· 인기 검색어 노출· 최근 검색어 개별 삭제· 엔터 시 목록 화면 이동· 결과 없음 문구. 상위 화면(홈 - 비로그인)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### HO0104 · 홈 - 비로그인 > FAQ 아코디언 펼침
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈 - 비로그인` — 누르면 `HO0101`로 간다
- **일정**: 2026-08-11 ~ 2026-08-12
- **기능정의**: 질문 클릭 시 답변 펼침 · 결제·환불·수료 카테고리 구분 · 도움됨 표시 · 고객센터 문의 링크 · 한 번에 하나만 열기
- **생성 프롬프트**:

  ```
  '홈 - 비로그인' 화면의 'FAQ 아코디언 펼침' 상태·세부 화면을 만들어줘. 질문 클릭 시 답변 펼침· 결제·환불·수료 카테고리 구분· 도움됨 표시· 고객센터 문의 링크· 한 번에 하나만 열기. 상위 화면(홈 - 비로그인)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### HO0201 · 홈 - 수강생 로그인
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈 - 비로그인` — 누르면 `HO0101`로 간다
- **일정**: 2026-08-13 ~ 2026-08-17
- **기능정의**: 이어보기 카드(마지막 시청 강의·현재 차시·진도율) 최상단 고정 · 이번 주 학습 요약 지표 3종(학습 시간·완료 차시·연속 학습일) · 수강 중 강의 목록 카드 · 마감 임박 과제 알림 리스트 · 미답변 질문 알림 · 수료 임박 강의 안내 배너 · 학습 습관 캘린더(잔디 그래프) · 추천 강의 캐러셀 · 관심 분야 신규 강의 · 하단 수강 후기 작성 유도 CTA
- **버튼 → 이동화면**:
  - `이어보기` → `CL0301` 강의 재생
  - `내 강의실` → `CL0101` 내 강의실 - 수강 중
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `기간 탭(이번 주/이번 달)` → 학습 시간·완료 차시 숫자와 그래프가 그 기간 것으로 바뀐다
  - `「이어보기」가 있는 강의 카드` → 고른 강의가 위 큰 카드로 올라오고 진도 막대가 그 강의 것으로 바뀐다
  - `학습 습관 잔디의 한 칸` → 그날 무엇을 얼마나 들었는지가 그 자리에 뜬다
- **생성 프롬프트**:

  ```
  수강생이 로그인한 상태의 홈(대시보드) 화면을 만들어줘. 스크롤 순서대로 아래 섹션을 배치해줘. 1) 인사말과 함께 "이어보기" 대형 카드 — 썸네일, 강의명, 현재 차시, 진도율 막대, 이어보기 버튼 2) 이번 주 학습 요약 지표 3종(이번 주 학습 시간, 완료한 차시 수, 연속 학습일) — 숫자 크게 3) 수강 중인 강의 목록 카드(각 카드에 진도율 막대와 남은 차시) 4) 마감 임박 과제 알림 리스트(D-day 배지) 5) 내 질문 중 미답변 항목 알림 6) 수료 조건 임박 강의 안내 배너 7) 학습 습관 캘린더 — 잔디 그래프 형태로 최근 3개월 학습일 표시 8) 추천 강의 캐러셀 9) 관심 분야 신규 강의 섹션 10) 하단에 수강 후기 작성 유도 CTA 진도율과 지표는 막대와 강조색으로 표현해줘.
  ```

### HO0202 · 홈 - 수강생 로그인 > 이어보기 없음
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈 - 수강생 로그인` — 누르면 `HO0201`로 간다
- **일정**: 2026-08-18 ~ 2026-08-18
- **기능정의**: 최근 시청 기록 없음 안내 · 수강 중 강의 첫 차시 바로가기 · 강의 둘러보기 유도 · 학습 목표 설정 안내
- **생성 프롬프트**:

  ```
  '홈 - 수강생 로그인' 화면의 '이어보기 없음' 상태·세부 화면을 만들어줘. 최근 시청 기록 없음 안내· 수강 중 강의 첫 차시 바로가기· 강의 둘러보기 유도· 학습 목표 설정 안내. 상위 화면(홈 - 수강생 로그인)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### HO0203 · 홈 - 수강생 로그인 > 마감 임박 과제 알림
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈 - 수강생 로그인` — 누르면 `HO0201`로 간다
- **일정**: 2026-08-19 ~ 2026-08-19
- **기능정의**: D-1 이하 빨강 강조 · 과제 제출 화면 바로가기 · 마감 지난 항목 회색 처리 · 알림 다시 받기 · 전체 과제 보기
- **생성 프롬프트**:

  ```
  '홈 - 수강생 로그인' 화면의 '마감 임박 과제 알림' 상태·세부 화면을 만들어줘. D-1 이하 빨강 강조· 과제 제출 화면 바로가기· 마감 지난 항목 회색 처리· 알림 다시 받기· 전체 과제 보기. 상위 화면(홈 - 수강생 로그인)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### HO0204 · 홈 - 수강생 로그인 > 학습 습관 캘린더
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈 - 수강생 로그인` — 누르면 `HO0201`로 간다
- **일정**: 2026-08-20 ~ 2026-08-20
- **기능정의**: 최근 3개월 잔디 그래프 · 날짜별 학습 시간 툴팁 · 연속 학습일 최고 기록 · 월 이동 · 미학습일 회색 처리
- **생성 프롬프트**:

  ```
  '홈 - 수강생 로그인' 화면의 '학습 습관 캘린더' 상태·세부 화면을 만들어줘. 최근 3개월 잔디 그래프· 날짜별 학습 시간 툴팁· 연속 학습일 최고 기록· 월 이동· 미학습일 회색 처리. 상위 화면(홈 - 수강생 로그인)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### TC0101 · 강사센터 홈
- **소속 메뉴**: 강사센터
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-08-21 ~ 2026-08-25
- **기능정의**: 핵심 지표 카드 4종(개설 강의 수·총 수강생·채점 대기·이번 달 정산액) · 처리가 필요한 일 목록(검수 대기·채점 대기·진도 미달) · 최근 30일 수강 신청 추이 차트 · 강의별 성과 요약 테이블(수강생·평점·완주율) · 최근 수강 후기 5건 · 미답변 질문 목록 · 진도 미달 학생 요약과 일괄 독려 진입 · 이번 달 정산 예정 요약 · 빠른 실행 버튼 3종 · 플랫폼 공지사항
- **버튼 → 이동화면**:
  - `새 강의 개설` → `CU0201` 강의 개설 - 기본 정보
  - `학생 관리` → `ST0101` 수강생 목록
  - `채점하기` → `GR0201` 과제 채점 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `기간 탭(7일/30일/90일)` → 추이 차트와 그 옆 합계·증감 숫자가 함께 바뀐다
  - `「처리가 필요한 일」의 처리 버튼` → 그 줄이 목록에서 빠지고 위의 건수가 하나 줄어든다
  - `지표 카드` → 아래 표가 그 지표 기준으로 다시 정렬된다
- **생성 프롬프트**:

  ```
  강사가 로그인한 상태의 홈(대시보드) 화면을 만들어줘. 스크롤 순서대로 아래 섹션을 배치해줘. 1) 상단 요약 지표 카드 4개 — 개설 강의 수, 총 수강생, 채점 대기 과제, 이번 달 예상 정산액(숫자 크게) 2) "처리가 필요한 일" 목록 — 검수 대기 강의, 채점 대기 과제, 진도 미달 학생을 건수와 함께 바로가기로 3) 최근 30일 수강 신청 추이 라인 차트 4) 강의별 성과 요약 테이블 — 강의명, 수강생 수, 평균 평점, 완주율 5) 최근 수강 후기 5건(별점, 한 줄 요약, 강의명) 6) 미답변 질문 목록 7) 진도 미달 학생 요약과 "일괄 독려 메시지 보내기" 진입 버튼 8) 이번 달 정산 예정 요약 카드 9) 우측 또는 상단에 빠른 실행 버튼 3종(새 강의 개설, 학생 관리, 채점하기) 10) 하단 플랫폼 공지사항 네이비 배경 카드형, 지표 숫자는 오렌지로 강조해줘.
  ```

### TC0102 · 강사센터 홈 > 수강 신청 추이 차트
- **소속 메뉴**: 강사센터
- **뒤로가기**: 좌측 상단에 `‹ 강사센터 홈` — 누르면 `TC0101`로 간다
- **일정**: 2026-08-26 ~ 2026-08-27
- **기능정의**: 최근 30일 라인 차트 · 기간 7·30·90일 전환 · 강의별 필터 · 전일 대비 증감 표시 · 데이터 없는 구간 안내
- **생성 프롬프트**:

  ```
  '강사센터 홈' 화면의 '수강 신청 추이 차트' 상태·세부 화면을 만들어줘. 최근 30일 라인 차트· 기간 7·30·90일 전환· 강의별 필터· 전일 대비 증감 표시· 데이터 없는 구간 안내. 상위 화면(강사센터 홈)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### TC0103 · 강사센터 홈 > 처리할 일 없음
- **소속 메뉴**: 강사센터
- **뒤로가기**: 좌측 상단에 `‹ 강사센터 홈` — 누르면 `TC0101`로 간다
- **일정**: 2026-08-28 ~ 2026-08-31
- **기능정의**: 검수·채점·독려 대기 0건 안내 · 새 강의 개설 유도 · 지난주 성과 요약 · 강의 홍보 팁 링크
- **생성 프롬프트**:

  ```
  '강사센터 홈' 화면의 '처리할 일 없음' 상태·세부 화면을 만들어줘. 검수·채점·독려 대기 0건 안내· 새 강의 개설 유도· 지난주 성과 요약· 강의 홍보 팁 링크. 상위 화면(강사센터 홈)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### TC0201 · 강사 지원
- **소속 메뉴**: 강사센터
- **뒤로가기**: 좌측 상단에 `‹ 강사센터 홈` — 누르면 `TC0101`로 간다
- **일정**: 2026-09-01 ~ 2026-09-03
- **기능정의**: 지원 안내와 심사 기준 · 활동명·소개·전문 분야 입력 · 경력·자격 증빙 파일 업로드 · 개설 예정 강의 주제 · 정산 계좌 등록 · 강사 이용약관 동의 · 심사 소요 기간 안내 · 임시 저장
- **버튼 → 이동화면**:
  - `지원 현황` → `TC0301` 강사 지원 - 심사 중
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `전문 분야 고르기(복수)` → 고른 것이 칩으로 쌓이고 다시 누르면 빠진다
  - `증빙 파일 끌어다 놓기` → 파일 줄이 늘어나고 「N/5개」가 올라간다
  - `필수 칸이 빈 상태` → 「제출」이 눌리지 않고, 어느 칸이 비었는지 표시된다
- **생성 프롬프트**:

  ```
  수강생이 강사가 되겠다고 신청하는 화면을 만들어줘. 상단에 "강사로 활동해 보세요" 안내와 심사 기준 3가지를 카드로. 활동명, 한 줄 소개, 전문 분야 선택(복수), 경력 입력칸, 자격·경력 증빙 파일 업로드 영역(드래그 앤 드롭, 최대 5개), 개설하고 싶은 강의 주제 입력, 정산 계좌 등록(은행 선택·계좌번호·예금주), 강사 이용약관 동의 체크박스. 하단에 "심사는 영업일 기준 3~5일 걸려요" 안내와 "임시 저장", "지원서 제출" 버튼.
  ```

### TC0301 · 강사 지원 - 심사 중
- **소속 메뉴**: 강사센터
- **뒤로가기**: 좌측 상단에 `‹ 강사센터 홈` — 누르면 `TC0101`로 간다
- **일정**: 2026-09-04 ~ 2026-09-07
- **기능정의**: 심사 진행 단계 표시(접수·서류 확인·최종 승인) · 접수 일시와 예상 완료일 · 제출한 내용 다시 보기 · 보완 요청이 왔을 때 안내와 재제출 · 지원 취소 · 심사 중에도 수강생으로 이용 가능 안내
- **버튼 → 이동화면**:
  - `강의 탐색` → `CO0101` 강의 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「심사 단계」` → 각 단계에서 무엇을 보는지 설명이 펼쳐진다
- **생성 프롬프트**:

  ```
  강사 지원 심사 중 화면을 만들어줘. 가운데에 진행 단계 표시(1 접수 완료 → 2 서류 확인 중 → 3 최종 승인), 현재 2단계를 강조하고 접수 일시와 "9월 18일쯤 끝나요" 예상 완료일. 아래에 제출한 내용 요약과 "지원서 다시 보기" 링크, 보완 요청이 온 상태(주황 안내 상자 + 요청 사유 + "다시 제출하기" 버튼)도 함께 만들어줘. 맨 아래 "심사 중에도 수강생으로 강의를 들으실 수 있어요" 안내와 "지원 취소" 링크.
  ```

### CO0101 · 강의 목록
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-09-08 ~ 2026-09-10
- **기능정의**: 카테고리·난이도·가격대 필터 · 정렬 드롭다운(인기순/최신순/평점순) · 강의 카드 그리드 12개씩 · 무한 스크롤 또는 페이지네이션 · 검색어 입력창
- **버튼 → 이동화면**:
  - `강의 상세` → `CO0301` 강의 상세
  - `검색` → `CO0201` 강의 목록 - 검색 결과 없음
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `왼쪽 필터(분야·난이도·가격)` → 고른 조건이 위에 칩으로 쌓이고, 강의 목록과 「N개」 숫자가 함께 바뀐다
  - `정렬 드롭다운(인기·최신·평점·낮은 가격)` → 카드 순서가 실제로 다시 늘어선다
  - `칩의 ✕` → 그 조건만 풀리고 목록이 다시 늘어난다
  - `「필터 적용」·「전체 초기화」` → 고른 조건이 목록에 반영되거나 모두 지워진다
  - `페이지 번호` → 그 페이지 목록으로 바뀌고 눌린 번호에 표시가 남는다
- **생성 프롬프트**:

  ```
  강의 목록 화면을 만들어줘. 좌측에 필터 사이드바(카테고리 체크박스, 난이도, 가격대 슬라이더, 무료만 보기), 우측에 강의 카드 그리드(3열, 썸네일/강의명/강사명/별점/수강생수/가격). 상단에 검색창과 정렬 드롭다운(인기순, 최신순, 평점순). 하단 페이지네이션. 모바일에서는 필터를 하단 시트로 전환.
  ```

### CO0102 · 강의 목록 > 필터 적용 상태
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 강의 목록` — 누르면 `CO0101`로 간다
- **일정**: 2026-09-11 ~ 2026-09-11
- **기능정의**: 선택 필터 칩 노출 · 결과 수 표시 · 칩 개별 해제 · 전체 초기화 · 무료만 보기 토글
- **생성 프롬프트**:

  ```
  '강의 목록' 화면의 '필터 적용 상태' 상태·세부 화면을 만들어줘. 선택 필터 칩 노출· 결과 수 표시· 칩 개별 해제· 전체 초기화· 무료만 보기 토글. 상위 화면(강의 목록)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CO0103 · 강의 목록 > 목록 로딩
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 강의 목록` — 누르면 `CO0101`로 간다
- **일정**: 2026-09-14 ~ 2026-09-14
- **기능정의**: 카드 자리 스켈레톤 12개 · 필터 사이드바 유지 · 다음 페이지 추가 로딩 스피너 · 정렬 변경 시 상단으로 스크롤
- **생성 프롬프트**:

  ```
  '강의 목록' 화면의 '목록 로딩' 상태·세부 화면을 만들어줘. 카드 자리 스켈레톤 12개· 필터 사이드바 유지· 다음 페이지 추가 로딩 스피너· 정렬 변경 시 상단으로 스크롤. 상위 화면(강의 목록)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CO0104 · 강의 목록 > 모바일 필터 시트
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 강의 목록` — 누르면 `CO0101`로 간다
- **일정**: 2026-09-15 ~ 2026-09-16
- **기능정의**: 하단 시트로 필터 열기 · 카테고리 아코디언 · 가격 슬라이더 · 적용 전 결과 수 미리보기 · 초기화·적용 버튼
- **생성 프롬프트**:

  ```
  '강의 목록' 화면의 '모바일 필터 시트' 상태·세부 화면을 만들어줘. 하단 시트로 필터 열기· 카테고리 아코디언· 가격 슬라이더· 적용 전 결과 수 미리보기· 초기화·적용 버튼. 상위 화면(강의 목록)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CO0201 · 강의 목록 - 검색 결과 없음
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 강의 목록` — 누르면 `CO0101`로 간다
- **일정**: 2026-09-17 ~ 2026-09-18
- **기능정의**: 검색어를 포함한 안내 문구 표시 · 일러스트 또는 아이콘 · 필터 초기화 버튼 · 추천 인기 강의 4개 노출 · 검색어 수정 유도
- **버튼 → 이동화면**:
  - `필터 초기화` → `CO0101` 강의 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `걸린 조건 칩의 ✕` → 그 조건이 풀리면서 결과 수가 0에서 늘어나고 빈 화면 대신 목록이 나타난다
  - `「조건 모두 풀기」` → 칩이 전부 사라지고 전체 강의가 나온다
- **생성 프롬프트**:

  ```
  강의 검색 결과가 없을 때의 빈 화면을 만들어줘. 중앙에 아이콘과 "'{검색어}'에 대한 강의를 찾지 못했어요" 문구, "필터 초기화" 버튼, 그 아래 "이런 강의는 어때요?" 추천 강의 4개 카드. 사용자가 막다른 길에 갇히지 않도록 다음 행동을 반드시 제시해줘.
  ```

### CO0202 · 강의 목록 - 검색 결과 없음 > 필터 과다 안내
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 강의 목록 - 검색 결과 없음` — 누르면 `CO0201`로 간다
- **일정**: 2026-09-21 ~ 2026-09-21
- **기능정의**: 결과를 0건으로 만든 필터 조건 지목 · 조건 하나씩 풀기 제안 · 유사 검색어 추천 · 인기 강의 4개 · 신규 강의 알림 신청
- **생성 프롬프트**:

  ```
  '강의 목록 - 검색 결과 없음' 화면의 '필터 과다 안내' 상태·세부 화면을 만들어줘. 결과를 0건으로 만든 필터 조건 지목· 조건 하나씩 풀기 제안· 유사 검색어 추천· 인기 강의 4개· 신규 강의 알림 신청. 상위 화면(강의 목록 - 검색 결과 없음)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CO0301 · 강의 상세
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 강의 목록` — 누르면 `CO0101`로 간다
- **일정**: 2026-09-22 ~ 2026-09-23
- **기능정의**: 강의 소개 영상 미리보기 · 커리큘럼 목록(챕터/차시, 각 영상 길이) · 강사 프로필 · 수강 후기와 별점 분포 · 수강 신청 고정 버튼(가격·할인)
- **버튼 → 이동화면**:
  - `수강 신청` → `CO0401` 수강 신청 - 결제
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `커리큘럼 챕터 줄` → 그 챕터의 차시 목록이 펼쳐지고 다시 누르면 접힌다
  - `후기 별점 필터(전체/5점/4점/3점 이하)` → 후기 목록과 개수가 함께 걸러진다
  - `「후기 더 보기」` → 후기가 20개 더 붙고, 더 없으면 버튼이 사라진다
  - `찜 아이콘` → 채워지고 다시 누르면 비워진다
  - `자주 묻는 질문` → 답이 펼쳐지고 다시 누르면 접힌다
- **생성 프롬프트**:

  ```
  강의 상세 화면을 만들어줘. 상단에 소개 영상 플레이어와 강의명/강사/평점/수강생수, 본문 탭(강의 소개, 커리큘럼, 강사 소개, 수강 후기). 커리큘럼은 챕터별 아코디언으로 차시 제목과 영상 길이를 보여주고 미리보기 가능 차시는 재생 아이콘 표시. 우측(모바일은 하단 고정)에 가격, 할인율, 수강 신청 버튼이 있는 스티키 카드.
  ```

### CO0302 · 강의 상세 > 커리큘럼 탭
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 강의 목록` — 누르면 `CO0101`로 간다
- **일정**: 2026-09-24 ~ 2026-09-25
- **기능정의**: 챕터별 아코디언 · 차시명과 영상 길이 · 미리보기 가능 차시 재생 아이콘 · 총 차시·총 시간 요약 · 전체 펼치기
- **생성 프롬프트**:

  ```
  '강의 상세' 화면의 '커리큘럼 탭' 탭을 만들어줘. 챕터별 아코디언· 차시명과 영상 길이· 미리보기 가능 차시 재생 아이콘· 총 차시·총 시간 요약· 전체 펼치기. 상위 화면(강의 상세)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CO0303 · 강의 상세 > 수강 후기 탭
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 강의 목록` — 누르면 `CO0101`로 간다
- **일정**: 2026-09-28 ~ 2026-09-28
- **기능정의**: 별점 분포 막대 · 포토 후기 그리드 · 정렬(최신·평점·도움순) · 도움돼요 · 강사 답글 표시
- **생성 프롬프트**:

  ```
  '강의 상세' 화면의 '수강 후기 탭' 탭을 만들어줘. 별점 분포 막대· 포토 후기 그리드· 정렬(최신·평점·도움순)· 도움돼요· 강사 답글 표시. 상위 화면(강의 상세)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CO0304 · 강의 상세 > 강사 소개 탭
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 강의 목록` — 누르면 `CO0101`로 간다
- **일정**: 2026-09-29 ~ 2026-09-30
- **기능정의**: 강사 경력·이력 · 평균 평점과 누적 수강생 수 · 다른 개설 강의 목록 · 강사에게 질문 · 팔로우
- **생성 프롬프트**:

  ```
  '강의 상세' 화면의 '강사 소개 탭' 탭을 만들어줘. 강사 경력·이력· 평균 평점과 누적 수강생 수· 다른 개설 강의 목록· 강사에게 질문· 팔로우. 상위 화면(강의 상세)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CO0305 · 강의 상세 > 미리보기 재생 모달
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 강의 상세` — 누르면 `CO0301`로 간다
- **일정**: 2026-10-01 ~ 2026-10-02
- **기능정의**: 무료 공개 차시 영상 재생 · 배속·자막 지원 · 남은 미리보기 차시 안내 · 수강 신청 유도 · 닫기
- **생성 프롬프트**:

  ```
  '강의 상세' 화면의 '미리보기 재생 모달' 상태·세부 화면을 만들어줘. 무료 공개 차시 영상 재생· 배속·자막 지원· 남은 미리보기 차시 안내· 수강 신청 유도· 닫기. 상위 화면(강의 상세)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CO0306 · 강의 상세 > 이미 수강 중 상태
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 강의 상세` — 누르면 `CO0301`로 간다
- **일정**: 2026-10-05 ~ 2026-10-05
- **기능정의**: 수강 신청 버튼이 이어보기로 전환 · 현재 진도율 표시 · 내 강의실 이동 · 중복 결제 불가 안내 · 수강 기간 만료일 표시
- **생성 프롬프트**:

  ```
  '강의 상세' 화면의 '이미 수강 중 상태' 상태·세부 화면을 만들어줘. 수강 신청 버튼이 이어보기로 전환· 현재 진도율 표시· 내 강의실 이동· 중복 결제 불가 안내· 수강 기간 만료일 표시. 상위 화면(강의 상세)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CO0401 · 수강 신청 - 결제
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 강의 목록` — 누르면 `CO0101`로 간다
- **일정**: 2026-10-06 ~ 2026-10-07
- **기능정의**: 선택 강의 요약 · 쿠폰 코드 입력과 적용 · 결제 수단 선택(카드/간편결제) · 최종 결제 금액 계산 표시 · 환불 규정 동의 체크박스
- **버튼 → 이동화면**:
  - `결제하기` → `CO0501` 수강 신청 - 완료
  - `결제 실패` → `CO0601` 수강 신청 - 결제 실패
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `쿠폰 고르기` → 쿠폰 할인 줄과 최종 결제 금액이 그 자리에서 다시 계산된다
  - `결제 수단 고르기` → 고른 것에 표시가 남고, 카드면 할부 개월 칸이 나타난다
  - `할부 개월 칩` → 고른 개월에 표시가 남고 월 납입액이 계산돼 뜬다
  - `환불 규정 동의 체크` → 체크해야 「결제하기」가 눌리게 켜진다. 풀면 다시 잠긴다
- **생성 프롬프트**:

  ```
  수강 신청 결제 화면을 만들어줘. 좌측에 신청 강의 요약 카드, 쿠폰 입력창과 적용 버튼, 결제 수단 선택(신용카드, 카카오페이, 계좌이체 라디오), 우측에 금액 요약(강의 금액, 쿠폰 할인, 최종 결제 금액을 크게). 하단에 환불 규정 동의 체크박스와 결제하기 버튼. 동의 전에는 버튼 비활성.
  ```

### CO0402 · 수강 신청 - 결제 > 쿠폰 적용 실패
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 수강 신청 - 결제` — 누르면 `CO0401`로 간다
- **일정**: 2026-10-08 ~ 2026-10-09
- **기능정의**: 유효하지 않은 코드·기간 만료·중복 사용 사유 구분 표시 · 입력값 유지 · 보유 쿠폰 목록 열기 · 다시 입력 유도
- **생성 프롬프트**:

  ```
  '수강 신청 - 결제' 화면의 '쿠폰 적용 실패' 상태·세부 화면을 만들어줘. 유효하지 않은 코드·기간 만료·중복 사용 사유 구분 표시· 입력값 유지· 보유 쿠폰 목록 열기· 다시 입력 유도. 상위 화면(수강 신청 - 결제)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CO0403 · 수강 신청 - 결제 > 환불 규정 미동의
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 수강 신청 - 결제` — 누르면 `CO0401`로 간다
- **일정**: 2026-10-12 ~ 2026-10-12
- **기능정의**: 필수 동의 미체크 강조 · 결제 버튼 비활성 · 규정 전문 펼치기 · 해당 항목으로 스크롤 이동
- **생성 프롬프트**:

  ```
  '수강 신청 - 결제' 화면의 '환불 규정 미동의' 상태·세부 화면을 만들어줘. 필수 동의 미체크 강조· 결제 버튼 비활성· 규정 전문 펼치기· 해당 항목으로 스크롤 이동. 상위 화면(수강 신청 - 결제)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CO0404 · 수강 신청 - 결제 > 결제 수단 확장
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 수강 신청 - 결제` — 누르면 `CO0401`로 간다
- **일정**: 2026-10-13 ~ 2026-10-13
- **기능정의**: 카드사 선택과 할부 개월 · 간편결제 계정 연결 · 계좌이체 은행 선택 · 저장된 수단 불러오기 · 현금영수증 정보 입력
- **생성 프롬프트**:

  ```
  '수강 신청 - 결제' 화면의 '결제 수단 확장' 상태·세부 화면을 만들어줘. 카드사 선택과 할부 개월· 간편결제 계정 연결· 계좌이체 은행 선택· 저장된 수단 불러오기· 현금영수증 정보 입력. 상위 화면(수강 신청 - 결제)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CO0405 · 수강 신청 - 결제 > 결제 진행 중
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 수강 신청 - 결제` — 누르면 `CO0401`로 간다
- **일정**: 2026-10-14 ~ 2026-10-14
- **기능정의**: 결제창 호출 대기 오버레이 · 중복 클릭 차단 · 창을 닫지 말라는 안내 · 취소 버튼 · 응답 지연 시 안내 문구
- **생성 프롬프트**:

  ```
  '수강 신청 - 결제' 화면의 '결제 진행 중' 상태·세부 화면을 만들어줘. 결제창 호출 대기 오버레이· 중복 클릭 차단· 창을 닫지 말라는 안내· 취소 버튼· 응답 지연 시 안내 문구. 상위 화면(수강 신청 - 결제)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CO0501 · 수강 신청 - 완료
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 강의 목록` — 누르면 `CO0101`로 간다
- **일정**: 2026-10-15 ~ 2026-10-16
- **기능정의**: 결제 완료 메시지와 주문번호 · 신청 강의 정보 요약 · 영수증 보기 링크 · 바로 학습 시작 버튼 · 내 강의실 이동 버튼
- **버튼 → 이동화면**:
  - `바로 학습 시작` → `CL0301` 강의 재생
  - `내 강의실` → `CL0101` 내 강의실 - 수강 중
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `주문번호` → 눌러서 복사되고 「복사했어요」가 뜬다
  - `「영수증 보기」` → 영수증 내역이 그 자리에서 펼쳐진다
- **생성 프롬프트**:

  ```
  수강 신청 완료 화면을 만들어줘. 중앙에 성공 체크 아이콘과 "수강 신청이 완료됐어요" 문구, 주문번호와 결제 금액, 신청한 강의 카드, 큰 CTA "바로 학습 시작하기"와 보조 버튼 "내 강의실 가기", 하단에 영수증 보기 링크.
  ```

### CO0502 · 수강 신청 - 완료 > 영수증 보기
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 수강 신청 - 완료` — 누르면 `CO0501`로 간다
- **일정**: 2026-10-19 ~ 2026-10-20
- **기능정의**: 주문번호·결제일시·금액 내역 · 카드 승인번호 · 현금영수증 발행 · 인쇄와 PDF 저장
- **생성 프롬프트**:

  ```
  '수강 신청 - 완료' 화면의 '영수증 보기' 상태·세부 화면을 만들어줘. 주문번호·결제일시·금액 내역· 카드 승인번호· 현금영수증 발행· 인쇄와 PDF 저장. 상위 화면(수강 신청 - 완료)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CO0601 · 수강 신청 - 결제 실패
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 강의 목록` — 누르면 `CO0101`로 간다
- **일정**: 2026-10-21 ~ 2026-10-22
- **기능정의**: 실패 사유 안내(한도 초과·카드 오류 등) · 다시 시도 버튼 · 다른 결제 수단 선택 유도 · 고객센터 문의 링크 · 장바구니 유지 안내
- **버튼 → 이동화면**:
  - `다시 시도` → `CO0401` 수강 신청 - 결제
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `실패 사유 「자세히」` → 무엇이 문제였는지 설명이 펼쳐진다
  - `다른 결제 수단 고르기` → 고른 것에 표시가 남고 「다시 시도」 문구가 그 수단 이름으로 바뀐다
- **생성 프롬프트**:

  ```
  결제 실패 화면을 만들어줘. 경고 아이콘과 "결제가 완료되지 않았어요" 문구, 실패 사유를 사람이 이해할 수 있는 문장으로 표시(예: 카드 한도를 초과했어요), "다시 시도" 버튼과 "다른 결제 수단 선택" 버튼, 하단에 고객센터 문의 링크. 신청 내용은 유지된다는 안내 문구도 넣어줘.
  ```

### CO0602 · 수강 신청 - 결제 실패 > 사유별 변형
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 수강 신청 - 결제 실패` — 누르면 `CO0601`로 간다
- **일정**: 2026-10-23 ~ 2026-10-26
- **기능정의**: 한도 초과·승인 거절·잔액 부족·본인인증 실패 사유별 문구 · 대처 방법 안내 · 다른 결제 수단 유도 · 고객센터 링크
- **생성 프롬프트**:

  ```
  '수강 신청 - 결제 실패' 화면의 '사유별 변형' 상태·세부 화면을 만들어줘. 한도 초과·승인 거절·잔액 부족·본인인증 실패 사유별 문구· 대처 방법 안내· 다른 결제 수단 유도· 고객센터 링크. 상위 화면(수강 신청 - 결제 실패)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CO0603 · 수강 신청 - 결제 실패 > 중복 결제 감지
- **소속 메뉴**: 강의 탐색
- **뒤로가기**: 좌측 상단에 `‹ 수강 신청 - 결제 실패` — 누르면 `CO0601`로 간다
- **일정**: 2026-10-27 ~ 2026-10-27
- **기능정의**: 같은 강의를 이미 결제했다는 안내 · 기존 주문 상태 확인 · 중복 승인 건 자동 취소 안내 · 내 강의실 이동
- **생성 프롬프트**:

  ```
  '수강 신청 - 결제 실패' 화면의 '중복 결제 감지' 상태·세부 화면을 만들어줘. 같은 강의를 이미 결제했다는 안내· 기존 주문 상태 확인· 중복 승인 건 자동 취소 안내· 내 강의실 이동. 상위 화면(수강 신청 - 결제 실패)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0101 · 내 강의실 - 수강 중
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-10-28 ~ 2026-10-29
- **기능정의**: 수강 중/완료 탭 전환 · 강의별 진도율 막대와 남은 차시 수 · 마지막 학습일 표시 · 이어보기 버튼 · 수료 조건 충족 시 수료증 발급 배지
- **버튼 → 이동화면**:
  - `이어보기` → `CL0301` 강의 재생
  - `수료증 발급` → `CL0801` 수료증 발급
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `수강 중·완료·전체 탭` → 아래 목록이 그 상태 것으로 바뀌고 탭 옆 숫자도 함께 바뀐다
  - `정렬(최근 학습순/진도순)` → 카드 순서가 다시 늘어선다
  - `진도 막대` → 마우스를 올리면 몇 차시 중 몇 차시인지가 뜬다
- **생성 프롬프트**:

  ```
  내 강의실 화면을 만들어줘. 상단 탭(수강 중, 수강 완료), 강의 카드 목록에 썸네일, 강의명, 진도율 막대와 퍼센트, "12/30차시", 마지막 학습일, 우측에 이어보기 버튼. 수료 조건을 충족한 강의에는 "수료증 발급 가능" 배지를 오렌지로 표시.
  ```

### CL0102 · 내 강의실 - 수강 중 > 수강 완료 탭
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-10-30 ~ 2026-10-30
- **기능정의**: 완료 강의 목록 · 수료 여부 배지 · 수료증 다시 받기 · 후기 작성 유도 · 재수강 가능 기간 안내
- **생성 프롬프트**:

  ```
  '내 강의실 - 수강 중' 화면의 '수강 완료 탭' 탭을 만들어줘. 완료 강의 목록· 수료 여부 배지· 수료증 다시 받기· 후기 작성 유도· 재수강 가능 기간 안내. 상위 화면(내 강의실 - 수강 중)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0103 · 내 강의실 - 수강 중 > 수강 기간 만료 임박
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 내 강의실 - 수강 중` — 누르면 `CL0101`로 간다
- **일정**: 2026-11-02 ~ 2026-11-02
- **기능정의**: D-7 이하 만료 배지 · 남은 기간 카운트다운 · 연장 신청 진입 · 만료 후 시청 불가 안내 · 남은 차시 수 강조
- **생성 프롬프트**:

  ```
  '내 강의실 - 수강 중' 화면의 '수강 기간 만료 임박' 상태·세부 화면을 만들어줘. D-7 이하 만료 배지· 남은 기간 카운트다운· 연장 신청 진입· 만료 후 시청 불가 안내· 남은 차시 수 강조. 상위 화면(내 강의실 - 수강 중)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0104 · 내 강의실 - 수강 중 > 수강 기간 만료
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 내 강의실 - 수강 중` — 누르면 `CL0101`로 간다
- **일정**: 2026-11-03 ~ 2026-11-03
- **기능정의**: 만료 강의 카드 흐림 처리 · 재수강 결제 유도 · 수료증 발급 가능 여부 표시 · 노트·자료 열람 범위 안내
- **생성 프롬프트**:

  ```
  '내 강의실 - 수강 중' 화면의 '수강 기간 만료' 상태·세부 화면을 만들어줘. 만료 강의 카드 흐림 처리· 재수강 결제 유도· 수료증 발급 가능 여부 표시· 노트·자료 열람 범위 안내. 상위 화면(내 강의실 - 수강 중)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0201 · 내 강의실 - 비어 있음
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 내 강의실 - 수강 중` — 누르면 `CL0101`로 간다
- **일정**: 2026-11-04 ~ 2026-11-05
- **기능정의**: 수강 중인 강의가 없다는 안내 · 일러스트 노출 · 강의 둘러보기 유도 버튼 · 인기 강의 추천 4개 · 첫 수강 할인 쿠폰 안내
- **버튼 → 이동화면**:
  - `강의 둘러보기` → `CO0101` 강의 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「무료 강의 보기」` → 빈 화면 대신 무료 강의 목록이 그 자리에 나타난다
- **생성 프롬프트**:

  ```
  수강 중인 강의가 하나도 없을 때의 내 강의실 빈 화면을 만들어줘. 일러스트와 "아직 수강 중인 강의가 없어요" 문구, 큰 CTA "강의 둘러보기", 그 아래 인기 강의 추천 4개 카드와 첫 수강 할인 쿠폰 안내 배너.
  ```

### CL0202 · 내 강의실 - 비어 있음 > 환불로 비워진 상태
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 내 강의실 - 비어 있음` — 누르면 `CL0201`로 간다
- **일정**: 2026-11-06 ~ 2026-11-06
- **기능정의**: 최근 환불 처리 안내 · 환불 진행 상태 확인 링크 · 재수강 할인 쿠폰 제공 · 강의 둘러보기 유도
- **생성 프롬프트**:

  ```
  '내 강의실 - 비어 있음' 화면의 '환불로 비워진 상태' 상태·세부 화면을 만들어줘. 최근 환불 처리 안내· 환불 진행 상태 확인 링크· 재수강 할인 쿠폰 제공· 강의 둘러보기 유도. 상위 화면(내 강의실 - 비어 있음)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0301 · 강의 재생
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 내 강의실 - 수강 중` — 누르면 `CL0101`로 간다
- **일정**: 2026-11-09 ~ 2026-11-10
- **기능정의**: 영상 플레이어(배속·자막·전체화면) · 우측 커리큘럼 목록과 현재 차시 하이라이트 · 이어보기 지점 자동 복원 · 시청 완료 시 다음 차시 자동 이동 · 강의 노트 작성
- **버튼 → 이동화면**:
  - `과제 제출` → `CL0501` 과제 제출
  - `질문하기` → `CL0701` 질문 게시판
  - `재생 오류` → `CL0401` 강의 재생 - 오류
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `이전 차시·다음 차시` → 영상과 제목·노트가 그 차시 것으로 바뀐다
  - `노트의 시간 지점(4:18)` → 영상이 그 지점으로 옮겨 가고 재생 위치 막대가 따라 움직인다
  - `재생 속도(1.0x/1.25x/1.5x)` → 고른 값에 표시가 남고 남은 시간이 다시 계산된다
  - `자막 켜기/끄기` → 영상 위 자막이 나타나고 사라진다
  - `커리큘럼의 차시 줄` → 그 차시로 옮겨 가고 현재 차시 표시가 이동한다
- **생성 프롬프트**:

  ```
  강의 영상 재생 화면을 만들어줘. 좌측 대부분을 영상 플레이어가 차지하고 (재생/일시정지, 배속 0.5~2배, 자막 토글, 전체화면, 진행 바), 우측에 커리큘럼 사이드바(챕터별 차시 목록, 완료 체크 표시, 현재 차시 하이라이트). 플레이어 아래에는 탭(강의 노트, 첨부 자료, 질문하기). 시청 위치를 저장했다가 다시 들어오면 이어서 재생되게 해줘.
  ```

### CL0302 · 강의 재생 > 강의 노트 탭
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 내 강의실 - 수강 중` — 누르면 `CL0101`로 간다
- **일정**: 2026-11-11 ~ 2026-11-12
- **기능정의**: 재생 시점 타임스탬프 자동 삽입 · 노트 작성·수정 · 노트에서 해당 시점으로 이동 · 전체 노트 내보내기 · 자동 저장 표시
- **생성 프롬프트**:

  ```
  '강의 재생' 화면의 '강의 노트 탭' 탭을 만들어줘. 재생 시점 타임스탬프 자동 삽입· 노트 작성·수정· 노트에서 해당 시점으로 이동· 전체 노트 내보내기· 자동 저장 표시. 상위 화면(강의 재생)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0303 · 강의 재생 > 첨부 자료 탭
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 내 강의실 - 수강 중` — 누르면 `CL0101`로 간다
- **일정**: 2026-11-13 ~ 2026-11-16
- **기능정의**: 차시별 PDF·ZIP 목록 · 개별·일괄 다운로드 · 파일 용량과 형식 표시 · 자료 미등록 시 안내 · 다운로드 권한 안내
- **생성 프롬프트**:

  ```
  '강의 재생' 화면의 '첨부 자료 탭' 탭을 만들어줘. 차시별 PDF·ZIP 목록· 개별·일괄 다운로드· 파일 용량과 형식 표시· 자료 미등록 시 안내· 다운로드 권한 안내. 상위 화면(강의 재생)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0304 · 강의 재생 > 재생 설정
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 강의 재생` — 누르면 `CL0301`로 간다
- **일정**: 2026-11-17 ~ 2026-11-17
- **기능정의**: 배속 0.5~2배 조절 · 자막 언어와 크기 · 화질 선택 · 다음 차시 자동 재생 토글 · 설정값 기기별 유지
- **생성 프롬프트**:

  ```
  '강의 재생' 화면의 '재생 설정' 상태·세부 화면을 만들어줘. 배속 0.5~2배 조절· 자막 언어와 크기· 화질 선택· 다음 차시 자동 재생 토글· 설정값 기기별 유지. 상위 화면(강의 재생)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0305 · 강의 재생 > 차시 완료 처리
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 강의 재생` — 누르면 `CL0301`로 간다
- **일정**: 2026-11-18 ~ 2026-11-18
- **기능정의**: 시청 95% 도달 시 완료 체크 · 진도율 갱신 애니메이션 · 다음 차시 이동 카운트다운 · 건너뛰기 시 미완료 처리 안내
- **생성 프롬프트**:

  ```
  '강의 재생' 화면의 '차시 완료 처리' 상태·세부 화면을 만들어줘. 시청 95% 도달 시 완료 체크· 진도율 갱신 애니메이션· 다음 차시 이동 카운트다운· 건너뛰기 시 미완료 처리 안내. 상위 화면(강의 재생)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0306 · 강의 재생 > 동시 접속 차단
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 강의 재생` — 누르면 `CL0301`로 간다
- **일정**: 2026-11-19 ~ 2026-11-19
- **기능정의**: 다른 기기에서 재생 중 안내 · 접속 기기명과 마지막 접속 시각 · 현재 기기로 이어보기 전환 · 계정 공유 제한 정책 안내
- **생성 프롬프트**:

  ```
  '강의 재생' 화면의 '동시 접속 차단' 상태·세부 화면을 만들어줘. 다른 기기에서 재생 중 안내· 접속 기기명과 마지막 접속 시각· 현재 기기로 이어보기 전환· 계정 공유 제한 정책 안내. 상위 화면(강의 재생)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0401 · 강의 재생 - 오류
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 내 강의실 - 수강 중` — 누르면 `CL0101`로 간다
- **일정**: 2026-11-20 ~ 2026-11-23
- **기능정의**: 영상 로드 실패 안내 문구 · 새로고침 버튼 · 네트워크 확인 안내 · 다른 차시로 이동 링크 · 오류 신고 버튼
- **버튼 → 이동화면**:
  - `다시 시도` → `CL0301` 강의 재생
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「화질 낮추기」` → 화질 표시가 바뀌고 다시 시도 안내가 그에 맞게 바뀐다
  - `「자세한 오류」` → 무엇이 문제였는지 설명이 펼쳐진다
- **생성 프롬프트**:

  ```
  강의 영상을 불러오지 못했을 때의 화면을 만들어줘. 플레이어 자리에 어두운 박스와 경고 아이콘, "영상을 불러올 수 없어요" 문구, "다시 시도" 버튼과 "네트워크 상태를 확인해주세요" 안내, 우측 커리큘럼은 그대로 유지해서 다른 차시로 이동할 수 있게 하고, 하단에 오류 신고 링크.
  ```

### CL0402 · 강의 재생 - 오류 > 인코딩 미완료
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 강의 재생 - 오류` — 누르면 `CL0401`로 간다
- **일정**: 2026-11-24 ~ 2026-11-24
- **기능정의**: 강사가 올린 영상 처리 중 안내 · 예상 완료 시각 · 완료 시 알림 신청 · 다른 차시로 이동 · 강사에게 문의
- **생성 프롬프트**:

  ```
  '강의 재생 - 오류' 화면의 '인코딩 미완료' 상태·세부 화면을 만들어줘. 강사가 올린 영상 처리 중 안내· 예상 완료 시각· 완료 시 알림 신청· 다른 차시로 이동· 강사에게 문의. 상위 화면(강의 재생 - 오류)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0403 · 강의 재생 - 오류 > 재생 권한 없음
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 강의 재생 - 오류` — 누르면 `CL0401`로 간다
- **일정**: 2026-11-25 ~ 2026-11-25
- **기능정의**: 수강 기간 만료·환불 처리로 재생 불가 안내 · 사유별 문구 구분 · 재수강 결제 진입 · 고객센터 문의 링크
- **생성 프롬프트**:

  ```
  '강의 재생 - 오류' 화면의 '재생 권한 없음' 상태·세부 화면을 만들어줘. 수강 기간 만료·환불 처리로 재생 불가 안내· 사유별 문구 구분· 재수강 결제 진입· 고객센터 문의 링크. 상위 화면(강의 재생 - 오류)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0501 · 과제 제출
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 내 강의실 - 수강 중` — 누르면 `CL0101`로 간다
- **일정**: 2026-11-26 ~ 2026-11-27
- **기능정의**: 과제 안내문과 배점 표시 · 파일 첨부(드래그앤드롭, 용량 제한 안내) · 텍스트 답안 입력 에디터 · 임시 저장 기능 · 마감일까지 남은 시간 표시
- **버튼 → 이동화면**:
  - `제출하기` → `CL0301` 강의 재생
  - `마감된 과제` → `CL0601` 과제 제출 - 마감됨
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `파일 끌어다 놓기` → 파일 줄이 늘어나고 「N/5개」와 용량 합계가 올라간다
  - `파일 줄의 ✕` → 그 파일이 빠지고 숫자와 용량이 줄어든다
  - `제출 전 「미리보기」` → 제출될 내용이 그 자리에서 펼쳐진다
  - `필수 항목이 빈 상태` → 「제출」이 눌리지 않고 어느 칸이 비었는지 표시된다
- **생성 프롬프트**:

  ```
  과제 제출 화면을 만들어줘. 상단에 과제명, 마감일까지 남은 시간(D-3 형태), 배점, 과제 안내문 영역, 텍스트 답안 에디터, 파일 첨부 영역(드래그앤드롭, 최대 20MB 안내), 하단에 임시 저장과 제출하기 버튼. 제출 후에는 수정 가능 여부를 명확히 안내해줘.
  ```

### CL0502 · 과제 제출 > 파일 용량 초과
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 과제 제출` — 누르면 `CL0501`로 간다
- **일정**: 2026-11-30 ~ 2026-12-01
- **기능정의**: 20MB 초과 파일 개별 표시 · 초과 파일만 제거 · 압축·분할 업로드 안내 · 허용 형식 재안내 · 나머지 첨부는 유지
- **생성 프롬프트**:

  ```
  '과제 제출' 화면의 '파일 용량 초과' 상태·세부 화면을 만들어줘. 20MB 초과 파일 개별 표시· 초과 파일만 제거· 압축·분할 업로드 안내· 허용 형식 재안내· 나머지 첨부는 유지. 상위 화면(과제 제출)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0503 · 과제 제출 > 임시 저장 상태
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 과제 제출` — 누르면 `CL0501`로 간다
- **일정**: 2026-12-02 ~ 2026-12-02
- **기능정의**: 자동 저장 시각 표시 · 이어서 작성 불러오기 · 저장본과 현재 내용 비교 · 저장본 삭제 · 저장 실패 시 재시도
- **생성 프롬프트**:

  ```
  '과제 제출' 화면의 '임시 저장 상태' 상태·세부 화면을 만들어줘. 자동 저장 시각 표시· 이어서 작성 불러오기· 저장본과 현재 내용 비교· 저장본 삭제· 저장 실패 시 재시도. 상위 화면(과제 제출)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0504 · 과제 제출 > 재제출 상태
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 과제 제출` — 누르면 `CL0501`로 간다
- **일정**: 2026-12-03 ~ 2026-12-03
- **기능정의**: 이미 제출한 답안 표시 · 허용 횟수와 남은 횟수 · 이전 제출본 열람 · 재제출 시 기존 점수 초기화 안내 · 확인 후 진행
- **생성 프롬프트**:

  ```
  '과제 제출' 화면의 '재제출 상태' 상태·세부 화면을 만들어줘. 이미 제출한 답안 표시· 허용 횟수와 남은 횟수· 이전 제출본 열람· 재제출 시 기존 점수 초기화 안내· 확인 후 진행. 상위 화면(과제 제출)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0505 · 과제 제출 > 제출 완료
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 과제 제출` — 누르면 `CL0501`로 간다
- **일정**: 2026-12-04 ~ 2026-12-04
- **기능정의**: 제출 완료 토스트와 제출 시각 · 채점 예상 소요 안내 · 제출 내역 보기 · 강의 재생으로 복귀
- **생성 프롬프트**:

  ```
  '과제 제출' 화면의 '제출 완료' 상태·세부 화면을 만들어줘. 제출 완료 토스트와 제출 시각· 채점 예상 소요 안내· 제출 내역 보기· 강의 재생으로 복귀. 상위 화면(과제 제출)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0601 · 과제 제출 - 마감됨
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 내 강의실 - 수강 중` — 누르면 `CL0101`로 간다
- **일정**: 2026-12-07 ~ 2026-12-08
- **기능정의**: 마감 안내 문구와 마감 일시 표시 · 입력 폼 비활성 처리 · 지각 제출 허용 여부 안내 · 강사에게 문의 버튼 · 다른 과제 목록으로 이동
- **버튼 → 이동화면**:
  - `질문하기` → `CL0701` 질문 게시판
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「마감 안내」` → 왜 못 내는지와 다음 기회가 언제인지 펼쳐진다
- **생성 프롬프트**:

  ```
  과제 제출 기간이 지났을 때의 화면을 만들어줘. 상단에 회색 안내 배너 "제출 기간이 종료된 과제예요 (마감: 2026년 9월 10일 23:59)", 과제 내용은 읽기 전용으로 보여주되 입력 폼은 비활성 처리, "강사에게 문의하기" 버튼과 다른 과제 목록으로 가는 링크를 제공해줘.
  ```

### CL0602 · 과제 제출 - 마감됨 > 지각 제출 허용
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 과제 제출 - 마감됨` — 누르면 `CL0601`로 간다
- **일정**: 2026-12-09 ~ 2026-12-09
- **기능정의**: 지각 제출 가능 기간과 감점률 안내 · 지각 배지와 함께 폼 활성 · 남은 시간 표시 · 제출 전 감점 확인 · 강사 정책 문구
- **생성 프롬프트**:

  ```
  '과제 제출 - 마감됨' 화면의 '지각 제출 허용' 상태·세부 화면을 만들어줘. 지각 제출 가능 기간과 감점률 안내· 지각 배지와 함께 폼 활성· 남은 시간 표시· 제출 전 감점 확인· 강사 정책 문구. 상위 화면(과제 제출 - 마감됨)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0701 · 질문 게시판
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 내 강의실 - 수강 중` — 누르면 `CL0101`로 간다
- **일정**: 2026-12-10 ~ 2026-12-11
- **기능정의**: 차시별 질문 목록과 답변 여부 배지 · 질문 작성 폼 · 강사 답변 강조 표시 · 좋아요와 정렬(최신순/답변순) · 내 질문만 보기 필터
- **버튼 → 이동화면**:
  - `강의로 돌아가기` → `CL0301` 강의 재생
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `전체·내 질문·답변 완료 탭` → 목록이 그 기준으로 걸러지고 탭 옆 숫자가 바뀐다
  - `질문 줄` → 답변이 펼쳐지고 다시 누르면 접힌다
  - `「도움돼요」` → 숫자가 하나 오르고 다시 누르면 되돌아간다
  - `차시 필터` → 그 차시 질문만 남는다
- **생성 프롬프트**:

  ```
  강의 질문 게시판을 만들어줘. 상단에 질문 작성 버튼과 필터(전체, 내 질문만, 미답변만), 질문 목록은 카드형으로 질문 제목, 작성자, 관련 차시, 답변 여부 배지(답변 완료/대기), 질문을 펼치면 본문과 강사 답변이 보이고 강사 답변은 배경색으로 구분해줘.
  ```

### CL0702 · 질문 게시판 > 질문 작성 폼
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 질문 게시판` — 누르면 `CL0701`로 간다
- **일정**: 2026-12-14 ~ 2026-12-15
- **기능정의**: 관련 차시와 재생 시점 자동 연결 · 제목·본문 입력 · 이미지·코드 첨부 · 비공개 질문 토글 · 유사 질문 추천
- **생성 프롬프트**:

  ```
  '질문 게시판' 화면의 '질문 작성 폼' 상태·세부 화면을 만들어줘. 관련 차시와 재생 시점 자동 연결· 제목·본문 입력· 이미지·코드 첨부· 비공개 질문 토글· 유사 질문 추천. 상위 화면(질문 게시판)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0703 · 질문 게시판 > 미답변만 보기
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 질문 게시판` — 누르면 `CL0701`로 간다
- **일정**: 2026-12-16 ~ 2026-12-16
- **기능정의**: 답변 대기 질문만 필터 · 경과 시간순 정렬 · 대기 건수 표시 · 필터 해제 · 결과 없음 안내
- **생성 프롬프트**:

  ```
  '질문 게시판' 화면의 '미답변만 보기' 상태·세부 화면을 만들어줘. 답변 대기 질문만 필터· 경과 시간순 정렬· 대기 건수 표시· 필터 해제· 결과 없음 안내. 상위 화면(질문 게시판)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0704 · 질문 게시판 > 질문 없음
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 질문 게시판` — 누르면 `CL0701`로 간다
- **일정**: 2026-12-17 ~ 2026-12-17
- **기능정의**: 이 차시에 등록된 질문 없음 안내 · 첫 질문 작성 유도 · 전체 차시 질문 보기 · 자주 묻는 질문 링크
- **생성 프롬프트**:

  ```
  '질문 게시판' 화면의 '질문 없음' 상태·세부 화면을 만들어줘. 이 차시에 등록된 질문 없음 안내· 첫 질문 작성 유도· 전체 차시 질문 보기· 자주 묻는 질문 링크. 상위 화면(질문 게시판)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0801 · 수료증 발급
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 내 강의실 - 수강 중` — 누르면 `CL0101`로 간다
- **일정**: 2026-12-18 ~ 2026-12-21
- **기능정의**: 수료 조건 충족 현황 표시(진도율·과제·출석) · 수료증 미리보기 · PDF 다운로드 버튼 · 수료번호 발급 · SNS 공유 버튼
- **버튼 → 이동화면**:
  - `내 강의실` → `CL0101` 내 강의실 - 수강 중
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `수료 조건 줄` → 무엇이 모자란지 펼쳐지고, 다 채웠으면 「발급」이 켜진다
  - `이름 표기 바꾸기` → 미리보기 수료증의 이름이 따라 바뀐다
- **생성 프롬프트**:

  ```
  수료증 발급 화면을 만들어줘. 상단에 수료 조건 충족 현황을 체크리스트로 (진도율 80% 이상 ✓, 과제 3개 제출 ✓, 출석 기준 충족 ✓), 중앙에 수료증 미리보기(이름, 강의명, 수료일, 수료번호가 들어간 A4 비율 카드), 하단에 PDF 다운로드 버튼과 링크드인 공유 버튼.
  ```

### CL0802 · 수료증 발급 > 수료 조건 미충족
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 수료증 발급` — 누르면 `CL0801`로 간다
- **일정**: 2026-12-22 ~ 2026-12-22
- **기능정의**: 미달 항목 빨강 표시(진도율·과제·출석) · 부족한 수치와 남은 분량 · 해당 차시·과제 바로가기 · 발급 버튼 비활성
- **생성 프롬프트**:

  ```
  '수료증 발급' 화면의 '수료 조건 미충족' 상태·세부 화면을 만들어줘. 미달 항목 빨강 표시(진도율·과제·출석)· 부족한 수치와 남은 분량· 해당 차시·과제 바로가기· 발급 버튼 비활성. 상위 화면(수료증 발급)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CL0803 · 수료증 발급 > 공유·다운로드
- **소속 메뉴**: 내 강의실
- **뒤로가기**: 좌측 상단에 `‹ 수료증 발급` — 누르면 `CL0801`로 간다
- **일정**: 2026-12-23 ~ 2026-12-23
- **기능정의**: PDF 다운로드 · 링크드인 공유 · 수료번호 복사 · 진위 확인 페이지 안내 · 재발급 이력 표시
- **생성 프롬프트**:

  ```
  '수료증 발급' 화면의 '공유·다운로드' 상태·세부 화면을 만들어줘. PDF 다운로드· 링크드인 공유· 수료번호 복사· 진위 확인 페이지 안내· 재발급 이력 표시. 상위 화면(수료증 발급)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CU0101 · 내 강의 목록
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-12-24 ~ 2026-12-25
- **기능정의**: 개설한 강의 카드 목록 · 상태 배지(작성 중/검수 대기/게시 중/게시 중지) · 강의별 수강생 수와 평점 · 새 강의 개설 버튼 · 상태별 필터
- **버튼 → 이동화면**:
  - `새 강의 개설` → `CU0201` 강의 개설 - 기본 정보
  - `커리큘럼 편성` → `CU0301` 커리큘럼 편성
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `전체·게시 중·검수 대기·작성 중 탭` → 표가 그 상태 것으로 바뀌고 탭 옆 숫자도 함께 바뀐다
  - `강의명 검색` → 치는 대로 표가 걸러진다
  - `정렬(최근 수정·수강생·매출)` → 표 순서가 다시 늘어선다
- **생성 프롬프트**:

  ```
  강사의 내 강의 목록 화면을 만들어줘. 상단에 "새 강의 개설" 버튼과 상태 필터 탭, 강의 카드에 썸네일, 강의명, 상태 배지(작성 중 회색, 검수 대기 노랑, 게시 중 초록, 게시 중지 빨강), 수강생 수, 평균 평점, 최근 수정일, 우측에 편집/통계 버튼을 배치해줘.
  ```

### CU0102 · 내 강의 목록 > 상태별 필터
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 내 강의 목록` — 누르면 `CU0101`로 간다
- **일정**: 2026-12-28 ~ 2026-12-29
- **기능정의**: 작성 중·검수 대기·게시 중·게시 중지 탭 · 상태별 건수 배지 · 정렬(최근 수정·수강생 수) · 필터 해제
- **생성 프롬프트**:

  ```
  '내 강의 목록' 화면의 '상태별 필터' 상태·세부 화면을 만들어줘. 작성 중·검수 대기·게시 중·게시 중지 탭· 상태별 건수 배지· 정렬(최근 수정·수강생 수)· 필터 해제. 상위 화면(내 강의 목록)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CU0103 · 내 강의 목록 > 개설 강의 없음
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 내 강의 목록` — 누르면 `CU0101`로 간다
- **일정**: 2026-12-30 ~ 2026-12-30
- **기능정의**: 아직 개설한 강의 없음 안내 · 첫 강의 개설 CTA · 개설 가이드와 심사 기준 링크 · 인기 강의 구성 예시
- **생성 프롬프트**:

  ```
  '내 강의 목록' 화면의 '개설 강의 없음' 상태·세부 화면을 만들어줘. 아직 개설한 강의 없음 안내· 첫 강의 개설 CTA· 개설 가이드와 심사 기준 링크· 인기 강의 구성 예시. 상위 화면(내 강의 목록)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CU0201 · 강의 개설 - 기본 정보
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 내 강의 목록` — 누르면 `CU0101`로 간다
- **일정**: 2026-12-31 ~ 2027-01-01
- **기능정의**: 강의명·부제 입력 · 카테고리와 난이도 선택 · 썸네일 이미지 업로드와 미리보기 · 가격 설정(무료 토글, 할인율) · 강의 소개 리치 텍스트 에디터
- **버튼 → 이동화면**:
  - `다음 단계` → `CU0301` 커리큘럼 편성
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「무료 강의로 열기」 체크` → 켜면 아래 가격 칸이 잠기고 미리보기가 「무료」로 바뀐다
  - `정가·할인율 입력` → 판매가와 아래 「수강생이 보는 가격」이 그 자리에서 다시 계산된다
  - `「+ 줄 추가」·줄의 −` → 이런 걸 배웁니다·이런 분께 맞습니다의 줄이 늘고 준다
  - `카테고리·난이도 고르기` → 고른 값이 남고 미리보기 카드의 배지가 바뀐다
- **생성 프롬프트**:

  ```
  강의 개설 1단계 기본 정보 입력 화면을 만들어줘. 상단에 진행 스텝 표시(1 기본정보 → 2 커리큘럼 → 3 검수 요청), 강의명, 부제, 카테고리 셀렉트, 난이도 라디오(입문/초급/중급/고급), 썸네일 업로드 영역(권장 사이즈 안내와 미리보기), 가격 입력(무료 토글 시 비활성), 강의 소개 리치 텍스트 에디터. 하단에 임시 저장과 다음 단계 버튼.
  ```

### CU0202 · 강의 개설 - 기본 정보 > 썸네일 업로드
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 강의 개설 - 기본 정보` — 누르면 `CU0201`로 간다
- **일정**: 2027-01-04 ~ 2027-01-05
- **기능정의**: 권장 사이즈와 형식 안내 · 드래그앤드롭 업로드 · 자르기와 미리보기 · 목록 카드에서 보이는 모습 확인 · 삭제·교체
- **생성 프롬프트**:

  ```
  '강의 개설 - 기본 정보' 화면의 '썸네일 업로드' 상태·세부 화면을 만들어줘. 권장 사이즈와 형식 안내· 드래그앤드롭 업로드· 자르기와 미리보기· 목록 카드에서 보이는 모습 확인· 삭제·교체. 상위 화면(강의 개설 - 기본 정보)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CU0203 · 강의 개설 - 기본 정보 > 필수값 누락
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 강의 개설 - 기본 정보` — 누르면 `CU0201`로 간다
- **일정**: 2027-01-06 ~ 2027-01-07
- **기능정의**: 강의명·카테고리·썸네일 미입력 항목 강조 · 첫 오류 항목으로 스크롤 · 다음 단계 버튼 비활성 · 항목별 안내 문구
- **생성 프롬프트**:

  ```
  '강의 개설 - 기본 정보' 화면의 '필수값 누락' 상태·세부 화면을 만들어줘. 강의명·카테고리·썸네일 미입력 항목 강조· 첫 오류 항목으로 스크롤· 다음 단계 버튼 비활성· 항목별 안내 문구. 상위 화면(강의 개설 - 기본 정보)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CU0204 · 강의 개설 - 기본 정보 > 가격·할인 설정
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 강의 개설 - 기본 정보` — 누르면 `CU0201`로 간다
- **일정**: 2027-01-08 ~ 2027-01-08
- **기능정의**: 정가 입력 · 무료 토글 시 가격 비활성 · 할인율과 할인가 자동 계산 · 할인 적용 기간 지정 · 수수료 제외 예상 정산액 표시
- **생성 프롬프트**:

  ```
  '강의 개설 - 기본 정보' 화면의 '가격·할인 설정' 상태·세부 화면을 만들어줘. 정가 입력· 무료 토글 시 가격 비활성· 할인율과 할인가 자동 계산· 할인 적용 기간 지정· 수수료 제외 예상 정산액 표시. 상위 화면(강의 개설 - 기본 정보)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CU0301 · 커리큘럼 편성
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 내 강의 목록` — 누르면 `CU0101`로 간다
- **일정**: 2027-01-11 ~ 2027-01-12
- **기능정의**: 챕터 추가와 이름 변경 · 챕터 안에 차시 추가 · 드래그앤드롭으로 순서 변경 · 차시별 영상 연결 상태 표시 · 미리보기 허용 차시 지정 · 총 강의 시간 자동 합산
- **버튼 → 이동화면**:
  - `영상 업로드` → `CU0401` 영상 업로드
  - `검수 요청` → `CU0601` 강의 게시 - 검수 대기
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `챕터 이름 입력` → 위 요약의 챕터 수·총 시간이 따라 바뀐다
  - `차시 줄의 ↑↓` → 순서가 실제로 바뀐다
  - `「+ 차시 추가」` → 빈 줄이 늘어나고 총 차시 수가 하나 오른다
  - `차시 줄의 삭제` → 그 줄이 빠지고 총 시간이 다시 계산된다
- **생성 프롬프트**:

  ```
  커리큘럼 편성 화면을 만들어줘. 이 화면이 이 서비스의 핵심이야. 좌측에 챕터 목록(챕터 추가 버튼, 챕터별로 접기/펼치기), 각 챕터 안에 차시 행(차시명, 영상 길이, 영상 연결 상태 아이콘, 미리보기 허용 토글, 삭제), 드래그앤드롭 핸들로 챕터와 차시 순서를 바꿀 수 있게 해줘. 우측 상단에 총 챕터 수, 총 차시 수, 총 강의 시간을 자동 합산해서 표시. 영상이 연결되지 않은 차시는 경고 아이콘으로 표시해줘.
  ```

### CU0302 · 커리큘럼 편성 > 순서 변경
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 커리큘럼 편성` — 누르면 `CU0301`로 간다
- **일정**: 2027-01-13 ~ 2027-01-14
- **기능정의**: 챕터·차시 드래그 핸들 · 이동 중 자리 표시선 · 드롭 후 번호 자동 재정렬 · 되돌리기 · 저장 상태 표시
- **생성 프롬프트**:

  ```
  '커리큘럼 편성' 화면의 '순서 변경' 상태·세부 화면을 만들어줘. 챕터·차시 드래그 핸들· 이동 중 자리 표시선· 드롭 후 번호 자동 재정렬· 되돌리기· 저장 상태 표시. 상위 화면(커리큘럼 편성)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CU0303 · 커리큘럼 편성 > 영상 미연결 경고
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 커리큘럼 편성` — 누르면 `CU0301`로 간다
- **일정**: 2027-01-15 ~ 2027-01-15
- **기능정의**: 영상 없는 차시 경고 아이콘 · 미연결 건수 요약 · 해당 차시로 바로 이동 · 검수 요청 차단 안내
- **생성 프롬프트**:

  ```
  '커리큘럼 편성' 화면의 '영상 미연결 경고' 상태·세부 화면을 만들어줘. 영상 없는 차시 경고 아이콘· 미연결 건수 요약· 해당 차시로 바로 이동· 검수 요청 차단 안내. 상위 화면(커리큘럼 편성)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CU0304 · 커리큘럼 편성 > 차시 삭제 확인
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 커리큘럼 편성` — 누르면 `CU0301`로 간다
- **일정**: 2027-01-18 ~ 2027-01-18
- **기능정의**: 삭제 대상 차시명과 연결 영상 표시 · 수강생 진도 데이터 영향 안내 · 게시 중 강의 주의 문구 · 취소·삭제 버튼
- **생성 프롬프트**:

  ```
  '커리큘럼 편성' 화면의 '차시 삭제 확인' 상태·세부 화면을 만들어줘. 삭제 대상 차시명과 연결 영상 표시· 수강생 진도 데이터 영향 안내· 게시 중 강의 주의 문구· 취소·삭제 버튼. 상위 화면(커리큘럼 편성)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CU0305 · 커리큘럼 편성 > 총 시간 요약
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 커리큘럼 편성` — 누르면 `CU0301`로 간다
- **일정**: 2027-01-19 ~ 2027-01-19
- **기능정의**: 총 챕터 수와 차시 수 · 총 강의 시간 자동 합산 · 미리보기 허용 차시 수 · 권장 분량 대비 안내
- **생성 프롬프트**:

  ```
  '커리큘럼 편성' 화면의 '총 시간 요약' 상태·세부 화면을 만들어줘. 총 챕터 수와 차시 수· 총 강의 시간 자동 합산· 미리보기 허용 차시 수· 권장 분량 대비 안내. 상위 화면(커리큘럼 편성)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CU0401 · 영상 업로드
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 내 강의 목록` — 누르면 `CU0101`로 간다
- **일정**: 2027-01-20 ~ 2027-01-21
- **기능정의**: 드래그앤드롭 업로드 영역 · 업로드 진행률 표시 · 인코딩 상태 안내 · 자막 파일 첨부 · 첨부 자료(PDF 등) 업로드 · 지원 형식과 용량 제한 안내
- **버튼 → 이동화면**:
  - `업로드 완료` → `CU0301` 커리큘럼 편성
  - `업로드 실패` → `CU0501` 영상 업로드 - 실패
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `파일 끌어다 놓기` → 올림 줄이 늘어나고 진행 막대가 움직인다
  - `차시에 영상 연결` → 그 차시 배지가 「연결됨」으로 바뀌고 위의 「N/M차시」가 오른다
  - `「연결 안 된 차시만 보기」 토글` → 목록이 걸러진다
- **생성 프롬프트**:

  ```
  차시 영상 업로드 화면을 만들어줘. 중앙에 큰 드래그앤드롭 영역(지원 형식 MP4/MOV, 최대 2GB 안내), 업로드 시작하면 파일명과 진행률 바, 완료 후 "인코딩 중" 상태 표시, 아래에 자막 파일(SRT) 첨부와 강의 자료(PDF, ZIP) 첨부 영역. 업로드 중에는 페이지를 벗어나지 말라는 안내를 보여줘.
  ```

### CU0402 · 영상 업로드 > 업로드 진행 중
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 영상 업로드` — 누르면 `CU0401`로 간다
- **일정**: 2027-01-22 ~ 2027-01-22
- **기능정의**: 파일별 진행률 바와 남은 시간 · 개별 취소 · 페이지 이탈 경고 · 백그라운드 업로드 지원 안내
- **생성 프롬프트**:

  ```
  '영상 업로드' 화면의 '업로드 진행 중' 상태·세부 화면을 만들어줘. 파일별 진행률 바와 남은 시간· 개별 취소· 페이지 이탈 경고· 백그라운드 업로드 지원 안내. 상위 화면(영상 업로드)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CU0403 · 영상 업로드 > 인코딩 대기
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 영상 업로드` — 누르면 `CU0401`로 간다
- **일정**: 2027-01-25 ~ 2027-01-25
- **기능정의**: 업로드 후 인코딩 대기 순번 · 예상 완료 시각 · 완료 시 알림 받기 · 인코딩 중 미리보기 불가 안내
- **생성 프롬프트**:

  ```
  '영상 업로드' 화면의 '인코딩 대기' 상태·세부 화면을 만들어줘. 업로드 후 인코딩 대기 순번· 예상 완료 시각· 완료 시 알림 받기· 인코딩 중 미리보기 불가 안내. 상위 화면(영상 업로드)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CU0501 · 영상 업로드 - 실패
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 내 강의 목록` — 누르면 `CU0101`로 간다
- **일정**: 2027-01-26 ~ 2027-01-27
- **기능정의**: 실패 사유 표시(용량 초과·형식 불일치·네트워크) · 재시도 버튼 · 실패한 파일만 선택적 재업로드 · 지원 형식 재안내 · 고객센터 문의 링크
- **버튼 → 이동화면**:
  - `다시 시도` → `CU0401` 영상 업로드
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `실패한 줄의 「다시 시도」` → 그 줄만 다시 올라가고 상태가 바뀐다
  - `「실패한 것만 보기」 토글` → 목록이 걸러진다
- **생성 프롬프트**:

  ```
  영상 업로드 실패 화면을 만들어줘. 실패한 파일 목록을 보여주고 각각 실패 사유를 명확한 문장으로 표시 (예: 파일 용량이 2GB를 초과했어요 / 지원하지 않는 형식이에요), 실패한 파일만 골라서 다시 올릴 수 있는 재시도 버튼, 성공한 파일은 유지된다는 안내와 지원 형식 재안내를 넣어줘.
  ```

### CU0502 · 영상 업로드 - 실패 > 실패 파일 부분 재시도
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 영상 업로드 - 실패` — 누르면 `CU0501`로 간다
- **일정**: 2027-01-28 ~ 2027-01-28
- **기능정의**: 실패한 파일만 선택해 재업로드 · 성공 파일 유지 안내 · 사유별 해결 방법 표시 · 전체 취소 · 지원 형식 재안내
- **생성 프롬프트**:

  ```
  '영상 업로드 - 실패' 화면의 '실패 파일 부분 재시도' 상태·세부 화면을 만들어줘. 실패한 파일만 선택해 재업로드· 성공 파일 유지 안내· 사유별 해결 방법 표시· 전체 취소· 지원 형식 재안내. 상위 화면(영상 업로드 - 실패)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### CU0601 · 강의 게시 - 검수 대기
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 내 강의 목록` — 누르면 `CU0101`로 간다
- **일정**: 2027-01-29 ~ 2027-02-01
- **기능정의**: 검수 요청 완료 안내 · 예상 소요 기간 표시 · 검수 체크리스트 결과(필수 항목 충족 여부) · 검수 중 수정 불가 안내 · 요청 취소 버튼
- **버튼 → 이동화면**:
  - `내 강의 목록` → `CU0101` 내 강의 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `점검 항목 줄` → 무엇을 봤는지 설명이 펼쳐진다
  - `모든 항목이 통과되지 않은 상태` → 「검수 요청」이 눌리지 않고 모자란 항목이 표시된다
- **생성 프롬프트**:

  ```
  강의 검수 요청 완료 화면을 만들어줘. 체크 아이콘과 "검수를 요청했어요" 문구, "보통 2~3영업일이 걸려요" 안내, 검수 전 자동 체크 결과를 리스트로(썸네일 등록 ✓, 모든 차시 영상 연결 ✓, 강의 소개 작성 ✓), "검수 중에는 커리큘럼을 수정할 수 없어요" 주의 문구와 요청 취소 버튼.
  ```

### CU0602 · 강의 게시 - 검수 대기 > 반려됨
- **소속 메뉴**: 수업 편성
- **뒤로가기**: 좌측 상단에 `‹ 강의 게시 - 검수 대기` — 누르면 `CU0601`로 간다
- **일정**: 2027-02-02 ~ 2027-02-02
- **기능정의**: 반려 사유 항목별 표시 · 수정 가이드와 해당 화면 바로가기 · 재검수 요청 버튼 · 재검수 소요 기간 안내
- **생성 프롬프트**:

  ```
  '강의 게시 - 검수 대기' 화면의 '반려됨' 상태·세부 화면을 만들어줘. 반려 사유 항목별 표시· 수정 가이드와 해당 화면 바로가기· 재검수 요청 버튼· 재검수 소요 기간 안내. 상위 화면(강의 게시 - 검수 대기)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### ST0101 · 수강생 목록
- **소속 메뉴**: 학생 관리
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2027-02-03 ~ 2027-02-05
- **기능정의**: 강의별 수강생 테이블 · 진도율 막대와 정렬 · 최근 접속일 표시 · 이름·이메일 검색 · 진도 구간 필터(0%/1~49%/50~79%/80%+) · 엑셀 다운로드
- **버튼 → 이동화면**:
  - `수강생 상세` → `ST0301` 수강생 상세 - 진도 현황
  - `검색` → `ST0201` 수강생 목록 - 검색 결과 없음
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `진도 구간 필터(전체/미달/완주)` → 목록과 「N명」 숫자가 함께 걸러진다
  - `이름·이메일 검색` → 치는 대로 목록이 걸러진다
  - `표 머리글(진도율·최근 접속)` → 그 기준으로 정렬되고 화살표 방향이 바뀐다
  - `줄 왼쪽 체크` → 고른 수가 위에 뜨고 「선택한 N명에게 알림」이 켜진다
- **생성 프롬프트**:

  ```
  강사의 수강생 목록 화면을 만들어줘. 상단에 강의 선택 드롭다운과 검색창(이름, 이메일), 진도 구간 필터 칩(전체, 미시작, 진행 중, 수료 가능), 테이블 컬럼: 이름, 이메일, 수강 시작일, 진도율(막대+퍼센트), 과제 제출 수, 최근 접속일, 상태. 진도율 컬럼은 클릭으로 정렬 가능하게 하고, 우측 상단에 엑셀 다운로드 버튼.
  ```

### ST0102 · 수강생 목록 > 진도 구간 필터
- **소속 메뉴**: 학생 관리
- **뒤로가기**: 좌측 상단에 `‹ 수강생 목록` — 누르면 `ST0101`로 간다
- **일정**: 2027-02-08 ~ 2027-02-09
- **기능정의**: 미시작·진행 중·수료 가능 칩 · 구간별 인원수 표시 · 복수 선택 · 초기화 · 결과 수 실시간 갱신
- **생성 프롬프트**:

  ```
  '수강생 목록' 화면의 '진도 구간 필터' 상태·세부 화면을 만들어줘. 미시작·진행 중·수료 가능 칩· 구간별 인원수 표시· 복수 선택· 초기화· 결과 수 실시간 갱신. 상위 화면(수강생 목록)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### ST0103 · 수강생 목록 > 엑셀 다운로드
- **소속 메뉴**: 학생 관리
- **뒤로가기**: 좌측 상단에 `‹ 수강생 목록` — 누르면 `ST0101`로 간다
- **일정**: 2027-02-10 ~ 2027-02-10
- **기능정의**: 내려받을 항목 선택 · 현재 필터 조건 적용 여부 선택 · 개인정보 마스킹 옵션 · 파일 생성 대기 후 다운로드
- **생성 프롬프트**:

  ```
  '수강생 목록' 화면의 '엑셀 다운로드' 상태·세부 화면을 만들어줘. 내려받을 항목 선택· 현재 필터 조건 적용 여부 선택· 개인정보 마스킹 옵션· 파일 생성 대기 후 다운로드. 상위 화면(수강생 목록)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### ST0104 · 수강생 목록 > 수강생 없음
- **소속 메뉴**: 학생 관리
- **뒤로가기**: 좌측 상단에 `‹ 수강생 목록` — 누르면 `ST0101`로 간다
- **일정**: 2027-02-11 ~ 2027-02-11
- **기능정의**: 선택한 강의에 수강생 없음 안내 · 다른 강의 선택 · 강의 링크 공유 · 게시 상태 확인 유도
- **생성 프롬프트**:

  ```
  '수강생 목록' 화면의 '수강생 없음' 상태·세부 화면을 만들어줘. 선택한 강의에 수강생 없음 안내· 다른 강의 선택· 강의 링크 공유· 게시 상태 확인 유도. 상위 화면(수강생 목록)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### ST0201 · 수강생 목록 - 검색 결과 없음
- **소속 메뉴**: 학생 관리
- **뒤로가기**: 좌측 상단에 `‹ 수강생 목록` — 누르면 `ST0101`로 간다
- **일정**: 2027-02-12 ~ 2027-02-15
- **기능정의**: 검색 조건에 맞는 수강생이 없다는 안내 · 적용된 필터 칩 표시 · 필터 초기화 버튼 · 전체 수강생 수 안내 · 검색 팁 제공
- **버튼 → 이동화면**:
  - `필터 초기화` → `ST0101` 수강생 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `검색어 지우기` → 빈 화면 대신 전체 수강생 목록이 나타난다
- **생성 프롬프트**:

  ```
  수강생 검색 결과가 없을 때의 화면을 만들어줘. 테이블 자리에 아이콘과 "조건에 맞는 수강생이 없어요" 문구, 현재 적용된 필터를 칩으로 보여주고 각각 X로 제거 가능하게, "필터 초기화" 버튼과 "전체 수강생은 128명이에요" 안내를 넣어줘.
  ```

### ST0202 · 수강생 목록 - 검색 결과 없음 > 검색 팁
- **소속 메뉴**: 학생 관리
- **뒤로가기**: 좌측 상단에 `‹ 수강생 목록 - 검색 결과 없음` — 누르면 `ST0201`로 간다
- **일정**: 2027-02-16 ~ 2027-02-17
- **기능정의**: 이름·이메일 부분 검색 안내 · 탈퇴·환불 수강생 제외 안내 · 최근 검색 조건 다시 적용 · 전체 보기로 복귀
- **생성 프롬프트**:

  ```
  '수강생 목록 - 검색 결과 없음' 화면의 '검색 팁' 상태·세부 화면을 만들어줘. 이름·이메일 부분 검색 안내· 탈퇴·환불 수강생 제외 안내· 최근 검색 조건 다시 적용· 전체 보기로 복귀. 상위 화면(수강생 목록 - 검색 결과 없음)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### ST0301 · 수강생 상세 - 진도 현황
- **소속 메뉴**: 학생 관리
- **뒤로가기**: 좌측 상단에 `‹ 수강생 목록` — 누르면 `ST0101`로 간다
- **일정**: 2027-02-18 ~ 2027-02-19
- **기능정의**: 수강생 기본 정보 · 차시별 시청 완료 여부 체크리스트 · 과제 제출 이력과 점수 · 질문 작성 이력 · 학습 시간 추이 그래프 · 개별 메시지 발송 버튼
- **버튼 → 이동화면**:
  - `메시지 발송` → `ST0501` 알림 발송
  - `과제 채점` → `GR0301` 과제 채점 - 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `차시별 진도 줄` → 언제 봤는지·몇 번 봤는지가 펼쳐진다
  - `기간 탭` → 학습 시간 그래프가 그 기간 것으로 바뀐다
- **생성 프롬프트**:

  ```
  수강생 상세 화면을 만들어줘. 상단에 프로필(이름, 이메일, 수강 시작일, 전체 진도율 큰 숫자), 탭으로 구분: 차시별 진도(챕터별 차시 목록에 시청 완료 체크와 시청 시간), 과제(제출일, 점수, 피드백), 질문 이력, 학습 시간 추이 라인 차트. 우측 상단에 "메시지 보내기" 버튼.
  ```

### ST0302 · 수강생 상세 - 진도 현황 > 과제 이력 탭
- **소속 메뉴**: 학생 관리
- **뒤로가기**: 좌측 상단에 `‹ 수강생 목록` — 누르면 `ST0101`로 간다
- **일정**: 2027-02-22 ~ 2027-02-23
- **기능정의**: 과제별 제출일·점수·피드백 · 미제출 항목 표시 · 지각 제출 배지 · 채점 화면 바로가기 · 총점 요약
- **생성 프롬프트**:

  ```
  '수강생 상세 - 진도 현황' 화면의 '과제 이력 탭' 탭을 만들어줘. 과제별 제출일·점수·피드백· 미제출 항목 표시· 지각 제출 배지· 채점 화면 바로가기· 총점 요약. 상위 화면(수강생 상세 - 진도 현황)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### ST0303 · 수강생 상세 - 진도 현황 > 질문 이력 탭
- **소속 메뉴**: 학생 관리
- **뒤로가기**: 좌측 상단에 `‹ 수강생 목록` — 누르면 `ST0101`로 간다
- **일정**: 2027-02-24 ~ 2027-02-24
- **기능정의**: 작성한 질문 목록과 관련 차시 · 답변 여부 표시 · 미답변 강조 · 답변 작성 화면 이동
- **생성 프롬프트**:

  ```
  '수강생 상세 - 진도 현황' 화면의 '질문 이력 탭' 탭을 만들어줘. 작성한 질문 목록과 관련 차시· 답변 여부 표시· 미답변 강조· 답변 작성 화면 이동. 상위 화면(수강생 상세 - 진도 현황)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### ST0304 · 수강생 상세 - 진도 현황 > 학습 시간 추이
- **소속 메뉴**: 학생 관리
- **뒤로가기**: 좌측 상단에 `‹ 수강생 상세 - 진도 현황` — 누르면 `ST0301`로 간다
- **일정**: 2027-02-25 ~ 2027-02-25
- **기능정의**: 일자별 학습 시간 라인 차트 · 기간 전환 · 미접속 구간 강조 · 강의 평균 대비 비교 · 데이터 없음 처리
- **생성 프롬프트**:

  ```
  '수강생 상세 - 진도 현황' 화면의 '학습 시간 추이' 상태·세부 화면을 만들어줘. 일자별 학습 시간 라인 차트· 기간 전환· 미접속 구간 강조· 강의 평균 대비 비교· 데이터 없음 처리. 상위 화면(수강생 상세 - 진도 현황)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### ST0401 · 진도 미달자 관리
- **소속 메뉴**: 학생 관리
- **뒤로가기**: 좌측 상단에 `‹ 수강생 목록` — 누르면 `ST0101`로 간다
- **일정**: 2027-02-26 ~ 2027-03-01
- **기능정의**: 기준 진도율 미만 수강생 자동 추출 · 미달 기간 표시(N일째 미접속) · 일괄 선택 체크박스 · 독려 메시지 일괄 발송 · 기준 진도율 설정 변경
- **버튼 → 이동화면**:
  - `메시지 발송` → `ST0501` 알림 발송
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `미달 기준 바꾸기` → 대상 인원 수와 아래 목록이 그 자리에서 다시 계산된다
  - `줄 체크·전체 선택` → 고른 수가 위에 뜨고 발송 버튼이 켜진다
- **생성 프롬프트**:

  ```
  진도 미달 학생 관리 화면을 만들어줘. 상단에 기준 설정(진도율 N% 미만, N일 이상 미접속)을 조정하는 컨트롤, 해당 조건에 걸린 수강생 목록을 체크박스가 있는 테이블로 표시(이름, 진도율, 마지막 접속일, 미접속 일수), 전체 선택 체크박스와 "선택한 N명에게 독려 메시지 보내기" 버튼을 상단에 고정해줘.
  ```

### ST0402 · 진도 미달자 관리 > 기준 설정
- **소속 메뉴**: 학생 관리
- **뒤로가기**: 좌측 상단에 `‹ 진도 미달자 관리` — 누르면 `ST0401`로 간다
- **일정**: 2027-03-02 ~ 2027-03-02
- **기능정의**: 진도율 기준과 미접속 일수 슬라이더 · 대상 인원 실시간 갱신 · 기준 저장 · 기본값 복원
- **생성 프롬프트**:

  ```
  '진도 미달자 관리' 화면의 '기준 설정' 상태·세부 화면을 만들어줘. 진도율 기준과 미접속 일수 슬라이더· 대상 인원 실시간 갱신· 기준 저장· 기본값 복원. 상위 화면(진도 미달자 관리)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### ST0403 · 진도 미달자 관리 > 대상자 없음
- **소속 메뉴**: 학생 관리
- **뒤로가기**: 좌측 상단에 `‹ 진도 미달자 관리` — 누르면 `ST0401`로 간다
- **일정**: 2027-03-03 ~ 2027-03-03
- **기능정의**: 조건에 걸린 미달자 없음 안내 · 기준 완화 제안 · 전체 수강생 진도 분포 요약 · 수강생 목록으로 이동
- **생성 프롬프트**:

  ```
  '진도 미달자 관리' 화면의 '대상자 없음' 상태·세부 화면을 만들어줘. 조건에 걸린 미달자 없음 안내· 기준 완화 제안· 전체 수강생 진도 분포 요약· 수강생 목록으로 이동. 상위 화면(진도 미달자 관리)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### ST0501 · 알림 발송
- **소속 메뉴**: 학생 관리
- **뒤로가기**: 좌측 상단에 `‹ 수강생 목록` — 누르면 `ST0101`로 간다
- **일정**: 2027-03-04 ~ 2027-03-05
- **기능정의**: 수신 대상 확인(선택 인원 수) · 발송 채널 선택(이메일·앱 푸시·문자) · 템플릿 선택과 치환 변수 안내 · 메시지 미리보기 · 예약 발송 시간 설정
- **버튼 → 이동화면**:
  - `수강생 목록` → `ST0101` 수강생 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `받는 사람 고르기` → 고른 수가 「N명에게 보냅니다」로 바뀐다
  - `문구 틀 고르기` → 아래 미리보기 글이 그 틀로 바뀐다
  - `문구 입력` → 치는 대로 미리보기와 글자 수가 따라 바뀐다
- **생성 프롬프트**:

  ```
  수강생 알림 발송 화면을 만들어줘. 상단에 수신 대상 요약("선택한 12명에게 발송"), 대상 목록 펼쳐보기, 발송 채널 체크박스(이메일, 앱 푸시, 문자), 템플릿 선택 드롭다운(진도 독려, 과제 마감 안내, 공지), 제목과 본문 입력(치환 변수 {이름} {강의명} {진도율} 안내 표시), 우측에 실시간 미리보기, 하단에 즉시 발송/예약 발송 선택과 발송 버튼.
  ```

### ST0502 · 알림 발송 > 미리보기
- **소속 메뉴**: 학생 관리
- **뒤로가기**: 좌측 상단에 `‹ 알림 발송` — 누르면 `ST0501`로 간다
- **일정**: 2027-03-08 ~ 2027-03-08
- **기능정의**: 치환 변수에 실제 값 적용 · 채널별(이메일·푸시·문자) 레이아웃 전환 · 글자 수와 분할 발송 안내 · 대상 1인 샘플 확인
- **생성 프롬프트**:

  ```
  '알림 발송' 화면의 '미리보기' 상태·세부 화면을 만들어줘. 치환 변수에 실제 값 적용· 채널별(이메일·푸시·문자) 레이아웃 전환· 글자 수와 분할 발송 안내· 대상 1인 샘플 확인. 상위 화면(알림 발송)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### ST0503 · 알림 발송 > 일부 발송 실패
- **소속 메뉴**: 학생 관리
- **뒤로가기**: 좌측 상단에 `‹ 알림 발송` — 누르면 `ST0501`로 간다
- **일정**: 2027-03-09 ~ 2027-03-09
- **기능정의**: 수신 거부·주소 오류 등 실패 건 목록 · 사유별 분류 · 실패 건만 재발송 · 성공 건수 요약
- **생성 프롬프트**:

  ```
  '알림 발송' 화면의 '일부 발송 실패' 상태·세부 화면을 만들어줘. 수신 거부·주소 오류 등 실패 건 목록· 사유별 분류· 실패 건만 재발송· 성공 건수 요약. 상위 화면(알림 발송)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### GR0101 · 출석 현황
- **소속 메뉴**: 출결·평가
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2027-03-10 ~ 2027-03-11
- **기능정의**: 주차별 출석 매트릭스 표 · 출석/지각/결석 색상 구분 · 개인별 출석률 합계 · 기간 선택 필터 · 출석 기준 안내 · 엑셀 내보내기
- **버튼 → 이동화면**:
  - `과제 채점` → `GR0201` 과제 채점 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `기간·차시 필터` → 출석 표와 출석률 숫자가 함께 바뀐다
  - `표 칸(출석/결석)` → 눌러서 상태가 바뀌고 그 줄의 출석률이 다시 계산된다
- **생성 프롬프트**:

  ```
  출석 현황 화면을 만들어줘. 가로축은 주차(1주~12주), 세로축은 수강생 이름인 매트릭스 표. 각 셀은 출석(초록), 지각(노랑), 결석(빨강), 해당없음(회색)으로 색상 구분하고 마우스 오버 시 상세 정보. 우측 끝에 개인별 출석률 퍼센트, 하단에 주차별 평균 출석률. 상단에 기간 필터와 엑셀 내보내기 버튼, 색상 범례를 넣어줘.
  ```

### GR0102 · 출석 현황 > 기간 필터
- **소속 메뉴**: 출결·평가
- **뒤로가기**: 좌측 상단에 `‹ 출석 현황` — 누르면 `GR0101`로 간다
- **일정**: 2027-03-12 ~ 2027-03-15
- **기능정의**: 주차 범위 선택 · 강의·분반 필터 · 출석률 합계 재계산 · 적용 조건 요약 · 초기화
- **생성 프롬프트**:

  ```
  '출석 현황' 화면의 '기간 필터' 상태·세부 화면을 만들어줘. 주차 범위 선택· 강의·분반 필터· 출석률 합계 재계산· 적용 조건 요약· 초기화. 상위 화면(출석 현황)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### GR0103 · 출석 현황 > 출석 수정 모달
- **소속 메뉴**: 출결·평가
- **뒤로가기**: 좌측 상단에 `‹ 출석 현황` — 누르면 `GR0101`로 간다
- **일정**: 2027-03-16 ~ 2027-03-17
- **기능정의**: 셀 클릭 시 출석·지각·결석 변경 · 변경 사유 입력 · 수정 이력 기록 · 수강생 통지 여부 선택 · 저장
- **생성 프롬프트**:

  ```
  '출석 현황' 화면의 '출석 수정 모달' 상태·세부 화면을 만들어줘. 셀 클릭 시 출석·지각·결석 변경· 변경 사유 입력· 수정 이력 기록· 수강생 통지 여부 선택· 저장. 상위 화면(출석 현황)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### GR0201 · 과제 채점 목록
- **소속 메뉴**: 출결·평가
- **뒤로가기**: 좌측 상단에 `‹ 출석 현황` — 누르면 `GR0101`로 간다
- **일정**: 2027-03-18 ~ 2027-03-19
- **기능정의**: 채점 대기·완료 탭 구분 · 제출일 기준 정렬 · 지각 제출 배지 표시 · 과제별 제출률 표시 · 일괄 채점 진입 · 미채점 건수 강조
- **버튼 → 이동화면**:
  - `채점하기` → `GR0301` 과제 채점 - 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `채점 전·채점 완료 탭` → 목록이 바뀌고 탭 옆 숫자도 바뀐다
  - `줄 체크` → 고른 수가 뜨고 「일괄 채점」이 켜진다
- **생성 프롬프트**:

  ```
  과제 채점 목록 화면을 만들어줘. 상단 탭(채점 대기 24, 채점 완료 108) — 대기 건수는 오렌지로 강조, 목록 행에 수강생 이름, 과제명, 제출일시, 지각 여부 배지, 첨부 파일 아이콘, 채점하기 버튼. 상단에 과제 선택 드롭다운과 정렬(제출일순, 이름순), 해당 과제의 제출률 표시(제출 84/128명).
  ```

### GR0202 · 과제 채점 목록 > 채점 완료 탭
- **소속 메뉴**: 출결·평가
- **뒤로가기**: 좌측 상단에 `‹ 출석 현황` — 누르면 `GR0101`로 간다
- **일정**: 2027-03-22 ~ 2027-03-22
- **기능정의**: 채점 완료 목록 · 점수와 채점일 · 재채점 진입 · 작성한 피드백 열람 · 점수 분포 요약
- **생성 프롬프트**:

  ```
  '과제 채점 목록' 화면의 '채점 완료 탭' 탭을 만들어줘. 채점 완료 목록· 점수와 채점일· 재채점 진입· 작성한 피드백 열람· 점수 분포 요약. 상위 화면(과제 채점 목록)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### GR0203 · 과제 채점 목록 > 채점 대기 없음
- **소속 메뉴**: 출결·평가
- **뒤로가기**: 좌측 상단에 `‹ 과제 채점 목록` — 누르면 `GR0201`로 간다
- **일정**: 2027-03-23 ~ 2027-03-23
- **기능정의**: 채점할 과제 없음 안내 · 최근 채점 요약 · 다른 과제 선택 · 마감 예정 과제 안내
- **생성 프롬프트**:

  ```
  '과제 채점 목록' 화면의 '채점 대기 없음' 상태·세부 화면을 만들어줘. 채점할 과제 없음 안내· 최근 채점 요약· 다른 과제 선택· 마감 예정 과제 안내. 상위 화면(과제 채점 목록)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### GR0301 · 과제 채점 - 상세
- **소속 메뉴**: 출결·평가
- **뒤로가기**: 좌측 상단에 `‹ 출석 현황` — 누르면 `GR0101`로 간다
- **일정**: 2027-03-24 ~ 2027-03-25
- **기능정의**: 제출 답안 본문과 첨부 파일 미리보기 · 채점 기준표(루브릭) 항목별 점수 입력 · 총점 자동 합산 · 피드백 코멘트 작성 · 이전/다음 학생 이동 · 임시 저장
- **버튼 → 이동화면**:
  - `채점 완료` → `GR0201` 과제 채점 목록
  - `수료 기준` → `GR0401` 수료 기준 설정
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `점수 입력` → 총점과 통과 여부 배지가 그 자리에서 바뀐다
  - `항목별 점수 슬라이더` → 총점이 따라 바뀐다
  - `「이전 제출물」` → 같은 학생의 지난 제출이 펼쳐진다
- **생성 프롬프트**:

  ```
  과제 채점 상세 화면을 만들어줘. 좌측에 학생이 제출한 답안 본문과 첨부 파일 미리보기(PDF 뷰어), 우측에 채점 패널: 루브릭 항목별 점수 입력(항목명, 배점, 입력칸), 총점 자동 합산 표시, 피드백 코멘트 텍스트 영역, 자주 쓰는 피드백 문구 선택 버튼. 상단에 "3/24" 진행 표시와 이전/다음 학생 이동 버튼, 하단에 임시 저장과 채점 완료 버튼.
  ```

### GR0302 · 과제 채점 - 상세 > 루브릭 점수 입력
- **소속 메뉴**: 출결·평가
- **뒤로가기**: 좌측 상단에 `‹ 과제 채점 - 상세` — 누르면 `GR0301`로 간다
- **일정**: 2027-03-26 ~ 2027-03-26
- **기능정의**: 항목별 배점과 점수 입력 · 총점 자동 합산 · 배점 초과 경고 · 감점 사유 메모 · 임시 저장
- **생성 프롬프트**:

  ```
  '과제 채점 - 상세' 화면의 '루브릭 점수 입력' 상태·세부 화면을 만들어줘. 항목별 배점과 점수 입력· 총점 자동 합산· 배점 초과 경고· 감점 사유 메모· 임시 저장. 상위 화면(과제 채점 - 상세)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### GR0303 · 과제 채점 - 상세 > 첨부 파일 미리보기
- **소속 메뉴**: 출결·평가
- **뒤로가기**: 좌측 상단에 `‹ 과제 채점 - 상세` — 누르면 `GR0301`로 간다
- **일정**: 2027-03-29 ~ 2027-03-29
- **기능정의**: PDF·이미지 인라인 뷰어 · 페이지 이동과 확대 · 지원하지 않는 형식 다운로드 안내 · 원본 내려받기
- **생성 프롬프트**:

  ```
  '과제 채점 - 상세' 화면의 '첨부 파일 미리보기' 상태·세부 화면을 만들어줘. PDF·이미지 인라인 뷰어· 페이지 이동과 확대· 지원하지 않는 형식 다운로드 안내· 원본 내려받기. 상위 화면(과제 채점 - 상세)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### GR0304 · 과제 채점 - 상세 > 미제출 학생
- **소속 메뉴**: 출결·평가
- **뒤로가기**: 좌측 상단에 `‹ 과제 채점 - 상세` — 누르면 `GR0301`로 간다
- **일정**: 2027-03-30 ~ 2027-03-30
- **기능정의**: 제출 내역 없음 안내 · 0점 처리 또는 보류 선택 · 제출 독려 메시지 발송 · 마감 연장 진입
- **생성 프롬프트**:

  ```
  '과제 채점 - 상세' 화면의 '미제출 학생' 상태·세부 화면을 만들어줘. 제출 내역 없음 안내· 0점 처리 또는 보류 선택· 제출 독려 메시지 발송· 마감 연장 진입. 상위 화면(과제 채점 - 상세)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### GR0401 · 수료 기준 설정
- **소속 메뉴**: 출결·평가
- **뒤로가기**: 좌측 상단에 `‹ 출석 현황` — 누르면 `GR0101`로 간다
- **일정**: 2027-03-31 ~ 2027-04-01
- **기능정의**: 진도율 기준 설정(슬라이더) · 과제 제출 최소 개수 · 출석률 기준 · 최종 평가 점수 기준 · 기준 변경 시 영향받는 인원 실시간 표시 · 수료증 양식 선택
- **버튼 → 이동화면**:
  - `수료 처리` → `GR0501` 수료 처리 - 확인
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `수료 기준 값 바꾸기(진도율·과제·시험)` → 아래 「지금 기준이면 N명이 수료」가 다시 계산된다
  - `기준 항목 켜고 끄기` → 그 항목이 계산에서 빠지고 인원이 바뀐다
- **생성 프롬프트**:

  ```
  수료 기준 설정 화면을 만들어줘. 각 기준을 설정하는 컨트롤: 진도율 최소 기준(슬라이더 0~100%), 과제 제출 최소 개수(숫자 입력), 출석률 최소 기준(슬라이더), 최종 평가 점수 기준(숫자). 기준을 조정할 때마다 우측에 "현재 기준으로 수료 가능: 96명 / 미달: 32명"을 실시간으로 갱신해서 보여줘. 하단에 수료증 양식 선택과 저장 버튼.
  ```

### GR0402 · 수료 기준 설정 > 영향 인원 시뮬레이션
- **소속 메뉴**: 출결·평가
- **뒤로가기**: 좌측 상단에 `‹ 수료 기준 설정` — 누르면 `GR0401`로 간다
- **일정**: 2027-04-02 ~ 2027-04-02
- **기능정의**: 기준 조정 시 수료 가능·미달 인원 실시간 갱신 · 항목별 미달 사유 분포 · 이전 기준과 비교 · 상세 명단 보기
- **생성 프롬프트**:

  ```
  '수료 기준 설정' 화면의 '영향 인원 시뮬레이션' 상태·세부 화면을 만들어줘. 기준 조정 시 수료 가능·미달 인원 실시간 갱신· 항목별 미달 사유 분포· 이전 기준과 비교· 상세 명단 보기. 상위 화면(수료 기준 설정)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### GR0403 · 수료 기준 설정 > 게시 후 기준 변경 경고
- **소속 메뉴**: 출결·평가
- **뒤로가기**: 좌측 상단에 `‹ 수료 기준 설정` — 누르면 `GR0401`로 간다
- **일정**: 2027-04-05 ~ 2027-04-05
- **기능정의**: 수강 중 강의의 기준 변경 영향 안내 · 이미 수료 처리된 인원 유지 여부 · 수강생 고지 필요 안내 · 취소·적용
- **생성 프롬프트**:

  ```
  '수료 기준 설정' 화면의 '게시 후 기준 변경 경고' 상태·세부 화면을 만들어줘. 수강 중 강의의 기준 변경 영향 안내· 이미 수료 처리된 인원 유지 여부· 수강생 고지 필요 안내· 취소·적용. 상위 화면(수료 기준 설정)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### GR0501 · 수료 처리 - 확인
- **소속 메뉴**: 출결·평가
- **뒤로가기**: 좌측 상단에 `‹ 출석 현황` — 누르면 `GR0101`로 간다
- **일정**: 2027-04-06 ~ 2027-04-07
- **기능정의**: 수료 대상자 명단 최종 확인 · 인원수와 기준 재확인 · 되돌릴 수 없다는 경고 표시 · 수료증 일괄 발급 옵션 · 알림 발송 여부 선택 · 최종 확인 체크박스
- **버튼 → 이동화면**:
  - `출석 현황` → `GR0101` 출석 현황
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `대상 목록의 체크` → 수료 처리할 인원 수가 바뀐다
  - `「제외 사유」 고르기` → 그 줄이 대상에서 빠지고 인원이 줄어든다
- **생성 프롬프트**:

  ```
  수료 처리 최종 확인 화면(모달)을 만들어줘. "96명을 수료 처리합니다" 큰 문구와 적용된 기준 요약, 수료 대상자 명단을 스크롤 목록으로 확인 가능하게, "수료 처리 후에는 되돌릴 수 없어요"라는 경고를 눈에 띄게, 옵션 체크박스(수료증 자동 발급, 수료 안내 메일 발송), 최종 확인 체크박스를 체크해야 "수료 처리" 버튼이 활성화되게 해줘.
  ```

### GR0502 · 수료 처리 - 확인 > 처리 실패
- **소속 메뉴**: 출결·평가
- **뒤로가기**: 좌측 상단에 `‹ 수료 처리 - 확인` — 누르면 `GR0501`로 간다
- **일정**: 2027-04-08 ~ 2027-04-08
- **기능정의**: 일부 수강생 처리 실패 목록과 사유 · 성공 건은 유지된다는 안내 · 실패 건만 재시도 · 고객센터 문의
- **생성 프롬프트**:

  ```
  '수료 처리 - 확인' 화면의 '처리 실패' 상태·세부 화면을 만들어줘. 일부 수강생 처리 실패 목록과 사유· 성공 건은 유지된다는 안내· 실패 건만 재시도· 고객센터 문의. 상위 화면(수료 처리 - 확인)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### MY0101 · 내 정보
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2027-04-09 ~ 2027-04-12
- **기능정의**: 프로필 이미지와 닉네임 수정 · 이메일·비밀번호 변경 · 알림 수신 설정(이메일/푸시/문자) · 강사 전환 신청 버튼 · 회원 탈퇴 링크
- **버튼 → 이동화면**:
  - `수강 이력` → `MY0201` 수강 이력
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「수정」` → 읽는 모양이던 칸이 입력 칸으로 바뀌고 저장하면 되돌아온다
  - `알림 받기 토글` → 켜고 끈 것이 그대로 남는다
- **생성 프롬프트**:

  ```
  마이페이지 내 정보 화면을 만들어줘. 좌측에 마이페이지 사이드 메뉴(내 정보, 수강 이력, 정산 내역, 알림 설정), 우측 본문에 프로필 이미지 업로드와 닉네임, 이메일(변경 버튼), 비밀번호 변경, 알림 수신 설정 토글 3개(이메일, 앱 푸시, 문자), 하단에 "강사로 전환 신청" 버튼과 회색 작은 글씨로 회원 탈퇴 링크.
  ```

### MY0102 · 내 정보 > 비밀번호 변경
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 내 정보` — 누르면 `MY0101`로 간다
- **일정**: 2027-04-13 ~ 2027-04-13
- **기능정의**: 현재 비밀번호 확인 · 새 비밀번호 규칙 실시간 검증 · 재입력 일치 확인 · 변경 후 재로그인 안내 · 실패 횟수 제한
- **생성 프롬프트**:

  ```
  '내 정보' 화면의 '비밀번호 변경' 상태·세부 화면을 만들어줘. 현재 비밀번호 확인· 새 비밀번호 규칙 실시간 검증· 재입력 일치 확인· 변경 후 재로그인 안내· 실패 횟수 제한. 상위 화면(내 정보)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### MY0103 · 내 정보 > 강사 전환 신청
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 내 정보` — 누르면 `MY0101`로 간다
- **일정**: 2027-04-14 ~ 2027-04-14
- **기능정의**: 전문 분야와 경력 입력 · 증빙 자료 첨부 · 정산 계좌 사전 안내 · 심사 소요 기간 표시 · 제출
- **생성 프롬프트**:

  ```
  '내 정보' 화면의 '강사 전환 신청' 상태·세부 화면을 만들어줘. 전문 분야와 경력 입력· 증빙 자료 첨부· 정산 계좌 사전 안내· 심사 소요 기간 표시· 제출. 상위 화면(내 정보)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### MY0104 · 내 정보 > 회원 탈퇴 확인
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 내 정보` — 누르면 `MY0101`로 간다
- **일정**: 2027-04-15 ~ 2027-04-16
- **기능정의**: 수강 중 강의·잔여 쿠폰·정산 예정 잔액 경고 · 삭제되는 데이터 범위 안내 · 탈퇴 사유 선택 · 확인 문구 입력 후 진행
- **생성 프롬프트**:

  ```
  '내 정보' 화면의 '회원 탈퇴 확인' 상태·세부 화면을 만들어줘. 수강 중 강의·잔여 쿠폰·정산 예정 잔액 경고· 삭제되는 데이터 범위 안내· 탈퇴 사유 선택· 확인 문구 입력 후 진행. 상위 화면(내 정보)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### MY0201 · 수강 이력
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 내 정보` — 누르면 `MY0101`로 간다
- **일정**: 2027-04-19 ~ 2027-04-20
- **기능정의**: 수강 완료 강의 목록과 수료 여부 · 결제 내역과 영수증 발급 · 환불 신청 버튼(가능 기간 표시) · 기간별 필터 · 수료증 재발급
- **버튼 → 이동화면**:
  - `수료증 발급` → `CL0801` 수료증 발급
  - `정산 내역` → `MY0301` 정산 내역
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `기간·상태 필터` → 이력 목록과 합계가 함께 걸러진다
  - `줄 펼치기` → 결제 내역과 수료 여부가 펼쳐진다
- **생성 프롬프트**:

  ```
  수강 이력 화면을 만들어줘. 기간 필터(전체, 최근 3개월, 최근 1년)와 함께 테이블 또는 카드 목록으로 강의명, 수강 기간, 결제 금액, 수료 여부 배지, 영수증 버튼. 환불 가능 기간 내인 항목에는 "환불 신청" 버튼을 표시하고, 수료한 강의에는 "수료증 다시 받기" 버튼을 넣어줘.
  ```

### MY0202 · 수강 이력 > 환불 신청
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 수강 이력` — 누르면 `MY0201`로 간다
- **일정**: 2027-04-21 ~ 2027-04-21
- **기능정의**: 환불 가능 여부와 기준 진도율 안내 · 사유 선택과 상세 입력 · 예상 환불 금액 계산 · 처리 소요 안내 · 신청
- **생성 프롬프트**:

  ```
  '수강 이력' 화면의 '환불 신청' 상태·세부 화면을 만들어줘. 환불 가능 여부와 기준 진도율 안내· 사유 선택과 상세 입력· 예상 환불 금액 계산· 처리 소요 안내· 신청. 상위 화면(수강 이력)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### MY0203 · 수강 이력 > 환불 불가 상태
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 수강 이력` — 누르면 `MY0201`로 간다
- **일정**: 2027-04-22 ~ 2027-04-22
- **기능정의**: 진도율 초과·기간 경과 등 불가 사유 표시 · 관련 규정 조항 노출 · 부분 환불 가능 여부 · 고객센터 문의
- **생성 프롬프트**:

  ```
  '수강 이력' 화면의 '환불 불가 상태' 상태·세부 화면을 만들어줘. 진도율 초과·기간 경과 등 불가 사유 표시· 관련 규정 조항 노출· 부분 환불 가능 여부· 고객센터 문의. 상위 화면(수강 이력)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### MY0204 · 수강 이력 > 기간 필터
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 수강 이력` — 누르면 `MY0201`로 간다
- **일정**: 2027-04-23 ~ 2027-04-26
- **기능정의**: 최근 3개월·1년·전체 전환 · 수료 여부 필터 · 결과 수와 합계 금액 갱신 · 초기화
- **생성 프롬프트**:

  ```
  '수강 이력' 화면의 '기간 필터' 상태·세부 화면을 만들어줘. 최근 3개월·1년·전체 전환· 수료 여부 필터· 결과 수와 합계 금액 갱신· 초기화. 상위 화면(수강 이력)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### MY0301 · 정산 내역
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 내 정보` — 누르면 `MY0101`로 간다
- **일정**: 2027-04-27 ~ 2027-04-28
- **기능정의**: 월별 정산 금액 요약 카드 · 강의별 매출 상세 내역 · 정산 상태 배지(정산 예정/완료) · 수수료 공제 내역 표시 · 세금계산서 발행 · 정산 계좌 관리
- **버튼 → 이동화면**:
  - `내역 없음` → `MY0401` 정산 내역 - 내역 없음
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `기간 고르기` → 표와 위의 합계 금액이 그 기간 것으로 다시 계산된다
  - `표 머리글(날짜·금액)` → 그 기준으로 정렬되고 화살표가 바뀐다
  - `정산 줄 펼치기` → 건별 내역과 수수료 계산이 펼쳐진다
- **생성 프롬프트**:

  ```
  강사 정산 내역 화면을 만들어줘. 상단에 요약 카드 3개(이번 달 예상 정산액, 누적 정산액, 정산 예정일), 월 선택 드롭다운과 함께 강의별 매출 테이블 (강의명, 판매 건수, 총 매출, 플랫폼 수수료, 실 정산액, 상태 배지), 하단에 합계 행과 세금계산서 발행 버튼, 우측 상단에 정산 계좌 관리 링크.
  ```

### MY0302 · 정산 내역 > 정산 계좌 관리
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 정산 내역` — 누르면 `MY0301`로 간다
- **일정**: 2027-04-29 ~ 2027-04-30
- **기능정의**: 예금주·은행·계좌번호 입력 · 1원 인증 · 사업자 정보 등록 · 변경 시 다음 정산부터 반영 안내
- **생성 프롬프트**:

  ```
  '정산 내역' 화면의 '정산 계좌 관리' 상태·세부 화면을 만들어줘. 예금주·은행·계좌번호 입력· 1원 인증· 사업자 정보 등록· 변경 시 다음 정산부터 반영 안내. 상위 화면(정산 내역)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### MY0303 · 정산 내역 > 세금계산서 발행
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 정산 내역` — 누르면 `MY0301`로 간다
- **일정**: 2027-05-03 ~ 2027-05-03
- **기능정의**: 발행 대상 월 선택 · 사업자 정보 확인 · 발행과 재발행 · 이메일 전송 · 발행 이력 조회
- **생성 프롬프트**:

  ```
  '정산 내역' 화면의 '세금계산서 발행' 상태·세부 화면을 만들어줘. 발행 대상 월 선택· 사업자 정보 확인· 발행과 재발행· 이메일 전송· 발행 이력 조회. 상위 화면(정산 내역)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### MY0401 · 정산 내역 - 내역 없음
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 내 정보` — 누르면 `MY0101`로 간다
- **일정**: 2027-05-04 ~ 2027-05-05
- **기능정의**: 선택 기간에 정산 내역이 없다는 안내 · 다른 기간 선택 유도 · 첫 정산 안내 문구 · 정산 주기 안내 · 강의 개설 유도 버튼
- **버튼 → 이동화면**:
  - `강의 개설` → `CU0201` 강의 개설 - 기본 정보
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `기간 바꾸기` → 내역이 있으면 빈 화면 대신 표가 나타난다
- **생성 프롬프트**:

  ```
  정산 내역이 없을 때의 빈 화면을 만들어줘. 아이콘과 "2026년 8월에는 정산 내역이 없어요" 문구, "다른 기간 보기" 버튼과 정산 주기 안내("매월 15일에 전월 매출을 정산해요"), 아직 강의가 없는 강사를 위해 "강의 개설하기" CTA도 함께 보여줘.
  ```

### MY0402 · 정산 내역 - 내역 없음 > 첫 정산 예정 안내
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 정산 내역 - 내역 없음` — 누르면 `MY0401`로 간다
- **일정**: 2027-05-06 ~ 2027-05-06
- **기능정의**: 정산 주기와 지급일 안내 · 첫 매출 발생 시점 설명 · 정산 계좌 등록 여부 확인 · 강의 개설 유도
- **생성 프롬프트**:

  ```
  '정산 내역 - 내역 없음' 화면의 '첫 정산 예정 안내' 상태·세부 화면을 만들어줘. 정산 주기와 지급일 안내· 첫 매출 발생 시점 설명· 정산 계좌 등록 여부 확인· 강의 개설 유도. 상위 화면(정산 내역 - 내역 없음)의 레이아웃·컴포넌트·톤을 그대로 유지하면서 이 부분만 바뀐 상태를 보여줘.
  ```

### AU0101 · 로그인
- **소속 메뉴**: 계정
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2027-05-07 ~ 2027-05-10
- **기능정의**: 이메일·비밀번호 입력 · 소셜 로그인 3종(카카오·네이버·구글) · 로그인 상태 유지 체크 · 비밀번호 찾기 링크 · 회원가입 링크 · 입력 오류 안내 문구 자리 · 로그인 후 원래 보던 화면으로 돌아가기
- **버튼 → 이동화면**:
  - `회원가입` → `AU0201` 회원가입
  - `비밀번호 찾기` → `AU0501` 비밀번호 찾기
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「비밀번호 보기」 눈 아이콘` → 가려진 글자가 보이고 다시 누르면 가려진다
  - `빈 칸으로 로그인 시도` → 어느 칸이 비었는지 칸 아래에 안내가 뜬다
- **생성 프롬프트**:

  ```
  로그인 화면을 만들어줘. 헤더·GNB 없이 가운데 정렬된 단독 화면으로. 로고 아래 이메일과 비밀번호 입력칸, "로그인 상태 유지" 체크박스, 큰 로그인 버튼, 그 아래 비밀번호 찾기 링크. 구분선 아래 소셜 로그인 버튼 3개(카카오 노란색, 네이버 초록색, 구글 흰색)를 세로로, 맨 아래 "아직 회원이 아니신가요? 회원가입" 링크. 입력이 잘못됐을 때 뜨는 안내 문구 자리도 만들어줘.
  ```

### AU0201 · 회원가입
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `AU0101`로 간다
- **일정**: 2027-05-11 ~ 2027-05-13
- **기능정의**: 이메일·비밀번호·비밀번호 확인 입력 · 비밀번호 조건 실시간 표시 · 이름 입력 · 휴대폰 번호 입력 · 관심 분야 선택 · 약관 동의(전체·필수 2종·선택 1종) · 소셜로 간편 가입 · 이미 가입된 이메일 안내
- **버튼 → 이동화면**:
  - `다음` → `AU0301` 회원가입 - 휴대폰 인증
  - `로그인` → `AU0101` 로그인
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `비밀번호 입력` → 치는 대로 아래 세기 막대가 약함→보통→강함으로 바뀐다
  - `비밀번호 확인 칸` → 위와 다르면 그 자리에서 안내가 뜬다
  - `약관 「전체 동의」` → 아래 개별 체크가 한꺼번에 켜지고, 하나를 끄면 전체 동의도 풀린다
- **생성 프롬프트**:

  ```
  회원가입 화면을 만들어줘. 헤더 없이 단독 화면으로. 상단에 진행 표시(1 정보 입력 → 2 인증 → 3 완료). 이메일, 비밀번호, 비밀번호 확인 입력칸을 두고 비밀번호 조건(8자 이상·영문+숫자)을 입력하는 동안 체크 표시로 알려줘. 이름과 휴대폰 번호 입력칸, 관심 분야를 칩으로 고르는 영역(복수 선택), 약관 동의 영역은 "전체 동의" 하나와 그 아래 이용약관(필수), 개인정보 처리방침(필수), 마케팅 수신(선택)을 각각 체크박스로, 각 항목 오른쪽에 전문 보기 링크. 하단에 다음 버튼, 위쪽에 "소셜 계정으로 간편 가입" 버튼 3개. 이미 가입된 이메일일 때 뜨는 안내 문구 자리도 만들어줘.
  ```

### AU0301 · 회원가입 - 휴대폰 인증
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `AU0101`로 간다
- **일정**: 2027-05-14 ~ 2027-05-17
- **기능정의**: 휴대폰 번호 확인 표시 · 인증번호 6자리 입력 · 남은 시간 카운트다운 · 재발송 버튼(대기 시간 표시) · 번호 바꾸기 · 인증번호 불일치 안내 · 인증 완료 시 다음 단계
- **버튼 → 이동화면**:
  - `다음` → `AU0401` 회원가입 완료
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「인증번호 받기」` → 입력 칸이 열리고 3분 타이머가 줄어들기 시작한다
  - `타이머가 0이 됨` → 칸이 잠기고 「다시 받기」가 켜진다
  - `인증번호 6자리를 다 넣음` → 「확인」이 눌리게 켜진다
- **생성 프롬프트**:

  ```
  회원가입 중 휴대폰 인증 단계 화면을 만들어줘. 진행 표시에서 2단계를 강조하고, "010-1234-5678로 인증번호를 보냈어요" 문구 아래 6칸짜리 인증번호 입력칸을 크게 두고, 오른쪽에 남은 시간(03:00 카운트다운), 아래에 "인증번호 다시 받기" 버튼(재발송 대기 중이면 남은 초 표시)과 "번호 바꾸기" 링크. 인증번호가 틀렸을 때 입력칸이 빨갛게 바뀌고 안내가 뜨는 상태도 만들어줘.
  ```

### AU0401 · 회원가입 완료
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `AU0101`로 간다
- **일정**: 2027-05-18 ~ 2027-05-19
- **기능정의**: 가입 축하 문구와 이름 표시 · 첫 수강 할인 쿠폰 지급 안내 · 관심 분야 기반 추천 강의 3개 · 강의 둘러보기 버튼 · 내 강의실로 가기 · 강사 지원 안내 링크
- **버튼 → 이동화면**:
  - `강의 탐색` → `CO0101` 강의 목록
  - `강사 지원` → `TC0201` 강사 지원
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「관심 분야 고르기」` → 고른 것이 칩으로 쌓이고 아래 추천 강의가 그에 맞게 바뀐다
- **생성 프롬프트**:

  ```
  회원가입 완료 화면을 만들어줘. 헤더 없이 단독 화면으로. 큰 체크 아이콘과 "가입을 환영해요, 김수현님" 문구, 아래에 "첫 수강 20% 할인 쿠폰을 드렸어요" 쿠폰 카드, "이런 강의는 어때요?" 추천 강의 3개 카드(가입할 때 고른 관심 분야 기준), 큰 "강의 둘러보기" 버튼과 그 아래 "내 강의실로 가기" 링크, 맨 아래 옅게 "가르치는 일에 관심 있으세요? 강사 지원하기" 링크.
  ```

### AU0501 · 비밀번호 찾기
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인` — 누르면 `AU0101`로 간다
- **일정**: 2027-05-20 ~ 2027-05-21
- **기능정의**: 가입한 이메일 입력 · 재설정 링크 보내기 · 보냈다는 안내와 재발송 · 등록되지 않은 이메일 안내 · 새 비밀번호 입력과 조건 표시 · 재설정 완료 후 로그인 이동
- **버튼 → 이동화면**:
  - `로그인` → `AU0101` 로그인
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `찾는 방법 고르기(문자/이메일)` → 아래 입력 칸이 그 방법에 맞게 바뀐다
  - `형식에 안 맞는 값 입력` → 칸 아래에 무엇이 틀렸는지 뜬다
- **생성 프롬프트**:

  ```
  비밀번호 찾기 화면을 만들어줘. 헤더 없이 단독 화면으로. "가입하신 이메일을 알려주세요" 문구와 이메일 입력칸, "재설정 링크 보내기" 버튼. 보낸 뒤 상태(메일 아이콘 + "메일함을 확인해 주세요" + "다시 보내기" 링크)와 등록되지 않은 이메일일 때 안내도 같이 만들어줘. 링크를 눌러 들어온 새 비밀번호 입력 화면도 — 새 비밀번호, 확인 입력칸과 조건 체크 표시, "비밀번호 바꾸기" 버튼.
  ```

## 5. 화면 이동 흐름 (FLOW)
- `홈 - 비로그인`(HO0101) —[강의 둘러보기]→ `강의 목록`(CO0101)
- `홈 - 비로그인`(HO0101) —[로그인]→ `내 정보`(MY0101)
- `홈 - 수강생 로그인`(HO0201) —[이어보기]→ `강의 재생`(CL0301)
- `홈 - 수강생 로그인`(HO0201) —[내 강의실]→ `내 강의실 - 수강 중`(CL0101)
- `강사센터 홈`(TC0101) —[새 강의 개설]→ `강의 개설 - 기본 정보`(CU0201)
- `강사센터 홈`(TC0101) —[학생 관리]→ `수강생 목록`(ST0101)
- `강사센터 홈`(TC0101) —[채점하기]→ `과제 채점 목록`(GR0201)
- `강사 지원`(TC0201) —[지원 현황]→ `강사 지원 - 심사 중`(TC0301)
- `강사 지원 - 심사 중`(TC0301) —[강의 탐색]→ `강의 목록`(CO0101)
- `강의 목록`(CO0101) —[강의 상세]→ `강의 상세`(CO0301)
- `강의 목록`(CO0101) —[검색]→ `강의 목록 - 검색 결과 없음`(CO0201)
- `강의 목록 - 검색 결과 없음`(CO0201) —[필터 초기화]→ `강의 목록`(CO0101)
- `강의 상세`(CO0301) —[수강 신청]→ `수강 신청 - 결제`(CO0401)
- `수강 신청 - 결제`(CO0401) —[결제하기]→ `수강 신청 - 완료`(CO0501)
- `수강 신청 - 결제`(CO0401) —[결제 실패]→ `수강 신청 - 결제 실패`(CO0601)
- `수강 신청 - 완료`(CO0501) —[바로 학습 시작]→ `강의 재생`(CL0301)
- `수강 신청 - 완료`(CO0501) —[내 강의실]→ `내 강의실 - 수강 중`(CL0101)
- `수강 신청 - 결제 실패`(CO0601) —[다시 시도]→ `수강 신청 - 결제`(CO0401)
- `내 강의실 - 수강 중`(CL0101) —[이어보기]→ `강의 재생`(CL0301)
- `내 강의실 - 수강 중`(CL0101) —[수료증 발급]→ `수료증 발급`(CL0801)
- `내 강의실 - 비어 있음`(CL0201) —[강의 둘러보기]→ `강의 목록`(CO0101)
- `강의 재생`(CL0301) —[과제 제출]→ `과제 제출`(CL0501)
- `강의 재생`(CL0301) —[질문하기]→ `질문 게시판`(CL0701)
- `강의 재생`(CL0301) —[재생 오류]→ `강의 재생 - 오류`(CL0401)
- `강의 재생 - 오류`(CL0401) —[다시 시도]→ `강의 재생`(CL0301)
- `과제 제출`(CL0501) —[제출하기]→ `강의 재생`(CL0301)
- `과제 제출`(CL0501) —[마감된 과제]→ `과제 제출 - 마감됨`(CL0601)
- `과제 제출 - 마감됨`(CL0601) —[질문하기]→ `질문 게시판`(CL0701)
- `질문 게시판`(CL0701) —[강의로 돌아가기]→ `강의 재생`(CL0301)
- `수료증 발급`(CL0801) —[내 강의실]→ `내 강의실 - 수강 중`(CL0101)
- `내 강의 목록`(CU0101) —[새 강의 개설]→ `강의 개설 - 기본 정보`(CU0201)
- `내 강의 목록`(CU0101) —[커리큘럼 편성]→ `커리큘럼 편성`(CU0301)
- `강의 개설 - 기본 정보`(CU0201) —[다음 단계]→ `커리큘럼 편성`(CU0301)
- `커리큘럼 편성`(CU0301) —[영상 업로드]→ `영상 업로드`(CU0401)
- `커리큘럼 편성`(CU0301) —[검수 요청]→ `강의 게시 - 검수 대기`(CU0601)
- `영상 업로드`(CU0401) —[업로드 완료]→ `커리큘럼 편성`(CU0301)
- `영상 업로드`(CU0401) —[업로드 실패]→ `영상 업로드 - 실패`(CU0501)
- `영상 업로드 - 실패`(CU0501) —[다시 시도]→ `영상 업로드`(CU0401)
- `강의 게시 - 검수 대기`(CU0601) —[내 강의 목록]→ `내 강의 목록`(CU0101)
- `수강생 목록`(ST0101) —[수강생 상세]→ `수강생 상세 - 진도 현황`(ST0301)
- `수강생 목록`(ST0101) —[검색]→ `수강생 목록 - 검색 결과 없음`(ST0201)
- `수강생 목록 - 검색 결과 없음`(ST0201) —[필터 초기화]→ `수강생 목록`(ST0101)
- `수강생 상세 - 진도 현황`(ST0301) —[메시지 발송]→ `알림 발송`(ST0501)
- `수강생 상세 - 진도 현황`(ST0301) —[과제 채점]→ `과제 채점 - 상세`(GR0301)
- `진도 미달자 관리`(ST0401) —[메시지 발송]→ `알림 발송`(ST0501)
- `알림 발송`(ST0501) —[수강생 목록]→ `수강생 목록`(ST0101)
- `출석 현황`(GR0101) —[과제 채점]→ `과제 채점 목록`(GR0201)
- `과제 채점 목록`(GR0201) —[채점하기]→ `과제 채점 - 상세`(GR0301)
- `과제 채점 - 상세`(GR0301) —[채점 완료]→ `과제 채점 목록`(GR0201)
- `과제 채점 - 상세`(GR0301) —[수료 기준]→ `수료 기준 설정`(GR0401)
- `수료 기준 설정`(GR0401) —[수료 처리]→ `수료 처리 - 확인`(GR0501)
- `수료 처리 - 확인`(GR0501) —[출석 현황]→ `출석 현황`(GR0101)
- `내 정보`(MY0101) —[수강 이력]→ `수강 이력`(MY0201)
- `수강 이력`(MY0201) —[수료증 발급]→ `수료증 발급`(CL0801)
- `수강 이력`(MY0201) —[정산 내역]→ `정산 내역`(MY0301)
- `정산 내역`(MY0301) —[내역 없음]→ `정산 내역 - 내역 없음`(MY0401)
- `정산 내역 - 내역 없음`(MY0401) —[강의 개설]→ `강의 개설 - 기본 정보`(CU0201)
- `로그인`(AU0101) —[회원가입]→ `회원가입`(AU0201)
- `로그인`(AU0101) —[비밀번호 찾기]→ `비밀번호 찾기`(AU0501)
- `회원가입`(AU0201) —[다음]→ `회원가입 - 휴대폰 인증`(AU0301)
- `회원가입`(AU0201) —[로그인]→ `로그인`(AU0101)
- `회원가입 - 휴대폰 인증`(AU0301) —[다음]→ `회원가입 완료`(AU0401)
- `회원가입 완료`(AU0401) —[강의 탐색]→ `강의 목록`(CO0101)
- `회원가입 완료`(AU0401) —[강사 지원]→ `강사 지원`(TC0201)
- `비밀번호 찾기`(AU0501) —[로그인]→ `로그인`(AU0101)

## 6. 빌드 가이드
### 구조
- 각 **화면ID**를 하나의 라우트/페이지로 만드세요 (예: `PCHOME0110` → `/home`).
- 화면명은 페이지 제목·컴포넌트 이름의 기준으로 사용하세요.
- **2장 공통 요소**(헤더·GNB·푸터)를 공용 레이아웃으로 만들고 모든 화면이 감싸도록 하세요.
- **쓰는 사람이 다른 메뉴는 헤더를 나누세요.** 손님이 쓰는 메뉴(홈·강의 탐색·내 강의실·마이페이지)와 운영자가 쓰는 메뉴(강사센터·수업 편성·학생 관리·출결·평가)를 **한 헤더에 나란히 놓지 마세요.** 둘은 사실상 다른 사이트입니다.
  · 손님 영역과 운영자 영역을 각각 다른 레이아웃(헤더·사이드바)으로 만들고, 오갈 때는 **따로 마련한 진입점** 하나로만 이동하세요(예: 우측 상단 `강사센터 화면으로`, 운영자 화면에서는 `고객 화면 보기`).
  · 운영자 영역은 보통 좌측 사이드바 + 상단 바가 맞습니다. 손님 영역의 검색·카테고리 내비를 그대로 쓰지 마세요.
- **버튼 → 이동화면** 관계를 링크/네비게이션으로 연결하세요.
- **4장 각 화면의 `눌렀을 때 (이 화면 안에서 끝남)` 항목을 그대로 만드세요.** 적혀 있는 것은 빠짐없이 실제로 동작해야 합니다 — 누르면 값이 바뀌고, 다시 누르면 되돌아옵니다.
  · 그 항목에 없더라도 **누를 수 있게 생긴 것은 전부 눌리게** 만드세요. 적힌 것은 최소한이지 전부가 아닙니다.
- **누를 수 있게 보이는 것은 전부 눌리게 만드세요.** 두 종류로 나눠 처리합니다.
  · **화면 안에서 끝나는 조작은 진짜로 동작해야 합니다** — 탭 전환, 좌측 카테고리·필터 선택, 목록에서 항목 고르기(디자이너·옵션·시간), 펼치기·접기, 캘린더 이전/다음 달, 수량 ±, 체크·토글. 자바스크립트로 실제 상태를 바꾸고 화면을 다시 그리세요. 이걸 안내 문구로 때우지 마세요.
  · **서버가 있어야 하는 것만** 짧은 안내(토스트)로 대신합니다 — 저장·삭제·발송·결제·전화 걸기·이의 신청 등.
  · **이런 것은 토스트로 때우지 마세요** — `모두 보기`(펼치기), `선택`·`적용`(고른 상태로 바꾸기), `추가`(줄 하나 늘리기), `닫기`·`✕`(정말 닫기), `도움이 됐어요`(숫자 올리기), 탭·필터·정렬. 전부 화면 안에서 끝나는 일이라 실제로 되어야 합니다.
  · **모달·팝업은 뜨는 것만으로 끝이 아닙니다.** `취소`와 `✕`는 정말 닫히고, `확인`은 눌렀을 때 무엇이 달라지는지 보여야 합니다(다음 화면으로 가거나, 목록이 바뀌거나, 안내가 뜨거나). 닫히지 않는 팝업은 갇힌 화면이 됩니다.
  · 아무 것도 하지 않는 `<button>`을 남기지 마세요. 만들고 나서 **버튼을 하나씩 눌러 보고**, 반응이 없는 것이 있으면 위 둘 중 하나로 채우세요.
- **뒤로가기는 화면마다 정해져 있습니다. 4장 각 화면 스펙의 `뒤로가기` 항목을 그대로 만드세요.** 121개 화면에 붙고, 나머지 11개(메뉴 첫 화면)에는 없습니다. 좌측 상단에 `‹ 상위 화면 이름`과 현재 위치(예: `매장 탐색 › 매장 상세`)를 둡니다.
  · 여러 단계짜리 흐름(1→2→3)은 **이전 단계로 돌아갈 수 있게** 하세요. 되돌아갔을 때 앞서 고른 값이 남아 있어야 합니다.
  · **한 묶음으로 나란히 보여주는 것은 동일 계위입니다.** 탭 줄에 걸린 화면들, 같은 사이드바에 나란히 올라온 화면들이 그렇습니다. 그 안을 오가는 길이 이미 탭·사이드바이므로 뒤로가기를 겹쳐 두지 마세요 — 같은 줄에 있는 화면인데 어떤 건 있고 어떤 건 없어집니다.
  · 빈 화면·오류·마감처럼 막다른 화면에는 **다음에 할 일 버튼**을 반드시 하나 두세요(예: `목록으로`, `다시 시도`, `둘러보기`).
  · **전체 화면 목록으로 돌아가는 길**을 모든 화면에 두되, 이름에 목적이 드러나게 하세요(`화면 목록`). 접힌 패널 안에 `화면 정보` 같은 이름으로 넣으면 아무도 못 찾습니다.
  · 다 만든 뒤 **화면을 하나씩 열어 뒤로가기가 있는지 세어 보세요.** 하나라도 빠져 있으면 채웁니다.
### 화면 구현
- 각 화면의 **생성 프롬프트**를 그 화면 구현 지시로 그대로 사용하세요.
- 반복 UI(카드·리스트 행·폼·버튼)는 **공용 컴포넌트**로 만들어 재사용하세요.
- 필요하면 상태 화면(로딩·빈 결과·에러)도 함께 만드세요.
### 디자인
- **디자인 프리셋 파일을 함께 넣었다면 그것이 우선입니다.** 1장의 디자인 컨셉이나 화면별 프롬프트에 다른 색·톤이 적혀 있어도 프리셋을 따르세요. 충돌이 아니라 **프리셋이 정답**입니다.
- 프리셋의 **레이아웃 골격**(히어로·목록·내비·상세를 어떻게 놓을지)을 먼저 잡고 색을 입히세요. 색만 맞추면 어떤 테마로 만들어도 같은 화면이 나옵니다.
- 프리셋이 없다면 1장의 **디자인 컨셉**을 전역 스타일(색·톤·라운드·여백)로 토큰화해 일관되게 적용하세요.
- **글꼴은 프리셋이 없어도 한 벌로 고정하세요.** 기본은 `Paperlogy`이고, 못 불러오면 `Pretendard` → `Noto Sans KR` 순으로 물러납니다. CSS로는 `font-family: Paperlogy, Pretendard, 'Noto Sans KR', sans-serif` 한 줄이면 됩니다.
  · **글꼴은 한 벌만 씁니다.** 제목과 본문에 서로 다른 글꼴을 쓰지 마세요 — 두 벌이 되는 순간 어디에 무엇을 쓸지가 화면마다 갈립니다. 위계는 글꼴이 아니라 **크기와 굵기**로 만듭니다.
  · 굵기는 **400·600·700 셋만**, 크기는 본문 15 · 카드 제목 18 · 섹션 제목 22 · 페이지 제목 32을 기준으로 잡으세요.

#### 읽기 폭 — 글을 한 단으로 좁힐 때

글이 주인공인 자리(상세 본문·안내·약관)는 **최대 760px** 로 좁히고 가운데 정렬합니다.
콘텐츠 영역(위의 최대 폭) **안에서** 좁히는 것입니다 — 바깥 폭을 바꾸는 게 아닙니다.

**왜 좁히나** — 한 줄이 길면 다음 줄 첫 글자를 눈이 못 찾습니다. 1440px 폭에 본문을 꽉 채우면
한 줄에 90자가 넘어가고, 읽는 사람은 두세 줄마다 같은 줄을 다시 읽게 됩니다.

- **이 값 하나만 씁니다.** 화면마다 680·720·760 처럼 다르게 잡지 마세요 — 저희도 그렇게 적혀 있었고,
  같은 팩 안의 상세 화면끼리 본문 폭이 달라 서로 다른 사이트처럼 보였습니다.
- **카드 격자에는 쓰지 않습니다.** 여기는 «글»을 좁히는 값입니다. 카드를 몇 열로 놓을지는 레이아웃이 정합니다.
- 사진·표·코드처럼 넓어야 읽히는 것은 이 폭을 넘겨도 됩니다. 좁히는 건 글줄입니다.

#### 격자 — 카드를 늘어놓는 자리

카드·타일을 격자로 늘어놓을 때 **좌우와 위아래를 다른 값으로** 벌립니다.

| 자리 | 값 |
| --- | --- |
| **카드와 카드 사이** (좌우) | 위 눈금의 「카드와 카드 사이 (가로)」 |
| **줄과 줄 사이** (위아래) | 위 눈금의 「덩어리와 덩어리 사이」 |

**같은 값을 쓰면 줄이 안 갈립니다.** 카드에 상자가 있을 때는 테두리가 경계를 만들어 줬지만,
상자를 걷어낸 뒤로는 **위 카드의 가격과 아래 카드의 사진이 붙어** 한 덩어리로 읽힙니다.
위아래를 더 벌려야 "여기까지가 한 줄"이 보입니다.

| 나누는 것 | 정하는 곳 |
| --- | --- |
| 몇 열로 나눌지 | 레이아웃 프리셋 |
| **사이를 얼마나 벌릴지** | **위 간격 눈금 (좌우·위아래 따로)** |
| 카드가 어떻게 생겼는지 | 카드 모양 |

- **레이아웃 프리셋이 「넓게」라고 해도 값을 새로 만들지 마세요.** 레이아웃은 *몇 열로 무엇을 어디에*를
  말하는 층이고, *얼마나 벌릴지*는 이 눈금이 정합니다. 실제로 그 한 줄 때문에 40px을 쓴 격자가 있었고,
  같은 화면의 다른 격자는 16px이라 두 값이 나란히 놓였습니다.

**간격은 변수로 못 박고, 자리마다 그것만 씁니다.** CSS 맨 위에 이걸 그대로 붙여 넣으세요.

```css
:root{
  --wrap: 1440px;      /* 콘텐츠 폭 — 헤더·본문·푸터·고정바가 모두 이 값을 쓴다 */
  --pad-x: 24px;    /* 좌우 여백 */
  --card-gap:   16px;     /* 카드와 카드 사이 — 좌우 */
  --card-gap-y: 28px;     /* 카드 줄과 줄 사이 — 위아래 */
  --side-w:     260px;               /* 사이드바 폭 (사이드바 뼈대에서만) */
  --side-gap:   32px;                /* 사이드바와 본문 사이 */
  --row-gap:    14px;                /* 가로 행 카드 안: 썸네일과 글자 사이 */

  --card-pad:   24px;                /* 카드 안쪽 여백 */
  --row-h:      48px;                /* 표 한 줄 높이 — 안 정하면 줄마다 들쭉날쭉해진다 */
  --btn-gap:    8px;                 /* 나란히 놓은 버튼 사이 */
}
@media (max-width: 720px){
  :root{ --pad-x: 16px; --card-gap: 12px; }
}
.wrap { max-width: var(--wrap); margin-inline: auto; padding-inline: var(--pad-x); }
.cards{ display: grid; column-gap: var(--card-gap); row-gap: var(--card-gap-y); }

/* 지표 줄 — 화면 맨 위에 숫자 카드를 나란히 놓는 자리.
   카드 격자(.cards)와 다른 물건이므로 .cards 를 쓰지 않는다. 지표는 넷이 기본이다. */
.stats{ display: grid; grid-template-columns: repeat(4, 1fr);
        column-gap: var(--card-gap); row-gap: var(--card-gap-y); }
@media (max-width: 1024px){ .stats{ grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 720px){ .stats{ grid-template-columns: 1fr; } }

/* 모자이크 — 홈 첫 화면에 「큰 1장 + 작은 4장」을 놓는 자리.
   첫 장이 2칸×2줄, 나머지 넷이 옆을 채운다.
   어느 것을 크게 할지 class 로 정하지 않는다 — 맨 앞에 둔 것이 큰 것이다. */
.mosaic{ display: grid; grid-template-columns: repeat(4, 1fr);
         column-gap: var(--card-gap); row-gap: var(--card-gap-y); }
.mosaic > :first-child{ grid-column: span 2; grid-row: span 2;
                        display: flex; flex-direction: column; }
/* 큰 칸의 사진은 카드 비율을 따르지 않는다 — 두 줄 높이와 안 맞아 제목이 밀려난다.
   큰 칸에서는 사진이 남는 자리를 채운다. */
.mosaic > :first-child > .thumb{ aspect-ratio: auto; flex: 1; min-height: 0; }
@media (max-width: 1024px){
  .mosaic{ grid-template-columns: repeat(2, 1fr); }
  .mosaic > :first-child{ grid-row: auto; }   /* 2열에서 세로로 겹치면 빈칸이 생긴다 */
}
@media (max-width: 720px){
  .mosaic{ grid-template-columns: 1fr; }
  .mosaic > :first-child{ grid-column: auto; }
}

/* 상세 화면의 대표 사진 — 카드 비율을 그대로 쓰지 않는다.
   4:3 을 본문 폭 1392px 에 적용하면 세로가 1044px 이 되어 노트북 화면을 통째로 덮는다.
   가로로 눕히고(16:9) 세로를 420px 로 막는다 — 사진 아래 내용이 같이 보이는 선이다. */
.detail-img{ aspect-ratio: 16 / 9; max-height: 420px; width: 100%;
             border-radius: 12px; overflow: hidden; }
.detail-img > img{ width: 100%; height: 100%; object-fit: cover; display: block; }

/* 간격은 반드시 이 변수로 쓴다. gap:16px 처럼 숫자를 직접 적으면 자리마다 값이 갈린다. */
```

- **`--card-pad`·`--row-h`·`--btn-gap` 도 꼭 쓰세요.** 카드 안쪽 여백, 표 한 줄 높이,
  버튼 사이 간격입니다. 이 셋을 안 정해주면 화면마다 다르게 나옵니다 — 저희도 겪었습니다.
- **`.stats`·`.mosaic`·`.detail-img` 는 카드 격자와 다른 자리입니다.** 지표 숫자 줄,
  홈의 큰 사진 한 장 + 작은 넷, 상세 화면의 대표 사진. 카드 규칙을 여기 갖다 쓰면
  사진이 화면을 다 덮습니다.

- **격자에 `gap: 20px` 처럼 숫자를 직접 쓰지 마세요.** 한 번 쓰는 순간 그 자리만 달라지고,
  나중에 값을 바꿀 때 그 자리만 안 바뀝니다. 실제로 한 사이트에서 좌우가 3·8·16·20·24px
  다섯 가지로 갈라진 적이 있습니다 — 전부 `gap:` 에 숫자를 적어서 생긴 일입니다.
- **`gap` 한 줄로 합치지도 마세요.** 좌우와 위아래는 값이 다르므로 `column-gap`·`row-gap` 둘로 씁니다.
- **이름도 이대로 씁니다.** `--grid-x`·`--gap-col` 처럼 제 나름의 이름을 지으면, 나중에
  다른 사람이 볼 때 그게 같은 자리인지 알 수가 없습니다. 실제로 같은 규칙을 준 두 사이트가
  하나는 `--grid-x/--grid-y`, 하나는 `--gap-col/--gap-row` 를 써서 값이 따로 놀았습니다.
- 값을 바꾸고 싶으면 **`:root` 의 두 줄만** 고칩니다. 그러면 화면 전체가 한 번에 따라옵니다.
- 히어로의 좌우 2단처럼 **카드 격자가 아닌 자리**는 이 규칙 밖입니다. 거기는 화면 뼈대라 레이아웃이 정합니다.

#### 박스형 콘텐츠 — 몇 개를 늘어놓고 사진은 어떤 비율로

사진이 든 카드를 격자로 늘어놓을 때, **몇 개를 놓느냐가 사진 비율을 정합니다.**

| 한 줄에 | 사진 비율 | 왜 |
| --- | --- | --- |
| **2개** | **4:3** | 칸이 넓으니 가로로 넓은 사진 |
| **3개** | **4:3** | 위와 같음 |
| **4개** | **1:1** | 칸이 좁다. 정사각이 가장 얌전하다 |

4개짜리를 세로로 긴 3:4로 뒀더니 **카드 한 줄이 화면을 다 먹었습니다.** 칸이 좁을수록 세로로
길게 두고 싶어지지만, 그러면 한 줄만 보이고 다음 단락이 안 보입니다. 좁은 칸에는 정사각입니다.

- **한 줄에 하나만 두지 않습니다.** 마지막 줄에도 하나만 남기지 마세요 —
  **항목 수를 열 수에 맞춰** 3열이면 3·6·9개, 4열이면 4·8개로 둡니다.
- **한 사이트에서 카드 사진 비율은 하나입니다.** 위 표의 짝(2·3열↔4:3, 4열↔1:1) 밖의 비율은 만들지 마세요.
  같은 홈에 세로 카드와 16:9가 섞여 있어서 **사는 사람이 사진을 두 벌 준비해야 했던** 적이 있습니다.
- **배너·차트·영상·프로필은 이 규칙 밖입니다.** 가로로 긴 배너·영상(16:9)·프로필(1:1)은 제 나름의 비율이 있습니다.
- 상품 사진처럼 **정사각이 업계 표준인 분야**는 1:1을 써도 됩니다. 다만 그때도 한 사이트 안에서는 하나로 갑니다.
- **가로 배너 한 장으로 한 단락을 채우지 마세요.** 배너는 위 표 밖이지만 "한 줄에 하나만 두지 않는다"는 그대로 걸립니다.
  배너 한 장이 있던 자리는 **두 장으로 나누고** 4:3으로 맞추세요.
- **가로로 넘기는 줄(캐러셀)도 몇 개 보이느냐로 셉니다.** 뒤에 더 있어도 눈에 4개가 보이면 4개 — 비율도 1:1입니다.
  카드 폭을 `260px`처럼 박아 두면 화면 폭이 바뀔 때 3.4개씩 어정쩡하게 걸치니, **칸 수로 나눠서** 정하세요.
- **가로로 넘기는 줄에는 좌우 화살표를 두고, 아래 스크롤바는 감춥니다.** 넘길 게 있다는 건
  화살표가 알려 줍니다. 스크롤바까지 나오면 줄 밑에 회색 막대가 하나 더 생겨 지저분합니다.

```css
.carousel{ scrollbar-width:none; -ms-overflow-style:none; }
.carousel::-webkit-scrollbar{ display:none; }
```

  화살표가 **없는데** 스크롤바까지 감추면 넘길 게 있다는 걸 아무도 모릅니다. 둘은 한 벌입니다.

#### 분리형과 혼합형 — 글자를 사진 아래 두느냐 위에 얹느냐

박스형 콘텐츠는 두 가지입니다. **어느 쪽이든 위의 개수·비율 규칙은 똑같이 적용됩니다.**

| | **분리형** | **혼합형** |
| --- | --- | --- |
| 생김새 | 사진 아래에 글자 | 사진 위에 글자를 얹음 |
| 담을 수 있는 것 | 제목·설명·평점·가격까지 여러 줄 | 제목 + 한 줄 정도 |
| 사진이 하는 일 | 무엇인지 알려 준다 | 분위기를 만든다 |
| 쓰는 자리 | 상품·강의 목록처럼 **고르는** 자리 | 특집·기획전처럼 **끌어당기는** 자리 |

**분리형은 상자를 두르지 마세요.** 흰 배경에 테두리·그림자까지 두르면, 사진과 글자가 이미 한 덩어리인데
선을 한 번 더 치는 셈입니다. 격자로 늘어놓으면 그 선이 줄줄이 겹쳐 화면이 답답해집니다.
**모서리는 사진이 가집니다** — 상자를 없앤 자리에서 사진이 카드 노릇을 합니다.
(혼합형은 사진이 곧 배경이라 이 규칙 밖입니다.)

```css
/* 사진 + 글자로 된 카드 — 상자를 두르지 않는다 */
.card{
  background: none;   /* surface 나 흰색을 깔지 않는다 */
  border: 0;
  box-shadow: none;
}
.card .thumb{ border-radius: 12px; }  /* 모서리는 사진이 가진다 */
.card .body{ padding: 16px 0 0; }     /* 좌우 여백 0 — 상자가 없으니 안쪽 여백도 없다 */
```

**`background: var(--surface)` 나 `border: 1px solid` 를 카드에 쓰면 이 규칙을 어긴 것입니다.**
사진이 없는 상자(설정 패널·안내 상자·표를 감싼 것)는 이 규칙 밖입니다 — 거기는 배경이 있어야
덩어리로 읽힙니다. **사진과 글자로 된 카드**만 걷어냅니다.

상자를 걷어내면 두 가지가 따라옵니다.

- **글자가 흰색이 아니라 페이지 배경 위**에 놓입니다. 대비를 다시 재세요.
- **카드끼리 갈라 주던 테두리가 없어집니다.** 줄과 줄 사이를 더 벌려 그 몫을 대신하세요.

그리고 **카드 안에는 선을 긋지 마세요.** 가격 위에 선을 두면 상자가 없는 상태에서는 그 선이 카드를
가르는 것처럼 보여, 사진·제목과 가격이 따로 노는 두 덩어리로 읽힙니다.
**갈라야 할 것은 카드 안이 아니라 카드끼리**입니다.

혼합형을 쓸 때 반드시 지킬 것:

- **어둠막을 까세요.** 사진 위에 흰 글자를 그냥 얹으면 사진의 밝은 자리에서 글자가 사라집니다.
  **사진은 사는 분이 바꿀 것**이라 우리가 밝기를 미리 알 수 없습니다.
  글자 쪽에서 반대편으로 옅어지는 어둠막을 항상 깔아 두면 어떤 사진이 와도 읽힙니다.
- **글자 대비 4.5 이상**을 지키세요. 가장 밝은 사진(흰색)이 왔다고 치고 재세요.
- **글자를 짧게 쓰세요.** 제목 한 줄 + 설명 한 줄까지입니다. 그보다 길면 사진을 다 덮어 혼합형인 의미가 없습니다.
- **가격·평점처럼 정확히 읽어야 하는 것은 얹지 마세요.** 사진 위에서는 숫자가 잘 안 읽힙니다.
- **한 화면에 두 가지를 섞어도 됩니다.** 위는 혼합형 특집, 아래는 분리형 목록 — 하는 일이 다르므로 어수선하지 않습니다.

#### 글꼴은 한 벌입니다

**기본은 `Paperlogy`**, 못 불러오면 `Pretendard` → `Noto Sans KR` 순으로 물러납니다.
CSS로는 `font-family: Paperlogy, Pretendard, 'Noto Sans KR', sans-serif` 한 줄입니다.

- **제목과 본문에 다른 글꼴을 쓰지 마세요.** 두 벌이 되는 순간 어디에 무엇을 쓸지가
  화면마다 갈립니다. 처음엔 「제목만 다르게」로 시작해서, 나중엔 세 벌 네 벌이 됩니다.
- **위계는 글꼴이 아니라 크기와 굵기로 만듭니다.** 굵기는 400·600·700 셋만 쓰세요.
- 크기는 본문 15 · 카드 제목 18 · 섹션 제목 22 · 페이지 제목 32이 기준입니다.
- **숫자가 많은 표에는 고정폭 숫자**(`font-variant-numeric: tabular-nums`)를 켜세요.
  안 켜면 값이 바뀔 때 자릿수가 흔들려 칸이 들썩입니다.

#### 움직임 — 바뀌었다는 걸 눈이 알아채게

값이 바뀌었는데 **딱 하고 갈아 끼우면 사람은 그걸 못 봅니다.** 숫자가 12에서 3으로
바뀌어도, 눈이 그 자리를 안 보고 있었으면 바뀐 줄 모릅니다.
움직임은 꾸미개가 아니라 **「방금 여기가 바뀌었어요」라고 알려 주는 장치**입니다.

| 언제 | 얼마나 | 무엇이 |
| --- | --- | --- |
| 마우스를 올렸을 때 | **0.12초** | 색·테두리 |
| 눌러서 값이 바뀔 때 | **0.2초** | 사라짐·나타남·자리 옮김 |
| 접히고 펴질 때 | **0.25초** | 높이 |
| 화면 위에 뜰 때 (모달·시트) | **0.25초** | 옅어짐 + 살짝 올라옴 |
| 안내 문구(토스트) | **0.2초 등장 · 3초 머무름** | 옅어짐 |

- **가속은 하나로 씁니다.** `ease-out`(빨리 시작해 부드럽게 멈춤)이 기본입니다.
  자리마다 다른 가속을 쓰면 같은 사이트에서 두 사람이 만든 것처럼 보입니다.
- **0.3초를 넘기지 마세요.** 그보다 길면 기다리는 느낌이 듭니다. 목록이 다시 그려질 때는
  **0.15초**면 충분합니다.
- **자리를 밀어내며 움직이지 마세요.** 아래 내용이 출렁이면 읽던 곳을 잃습니다.
  옅어지거나 제자리에서 바뀌게 하세요.
- **한 번에 한 곳만 움직입니다.** 눌렀는데 화면 여기저기가 같이 움직이면 어디가 바뀐 건지 모릅니다.
- **끝없이 도는 움직임은 두지 않습니다.** 배너 자동 넘김·깜빡임은 읽는 걸 방해합니다.
  기다리는 동안 도는 것(로딩)만 예외입니다.
- **움직임을 꺼 둔 분을 존중하세요.** `prefers-reduced-motion` 이 켜져 있으면 전부 0초로 둡니다.
  어지럼증 때문에 꺼 두신 분들이 있습니다. 값은 바뀌되 움직이지만 않으면 됩니다.

#### 떠 있는 부품과 줄 안에 서는 부품은 다른 것입니다

사진 위에 **떠 있는** 동그란 찜 아이콘과, 버튼 줄 안에 **나란히 서는** 찜 버튼은
생김새가 비슷해 보여도 다른 부품입니다. 떠 있는 쪽은 자리를 빠져나가도록(`position:absolute`)
만들어져 있어서, 그 이름을 줄 안의 버튼에 붙이면 **줄을 빠져나가 엉뚱한 데 가서 앉습니다.**
실제로 찜 버튼이 가격 위에 겹쳐 앉은 적이 있습니다.

- **이름이 자리를 뜻하면 그 자리에서만 쓰세요.** 「사진 위 찜」과 「줄 안 아이콘 버튼」은
  이름을 나눠 두고, 섞어 쓰지 않습니다.
- **부품 이름과 실제 쓰임이 어긋나면 이름을 고치세요.** 「타임라인」이라 지어 놓고
  여섯 곳 모두 한 줄짜리 목록으로 쓰고 있던 적이 있습니다. 세로 점만 남아 글자 사이에 끼었습니다.
- **아이콘만으로 뜻이 통하면 글자를 빼세요.** 하트·공유·닫기가 그렇습니다.
  다만 **`aria-label`로 이름은 남깁니다** — 눈으로 못 보는 분에게는 그게 유일한 이름입니다.
- 반대로 **아이콘만으로 못 알아보는 것에는 글자를 붙이세요.** 「빼기」·「내보내기」처럼
  모양이 여러 뜻으로 읽히는 것이 그렇습니다.

#### 쓰는 화면이 있으면 보여주는 자리도 있어야 합니다

글을 쓰게 해 놓고 그 글이 나올 자리를 안 만드는 일이 잦습니다.
실제로 강사가 **1,240자짜리 강의 소개를 쓰는 화면**은 있는데, 상세 화면에는 그 글이
나올 자리가 없었습니다. 게다가 검수 화면은 "강의 소개 1,240자 — 확인됨"이라고까지 말하고 있었습니다.

- **입력 칸을 만들 때마다 "이건 어디에 보이나"를 한 번 물으세요.** 답이 없으면 둘 중 하나입니다 —
  보여줄 자리를 만들거나, 그 입력 칸을 빼거나.
- **에디터(굵게·목록·이미지 버튼이 붙은 칸)로 쓴 글**은 보여주는 쪽에도 그 모양이 살아야 합니다.
  제목·목록·굵게를 쓸 수 있게 해 놓고 보여줄 때 한 덩어리 문단으로 뭉개면, 쓴 사람이 헛수고합니다.
- **한 줄 요약과 본문 소개는 다른 것입니다.** 목록 카드에 들어가는 한 줄과, 상세에서 읽는 본문은
  자리도 길이도 다릅니다. 하나로 합치지 마세요.

#### 권장 크기는 실제 자리와 같아야 합니다

"권장 1280×720 (16:9)"이라고 적어 두고 옆의 자리는 4:3인 화면이 있었습니다.
**사는 분은 글자를 믿고 그 크기로 사진을 준비**하는데, 올려 보면 잘립니다.

- 안내 문구의 숫자와 **바로 옆 자리의 비율이 같은지** 확인하세요.
- 검수·확인 화면에 크기가 또 적힌다면 **거기도 같은 숫자**여야 합니다. 세 곳이 따로 놀기 쉽습니다.
- **영상만 16:9입니다.** 썸네일·카드 사진은 위 표(2·3개↔4:3, 4개↔1:1)를 따릅니다.

#### 차트 — 이미지가 아니라 데이터로 그립니다

추이·막대·도넛 같은 차트를 **이미지 자리로 두지 마세요.** `이미지 영역 (라인 차트 · 권장 1200×420)`
이라고 적어 두면 사는 분은 차트를 **그림 파일로 만들어 올려야 하는 줄** 압니다.
차트는 숫자가 바뀌면 따라 바뀌어야 하는 것이라, 사진 자리와 성격이 다릅니다.

- **가벼운 것은 라이브러리 없이 SVG로 그리세요.** 선 하나·막대 열두 개짜리는 30줄이면 됩니다.
  바깥에서 아무것도 안 불러오니 **인터넷 없이 폴더에서 열어도** 그대로 보입니다.

**대시보드를 만든다면 최소한 이 셋은 갖추세요.**

| 무엇 | 어디에 | 왜 |
| --- | --- | --- |
| **큰 추이 차트** | 화면에 한둘 | 기간을 놓고 흐름을 본다 |
| **스파크라인** (작은 추이선) | 지표 카드 안 | 숫자 옆에서 방향만 알려 준다 |
| **증감 표시** (▴ 812) | 지표 카드 안 | 좋아졌는지 나빠졌는지 |

숫자 하나만 두면 **좋은지 나쁜지를 못 말합니다.** "10,374명"만 있으면 늘고 있는지
줄고 있는지 알 수 없어서, 사는 분은 그 카드를 보고 아무 판단도 못 합니다.
지표 카드는 **숫자 + 증감 + 추이선**이 한 벌입니다.

- **본격적인 차트가 필요하면 무료(MIT) 라이브러리를 쓰세요.** 값을 치르지 않아도 되고 상업적으로 써도 됩니다.

| 라이브러리 | 크기 | 언제 |
| --- | --- | --- |
| **Chart.js** | ~200KB | 무난한 첫 선택. 자료가 많아 AI도 정확하게 짭니다 |
| **ApexCharts** | ~500KB | 기본 모양이 예쁘고 종류가 많을 때 |
| **uPlot** | ~40KB | 점이 수천 개라 가벼워야 할 때 |
| **Frappe Charts** | ~30KB | 작고 산뜻한 SVG면 충분할 때 |

- **색은 반드시 프리셋 색을 쓰세요.** 라이브러리 기본 팔레트(빨강·노랑·초록)를 그대로 두면
  그 자리만 다른 사이트처럼 보입니다.
- **눈금선은 옅게, 선은 진하게.** 눈금이 데이터보다 진하면 숫자가 안 읽힙니다.
- 라이브러리를 쓰면 **인터넷이 필요해집니다.** 오프라인으로 열어 볼 화면이라면 파일을 함께 받아 두거나 SVG로 그리세요.

#### 가격 표기 — 낼 돈부터

**판매가 · 정가 · 할인율** 순서로 한 줄에 세웁니다.

| 자리 | 모양 | 크기 |
| --- | --- | --- |
| **판매가** (낼 돈) | 진한 기본 글자색, 굵게 | 18 (상세는 28) |
| **정가** | 회색 + 취소선 | 13 |
| **할인율** | **강조색 글자**, 굵게 | 14 |

- **할인율을 앞에 두지 마세요.** 눈이 먼저 "20%"에 걸려 정작 얼마인지를 뒤늦게 찾습니다.
  사는 분이 알고 싶은 건 **낼 돈**입니다.
- **할인율은 배지가 아니라 색 글자입니다.** 배지로 두면 정가·판매가와 무게가 비슷해져 셋이 서로 다툽니다.
  배지 자리는 `입문`·`NEW` 같은 **꼬리표 몫**입니다.
- **셋의 크기를 벌려 두세요.** 비슷하면 무엇을 먼저 읽어야 할지 눈이 헤맵니다.
- **강조색 원색을 글자로 쓰지 마세요.** 주황·코랄 원색은 흰 배경에서 3:1을 못 넘깁니다.
  글자용으로 한 단계 어두운 색을 따로 두세요.
- 가격 묶음은 **한 곳에서 만들어 쓰세요.** 자리마다 손으로 짜면 순서가 갈립니다.

#### 버튼이냐 링크냐

**다른 화면으로 데려가면 링크, 이 화면 안에서 끝나면 버튼입니다.**

| 하는 일 | 무엇으로 | 눌렀을 때 |
| --- | --- | --- |
| 다른 화면으로 간다 | 링크 `<a href>` | 그 화면이 열린다 |
| 이 화면 안에서 끝난다 | 버튼 `<button>` | 안내 문구가 뜬다 |

- 필터 적용·정렬·검색·다음 차시·시간 지점 이동은 **화면이 바뀌지 않는 조작**입니다.
  링크로 만들면 **자기 자신으로 가는 링크**가 되어, 눌러도 같은 화면이 다시 열립니다.
  사는 분 눈에는 *"눌렀는데 아무 일도 안 나네"* 로 보입니다.
- **나란히 선 버튼끼리는 종류를 섞지 마세요.** 하나는 화면이 넘어가고 하나는 안 넘어가면
  규칙이 없어 보입니다.

**그리고 화면 안에서 끝나는 조작은 진짜로 값이 바뀌어야 합니다.**
눌러도 숫자가 그대로면, 사는 분 눈에는 **눌리지 않는 포스터**입니다.

| 눌렀을 때 | 바뀌어야 하는 것 |
| --- | --- |
| 기간 탭 (7일·30일·90일) | 차트와 그 옆 요약 숫자 |
| 쿠폰·옵션 고르기 | 결제 금액 줄과 합계 |
| 수량 늘리기 | 금액 |
| 정렬·필터 | 목록 순서나 개수 |

- **미리 만들어 두고 바꿔 끼세요.** 차트 셋을 미리 그려 두고 탭에 따라 보여 주는 편이,
  누를 때마다 자바스크립트로 다시 그리는 것보다 낫습니다 — 그리는 규칙이 두 곳으로 갈라지지 않습니다.
- **계산은 한 곳에서만 하세요.** 합계를 화면마다 손으로 적어 두면 쿠폰을 바꿨을 때 한 군데만 바뀝니다.
- **탭에 갈 곳도 바꿀 것도 없으면 탭이 아닙니다.** 그냥 제목이거나, 아직 안 만든 자리입니다.

**같은 길을 두 번 두지 마세요 — 위 카테고리 줄과 왼쪽 LNB.**
둘이 같은 곳을 가리키면 위쪽이 두 줄로 두꺼워지고, 어느 쪽을 눌러야 할지 잠깐 멈칫합니다.

| 상태 | 왼쪽 LNB | 위 카테고리 줄 |
| --- | --- | --- |
| 맨 위 (LNB 보임) | 보인다 | **접힌다** |
| 스크롤해서 LNB가 위로 사라짐 | 안 보인다 | **내려온다** |
| LNB가 아예 없는 화면 | — | **늘 보인다** |

- **LNB가 있는 화면은 접힌 채로 시작하세요.** 펼쳐 두었다가 나중에 접으면 첫 화면에서 한 번 번쩍입니다.
- **LNB가 없는 화면에서는 절대 접지 마세요.** 접으면 갈 길이 통째로 사라집니다.
- **기준은 "LNB가 붙박이 머리말 아래로 사라지는 순간"입니다.** 머리말 높이만큼 위를 깎아서 재세요.
- 같은 규칙을 **LNB를 쓰는 모든 레이아웃에 똑같이** 거세요. 화면마다 다르면 위쪽 높이가 제멋대로 바뀌어 보입니다.

- **견본으로 가는 버튼을 화면 안에 두지 마세요.** `결과가 없을 때` `대화가 없을 때는 이렇게 보여요`
  처럼 예외 화면을 보여주려고 넣은 버튼·단락은 진짜 사이트에 없는 것입니다.
  검색창 옆에 그런 버튼이 서 있으면 사는 분은 그게 기능인 줄 압니다.
  빈 화면·오류 화면은 **화면 목록에 따로 한 장**으로 두세요 — 거기서 눌러 보면 됩니다.

#### 화면 목록으로 가는 길은 한 곳에만

만든 화면을 훑어보려면 목록이 필요합니다. 그런데 그 목록을 **화면 안에 늘어놓으면**
사는 분 눈에는 사이트의 일부로 보입니다.

```
이렇게 두지 마세요 — 화면 위쪽에 줄줄이
  [CO-01 · 강의 목록] [CO-02 · 검색 결과 없음] [CO-03 · 강의 상세] [CO-04 · 결제] …
```

**진짜 사이트에는 이런 줄이 없습니다.** `CO-02 · 검색 결과 없음` 은 만드는 사람의 말이지
쓰는 사람의 말이 아닙니다. 화면 ID 가 손님 눈에 보이는 것부터가 어긋납니다.

- **한 귀퉁이에 작게 하나만 둡니다.** 오른쪽 아래에 떠 있는 `화면 목록` 단추 하나면 됩니다.
  누르면 `index.html` 로 갑니다.
- **그 단추는 화면 내용 위에 떠 있게** 두고, 본문 흐름에는 끼워 넣지 않습니다.
  본문에 들어가면 그것도 사이트의 일부가 됩니다.
- **화면 ID 를 손님이 보는 자리에 쓰지 않습니다.** 제목·탭·빵부스러기 어디에도요.
  ID 는 우리가 화면을 세는 이름이지 서비스의 말이 아닙니다.
- 화면끼리 오가는 길은 **그 서비스의 진짜 메뉴와 버튼**으로 만듭니다.
  「강의 목록 → 강의 상세」는 강의 카드를 눌러서 가지, 탭으로 가지 않습니다.

### 이미지 · 썸네일
- 아직 사진이 없으므로 이미지 자리는 **테마 색이 아니라 옅은 파스텔**로 채우세요. 테마 색으로 칠하면 화면이 그 색 덩어리로 뒤덮여 디자인이 안 보입니다.
- 배경 `#EEF2F7` · `#F2EFF7` · `#EDF4F1` · `#F7F1EC` · `#F2F4F7` 중 하나를 카드마다 돌려 쓰고, 테두리 `#E3E8F0` 1px, 글자색 `#8B94A6` 13px.
- 자리 안에 `이미지 영역 (무엇 · 권장 가로×세로)` 형식으로 적으세요. 예: `이미지 영역 (매장 대표 · 권장 1200×900)` · `이미지 영역 (프로필 · 권장 400×400)` · `이미지 영역 (배너 · 권장 1600×600)`
- 이미지 자리는 테마 색으로 칠하지 않는다. 옅은 파스텔 한 톤 + 1px 테두리로 둔다.
- 자리 안에 무엇이 들어갈 이미지인지와 권장 크기를 글자로 적는다.
- 적어 둔 크기의 **비율을 실제로 지킨다** — `400×400`이라 적었으면 정사각으로 보여야 한다(aspect-ratio).
- 특히 프로필·썸네일처럼 크기가 고정된 자리는, 가로로 나란히 놓아도 늘어나지 않게 한다. 목록·행 안에 넣을 때 `flex`가 잡아당겨 타원이 되는 일이 잦으니, 만든 뒤 실제로 열어 모양을 확인한다.
- 썸네일처럼 작아서 글자가 안 들어가면 크기만 적거나 아이콘 하나만 둔다.
- 실제 사진을 넣기 전까지 이 규칙을 유지한다 — 그래야 어디에 무엇을 넣어야 하는지 보인다.
### 파일을 어디에 두나

```
완성화면/
  index.html              전체 화면 목록 (여기서 화면으로 들어간다)
  pages/
    HO-01.html            화면 하나에 파일 하나 · 이름은 화면 ID 그대로
    CO-03.html
  assets/
    css/style.css         스타일 한 벌
    js/app.js             동작 한 벌
```

- **`pages/` 안에서는 `../assets/css/style.css` 로 부릅니다.** `assets/...` 라고 쓰면 `pages/assets/...` 를 찾다가 못 찾아서 **스타일이 통째로 안 붙습니다** — 글꼴도 여백도 격자도 전부 날아갑니다.
- **`index.html` 은 `완성화면/` 바로 아래**에 둡니다. 거기서는 `assets/...` 그대로 부르면 됩니다.
- **스타일과 동작은 각각 한 벌**입니다. 화면마다 따로 만들면 44장이 서로 달라집니다.
- 화면끼리 오갈 때는 `CO-03.html` 처럼 **같은 폴더 안 이름**으로 겁니다.
- 다 만든 뒤 **화면 하나를 브라우저로 직접 열어 보세요.** 글꼴이 기본 고딕으로 보이면 경로가 틀린 것입니다.

### 추천 스택
- 정적 다중 페이지(HTML/CSS) 또는 컴포넌트 기반(React·Next.js·Vue 등) 중 프로젝트 규모에 맞게 선택하세요. 화면이 많고 상태가 복잡하면 컴포넌트 기반을 권장합니다.
