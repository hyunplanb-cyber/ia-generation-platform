# 공동구매(공구) 플랫폼 — AI 빌드 스펙팩

> 이 문서 하나로 사이트를 만들 수 있게 정리한 스펙이에요. 화면별 **생성 프롬프트**를 그대로 사용하세요.

## 1. 프로젝트 개요
- **컨셉**: 여러 사람이 함께 사서 더 싸게 사는 공동구매(공구) 플랫폼. 참여자는 진행 중인 공구를 둘러보고, 목표 인원·수량이 차면 할인가로 성사되는 상품에 참여한다. 목표에 못 미치면 자동으로 취소되고 전액 환불된다. 공구 진행자는 공구를 열어 목표·마감·단계별 가격을 설정하고, 참여 현황을 관리하며 성사분을 발주·배송하고 정산받는다. 달성률 게이지, 마감 카운트다운, 조건부 결제(미달 시 자동 환불)가 서비스의 핵심이다.
- **디자인 컨셉**: 모바일 우선의 활기찬 커머스. 달성률 게이지와 마감 카운트다운이 화면의 주인공. 화이트 배경에 포인트 코랄레드(#FF5A5F — 배경·배지 전용, 글자에는 #B42318)로 긴박함과 할인율을 강조하고, 상품 이미지가 큰 카드형 레이아웃. 참여 인원·남은 시간·단계별 가격을 뱃지로 즉시 보이게 한다.
- **디바이스**: 반응형
- **전체 일정**: 2026-08-03 ~ 2026-10-16

## 2. 공통 요소 (모든 화면 공통)
> 아래 요소는 **한 번만 만들어 모든 화면에서 재사용**하세요. 각 화면 스펙(4장)은 이 공통 요소 위에 올라가는 콘텐츠만 설명합니다.
- **영역이 둘입니다. 헤더를 각각 따로 만드세요.**
  - **손님 화면 GNB**: `홈·탐색` · `공구 상세` · `참여·결제` · `내 공구함` · `후기·문의·알림` · `고객지원`
  - **운영자 화면 GNB**: `공구 개설(진행자)` · `공구 관리(진행자)` · `정산(진행자)` — 좌측 사이드바 + 상단 바 형태가 맞습니다
  - **로그인·가입 화면**: `계정` — 헤더 없이 단독 화면으로 만드세요
  - 두 영역을 오가는 길은 **하나만** 둡니다 — 손님 화면 우측 상단에 운영자 영역으로 넘어가는 버튼 하나(예: `공구 개설 화면으로`), 운영자 화면 우측 상단에 `고객 화면 보기`. 한 계정이 두 권한을 가지며(크몽·숨고와 같은 방식), 아직 권한이 없으면 전환 대신 **신청 화면으로 보냅니다.**
- **콘텐츠 영역**: 최대 1440px, 좌우 여백 24px — 헤더·본문·푸터·하단 고정 바가 모두 이 폭을 쓴다. 화면마다 따로 정하지 않는다. 화면마다 폭을 새로 정하면 위아래가 어긋납니다.
- **읽기 폭**: 글이 주인공인 자리(상세 본문·안내·약관)는 최대 760px로 좁혀 콘텐츠 영역 **안에서** 가운데 정렬 — 카드 격자에는 쓰지 않습니다. 아래 「읽기 폭」 참고.
- **헤더**: 로고 + 검색 + 상단 내비게이션(GNB)
- **푸터**: 저작권 · 기본 링크
- **재사용 컴포넌트**: 반복되는 UI(상품/항목 **카드**, 리스트 **행**, **폼 필드**, **버튼**)는 공용 컴포넌트로 만들어 화면마다 재사용하세요.

## 3. 메뉴 구조
- **[HO] 홈·탐색** (Home)
  - `HO-01` 홈
  - `HO-02` 공구 목록
  - `HO-03` 마감 임박 타임딜
  - `HO-04` 공구 목록 - 결과 없음
- **[DE] 공구 상세** (Deal)
  - `DE-01` 공구 상세
  - `DE-02` 상품 상세정보·거래 조건
  - `DE-03` 공구 마감 - 성사
  - `DE-04` 공구 마감 - 불발(미달)
- **[JO] 참여·결제** (Join)
  - `JO-01` 옵션·수량 선택
  - `JO-02` 참여 결제
  - `JO-03` 참여 완료
  - `JO-04` 결제 실패
- **[MY] 내 공구함** (My)
  - `MY-01` 내 공구함
  - `MY-02` 참여 상세
  - `MY-03` 참여 취소·환불 요청
  - `MY-04` 내 공구함 - 비어 있음
- **[RV] 후기·문의·알림** (Review)
  - `RV-01` 후기 작성·목록
  - `RV-02` 상품 문의(Q&A)
  - `RV-03` 관심 공구·알림함
- **[AC] 계정** (Account)
  - `AC-01` 로그인·회원가입
  - `AC-02` 내 정보·배송지
  - `AC-03` 알림 설정
- **[HS] 공구 개설(진행자)** (Host)
  - `HS-01` 진행자 시작 안내
  - `HS-02` 공구 개설 폼
  - `HS-03` 개설 검수 대기·반려
- **[HM] 공구 관리(진행자)** (HostManage)
  - `HM-01` 진행자 대시보드
  - `HM-02` 공구별 참여 현황
  - `HM-03` 마감·성사 처리
  - `HM-04` 발주·배송 관리
  - `HM-05` 참여자 공지·알림 발송
- **[SE] 정산(진행자)** (Settlement)
  - `SE-01` 정산 내역
  - `SE-02` 정산 계좌·세금 설정
  - `SE-03` 정산 상세
- **[CS] 고객지원** (Support)
  - `CS-01` 이용 안내·자주 묻는 질문
  - `CS-02` 1:1 문의
  - `CS-03` 공지·이벤트
  - `CS-04` 환불·분쟁 정책 안내

## 4. 화면별 스펙
### HO-01 · 홈
- **소속 메뉴**: 홈·탐색
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-08-03 ~ 2026-08-06
- **기능정의**: 상단 GNB(로고·검색·내 공구함·로그인) · 히어로 배너(오늘의 대표 공구, 달성률·남은시간) · 마감 임박 공구 가로 스크롤(카운트다운 뱃지) · 카테고리 바로가기 · 지금 뜨는 공구 그리드(달성률 게이지·참여 인원·할인율) · 곧 열리는 공구 예고와 알림 신청 · 진행자 모집 배너 · 이용 안내 3단계 · 하단 앱 다운로드/뉴스레터
- **버튼 → 이동화면**:
  - `공구 둘러보기` → `HO-02` 공구 목록
  - `마감 임박` → `HO-03` 마감 임박 타임딜
  - `로그인` → `AC-01` 로그인·회원가입
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `카테고리 아이콘` → 고른 카테고리에 표시가 남고 아래 공구 목록이 걸러진다
  - `마감 임박 카드의 남은 시간` → 1초마다 줄어들고 0이 되면 「마감」 배지로 바뀐다
  - `달성률 막대` → 마우스를 올리면 몇 명 중 몇 명인지가 뜬다
  - `자주 묻는 질문` → 답이 펼쳐지고 다시 누르면 접힌다
- **생성 프롬프트**:

  ```
  공동구매 플랫폼의 홈 화면을 만들어줘. 모바일 우선, 스크롤 순서대로: 1) 상단 GNB(로고, 검색, 내 공구함, 로그인) 2) 히어로 — 오늘의 대표 공구 배너에 상품 이미지, "목표 100명 중 72명" 달성률 바, "마감까지 05:23:11" 카운트다운, "지금 참여" 버튼 3) "마감 임박" 가로 스크롤 카드(각 카드에 빨간 카운트다운 뱃지) 4) 카테고리 바로가기 아이콘(식품·생활·뷰티·가전 등) 5) "지금 뜨는 공구" 그리드 — 카드마다 상품 이미지, 할인율 뱃지(정가 대비 -33%), 달성률 게이지, 참여 인원, 남은 시간 6) "곧 열려요" 예고 공구와 "오픈 알림 받기" 버튼 7) "진행자가 되어 직접 공구를 열어보세요" 모집 배너 8) 공구 이용 3단계 안내(참여→목표 달성→배송) 상품 이미지 큰 카드형. 달성률 바와 카운트다운이 눈에 확 들어오게.
  ```

### HO-02 · 공구 목록
- **소속 메뉴**: 홈·탐색
- **뒤로가기**: 좌측 상단에 `‹ 홈` — 누르면 `HO-01`로 간다
- **일정**: 2026-08-07 ~ 2026-08-11
- **기능정의**: 카테고리 탭 · 필터(진행 상태·가격대·배송·마감 임박·달성률) · 정렬(마감 임박·인기·달성률 높은순·할인율·최신) · 공구 카드 그리드(이미지·할인율·달성률 게이지·참여 인원·남은 시간·최저가) · 무한 스크롤 · 관심 공구 찜 · 검색창
- **버튼 → 이동화면**:
  - `공구 상세` → `DE-01` 공구 상세
  - `결과 없음` → `HO-04` 공구 목록 - 결과 없음
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `필터(카테고리·달성률·배송)` → 고른 조건이 칩으로 쌓이고 목록과 「N개」 숫자가 함께 바뀐다
  - `정렬(마감 임박·인기·달성률·할인율)` → 카드 순서가 실제로 다시 늘어선다
  - `칩의 ✕` → 그 조건만 풀리고 목록이 다시 늘어난다
  - `「더 보기」` → 공구가 더 붙고 남은 개수가 줄어든다
- **생성 프롬프트**:

  ```
  공구 목록(탐색) 화면을 만들어줘. 상단에 카테고리 탭과 검색창, 필터 바(진행 상태·가격대·마감 임박·달성 임박·무료배송), 정렬 드롭다운(마감 임박순·인기순·달성률 높은순·할인율순·최신순). 공구 카드 그리드 — 각 카드: 상품 이미지, 할인율 뱃지, 상품명, 달성률 게이지(72%)와 참여 인원, 남은 시간 카운트다운, 최저가(단계 도달 시 더 싸진다는 안내), 우상단 하트(관심 공구 찜). 모바일 2열, 무한 스크롤. "거의 다 찼어요"(90% 이상)나 "마감 임박"은 카드에 강조 뱃지로 표시해줘.
  ```

### HO-03 · 마감 임박 타임딜
- **소속 메뉴**: 홈·탐색
- **뒤로가기**: 좌측 상단에 `‹ 홈` — 누르면 `HO-01`로 간다
- **일정**: 2026-08-12 ~ 2026-08-13
- **기능정의**: 실시간 카운트다운 헤더 · 마감 임박순 정렬 공구 리스트(남은 시간·달성률·부족 인원) · '앞으로 N명이면 성사' 강조 · 함께 참여 독려 문구 · 놓친 딜 다시 알림 신청 · 필터(오늘 마감·1시간 내 마감)
- **버튼 → 이동화면**:
  - `공구 상세` → `DE-01` 공구 상세
  - `전체 공구` → `HO-02` 공구 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `맨 위 카운트다운` → 1초마다 줄어들고 0이 되면 그 공구가 목록에서 「마감」으로 바뀐다
  - `「한 명만 더」 필터 칩` → 성사 직전 공구만 남는다
- **생성 프롬프트**:

  ```
  "마감 임박 타임딜" 모아보기 화면을 만들어줘. 상단에 큰 카운트다운 헤더와 "곧 마감되는 공구예요" 안내. 리스트는 마감이 임박한 순으로 — 각 항목에 상품, 남은 시간(빨간 카운트다운), 달성률과 "성사까지 8명 남았어요" 부족 인원 강조, "지금 참여하면 성사에 힘을 보태요" 독려 문구. 필터로 '오늘 마감' '1시간 내 마감'을 고를 수 있고, 이미 마감된 딜은 "다음에 열리면 알림 받기" 버튼으로. 긴박감이 느껴지게 코랄레드를 적극 써줘.
  ```

### HO-04 · 공구 목록 - 결과 없음
- **소속 메뉴**: 홈·탐색
- **뒤로가기**: 좌측 상단에 `‹ 홈` — 누르면 `HO-01`로 간다
- **일정**: 2026-08-14 ~ 2026-08-17
- **기능정의**: 검색어·필터 포함 안내 문구 · 필터 초기화 버튼 · 인기 공구 추천 4개 · 원하는 상품 공구 요청·알림 신청 · 카테고리 둘러보기 링크
- **버튼 → 이동화면**:
  - `전체 공구 보기` → `HO-02` 공구 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `걸린 조건 칩의 ✕` → 결과 수가 0에서 늘어나고 빈 화면 대신 목록이 나타난다
  - `「조건 모두 풀기」` → 칩이 전부 사라지고 전체 공구가 나온다
- **생성 프롬프트**:

  ```
  공구 검색·필터 결과가 없을 때의 화면을 만들어줘. 중앙에 아이콘과 "'{검색어}'로 진행 중인 공구가 없어요" 문구, "필터를 줄여보거나, 곧 열릴 수 있으니 알림을 받아보세요" 안내, "필터 초기화" 버튼과 "이 상품 공구 열리면 알림 받기" 버튼, 그 아래 "지금 인기 있는 공구" 추천 4개 카드를 보여줘.
  ```

### DE-01 · 공구 상세
- **소속 메뉴**: 공구 상세
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-08-18 ~ 2026-08-21
- **기능정의**: 상품 이미지 갤러리 · 달성률 게이지와 목표/현재 인원 · 마감 카운트다운 · 단계별 가격표(N명 도달 시 추가 할인) · 현재 적용가와 정가 대비 할인율 · 실시간 참여 현황(최근 참여자·증가 애니메이션) · 옵션 요약 · 조건부 결제 안내(미달 시 자동 환불) · 배송·정산 정보 · 진행자 프로필과 신뢰 지표 · 후기·별점 · 상품 문의 · 하단 고정 참여 버튼
- **버튼 → 이동화면**:
  - `참여하기` → `JO-01` 옵션·수량 선택
  - `상품 상세정보` → `DE-02` 상품 상세정보·거래 조건
  - `후기 보기` → `RV-01` 후기 작성·목록
  - `문의하기` → `RV-02` 상품 문의(Q&A)
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `옵션 고르기(색·크기)` → 선택한 옵션이 요약에 남고 재고 표시가 그 옵션 것으로 바뀐다
  - `수량 늘리고 줄이기` → 합계 금액이 그 자리에서 다시 계산된다
  - `단계별 가격표의 단계` → 지금 몇 단계인지 표시되고, 몇 명 더 모이면 얼마가 되는지 강조된다
  - `남은 시간` → 1초마다 줄어들고 0이 되면 「참여하기」가 잠긴다
  - `관심 하트` → 채워지고 다시 누르면 비워진다
- **생성 프롬프트**:

  ```
  공동구매의 핵심 화면인 공구 상세를 만들어줘. 이 화면이 제일 중요해. 상단: 상품 이미지 갤러리(스와이프). 그 아래 딜 현황 카드 — ① 달성률 게이지(72%, "목표 100명 중 72명 참여") ② "마감까지 05:23:11" 카운트다운 ③ 단계별 가격표: "50명 25,000원 / 80명 22,000원 / 100명 19,000원" 처럼 인원이 찰수록 싸지는 계단표, 현재 적용 중인 단계와 "8명만 더 모이면 22,000원!" 다음 단계 안내를 강조. ④ 현재 적용가와 정가 대비 할인율. 실시간 참여 현황(최근 참여자 목록, 인원이 오르는 느낌). "조건부 결제 안내" 박스 — "지금 결제되지만, 목표 미달로 마감되면 전액 자동 환불돼요" 를 눈에 띄게. 상품 구성·옵션 요약, 배송 정보, 공구 진행자 프로필과 신뢰 지표(성사율·후기), 구매 후기와 별점, 상품 문의 섹션. 하단에 고정된 "참여하기" 버튼(현재 적용가 표시). 모바일 우선, 코랄레드 포인트, 게이지·카운트다운이 주인공.
  ```

### DE-02 · 상품 상세정보·거래 조건
- **소속 메뉴**: 공구 상세
- **뒤로가기**: 좌측 상단에 `‹ 공구 상세` — 누르면 `DE-01`로 간다
- **일정**: 2026-08-24 ~ 2026-08-26
- **기능정의**: 상품 상세 설명·이미지 · 구성/옵션별 안내 · 배송 정보(성사 후 발송 일정·배송비·지역 제한) · 조건부 결제 규정(성사 조건·자동 환불 시점·환불 방법) · 교환·반품 규정 · 진행자·판매자 사업자 정보 · 유의사항 아코디언
- **버튼 → 이동화면**:
  - `참여하기` → `JO-01` 옵션·수량 선택
  - `공구로 돌아가기` → `DE-01` 공구 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `상세정보·거래조건·배송 탭` → 아래 내용이 그 탭 것으로 바뀐다
  - `긴 상세 이미지의 「더 보기」` → 접힌 아랫부분이 펼쳐진다
- **생성 프롬프트**:

  ```
  공구 상품의 상세정보와 거래 조건 화면을 만들어줘. 상단 탭 또는 섹션: [상품 설명][배송/환불][판매자 정보]. 상품 설명은 이미지와 텍스트로 구성/옵션 안내. 배송/환불 섹션에는 "성사 후 언제 발송되는지(예: 마감 후 7일 내)", 배송비, 지역 제한, 그리고 공구 특유의 조건부 결제 규정을 표로 — "성사 조건(목표 인원)", "미달 시 언제 자동 환불되는지", "환불 방법(원결제수단)", 성사 후 단순변심 교환·반품 규정. 하단에 진행자·판매자 사업자 정보와 유의사항 아코디언, 고정 "참여하기" 버튼.
  ```

### DE-03 · 공구 마감 - 성사
- **소속 메뉴**: 공구 상세
- **뒤로가기**: 좌측 상단에 `‹ 공구 상세` — 누르면 `DE-01`로 간다
- **일정**: 2026-08-27 ~ 2026-08-31
- **기능정의**: 성사 축하 배너와 최종 달성 인원·적용가 · 확정된 할인 단계 안내 · 배송 예정 일정 · 내 참여 확인 유도 · 후기 예고 · 비슷한 진행 중 공구 추천 · 재오픈 알림 신청
- **버튼 → 이동화면**:
  - `내 공구함` → `MY-01` 내 공구함
  - `비슷한 공구` → `HO-02` 공구 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「참여자 명단」` → 몇 명이 어떤 옵션을 골랐는지 요약이 펼쳐진다
  - `「다음 일정」` → 발주·배송 예정일이 펼쳐진다
- **생성 프롬프트**:

  ```
  목표를 달성해 공구가 "성사"되어 마감된 상태의 상세 화면을 만들어줘. 상단에 축하 배너 "🎉 성사됐어요! 최종 118명이 함께했어요", 최종 적용가와 도달한 할인 단계("100명 돌파로 19,000원 확정"), "성사 상품은 마감 후 7일 내 발송돼요" 배송 예정 안내, "내 참여 상세에서 배송을 확인하세요" 유도(내 공구함 링크), 하단에 비슷한 진행 중 공구 추천과 "이 공구 다시 열리면 알림 받기". 상품 정보는 읽기 전용으로 두고, 새로 참여는 안 되게(참여 버튼 대신 "마감됨" 상태).
  ```

### DE-04 · 공구 마감 - 불발(미달)
- **소속 메뉴**: 공구 상세
- **뒤로가기**: 좌측 상단에 `‹ 공구 상세` — 누르면 `DE-01`로 간다
- **일정**: 2026-09-01 ~ 2026-09-03
- **기능정의**: 미달 안내 문구와 최종 인원/목표 · 자동 전액 환불 안내와 환불 예정 시점 · 환불 진행 상태 표시 · 아쉬움 위로 카피 · 재도전(재오픈) 알림 신청 · 비슷한 진행 중 공구 추천 · 문의 링크
- **버튼 → 이동화면**:
  - `비슷한 공구 보기` → `HO-02` 공구 목록
  - `내 공구함` → `MY-01` 내 공구함
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「환불 안내」` → 언제 얼마가 돌아오는지 펼쳐진다
  - `「재오픈 알림」 토글` → 켜면 연락 방법 칸이 나타난다
- **생성 프롬프트**:

  ```
  목표에 못 미쳐 "불발"로 마감된 공구의 상세 화면을 만들어줘. 상단에 담담한 안내 "아쉽게 목표에 도달하지 못했어요 (최종 63명 / 목표 100명)", 그 아래 눈에 띄는 환불 안내 박스 — "결제하신 금액은 전액 자동 환불돼요. 카드는 3~5영업일 내 취소 처리됩니다" 와 환불 진행 상태(환불 예정/처리 중/완료). 위로 카피 "다음엔 함께 성사시켜요", "이 공구 다시 열리면 알림 받기" 버튼과 비슷한 진행 중 공구 추천, 하단 문의 링크. 참여자가 손해 볼 게 없다는 안심을 주는 톤으로. 참여 버튼은 없음.
  ```

### JO-01 · 옵션·수량 선택
- **소속 메뉴**: 참여·결제
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-09-04 ~ 2026-09-08
- **기능정의**: 옵션 조합 선택(색상·용량 등) · 수량 선택 · 현재 적용 단계 가격 반영 · '내 참여로 달성률 얼마나 오르는지' 안내 · 배송지 확인 · 예상 결제 금액 · 다음 단계까지 남은 인원 안내 · 관심 공구 담기 · 결제로 이동
- **버튼 → 이동화면**:
  - `결제하기` → `JO-02` 참여 결제
  - `관심 공구 담기` → `RV-03` 관심 공구·알림함
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `옵션 고르기` → 요약과 합계 금액이 그 자리에서 다시 계산된다
  - `수량 늘리고 줄이기` → 합계와 배송비 무료 여부가 다시 계산된다
  - `재고가 0인 옵션` → 눌리지 않고 흐리게 남는다
- **생성 프롬프트**:

  ```
  공구 참여 첫 단계인 옵션·수량 선택 화면(또는 하단 바텀시트)을 만들어줘. 상품 요약(이미지·상품명·현재 적용가)과 옵션 선택(색상·용량 등 드롭다운/칩), 수량 선택 스텝퍼. "지금 참여하면 달성률이 72% → 73%로 올라가요" 안내, "다음 단계(80명)까지 8명 남았어요 — 도달하면 22,000원" 안내로 참여를 독려. 배송지 요약(변경 링크)과 예상 결제 금액을 크게. 하단에 "결제하기"와 "관심 공구에 담기" 버튼. 재고/1인 최대 수량 제한도 반영해줘.
  ```

### JO-02 · 참여 결제
- **소속 메뉴**: 참여·결제
- **뒤로가기**: 좌측 상단에 `‹ 옵션·수량 선택` — 누르면 `JO-01`로 간다
- **일정**: 2026-09-09 ~ 2026-09-14
- **기능정의**: 참여 상품·옵션·수량 요약 · 배송지 입력/선택 · 쿠폰·적립금 적용 · 결제 수단 선택 · 조건부 결제 고지(지금 결제·미달 시 자동 전액 환불) 필수 동의 · 개인정보 제3자 제공(진행자) 동의 · 최종 금액과 예상 성사 시 추가할인 안내 · 결제 버튼
- **버튼 → 이동화면**:
  - `참여 완료` → `JO-03` 참여 완료
  - `결제 실패` → `JO-04` 결제 실패
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `쿠폰·적립금 적용` → 할인 줄과 최종 결제 금액이 다시 계산된다
  - `배송지 고르기` → 요약의 주소가 바뀌고 도서산간이면 추가비가 붙는다
  - `약관 동의 체크` → 다 체크해야 「결제하기」가 켜진다
- **생성 프롬프트**:

  ```
  공구 참여 결제 화면을 만들어줘. 일반 결제와 다른 점(조건부 결제)을 분명히 보여줘야 해. 좌측: 참여 상품 요약(이미지·옵션·수량·현재 적용가), 배송지 선택/입력, 쿠폰·적립금 적용, 결제 수단 선택(카드·카카오페이·계좌이체). 우측: 금액 요약(상품금액·배송비·할인·최종 결제 금액 크게), "지금은 현재 단계 가격으로 결제되고, 성사 시 더 낮은 단계에 도달하면 차액이 환급돼요" 안내. 하단에 필수 동의 — ① "목표 미달로 마감되면 전액 자동 환불된다는 데 동의" ② 진행자(판매자)에게 배송정보 제공 동의. 동의 전에는 결제 버튼 비활성. 결제 버튼에 "OO원 결제하고 참여하기".
  ```

### JO-03 · 참여 완료
- **소속 메뉴**: 참여·결제
- **뒤로가기**: 좌측 상단에 `‹ 옵션·수량 선택` — 누르면 `JO-01`로 간다
- **일정**: 2026-09-15 ~ 2026-09-16
- **기능정의**: 참여 완료 메시지와 참여 번호 · 갱신된 달성률과 남은 시간 · '친구를 초대해 성사에 힘 보태기' 공유 링크(카톡·URL 복사)와 초대 보상 안내 · 성사/미달 시 각각 어떻게 되는지 안내 · 내 참여 상세로 이동 · 다른 공구 추천
- **버튼 → 이동화면**:
  - `내 공구함` → `MY-01` 내 공구함
  - `공구 더 보기` → `HO-02` 공구 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `주문 번호` → 눌러서 복사되고 「복사했어요」가 뜬다
  - `「달성 현황」` → 지금 몇 명인지와 성사까지 남은 수가 뜬다
- **생성 프롬프트**:

  ```
  공구 참여를 마친 뒤의 완료 화면을 만들어줘. 중앙에 성공 체크와 "참여 완료! 함께 성사시켜요" 문구, 참여 번호. 내 참여로 갱신된 달성률("이제 73%!")과 남은 시간을 다시 보여주고, 핵심으로 "친구를 초대하면 더 빨리 성사돼요" 공유 카드 — 카카오톡 공유, URL 복사 버튼과 "초대로 성사되면 적립금" 같은 보상 안내. "성사되면 배송, 미달이면 전액 환불" 두 갈래를 짧게 다시 안내, 하단에 "내 참여 상세 보기"와 "다른 공구 둘러보기". 바이럴이 잘 일어나게 공유를 눈에 띄게.
  ```

### JO-04 · 결제 실패
- **소속 메뉴**: 참여·결제
- **뒤로가기**: 좌측 상단에 `‹ 옵션·수량 선택` — 누르면 `JO-01`로 간다
- **일정**: 2026-09-17 ~ 2026-09-18
- **기능정의**: 실패 사유를 쉬운 문장으로 안내 · 참여 정보 유지 안내 · 다시 시도 버튼 · 다른 결제 수단 선택 · 마감 임박 시 서두름 안내 · 문의 링크
- **버튼 → 이동화면**:
  - `다시 시도` → `JO-02` 참여 결제
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `실패 사유 「자세히」` → 무엇이 문제였는지 펼쳐진다
  - `다른 결제 수단 고르기` → 표시가 옮겨 가고 「다시 시도」 문구가 바뀐다
- **생성 프롬프트**:

  ```
  공구 참여 결제가 실패했을 때의 화면을 만들어줘. 경고 아이콘과 "결제가 완료되지 않아 아직 참여되지 않았어요" 문구, 실패 사유를 쉬운 말로(예: 카드 한도를 초과했어요), "고른 옵션은 그대로 있어요" 안내와 "다시 시도"· "다른 결제 수단" 버튼. 마감이 임박했다면 "마감까지 12분 — 서둘러 주세요" 카운트다운으로 재시도를 유도, 하단에 문의하기 링크.
  ```

### MY-01 · 내 공구함
- **소속 메뉴**: 내 공구함
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-09-21 ~ 2026-09-23
- **기능정의**: 상태 탭(진행 중·성사·불발·배송/완료) · 참여 공구 카드(상품·현재 달성률·남은 시간·내 결제액·상태 배지) · 진행 중은 달성률·공유 유도 · 불발은 환불 상태 · 배송은 송장·배송 조회 · 후기 미작성 알림 · 정렬·검색
- **버튼 → 이동화면**:
  - `참여 상세` → `MY-02` 참여 상세
  - `비어있음` → `MY-04` 내 공구함 - 비어 있음
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `상태 탭(모집 중·성사·불발·배송 중)` → 목록과 탭 옆 숫자가 함께 바뀐다
  - `공구 카드의 달성률 막대` → 1분마다 갱신되고 성사되면 배지가 바뀐다
- **생성 프롬프트**:

  ```
  내가 참여한 공구를 모아보는 "내 공구함" 화면을 만들어줘. 상단에 상태 탭 — [진행 중][성사][불발][배송/완료]. 각 참여 건을 카드로: 상품 이미지, 상품명, 상태에 따라 다른 정보 — 진행 중이면 달성률 게이지·남은 시간·"친구 초대" 버튼, 성사면 "성사! 곧 배송돼요", 불발이면 "환불 처리 중/완료" 상태 배지, 배송이면 송장 번호와 "배송 조회" 버튼. 후기를 안 쓴 완료 건에는 "후기 쓰기" 버튼. 카드 클릭 시 참여 상세로.
  ```

### MY-02 · 참여 상세
- **소속 메뉴**: 내 공구함
- **뒤로가기**: 좌측 상단에 `‹ 내 공구함` — 누르면 `MY-01`로 간다
- **일정**: 2026-09-24 ~ 2026-09-29
- **기능정의**: 공구·상품 요약 · 내 참여 정보(옵션·수량·결제액·참여일) · 진행 상태 타임라인(참여→마감→성사/불발→발주→배송→완료) · 현재 달성률과 남은 시간(진행 중) · 성사 시 차액 환급 내역 · 배송 조회 · 취소·환불 요청 · 영수증 · 문의
- **버튼 → 이동화면**:
  - `취소·환불 요청` → `MY-03` 참여 취소·환불 요청
  - `공구 상세` → `DE-01` 공구 상세
  - `후기 쓰기` → `RV-01` 후기 작성·목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「내 옵션·수량」` → 고른 내용과 결제 내역이 펼쳐진다
  - `「배송 조회」` → 배송 단계가 그 자리에서 펼쳐진다
- **생성 프롬프트**:

  ```
  참여 상세 화면을 만들어줘. 상단에 공구·상품 요약과 현재 상태 배지. 내 참여 정보 카드(옵션, 수량, 결제 금액, 참여일, 참여 번호). 진행 상태 타임라인 — 참여 완료 → 공구 마감 → 성사/불발 → (성사 시) 발주 → 배송 → 수령 완료, 지난 단계는 체크. 진행 중이면 실시간 달성률과 남은 시간을 보여주고, 성사로 더 낮은 단계에 도달했다면 "차액 3,000원이 적립금으로 환급됐어요" 내역, 배송 단계면 송장·배송 조회 버튼. 하단 액션: "참여 취소·환불 요청"(마감 전만 가능 안내), "영수증", "문의하기", 완료 시 "후기 쓰기".
  ```

### MY-03 · 참여 취소·환불 요청
- **소속 메뉴**: 내 공구함
- **뒤로가기**: 좌측 상단에 `‹ 내 공구함` — 누르면 `MY-01`로 간다
- **일정**: 2026-09-30 ~ 2026-10-02
- **기능정의**: 취소 가능 여부 안내(마감 전 자유 취소·성사 후 규정 적용) · 취소 시 달성률 영향 안내 · 취소 사유 선택 · 환불 예정 금액과 수단·시점 · 성사 후 반품은 배송 규정 안내 · 취소 확정 버튼(확인 모달) · 취소 대신 유지 유도
- **버튼 → 이동화면**:
  - `내 공구함` → `MY-01` 내 공구함
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `취소 사유 고르기` → 고른 것에 표시가 남고 아래 환불 예상 금액이 다시 계산된다
  - `사유를 안 고른 상태` → 「요청」이 눌리지 않는다
- **생성 프롬프트**:

  ```
  공구 참여 취소·환불 요청 화면을 만들어줘. 상단에 현재 상태에 따른 안내 — "마감 전에는 자유롭게 취소할 수 있어요" 또는 "성사되어 발송 준비 중이라 반품 규정이 적용돼요". "취소하면 달성률이 73% → 72%로 내려가요" 같은 영향 안내(마감 전), 취소 사유 선택(단순 변심·중복 참여·기타)과 상세, 환불 예정 금액·수단(원결제수단)·시점 표시. 빨간 "취소 확정" 버튼(누르면 확인 모달)과, 그 위에 "조금만 기다리면 성사될 수도 있어요, 유지할래요?" 유지 유도 버튼.
  ```

### MY-04 · 내 공구함 - 비어 있음
- **소속 메뉴**: 내 공구함
- **뒤로가기**: 좌측 상단에 `‹ 내 공구함` — 누르면 `MY-01`로 간다
- **일정**: 2026-10-05 ~ 2026-10-06
- **기능정의**: 참여 내역 없음 안내와 일러스트 · 공구 둘러보기 CTA · 첫 참여 할인 쿠폰 안내 · 인기·마감 임박 공구 추천 · 관심 공구로 이동
- **버튼 → 이동화면**:
  - `공구 둘러보기` → `HO-02` 공구 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「공구 둘러보기」` → 빈 화면 대신 추천 공구 목록이 그 자리에 나타난다
- **생성 프롬프트**:

  ```
  아직 참여한 공구가 없을 때의 "내 공구함" 화면을 만들어줘. 일러스트와 "아직 참여한 공구가 없어요" 문구, 큰 CTA "지금 열린 공구 둘러보기", "첫 참여 시 쓸 수 있는 할인 쿠폰"과 "마감 임박 인기 공구" 2~3개 카드, "찜해둔 공구 보기" 링크도 함께 넣어줘.
  ```

### RV-01 · 후기 작성·목록
- **소속 메뉴**: 후기·문의·알림
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-10-07 ~ 2026-10-09
- **기능정의**: 별점 선택 · 포토·동영상 첨부 · 옵션 선택 정보 자동 표시 · 후기 본문 · 재구매 의사 · 도움돼요 추천 · 후기 목록(별점 분포·정렬·포토후기 필터) · 진행자 답글 · 신고 · 후기 작성 보상 안내
- **버튼 → 이동화면**:
  - `공구 상세` → `DE-01` 공구 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `별점` → 누른 별까지 채워지고 안내 문구가 바뀐다
  - `사진 올리기·빼기` → 미리보기가 늘고 줄며 「N/5장」이 따라 바뀐다
  - `「도움돼요」` → 숫자가 하나 오르고 다시 누르면 되돌아간다
- **생성 프롬프트**:

  ```
  공구 상품 후기 작성과 목록을 함께 보여주는 화면을 만들어줘. 상단 작성 영역: 별점(1~5), 사진·동영상 첨부, 내가 산 옵션 자동 표시, 후기 본문 입력, "재구매 의사" 체크, "후기 쓰면 적립금" 보상 안내. 그 아래 후기 목록 — 별점 분포 막대, 정렬(최신·별점·도움순), 포토후기만 보기 필터, 각 후기에 작성자(일부 마스킹), 별점, 옵션, 본문, 사진, "도움돼요" 수, 진행자(판매자)의 답글, 신고 버튼.
  ```

### RV-02 · 상품 문의(Q&A)
- **소속 메뉴**: 후기·문의·알림
- **뒤로가기**: 좌측 상단에 `‹ 후기 작성·목록` — 누르면 `RV-01`로 간다
- **일정**: 2026-10-12 ~ 2026-10-14
- **기능정의**: 문의 작성(유형 선택·비공개 옵션) · 문의 목록(질문·답변·상태 배지) · 자주 묻는 질문 상단 노출 · 답변 알림 안내 · 검색 · 진행자 답변 대기 표시 · 성사·배송 관련 문의 분류
- **버튼 → 이동화면**:
  - `공구 상세` → `DE-01` 공구 상세
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「답변 완료만」 토글` → 목록이 걸러진다
  - `질문 줄` → 답변이 펼쳐지고 다시 누르면 접힌다
  - `「비밀글」 체크` → 글쓰기 칸에 자물쇠 표시가 붙는다
- **생성 프롬프트**:

  ```
  공구 상품 문의(Q&A) 화면을 만들어줘. 상단에 "문의하기" 작성 폼 — 유형 선택(상품·배송·성사조건·환불), 내용, "비공개 문의" 체크. 자주 묻는 질문(공구는 언제 성사되나요? 미달이면 환불은? 배송은 언제?)을 상단에 접이식으로. 그 아래 문의 목록 — 질문, 상태 배지(답변대기·답변완료), 진행자 답변, 비공개 문의는 작성자만 보이게. 답변이 달리면 알림으로 알려준다는 안내도.
  ```

### RV-03 · 관심 공구·알림함
- **소속 메뉴**: 후기·문의·알림
- **뒤로가기**: 좌측 상단에 `‹ 후기 작성·목록` — 누르면 `RV-01`로 간다
- **일정**: 2026-10-15 ~ 2026-10-19
- **기능정의**: 찜한 공구 목록(달성률·남은 시간·상태 변화 표시) · 오픈 예정 공구 알림 신청 목록 · 알림 내역(성사·마감 임박·재오픈·배송) · 읽음 처리 · 관심 공구에서 바로 참여 · 알림 설정 링크
- **버튼 → 이동화면**:
  - `공구 상세` → `DE-01` 공구 상세
  - `알림 설정` → `AC-03` 알림 설정
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `관심·알림 탭` → 목록과 탭 옆 숫자가 함께 바뀐다
  - `관심 해제` → 그 카드가 목록에서 빠지고 숫자가 줄어든다
  - `「모두 읽음」` → 안 읽음 표시가 전부 사라지고 배지가 0이 된다
- **생성 프롬프트**:

  ```
  관심 공구와 알림을 모아보는 화면을 만들어줘. 탭으로 [찜한 공구][알림함]. '찜한 공구' 탭: 하트 눌러둔 공구 카드 목록에 달성률·남은 시간, "마감 임박!"이나 "성사됐어요" 같은 상태 변화 뱃지, 바로 "참여하기" 버튼. '알림함' 탭: 시간순 알림 리스트(성사 알림, 마감 임박, 오픈 예정 공구 열림, 배송 출발 등), 안 읽은 건 강조, "모두 읽음" 버튼, 우상단 "알림 설정" 링크.
  ```

### AC-01 · 로그인·회원가입
- **소속 메뉴**: 계정
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-10-20 ~ 2026-10-22
- **기능정의**: 소셜 로그인(카카오·네이버·구글) · 이메일 로그인 · 회원가입 전환 · 휴대폰 인증 · 약관·개인정보·마케팅 수신 동의 · 첫 가입 쿠폰 안내 · 비회원 주문 조회 · 비밀번호 찾기
- **버튼 → 이동화면**:
  - `홈` → `HO-01` 홈
  - `내 정보` → `AC-02` 내 정보·배송지
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `로그인·회원가입 탭` → 아래 입력 칸 묶음이 그 탭 것으로 바뀐다
  - `「비밀번호 보기」 눈 아이콘` → 가려진 글자가 보이고 다시 누르면 가려진다
  - `약관 「전체 동의」` → 개별 체크가 한꺼번에 켜지고, 하나를 끄면 전체 동의가 풀린다
- **생성 프롬프트**:

  ```
  공구 플랫폼 로그인·회원가입 화면을 만들어줘. 모바일 우선. 상단에 소셜 로그인 버튼(카카오·네이버·구글)을 크게 —"3초 만에 시작", 아래 이메일 로그인 입력과 "회원가입" 전환 링크. 회원가입 시 휴대폰 인증, 약관·개인정보 필수 동의와 마케팅 수신 선택 동의, "가입하면 첫 참여 3천원 쿠폰" 안내. 하단에 "비회원 주문 조회"와 "비밀번호 찾기" 링크.
  ```

### AC-02 · 내 정보·배송지
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인·회원가입` — 누르면 `AC-01`로 간다
- **일정**: 2026-10-23 ~ 2026-10-28
- **기능정의**: 프로필(닉네임·프로필 사진) · 휴대폰·이메일 · 배송지 목록 관리(기본 배송지 지정·추가·수정·삭제) · 결제 수단 관리 · 쿠폰·적립금 잔액 · 참여·후기 통계 · 진행자 전환/진행자 페이지 진입 · 회원 탈퇴 안내
- **버튼 → 이동화면**:
  - `알림 설정` → `AC-03` 알림 설정
  - `내 공구함` → `MY-01` 내 공구함
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `배송지 「기본으로」` → 그 주소에 배지가 옮겨 붙는다
  - `「+ 배송지 추가」` → 입력 칸이 펼쳐지고 저장하면 목록에 한 줄이 는다
  - `배송지 「삭제」` → 확인을 한 번 물은 뒤 그 줄이 사라진다
- **생성 프롬프트**:

  ```
  내 정보·배송지 관리 화면을 만들어줘. 상단에 프로필(닉네임, 프로필 사진, 휴대폰·이메일)과 요약(적립금·쿠폰 잔액, 참여 n건·후기 n건). 배송지 관리 섹션 — 배송지 카드 목록, 기본 배송지 지정 토글, 추가/수정/삭제, 결제 수단 관리. "직접 공구를 열고 싶다면? 진행자 시작하기" 진입 배너(진행자 안내로 이동), 하단에 회원 탈퇴 안내. 자주 바뀌는 배송지 관리가 편하게 보이도록.
  ```

### AC-03 · 알림 설정
- **소속 메뉴**: 계정
- **뒤로가기**: 좌측 상단에 `‹ 로그인·회원가입` — 누르면 `AC-01`로 간다
- **일정**: 2026-10-29 ~ 2026-10-30
- **기능정의**: 알림 채널(앱 푸시·카카오·문자·이메일) 토글 · 알림 유형별 수신(찜한 공구 마감 임박·성사·불발 환불·오픈 예정·배송·혜택) · 야간 방해금지 시간 · 마케팅 수신 동의와 철회 · 관심 카테고리 알림 · 저장
- **버튼 → 이동화면**:
  - `내 정보` → `AC-02` 내 정보·배송지
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `알림 항목 토글` → 켜고 끈 것이 그대로 남는다
  - `「전체 끄기」` → 아래 항목이 한꺼번에 꺼지고, 하나를 켜면 전체 끄기가 풀린다
  - `방해 금지 시간 고르기` → 아래 안내 문구가 그 시간대로 바뀐다
- **생성 프롬프트**:

  ```
  알림 설정 화면을 만들어줘. 채널별 토글(앱 푸시·카카오 알림톡·문자·이메일), 알림 유형별 수신 설정 — 찜한 공구 마감 임박, 성사 알림, 불발·환불 안내, 오픈 예정 공구 열림, 배송 알림, 혜택·쿠폰. 각각 켜고 끄기. "밤 9시~오전 8시 방해 금지" 시간 설정, 마케팅 정보 수신 동의와 철회, 관심 카테고리 신규 공구 알림. 토글 위주로 한눈에, 하단 "저장" 버튼.
  ```

### HS-01 · 진행자 시작 안내
- **소속 메뉴**: 공구 개설(진행자)
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-11-02 ~ 2026-11-04
- **기능정의**: 공구 진행자가 하는 일 소개 · 수익 구조와 수수료 안내 · 정산 방식·시점 · 성공 사례와 팁 · 필요한 준비물(상품·공급처·사업자 여부) · 자격 요건과 신뢰 등급 · 시작하기 CTA · 자주 묻는 질문
- **버튼 → 이동화면**:
  - `공구 개설하기` → `HS-02` 공구 개설 폼
  - `정산 안내` → `SE-01` 정산 내역
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `수수료 계산기 값 바꾸기` → 예상 수익이 그 자리에서 다시 계산된다
  - `「진행 절차」 단계` → 그 단계에서 무엇을 하는지 펼쳐진다
- **생성 프롬프트**:

  ```
  "공구 진행자 되기"를 안내하는 랜딩 화면을 만들어줘. 공구를 직접 열고 싶은 사람 대상. 상단 히어로 "직접 공구를 열고 수익을 만들어보세요", 진행자가 하는 일 3단계(상품·목표 설정 → 참여자 모집 → 성사분 발주·배송), 수익 구조와 플랫폼 수수료(예: 성사 매출의 X%), 정산 시점 안내, 성공 사례 카드와 팁, 필요한 준비물(상품/공급처, 사업자 등록 여부), 진행자 신뢰 등급 안내, "공구 개설하기" 큰 CTA, 하단 자주 묻는 질문 아코디언.
  ```

### HS-02 · 공구 개설 폼
- **소속 메뉴**: 공구 개설(진행자)
- **뒤로가기**: 좌측 상단에 `‹ 진행자 시작 안내` — 누르면 `HS-01`로 간다
- **일정**: 2026-11-05 ~ 2026-11-10
- **기능정의**: 상품명·카테고리·이미지 업로드 · 상세 설명 에디터 · 정가와 단계별 가격 설정(인원 구간별 할인가) · 목표 인원·최소 성사 조건 · 모집 기간(시작·마감일시) · 1인 최대 수량·총 수량 한도 · 옵션 관리 · 배송비·발송 일정·지역 제한 · 조건부 환불 정책 확인 · 공급가·예상 수익 계산 · 임시 저장·검수 제출
- **버튼 → 이동화면**:
  - `미리보기·제출` → `HS-03` 개설 검수 대기·반려
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `단계별 가격 줄 추가·삭제` → 단계가 늘고 줄며 미리보기 가격표가 따라 바뀐다
  - `목표 인원·마감일 입력` → 아래 「고객에게 보이는 모습」이 그 값으로 다시 그려진다
  - `사진 올리기·빼기` → 장수가 늘고 줄며 대표 사진이 표시된다
  - `필수 칸이 빈 상태` → 「검수 요청」이 눌리지 않고 어느 칸이 비었는지 표시된다
- **생성 프롬프트**:

  ```
  공구 개설 폼을 만들어줘. 진행자가 새 공구를 등록하는 화면이야. 섹션으로: 1) 상품 — 상품명, 카테고리, 대표·추가 이미지, 상세 설명 에디터, 옵션(색상·용량 등) 2) 가격 — 정가와 "단계별 가격표" 설정: 인원 구간(50명/80명/100명)마다 적용가를 입력하는 표, 미리보기로 "이렇게 계단식으로 싸져요"를 보여줌 3) 목표·기간 — 최소 성사 인원(목표), 모집 시작·마감 일시(카운트다운에 쓰임), 1인 최대 수량, 총 수량 한도 4) 배송 — 배송비, 성사 후 발송 일정(예: 마감 후 7일), 지역 제한 5) 정산·수익 — 공급가 입력 시 예상 수익·수수료 자동 계산 6) 정책 — 조건부 환불(미달 시 전액 환불) 정책 확인 체크. 하단에 "임시 저장"과 "검수 제출". 잘못 입력하면(마감이 과거, 단계 가격 역전 등) 경고를 띄워줘.
  ```

### HS-03 · 개설 검수 대기·반려
- **소속 메뉴**: 공구 개설(진행자)
- **뒤로가기**: 좌측 상단에 `‹ 진행자 시작 안내` — 누르면 `HS-01`로 간다
- **일정**: 2026-11-11 ~ 2026-11-13
- **기능정의**: 제출한 공구 미리보기 · 검수 상태(대기·승인·반려) 표시 · 반려 사유와 수정 안내 · 예상 검수 소요 시간 · 승인 시 자동 오픈 예약 안내 · 수정 후 재제출 · 진행자 대시보드로 이동 · 검수 가이드 링크
- **버튼 → 이동화면**:
  - `개설 수정` → `HS-02` 공구 개설 폼
  - `진행자 대시보드` → `HM-01` 진행자 대시보드
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `점검 항목 줄` → 무엇을 봤는지 설명이 펼쳐진다
  - `반려 사유 「자세히」` → 무엇을 고쳐야 하는지 펼쳐진다
- **생성 프롬프트**:

  ```
  공구를 검수 제출한 뒤의 상태 화면을 만들어줘. 상단에 제출한 공구 미리보기 카드와 검수 상태 배지 — [검수 대기 중] / [승인됨] / [반려됨]. 대기 중이면 "보통 1영업일 내 검토돼요" 예상 시간, 승인되면 "설정한 시작 시각에 자동으로 오픈돼요" 안내, 반려면 사유를 항목별로 보여주고(예: 이미지 화질, 가격 정보 불명확) "수정 후 다시 제출" 버튼. 하단에 "진행자 대시보드로 가기"와 검수 가이드 링크. 반려가 기분 나쁘지 않게 개선 위주로 안내해줘.
  ```

### HM-01 · 진행자 대시보드
- **소속 메뉴**: 공구 관리(진행자)
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-11-16 ~ 2026-11-18
- **기능정의**: 요약 지표(진행 중 공구·총 참여자·이번 달 매출·정산 예정액) · 내 공구 목록(상태·달성률·남은 시간·참여 인원·매출) · 마감 임박/미달 위험 공구 강조 · 처리 필요 항목(발주 대기·배송 대기·문의 미답변) · 신규 공구 개설 버튼 · 성사율·후기 평점
- **버튼 → 이동화면**:
  - `참여 현황` → `HM-02` 공구별 참여 현황
  - `개설하기` → `HS-02` 공구 개설 폼
  - `정산 내역` → `SE-01` 정산 내역
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `기간 탭(오늘·이번 주·이번 달)` → 지표 숫자와 참여 추이 차트가 그 기간 것으로 바뀐다
  - `「처리할 일」 줄의 처리 버튼` → 그 줄이 빠지고 위의 건수가 줄어든다
  - `공구 카드` → 아래 표가 그 공구 참여자 목록으로 바뀐다
- **생성 프롬프트**:

  ```
  공구 진행자 대시보드를 만들어줘. 상단에 요약 카드 4개 (진행 중 공구 수, 총 참여자, 이번 달 매출, 정산 예정액). 내 공구 리스트(카드 또는 표) — 각 공구의 상태 배지(모집 중·성사·불발·배송 중), 달성률 게이지, 남은 시간, 참여 인원, 예상/확정 매출. "미달 위험(마감 임박인데 목표 미달)" 공구는 빨갛게 강조하고 "참여자 독려 알림 보내기" 유도. 처리 필요 위젯 — 발주 대기 n건, 배송 대기 n건, 미답변 문의 n건(클릭 시 이동). 우상단 "새 공구 개설" 버튼과 내 진행자 성사율·후기 평점 표시.
  ```

### HM-02 · 공구별 참여 현황
- **소속 메뉴**: 공구 관리(진행자)
- **뒤로가기**: 좌측 상단에 `‹ 진행자 대시보드` — 누르면 `HM-01`로 간다
- **일정**: 2026-11-19 ~ 2026-11-24
- **기능정의**: 선택 공구 실시간 달성률·목표 대비 그래프 · 시간대별 참여 추이 · 현재 단계와 다음 단계까지 남은 인원 · 참여자 목록(닉네임·옵션·수량·금액·참여시각·상태) · 검색·필터 · 취소·환불 참여 표시 · 참여자에게 공지 발송 · 엑셀 내보내기
- **버튼 → 이동화면**:
  - `마감·성사 처리` → `HM-03` 마감·성사 처리
  - `참여자 공지` → `HM-05` 참여자 공지·알림 발송
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `옵션별 탭` → 참여자 표가 그 옵션 것으로 걸러진다
  - `표 머리글(참여일·수량)` → 그 기준으로 정렬되고 화살표가 바뀐다
  - `줄 체크` → 고른 수가 뜨고 「선택한 N명에게 알림」이 켜진다
- **생성 프롬프트**:

  ```
  특정 공구의 참여 현황 관리 화면을 만들어줘. 상단에 그 공구 요약과 실시간 달성률 게이지(72/100명), 목표까지 남은 인원, 현재 적용 단계와 "다음 단계까지 8명". 시간대별 참여 추이 그래프(언제 많이 들어왔는지). 참여자 테이블 — 닉네임(마스킹), 선택 옵션, 수량, 결제액, 참여 시각, 상태(참여·취소). 검색·필터, "참여자에게 공지 보내기" 버튼(예: 마감 임박 독려), "엑셀 내보내기". 아직 모집 중이면 마감 시각과 카운트다운도 같이 보여줘.
  ```

### HM-03 · 마감·성사 처리
- **소속 메뉴**: 공구 관리(진행자)
- **뒤로가기**: 좌측 상단에 `‹ 진행자 대시보드` — 누르면 `HM-01`로 간다
- **일정**: 2026-11-25 ~ 2026-11-27
- **기능정의**: 마감 임박 공구 요약과 최종 달성 여부 판정 · 성사 확정 처리 · 목표 근접 시 마감 연장 옵션 · 미달 시 불발 처리와 자동 환불 실행 안내 · 참여자 일괄 알림(성사/환불) · 성사분 수량 집계와 발주 준비 · 처리 확인 모달
- **버튼 → 이동화면**:
  - `발주·배송` → `HM-04` 발주·배송 관리
  - `진행자 대시보드` → `HM-01` 진행자 대시보드
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `「지금 마감」` → 확인을 한 번 물은 뒤 상태가 「마감」으로 바뀌고 아래 안내가 바뀐다
  - `목표 인원 바꾸기` → 성사 여부 판정과 아래 안내 문구가 다시 계산된다
- **생성 프롬프트**:

  ```
  공구 마감·성사 처리 화면을 만들어줘. 진행자가 마감 시점에 결정하는 화면이야. 상단에 공구 요약과 "목표 100명 / 현재 96명" 최종 근접 현황. 선택지 — ① "성사 확정"(목표 달성 시 활성), ② "마감 24시간 연장"(목표에 근접했을 때 한 번 제공), ③ 미달이면 "불발 처리 → 전액 자동 환불" 안내. 성사 확정을 누르면 "참여자 96명에게 성사 알림이 가고, 총 수량이 집계돼 발주 준비로 넘어가요" 안내와 확인 모달. 불발이면 "참여자에게 환불 안내가 자동 발송되고 원결제수단으로 취소돼요" 안내. 잘못 누르지 않게 각 처리에 확인 단계를 둬줘.
  ```

### HM-04 · 발주·배송 관리
- **소속 메뉴**: 공구 관리(진행자)
- **뒤로가기**: 좌측 상단에 `‹ 진행자 대시보드` — 누르면 `HM-01`로 간다
- **일정**: 2026-11-30 ~ 2026-12-03
- **기능정의**: 성사분 총 수량·옵션별 집계 · 공급처 발주서 생성·다운로드 · 배송 상태 관리(준비·발송·완료) · 참여자별 배송지 목록과 송장번호 일괄 등록(엑셀 업로드) · 배송 출발 알림 발송 · 반품·교환 접수 · 배송 지연 공지 · 진행률 표시
- **버튼 → 이동화면**:
  - `참여 현황` → `HM-02` 공구별 참여 현황
  - `정산 내역` → `SE-01` 정산 내역
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `송장번호 입력` → 그 줄 상태가 「배송 중」으로 바뀌고 위의 미발송 건수가 줄어든다
  - `「일괄 등록」` → 붙여넣은 만큼 줄 상태가 한꺼번에 바뀐다
  - `「미발송만 보기」 토글` → 목록이 걸러진다
- **생성 프롬프트**:

  ```
  성사된 공구의 발주·배송 관리 화면을 만들어줘. 상단에 성사 요약(총 96건, 옵션별 수량 집계 표)과 "공급처 발주서 만들기(다운로드)" 버튼. 배송 섹션 — 참여자별 배송지 목록(이름·주소·옵션·수량), 송장번호 입력 또는 엑셀 일괄 업로드, 배송 상태 일괄 변경(배송 준비 → 발송 → 완료), "발송 알림 보내기". 상단에 배송 진행률 바(120건 중 80건 발송). 반품·교환 접수 목록과 "배송이 늦어지면 공지 보내기" 기능도 넣어줘.
  ```

### HM-05 · 참여자 공지·알림 발송
- **소속 메뉴**: 공구 관리(진행자)
- **뒤로가기**: 좌측 상단에 `‹ 진행자 대시보드` — 누르면 `HM-01`로 간다
- **일정**: 2026-12-04 ~ 2026-12-08
- **기능정의**: 대상 공구·참여자 세그먼트 선택 · 공지 유형(마감 임박 독려·성사 안내·배송 지연·감사) · 템플릿 불러오기와 치환 변수 · 발송 채널(앱 알림·카톡·문자) · 예상 도달 수·비용 · 미리보기 · 즉시·예약 발송 · 발송 이력
- **버튼 → 이동화면**:
  - `참여 현황` → `HM-02` 공구별 참여 현황
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `받는 사람 고르기` → 「N명에게 보냅니다」 숫자가 바뀐다
  - `문구 틀 고르기` → 아래 미리보기 글이 그 틀로 바뀐다
  - `문구 입력` → 치는 대로 미리보기와 글자 수가 따라 바뀐다
- **생성 프롬프트**:

  ```
  진행자가 참여자에게 공지·알림을 보내는 화면을 만들어줘. 대상 선택 — 어떤 공구의 누구에게(전체 참여자 / 미결제 관심자 / 특정 상태), 공지 유형 템플릿(마감 임박 독려, 성사 축하, 배송 지연 사과, 감사 인사), 메시지 편집(닉네임·상품명 치환 변수 삽입), 채널 선택(앱 알림·카카오·문자)과 예상 도달 수·비용. 우측에 미리보기, 하단에 "지금 보내기 / 예약 발송". 아래에 지난 공지 발송 이력(일시·대상·도달·유형)을 표로 보여줘.
  ```

### SE-01 · 정산 내역
- **소속 메뉴**: 정산(진행자)
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-12-09 ~ 2026-12-11
- **기능정의**: 정산 요약(정산 예정·정산 완료·보류 금액) · 공구별 정산 표(매출·수수료·환불·순정산액·상태) · 정산 주기·예정일 안내 · 기간 필터 · 정산 상세로 이동 · 세금계산서·명세서 · 정산 계좌 설정 링크 · 내보내기
- **버튼 → 이동화면**:
  - `정산 상세` → `SE-03` 정산 상세
  - `정산 계좌 설정` → `SE-02` 정산 계좌·세금 설정
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `기간 고르기` → 표와 위의 합계 금액이 그 기간 것으로 다시 계산된다
  - `표 머리글(날짜·금액)` → 그 기준으로 정렬되고 화살표가 바뀐다
  - `정산 줄 펼치기` → 건별 내역과 수수료 계산이 펼쳐진다
- **생성 프롬프트**:

  ```
  진행자 정산 내역 화면을 만들어줘. 상단에 요약 카드(정산 예정 금액, 이번 달 정산 완료, 보류 중). 공구별 정산 테이블 — 공구명, 성사 매출, 플랫폼 수수료, 환불액, 순 정산액, 상태(정산 예정·완료·보류), 정산 예정일. 기간 필터와 "내보내기". "정산은 성사·배송 완료 후 X일에 등록 계좌로 입금돼요" 정산 주기 안내, 각 행 클릭 시 정산 상세, 우상단 "정산 계좌 설정" 링크와 "세금계산서 발행".
  ```

### SE-02 · 정산 계좌·세금 설정
- **소속 메뉴**: 정산(진행자)
- **뒤로가기**: 좌측 상단에 `‹ 정산 내역` — 누르면 `SE-01`로 간다
- **일정**: 2026-12-14 ~ 2026-12-16
- **기능정의**: 정산 받을 계좌 등록(은행·계좌·예금주 인증) · 사업자/개인 구분과 사업자등록증 · 세금 유형(과세·간이·면세) · 원천징수·부가세 안내 · 세금계산서 발행 정보 · 정산 주기 선택 · 저장 · 민감정보 보안 안내
- **버튼 → 이동화면**:
  - `정산 내역` → `SE-01` 정산 내역
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `계좌 정보 입력` → 형식이 맞으면 확인 표시, 틀리면 칸 아래 안내가 뜬다
  - `사업자·개인 고르기` → 아래 입력 칸 묶음이 그에 맞게 바뀐다
- **생성 프롬프트**:

  ```
  진행자의 정산 계좌·세금 설정 화면을 만들어줘. 1) 정산 계좌 — 은행 선택, 계좌번호, 예금주, 계좌 인증(1원 인증 안내) 2) 사업자 정보 — 개인/사업자 구분, 사업자면 사업자등록번호·등록증 업로드 3) 세금 — 과세 유형(과세·간이·면세), 개인이면 원천징수 안내, 부가세 처리, 세금계산서 발행에 필요한 정보 4) 정산 주기 선택(주 단위·월 단위). 민감 정보라 안전하게 보관된다는 안내와 하단 "저장". 실제 인증·입력은 신중하게 다루도록 구성해줘.
  ```

### SE-03 · 정산 상세
- **소속 메뉴**: 정산(진행자)
- **뒤로가기**: 좌측 상단에 `‹ 정산 내역` — 누르면 `SE-01`로 간다
- **일정**: 2026-12-17 ~ 2026-12-21
- **기능정의**: 선택 공구 정산 상세 내역 · 매출 구성(참여 건별 결제액 합계) · 차감 항목(수수료·환불·배송비·쿠폰 분담) · 최종 순정산액 · 정산 상태 타임라인 · 세금계산서·거래명세서 다운로드 · 정산 문의 · 이전/다음 정산 이동
- **버튼 → 이동화면**:
  - `정산 내역` → `SE-01` 정산 내역
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `항목 펼치기` → 건별 참여자와 금액이 펼쳐진다
  - `「수수료 계산 보기」` → 어떻게 계산됐는지 식이 펼쳐진다
- **생성 프롬프트**:

  ```
  특정 공구의 정산 상세 화면을 만들어줘. 상단에 공구명과 정산 상태 배지. 매출 구성 — 참여 건수와 결제액 합계. 차감 내역 표 — 플랫폼 수수료, 환불된 금액, 배송비 정산, 쿠폰·적립금 분담 등 항목별로, 맨 아래 "최종 순 정산액"을 크게. 정산 상태 타임라인(정산 확정 → 세금계산서 발행 → 입금 예정 → 입금 완료). "거래명세서·세금계산서 다운로드" 버튼과 "정산 문의" 링크, 이전/다음 공구 정산으로 넘어가는 이동도 넣어줘.
  ```

### CS-01 · 이용 안내·자주 묻는 질문
- **소속 메뉴**: 고객지원
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-12-22 ~ 2026-12-24
- **기능정의**: 공동구매 개념 설명(성사·불발·조건부 결제) · 참여자용 3단계 이용법 · 진행자용 이용법 · 카테고리별 FAQ(참여·결제·성사/환불·배송·진행자·정산) · 검색 · 인기 질문 · 해결 안 되면 1:1 문의 유도 · 용어 설명
- **버튼 → 이동화면**:
  - `1:1 문의` → `CS-02` 1:1 문의
  - `공구 둘러보기` → `HO-02` 공구 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `분류 탭` → 질문 목록이 그 분류 것으로 바뀐다
  - `질문 줄` → 답이 펼쳐지고 다시 누르면 접힌다
  - `검색` → 치는 대로 목록이 걸러진다
- **생성 프롬프트**:

  ```
  공구 플랫폼 이용 안내와 FAQ 화면을 만들어줘. 상단에 "공동구매가 처음이신가요?" 개념 설명 — 목표 인원이 차면 성사(할인 확정), 미달이면 자동 전액 환불된다는 핵심을 그림과 함께. 참여자용 3단계, 진행자용 안내를 탭으로 구분. 그 아래 카테고리별 FAQ 아코디언(참여·결제, 성사·환불, 배송, 진행자·정산) 과 검색창, 인기 질문. "그래도 해결이 안 되면 1:1 문의하기" 유도 버튼과 어려운 용어(진행자·성사·단계별 가격) 설명.
  ```

### CS-02 · 1:1 문의
- **소속 메뉴**: 고객지원
- **뒤로가기**: 좌측 상단에 `‹ 이용 안내·자주 묻는 질문` — 누르면 `CS-01`로 간다
- **일정**: 2026-12-25 ~ 2026-12-29
- **기능정의**: 문의 유형 선택(참여·결제·환불·배송·진행자·정산·신고) · 관련 공구·주문 연결 · 내용과 첨부 · 연락처·답변 방식 선택 · 예상 답변 시간 안내 · 내 문의 내역과 상태 · 답변 확인 · 챗봇 우선 안내
- **버튼 → 이동화면**:
  - `이용 안내` → `CS-01` 이용 안내·자주 묻는 질문
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `문의 유형 고르기` → 아래 입력 칸이 그 유형에 맞게 바뀐다
  - `파일 올리기·빼기` → 파일 줄이 늘고 준다
  - `필수 칸이 빈 상태` → 「등록」이 눌리지 않는다
- **생성 프롬프트**:

  ```
  1:1 문의 화면을 만들어줘. 상단에 "먼저 자주 묻는 질문을 확인해보세요" 안내와 링크. 문의 작성 — 유형 선택(참여·결제·환불·배송·진행자/정산·신고), 관련 공구나 주문을 연결(선택), 내용 입력과 파일 첨부, 답변 받을 방식(앱 알림·이메일)과 예상 답변 시간 안내. 하단에 "내 문의 내역" — 각 문의의 상태(접수·처리 중·답변 완료)와 답변 보기.
  ```

### CS-03 · 공지·이벤트
- **소속 메뉴**: 고객지원
- **뒤로가기**: 좌측 상단에 `‹ 이용 안내·자주 묻는 질문` — 누르면 `CS-01`로 간다
- **일정**: 2026-12-30 ~ 2027-01-01
- **기능정의**: 공지사항 목록(중요 공지 상단 고정) · 이벤트·기획전 배너 · 점검·정책 변경 안내 · 카테고리 필터 · 상세 보기 · 이벤트 참여 유도 · 신규 진행자 지원 이벤트
- **버튼 → 이동화면**:
  - `공구 둘러보기` → `HO-02` 공구 목록
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `공지·이벤트 탭` → 목록이 그 탭 것으로 바뀐다
  - `공지 줄` → 본문이 펼쳐지고 다시 누르면 접힌다
- **생성 프롬프트**:

  ```
  공지·이벤트 화면을 만들어줘. 상단에 진행 중 이벤트·기획전 배너 캐러셀, 그 아래 공지사항 리스트 — 중요 공지는 상단 고정(핀 아이콘), 각 공지에 카테고리 뱃지(점검·정책·이벤트·업데이트)와 날짜. "신규 진행자 첫 공구 수수료 면제" 같은 이벤트를 눈에 띄게, 항목 클릭 시 상세. 필터로 유형별로 볼 수 있게.
  ```

### CS-04 · 환불·분쟁 정책 안내
- **소속 메뉴**: 고객지원
- **뒤로가기**: 좌측 상단에 `‹ 이용 안내·자주 묻는 질문` — 누르면 `CS-01`로 간다
- **일정**: 2027-01-04 ~ 2027-01-06
- **기능정의**: 조건부 결제·자동 환불 원칙 정리 · 상황별 환불(미달 불발·마감 전 취소·성사 후 반품·상품 하자) 기준표 · 환불 소요 시간과 수단 · 진행자-참여자 분쟁 처리 절차 · 소비자 보호·에스크로 안내 · 신고 접수 · 관련 문의 링크
- **버튼 → 이동화면**:
  - `1:1 문의` → `CS-02` 1:1 문의
- **눌렀을 때 (이 화면 안에서 끝남)**:
  - `상황별 탭(불발·배송 지연·상품 하자)` → 아래 규정 내용이 그 상황 것으로 바뀐다
  - `「내 경우로 계산해 보기」` → 남은 일수에 따른 환불액이 계산돼 뜬다
- **생성 프롬프트**:

  ```
  환불·분쟁 처리 정책 안내 화면을 만들어줘. 신뢰가 중요한 커머스라 명확해야 해. 상단에 원칙 — "결제는 안전하게 보관되고, 목표 미달 시 전액 자동 환불" 에스크로/소비자 보호 개념. 상황별 환불 기준표 — ① 미달로 불발 ② 마감 전 자유 취소 ③ 성사 후 단순 변심 ④ 상품 하자·오배송, 각각 환불 가능 여부·부담 주체·소요 시간을 표로. 진행자와 참여자 간 분쟁이 생겼을 때의 처리 절차(중재 요청 → 검토 → 결정)와 신고 접수, 하단에 관련 1:1 문의 링크. 딱딱하지 않게, 안심되도록 정리해줘.
  ```

## 5. 화면 이동 흐름 (FLOW)
- `홈`(HO-01) —[공구 둘러보기]→ `공구 목록`(HO-02)
- `홈`(HO-01) —[마감 임박]→ `마감 임박 타임딜`(HO-03)
- `홈`(HO-01) —[로그인]→ `로그인·회원가입`(AC-01)
- `공구 목록`(HO-02) —[공구 상세]→ `공구 상세`(DE-01)
- `공구 목록`(HO-02) —[결과 없음]→ `공구 목록 - 결과 없음`(HO-04)
- `마감 임박 타임딜`(HO-03) —[공구 상세]→ `공구 상세`(DE-01)
- `마감 임박 타임딜`(HO-03) —[전체 공구]→ `공구 목록`(HO-02)
- `공구 목록 - 결과 없음`(HO-04) —[전체 공구 보기]→ `공구 목록`(HO-02)
- `공구 상세`(DE-01) —[참여하기]→ `옵션·수량 선택`(JO-01)
- `공구 상세`(DE-01) —[상품 상세정보]→ `상품 상세정보·거래 조건`(DE-02)
- `공구 상세`(DE-01) —[후기 보기]→ `후기 작성·목록`(RV-01)
- `공구 상세`(DE-01) —[문의하기]→ `상품 문의(Q&A)`(RV-02)
- `상품 상세정보·거래 조건`(DE-02) —[참여하기]→ `옵션·수량 선택`(JO-01)
- `상품 상세정보·거래 조건`(DE-02) —[공구로 돌아가기]→ `공구 상세`(DE-01)
- `공구 마감 - 성사`(DE-03) —[내 공구함]→ `내 공구함`(MY-01)
- `공구 마감 - 성사`(DE-03) —[비슷한 공구]→ `공구 목록`(HO-02)
- `공구 마감 - 불발(미달)`(DE-04) —[비슷한 공구 보기]→ `공구 목록`(HO-02)
- `공구 마감 - 불발(미달)`(DE-04) —[내 공구함]→ `내 공구함`(MY-01)
- `옵션·수량 선택`(JO-01) —[결제하기]→ `참여 결제`(JO-02)
- `옵션·수량 선택`(JO-01) —[관심 공구 담기]→ `관심 공구·알림함`(RV-03)
- `참여 결제`(JO-02) —[참여 완료]→ `참여 완료`(JO-03)
- `참여 결제`(JO-02) —[결제 실패]→ `결제 실패`(JO-04)
- `참여 완료`(JO-03) —[내 공구함]→ `내 공구함`(MY-01)
- `참여 완료`(JO-03) —[공구 더 보기]→ `공구 목록`(HO-02)
- `결제 실패`(JO-04) —[다시 시도]→ `참여 결제`(JO-02)
- `내 공구함`(MY-01) —[참여 상세]→ `참여 상세`(MY-02)
- `내 공구함`(MY-01) —[비어있음]→ `내 공구함 - 비어 있음`(MY-04)
- `참여 상세`(MY-02) —[취소·환불 요청]→ `참여 취소·환불 요청`(MY-03)
- `참여 상세`(MY-02) —[공구 상세]→ `공구 상세`(DE-01)
- `참여 상세`(MY-02) —[후기 쓰기]→ `후기 작성·목록`(RV-01)
- `참여 취소·환불 요청`(MY-03) —[내 공구함]→ `내 공구함`(MY-01)
- `내 공구함 - 비어 있음`(MY-04) —[공구 둘러보기]→ `공구 목록`(HO-02)
- `후기 작성·목록`(RV-01) —[공구 상세]→ `공구 상세`(DE-01)
- `상품 문의(Q&A)`(RV-02) —[공구 상세]→ `공구 상세`(DE-01)
- `관심 공구·알림함`(RV-03) —[공구 상세]→ `공구 상세`(DE-01)
- `관심 공구·알림함`(RV-03) —[알림 설정]→ `알림 설정`(AC-03)
- `로그인·회원가입`(AC-01) —[홈]→ `홈`(HO-01)
- `로그인·회원가입`(AC-01) —[내 정보]→ `내 정보·배송지`(AC-02)
- `내 정보·배송지`(AC-02) —[알림 설정]→ `알림 설정`(AC-03)
- `내 정보·배송지`(AC-02) —[내 공구함]→ `내 공구함`(MY-01)
- `알림 설정`(AC-03) —[내 정보]→ `내 정보·배송지`(AC-02)
- `진행자 시작 안내`(HS-01) —[공구 개설하기]→ `공구 개설 폼`(HS-02)
- `진행자 시작 안내`(HS-01) —[정산 안내]→ `정산 내역`(SE-01)
- `공구 개설 폼`(HS-02) —[미리보기·제출]→ `개설 검수 대기·반려`(HS-03)
- `개설 검수 대기·반려`(HS-03) —[개설 수정]→ `공구 개설 폼`(HS-02)
- `개설 검수 대기·반려`(HS-03) —[진행자 대시보드]→ `진행자 대시보드`(HM-01)
- `진행자 대시보드`(HM-01) —[참여 현황]→ `공구별 참여 현황`(HM-02)
- `진행자 대시보드`(HM-01) —[개설하기]→ `공구 개설 폼`(HS-02)
- `진행자 대시보드`(HM-01) —[정산 내역]→ `정산 내역`(SE-01)
- `공구별 참여 현황`(HM-02) —[마감·성사 처리]→ `마감·성사 처리`(HM-03)
- `공구별 참여 현황`(HM-02) —[참여자 공지]→ `참여자 공지·알림 발송`(HM-05)
- `마감·성사 처리`(HM-03) —[발주·배송]→ `발주·배송 관리`(HM-04)
- `마감·성사 처리`(HM-03) —[진행자 대시보드]→ `진행자 대시보드`(HM-01)
- `발주·배송 관리`(HM-04) —[참여 현황]→ `공구별 참여 현황`(HM-02)
- `발주·배송 관리`(HM-04) —[정산 내역]→ `정산 내역`(SE-01)
- `참여자 공지·알림 발송`(HM-05) —[참여 현황]→ `공구별 참여 현황`(HM-02)
- `정산 내역`(SE-01) —[정산 상세]→ `정산 상세`(SE-03)
- `정산 내역`(SE-01) —[정산 계좌 설정]→ `정산 계좌·세금 설정`(SE-02)
- `정산 계좌·세금 설정`(SE-02) —[정산 내역]→ `정산 내역`(SE-01)
- `정산 상세`(SE-03) —[정산 내역]→ `정산 내역`(SE-01)
- `이용 안내·자주 묻는 질문`(CS-01) —[1:1 문의]→ `1:1 문의`(CS-02)
- `이용 안내·자주 묻는 질문`(CS-01) —[공구 둘러보기]→ `공구 목록`(HO-02)
- `1:1 문의`(CS-02) —[이용 안내]→ `이용 안내·자주 묻는 질문`(CS-01)
- `공지·이벤트`(CS-03) —[공구 둘러보기]→ `공구 목록`(HO-02)
- `환불·분쟁 정책 안내`(CS-04) —[1:1 문의]→ `1:1 문의`(CS-02)

## 6. 빌드 가이드
### 구조
- 각 **화면ID**를 하나의 라우트/페이지로 만드세요 (예: `PCHOME0110` → `/home`).
- 화면명은 페이지 제목·컴포넌트 이름의 기준으로 사용하세요.
- **2장 공통 요소**(헤더·GNB·푸터)를 공용 레이아웃으로 만들고 모든 화면이 감싸도록 하세요.
- **쓰는 사람이 다른 메뉴는 헤더를 나누세요.** 손님이 쓰는 메뉴(홈·탐색·공구 상세·참여·결제·내 공구함)와 운영자가 쓰는 메뉴(공구 개설(진행자)·공구 관리(진행자)·정산(진행자))를 **한 헤더에 나란히 놓지 마세요.** 둘은 사실상 다른 사이트입니다.
  · 손님 영역과 운영자 영역을 각각 다른 레이아웃(헤더·사이드바)으로 만들고, 오갈 때는 **따로 마련한 진입점** 하나로만 이동하세요(예: 우측 상단 `공구 개설 화면으로`, 운영자 화면에서는 `고객 화면 보기`).
  · 운영자 영역은 보통 좌측 사이드바 + 상단 바가 맞습니다. 손님 영역의 검색·카테고리 내비를 그대로 쓰지 마세요.
- **버튼 → 이동화면** 관계를 링크/네비게이션으로 연결하세요.
- **4장 각 화면의 `눌렀을 때 (이 화면 안에서 끝남)` 항목을 그대로 만드세요.** 적혀 있는 것은 빠짐없이 실제로 동작해야 합니다 — 누르면 값이 바뀌고, 다시 누르면 되돌아옵니다.
  · 그 항목에 없더라도 **누를 수 있게 생긴 것은 전부 눌리게** 만드세요. 적힌 것은 최소한이지 전부가 아닙니다.
- **누를 수 있게 보이는 것은 전부 눌리게 만드세요.** 두 종류로 나눠 처리합니다.
  · **화면 안에서 끝나는 조작은 진짜로 동작해야 합니다** — 탭 전환, 좌측 카테고리·필터 선택, 목록에서 항목 고르기(디자이너·옵션·시간), 펼치기·접기, 캘린더 이전/다음 달, 수량 ±, 체크·토글. 자바스크립트로 실제 상태를 바꾸고 화면을 다시 그리세요. 이걸 안내 문구로 때우지 마세요.
  · **서버가 있어야 하는 것만** 짧은 안내(토스트)로 대신합니다 — 저장·삭제·발송·결제·전화 걸기·이의 신청 등.
  · **이런 것은 토스트로 때우지 마세요** — `모두 보기`(펼치기), `선택`·`적용`(고른 상태로 바꾸기), `추가`(줄 하나 늘리기), `닫기`·`✕`(정말 닫기), `도움이 됐어요`(숫자 올리기), 탭·필터·정렬. 전부 화면 안에서 끝나는 일이라 실제로 되어야 합니다.
  · **모달·팝업은 뜨는 것만으로 끝이 아닙니다.** `취소`와 `✕`는 정말 닫히고, `확인`은 눌렀을 때 무엇이 달라지는지 보여야 합니다(다음 화면으로 가거나, 목록이 바뀌거나, 안내가 뜨거나). 닫히지 않는 팝업은 갇힌 화면이 됩니다.
  · 아무 것도 하지 않는 `<button>`을 남기지 마세요. 만들고 나서 **버튼을 하나씩 눌러 보고**, 반응이 없는 것이 있으면 위 둘 중 하나로 채우세요.
- **뒤로가기는 화면마다 정해져 있습니다. 4장 각 화면 스펙의 `뒤로가기` 항목을 그대로 만드세요.** 27개 화면에 붙고, 나머지 10개(메뉴 첫 화면)에는 없습니다. 좌측 상단에 `‹ 상위 화면 이름`과 현재 위치(예: `매장 탐색 › 매장 상세`)를 둡니다.
  · 여러 단계짜리 흐름(1→2→3)은 **이전 단계로 돌아갈 수 있게** 하세요. 되돌아갔을 때 앞서 고른 값이 남아 있어야 합니다.
  · **한 묶음으로 나란히 보여주는 것은 동일 계위입니다.** 탭 줄에 걸린 화면들, 같은 사이드바에 나란히 올라온 화면들이 그렇습니다. 그 안을 오가는 길이 이미 탭·사이드바이므로 뒤로가기를 겹쳐 두지 마세요 — 같은 줄에 있는 화면인데 어떤 건 있고 어떤 건 없어집니다.
  · 빈 화면·오류·마감처럼 막다른 화면에는 **다음에 할 일 버튼**을 반드시 하나 두세요(예: `목록으로`, `다시 시도`, `둘러보기`).
  · **전체 화면 목록으로 돌아가는 길**을 모든 화면에 두되, 이름에 목적이 드러나게 하세요(`화면 목록`). 접힌 패널 안에 `화면 정보` 같은 이름으로 넣으면 아무도 못 찾습니다.
  · 다 만든 뒤 **화면을 하나씩 열어 뒤로가기가 있는지 세어 보세요.** 하나라도 빠져 있으면 채웁니다.
### 화면 구현
- 각 화면의 **생성 프롬프트**를 그 화면 구현 지시로 그대로 사용하세요.
- 반복 UI(카드·리스트 행·폼·버튼)는 **공용 컴포넌트**로 만들어 재사용하세요.
- 필요하면 상태 화면(로딩·빈 결과·에러)도 함께 만드세요.
### 디자인
- **디자인 프리셋 파일을 함께 넣었다면 그것이 우선입니다.** 1장의 디자인 컨셉이나 화면별 프롬프트에 다른 색·톤이 적혀 있어도 프리셋을 따르세요. 충돌이 아니라 **프리셋이 정답**입니다.
- 프리셋의 **레이아웃 골격**(히어로·목록·내비·상세를 어떻게 놓을지)을 먼저 잡고 색을 입히세요. 색만 맞추면 어떤 테마로 만들어도 같은 화면이 나옵니다.
- 프리셋이 없다면 1장의 **디자인 컨셉**을 전역 스타일(색·톤·라운드·여백)로 토큰화해 일관되게 적용하세요.
- **글꼴은 프리셋이 없어도 한 벌로 고정하세요.** 기본은 `Paperlogy`이고, 못 불러오면 `Pretendard` → `Noto Sans KR` 순으로 물러납니다. CSS로는 `font-family: Paperlogy, Pretendard, 'Noto Sans KR', sans-serif` 한 줄이면 됩니다.
  · **글꼴은 한 벌만 씁니다.** 제목과 본문에 서로 다른 글꼴을 쓰지 마세요 — 두 벌이 되는 순간 어디에 무엇을 쓸지가 화면마다 갈립니다. 위계는 글꼴이 아니라 **크기와 굵기**로 만듭니다.
  · 굵기는 **400·600·700 셋만**, 크기는 본문 15 · 카드 제목 18 · 섹션 제목 22 · 페이지 제목 32을 기준으로 잡으세요.

#### 읽기 폭 — 글을 한 단으로 좁힐 때

글이 주인공인 자리(상세 본문·안내·약관)는 **최대 760px** 로 좁히고 가운데 정렬합니다.
콘텐츠 영역(위의 최대 폭) **안에서** 좁히는 것입니다 — 바깥 폭을 바꾸는 게 아닙니다.

**왜 좁히나** — 한 줄이 길면 다음 줄 첫 글자를 눈이 못 찾습니다. 1440px 폭에 본문을 꽉 채우면
한 줄에 90자가 넘어가고, 읽는 사람은 두세 줄마다 같은 줄을 다시 읽게 됩니다.

- **이 값 하나만 씁니다.** 화면마다 680·720·760 처럼 다르게 잡지 마세요 — 저희도 그렇게 적혀 있었고,
  같은 팩 안의 상세 화면끼리 본문 폭이 달라 서로 다른 사이트처럼 보였습니다.
- **카드 격자에는 쓰지 않습니다.** 여기는 «글»을 좁히는 값입니다. 카드를 몇 열로 놓을지는 레이아웃이 정합니다.
- 사진·표·코드처럼 넓어야 읽히는 것은 이 폭을 넘겨도 됩니다. 좁히는 건 글줄입니다.

#### 격자 — 카드를 늘어놓는 자리

카드·타일을 격자로 늘어놓을 때 **좌우와 위아래를 다른 값으로** 벌립니다.

| 자리 | 값 |
| --- | --- |
| **카드와 카드 사이** (좌우) | 위 눈금의 「카드와 카드 사이 (가로)」 |
| **줄과 줄 사이** (위아래) | 위 눈금의 「덩어리와 덩어리 사이」 |

**같은 값을 쓰면 줄이 안 갈립니다.** 카드에 상자가 있을 때는 테두리가 경계를 만들어 줬지만,
상자를 걷어낸 뒤로는 **위 카드의 가격과 아래 카드의 사진이 붙어** 한 덩어리로 읽힙니다.
위아래를 더 벌려야 "여기까지가 한 줄"이 보입니다.

| 나누는 것 | 정하는 곳 |
| --- | --- |
| 몇 열로 나눌지 | 레이아웃 프리셋 |
| **사이를 얼마나 벌릴지** | **위 간격 눈금 (좌우·위아래 따로)** |
| 카드가 어떻게 생겼는지 | 카드 모양 |

- **레이아웃 프리셋이 「넓게」라고 해도 값을 새로 만들지 마세요.** 레이아웃은 *몇 열로 무엇을 어디에*를
  말하는 층이고, *얼마나 벌릴지*는 이 눈금이 정합니다. 실제로 그 한 줄 때문에 40px을 쓴 격자가 있었고,
  같은 화면의 다른 격자는 16px이라 두 값이 나란히 놓였습니다.

**간격은 변수로 못 박고, 자리마다 그것만 씁니다.** CSS 맨 위에 이걸 그대로 붙여 넣으세요.

```css
:root{
  --wrap: 1440px;      /* 콘텐츠 폭 — 헤더·본문·푸터·고정바가 모두 이 값을 쓴다 */
  --pad-x: 24px;    /* 좌우 여백 */
  --card-gap:   16px;     /* 카드와 카드 사이 — 좌우 */
  --card-gap-y: 28px;     /* 카드 줄과 줄 사이 — 위아래 */
  --side-w:     260px;               /* 사이드바 폭 (사이드바 뼈대에서만) */
  --side-gap:   32px;                /* 사이드바와 본문 사이 */
  --row-gap:    14px;                /* 가로 행 카드 안: 썸네일과 글자 사이 */

  --card-pad:   24px;                /* 카드 안쪽 여백 */
  --row-h:      48px;                /* 표 한 줄 높이 — 안 정하면 줄마다 들쭉날쭉해진다 */
  --btn-gap:    8px;                 /* 나란히 놓은 버튼 사이 */
}
@media (max-width: 720px){
  :root{ --pad-x: 16px; --card-gap: 12px; }
}
.wrap { max-width: var(--wrap); margin-inline: auto; padding-inline: var(--pad-x); }
.cards{ display: grid; column-gap: var(--card-gap); row-gap: var(--card-gap-y); }

/* 지표 줄 — 화면 맨 위에 숫자 카드를 나란히 놓는 자리.
   카드 격자(.cards)와 다른 물건이므로 .cards 를 쓰지 않는다. 지표는 넷이 기본이다. */
.stats{ display: grid; grid-template-columns: repeat(4, 1fr);
        column-gap: var(--card-gap); row-gap: var(--card-gap-y); }
@media (max-width: 1024px){ .stats{ grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 720px){ .stats{ grid-template-columns: 1fr; } }

/* 모자이크 — 홈 첫 화면에 「큰 1장 + 작은 4장」을 놓는 자리.
   첫 장이 2칸×2줄, 나머지 넷이 옆을 채운다.
   어느 것을 크게 할지 class 로 정하지 않는다 — 맨 앞에 둔 것이 큰 것이다. */
.mosaic{ display: grid; grid-template-columns: repeat(4, 1fr);
         column-gap: var(--card-gap); row-gap: var(--card-gap-y); }
.mosaic > :first-child{ grid-column: span 2; grid-row: span 2;
                        display: flex; flex-direction: column; }
/* 큰 칸의 사진은 카드 비율을 따르지 않는다 — 두 줄 높이와 안 맞아 제목이 밀려난다.
   큰 칸에서는 사진이 남는 자리를 채운다. */
.mosaic > :first-child > .thumb{ aspect-ratio: auto; flex: 1; min-height: 0; }
@media (max-width: 1024px){
  .mosaic{ grid-template-columns: repeat(2, 1fr); }
  .mosaic > :first-child{ grid-row: auto; }   /* 2열에서 세로로 겹치면 빈칸이 생긴다 */
}
@media (max-width: 720px){
  .mosaic{ grid-template-columns: 1fr; }
  .mosaic > :first-child{ grid-column: auto; }
}

/* 상세 화면의 대표 사진 — 카드 비율을 그대로 쓰지 않는다.
   4:3 을 본문 폭 1392px 에 적용하면 세로가 1044px 이 되어 노트북 화면을 통째로 덮는다.
   가로로 눕히고(16:9) 세로를 420px 로 막는다 — 사진 아래 내용이 같이 보이는 선이다. */
.detail-img{ aspect-ratio: 16 / 9; max-height: 420px; width: 100%;
             border-radius: 12px; overflow: hidden; }
.detail-img > img{ width: 100%; height: 100%; object-fit: cover; display: block; }

/* 간격은 반드시 이 변수로 쓴다. gap:16px 처럼 숫자를 직접 적으면 자리마다 값이 갈린다. */
```

- **`--card-pad`·`--row-h`·`--btn-gap` 도 꼭 쓰세요.** 카드 안쪽 여백, 표 한 줄 높이,
  버튼 사이 간격입니다. 이 셋을 안 정해주면 화면마다 다르게 나옵니다 — 저희도 겪었습니다.
- **`.stats`·`.mosaic`·`.detail-img` 는 카드 격자와 다른 자리입니다.** 지표 숫자 줄,
  홈의 큰 사진 한 장 + 작은 넷, 상세 화면의 대표 사진. 카드 규칙을 여기 갖다 쓰면
  사진이 화면을 다 덮습니다.

- **격자에 `gap: 20px` 처럼 숫자를 직접 쓰지 마세요.** 한 번 쓰는 순간 그 자리만 달라지고,
  나중에 값을 바꿀 때 그 자리만 안 바뀝니다. 실제로 한 사이트에서 좌우가 3·8·16·20·24px
  다섯 가지로 갈라진 적이 있습니다 — 전부 `gap:` 에 숫자를 적어서 생긴 일입니다.
- **`gap` 한 줄로 합치지도 마세요.** 좌우와 위아래는 값이 다르므로 `column-gap`·`row-gap` 둘로 씁니다.
- **이름도 이대로 씁니다.** `--grid-x`·`--gap-col` 처럼 제 나름의 이름을 지으면, 나중에
  다른 사람이 볼 때 그게 같은 자리인지 알 수가 없습니다. 실제로 같은 규칙을 준 두 사이트가
  하나는 `--grid-x/--grid-y`, 하나는 `--gap-col/--gap-row` 를 써서 값이 따로 놀았습니다.
- 값을 바꾸고 싶으면 **`:root` 의 두 줄만** 고칩니다. 그러면 화면 전체가 한 번에 따라옵니다.
- 히어로의 좌우 2단처럼 **카드 격자가 아닌 자리**는 이 규칙 밖입니다. 거기는 화면 뼈대라 레이아웃이 정합니다.

#### 박스형 콘텐츠 — 몇 개를 늘어놓고 사진은 어떤 비율로

사진이 든 카드를 격자로 늘어놓을 때, **몇 개를 놓느냐가 사진 비율을 정합니다.**

| 한 줄에 | 사진 비율 | 왜 |
| --- | --- | --- |
| **2개** | **4:3** | 칸이 넓으니 가로로 넓은 사진 |
| **3개** | **4:3** | 위와 같음 |
| **4개** | **1:1** | 칸이 좁다. 정사각이 가장 얌전하다 |

4개짜리를 세로로 긴 3:4로 뒀더니 **카드 한 줄이 화면을 다 먹었습니다.** 칸이 좁을수록 세로로
길게 두고 싶어지지만, 그러면 한 줄만 보이고 다음 단락이 안 보입니다. 좁은 칸에는 정사각입니다.

- **한 줄에 하나만 두지 않습니다.** 마지막 줄에도 하나만 남기지 마세요 —
  **항목 수를 열 수에 맞춰** 3열이면 3·6·9개, 4열이면 4·8개로 둡니다.
- **한 사이트에서 카드 사진 비율은 하나입니다.** 위 표의 짝(2·3열↔4:3, 4열↔1:1) 밖의 비율은 만들지 마세요.
  같은 홈에 세로 카드와 16:9가 섞여 있어서 **사는 사람이 사진을 두 벌 준비해야 했던** 적이 있습니다.
- **배너·차트·영상·프로필은 이 규칙 밖입니다.** 가로로 긴 배너·영상(16:9)·프로필(1:1)은 제 나름의 비율이 있습니다.
- 상품 사진처럼 **정사각이 업계 표준인 분야**는 1:1을 써도 됩니다. 다만 그때도 한 사이트 안에서는 하나로 갑니다.
- **가로 배너 한 장으로 한 단락을 채우지 마세요.** 배너는 위 표 밖이지만 "한 줄에 하나만 두지 않는다"는 그대로 걸립니다.
  배너 한 장이 있던 자리는 **두 장으로 나누고** 4:3으로 맞추세요.
- **가로로 넘기는 줄(캐러셀)도 몇 개 보이느냐로 셉니다.** 뒤에 더 있어도 눈에 4개가 보이면 4개 — 비율도 1:1입니다.
  카드 폭을 `260px`처럼 박아 두면 화면 폭이 바뀔 때 3.4개씩 어정쩡하게 걸치니, **칸 수로 나눠서** 정하세요.
- **가로로 넘기는 줄에는 좌우 화살표를 두고, 아래 스크롤바는 감춥니다.** 넘길 게 있다는 건
  화살표가 알려 줍니다. 스크롤바까지 나오면 줄 밑에 회색 막대가 하나 더 생겨 지저분합니다.

```css
.carousel{ scrollbar-width:none; -ms-overflow-style:none; }
.carousel::-webkit-scrollbar{ display:none; }
```

  화살표가 **없는데** 스크롤바까지 감추면 넘길 게 있다는 걸 아무도 모릅니다. 둘은 한 벌입니다.

#### 분리형과 혼합형 — 글자를 사진 아래 두느냐 위에 얹느냐

박스형 콘텐츠는 두 가지입니다. **어느 쪽이든 위의 개수·비율 규칙은 똑같이 적용됩니다.**

| | **분리형** | **혼합형** |
| --- | --- | --- |
| 생김새 | 사진 아래에 글자 | 사진 위에 글자를 얹음 |
| 담을 수 있는 것 | 제목·설명·평점·가격까지 여러 줄 | 제목 + 한 줄 정도 |
| 사진이 하는 일 | 무엇인지 알려 준다 | 분위기를 만든다 |
| 쓰는 자리 | 상품·강의 목록처럼 **고르는** 자리 | 특집·기획전처럼 **끌어당기는** 자리 |

**분리형은 상자를 두르지 마세요.** 흰 배경에 테두리·그림자까지 두르면, 사진과 글자가 이미 한 덩어리인데
선을 한 번 더 치는 셈입니다. 격자로 늘어놓으면 그 선이 줄줄이 겹쳐 화면이 답답해집니다.
**모서리는 사진이 가집니다** — 상자를 없앤 자리에서 사진이 카드 노릇을 합니다.
(혼합형은 사진이 곧 배경이라 이 규칙 밖입니다.)

```css
/* 사진 + 글자로 된 카드 — 상자를 두르지 않는다 */
.card{
  background: none;   /* surface 나 흰색을 깔지 않는다 */
  border: 0;
  box-shadow: none;
}
.card .thumb{ border-radius: 12px; }  /* 모서리는 사진이 가진다 */
.card .body{ padding: 16px 0 0; }     /* 좌우 여백 0 — 상자가 없으니 안쪽 여백도 없다 */
```

**`background: var(--surface)` 나 `border: 1px solid` 를 카드에 쓰면 이 규칙을 어긴 것입니다.**
사진이 없는 상자(설정 패널·안내 상자·표를 감싼 것)는 이 규칙 밖입니다 — 거기는 배경이 있어야
덩어리로 읽힙니다. **사진과 글자로 된 카드**만 걷어냅니다.

상자를 걷어내면 두 가지가 따라옵니다.

- **글자가 흰색이 아니라 페이지 배경 위**에 놓입니다. 대비를 다시 재세요.
- **카드끼리 갈라 주던 테두리가 없어집니다.** 줄과 줄 사이를 더 벌려 그 몫을 대신하세요.

그리고 **카드 안에는 선을 긋지 마세요.** 가격 위에 선을 두면 상자가 없는 상태에서는 그 선이 카드를
가르는 것처럼 보여, 사진·제목과 가격이 따로 노는 두 덩어리로 읽힙니다.
**갈라야 할 것은 카드 안이 아니라 카드끼리**입니다.

혼합형을 쓸 때 반드시 지킬 것:

- **어둠막을 까세요.** 사진 위에 흰 글자를 그냥 얹으면 사진의 밝은 자리에서 글자가 사라집니다.
  **사진은 사는 분이 바꿀 것**이라 우리가 밝기를 미리 알 수 없습니다.
  글자 쪽에서 반대편으로 옅어지는 어둠막을 항상 깔아 두면 어떤 사진이 와도 읽힙니다.
- **글자 대비 4.5 이상**을 지키세요. 가장 밝은 사진(흰색)이 왔다고 치고 재세요.
- **글자를 짧게 쓰세요.** 제목 한 줄 + 설명 한 줄까지입니다. 그보다 길면 사진을 다 덮어 혼합형인 의미가 없습니다.
- **가격·평점처럼 정확히 읽어야 하는 것은 얹지 마세요.** 사진 위에서는 숫자가 잘 안 읽힙니다.
- **한 화면에 두 가지를 섞어도 됩니다.** 위는 혼합형 특집, 아래는 분리형 목록 — 하는 일이 다르므로 어수선하지 않습니다.

#### 글꼴은 한 벌입니다

**기본은 `Paperlogy`**, 못 불러오면 `Pretendard` → `Noto Sans KR` 순으로 물러납니다.
CSS로는 `font-family: Paperlogy, Pretendard, 'Noto Sans KR', sans-serif` 한 줄입니다.

- **제목과 본문에 다른 글꼴을 쓰지 마세요.** 두 벌이 되는 순간 어디에 무엇을 쓸지가
  화면마다 갈립니다. 처음엔 「제목만 다르게」로 시작해서, 나중엔 세 벌 네 벌이 됩니다.
- **위계는 글꼴이 아니라 크기와 굵기로 만듭니다.** 굵기는 400·600·700 셋만 쓰세요.
- 크기는 본문 15 · 카드 제목 18 · 섹션 제목 22 · 페이지 제목 32이 기준입니다.
- **숫자가 많은 표에는 고정폭 숫자**(`font-variant-numeric: tabular-nums`)를 켜세요.
  안 켜면 값이 바뀔 때 자릿수가 흔들려 칸이 들썩입니다.

#### 움직임 — 바뀌었다는 걸 눈이 알아채게

값이 바뀌었는데 **딱 하고 갈아 끼우면 사람은 그걸 못 봅니다.** 숫자가 12에서 3으로
바뀌어도, 눈이 그 자리를 안 보고 있었으면 바뀐 줄 모릅니다.
움직임은 꾸미개가 아니라 **「방금 여기가 바뀌었어요」라고 알려 주는 장치**입니다.

| 언제 | 얼마나 | 무엇이 |
| --- | --- | --- |
| 마우스를 올렸을 때 | **0.12초** | 색·테두리 |
| 눌러서 값이 바뀔 때 | **0.2초** | 사라짐·나타남·자리 옮김 |
| 접히고 펴질 때 | **0.25초** | 높이 |
| 화면 위에 뜰 때 (모달·시트) | **0.25초** | 옅어짐 + 살짝 올라옴 |
| 안내 문구(토스트) | **0.2초 등장 · 3초 머무름** | 옅어짐 |

- **가속은 하나로 씁니다.** `ease-out`(빨리 시작해 부드럽게 멈춤)이 기본입니다.
  자리마다 다른 가속을 쓰면 같은 사이트에서 두 사람이 만든 것처럼 보입니다.
- **0.3초를 넘기지 마세요.** 그보다 길면 기다리는 느낌이 듭니다. 목록이 다시 그려질 때는
  **0.15초**면 충분합니다.
- **자리를 밀어내며 움직이지 마세요.** 아래 내용이 출렁이면 읽던 곳을 잃습니다.
  옅어지거나 제자리에서 바뀌게 하세요.
- **한 번에 한 곳만 움직입니다.** 눌렀는데 화면 여기저기가 같이 움직이면 어디가 바뀐 건지 모릅니다.
- **끝없이 도는 움직임은 두지 않습니다.** 배너 자동 넘김·깜빡임은 읽는 걸 방해합니다.
  기다리는 동안 도는 것(로딩)만 예외입니다.
- **움직임을 꺼 둔 분을 존중하세요.** `prefers-reduced-motion` 이 켜져 있으면 전부 0초로 둡니다.
  어지럼증 때문에 꺼 두신 분들이 있습니다. 값은 바뀌되 움직이지만 않으면 됩니다.

#### 떠 있는 부품과 줄 안에 서는 부품은 다른 것입니다

사진 위에 **떠 있는** 동그란 찜 아이콘과, 버튼 줄 안에 **나란히 서는** 찜 버튼은
생김새가 비슷해 보여도 다른 부품입니다. 떠 있는 쪽은 자리를 빠져나가도록(`position:absolute`)
만들어져 있어서, 그 이름을 줄 안의 버튼에 붙이면 **줄을 빠져나가 엉뚱한 데 가서 앉습니다.**
실제로 찜 버튼이 가격 위에 겹쳐 앉은 적이 있습니다.

- **이름이 자리를 뜻하면 그 자리에서만 쓰세요.** 「사진 위 찜」과 「줄 안 아이콘 버튼」은
  이름을 나눠 두고, 섞어 쓰지 않습니다.
- **부품 이름과 실제 쓰임이 어긋나면 이름을 고치세요.** 「타임라인」이라 지어 놓고
  여섯 곳 모두 한 줄짜리 목록으로 쓰고 있던 적이 있습니다. 세로 점만 남아 글자 사이에 끼었습니다.
- **아이콘만으로 뜻이 통하면 글자를 빼세요.** 하트·공유·닫기가 그렇습니다.
  다만 **`aria-label`로 이름은 남깁니다** — 눈으로 못 보는 분에게는 그게 유일한 이름입니다.
- 반대로 **아이콘만으로 못 알아보는 것에는 글자를 붙이세요.** 「빼기」·「내보내기」처럼
  모양이 여러 뜻으로 읽히는 것이 그렇습니다.

#### 쓰는 화면이 있으면 보여주는 자리도 있어야 합니다

글을 쓰게 해 놓고 그 글이 나올 자리를 안 만드는 일이 잦습니다.
실제로 강사가 **1,240자짜리 강의 소개를 쓰는 화면**은 있는데, 상세 화면에는 그 글이
나올 자리가 없었습니다. 게다가 검수 화면은 "강의 소개 1,240자 — 확인됨"이라고까지 말하고 있었습니다.

- **입력 칸을 만들 때마다 "이건 어디에 보이나"를 한 번 물으세요.** 답이 없으면 둘 중 하나입니다 —
  보여줄 자리를 만들거나, 그 입력 칸을 빼거나.
- **에디터(굵게·목록·이미지 버튼이 붙은 칸)로 쓴 글**은 보여주는 쪽에도 그 모양이 살아야 합니다.
  제목·목록·굵게를 쓸 수 있게 해 놓고 보여줄 때 한 덩어리 문단으로 뭉개면, 쓴 사람이 헛수고합니다.
- **한 줄 요약과 본문 소개는 다른 것입니다.** 목록 카드에 들어가는 한 줄과, 상세에서 읽는 본문은
  자리도 길이도 다릅니다. 하나로 합치지 마세요.

#### 권장 크기는 실제 자리와 같아야 합니다

"권장 1280×720 (16:9)"이라고 적어 두고 옆의 자리는 4:3인 화면이 있었습니다.
**사는 분은 글자를 믿고 그 크기로 사진을 준비**하는데, 올려 보면 잘립니다.

- 안내 문구의 숫자와 **바로 옆 자리의 비율이 같은지** 확인하세요.
- 검수·확인 화면에 크기가 또 적힌다면 **거기도 같은 숫자**여야 합니다. 세 곳이 따로 놀기 쉽습니다.
- **영상만 16:9입니다.** 썸네일·카드 사진은 위 표(2·3개↔4:3, 4개↔1:1)를 따릅니다.

#### 차트 — 이미지가 아니라 데이터로 그립니다

추이·막대·도넛 같은 차트를 **이미지 자리로 두지 마세요.** `이미지 영역 (라인 차트 · 권장 1200×420)`
이라고 적어 두면 사는 분은 차트를 **그림 파일로 만들어 올려야 하는 줄** 압니다.
차트는 숫자가 바뀌면 따라 바뀌어야 하는 것이라, 사진 자리와 성격이 다릅니다.

- **가벼운 것은 라이브러리 없이 SVG로 그리세요.** 선 하나·막대 열두 개짜리는 30줄이면 됩니다.
  바깥에서 아무것도 안 불러오니 **인터넷 없이 폴더에서 열어도** 그대로 보입니다.

**대시보드를 만든다면 최소한 이 셋은 갖추세요.**

| 무엇 | 어디에 | 왜 |
| --- | --- | --- |
| **큰 추이 차트** | 화면에 한둘 | 기간을 놓고 흐름을 본다 |
| **스파크라인** (작은 추이선) | 지표 카드 안 | 숫자 옆에서 방향만 알려 준다 |
| **증감 표시** (▴ 812) | 지표 카드 안 | 좋아졌는지 나빠졌는지 |

숫자 하나만 두면 **좋은지 나쁜지를 못 말합니다.** "10,374명"만 있으면 늘고 있는지
줄고 있는지 알 수 없어서, 사는 분은 그 카드를 보고 아무 판단도 못 합니다.
지표 카드는 **숫자 + 증감 + 추이선**이 한 벌입니다.

- **본격적인 차트가 필요하면 무료(MIT) 라이브러리를 쓰세요.** 값을 치르지 않아도 되고 상업적으로 써도 됩니다.

| 라이브러리 | 크기 | 언제 |
| --- | --- | --- |
| **Chart.js** | ~200KB | 무난한 첫 선택. 자료가 많아 AI도 정확하게 짭니다 |
| **ApexCharts** | ~500KB | 기본 모양이 예쁘고 종류가 많을 때 |
| **uPlot** | ~40KB | 점이 수천 개라 가벼워야 할 때 |
| **Frappe Charts** | ~30KB | 작고 산뜻한 SVG면 충분할 때 |

- **색은 반드시 프리셋 색을 쓰세요.** 라이브러리 기본 팔레트(빨강·노랑·초록)를 그대로 두면
  그 자리만 다른 사이트처럼 보입니다.
- **눈금선은 옅게, 선은 진하게.** 눈금이 데이터보다 진하면 숫자가 안 읽힙니다.
- 라이브러리를 쓰면 **인터넷이 필요해집니다.** 오프라인으로 열어 볼 화면이라면 파일을 함께 받아 두거나 SVG로 그리세요.

#### 가격 표기 — 낼 돈부터

**판매가 · 정가 · 할인율** 순서로 한 줄에 세웁니다.

| 자리 | 모양 | 크기 |
| --- | --- | --- |
| **판매가** (낼 돈) | 진한 기본 글자색, 굵게 | 18 (상세는 28) |
| **정가** | 회색 + 취소선 | 13 |
| **할인율** | **강조색 글자**, 굵게 | 14 |

- **할인율을 앞에 두지 마세요.** 눈이 먼저 "20%"에 걸려 정작 얼마인지를 뒤늦게 찾습니다.
  사는 분이 알고 싶은 건 **낼 돈**입니다.
- **할인율은 배지가 아니라 색 글자입니다.** 배지로 두면 정가·판매가와 무게가 비슷해져 셋이 서로 다툽니다.
  배지 자리는 `입문`·`NEW` 같은 **꼬리표 몫**입니다.
- **셋의 크기를 벌려 두세요.** 비슷하면 무엇을 먼저 읽어야 할지 눈이 헤맵니다.
- **강조색 원색을 글자로 쓰지 마세요.** 주황·코랄 원색은 흰 배경에서 3:1을 못 넘깁니다.
  글자용으로 한 단계 어두운 색을 따로 두세요.
- 가격 묶음은 **한 곳에서 만들어 쓰세요.** 자리마다 손으로 짜면 순서가 갈립니다.

#### 버튼이냐 링크냐

**다른 화면으로 데려가면 링크, 이 화면 안에서 끝나면 버튼입니다.**

| 하는 일 | 무엇으로 | 눌렀을 때 |
| --- | --- | --- |
| 다른 화면으로 간다 | 링크 `<a href>` | 그 화면이 열린다 |
| 이 화면 안에서 끝난다 | 버튼 `<button>` | 안내 문구가 뜬다 |

- 필터 적용·정렬·검색·다음 차시·시간 지점 이동은 **화면이 바뀌지 않는 조작**입니다.
  링크로 만들면 **자기 자신으로 가는 링크**가 되어, 눌러도 같은 화면이 다시 열립니다.
  사는 분 눈에는 *"눌렀는데 아무 일도 안 나네"* 로 보입니다.
- **나란히 선 버튼끼리는 종류를 섞지 마세요.** 하나는 화면이 넘어가고 하나는 안 넘어가면
  규칙이 없어 보입니다.

**그리고 화면 안에서 끝나는 조작은 진짜로 값이 바뀌어야 합니다.**
눌러도 숫자가 그대로면, 사는 분 눈에는 **눌리지 않는 포스터**입니다.

| 눌렀을 때 | 바뀌어야 하는 것 |
| --- | --- |
| 기간 탭 (7일·30일·90일) | 차트와 그 옆 요약 숫자 |
| 쿠폰·옵션 고르기 | 결제 금액 줄과 합계 |
| 수량 늘리기 | 금액 |
| 정렬·필터 | 목록 순서나 개수 |

- **미리 만들어 두고 바꿔 끼세요.** 차트 셋을 미리 그려 두고 탭에 따라 보여 주는 편이,
  누를 때마다 자바스크립트로 다시 그리는 것보다 낫습니다 — 그리는 규칙이 두 곳으로 갈라지지 않습니다.
- **계산은 한 곳에서만 하세요.** 합계를 화면마다 손으로 적어 두면 쿠폰을 바꿨을 때 한 군데만 바뀝니다.
- **탭에 갈 곳도 바꿀 것도 없으면 탭이 아닙니다.** 그냥 제목이거나, 아직 안 만든 자리입니다.

**같은 길을 두 번 두지 마세요 — 위 카테고리 줄과 왼쪽 LNB.**
둘이 같은 곳을 가리키면 위쪽이 두 줄로 두꺼워지고, 어느 쪽을 눌러야 할지 잠깐 멈칫합니다.

| 상태 | 왼쪽 LNB | 위 카테고리 줄 |
| --- | --- | --- |
| 맨 위 (LNB 보임) | 보인다 | **접힌다** |
| 스크롤해서 LNB가 위로 사라짐 | 안 보인다 | **내려온다** |
| LNB가 아예 없는 화면 | — | **늘 보인다** |

- **LNB가 있는 화면은 접힌 채로 시작하세요.** 펼쳐 두었다가 나중에 접으면 첫 화면에서 한 번 번쩍입니다.
- **LNB가 없는 화면에서는 절대 접지 마세요.** 접으면 갈 길이 통째로 사라집니다.
- **기준은 "LNB가 붙박이 머리말 아래로 사라지는 순간"입니다.** 머리말 높이만큼 위를 깎아서 재세요.
- 같은 규칙을 **LNB를 쓰는 모든 레이아웃에 똑같이** 거세요. 화면마다 다르면 위쪽 높이가 제멋대로 바뀌어 보입니다.

- **견본으로 가는 버튼을 화면 안에 두지 마세요.** `결과가 없을 때` `대화가 없을 때는 이렇게 보여요`
  처럼 예외 화면을 보여주려고 넣은 버튼·단락은 진짜 사이트에 없는 것입니다.
  검색창 옆에 그런 버튼이 서 있으면 사는 분은 그게 기능인 줄 압니다.
  빈 화면·오류 화면은 **화면 목록에 따로 한 장**으로 두세요 — 거기서 눌러 보면 됩니다.

#### 화면 목록으로 가는 길은 한 곳에만

만든 화면을 훑어보려면 목록이 필요합니다. 그런데 그 목록을 **화면 안에 늘어놓으면**
사는 분 눈에는 사이트의 일부로 보입니다.

```
이렇게 두지 마세요 — 화면 위쪽에 줄줄이
  [CO-01 · 강의 목록] [CO-02 · 검색 결과 없음] [CO-03 · 강의 상세] [CO-04 · 결제] …
```

**진짜 사이트에는 이런 줄이 없습니다.** `CO-02 · 검색 결과 없음` 은 만드는 사람의 말이지
쓰는 사람의 말이 아닙니다. 화면 ID 가 손님 눈에 보이는 것부터가 어긋납니다.

- **한 귀퉁이에 작게 하나만 둡니다.** 오른쪽 아래에 떠 있는 `화면 목록` 단추 하나면 됩니다.
  누르면 `index.html` 로 갑니다.
- **그 단추는 화면 내용 위에 떠 있게** 두고, 본문 흐름에는 끼워 넣지 않습니다.
  본문에 들어가면 그것도 사이트의 일부가 됩니다.
- **화면 ID 를 손님이 보는 자리에 쓰지 않습니다.** 제목·탭·빵부스러기 어디에도요.
  ID 는 우리가 화면을 세는 이름이지 서비스의 말이 아닙니다.
- 화면끼리 오가는 길은 **그 서비스의 진짜 메뉴와 버튼**으로 만듭니다.
  「강의 목록 → 강의 상세」는 강의 카드를 눌러서 가지, 탭으로 가지 않습니다.

### 이미지 · 썸네일
- 아직 사진이 없으므로 이미지 자리는 **테마 색이 아니라 옅은 파스텔**로 채우세요. 테마 색으로 칠하면 화면이 그 색 덩어리로 뒤덮여 디자인이 안 보입니다.
- 배경 `#EEF2F7` · `#F2EFF7` · `#EDF4F1` · `#F7F1EC` · `#F2F4F7` 중 하나를 카드마다 돌려 쓰고, 테두리 `#E3E8F0` 1px, 글자색 `#8B94A6` 13px.
- 자리 안에 `이미지 영역 (무엇 · 권장 가로×세로)` 형식으로 적으세요. 예: `이미지 영역 (매장 대표 · 권장 1200×900)` · `이미지 영역 (프로필 · 권장 400×400)` · `이미지 영역 (배너 · 권장 1600×600)`
- 이미지 자리는 테마 색으로 칠하지 않는다. 옅은 파스텔 한 톤 + 1px 테두리로 둔다.
- 자리 안에 무엇이 들어갈 이미지인지와 권장 크기를 글자로 적는다.
- 적어 둔 크기의 **비율을 실제로 지킨다** — `400×400`이라 적었으면 정사각으로 보여야 한다(aspect-ratio).
- 특히 프로필·썸네일처럼 크기가 고정된 자리는, 가로로 나란히 놓아도 늘어나지 않게 한다. 목록·행 안에 넣을 때 `flex`가 잡아당겨 타원이 되는 일이 잦으니, 만든 뒤 실제로 열어 모양을 확인한다.
- 썸네일처럼 작아서 글자가 안 들어가면 크기만 적거나 아이콘 하나만 둔다.
- 실제 사진을 넣기 전까지 이 규칙을 유지한다 — 그래야 어디에 무엇을 넣어야 하는지 보인다.
### 파일을 어디에 두나

```
완성화면/
  index.html              전체 화면 목록 (여기서 화면으로 들어간다)
  pages/
    HO-01.html            화면 하나에 파일 하나 · 이름은 화면 ID 그대로
    CO-03.html
  assets/
    css/style.css         스타일 한 벌
    js/app.js             동작 한 벌
```

- **`pages/` 안에서는 `../assets/css/style.css` 로 부릅니다.** `assets/...` 라고 쓰면 `pages/assets/...` 를 찾다가 못 찾아서 **스타일이 통째로 안 붙습니다** — 글꼴도 여백도 격자도 전부 날아갑니다.
- **`index.html` 은 `완성화면/` 바로 아래**에 둡니다. 거기서는 `assets/...` 그대로 부르면 됩니다.
- **스타일과 동작은 각각 한 벌**입니다. 화면마다 따로 만들면 44장이 서로 달라집니다.
- 화면끼리 오갈 때는 `CO-03.html` 처럼 **같은 폴더 안 이름**으로 겁니다.
- 다 만든 뒤 **화면 하나를 브라우저로 직접 열어 보세요.** 글꼴이 기본 고딕으로 보이면 경로가 틀린 것입니다.

### 추천 스택
- 정적 다중 페이지(HTML/CSS) 또는 컴포넌트 기반(React·Next.js·Vue 등) 중 프로젝트 규모에 맞게 선택하세요. 화면이 많고 상태가 복잡하면 컴포넌트 기반을 권장합니다.
