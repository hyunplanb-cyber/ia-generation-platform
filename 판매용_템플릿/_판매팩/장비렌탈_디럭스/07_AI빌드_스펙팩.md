# 장비 렌탈·대여 예약 플랫폼 — AI 빌드 스펙팩

> 이 문서 하나로 사이트를 만들 수 있게 정리한 스펙이에요. 화면별 **생성 프롬프트**를 그대로 사용하세요.

## 1. 프로젝트 개요
- **컨셉**: 캠핑·카메라 장비를 날짜를 잡아 빌려주고 돌려받는 렌탈 예약 플랫폼. 빌리는 사람은 원하는 기간에 그 장비가 비어 있는지 달력에서 확인하고, 보증금과 함께 결제한 뒤 방문이나 택배로 받는다. 쓰고 나면 반납하고, 연체·파손이 있으면 정산한 뒤 보증금을 돌려받는다. 대여 신청부터 반납·정산·환급까지 빌리는 사람이 쓰는 화면 전체를 담는다.
- **디자인 컨셉**: 장비 사진이 또렷하게 보이는 밝은 배경, 포인트는 내추럴 그린(#15803D — 배경·배지·큰 글자용, 본문 글자에는 #166534) 하나. 날짜와 재고가 주인공이라 달력과 상태 배지를 가장 크게, 금액은 표로 또렷하게
- **디바이스**: 반응형
- **전체 일정**: 2026-08-03 ~ 2026-10-16

## 2. 공통 요소 (모든 화면 공통)
> 아래 요소는 **한 번만 만들어 모든 화면에서 재사용**하세요. 각 화면 스펙(4장)은 이 공통 요소 위에 올라가는 콘텐츠만 설명합니다.
- **상단 내비게이션(GNB)**: `홈` · `장비` · `장바구니` · `예약·결제` · `수령·반납` · `정산` · `마이페이지` · `고객센터` — 모든 화면 상단에 고정
- **콘텐츠 영역**: 최대 1440px, 좌우 여백 24px — 헤더·본문·푸터·하단 고정 바가 모두 이 폭을 쓴다. 화면마다 따로 정하지 않는다. 화면마다 폭을 새로 정하면 위아래가 어긋납니다.
- **읽기 폭**: 글이 주인공인 자리(상세 본문·안내·약관)는 최대 760px로 좁혀 콘텐츠 영역 **안에서** 가운데 정렬 — 카드 격자에는 쓰지 않습니다. 아래 「읽기 폭」 참고.
- **헤더**: 로고 + 검색 + 상단 내비게이션(GNB)
- **푸터**: 저작권 · 기본 링크
- **재사용 컴포넌트**: 반복되는 UI(상품/항목 **카드**, 리스트 **행**, **폼 필드**, **버튼**)는 공용 컴포넌트로 만들어 화면마다 재사용하세요.

## 3. 메뉴 구조
- **[HO] 홈** (Home)
  - `HO-01` 홈
  - `HO-02` 카테고리 홈
  - `HO-03` 기간으로 찾기 - 결과
  - `HO-04` 결과 없음 - 그 기간에 빈 장비가 없음
  - `HO-05` 기획전·특가
- **[PD] 장비** (Products)
  - `PD-01` 장비 목록
  - `PD-02` 장비 상세
  - `PD-03` 날짜별 가용 재고 달력
  - `PD-04` 옵션·부속품 고르기
  - `PD-05` 대여 불가 - 그 날짜 마감
  - `PD-06` 세트 패키지
- **[CT] 장바구니** (Cart)
  - `CT-01` 대여 장바구니
  - `CT-02` 기간 한꺼번에 바꾸기
  - `CT-03` 빈 장바구니
- **[BK] 예약·결제** (Booking)
  - `BK-01` 대여 신청서
  - `BK-02` 보증금·결제
  - `BK-03` 대여 확정 완료
  - `BK-04` 결제 실패
  - `BK-05` 대여 계약 동의·서명
  - `BK-06` 대여 기간 연장
- **[RT] 수령·반납** (Return)
  - `RT-01` 수령 확인
  - `RT-02` 이용 중
  - `RT-03` 반납 안내
  - `RT-04` 반납 접수
  - `RT-05` 연체 안내
  - `RT-06` 파손·분실 신고
- **[ST] 정산** (Settlement)
  - `ST-01` 점검 진행 중
  - `ST-02` 정산 내역 - 연체·파손 청구
  - `ST-03` 보증금 환급
  - `ST-04` 환급 완료
- **[MY] 마이페이지** (MyPage)
  - `MY-01` 내 대여 내역
  - `MY-02` 대여 상세
  - `MY-03` 후기 쓰기
  - `MY-04` 알림·쿠폰
- **[CS] 고객센터** (Support)
  - `CS-01` 자주 묻는 질문
  - `CS-02` 1:1 문의
  - `CS-03` 대여 약관·규정

## 4. 화면별 스펙
### HO-01 · 홈
- **소속 메뉴**: 홈
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-08-03 ~ 2026-08-06
- **기능정의**: 빌릴 기간을 먼저 고르는 검색바가 얹힌 히어로 배너 · 카테고리 바로가기 8종(텐트·타프·화로대·카메라·렌즈·짐벌·조명·음향) · 이번 주 인기 장비 8개 카드 · 이번 주말 남은 장비 캐러셀(잔여 수량 표시) · 세트로 빌리면 싼 패키지 안내 배너 · 처음이신가요 3단계 안내(고른다-받는다-돌려준다) · 실제 이용 후기 슬라이드 5개 · 보증금·연체료 정책 요약 카드 · 매장 위치와 운영시간 · 하단 카톡 채널 안내
- **버튼 → 이동화면**:
  - `장비 둘러보기` → `PD-01` 장비 목록
  - `기간으로 찾기` → `HO-03` 기간으로 찾기 - 결과
  - `장비 상세` → `PD-02` 장비 상세
  - `패키지 보기` → `PD-06` 세트 패키지
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 검색 — 입력한 말이 든 것만 남깁니다. 없으면 「검색 결과가 없습니다」와 다시 찾는 길을 보여줍니다
  - 수량 — 누를 때마다 **숫자와 합계가 같이** 바뀝니다
- **생성 프롬프트**:

  ```
  장비 렌탈 예약 사이트의 홈 화면을 만들어줘. 스크롤 순서대로 아래 섹션을 위에서 아래로 쌓아줘. 1) 상단 GNB(로고, 캠핑장비, 카메라장비, 패키지, 이용안내, 로그인, 장바구니 아이콘) 2) 히어로 배너 — 장비 사진 위에 "언제 쓰실 건가요?" 기간 선택창(빌리는 날 ~ 돌려주는 날)과 "그 기간에 빌릴 수 있는 것만 보여드려요" 안내. 날짜를 먼저 묻는 것이 이 서비스의 핵심이다 3) 카테고리 바로가기 8종 아이콘 4) 이번 주 인기 장비 8개 카드 — 사진, 장비명, 1일 대여료, 별점, 보증금 배지 5) 이번 주말 남은 장비 가로 캐러셀 — "3대 중 1대 남음"처럼 잔여 수량을 붉게 6) 세트 패키지 안내 배너 — "텐트+타프+화로대 함께 빌리면 22% 싸요" 7) 처음이신가요 — 고른다 → 받는다 → 돌려준다 3단계 그림 설명 8) 실제 이용 후기 슬라이드 5개 9) 보증금·연체료 요약 카드 — "보증금은 반납 확인 후 3영업일 안에 돌려드려요" 10) 매장 위치·운영시간·지도 썸네일 11) 하단 푸터 장비 사진이 또렷하게 보이도록 사진은 크게, 대여료와 보증금은 나란히 읽히게 배치해줘.
  ```

### HO-02 · 카테고리 홈
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈` — 누르면 `HO-01`로 간다
- **일정**: 2026-08-07 ~ 2026-08-11
- **기능정의**: 카테고리 대표 사진과 한 줄 소개 · 이 종류 인기 장비 8개 · 하위 종류 필터 칩 · 이 종류를 처음 빌리는 분을 위한 고르는 법 · 함께 빌리면 좋은 장비 추천 · 이 종류 자주 묻는 질문 4개 · 이 종류 후기 모음 · 보관·사용 주의사항 안내
- **버튼 → 이동화면**:
  - `장비 목록` → `PD-01` 장비 목록
  - `장비 상세` → `PD-02` 장비 상세
  - `이용 안내` → `CS-03` 대여 약관·규정
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 필터 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
- **생성 프롬프트**:

  ```
  특정 카테고리(예: 캠핑 장비) 전용 홈 화면을 만들어줘. 1) 상단에 카테고리 대표 사진과 이름, 한 줄 소개 2) 하위 종류 필터 칩 — 전체 / 텐트 / 타프 / 침낭 / 화로대 / 테이블 3) "이 기간에 빌릴 수 있는 것만 보기" 토글 스위치와 고른 기간 표시 4) 이 종류 인기 장비 8개 카드 그리드 — 사진, 이름, 1일 대여료, 보증금, 잔여 대수 5) 처음 빌리는 분을 위한 고르는 법 — 인원수별 텐트 크기 표 6) 함께 빌리면 좋은 장비 3개 추천 7) 이 종류 자주 묻는 질문 4개 아코디언 8) 보관·사용 주의사항 박스 — 젖은 채로 반납하면 건조비가 붙는다는 안내 사진 카드 그리드가 주인공이고, 잔여 대수는 카드 위에 배지로 얹어줘.
  ```

### HO-03 · 기간으로 찾기 - 결과
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈` — 누르면 `HO-01`로 간다
- **일정**: 2026-08-12 ~ 2026-08-14
- **기능정의**: 검색 조건 요약 바(기간·종류·인원) · 기간 다시 고르기 달력 · 결과 수와 정렬(추천·낮은 대여료·별점·잔여 많은 순) · 필터 패널(대여료 범위·보증금 범위·수령 방법·브랜드) · 장비 카드 목록(사진·이름·1일 대여료·총 대여료·보증금·잔여 대수·별점) · 그 기간에 마감된 장비는 흐리게 뒤로 · 다음 가능한 날짜 안내 · 더 보기 페이지네이션
- **버튼 → 이동화면**:
  - `장비 상세` → `PD-02` 장비 상세
  - `결과 없음` → `HO-04` 결과 없음 - 그 기간에 빈 장비가 없음
  - `재고 달력` → `PD-03` 날짜별 가용 재고 달력
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 필터 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
  - 정렬 — 고른 기준으로 **순서를 실제로 바꿔** 다시 그립니다
  - 검색 — 입력한 말이 든 것만 남깁니다. 없으면 「검색 결과가 없습니다」와 다시 찾는 길을 보여줍니다
  - 더 보기 — 누르면 그 자리에서 펴지고 접힙니다. 다른 화면으로 가지 않습니다
  - 페이지네이션 — 누른 쪽의 목록으로 **내용이 바뀌고**, 지금 쪽이 표시됩니다
- **생성 프롬프트**:

  ```
  기간을 골라 장비를 찾은 결과 화면을 만들어줘. 1) 상단에 조건 요약 바 — "8월 15일 ~ 8월 17일· 2박 3일· 캠핑장비" 와 [기간 바꾸기] 버튼 2) 좌측에 필터 패널 — 1일 대여료 범위 슬라이더, 보증금 범위, 수령 방법(매장 방문·택배), 브랜드 체크박스 3) 우측 상단에 결과 수("이 기간에 빌릴 수 있는 장비 24개")와 정렬 드롭다운 4) 장비 카드 목록 — 사진, 이름, 1일 대여료와 "2박 3일 총 OO원", 보증금 배지, "3대 중 1대 남음" 잔여 배지, 별점 5) 그 기간에 이미 다 나간 장비는 카드를 흐리게 하고 "8월 20일부터 가능" 처럼 다음 가능한 날짜를 적어 뒤쪽에 배치 6) 하단 더 보기 버튼 핵심은 "고른 기간 기준으로 값과 재고가 다시 매겨진다"는 것이 눈에 보이는 것이다.
  ```

### HO-04 · 결과 없음 - 그 기간에 빈 장비가 없음
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈` — 누르면 `HO-01`로 간다
- **일정**: 2026-08-17 ~ 2026-08-19
- **기능정의**: 고른 기간과 조건 요약 · 왜 없는지 한 줄 설명(주말·연휴에 몰린다) · 기간을 앞뒤로 넓혀 보기 버튼 · 빌릴 수 있는 가까운 날짜 3개 추천 칩 · 조건을 풀어 보기(브랜드·수령 방법 해제) · 자리 나면 알려드리기 신청 · 비슷한 장비 대체 추천 4개
- **버튼 → 이동화면**:
  - `기간 다시 고르기` → `HO-03` 기간으로 찾기 - 결과
  - `장비 둘러보기` → `PD-01` 장비 목록
- **생성 프롬프트**:

  ```
  고른 기간에 빌릴 수 있는 장비가 하나도 없을 때의 화면을 만들어줘. 1) 가운데 큰 안내 — "8월 15일 ~ 17일에 빌릴 수 있는 장비가 없어요" 2) 왜 그런지 한 줄 — "광복절 연휴라 캠핑 장비가 일찍 나갔어요" 3) [기간을 하루씩 넓혀 보기] 버튼 4) "이 날짜는 비어 있어요" 추천 날짜 칩 3개 — 8/13~15, 8/20~22, 8/22~24 5) 걸어 둔 조건 풀어 보기 — 브랜드·수령 방법 필터를 해제하는 버튼 6) [자리 나면 알려드릴게요] 알림 신청 박스 — 취소가 나오면 문자로 알림 7) 비슷한 장비 4개 카드 헛걸음시키지 않는 것이 목적이다. 대안을 반드시 함께 준다.
  ```

### HO-05 · 기획전·특가
- **소속 메뉴**: 홈
- **뒤로가기**: 좌측 상단에 `‹ 홈` — 누르면 `HO-01`로 간다
- **일정**: 2026-08-20 ~ 2026-08-24
- **기능정의**: 기획전 대표 배너와 남은 기간 카운트다운 · 기획전 쿠폰 받기 · 특가 장비 목록(정가·특가·할인율) · 평일 할인 안내(월~목 30% 싸다) · 장기 대여 할인 단계표(7일 이상·30일 이상) · 첫 대여 고객 보증금 면제 안내 · 기획전 이용 조건과 제외 대상 · 종료된 기획전 안내
- **버튼 → 이동화면**:
  - `장비 상세` → `PD-02` 장비 상세
  - `장비 목록` → `PD-01` 장비 목록
  - `종료된 기획전` → `HO-05` 기획전·특가
- **생성 프롬프트**:

  ```
  기획전·특가 화면을 만들어줘. 1) 상단 큰 배너와 "종료까지 2일 14:22:07" 카운트다운 2) [기획전 쿠폰 받기] 버튼 — 누르면 "받았어요"로 바뀌고 비활성 3) 기획전 탭 — 평일 특가 / 장기 대여 / 첫 대여 혜택 4) 특가 장비 목록 — 정가에 취소선, 특가와 할인율 배지 5) 평일 할인 안내 박스 — "월~목에 빌리면 30% 싸요" 요일 달력 그림 6) 장기 대여 할인 단계표 — 3일 이상 10%, 7일 이상 20%, 30일 이상 35% 7) 첫 대여 고객 보증금 면제 안내 8) 이용 조건과 제외 대상 — 세트 패키지·신상 장비는 제외 할인 구조가 여러 겹이라 "무엇이 얼마나 싸지는지"가 한눈에 보이게 표로 정리해줘.
  ```

### PD-01 · 장비 목록
- **소속 메뉴**: 장비
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-08-25 ~ 2026-08-28
- **기능정의**: 종류·브랜드·대여료·보증금 필터 · 정렬(추천·낮은 대여료·많이 빌린 순·별점) · 장비 카드 목록(사진·이름·1일 대여료·보증금·잔여 대수·별점·찜) · 고른 기간 표시와 바꾸기 · 그 기간 불가 장비 흐림 처리와 다음 가능일 · 세트 패키지 섞어 보여주기 · 무한 스크롤
- **버튼 → 이동화면**:
  - `장비 상세` → `PD-02` 장비 상세
  - `재고 달력` → `PD-03` 날짜별 가용 재고 달력
  - `패키지` → `PD-06` 세트 패키지
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 필터 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
  - 정렬 — 고른 기준으로 **순서를 실제로 바꿔** 다시 그립니다
- **생성 프롬프트**:

  ```
  장비 목록 화면을 만들어줘. 1) 상단에 고른 기간 칩과 [기간 바꾸기], "이 기간에 되는 것만" 토글 2) 좌측 필터 — 종류, 브랜드, 1일 대여료 범위, 보증금 있음/없음, 수령 방법 3) 우측 상단 결과 수와 정렬 드롭다운 4) 장비 카드 3열 그리드 — 사진(4:3), 이름, 1일 대여료, 보증금 배지, "5대 중 2대 남음" 잔여 배지, 별점과 후기 수, 우상단 찜 하트 5) 그 기간에 다 나간 장비는 흐리게 + "8/20부터 가능" 배지 6) 중간중간 세트 패키지 카드를 섞어 배치 잔여 대수가 카드에서 가장 먼저 눈에 들어와야 한다. 렌탈은 "있냐 없냐"가 먼저다.
  ```

### PD-02 · 장비 상세
- **소속 메뉴**: 장비
- **뒤로가기**: 좌측 상단에 `‹ 장비 목록` — 누르면 `PD-01`로 간다
- **일정**: 2026-08-31 ~ 2026-09-03
- **기능정의**: 장비 사진 갤러리 5장 · 장비명·브랜드·모델과 별점 · 1일 대여료와 고른 기간 총액 · 보증금 금액과 언제 돌려주는지 · 날짜별 재고 달력 열기 · 대여 기간과 수량 고르기 · 수령 방법 고르기(매장 방문·택배 왕복) · 구성품 목록과 사진 · 이용 안내(사용법·주의사항·금지사항) · 파손 시 배상 기준표 · 함께 빌리면 좋은 장비 · 후기 목록과 별점 분포 · 하단 고정 바(총액·보증금·담기·바로 빌리기)
- **버튼 → 이동화면**:
  - `재고 달력` → `PD-03` 날짜별 가용 재고 달력
  - `장바구니 담기` → `CT-01` 대여 장바구니
  - `바로 빌리기` → `BK-01` 대여 신청서
  - `그 날짜 마감` → `PD-05` 대여 불가 - 그 날짜 마감
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 수량 — 누를 때마다 **숫자와 합계가 같이** 바뀝니다
- **생성 프롬프트**:

  ```
  장비 상세 화면을 만들어줘. 1) 좌측에 사진 갤러리(큰 사진 + 아래 썸네일 5장), 우측에 예약 패널 2) 우측 예약 패널 — 장비명, 브랜드·모델, 별점, 1일 대여료, [대여 기간] 날짜 두 개 고르는 칸, [수량] 스테퍼, "이 날짜에 되나요?" 재고 달력 열기 링크, 수령 방법 라디오(매장 방문 / 택배 왕복 +6,000원), 금액 요약 — 대여료 소계 / 기간 할인 / 배송비 / 보증금(별도) / 지금 낼 금액 3) 보증금 안내 박스 — "보증금 100,000원은 반납 확인 후 3영업일 안에 돌려드려요" 4) 하단 탭 — 구성품 / 이용 안내 / 파손 배상 기준 / 후기 5) 구성품 탭 — 사진과 함께 목록(본체, 폴대 2, 팩 12, 보관가방). 빠지면 부품비가 붙는다는 안내 6) 이용 안내 탭 — 사용법 요약, 주의사항, 금지사항(우천 시 그대로 접어 넣지 말 것) 7) 파손 배상 기준 탭 — 부품별 배상액 표(폴대 1개 15,000원 등) 8) 후기 탭 — 별점 분포 막대와 후기 목록 9) 화면 아래 고정 바 — 총액, 보증금, [장바구니] [바로 빌리기] 보증금은 "대여료와 다른 돈"이라는 것이 반드시 구별돼 보여야 한다. 여기서 오해가 가장 많다.
  ```

### PD-03 · 날짜별 가용 재고 달력
- **소속 메뉴**: 장비
- **뒤로가기**: 좌측 상단에 `‹ 장비 목록` — 누르면 `PD-01`로 간다
- **일정**: 2026-09-04 ~ 2026-09-09
- **기능정의**: 월 단위 달력에 날짜마다 남은 수량 표시 · 다 나간 날은 흐리게 하고 못 고르게 막음 · 빌리는 날과 돌려주는 날을 두 번 눌러 기간으로 잡기 · 고른 기간에 하루라도 모자라면 그 날을 짚어 알림 · 수량을 바꾸면 가능한 날이 다시 계산됨 · 최소·최대 대여 일수 안내 · 요일별 대여료 차이 표시(주말 할증) · 준비·점검에 필요한 날(반납 다음 날은 못 빌림) 안내 · 고른 기간 요약과 총액 · 이 기간으로 예약하기
- **버튼 → 이동화면**:
  - `이 기간으로 예약` → `BK-01` 대여 신청서
  - `장비 상세로` → `PD-02` 장비 상세
  - `그 날짜 마감` → `PD-05` 대여 불가 - 그 날짜 마감
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 수량 — 누를 때마다 **숫자와 합계가 같이** 바뀝니다
- **생성 프롬프트**:

  ```
  이 서비스에서 가장 중요한 화면이다. 날짜별로 재고가 남았는지 보여주는 달력을 만들어줘. 일반 쇼핑몰과 다른 점이 여기서 드러나야 한다 — 재고는 "몇 개 남았나"가 아니라 "이 날짜에 비었나"다. 1) 상단에 장비명과 총 보유 대수 — "카즈미 텐트 5대 보유" 2) 월 달력 — 날짜 칸마다 그날 남은 수량을 숫자와 색으로 표시. 넉넉(3대 이상) 초록 / 얼마 안 남음(1~2대) 주황 / 다 나감(0대) 회색 흐림 + 클릭 불가 3) 이전·다음 달 이동 버튼 4) 날짜를 두 번 눌러 기간을 잡으면 그 사이 날짜가 색으로 이어짐 5) 고른 기간 중 하루라도 재고가 모자라면 붉은 안내 — "8월 16일에 재고가 없어요. 15~17일 중 16일만 비워야 합니다" 6) 수량 스테퍼 — 2대로 올리면 1대만 남은 날이 회색으로 바뀜 7) 안내 박스 — 최소 1박, 최대 30박 / 반납 다음 날은 점검일이라 못 빌림 / 금·토는 대여료 20% 할증 8) 하단에 고른 기간 요약(2박 3일, 8/15~8/17)과 총액, [이 기간으로 예약하기] 버튼 달력 칸의 숫자가 이 화면의 주인공이다. 색만으로 알리지 말고 숫자를 반드시 함께 적어줘.
  ```

### PD-04 · 옵션·부속품 고르기
- **소속 메뉴**: 장비
- **뒤로가기**: 좌측 상단에 `‹ 장비 목록` — 누르면 `PD-01`로 간다
- **일정**: 2026-09-10 ~ 2026-09-14
- **기능정의**: 고른 장비와 기간 요약 · 옵션 고르기(크기·색·구성) · 함께 빌릴 부속품 체크(전용 매트·팩 추가·보관함) · 부속품별 대여료와 보증금 · 보험 붙이기 선택(파손 면책 하루 3,000원) · 면책 범위와 제외 사항 안내 · 옵션에 따른 재고 차이 안내 · 금액 요약과 다음 단계
- **버튼 → 이동화면**:
  - `장바구니 담기` → `CT-01` 대여 장바구니
  - `바로 빌리기` → `BK-01` 대여 신청서
  - `장비 상세` → `PD-02` 장비 상세
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 선택 — 고른 개수를 **숫자로 보여주고**, 하나도 안 골랐으면 일괄 단추를 눌리지 않게 둡니다
- **생성 프롬프트**:

  ```
  고른 장비의 옵션과 부속품을 정하는 화면을 만들어줘. 1) 상단에 고른 장비 사진과 이름, 기간 요약 2) 옵션 라디오 — 크기(4인용 / 6인용), 색상. 옵션마다 재고가 다르므로 "6인용은 이 기간에 1대 남음"처럼 옵션별 잔여를 함께 표시 3) 함께 빌릴 부속품 체크리스트 — 전용 매트(+5,000원/일), 팩 추가 12개(+3,000원/일), 보관함(+4,000원/일). 각각 보증금이 따로 있으면 배지로 4) 파손 면책 보험 — "하루 3,000원으로 20만원까지 면책" 카드. 면책 되는 것과 안 되는 것(고의·분실·물에 잠김)을 표로 명확히 5) 우측 금액 요약 — 대여료, 부속품, 보험료, 기간 할인, 보증금(별도), 지금 낼 금액 6) 하단 [장바구니 담기] [바로 빌리기] 보험을 권하되 강요하지 않는 문장으로. 면책 제외 사항을 숨기지 않는 것이 중요하다.
  ```

### PD-05 · 대여 불가 - 그 날짜 마감
- **소속 메뉴**: 장비
- **뒤로가기**: 좌측 상단에 `‹ 장비 목록` — 누르면 `PD-01`로 간다
- **일정**: 2026-09-15 ~ 2026-09-17
- **기능정의**: 고른 기간과 장비 요약 · 어느 날짜가 왜 안 되는지 짚어 알림 · 가장 가까운 빌릴 수 있는 날짜 3개 추천 · 수량을 줄이면 되는지 알림 · 같은 종류 다른 장비 추천 4개 · 취소 자리 나면 알림 신청 · 알림은 문자로 온다는 안내 · 대기 순번 표시
- **버튼 → 이동화면**:
  - `재고 달력` → `PD-03` 날짜별 가용 재고 달력
  - `다른 장비 보기` → `PD-01` 장비 목록
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 수량 — 누를 때마다 **숫자와 합계가 같이** 바뀝니다
- **생성 프롬프트**:

  ```
  고른 날짜에 그 장비를 빌릴 수 없을 때의 화면을 만들어줘. 1) 상단에 붉은 안내 — "8월 15~17일에 카즈미 텐트 2대는 빌릴 수 없어요" 2) 어디가 막혔는지 짚어서 — "8월 16일에 1대만 남아 있어요" 와 작은 달력 그림 3) 해결책 세 가지를 나란히 카드로 ① 수량을 1대로 줄이면 그대로 가능합니다 [1대로 빌리기] ② 이 날짜는 비어 있어요 — 8/13~15, 8/20~22, 8/22~24 칩 ③ 비슷한 다른 텐트 4개 카드 4) [자리 나면 알려주세요] 알림 신청 박스 — 취소가 나오면 문자로 알림, 지금 대기 3번째라는 순번 표시 5) 알림은 문자로 가고 선착순이라는 안내 막다른 길로 두지 않는다. 반드시 세 갈래를 준다.
  ```

### PD-06 · 세트 패키지
- **소속 메뉴**: 장비
- **뒤로가기**: 좌측 상단에 `‹ 장비 목록` — 누르면 `PD-01`로 간다
- **일정**: 2026-09-18 ~ 2026-09-22
- **기능정의**: 패키지 대표 사진과 이름 · 낱개로 빌릴 때와 견준 절약 금액 · 들어 있는 장비 목록과 각각의 낱개 대여료 · 구성 바꾸기(빼면 얼마 빠지는지) · 인원수 추천(4인 가족 기준) · 패키지 보증금 합계 · 이 기간에 패키지 전부가 되는지 확인 · 하나라도 모자라면 어떤 장비가 모자란지 알림 · 패키지 이용 후기 · 담기와 바로 빌리기
- **버튼 → 이동화면**:
  - `담기` → `CT-01` 대여 장바구니
  - `바로 빌리기` → `BK-01` 대여 신청서
  - `재고 달력` → `PD-03` 날짜별 가용 재고 달력
- **생성 프롬프트**:

  ```
  여러 장비를 묶은 세트 패키지 상세 화면을 만들어줘. 1) 상단에 패키지 사진과 이름 — "4인 가족 캠핑 풀세트" 2) 절약 금액 강조 배지 — "낱개로 빌리면 178,000원 → 세트 139,000원 (22% 절약)" 3) 들어 있는 장비 목록 표 — 사진, 이름, 낱개 대여료, [빼기] 체크박스. 빼면 총액과 할인율이 다시 계산됨 4) 인원수 추천 — "4인 가족에게 맞아요" 와 인원별 추천표 5) 재고 확인 박스 — "8월 15~17일에 이 패키지 전부 가능합니다" 초록, 하나라도 모자라면 "화로대가 8월 16일에 없어요" 처럼 어떤 장비가 막혔는지 짚어서 붉게 6) 보증금 합계 — 낱개 보증금의 합이 아니라 패키지 보증금은 따로라는 안내 7) 패키지 이용 후기 3개 8) 하단 고정 바 — 총액, 보증금, [담기] [바로 빌리기] 세트는 "하나만 없어도 못 빌린다"는 것이 렌탈의 어려운 점이다. 그것이 화면에 드러나야 한다.
  ```

### CT-01 · 대여 장바구니
- **소속 메뉴**: 장바구니
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-09-23 ~ 2026-09-25
- **기능정의**: 담은 장비 목록(사진·이름·옵션·기간·수량·대여료·보증금) · 줄마다 수량 바꾸기와 삭제 · 기간이 다른 줄이 섞여 있으면 갈라서 보여주기 · 기간을 한꺼번에 맞추기 · 담은 뒤 재고가 빠진 줄에 경고 표시 · 쿠폰 고르기와 할인 적용 · 금액 요약(대여료 합계·기간 할인·배송비·보증금 합계·지금 낼 금액) · 보증금은 따로 잡힌다는 안내 · 계속 둘러보기와 신청하기
- **버튼 → 이동화면**:
  - `신청하기` → `BK-01` 대여 신청서
  - `기간 바꾸기` → `CT-02` 기간 한꺼번에 바꾸기
  - `빈 장바구니` → `CT-03` 빈 장바구니
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 수량 — 누를 때마다 **숫자와 합계가 같이** 바뀝니다
- **생성 프롬프트**:

  ```
  대여 장바구니 화면을 만들어줘. 1) 상단에 "빌릴 기간이 다른 장비가 섞여 있어요" 안내가 필요한 경우 배너 2) 기간별로 묶은 장비 목록 — 기간이 소제목이 되고 그 아래 장비 줄들 3) 각 줄 — 체크박스, 사진, 이름과 옵션, 기간, 수량 스테퍼, 대여료, 보증금, 삭제 버튼 4) 담은 뒤 재고가 빠진 줄은 붉은 테두리와 "이 기간에 재고가 없어졌어요. 다른 날짜를 고르거나 빼주세요" 안내 + [날짜 바꾸기] 버튼 5) [기간 한꺼번에 맞추기] 버튼 — 누르면 모든 줄을 같은 기간으로 6) 우측 금액 요약 카드 — 대여료 합계, 기간 할인, 배송비, 구분선 아래에 보증금 합계(별도 표시), 지금 낼 금액 7) 보증금 안내 — "보증금은 반납 확인 후 돌려드리는 돈이라 대여료와 따로 잡혀요" 8) 쿠폰 고르기 드롭다운 9) 하단 [계속 둘러보기] [신청하기] "담아둔 사이에 남이 먼저 빌려갈 수 있다"는 것이 렌탈 장바구니의 특징이다. 그것을 숨기지 않는다.
  ```

### CT-02 · 기간 한꺼번에 바꾸기
- **소속 메뉴**: 장바구니
- **뒤로가기**: 좌측 상단에 `‹ 대여 장바구니` — 누르면 `CT-01`로 간다
- **일정**: 2026-09-28 ~ 2026-09-30
- **기능정의**: 지금 기간과 바꿀 기간 나란히 보기 · 새 기간 고르는 달력 · 담은 장비마다 새 기간에 되는지 하나씩 확인 표시 · 안 되는 장비는 무엇이 언제 막혔는지 · 안 되는 것만 빼고 바꾸기 · 기간이 바뀌면 대여료가 얼마나 달라지는지 견주기 · 기간 할인 단계가 바뀌면 알림 · 되돌리기
- **버튼 → 이동화면**:
  - `장바구니` → `CT-01` 대여 장바구니
  - `신청하기` → `BK-01` 대여 신청서
- **생성 프롬프트**:

  ```
  장바구니에 담은 장비들의 기간을 한꺼번에 바꾸는 화면을 만들어줘. 1) 상단에 지금 기간과 바꿀 기간을 나란히 — "8/15~17 (2박 3일) → 8/22~25 (3박 4일)" 2) 새 기간 고르는 달력 3) 담은 장비 목록 — 각 줄 오른쪽에 새 기간 가능 여부 배지 ✓ 가능 / ⚠ 8/23에 재고 없음 / ✗ 전 기간 불가 4) 안 되는 것이 있으면 [안 되는 장비는 빼고 바꾸기] 버튼 5) 금액 견주기 표 — 지금 총액 / 바꾼 뒤 총액 / 차액. "3박이 되면 기간 할인 10%가 붙어 오히려 8,000원 싸져요" 같은 안내 6) 하단 [되돌리기] [이 기간으로 바꾸기] 기간을 바꾸면 재고와 금액이 «둘 다» 바뀐다. 둘 다 보여줘야 한다.
  ```

### CT-03 · 빈 장바구니
- **소속 메뉴**: 장바구니
- **뒤로가기**: 좌측 상단에 `‹ 대여 장바구니` — 누르면 `CT-01`로 간다
- **일정**: 2026-10-01 ~ 2026-10-02
- **기능정의**: 담은 것이 없다는 안내 · 장비 둘러보기 유도 · 최근 본 장비 4개 · 찜한 장비 바로가기 · 이번 주말 남은 장비 추천 · 처음이신가요 3단계 안내 링크
- **버튼 → 이동화면**:
  - `장비 둘러보기` → `PD-01` 장비 목록
  - `홈` → `HO-01` 홈
- **생성 프롬프트**:

  ```
  장바구니가 비었을 때의 화면을 만들어줘. 1) 가운데 큰 아이콘과 "아직 담은 장비가 없어요" 2) [장비 둘러보기] 버튼 3) 최근 본 장비 4개 카드 4) 찜한 장비 바로가기 링크 5) "이번 주말 아직 남은 장비" 추천 4개 6) 처음이신 분을 위한 3단계 안내 링크 비어 있다고 텅 빈 화면을 주지 않는다. 다음에 할 일을 준다.
  ```

### BK-01 · 대여 신청서
- **소속 메뉴**: 예약·결제
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-10-05 ~ 2026-10-08
- **기능정의**: 빌릴 장비와 기간 요약 · 신청자 정보(이름·연락처·이메일) · 수령 방법 고르기(매장 방문·택배 왕복) · 방문 수령 시 방문 예정 시각 고르기 · 택배 시 받는 주소와 회수 주소 · 반납 예정일과 반납 방법 안내 · 신분 확인 안내(고가 장비는 신분증 확인) · 요청사항 · 필수 동의(대여 약관·파손 배상 기준·개인정보) · 보증금 안내 · 금액 요약과 결제로 가기
- **버튼 → 이동화면**:
  - `결제하기` → `BK-02` 보증금·결제
  - `약관 보기` → `CS-03` 대여 약관·규정
  - `장바구니로` → `CT-01` 대여 장바구니
- **생성 프롬프트**:

  ```
  대여 신청서 화면을 만들어줘. 1) 상단에 빌릴 장비 요약 카드 — 사진, 이름, 기간, 수량 2) 신청자 정보 — 이름(필수), 연락처(필수, 형식 검증), 이메일 3) 수령 방법 라디오 — 매장 방문 / 택배 왕복(+6,000원)· 매장 방문을 고르면 → 방문 예정 시각 드롭다운, 매장 주소·지도, 운영시간· 택배를 고르면 → 받는 주소 입력, "회수 주소가 같나요" 체크, 회수 주소 입력 4) 반납 안내 박스 — "8월 17일 18시까지 반납해 주세요. 하루 늦으면 대여료의 30%가 붙어요" 5) 신분 확인 안내 — "50만원 넘는 장비는 수령할 때 신분증을 확인해요" 6) 요청사항 자유 입력 7) 필수 동의 체크 3개 — 대여 약관, 파손 배상 기준, 개인정보 수집. 각각 [내용 보기] 링크. 다 체크해야 결제 버튼이 열림 8) 우측 금액 요약 — 대여료, 배송비, 할인, 지금 낼 금액, 구분선 아래 보증금(카드에 걸어두고 반납 후 해제) 9) 하단 [결제하기] 보증금이 "빠져나가는 돈이 아니라 걸어두는 돈"이라는 것을 여기서 확실히 알려야 한다.
  ```

### BK-02 · 보증금·결제
- **소속 메뉴**: 예약·결제
- **뒤로가기**: 좌측 상단에 `‹ 대여 신청서` — 누르면 `BK-01`로 간다
- **일정**: 2026-10-09 ~ 2026-10-13
- **기능정의**: 결제 금액과 보증금을 갈라서 보여주기 · 결제 수단 고르기(카드·간편결제·계좌이체) · 보증금 잡는 방식 고르기(카드 한도 보류·실제 결제 후 환불) · 카드 보류가 무엇인지 설명 · 보증금 면제 조건 안내 · 언제 얼마가 빠지는지 시각 순서로 · 현금영수증·세금계산서 · 최종 확인 체크 · 결제 실행
- **버튼 → 이동화면**:
  - `결제 완료` → `BK-03` 대여 확정 완료
  - `결제 실패` → `BK-04` 결제 실패
  - `신청서로` → `BK-01` 대여 신청서
- **생성 프롬프트**:

  ```
  보증금과 대여료를 함께 결제하는 화면을 만들어줘. 이 서비스에서 가장 오해가 잦은 자리다. 1) 상단에 금액을 «두 덩어리로 갈라서» 크게 [지금 빠지는 돈] 대여료 139,000원 [걸어두는 돈] 보증금 100,000원 — 반납 확인 후 돌려드려요 2) 시간 순서 그림 — 오늘 결제 → 8/15 수령 → 8/17 반납 → 8/20 보증금 환급 3) 보증금 방식 라디오· 카드 한도 보류 (권장) — 실제로 빠져나가지 않고 한도만 잡혀요· 실제 결제 후 환불 — 결제되고 반납 후 돌려드려요 (3~5영업일) 고른 쪽의 설명이 아래 펼쳐짐 4) 결제 수단 탭 — 카드 / 간편결제 / 계좌이체. 계좌이체는 보증금 한도 보류가 안 된다는 안내 5) 보증금 면제 조건 — "3회 이상 이용하고 연체가 없으면 면제됩니다" 6) 현금영수증·세금계산서 선택 7) 최종 확인 체크 — "대여료와 보증금이 따로라는 것을 확인했습니다" 8) [139,000원 결제하고 보증금 100,000원 걸기] 버튼 — 금액을 버튼에 그대로 적는다 숫자를 버튼에 적는 것이 중요하다. 얼마가 빠지는지 모르고 누르게 하면 안 된다.
  ```

### BK-03 · 대여 확정 완료
- **소속 메뉴**: 예약·결제
- **뒤로가기**: 좌측 상단에 `‹ 대여 신청서` — 누르면 `BK-01`로 간다
- **일정**: 2026-10-14 ~ 2026-10-19
- **기능정의**: 확정 안내와 대여번호 · 빌린 장비와 기간 요약 · 수령 방법별 다음 할 일 안내 · 매장 방문이면 주소·시각·QR · 택배면 발송 예정일과 송장 안내 · 반납 예정일 강조와 캘린더에 넣기 · 결제 금액과 보증금 상태 · 수령 전 준비물(신분증) · 대여 내역으로 가기 · 카톡 알림 받기 신청
- **버튼 → 이동화면**:
  - `대여 내역` → `MY-01` 내 대여 내역
  - `수령 확인` → `RT-01` 수령 확인
  - `홈` → `HO-01` 홈
- **생성 프롬프트**:

  ```
  대여가 확정된 완료 화면을 만들어줘. 1) 상단에 초록 체크와 "대여가 확정됐어요", 대여번호 R-20260810-0031 2) 빌린 장비 요약 카드 — 사진, 이름, 수량, 기간 3) 다음에 할 일 — 수령 방법에 따라· 매장 방문: 8월 15일 10시, 매장 주소와 지도, 수령 QR 코드, "신분증을 가져오세요"· 택배: 8월 14일 발송 예정, 송장은 발송되면 문자로 4) 반납 예정일을 가장 크게 — "8월 17일(일) 18시까지 반납" 과 [캘린더에 넣기] 버튼 5) 결제 요약 — 대여료 139,000원 결제 완료 / 보증금 100,000원 카드 한도 보류 중 6) [대여 내역 보기] [홈으로] 7) 카톡으로 반납 알림 받기 신청 박스 반납일을 잊게 만들면 연체가 나고 서로 손해다. 반납일이 이 화면에서 가장 커야 한다.
  ```

### BK-04 · 결제 실패
- **소속 메뉴**: 예약·결제
- **뒤로가기**: 좌측 상단에 `‹ 대여 신청서` — 누르면 `BK-01`로 간다
- **일정**: 2026-10-20 ~ 2026-10-22
- **기능정의**: 실패 안내와 무엇 때문인지 · 흔한 까닭 목록(한도 초과·보증금 한도 부족·카드사 거절) · 보증금 때문에 한도가 모자란 경우 따로 안내 · 장비는 아직 잡혀 있다는 것과 언제까지인지 · 다시 시도하기 · 다른 결제 수단으로 · 보증금 방식 바꾸기 · 고객센터 안내
- **버튼 → 이동화면**:
  - `다시 결제` → `BK-02` 보증금·결제
  - `신청서로` → `BK-01` 대여 신청서
  - `문의하기` → `CS-02` 1:1 문의
- **생성 프롬프트**:

  ```
  결제가 실패했을 때의 화면을 만들어줘. 1) 상단에 붉은 안내 — "결제하지 못했어요" 와 사유 — "카드 한도가 모자랍니다" 2) 중요한 안내 박스 — "대여료 139,000원 + 보증금 한도 100,000원 = 239,000원의 여유가 필요해요. 보증금은 빠져나가지 않지만 한도는 잡힙니다" 렌탈 결제 실패의 가장 흔한 까닭이 이것이라 따로 크게 설명 3) 흔한 까닭 목록 — 한도 초과 / 해외 결제 차단 / 카드사 승인 거절 / 잔액 부족 4) 안심 박스 — "고르신 장비는 20분 동안 잡아두었어요. 12:34 남음" 카운트다운 5) 해결책 버튼 세 개 — [다시 시도] [다른 카드로] [보증금을 실제 결제로 바꾸기] 6) 고객센터 전화·카톡 안내 "장비가 아직 잡혀 있다"를 알려주는 것이 중요하다. 없으면 손님이 급해진다.
  ```

### BK-05 · 대여 계약 동의·서명
- **소속 메뉴**: 예약·결제
- **뒤로가기**: 좌측 상단에 `‹ 대여 신청서` — 누르면 `BK-01`로 간다
- **일정**: 2026-10-23 ~ 2026-10-27
- **기능정의**: 대여 계약 요약(빌리는 것·기간·금액·보증금) · 조항별 펼쳐보기 · 파손·분실 시 배상 기준 요약표 · 연체료 계산 방식 · 반납 상태 기준(세척·건조) · 개인정보 이용 범위 · 필수와 선택 동의 갈라 두기 · 전자 서명 칸 · 계약서 사본 받기(이메일·다운로드) · 동의 없이는 수령 불가 안내
- **버튼 → 이동화면**:
  - `서명 완료` → `BK-03` 대여 확정 완료
  - `약관 전문` → `CS-03` 대여 약관·규정
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 선택 — 고른 개수를 **숫자로 보여주고**, 하나도 안 골랐으면 일괄 단추를 눌리지 않게 둡니다
- **생성 프롬프트**:

  ```
  대여 계약에 동의하고 서명하는 화면을 만들어줘. 고가 장비는 수령 전에 이 절차가 필요하다. 1) 상단에 계약 요약 카드 — 빌리는 장비, 기간, 대여료, 보증금 2) 조항 아코디언 — 각 조항 제목과 한 줄 요약, 누르면 본문 펼침 제1조 대여 기간과 반납 / 제2조 보증금 / 제3조 파손·분실 배상 / 제4조 연체료 / 제5조 반납 상태 / 제6조 개인정보 3) 꼭 알아야 할 것 세 가지를 요약표로 앞에 빼서· 하루 연체 = 1일 대여료의 30%· 분실 = 장비 정가의 100%· 젖은 채 반납 = 건조비 20,000원 4) 동의 체크 — 필수 4개, 선택 1개(마케팅) 갈라서 5) 전자 서명 칸 — 마우스나 손가락으로 그리는 박스와 [다시 그리기] 6) [계약서 사본 이메일로 받기] 체크 7) 하단 [동의하고 서명 완료] 겁주는 문서가 아니라 «미리 알려주는» 문서로 쓴다. 요약표를 앞에 두는 이유가 그것이다.
  ```

### BK-06 · 대여 기간 연장
- **소속 메뉴**: 예약·결제
- **뒤로가기**: 좌측 상단에 `‹ 대여 신청서` — 누르면 `BK-01`로 간다
- **일정**: 2026-10-28 ~ 2026-10-30
- **기능정의**: 지금 빌린 장비와 반납 예정일 · 얼마나 더 쓸지 고르기 · 연장할 기간에 그 장비가 비어 있는지 확인 · 다음 예약이 있으면 언제까지만 연장되는지 · 추가 대여료 계산과 기간 할인 다시 적용 · 보증금은 그대로 유지된다는 안내 · 연장은 반납일 전에만 된다는 안내 · 연체 중이면 연장 대신 연체 정산으로 안내 · 연장 결제
- **버튼 → 이동화면**:
  - `연장 결제` → `BK-02` 보증금·결제
  - `대여 상세` → `MY-02` 대여 상세
  - `연체 안내` → `RT-05` 연체 안내
- **생성 프롬프트**:

  ```
  빌린 기간을 더 늘리는 화면을 만들어줘. 1) 상단에 지금 상태 — "카즈미 텐트 2대· 8월 17일 18시 반납 예정· 남은 시간 1일 6시간" 2) 얼마나 더 쓸지 — [+1일] [+2일] [+3일] 버튼과 직접 날짜 고르기 3) 재고 확인 결과 박스· 가능하면 초록 — "8월 20일까지 연장할 수 있어요"· 다음 예약이 있으면 주황 — "8월 19일에 다른 예약이 있어 18일까지만 연장돼요" 4) 금액 계산 표 — 추가 일수 × 1일 대여료, 기간 할인이 한 단계 올라가면 그 안내, 최종 추가 결제액 5) 보증금 안내 — "보증금은 그대로 유지돼요. 추가로 걸지 않습니다" 6) 경고 박스 — "반납일이 지난 뒤에는 연장이 안 되고 연체료가 붙어요. 지금 연장하는 것이 훨씬 쌉니다" 와 연장료 vs 연체료 견주기 7) 하단 [연장하고 결제하기] 연장을 쉽게 만들어야 연체가 준다. 연체료보다 연장료가 싸다는 것을 보여주는 것이 핵심이다.
  ```

### RT-01 · 수령 확인
- **소속 메뉴**: 수령·반납
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-11-02 ~ 2026-11-04
- **기능정의**: 수령 QR 코드와 대여번호 · 받을 장비와 구성품 목록 · 구성품 하나씩 체크하며 확인 · 받을 때 상태 사진 찍어 올리기 · 이미 있던 흠집 표시하기 · 신분증 확인 안내 · 반납 예정일 다시 알림 · 사용법 간단 안내 링크 · 수령 확인 완료 · 확인해야 보증금 규정이 시작된다는 안내
- **버튼 → 이동화면**:
  - `확인 완료` → `RT-02` 이용 중
  - `대여 상세` → `MY-02` 대여 상세
- **생성 프롬프트**:

  ```
  장비를 받을 때 확인하는 화면을 만들어줘. 1) 상단에 큰 QR 코드와 대여번호 — 매장 직원이 찍는다 2) "받으신 장비를 확인해 주세요" 안내 3) 구성품 체크리스트 — 사진과 함께 하나씩. 본체, 폴대 2개, 팩 12개, 보관가방. 각 줄에 체크박스와 [사진] 버튼 4) 지금 상태 사진 올리기 — 4칸(정면·측면·바닥·보관가방). "받을 때 사진을 남겨두면 반납할 때 다툼이 없어요" 5) 이미 있던 흠집 표시 — [흠집 있어요] 버튼과 자유 입력. "미리 알려주시면 반납할 때 배상하지 않으셔도 돼요" 6) 신분증 확인 완료 배지 7) 반납 예정일 다시 크게 — 8월 17일(일) 18시 8) 하단 [수령 확인 완료] 받을 때 사진을 남기는 것이 렌탈 분쟁의 90%를 막는다. 그래서 이 단계를 건너뛰기 어렵게 만든다.
  ```

### RT-02 · 이용 중
- **소속 메뉴**: 수령·반납
- **뒤로가기**: 좌측 상단에 `‹ 수령 확인` — 누르면 `RT-01`로 간다
- **일정**: 2026-11-05 ~ 2026-11-09
- **기능정의**: 빌린 장비와 남은 시간 카운트 · 반납 예정일까지 남은 시간을 크게 · 반납 방법 미리 안내(매장·택배) · 반납 전 해야 할 일 체크리스트(세척·건조·구성품 확인) · 사용 중 문제 신고 · 기간 연장하기 · 파손 미리 알리기 · 사용법과 주의사항 다시 보기 · 반납 알림 설정
- **버튼 → 이동화면**:
  - `연장하기` → `BK-06` 대여 기간 연장
  - `반납 안내` → `RT-03` 반납 안내
  - `파손 신고` → `RT-06` 파손·분실 신고
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 알림 설정 — 누르면 **켜짐·꺼짐이 눈에 보이게** 바뀌고 그대로 남습니다
- **생성 프롬프트**:

  ```
  장비를 쓰고 있는 동안 보는 화면을 만들어줘. 1) 상단에 남은 시간을 가장 크게 — "반납까지 1일 6시간 22분" 실시간 카운트. 하루 안쪽으로 들어오면 주황, 지나면 붉게 2) 빌린 장비 카드 목록 3) [기간 연장하기] 버튼을 눈에 띄게 — 연체보다 싸다는 한 줄과 함께 4) 반납 전 체크리스트 — 흙·먼지 털기 / 완전히 말리기 / 구성품 다 챙기기 / 보관가방에 넣기. 각각 체크박스와 "왜 필요한지" 한 줄 5) 반납 방법 안내 — 매장 방문이면 주소와 운영시간, 택배면 회수 신청 버튼 6) 문제가 생겼나요 — [고장 신고] [파손 미리 알리기] 버튼. "미리 알리면 배상액이 줄어들 수 있어요" 7) 사용법·주의사항 다시 보기 링크 8) 반납 알림 설정 — 하루 전, 3시간 전 문자 남은 시간과 [연장하기]가 이 화면의 주인공이다. 연체를 막는 것이 서로에게 이롭다.
  ```

### RT-03 · 반납 안내
- **소속 메뉴**: 수령·반납
- **뒤로가기**: 좌측 상단에 `‹ 수령 확인` — 누르면 `RT-01`로 간다
- **일정**: 2026-11-10 ~ 2026-11-13
- **기능정의**: 반납 기한과 남은 시간 · 반납 방법 고르기(매장 방문·택배 회수) · 매장 반납 시 주소·운영시간·지도·주차 안내 · 택배 회수 신청과 방문 예정 시각 · 반납 전 준비 체크리스트 · 이 상태로 반납하면 안 되는 예시 사진 · 늦으면 얼마가 붙는지 표 · 구성품 빠지면 얼마인지 표 · 반납 접수로 가기
- **버튼 → 이동화면**:
  - `반납 접수` → `RT-04` 반납 접수
  - `이용 중` → `RT-02` 이용 중
  - `연체 안내` → `RT-05` 연체 안내
- **생성 프롬프트**:

  ```
  장비를 어떻게 돌려주는지 안내하는 화면을 만들어줘. 1) 상단에 반납 기한과 남은 시간 2) 반납 방법 탭 — 매장 방문 / 택배 회수· 매장 방문: 주소, 지도, 운영시간(평일 10~19시, 토 10~17시, 일 휴무), 주차 안내, "일요일에 반납하시려면 토요일까지 오세요" 주의· 택배 회수: [회수 신청] 버튼, 회수 예정 시각, 포장 방법 안내, "택배는 회수 기사가 가져간 날이 반납일이에요" 기준 명시 3) 반납 전 준비 체크리스트 — 사진과 함께 4) 이렇게 반납하면 추가 비용이 붙어요 — 젖은 텐트 사진, 흙 묻은 팩 사진과 각각의 추가 비용 5) 늦으면 얼마인지 표 — 1일 30%, 2일 60%, 3일 이상 100%+ 6) 구성품 빠지면 얼마인지 표 — 폴대 15,000원, 팩 1개 1,000원 등 7) 하단 [반납 접수하기] "언제가 반납일로 잡히는가"를 방법마다 다르게 명확히 적는 것이 중요하다. 여기서 다툼이 난다.
  ```

### RT-04 · 반납 접수
- **소속 메뉴**: 수령·반납
- **뒤로가기**: 좌측 상단에 `‹ 수령 확인` — 누르면 `RT-01`로 간다
- **일정**: 2026-11-16 ~ 2026-11-19
- **기능정의**: 반납할 장비와 기간 요약 · 구성품 다 챙겼는지 하나씩 체크 · 반납 상태 사진 올리기 · 수령 때 사진과 나란히 견주기 · 파손·분실 있으면 미리 알리기 · 미리 알리면 배상이 줄어든다는 안내 · 세척·건조 상태 스스로 확인 · 반납 방법과 시각 확정 · 접수 후 점검에 1~2일 걸린다는 안내 · 점검 끝나야 보증금이 풀린다는 안내 · 반납 접수 완료
- **버튼 → 이동화면**:
  - `접수 완료` → `ST-01` 점검 진행 중
  - `파손 신고` → `RT-06` 파손·분실 신고
  - `반납 안내` → `RT-03` 반납 안내
- **생성 프롬프트**:

  ```
  장비를 돌려주며 접수하는 화면을 만들어줘. 1) 상단에 반납할 장비와 기간 요약, "8월 17일 16:30 반납 접수" 시각 2) 구성품 체크리스트 — 받을 때와 같은 목록. 하나라도 빠지면 "폴대 1개가 빠졌어요. 15,000원이 정산됩니다" 즉시 안내 3) 반납 상태 사진 올리기 4칸 — 옆에 수령 때 찍은 사진을 나란히 놓아 견주게 4) 파손·분실 미리 알리기 — [있어요] 체크하면 내용 적는 칸과 사진 칸 펼침. "미리 알리시면 점검이 빨라지고 배상액도 조정될 수 있어요" 5) 스스로 확인 — 흙 털었나 / 말렸나 / 가방에 넣었나 체크 6) 반납 방법 확인 — 매장 접수 / 택배 회수 완료 7) 다음 단계 안내 — "점검에 1~2일 걸려요. 점검이 끝나면 보증금이 풀립니다" 8) 하단 [반납 접수하기] 수령 때 사진과 «나란히» 보여주는 것이 이 화면의 핵심이다. 그래야 서로 납득한다.
  ```

### RT-05 · 연체 안내
- **소속 메뉴**: 수령·반납
- **뒤로가기**: 좌측 상단에 `‹ 수령 확인` — 누르면 `RT-01`로 간다
- **일정**: 2026-11-20 ~ 2026-11-24
- **기능정의**: 반납 기한이 지났다는 안내와 얼마나 지났는지 · 지금까지 쌓인 연체료 · 연체료 계산 방식 공개 · 하루 더 지나면 얼마가 되는지 미리 보기 · 지금 반납하면 얼마인지 · 연장으로 바꿀 수 있는지 안내 · 보증금에서 먼저 빠진다는 안내 · 보증금을 넘으면 추가 청구된다는 안내 · 연락 없이 7일 넘으면 분실 처리된다는 안내 · 지금 반납 접수하기
- **버튼 → 이동화면**:
  - `지금 반납` → `RT-04` 반납 접수
  - `정산 내역` → `ST-02` 정산 내역 - 연체·파손 청구
  - `문의하기` → `CS-02` 1:1 문의
- **생성 프롬프트**:

  ```
  반납이 늦었을 때 보는 화면을 만들어줘. 1) 상단에 붉은 띠 — "반납이 1일 4시간 늦었어요" 2) 지금까지 연체료를 가장 크게 — "연체료 41,700원" 과 실시간으로 늘어난다는 표시 3) 계산 방식을 숨기지 않고 공개 — "1일 대여료 46,300원 × 30% × 1일 = 13,890원" 같은 식으로 표에 적어서 4) 하루 더 지나면 — "내일 이 시각이면 83,400원이 됩니다" 미리 보기 5) 보증금에서 먼저 빠진다는 안내 — 보증금 100,000원 중 41,700원 차감, 남는 58,300원 환급 예정. 막대 그림으로 6) 보증금을 넘으면 — "연체료가 보증금을 넘으면 등록하신 카드로 추가 청구돼요" 7) 경고 박스 — "연락 없이 7일이 지나면 분실로 처리되고 장비 정가가 청구됩니다" 8) 하단 [지금 반납 접수하기] 큰 버튼과 [고객센터 문의] 겁주기보다 «지금 하면 얼마》를 또렷하게 보여주는 것이 목적이다. 계산식을 공개해야 납득한다.
  ```

### RT-06 · 파손·분실 신고
- **소속 메뉴**: 수령·반납
- **뒤로가기**: 좌측 상단에 `‹ 수령 확인` — 누르면 `RT-01`로 간다
- **일정**: 2026-11-25 ~ 2026-11-30
- **기능정의**: 무엇이 어떻게 됐는지 고르기 · 파손 부위 고르고 정도 고르기 · 사진 올리기(필수) · 언제 어떻게 생긴 일인지 적기 · 부위별 배상 기준액 바로 보기 · 가입한 보험이 있으면 면책 되는지 판정 · 면책 제외 사유 안내 · 예상 배상액과 보증금에서 얼마가 빠지는지 · 수리 가능하면 수리비만 낸다는 안내 · 신고하면 배상이 줄 수 있다는 안내 · 신고 접수
- **버튼 → 이동화면**:
  - `신고 접수` → `ST-02` 정산 내역 - 연체·파손 청구
  - `반납 접수` → `RT-04` 반납 접수
  - `이용 중` → `RT-02` 이용 중
- **생성 프롬프트**:

  ```
  장비가 망가지거나 없어졌을 때 신고하는 화면을 만들어줘. 1) 상단에 안내 — "미리 알려주시면 점검이 빨라지고, 배상액이 조정될 수 있어요" 숨기지 말라는 뜻을 겁주지 않는 문장으로 2) 무슨 일인가요 라디오 — 파손 / 분실 / 오염 / 작동 안 함 3) 어느 부분인가요 — 장비 그림 위에 부위를 누르거나 목록에서 고르기. 고르면 아래에 배상 기준액이 바로 뜸 — "폴대 1개 파손: 15,000원" 4) 얼마나 심한가요 — 가벼움 / 보통 / 심함 라디오와 각각의 예시 사진 5) 사진 올리기 (필수) — 최소 2장, "가까이 한 장, 전체 한 장" 6) 언제 어떻게 생긴 일인가요 자유 입력 7) 보험 판정 박스 — 가입했으면 "파손 면책 보험 적용 대상입니다. 20만원까지 면책되어 배상액 0원" 초록, 제외면 "고의·분실은 면책 대상이 아니에요" 주황 8) 예상 정산 — 배상액, 보증금에서 차감, 환급 예정액을 막대로 9) 수리 안내 — "수리로 될 것 같으면 수리비만 받아요. 점검 후 알려드립니다" 10) 하단 [신고 접수하기] 정직하게 신고하는 것이 손해가 아니라는 것을 화면이 증명해야 한다.
  ```

### ST-01 · 점검 진행 중
- **소속 메뉴**: 정산
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-12-01 ~ 2026-12-03
- **기능정의**: 반납 접수됨과 접수 시각 · 점검 단계 진행 막대(접수·수량 확인·상태 점검·정산 확정) · 지금 어느 단계인지 · 각 단계에서 무엇을 보는지 설명 · 점검 예상 완료 시각 · 점검 결과가 나오면 알림 간다는 안내 · 보증금은 점검이 끝나야 풀린다는 안내 · 반납할 때 올린 사진 다시 보기 · 점검이 늦어지면 어떻게 되는지
- **버튼 → 이동화면**:
  - `정산 내역` → `ST-02` 정산 내역 - 연체·파손 청구
  - `대여 상세` → `MY-02` 대여 상세
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 수량 — 누를 때마다 **숫자와 합계가 같이** 바뀝니다
- **생성 프롬프트**:

  ```
  반납한 장비를 점검하는 동안 보는 화면을 만들어줘. 1) 상단에 "반납 접수됐어요. 점검하고 있습니다" 와 접수 시각 2) 진행 막대 4단계 — 반납 접수 ✓ / 구성품 확인 ✓ / 상태 점검 (진행 중) / 정산 확정 각 단계를 누르면 무엇을 보는지 펼쳐짐 3) 지금 단계 강조 — "상태 점검 중이에요. 오늘 18시쯤 끝날 것 같아요" 4) 안내 박스 — "점검이 끝나면 문자로 알려드려요. 보증금은 점검이 끝난 뒤 3영업일 안에 풀립니다" 5) 내가 반납할 때 올린 사진 다시 보기 6) 점검이 늦어지면 — "2영업일이 넘으면 자동으로 이상 없음 처리되고 보증금이 전액 환급됩니다" 라는 손님에게 유리한 규칙을 명시 기다리는 화면일수록 "언제 끝나는지"와 "안 끝나면 어떻게 되는지"를 적어야 한다.
  ```

### ST-02 · 정산 내역 - 연체·파손 청구
- **소속 메뉴**: 정산
- **뒤로가기**: 좌측 상단에 `‹ 점검 진행 중` — 누르면 `ST-01`로 간다
- **일정**: 2026-12-04 ~ 2026-12-09
- **기능정의**: 점검 결과 요약 · 청구 항목별 표(연체료·파손 배상·구성품 부족·세척비) · 항목마다 근거 사진과 계산식 · 보험으로 면책된 금액 · 청구 합계와 보증금에서 차감 · 보증금이 모자라면 추가 청구액 · 추가 청구 결제 방법 · 이의 제기하기 · 이의 제기 기한 안내 · 정산 확정되면 되돌릴 수 없다는 안내 · 정산서 내려받기
- **버튼 → 이동화면**:
  - `보증금 환급` → `ST-03` 보증금 환급
  - `이의 제기` → `CS-02` 1:1 문의
  - `대여 상세` → `MY-02` 대여 상세
- **생성 프롬프트**:

  ```
  점검이 끝나고 얼마를 정산하는지 보여주는 화면을 만들어줘. 1) 상단에 점검 결과 한 줄 — "폴대 1개 파손과 1일 연체가 확인됐어요" 2) 청구 항목 표 — 항목, 근거, 금액. 각 줄을 누르면 근거 사진과 계산식 펼침· 연체료 1일 — 46,300 × 30% × 1일 = 13,890원 [근거: 반납 접수 시각]· 폴대 1개 파손 — 기준 배상액 15,000원 [근거: 반납 사진 2장]· 세척비 — 흙 오염 20,000원 [근거: 사진] 3) 보험 면책 줄 — "파손 면책 보험으로 15,000원 면책" 초록 마이너스 줄 4) 합계 박스 — 청구 합계 33,890원 5) 보증금 정산 막대 그림 — 보증금 100,000원 중 33,890원 차감, 66,110원 환급 예정 6) 보증금이 모자란 경우의 표시 — "12,000원이 추가 청구됩니다" 와 [결제하기] 7) [이의 있어요] 버튼 — 누르면 이의 내용 적는 칸과 사진 올리는 칸 펼침. "정산 확정 후 7일 안에 이의를 제기할 수 있어요" 8) [정산서 내려받기] [정산 확정하기] 근거를 반드시 함께 보여준다. 금액만 적으면 손님은 납득하지 않는다.
  ```

### ST-03 · 보증금 환급
- **소속 메뉴**: 정산
- **뒤로가기**: 좌측 상단에 `‹ 점검 진행 중` — 누르면 `ST-01`로 간다
- **일정**: 2026-12-10 ~ 2026-12-14
- **기능정의**: 보증금 원금과 차감 내역 요약 · 돌려받을 금액 크게 · 환급 방법(카드 보류 해제·계좌 입금·적립금) · 방법별 걸리는 기간 안내 · 카드 보류 해제가 무엇인지 설명 · 적립금으로 받으면 더 주는 혜택 · 환급 진행 단계 표시 · 카드사 사정으로 늦어질 수 있다는 안내 · 언제까지 안 들어오면 어떻게 하는지 · 환급 내역서 받기
- **버튼 → 이동화면**:
  - `환급 완료` → `ST-04` 환급 완료
  - `정산 내역` → `ST-02` 정산 내역 - 연체·파손 청구
  - `문의하기` → `CS-02` 1:1 문의
- **생성 프롬프트**:

  ```
  보증금을 돌려받는 화면을 만들어줘. 1) 상단에 돌려받을 금액을 가장 크게 — "66,110원을 돌려드려요" 2) 계산 요약 — 보증금 100,000원 − 정산 33,890원 = 66,110원 (한 줄 식) 3) 환급 방법 라디오· 카드 한도 보류 해제 (기본) — 걸어둔 한도가 풀려요. 1~3영업일. "실제로 빠져나간 돈이 없어서 입금이 아니라 «한도 해제»입니다" 설명· 계좌로 입금 — 계좌 입력, 3~5영업일· 적립금으로 받기 — 5% 더 드려요 (69,415원), 즉시 4) 진행 단계 — 정산 확정 ✓ / 환급 요청 ✓ / 카드사 처리 중 / 완료 5) 안내 — "카드사 사정으로 며칠 더 걸릴 수 있어요. 7영업일이 지나도 안 풀리면 알려주세요" 와 [문의하기] 6) [환급 내역서 받기] "카드 한도 보류 해제"는 손님이 가장 헷갈려하는 개념이다. 입금이 아니라는 것을 반드시 설명한다.
  ```

### ST-04 · 환급 완료
- **소속 메뉴**: 정산
- **뒤로가기**: 좌측 상단에 `‹ 점검 진행 중` — 누르면 `ST-01`로 간다
- **일정**: 2026-12-15 ~ 2026-12-17
- **기능정의**: 환급 완료 안내와 금액 · 언제 어떤 방법으로 처리됐는지 · 이번 대여 전체 요약(빌린 것·기간·낸 돈·돌려받은 돈) · 영수증과 정산서 내려받기 · 후기 쓰기 유도와 적립금 혜택 · 같은 장비 다시 빌리기 · 다음에 쓸 쿠폰 · 이용 횟수와 보증금 면제까지 남은 횟수
- **버튼 → 이동화면**:
  - `후기 쓰기` → `MY-03` 후기 쓰기
  - `다시 빌리기` → `PD-02` 장비 상세
  - `홈` → `HO-01` 홈
- **생성 프롬프트**:

  ```
  보증금 환급이 끝난 화면을 만들어줘. 1) 상단에 초록 체크와 "66,110원 환급이 끝났어요" 2) 처리 내역 — 8월 20일 14:22, 신한카드 한도 보류 해제 3) 이번 대여 전체 요약 카드 — 빌린 장비, 기간, 낸 대여료, 정산 금액, 돌려받은 금액 4) [영수증 받기] [정산서 받기] 5) 후기 유도 박스 — "후기를 남기시면 적립금 3,000원을 드려요" 와 [후기 쓰기] 6) [이 장비 다시 빌리기] 버튼 7) 다음에 쓸 쿠폰 — "재이용 10% 쿠폰이 발급됐어요" 8) 단골 안내 — "3회 이용 중 2회 완료. 한 번 더 이용하면 보증금이 면제돼요" 진행 막대 끝나는 화면에서 다음 대여로 이어지게 만든다. 렌탈은 재이용이 생명이다.
  ```

### MY-01 · 내 대여 내역
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2026-12-18 ~ 2026-12-22
- **기능정의**: 상태 탭(전체·예약확정·이용중·반납접수·점검중·정산완료·취소) · 상태별 건수 배지 · 대여 카드 목록(장비 사진·이름·기간·상태·금액) · 이용 중인 것은 남은 시간 표시 · 연체는 붉게 맨 위로 · 점검 중인 것은 예상 완료 시각 · 보증금 상태 배지(보류중·환급완료) · 기간·장비명 검색 · 다음에 할 일 버튼(반납하기·후기쓰기·다시빌리기)
- **버튼 → 이동화면**:
  - `대여 상세` → `MY-02` 대여 상세
  - `반납 안내` → `RT-03` 반납 안내
  - `후기 쓰기` → `MY-03` 후기 쓰기
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
  - 검색 — 입력한 말이 든 것만 남깁니다. 없으면 「검색 결과가 없습니다」와 다시 찾는 길을 보여줍니다
- **생성 프롬프트**:

  ```
  내가 빌린 내역을 모아 보는 화면을 만들어줘. 1) 상단 상태 탭 — 전체 12 / 예약확정 1 / 이용중 1 / 반납접수 0 / 점검중 1 / 정산완료 8 / 취소 1. 각 탭에 건수 배지 2) 연체 중인 것이 있으면 맨 위에 붉은 카드로 고정 — "반납이 1일 늦었어요. 연체료 13,890원" 과 [지금 반납하기] 3) 대여 카드 목록 — 장비 사진, 이름과 수량, 기간, 상태 배지, 결제 금액, 보증금 상태 배지(보류중 / 환급완료) 4) 상태에 따라 카드 오른쪽 버튼이 다르게· 이용중 → 남은 시간과 [반납 안내]· 점검중 → "오늘 18시 완료 예정"· 정산완료 → [후기 쓰기] 또는 [다시 빌리기] 5) 기간 필터와 장비명 검색 상태가 많은 서비스다. 상태 배지 색을 일관되게 쓰고, 다음에 할 일을 카드마다 붙인다.
  ```

### MY-02 · 대여 상세
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 내 대여 내역` — 누르면 `MY-01`로 간다
- **일정**: 2026-12-23 ~ 2026-12-28
- **기능정의**: 대여번호와 상태 · 진행 단계 타임라인(신청·결제·수령·이용·반납·점검·정산·환급) · 빌린 장비와 구성품 · 기간과 실제 수령·반납 시각 · 결제 내역(대여료·배송비·할인·보증금) · 보증금 상태와 환급 예정일 · 수령 때와 반납 때 사진 견주기 · 계약서와 정산서 내려받기 · 상태에 따른 다음 행동 버튼 · 이 대여 문의하기
- **버튼 → 이동화면**:
  - `반납 안내` → `RT-03` 반납 안내
  - `연장하기` → `BK-06` 대여 기간 연장
  - `정산 내역` → `ST-02` 정산 내역 - 연체·파손 청구
- **생성 프롬프트**:

  ```
  대여 한 건의 자세한 내역 화면을 만들어줘. 1) 상단에 대여번호, 상태 배지, 장비 요약 2) 진행 타임라인 — 신청 ✓ 8/10 / 결제 ✓ 8/10 / 수령 ✓ 8/15 10:12 / 이용 중 / 반납 / 점검 / 정산 / 환급. 지난 단계는 시각까지, 남은 단계는 회색 3) 빌린 장비와 구성품 목록 4) 기간 — 예정과 실제를 나란히. 실제가 늦었으면 붉게 5) 결제 내역 표 — 대여료, 배송비, 할인, 소계 / 구분선 / 보증금(별도) 6) 보증금 상태 카드 — "신한카드에 100,000원 보류 중· 반납 후 3영업일 내 해제" 7) 사진 견주기 — 수령 때 4장과 반납 때 4장을 위아래로 나란히 8) [계약서 받기] [정산서 받기] 9) 상태에 따라 하단 버튼 — 이용중이면 [연장하기] [반납하기] 10) [이 대여 문의하기] 타임라인이 이 화면의 뼈대다. 지금 어디쯤인지가 한눈에 보여야 한다.
  ```

### MY-03 · 후기 쓰기
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 내 대여 내역` — 누르면 `MY-01`로 간다
- **일정**: 2026-12-29 ~ 2027-01-01
- **기능정의**: 빌린 장비 확인 · 전체 별점 주기 · 항목별 별점(상태·설명과 같은지·수령 편의·응대) · 후기 글 쓰기 · 사진 올리기 · 어떤 용도로 썼는지 태그 · 다음 사람에게 도움 될 팁 적기 · 후기 작성 적립금 안내 · 사진 올리면 더 주는 혜택 · 비공개로 쓰기 · 등록하기
- **버튼 → 이동화면**:
  - `등록 완료` → `MY-01` 내 대여 내역
  - `대여 상세` → `MY-02` 대여 상세
- **생성 프롬프트**:

  ```
  빌려 쓴 장비의 후기를 쓰는 화면을 만들어줘. 1) 상단에 빌린 장비 카드와 이용 기간 2) 전체 별점 — 큰 별 5개, 누르면 그 자리까지 채워지고 "만족해요" 같은 말이 함께 3) 항목별 별점 — 장비 상태 / 설명과 같은지 / 수령·반납 편의 / 응대 4) 후기 글 — 최소 20자 안내와 글자 수 표시 5) 사진 올리기 — 최대 5장. "사진을 올리면 적립금 2,000원을 더 드려요" 6) 어떤 용도였나요 태그 칩 — 가족캠핑 / 백패킹 / 차박 / 촬영 7) 다음 사람에게 도움 될 팁 — "설치가 생각보다 쉬웠어요" 같은 자유 입력 8) 적립금 안내 박스 — 기본 3,000원 + 사진 2,000원 9) 비공개로 쓰기 체크 10) [등록하기] 렌탈 후기는 "장비 상태가 사진과 같았나"가 가장 중요하다. 그 항목을 따로 뺀 이유다.
  ```

### MY-04 · 알림·쿠폰
- **소속 메뉴**: 마이페이지
- **뒤로가기**: 좌측 상단에 `‹ 내 대여 내역` — 누르면 `MY-01`로 간다
- **일정**: 2027-01-04 ~ 2027-01-06
- **기능정의**: 받을 알림 켜고 끄기(수령 안내·반납 하루 전·반납 3시간 전·연체·점검 완료·환급 완료) · 알림 받을 방법(문자·카톡·이메일) · 자리 나면 알림 신청 목록 · 보유 쿠폰 목록과 남은 기간 · 쿠폰 탭(사용가능·사용완료·기간만료) · 적립금 잔액과 쌓인 내역 · 적립금 쓰는 법 안내 · 보증금 면제까지 남은 횟수 · 쿠폰 등록하기
- **버튼 → 이동화면**:
  - `대여 내역` → `MY-01` 내 대여 내역
  - `장비 둘러보기` → `PD-01` 장비 목록
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
  - 끄기 — 누르면 **켜짐·꺼짐이 눈에 보이게** 바뀌고 그대로 남습니다
- **생성 프롬프트**:

  ```
  알림 설정과 쿠폰·적립금을 한곳에서 보는 화면을 만들어줘. 1) 알림 설정 표 — 무엇을 / 문자 / 카톡 / 이메일 토글 3열 수령 안내 / 반납 하루 전 / 반납 3시간 전 / 연체 / 점검 완료 / 환급 완료 "반납 알림은 끄지 않는 것을 권해요" 한 줄 2) 자리 나면 알림 신청 목록 — 신청한 장비와 기간, 대기 순번, [취소] 3) 쿠폰 탭 — 사용가능 3 / 사용완료 5 / 기간만료 2 4) 쿠폰 카드 목록 — 할인율, 이름, 조건(5만원 이상), 남은 기간("3일 남음" 붉게) 5) 쿠폰 등록 — 코드 입력칸과 [등록] 6) 적립금 카드 — 잔액, 이번 달 소멸 예정액, [쌓인 내역 보기] 7) 단골 진행 — "3회 이용 중 2회. 한 번 더 이용하면 보증금이 면제돼요" 막대 반납 알림을 끄지 말라고 권하는 것이 서로에게 이롭다.
  ```

### CS-01 · 자주 묻는 질문
- **소속 메뉴**: 고객센터
- **뒤로가기**: 없음 (메뉴의 첫 화면 — GNB로 오간다)
- **일정**: 2027-01-07 ~ 2027-01-11
- **기능정의**: 질문 검색창 · 분류 탭(대여·보증금·수령배송·반납·연체파손·환불) · 많이 찾는 질문 5개 먼저 · 질문 아코디언 목록 · 답 안에서 관련 화면으로 바로 가기 · 도움이 됐는지 묻기 · 원하는 답이 없을 때 문의하기 · 고객센터 운영시간과 전화번호
- **버튼 → 이동화면**:
  - `문의하기` → `CS-02` 1:1 문의
  - `약관·규정` → `CS-03` 대여 약관·규정
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 분류 — 고른 조건에 **맞는 것만 남기고** 나머지는 감춥니다. 하나도 안 남으면 「결과가 없습니다」를 보여줍니다
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
  - 검색 — 입력한 말이 든 것만 남깁니다. 없으면 「검색 결과가 없습니다」와 다시 찾는 길을 보여줍니다
  - 아코디언 — 누르면 그 자리에서 펴지고 접힙니다. 다른 화면으로 가지 않습니다
- **생성 프롬프트**:

  ```
  자주 묻는 질문 화면을 만들어줘. 1) 상단 검색창 — "무엇이 궁금하세요?" 2) 분류 탭 — 전체 / 대여 / 보증금 / 수령·배송 / 반납 / 연체·파손 / 환불 3) "많이 찾는 질문" 5개를 먼저 — 렌탈에서 실제로 자주 묻는 것으로· 보증금은 언제 돌려받나요?· 반납이 늦으면 얼마가 붙나요?· 비 맞은 장비는 어떻게 반납하나요?· 카드 한도 보류가 무슨 뜻인가요?· 예약한 날짜를 바꿀 수 있나요? 4) 질문 아코디언 — 누르면 답이 펼쳐지고, 답 안에 관련 화면 바로가기 버튼 5) 답 아래 "도움이 되셨나요?" 👍👎 6) 하단 — "원하는 답이 없으신가요?" [1:1 문의] 와 운영시간·전화번호 렌탈은 보증금과 연체 질문이 압도적으로 많다. 그 둘을 맨 위에 둔다.
  ```

### CS-02 · 1:1 문의
- **소속 메뉴**: 고객센터
- **뒤로가기**: 좌측 상단에 `‹ 자주 묻는 질문` — 누르면 `CS-01`로 간다
- **일정**: 2027-01-12 ~ 2027-01-14
- **기능정의**: 문의 종류 고르기(대여·보증금·정산 이의·파손·배송·기타) · 관련 대여 건 고르기와 정보 자동 채움 · 문의 내용 쓰기 · 사진 올리기 · 답변 받을 방법(문자·이메일) · 답변까지 걸리는 시간 안내 · 정산 이의는 기한이 있다는 안내 · 지난 문의 내역과 답변 · 급하면 전화 안내 · 문의 보내기
- **버튼 → 이동화면**:
  - `보내기` → `CS-01` 자주 묻는 질문
  - `대여 내역` → `MY-01` 내 대여 내역
- **생성 프롬프트**:

  ```
  1:1 문의 화면을 만들어줘. 1) 문의 종류 드롭다운 — 대여 관련 / 보증금 / 정산 이의 / 파손·분실 / 배송 / 기타 2) "정산 이의"를 고르면 주의 박스가 뜸 — "정산 확정 후 7일 안에만 이의를 제기할 수 있어요. 남은 기간 5일" 3) 관련 대여 건 고르기 드롭다운 — 고르면 장비명·기간·대여번호가 자동으로 채워짐 4) 문의 내용 자유 입력 (최소 10자) 5) 사진 올리기 최대 5장 — "파손이나 정산 문의는 사진이 있으면 훨씬 빨라요" 6) 답변 받을 방법 — 문자 / 이메일 체크 7) 안내 — "평일 기준 1영업일 안에 답변드려요. 급하시면 02-000-0000" 8) 하단 [문의 보내기] 9) 아래에 지난 문의 목록 — 제목, 종류, 상태(답변대기·답변완료), 날짜 정산 이의는 «기한»이 있다. 그 기한을 화면에서 알려주지 않으면 손님이 권리를 잃는다.
  ```

### CS-03 · 대여 약관·규정
- **소속 메뉴**: 고객센터
- **뒤로가기**: 좌측 상단에 `‹ 자주 묻는 질문` — 누르면 `CS-01`로 간다
- **일정**: 2027-01-15 ~ 2027-01-20
- **기능정의**: 한 줄 요약으로 먼저 보여주기 · 조항 탭(대여·보증금·연체·파손배상·취소환불·개인정보) · 연체료 계산 방식과 단계표 · 부품별 파손 배상 기준표 · 취소 시점별 환불율 단계표 · 무료 취소 기한 계산 예시 · 반납 상태 기준(세척·건조) · 분실 처리 기준 · 내 대여로 계산해보기 · 규정 바뀐 이력 · 약관 내려받기
- **버튼 → 이동화면**:
  - `자주 묻는 질문` → `CS-01` 자주 묻는 질문
  - `대여 내역` → `MY-01` 내 대여 내역
  - `문의하기` → `CS-02` 1:1 문의
- **눌렀을 때 화면 안에서 끝나는 것** — 여기가 «포스터»가 되기 제일 쉬운 자리입니다:
  - 탭 — 고른 탭의 내용만 보여줍니다. 탭을 옮겨도 **화면 주소는 그대로**입니다(뒤로가기가 탭에 끼어들면 안 됩니다)
- **생성 프롬프트**:

  ```
  대여 약관과 규정을 한곳에서 설명하는 화면을 만들어줘. 1) 상단에 "세 가지만 기억하세요" 요약 카드 3장· 늦으면 하루 대여료의 30%· 망가뜨리면 부품 기준액 (미리 알리면 조정 가능)· 보증금은 반납 확인 후 3영업일 안에 2) 조항 탭 — 대여 / 보증금 / 연체 / 파손 배상 / 취소·환불 / 개인정보 3) 연체 탭 — 단계 막대와 표. 1일 30% / 2일 60% / 3일 이상 100% / 7일 분실 처리 4) 파손 배상 탭 — 부품별 기준액 표. 폴대 15,000 / 팩 1,000 / 원단 찢어짐 40,000 / 지퍼 25,000 / 분실 정가 100%. "수리로 되면 수리비만" 이라는 원칙 명시 5) 취소·환불 탭 — 수령 며칠 전이냐에 따른 환불율 단계 막대 (3일 전 100% / 1일 전 50% / 당일 취소 불가) 와 날짜를 넣은 계산 예시 6) 반납 상태 탭 — 이렇게 반납해 주세요 / 이러면 추가 비용 사진 견주기 7) [내 대여로 계산해보기] 버튼 — 누르면 내 대여 기준 금액이 계산돼 뜸 8) 규정 바뀐 이력과 [약관 내려받기] 규정 문서처럼 딱딱하지 않게, 표와 예시를 앞세워 읽히게 써줘. 요약 카드 3장을 맨 앞에 두는 것이 핵심이다.
  ```

## 5. 화면 이동 흐름 (FLOW)
- `홈`(HO-01) —[장비 둘러보기]→ `장비 목록`(PD-01)
- `홈`(HO-01) —[기간으로 찾기]→ `기간으로 찾기 - 결과`(HO-03)
- `홈`(HO-01) —[장비 상세]→ `장비 상세`(PD-02)
- `홈`(HO-01) —[패키지 보기]→ `세트 패키지`(PD-06)
- `카테고리 홈`(HO-02) —[장비 목록]→ `장비 목록`(PD-01)
- `카테고리 홈`(HO-02) —[장비 상세]→ `장비 상세`(PD-02)
- `카테고리 홈`(HO-02) —[이용 안내]→ `대여 약관·규정`(CS-03)
- `기간으로 찾기 - 결과`(HO-03) —[장비 상세]→ `장비 상세`(PD-02)
- `기간으로 찾기 - 결과`(HO-03) —[결과 없음]→ `결과 없음 - 그 기간에 빈 장비가 없음`(HO-04)
- `기간으로 찾기 - 결과`(HO-03) —[재고 달력]→ `날짜별 가용 재고 달력`(PD-03)
- `결과 없음 - 그 기간에 빈 장비가 없음`(HO-04) —[기간 다시 고르기]→ `기간으로 찾기 - 결과`(HO-03)
- `결과 없음 - 그 기간에 빈 장비가 없음`(HO-04) —[장비 둘러보기]→ `장비 목록`(PD-01)
- `기획전·특가`(HO-05) —[장비 상세]→ `장비 상세`(PD-02)
- `기획전·특가`(HO-05) —[장비 목록]→ `장비 목록`(PD-01)
- `기획전·특가`(HO-05) —[종료된 기획전]→ `기획전·특가`(HO-05)
- `장비 목록`(PD-01) —[장비 상세]→ `장비 상세`(PD-02)
- `장비 목록`(PD-01) —[재고 달력]→ `날짜별 가용 재고 달력`(PD-03)
- `장비 목록`(PD-01) —[패키지]→ `세트 패키지`(PD-06)
- `장비 상세`(PD-02) —[재고 달력]→ `날짜별 가용 재고 달력`(PD-03)
- `장비 상세`(PD-02) —[장바구니 담기]→ `대여 장바구니`(CT-01)
- `장비 상세`(PD-02) —[바로 빌리기]→ `대여 신청서`(BK-01)
- `장비 상세`(PD-02) —[그 날짜 마감]→ `대여 불가 - 그 날짜 마감`(PD-05)
- `날짜별 가용 재고 달력`(PD-03) —[이 기간으로 예약]→ `대여 신청서`(BK-01)
- `날짜별 가용 재고 달력`(PD-03) —[장비 상세로]→ `장비 상세`(PD-02)
- `날짜별 가용 재고 달력`(PD-03) —[그 날짜 마감]→ `대여 불가 - 그 날짜 마감`(PD-05)
- `옵션·부속품 고르기`(PD-04) —[장바구니 담기]→ `대여 장바구니`(CT-01)
- `옵션·부속품 고르기`(PD-04) —[바로 빌리기]→ `대여 신청서`(BK-01)
- `옵션·부속품 고르기`(PD-04) —[장비 상세]→ `장비 상세`(PD-02)
- `대여 불가 - 그 날짜 마감`(PD-05) —[재고 달력]→ `날짜별 가용 재고 달력`(PD-03)
- `대여 불가 - 그 날짜 마감`(PD-05) —[다른 장비 보기]→ `장비 목록`(PD-01)
- `세트 패키지`(PD-06) —[담기]→ `대여 장바구니`(CT-01)
- `세트 패키지`(PD-06) —[바로 빌리기]→ `대여 신청서`(BK-01)
- `세트 패키지`(PD-06) —[재고 달력]→ `날짜별 가용 재고 달력`(PD-03)
- `대여 장바구니`(CT-01) —[신청하기]→ `대여 신청서`(BK-01)
- `대여 장바구니`(CT-01) —[기간 바꾸기]→ `기간 한꺼번에 바꾸기`(CT-02)
- `대여 장바구니`(CT-01) —[빈 장바구니]→ `빈 장바구니`(CT-03)
- `기간 한꺼번에 바꾸기`(CT-02) —[장바구니]→ `대여 장바구니`(CT-01)
- `기간 한꺼번에 바꾸기`(CT-02) —[신청하기]→ `대여 신청서`(BK-01)
- `빈 장바구니`(CT-03) —[장비 둘러보기]→ `장비 목록`(PD-01)
- `빈 장바구니`(CT-03) —[홈]→ `홈`(HO-01)
- `대여 신청서`(BK-01) —[결제하기]→ `보증금·결제`(BK-02)
- `대여 신청서`(BK-01) —[약관 보기]→ `대여 약관·규정`(CS-03)
- `대여 신청서`(BK-01) —[장바구니로]→ `대여 장바구니`(CT-01)
- `보증금·결제`(BK-02) —[결제 완료]→ `대여 확정 완료`(BK-03)
- `보증금·결제`(BK-02) —[결제 실패]→ `결제 실패`(BK-04)
- `보증금·결제`(BK-02) —[신청서로]→ `대여 신청서`(BK-01)
- `대여 확정 완료`(BK-03) —[대여 내역]→ `내 대여 내역`(MY-01)
- `대여 확정 완료`(BK-03) —[수령 확인]→ `수령 확인`(RT-01)
- `대여 확정 완료`(BK-03) —[홈]→ `홈`(HO-01)
- `결제 실패`(BK-04) —[다시 결제]→ `보증금·결제`(BK-02)
- `결제 실패`(BK-04) —[신청서로]→ `대여 신청서`(BK-01)
- `결제 실패`(BK-04) —[문의하기]→ `1:1 문의`(CS-02)
- `대여 계약 동의·서명`(BK-05) —[서명 완료]→ `대여 확정 완료`(BK-03)
- `대여 계약 동의·서명`(BK-05) —[약관 전문]→ `대여 약관·규정`(CS-03)
- `대여 기간 연장`(BK-06) —[연장 결제]→ `보증금·결제`(BK-02)
- `대여 기간 연장`(BK-06) —[대여 상세]→ `대여 상세`(MY-02)
- `대여 기간 연장`(BK-06) —[연체 안내]→ `연체 안내`(RT-05)
- `수령 확인`(RT-01) —[확인 완료]→ `이용 중`(RT-02)
- `수령 확인`(RT-01) —[대여 상세]→ `대여 상세`(MY-02)
- `이용 중`(RT-02) —[연장하기]→ `대여 기간 연장`(BK-06)
- `이용 중`(RT-02) —[반납 안내]→ `반납 안내`(RT-03)
- `이용 중`(RT-02) —[파손 신고]→ `파손·분실 신고`(RT-06)
- `반납 안내`(RT-03) —[반납 접수]→ `반납 접수`(RT-04)
- `반납 안내`(RT-03) —[이용 중]→ `이용 중`(RT-02)
- `반납 안내`(RT-03) —[연체 안내]→ `연체 안내`(RT-05)
- `반납 접수`(RT-04) —[접수 완료]→ `점검 진행 중`(ST-01)
- `반납 접수`(RT-04) —[파손 신고]→ `파손·분실 신고`(RT-06)
- `반납 접수`(RT-04) —[반납 안내]→ `반납 안내`(RT-03)
- `연체 안내`(RT-05) —[지금 반납]→ `반납 접수`(RT-04)
- `연체 안내`(RT-05) —[정산 내역]→ `정산 내역 - 연체·파손 청구`(ST-02)
- `연체 안내`(RT-05) —[문의하기]→ `1:1 문의`(CS-02)
- `파손·분실 신고`(RT-06) —[신고 접수]→ `정산 내역 - 연체·파손 청구`(ST-02)
- `파손·분실 신고`(RT-06) —[반납 접수]→ `반납 접수`(RT-04)
- `파손·분실 신고`(RT-06) —[이용 중]→ `이용 중`(RT-02)
- `점검 진행 중`(ST-01) —[정산 내역]→ `정산 내역 - 연체·파손 청구`(ST-02)
- `점검 진행 중`(ST-01) —[대여 상세]→ `대여 상세`(MY-02)
- `정산 내역 - 연체·파손 청구`(ST-02) —[보증금 환급]→ `보증금 환급`(ST-03)
- `정산 내역 - 연체·파손 청구`(ST-02) —[이의 제기]→ `1:1 문의`(CS-02)
- `정산 내역 - 연체·파손 청구`(ST-02) —[대여 상세]→ `대여 상세`(MY-02)
- `보증금 환급`(ST-03) —[환급 완료]→ `환급 완료`(ST-04)
- `보증금 환급`(ST-03) —[정산 내역]→ `정산 내역 - 연체·파손 청구`(ST-02)
- `보증금 환급`(ST-03) —[문의하기]→ `1:1 문의`(CS-02)
- `환급 완료`(ST-04) —[후기 쓰기]→ `후기 쓰기`(MY-03)
- `환급 완료`(ST-04) —[다시 빌리기]→ `장비 상세`(PD-02)
- `환급 완료`(ST-04) —[홈]→ `홈`(HO-01)
- `내 대여 내역`(MY-01) —[대여 상세]→ `대여 상세`(MY-02)
- `내 대여 내역`(MY-01) —[반납 안내]→ `반납 안내`(RT-03)
- `내 대여 내역`(MY-01) —[후기 쓰기]→ `후기 쓰기`(MY-03)
- `대여 상세`(MY-02) —[반납 안내]→ `반납 안내`(RT-03)
- `대여 상세`(MY-02) —[연장하기]→ `대여 기간 연장`(BK-06)
- `대여 상세`(MY-02) —[정산 내역]→ `정산 내역 - 연체·파손 청구`(ST-02)
- `후기 쓰기`(MY-03) —[등록 완료]→ `내 대여 내역`(MY-01)
- `후기 쓰기`(MY-03) —[대여 상세]→ `대여 상세`(MY-02)
- `알림·쿠폰`(MY-04) —[대여 내역]→ `내 대여 내역`(MY-01)
- `알림·쿠폰`(MY-04) —[장비 둘러보기]→ `장비 목록`(PD-01)
- `자주 묻는 질문`(CS-01) —[문의하기]→ `1:1 문의`(CS-02)
- `자주 묻는 질문`(CS-01) —[약관·규정]→ `대여 약관·규정`(CS-03)
- `1:1 문의`(CS-02) —[보내기]→ `자주 묻는 질문`(CS-01)
- `1:1 문의`(CS-02) —[대여 내역]→ `내 대여 내역`(MY-01)
- `대여 약관·규정`(CS-03) —[자주 묻는 질문]→ `자주 묻는 질문`(CS-01)
- `대여 약관·규정`(CS-03) —[대여 내역]→ `내 대여 내역`(MY-01)
- `대여 약관·규정`(CS-03) —[문의하기]→ `1:1 문의`(CS-02)

## 6. 빌드 가이드
### 구조
- 각 **화면ID**를 하나의 라우트/페이지로 만드세요 (예: `PCHOME0110` → `/home`).
- 화면명은 페이지 제목·컴포넌트 이름의 기준으로 사용하세요.
- **2장 공통 요소**(헤더·GNB·푸터)를 공용 레이아웃으로 만들고 모든 화면이 감싸도록 하세요.
- **쓰는 사람이 다른 메뉴는 헤더를 나누세요.** 손님이 쓰는 메뉴(둘러보기·내 내역)와 운영자가 쓰는 메뉴(운영 관리·정산)를 **한 헤더에 나란히 놓지 마세요.** 둘은 사실상 다른 사이트입니다.
  · 손님 영역과 운영자 영역을 각각 다른 레이아웃(헤더·사이드바)으로 만들고, 오갈 때는 **따로 마련한 진입점** 하나로만 이동하세요(예: 우측 상단 `운영 화면으로`, 운영자 화면에서는 `고객 화면 보기`).
  · 운영자 영역은 보통 좌측 사이드바 + 상단 바가 맞습니다. 손님 영역의 검색·카테고리 내비를 그대로 쓰지 마세요.
- **버튼 → 이동화면** 관계를 링크/네비게이션으로 연결하세요.
- **4장 각 화면의 `눌렀을 때 (이 화면 안에서 끝남)` 항목을 그대로 만드세요.** 적혀 있는 것은 빠짐없이 실제로 동작해야 합니다 — 누르면 값이 바뀌고, 다시 누르면 되돌아옵니다.
  · 그 항목에 없더라도 **누를 수 있게 생긴 것은 전부 눌리게** 만드세요. 적힌 것은 최소한이지 전부가 아닙니다.
- **누를 수 있게 보이는 것은 전부 눌리게 만드세요.** 두 종류로 나눠 처리합니다.
  · **화면 안에서 끝나는 조작은 진짜로 동작해야 합니다** — 탭 전환, 좌측 카테고리·필터 선택, 목록에서 항목 고르기(디자이너·옵션·시간), 펼치기·접기, 캘린더 이전/다음 달, 수량 ±, 체크·토글. 자바스크립트로 실제 상태를 바꾸고 화면을 다시 그리세요. 이걸 안내 문구로 때우지 마세요.
  · **서버가 있어야 하는 것만** 짧은 안내(토스트)로 대신합니다 — 저장·삭제·발송·결제·전화 걸기·이의 신청 등.
  · **이런 것은 토스트로 때우지 마세요** — `모두 보기`(펼치기), `선택`·`적용`(고른 상태로 바꾸기), `추가`(줄 하나 늘리기), `닫기`·`✕`(정말 닫기), `도움이 됐어요`(숫자 올리기), 탭·필터·정렬. 전부 화면 안에서 끝나는 일이라 실제로 되어야 합니다.
  · **모달·팝업은 뜨는 것만으로 끝이 아닙니다.** `취소`와 `✕`는 정말 닫히고, `확인`은 눌렀을 때 무엇이 달라지는지 보여야 합니다(다음 화면으로 가거나, 목록이 바뀌거나, 안내가 뜨거나). 닫히지 않는 팝업은 갇힌 화면이 됩니다.
  · 아무 것도 하지 않는 `<button>`을 남기지 마세요. 만들고 나서 **버튼을 하나씩 눌러 보고**, 반응이 없는 것이 있으면 위 둘 중 하나로 채우세요.
- **뒤로가기는 화면마다 정해져 있습니다. 4장 각 화면 스펙의 `뒤로가기` 항목을 그대로 만드세요.** 29개 화면에 붙고, 나머지 8개(메뉴 첫 화면)에는 없습니다. 좌측 상단에 `‹ 상위 화면 이름`과 현재 위치(예: `매장 탐색 › 매장 상세`)를 둡니다.
  · 여러 단계짜리 흐름(1→2→3)은 **이전 단계로 돌아갈 수 있게** 하세요. 되돌아갔을 때 앞서 고른 값이 남아 있어야 합니다.
  · **한 묶음으로 나란히 보여주는 것은 동일 계위입니다.** 탭 줄에 걸린 화면들, 같은 사이드바에 나란히 올라온 화면들이 그렇습니다. 그 안을 오가는 길이 이미 탭·사이드바이므로 뒤로가기를 겹쳐 두지 마세요 — 같은 줄에 있는 화면인데 어떤 건 있고 어떤 건 없어집니다.
  · 빈 화면·오류·마감처럼 막다른 화면에는 **다음에 할 일 버튼**을 반드시 하나 두세요(예: `목록으로`, `다시 시도`, `둘러보기`).
  · **전체 화면 목록으로 돌아가는 길**을 **146개 화면 «전부»에** 두세요 — 오른쪽 아래에 떠 있는 `화면 목록` 단추 하나면 됩니다. 누르면 `index.html`(전체 화면 목록)으로 갑니다. 접힌 패널 안에 `화면 정보` 같은 이름으로 넣으면 아무도 못 찾습니다.
  · 다 만든 뒤 **화면을 하나씩 열어 뒤로가기가 있는지 세어 보세요.** 하나라도 빠져 있으면 채웁니다.
### 화면 구현
- 각 화면의 **생성 프롬프트**를 그 화면 구현 지시로 그대로 사용하세요.
- 반복 UI(카드·리스트 행·폼·버튼)는 **공용 컴포넌트**로 만들어 재사용하세요.
- 필요하면 상태 화면(로딩·빈 결과·에러)도 함께 만드세요.
### 디자인
- **디자인 프리셋 파일을 함께 넣었다면 그것이 우선입니다.** 1장의 디자인 컨셉이나 화면별 프롬프트에 다른 색·톤이 적혀 있어도 프리셋을 따르세요. 충돌이 아니라 **프리셋이 정답**입니다.
- 프리셋의 **레이아웃 골격**(히어로·목록·내비·상세를 어떻게 놓을지)을 먼저 잡고 색을 입히세요. 색만 맞추면 어떤 테마로 만들어도 같은 화면이 나옵니다.
- 프리셋이 없다면 1장의 **디자인 컨셉**을 전역 스타일(색·톤·라운드·여백)로 토큰화해 일관되게 적용하세요.
- **글꼴은 프리셋이 없어도 한 벌로 고정하세요.** 기본은 `Paperlogy`이고, 못 불러오면 `Pretendard` → `Noto Sans KR` 순으로 물러납니다. CSS로는 `font-family: Paperlogy, Pretendard, 'Noto Sans KR', sans-serif` 한 줄이면 됩니다.
  · **글꼴은 한 벌만 씁니다.** 제목과 본문에 서로 다른 글꼴을 쓰지 마세요 — 두 벌이 되는 순간 어디에 무엇을 쓸지가 화면마다 갈립니다. 위계는 글꼴이 아니라 **크기와 굵기**로 만듭니다.
  · 굵기는 **400·600·700 셋만**, 크기는 본문 15 · 카드 제목 18 · 섹션 제목 22 · 페이지 제목 32을 기준으로 잡으세요.

#### 좁은 화면에서 넘치지 않게

폰 폭(**390px**)에서 **페이지가 좌우로 스크롤되면 안 됩니다.**
표·그림처럼 일부러 옆으로 넘기는 것은 «자기 상자 안에서만» 넘깁니다.

**왜 적어 두나** — 열 수(1024에서 2열, 720에서 1열)는 다들 맞추는데, 가로로 밀리는지는 잘 안 봅니다.
그리고 미는 것은 대개 표가 아닙니다. **표 밑의 페이지 번호 줄**과 「다음 프로그램 ›」 같은
이동 단추가 한 줄로 늘어서면서 밉니다 — 표는 감쌌는데 그 옆은 안 감싸기 때문입니다.

- 폰 폭(390px)에서 **페이지가 좌우로 스크롤되면 안 됩니다.** 표·그림처럼 일부러 옆으로 넘기는 것은 자기 상자 안에서만 넘깁니다.
- **페이지 번호 줄**은 줄바꿈하거나(`flex-wrap: wrap`) 자기 상자 안에서 옆으로 돌립니다(`overflow-x: auto`). 쪽수가 늘면 그만큼 길어지므로 한 줄로 두면 반드시 넘칩니다.
- **「이전 · 다음」 같은 이동 단추**는 줄바꿈을 허용하고, 글자가 길면 줄여 적습니다(`다음 프로그램 ›` → `다음 ›`).
- **표**는 `overflow-x: auto` 상자로 감쌉니다. 표 자체를 좁히지 않습니다 — 칸 글자가 세로로 접혀 읽을 수 없게 됩니다.
- **탭 줄·필터 줄**도 같습니다. 개수가 늘어나는 줄은 전부 줄바꿈이나 상자 안 스크롤 중 하나를 씁니다.
- ⛔ 옆으로 흘려보낼 때는 **다른 축을 반드시 막습니다** — `overflow: auto hidden`. `overflow-x` 만 열면 브라우저가 세로도 함께 열어, 테두리 1px 때문에 세로 스크롤바가 생깁니다.
- ⛔ **푸터는 화면 바닥에 붙입니다.** 내용이 짧은 화면(완료·빈 목록·오류)에서 푸터가 화면 가운데 떠 있으면 «덜 만들어진 화면»으로 보입니다. `body` 를 세로 flex 로 두고 푸터에 `margin-top: auto` 를 줍니다.
- ⛔ **`[hidden]` 을 쓸 때는 `[hidden] { display: none !important; }` 을 반드시 함께 넣습니다.** `[hidden]` 은 브라우저 기본값이라 `.row { display: flex }` 같은 규칙 하나에도 집니다. 그러면 «거르는 단추를 눌러도 목록이 안 줄어드는» 화면이 됩니다 — 숫자 안내만 바뀌고 목록은 그대로라, 눌러 보지 않으면 안 보입니다.
- ⛔ **그리드·플렉스 칸의 자식에는 `min-width: 0` 을 줍니다.** 기본값이 `auto` 라, 칸을 340px 로 정해도 내용이 그보다 넓으면 칸 밖으로 삐져나와 옆 칸을 덮습니다.
- ⛔ **여백을 지우는 클래스(`flush` 등)와 음수 마진을 «같이» 쓰지 않습니다.** 상쇄할 여백이 이미 없는데 음수 마진을 걸면 그만큼 넓어져 칸 밖으로 나갑니다.

```css
/* 개수가 늘어나는 줄은 접거나, 자기 상자 안에서 돌린다 */
.pager { display: flex; flex-wrap: wrap; justify-content: center; gap: 4px; }
.tabs, .filters { display: flex; flex-wrap: wrap; gap: 8px; }
.tabs-scroll { overflow: auto hidden; }  /* 탭을 옆으로 흘릴 때도 마찬가지 */

/* 줄바꿈 대신 «옆으로 흘려보낼» 때는 반드시 두 축을 다 적는다 */
.scroll-x { overflow: auto hidden; }   /* overflow-x 만 쓰면 세로 스크롤바가 생긴다 */

/* 표는 좁히지 말고 감싼다 — 좁히면 칸 글자가 세로로 접혀 못 읽는다 */
.table-wrap { overflow: auto hidden; }   /* x 만 열면 세로 스크롤바가 딸려 온다 */
.table-wrap table { min-width: max-content; }

/* 좌우로 벌리는 줄에 긴 단추가 들어오면 내려보낸다 */
.row-between { display: flex; flex-wrap: wrap; align-items: center; justify-content: space-between; gap: 12px; }

/* 푸터는 «화면 바닥»에 붙인다 — 짧은 화면에서 가운데 떠 있으면 안 만들어진 것처럼 보인다 */
body { min-height: 100dvh; display: flex; flex-direction: column; }
body > main { flex: 1 0 auto; }
body > footer { margin-top: auto; }

/* ⛔⛔ 거르기가 목록을 못 줄이는 사고는 거의 다 여기서 난다.
   [hidden] 은 «브라우저 기본값»이라 .row{display:flex} 한 줄에도 진다.
   숫자 안내는 바뀌는데 목록만 그대로인 화면이 만들어진다. */
[hidden] { display: none !important; }

/* ⛔ 위 푸터 규칙은 «body 에만» 씁니다. 감싸개(.app 같은 것)에 옮겨 붙이지 마세요.
   사이드바형(감싸개가 grid 2열)에 flex-direction:column 을 덮었더니
   사이드바가 화면 폭을 다 먹고 본문이 그 아래로 밀렸습니다(2026-08-19에 실제로 그랬습니다).
   감싸개는 «제 짜임 그대로» 둡니다 — 사이드바형이면 grid 2열, 상단바형이면 세로 flex.
   짜임이 다른 화면 두 가지를 꼭 같이 열어 보세요. 한쪽에서 되는 것이 다른 쪽을 무너뜨립니다. */

/* 사진 위에 얹는 단추(카드 위 찜 하트·빠른 담기)는 사진에 덮여 안 눌립니다.
   «얹은 단추에만» 아래 둘을 주세요 — 줄 안에 서는 단추에 주면 자리가 튑니다. */
.card { position: relative; }              /* 얹을 자리의 기준점 */
.card > .overlay-btn { position: absolute; z-index: 2; }

/* 그리드·플렉스 자식은 min-width 를 0 으로 — 안 그러면 칸보다 넓은 내용이 삐져나온다 */
.split-r > *, .split-l > *, .app > * { min-width: 0; }
```

#### 읽기 폭 — 글을 한 단으로 좁힐 때

글이 주인공인 자리(상세 본문·안내·약관)는 **최대 760px** 로 좁히고 가운데 정렬합니다.
콘텐츠 영역(위의 최대 폭) **안에서** 좁히는 것입니다 — 바깥 폭을 바꾸는 게 아닙니다.

**왜 좁히나** — 한 줄이 길면 다음 줄 첫 글자를 눈이 못 찾습니다. 1440px 폭에 본문을 꽉 채우면
한 줄에 90자가 넘어가고, 읽는 사람은 두세 줄마다 같은 줄을 다시 읽게 됩니다.

- **이 값 하나만 씁니다.** 화면마다 680·720·760 처럼 다르게 잡지 마세요 — 저희도 그렇게 적혀 있었고,
  같은 팩 안의 상세 화면끼리 본문 폭이 달라 서로 다른 사이트처럼 보였습니다.
- **카드 격자에는 쓰지 않습니다.** 여기는 «글»을 좁히는 값입니다. 카드를 몇 열로 놓을지는 레이아웃이 정합니다.
- 사진·표·코드처럼 넓어야 읽히는 것은 이 폭을 넘겨도 됩니다. 좁히는 건 글줄입니다.

#### 줄어드는 지점 — 두 곳만 씁니다

화면이 좁아질 때 칸이 줄어드는 지점은 **1024px** 와 **720px**, 이 둘뿐입니다.
1024 은 태블릿 가로, 720 은 세로입니다. 720px 아래에서는 좌우 여백도 16px 로 줄입니다.

- **여기 없는 지점을 새로 만들지 마세요.** 560·860·520 처럼 지어낸 지점이 섞이면, 같은 팩으로 만든 화면끼리 줄어드는 자리가 달라 서로 다른 사이트처럼 보입니다.
- 더 잘게 나눠도 사람 눈에는 안 보입니다. 대신 **지킬 곳만 늘어납니다.**
- 두 지점에서 열이 어떻게 줄어드는지는 위 레이아웃 표에 적혀 있습니다(예: 3열 → 2열 → 1열).

#### 격자 — 카드를 늘어놓는 자리

카드·타일을 격자로 늘어놓을 때 **좌우와 위아래를 다른 값으로** 벌립니다.

| 자리 | 값 |
| --- | --- |
| **카드와 카드 사이** (좌우) | 위 눈금의 「카드와 카드 사이 (가로)」 |
| **줄과 줄 사이** (위아래) | 위 눈금의 「덩어리와 덩어리 사이」 |

**같은 값을 쓰면 줄이 안 갈립니다.** 카드에 상자가 있을 때는 테두리가 경계를 만들어 줬지만,
상자를 걷어낸 뒤로는 **위 카드의 가격과 아래 카드의 사진이 붙어** 한 덩어리로 읽힙니다.
위아래를 더 벌려야 "여기까지가 한 줄"이 보입니다.

| 나누는 것 | 정하는 곳 |
| --- | --- |
| 몇 열로 나눌지 | 레이아웃 프리셋 |
| **사이를 얼마나 벌릴지** | **위 간격 눈금 (좌우·위아래 따로)** |
| 카드가 어떻게 생겼는지 | 카드 모양 |

- **레이아웃 프리셋이 「넓게」라고 해도 값을 새로 만들지 마세요.** 레이아웃은 *몇 열로 무엇을 어디에*를
  말하는 층이고, *얼마나 벌릴지*는 이 눈금이 정합니다. 실제로 그 한 줄 때문에 40px을 쓴 격자가 있었고,
  같은 화면의 다른 격자는 16px이라 두 값이 나란히 놓였습니다.

**간격은 변수로 못 박고, 자리마다 그것만 씁니다.** CSS 맨 위에 이걸 그대로 붙여 넣으세요.

```css
:root{
  --wrap: 1440px;      /* 콘텐츠 폭 — 헤더·본문·푸터·고정바가 모두 이 값을 쓴다 */
  --pad-x: 24px;    /* 좌우 여백 */
  --card-gap:   16px;     /* 카드와 카드 사이 — 좌우 */
  --card-gap-y: 28px;     /* 카드 줄과 줄 사이 — 위아래 */
  --side-w:     260px;               /* 사이드바 폭 (사이드바 뼈대에서만) */
  --side-gap:   32px;                /* 사이드바와 본문 사이 */
  --row-gap:    14px;                /* 가로 행 카드 안: 썸네일과 글자 사이 */

  --card-pad:   24px;                /* 카드 안쪽 여백 */
  --row-h:      48px;                /* 표 한 줄 높이 — 안 정하면 줄마다 들쭉날쭉해진다 */
  --btn-gap:    8px;                 /* 나란히 놓은 버튼 사이 */
}
@media (max-width: 720px){
  :root{ --pad-x: 16px; --card-gap: 12px; }
}
.wrap { max-width: var(--wrap); margin-inline: auto; padding-inline: var(--pad-x); }
.cards{ display: grid; column-gap: var(--card-gap); row-gap: var(--card-gap-y); }

/* 지표 줄 — 화면 맨 위에 숫자 카드를 나란히 놓는 자리.
   카드 격자(.cards)와 다른 물건이므로 .cards 를 쓰지 않는다. 지표는 넷이 기본이다. */
.stats{ display: grid; grid-template-columns: repeat(4, 1fr);
        column-gap: var(--card-gap); row-gap: var(--card-gap-y); }
@media (max-width: 1024px){ .stats{ grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 720px){ .stats{ grid-template-columns: 1fr; } }

/* 모자이크 — 홈 첫 화면에 「큰 1장 + 작은 4장」을 놓는 자리.
   첫 장이 2칸×2줄, 나머지 넷이 옆을 채운다.
   어느 것을 크게 할지 class 로 정하지 않는다 — 맨 앞에 둔 것이 큰 것이다. */
.mosaic{ display: grid; grid-template-columns: repeat(4, 1fr);
         column-gap: var(--card-gap); row-gap: var(--card-gap-y); }
.mosaic > :first-child{ grid-column: span 2; grid-row: span 2;
                        display: flex; flex-direction: column; }
/* 큰 칸의 사진은 카드 비율을 따르지 않는다 — 두 줄 높이와 안 맞아 제목이 밀려난다.
   큰 칸에서는 사진이 남는 자리를 채운다. */
.mosaic > :first-child > .thumb{ aspect-ratio: auto; flex: 1; min-height: 0; }
@media (max-width: 1024px){
  .mosaic{ grid-template-columns: repeat(2, 1fr); }
  .mosaic > :first-child{ grid-row: auto; }   /* 2열에서 세로로 겹치면 빈칸이 생긴다 */
}
@media (max-width: 720px){
  .mosaic{ grid-template-columns: 1fr; }
  .mosaic > :first-child{ grid-column: auto; }
}

/* 상세 화면의 대표 사진 — 카드 비율을 그대로 쓰지 않는다.
   4:3 을 본문 폭 1392px 에 적용하면 세로가 1044px 이 되어 노트북 화면을 통째로 덮는다.
   가로로 눕히고(16:9) 세로를 420px 로 막는다 — 사진 아래 내용이 같이 보이는 선이다. */
.detail-img{ aspect-ratio: 16 / 9; max-height: 420px; width: 100%;
             border-radius: 12px; overflow: hidden; }
.detail-img > img{ width: 100%; height: 100%; object-fit: cover; display: block; }

/* 간격은 반드시 이 변수로 쓴다. gap:16px 처럼 숫자를 직접 적으면 자리마다 값이 갈린다. */
```

- **`--card-pad`·`--row-h`·`--btn-gap` 도 꼭 쓰세요.** 카드 안쪽 여백, 표 한 줄 높이,
  버튼 사이 간격입니다. 이 셋을 안 정해주면 화면마다 다르게 나옵니다 — 저희도 겪었습니다.
- **`.stats`·`.mosaic`·`.detail-img` 는 카드 격자와 다른 자리입니다.** 지표 숫자 줄,
  홈의 큰 사진 한 장 + 작은 넷, 상세 화면의 대표 사진. 카드 규칙을 여기 갖다 쓰면
  사진이 화면을 다 덮습니다.

- **격자에 `gap: 20px` 처럼 숫자를 직접 쓰지 마세요.** 한 번 쓰는 순간 그 자리만 달라지고,
  나중에 값을 바꿀 때 그 자리만 안 바뀝니다. 실제로 한 사이트에서 좌우가 3·8·16·20·24px
  다섯 가지로 갈라진 적이 있습니다 — 전부 `gap:` 에 숫자를 적어서 생긴 일입니다.
- **`gap` 한 줄로 합치지도 마세요.** 좌우와 위아래는 값이 다르므로 `column-gap`·`row-gap` 둘로 씁니다.
- **이름도 이대로 씁니다.** `--grid-x`·`--gap-col` 처럼 제 나름의 이름을 지으면, 나중에
  다른 사람이 볼 때 그게 같은 자리인지 알 수가 없습니다. 실제로 같은 규칙을 준 두 사이트가
  하나는 `--grid-x/--grid-y`, 하나는 `--gap-col/--gap-row` 를 써서 값이 따로 놀았습니다.
- 값을 바꾸고 싶으면 **`:root` 의 두 줄만** 고칩니다. 그러면 화면 전체가 한 번에 따라옵니다.
- 히어로의 좌우 2단처럼 **카드 격자가 아닌 자리**는 이 규칙 밖입니다. 거기는 화면 뼈대라 레이아웃이 정합니다.

#### 박스형 콘텐츠 — 몇 개를 늘어놓고 사진은 어떤 비율로

사진이 든 카드를 격자로 늘어놓을 때, **몇 개를 놓느냐가 사진 비율을 정합니다.**

| 한 줄에 | 사진 비율 | 왜 |
| --- | --- | --- |
| **2개** | **4:3** | 칸이 넓으니 가로로 넓은 사진 |
| **3개** | **4:3** | 위와 같음 |
| **4개** | **1:1** | 칸이 좁다. 정사각이 가장 얌전하다 |

4개짜리를 세로로 긴 3:4로 뒀더니 **카드 한 줄이 화면을 다 먹었습니다.** 칸이 좁을수록 세로로
길게 두고 싶어지지만, 그러면 한 줄만 보이고 다음 단락이 안 보입니다. 좁은 칸에는 정사각입니다.

- **한 줄에 하나만 두지 않습니다.** 마지막 줄에도 하나만 남기지 마세요 —
  **항목 수를 열 수에 맞춰** 3열이면 3·6·9개, 4열이면 4·8개로 둡니다.
- **한 사이트에서 카드 사진 비율은 하나입니다.** 위 표의 짝(2·3열↔4:3, 4열↔1:1) 밖의 비율은 만들지 마세요.
  같은 홈에 세로 카드와 16:9가 섞여 있어서 **사는 사람이 사진을 두 벌 준비해야 했던** 적이 있습니다.
- **배너·차트·영상·프로필은 이 규칙 밖입니다.** 가로로 긴 배너·영상(16:9)·프로필(1:1)은 제 나름의 비율이 있습니다.
- 상품 사진처럼 **정사각이 업계 표준인 분야**는 1:1을 써도 됩니다. 다만 그때도 한 사이트 안에서는 하나로 갑니다.
- **가로 배너 한 장으로 한 단락을 채우지 마세요.** 배너는 위 표 밖이지만 "한 줄에 하나만 두지 않는다"는 그대로 걸립니다.
  배너 한 장이 있던 자리는 **두 장으로 나누고** 4:3으로 맞추세요.
- **가로로 넘기는 줄(캐러셀)도 몇 개 보이느냐로 셉니다.** 뒤에 더 있어도 눈에 4개가 보이면 4개 — 비율도 1:1입니다.
  카드 폭을 `260px`처럼 박아 두면 화면 폭이 바뀔 때 3.4개씩 어정쩡하게 걸치니, **칸 수로 나눠서** 정하세요.
- **가로로 넘기는 줄에는 좌우 화살표를 두고, 아래 스크롤바는 감춥니다.** 넘길 게 있다는 건
  화살표가 알려 줍니다. 스크롤바까지 나오면 줄 밑에 회색 막대가 하나 더 생겨 지저분합니다.

```css
.carousel{ scrollbar-width:none; -ms-overflow-style:none; }
.carousel::-webkit-scrollbar{ display:none; }
```

  화살표가 **없는데** 스크롤바까지 감추면 넘길 게 있다는 걸 아무도 모릅니다. 둘은 한 벌입니다.

#### 분리형과 혼합형 — 글자를 사진 아래 두느냐 위에 얹느냐

박스형 콘텐츠는 두 가지입니다. **어느 쪽이든 위의 개수·비율 규칙은 똑같이 적용됩니다.**

| | **분리형** | **혼합형** |
| --- | --- | --- |
| 생김새 | 사진 아래에 글자 | 사진 위에 글자를 얹음 |
| 담을 수 있는 것 | 제목·설명·평점·가격까지 여러 줄 | 제목 + 한 줄 정도 |
| 사진이 하는 일 | 무엇인지 알려 준다 | 분위기를 만든다 |
| 쓰는 자리 | 상품·강의 목록처럼 **고르는** 자리 | 특집·기획전처럼 **끌어당기는** 자리 |

**분리형은 상자를 두르지 마세요.** 흰 배경에 테두리·그림자까지 두르면, 사진과 글자가 이미 한 덩어리인데
선을 한 번 더 치는 셈입니다. 격자로 늘어놓으면 그 선이 줄줄이 겹쳐 화면이 답답해집니다.
**모서리는 사진이 가집니다** — 상자를 없앤 자리에서 사진이 카드 노릇을 합니다.
(혼합형은 사진이 곧 배경이라 이 규칙 밖입니다.)

```css
/* 사진 + 글자로 된 카드 — 상자를 두르지 않는다 */
.card{
  background: none;   /* surface 나 흰색을 깔지 않는다 */
  border: 0;
  box-shadow: none;
}
.card .thumb{ border-radius: 12px; }  /* 모서리는 사진이 가진다 */
.card .body{ padding: 16px 0 0; }     /* 좌우 여백 0 — 상자가 없으니 안쪽 여백도 없다 */
```

**`background: var(--surface)` 나 `border: 1px solid` 를 카드에 쓰면 이 규칙을 어긴 것입니다.**
사진이 없는 상자(설정 패널·안내 상자·표를 감싼 것)는 이 규칙 밖입니다 — 거기는 배경이 있어야
덩어리로 읽힙니다. **사진과 글자로 된 카드**만 걷어냅니다.

상자를 걷어내면 두 가지가 따라옵니다.

- **글자가 흰색이 아니라 페이지 배경 위**에 놓입니다. 대비를 다시 재세요.
- **카드끼리 갈라 주던 테두리가 없어집니다.** 줄과 줄 사이를 더 벌려 그 몫을 대신하세요.

그리고 **카드 안에는 선을 긋지 마세요.** 가격 위에 선을 두면 상자가 없는 상태에서는 그 선이 카드를
가르는 것처럼 보여, 사진·제목과 가격이 따로 노는 두 덩어리로 읽힙니다.
**갈라야 할 것은 카드 안이 아니라 카드끼리**입니다.

혼합형을 쓸 때 반드시 지킬 것:

- **어둠막을 까세요.** 사진 위에 흰 글자를 그냥 얹으면 사진의 밝은 자리에서 글자가 사라집니다.
  **사진은 사는 분이 바꿀 것**이라 우리가 밝기를 미리 알 수 없습니다.
  글자 쪽에서 반대편으로 옅어지는 어둠막을 항상 깔아 두면 어떤 사진이 와도 읽힙니다.
- **글자 대비 4.5 이상**을 지키세요. 가장 밝은 사진(흰색)이 왔다고 치고 재세요.
- **글자를 짧게 쓰세요.** 제목 한 줄 + 설명 한 줄까지입니다. 그보다 길면 사진을 다 덮어 혼합형인 의미가 없습니다.
- **가격·평점처럼 정확히 읽어야 하는 것은 얹지 마세요.** 사진 위에서는 숫자가 잘 안 읽힙니다.
- **한 화면에 두 가지를 섞어도 됩니다.** 위는 혼합형 특집, 아래는 분리형 목록 — 하는 일이 다르므로 어수선하지 않습니다.

#### 글꼴은 한 벌입니다

**기본은 `Paperlogy`**, 못 불러오면 `Pretendard` → `Noto Sans KR` 순으로 물러납니다.
CSS로는 `font-family: Paperlogy, Pretendard, 'Noto Sans KR', sans-serif` 한 줄입니다.

- **제목과 본문에 다른 글꼴을 쓰지 마세요.** 두 벌이 되는 순간 어디에 무엇을 쓸지가
  화면마다 갈립니다. 처음엔 「제목만 다르게」로 시작해서, 나중엔 세 벌 네 벌이 됩니다.
- **위계는 글꼴이 아니라 크기와 굵기로 만듭니다.** 굵기는 400·600·700 셋만 쓰세요.
- 크기는 본문 15 · 카드 제목 18 · 섹션 제목 22 · 페이지 제목 32이 기준입니다.
- **숫자가 많은 표에는 고정폭 숫자**(`font-variant-numeric: tabular-nums`)를 켜세요.
  안 켜면 값이 바뀔 때 자릿수가 흔들려 칸이 들썩입니다.

#### 움직임 — 바뀌었다는 걸 눈이 알아채게

값이 바뀌었는데 **딱 하고 갈아 끼우면 사람은 그걸 못 봅니다.** 숫자가 12에서 3으로
바뀌어도, 눈이 그 자리를 안 보고 있었으면 바뀐 줄 모릅니다.
움직임은 꾸미개가 아니라 **「방금 여기가 바뀌었어요」라고 알려 주는 장치**입니다.

| 언제 | 얼마나 | 무엇이 |
| --- | --- | --- |
| 마우스를 올렸을 때 | **0.12초** | 색·테두리 |
| 눌러서 값이 바뀔 때 | **0.2초** | 사라짐·나타남·자리 옮김 |
| 접히고 펴질 때 | **0.25초** | 높이 |
| 화면 위에 뜰 때 (모달·시트) | **0.25초** | 옅어짐 + 살짝 올라옴 |
| 안내 문구(토스트) | **0.2초 등장 · 3초 머무름** | 옅어짐 |

- **가속은 하나로 씁니다.** `ease-out`(빨리 시작해 부드럽게 멈춤)이 기본입니다.
  자리마다 다른 가속을 쓰면 같은 사이트에서 두 사람이 만든 것처럼 보입니다.
- **0.3초를 넘기지 마세요.** 그보다 길면 기다리는 느낌이 듭니다. 목록이 다시 그려질 때는
  **0.15초**면 충분합니다.
- **자리를 밀어내며 움직이지 마세요.** 아래 내용이 출렁이면 읽던 곳을 잃습니다.
  옅어지거나 제자리에서 바뀌게 하세요.
- **한 번에 한 곳만 움직입니다.** 눌렀는데 화면 여기저기가 같이 움직이면 어디가 바뀐 건지 모릅니다.
- **끝없이 도는 움직임은 두지 않습니다.** 배너 자동 넘김·깜빡임은 읽는 걸 방해합니다.
  기다리는 동안 도는 것(로딩)만 예외입니다.
- **움직임을 꺼 둔 분을 존중하세요.** `prefers-reduced-motion` 이 켜져 있으면 전부 0초로 둡니다.
  어지럼증 때문에 꺼 두신 분들이 있습니다. 값은 바뀌되 움직이지만 않으면 됩니다.

#### 떠 있는 부품과 줄 안에 서는 부품은 다른 것입니다

사진 위에 **떠 있는** 동그란 찜 아이콘과, 버튼 줄 안에 **나란히 서는** 찜 버튼은
생김새가 비슷해 보여도 다른 부품입니다. 떠 있는 쪽은 자리를 빠져나가도록(`position:absolute`)
만들어져 있어서, 그 이름을 줄 안의 버튼에 붙이면 **줄을 빠져나가 엉뚱한 데 가서 앉습니다.**
실제로 찜 버튼이 가격 위에 겹쳐 앉은 적이 있습니다.

- **이름이 자리를 뜻하면 그 자리에서만 쓰세요.** 「사진 위 찜」과 「줄 안 아이콘 버튼」은
  이름을 나눠 두고, 섞어 쓰지 않습니다.
- **부품 이름과 실제 쓰임이 어긋나면 이름을 고치세요.** 「타임라인」이라 지어 놓고
  여섯 곳 모두 한 줄짜리 목록으로 쓰고 있던 적이 있습니다. 세로 점만 남아 글자 사이에 끼었습니다.
- **아이콘만으로 뜻이 통하면 글자를 빼세요.** 하트·공유·닫기가 그렇습니다.
  다만 **`aria-label`로 이름은 남깁니다** — 눈으로 못 보는 분에게는 그게 유일한 이름입니다.
- 반대로 **아이콘만으로 못 알아보는 것에는 글자를 붙이세요.** 「빼기」·「내보내기」처럼
  모양이 여러 뜻으로 읽히는 것이 그렇습니다.

#### 쓰는 화면이 있으면 보여주는 자리도 있어야 합니다

글을 쓰게 해 놓고 그 글이 나올 자리를 안 만드는 일이 잦습니다.
실제로 강사가 **1,240자짜리 강의 소개를 쓰는 화면**은 있는데, 상세 화면에는 그 글이
나올 자리가 없었습니다. 게다가 검수 화면은 "강의 소개 1,240자 — 확인됨"이라고까지 말하고 있었습니다.

- **입력 칸을 만들 때마다 "이건 어디에 보이나"를 한 번 물으세요.** 답이 없으면 둘 중 하나입니다 —
  보여줄 자리를 만들거나, 그 입력 칸을 빼거나.
- **에디터(굵게·목록·이미지 버튼이 붙은 칸)로 쓴 글**은 보여주는 쪽에도 그 모양이 살아야 합니다.
  제목·목록·굵게를 쓸 수 있게 해 놓고 보여줄 때 한 덩어리 문단으로 뭉개면, 쓴 사람이 헛수고합니다.
- **한 줄 요약과 본문 소개는 다른 것입니다.** 목록 카드에 들어가는 한 줄과, 상세에서 읽는 본문은
  자리도 길이도 다릅니다. 하나로 합치지 마세요.

#### 권장 크기는 실제 자리와 같아야 합니다

"권장 1280×720 (16:9)"이라고 적어 두고 옆의 자리는 4:3인 화면이 있었습니다.
**사는 분은 글자를 믿고 그 크기로 사진을 준비**하는데, 올려 보면 잘립니다.

- 안내 문구의 숫자와 **바로 옆 자리의 비율이 같은지** 확인하세요.
- 검수·확인 화면에 크기가 또 적힌다면 **거기도 같은 숫자**여야 합니다. 세 곳이 따로 놀기 쉽습니다.
- **영상만 16:9입니다.** 썸네일·카드 사진은 위 표(2·3개↔4:3, 4개↔1:1)를 따릅니다.

#### 차트 — 이미지가 아니라 데이터로 그립니다

추이·막대·도넛 같은 차트를 **이미지 자리로 두지 마세요.** `이미지 영역 (라인 차트 · 권장 1200×420)`
이라고 적어 두면 사는 분은 차트를 **그림 파일로 만들어 올려야 하는 줄** 압니다.
차트는 숫자가 바뀌면 따라 바뀌어야 하는 것이라, 사진 자리와 성격이 다릅니다.

- **가벼운 것은 라이브러리 없이 SVG로 그리세요.** 선 하나·막대 열두 개짜리는 30줄이면 됩니다.
  바깥에서 아무것도 안 불러오니 **인터넷 없이 폴더에서 열어도** 그대로 보입니다.

**대시보드를 만든다면 최소한 이 셋은 갖추세요.**

| 무엇 | 어디에 | 왜 |
| --- | --- | --- |
| **큰 추이 차트** | 화면에 한둘 | 기간을 놓고 흐름을 본다 |
| **스파크라인** (작은 추이선) | 지표 카드 안 | 숫자 옆에서 방향만 알려 준다 |
| **증감 표시** (▴ 812) | 지표 카드 안 | 좋아졌는지 나빠졌는지 |

숫자 하나만 두면 **좋은지 나쁜지를 못 말합니다.** "10,374명"만 있으면 늘고 있는지
줄고 있는지 알 수 없어서, 사는 분은 그 카드를 보고 아무 판단도 못 합니다.
지표 카드는 **숫자 + 증감 + 추이선**이 한 벌입니다.

- **본격적인 차트가 필요하면 무료(MIT) 라이브러리를 쓰세요.** 값을 치르지 않아도 되고 상업적으로 써도 됩니다.

| 라이브러리 | 크기 | 언제 |
| --- | --- | --- |
| **Chart.js** | ~200KB | 무난한 첫 선택. 자료가 많아 AI도 정확하게 짭니다 |
| **ApexCharts** | ~500KB | 기본 모양이 예쁘고 종류가 많을 때 |
| **uPlot** | ~40KB | 점이 수천 개라 가벼워야 할 때 |
| **Frappe Charts** | ~30KB | 작고 산뜻한 SVG면 충분할 때 |

- **색은 반드시 프리셋 색을 쓰세요.** 라이브러리 기본 팔레트(빨강·노랑·초록)를 그대로 두면
  그 자리만 다른 사이트처럼 보입니다.
- **눈금선은 옅게, 선은 진하게.** 눈금이 데이터보다 진하면 숫자가 안 읽힙니다.
- 라이브러리를 쓰면 **인터넷이 필요해집니다.** 오프라인으로 열어 볼 화면이라면 파일을 함께 받아 두거나 SVG로 그리세요.

#### 가격 표기 — 낼 돈부터

**판매가 · 정가 · 할인율** 순서로 한 줄에 세웁니다.

| 자리 | 모양 | 크기 |
| --- | --- | --- |
| **판매가** (낼 돈) | 진한 기본 글자색, 굵게 | 18 (상세는 28) |
| **정가** | 회색 + 취소선 | 13 |
| **할인율** | **강조색 글자**, 굵게 | 14 |

- **할인율을 앞에 두지 마세요.** 눈이 먼저 "20%"에 걸려 정작 얼마인지를 뒤늦게 찾습니다.
  사는 분이 알고 싶은 건 **낼 돈**입니다.
- **할인율은 배지가 아니라 색 글자입니다.** 배지로 두면 정가·판매가와 무게가 비슷해져 셋이 서로 다툽니다.
  배지 자리는 `입문`·`NEW` 같은 **꼬리표 몫**입니다.
- **셋의 크기를 벌려 두세요.** 비슷하면 무엇을 먼저 읽어야 할지 눈이 헤맵니다.
- **강조색 원색을 글자로 쓰지 마세요.** 주황·코랄 원색은 흰 배경에서 3:1을 못 넘깁니다.
  글자용으로 한 단계 어두운 색을 따로 두세요.
- 가격 묶음은 **한 곳에서 만들어 쓰세요.** 자리마다 손으로 짜면 순서가 갈립니다.

#### 버튼이냐 링크냐

**다른 화면으로 데려가면 링크, 이 화면 안에서 끝나면 버튼입니다.**

| 하는 일 | 무엇으로 | 눌렀을 때 |
| --- | --- | --- |
| 다른 화면으로 간다 | 링크 `<a href>` | 그 화면이 열린다 |
| 이 화면 안에서 끝난다 | 버튼 `<button>` | 안내 문구가 뜬다 |

- 필터 적용·정렬·검색·다음 차시·시간 지점 이동은 **화면이 바뀌지 않는 조작**입니다.
  링크로 만들면 **자기 자신으로 가는 링크**가 되어, 눌러도 같은 화면이 다시 열립니다.
  사는 분 눈에는 *"눌렀는데 아무 일도 안 나네"* 로 보입니다.
- **나란히 선 버튼끼리는 종류를 섞지 마세요.** 하나는 화면이 넘어가고 하나는 안 넘어가면
  규칙이 없어 보입니다.

**그리고 화면 안에서 끝나는 조작은 진짜로 값이 바뀌어야 합니다.**
눌러도 숫자가 그대로면, 사는 분 눈에는 **눌리지 않는 포스터**입니다.

| 눌렀을 때 | 바뀌어야 하는 것 |
| --- | --- |
| 기간 탭 (7일·30일·90일) | 차트와 그 옆 요약 숫자 |
| 쿠폰·옵션 고르기 | 결제 금액 줄과 합계 |
| 수량 늘리기 | 금액 |
| 정렬·필터 | 목록 순서나 개수 |

#### 눌러 봐야만 드러나는 것들 — 2026-08-19 LMS 두 팩 검수에서 나온 것

화면을 눈으로만 보면 멀쩡한데, 눌러 보면 어긋나는 자리가 있습니다. 실제로 나온 것들입니다.

**① 켜짐 표시와 실제 걸린 조건이 따로 놀지 않게.**
거르는 단추 여덟 개와 「전체 보기」가 서로 다른 상자에 담겨 있었습니다. 그래서
「디자인」을 눌러도 「전체 보기」가 안 꺼지고, 「전체 보기」를 눌러도 「디자인」이 안 꺼졌습니다.
**목록은 맞게 걸러지는데 표시만 손님을 속였습니다.**
- 같은 갈래의 단추는 **상자를 넘어** 한 벌로 갱신하세요. 상자 안에서만 끄면 밖에 있는 게 남습니다.
- 조건 칩의 ✕ 로 지웠을 때도 **왼쪽 단추의 켜짐이 같이 꺼져야** 합니다.

**② 쪽 번호는 결과를 따라가야 합니다.**
12개를 2개로 걸렀는데 쪽 번호가 「1 2 3 4 5」 그대로였고, 어느 쪽을 눌러도 같은 목록이 나왔습니다.
- 결과가 한 쪽에 다 들어가면 **쪽 번호를 감추세요.**
- 조건이 바뀌면 **1쪽으로 되돌리세요.** 3쪽을 보다 조건을 바꾸면 3쪽에 머물면 안 됩니다.

**③ 화면을 넘길 때 고른 값도 같이 넘기세요.**
결제 화면에서 「12개월 할부」를 골라 「매달 5,270원 × 12개월」까지 확인했는데,
완료 화면은 「신용카드 (일시불)」이었습니다. 고른 값이 다음 화면으로 안 넘어갔습니다.
- 앞 화면에서 고른 것이 뒤 화면에 다시 적힌다면 **둘을 이어 두세요**(sessionStorage 면 충분합니다).
- 이어 둘 수 없으면 **뒤 화면에 그 값을 적지 마세요.** 틀린 값보다 없는 게 낫습니다.

**④ 견본 날짜는 「오늘」에서 거꾸로 잡으세요.**
만든 날에 맞춰 날짜를 적어 두면 시간이 지나 여는 손님은 지난 날짜만 봅니다.
실제로 「D-3 · 마감 8월 10일」인데 그날은 8월 19일이었습니다 — 9일 지난 마감을 「3일 남음」이라 했습니다.
- **기준일 하나를 정해 두고, 화면을 열 때 오늘까지의 차이만큼 통째로 미세요.**
  날짜 사이 간격이 그대로라 「D-3」이나 「수강 기간 6개월」 같은 계산값은 손대지 않아도 맞습니다.
- 주문번호에 박힌 날짜(ORD-20260807-…)도 같이 밀어야 합니다.

**⑤ 숫자 하나를 고치면 그 숫자로 계산되는 값을 전부 따라가세요.**
차시 수를 30에서 18로 고치면서 딸린 값을 안 고쳤더니,
같은 강의 진도가 화면마다 67% 와 40% 로 갈라졌고 「12/18차시 · 남은 차시 18개」가 됐습니다.
- 차시 수를 바꿀 때 딸린 것: **총 재생 시간 · N/M차시 · 진도 % · 남은 차시 · 장별 완료 수.**
- 고친 뒤에는 그 값이 나오는 **모든 화면을 훑어** 한 벌로 맞았는지 보세요.

**⑥ 「비로그인」 화면은 헤더도 비로그인이어야 합니다.**
「홈 - 비로그인」인데 헤더에 알림 종과 프로필이 있고 로그인 단추가 없었습니다.
사이드바에는 「로그인·회원가입」이 있는데 헤더에는 「김수현님」이라, 한 화면에서 앞뒤가 안 맞았습니다.
- 로그인 전/후 화면을 나눠 만들었다면 **머리띠와 옆 메뉴도 같이 갈라야** 합니다.

**⑦ 견본이라 좁은 자리는 그 말을 화면에 적으세요.**
분야 카드는 「개발 284개」인데 눌러 거르면 2개가 나왔습니다. 견본이라 그런 건데 그 말이 없었습니다.
- 「전체 1,280개 가운데 12개만 보여 주는 견본입니다」 처럼 **한 줄로 밝히면** 어긋남이 사라집니다.
- 목록에서 어느 카드를 눌러도 같은 상세가 열린다면 **상세 화면에도** 그렇게 적으세요.

**⑧ 겹쳐 놓은 단추는 정말 눌리는지 확인하세요.**
카드 위 찜 하트가 사진에 덮여 있어서, 누르면 찜이 안 되고 강의 상세로 넘어갔습니다.
- 겹치는 단추에는 **z-index** 를 주고, `document.elementFromPoint` 로 그 자리 맨 위가 무엇인지 확인하세요.
- 링크 안에 단추를 넣었다면 `preventDefault` 만으로는 부족합니다 — **덮였는지부터** 보세요.

**⑨ 못 채운 곳으로 화면을 옮겨 주세요.**
「결제하기」는 오른쪽 위에 있고 「동의를 아직 채우지 않았어요」는 화면 아래에 떴습니다.
스크롤해야만 보이니 손님은 왜 안 되는지 모릅니다.
- 막혔으면 **못 채운 자리로 스크롤**하고, 채워지면 안내를 거두세요.

**⑩ 끝난 조작은 끝난 티를 내세요.**
쿠폰 「받기」를 몇 번이고 눌러도 「받았어요」가 또 나왔고, 과제를 내도 알림 한 줄 없이 화면이 넘어갔습니다.
- 한 번으로 끝나는 단추는 **글자를 바꾸고 다시 눌리지 않게** 하세요(받기 → 받음).
- 낸 뒤에는 **그 자리에서** 낸 것이 보여야 합니다.

- **미리 만들어 두고 바꿔 끼세요.** 차트 셋을 미리 그려 두고 탭에 따라 보여 주는 편이,
  누를 때마다 자바스크립트로 다시 그리는 것보다 낫습니다 — 그리는 규칙이 두 곳으로 갈라지지 않습니다.
- **계산은 한 곳에서만 하세요.** 합계를 화면마다 손으로 적어 두면 쿠폰을 바꿨을 때 한 군데만 바뀝니다.
- **탭에 갈 곳도 바꿀 것도 없으면 탭이 아닙니다.** 그냥 제목이거나, 아직 안 만든 자리입니다.

**같은 길을 두 번 두지 마세요 — 위 카테고리 줄과 왼쪽 LNB.**
둘이 같은 곳을 가리키면 위쪽이 두 줄로 두꺼워지고, 어느 쪽을 눌러야 할지 잠깐 멈칫합니다.

| 상태 | 왼쪽 LNB | 위 카테고리 줄 |
| --- | --- | --- |
| 맨 위 (LNB 보임) | 보인다 | **접힌다** |
| 스크롤해서 LNB가 위로 사라짐 | 안 보인다 | **내려온다** |
| LNB가 아예 없는 화면 | — | **늘 보인다** |

- **LNB가 있는 화면은 접힌 채로 시작하세요.** 펼쳐 두었다가 나중에 접으면 첫 화면에서 한 번 번쩍입니다.
- **LNB가 없는 화면에서는 절대 접지 마세요.** 접으면 갈 길이 통째로 사라집니다.
- **기준은 "LNB가 붙박이 머리말 아래로 사라지는 순간"입니다.** 머리말 높이만큼 위를 깎아서 재세요.
- 같은 규칙을 **LNB를 쓰는 모든 레이아웃에 똑같이** 거세요. 화면마다 다르면 위쪽 높이가 제멋대로 바뀌어 보입니다.

- **견본으로 가는 버튼을 화면 안에 두지 마세요.** `결과가 없을 때` `대화가 없을 때는 이렇게 보여요`
  처럼 예외 화면을 보여주려고 넣은 버튼·단락은 진짜 사이트에 없는 것입니다.
  검색창 옆에 그런 버튼이 서 있으면 사는 분은 그게 기능인 줄 압니다.
  빈 화면·오류 화면은 **화면 목록에 따로 한 장**으로 두세요 — 거기서 눌러 보면 됩니다.

#### 옮겨 다니며 봐야 드러나는 것들 — 2026-08-20 사장님 검수에서 나온 것

앞의 것들이 «눌러 봐야» 나오는 흠이라면, 이번 것은 **화면을 옮겨 다니고 끝까지 굴려야** 나옵니다.
한 장만 세워 놓고 보면 전부 멀쩡해 보입니다. 실제로 우리 팩 열넷에서 나온 것만 적습니다.

**① 화면을 옮길 때 본문이 좌우로 튀지 않게.**
세로 스크롤 막대는 «생길 때만» 15px 을 먹습니다. 그래서 목록이 짧은 화면에서 긴 화면으로 가면
본문이 통째로 15px 밀립니다. 한 장씩 보면 둘 다 멀쩡한데 오가면 화면이 흔들립니다.
```css
html { scrollbar-gutter: stable; }   /* 막대 자리를 늘 비워 둔다 */
```
- 화면 «안»에서 탭으로 목록을 거를 때도 같습니다. 3개 보이다 5개가 되면 막대가 생깁니다.

**② 콘텐츠 폭은 모든 화면에서 같아야 합니다.**
한 화면만 폭이 다르면 옮길 때마다 글이 좌우로 밀려 «따라다니는» 느낌이 납니다.
- 폭은 화면마다 적지 말고 **한 곳(`--wrap`)에 두고 모두 그것을 쓰세요.**

**③ 푸터가 본문에 맞붙지 않게.**
`margin-top: auto` 는 **자리가 남을 때만** 벌어집니다. 내용이 한 화면을 넘기면 0 이 되어
푸터 윗선이 버튼에 그대로 붙습니다. 실제로 「강사로 전환 신청」 바로 밑에 선이 붙었습니다.
```css
/* auto 는 그대로 두고(짧은 화면에서 바닥에 붙이는 일은 auto 만 합니다)
   푸터 «바로 앞 덩어리»에 최소 간격을 줍니다 */
:where(main, .page-wrap) > *:has(+ footer) { margin-bottom: 32px; }
```
- GNB 와 본문 사이도 마찬가지입니다. **16px 밑으로 붙지 않게** 하세요.

**④ 덩어리 사이 간격은 그 화면이 쓰는 «리듬»을 지키세요.**
다른 칸들은 24px 씩 벌어져 있는데 한 곳만 8px 이면, 0px 이 아니어도 **붙어 보입니다.**
- 새 묶음이 시작되는 자리(안내 상자 다음에 오는 소제목 같은 것)를 특히 보세요.
- 제목 바로 밑 부제는 **붙어 있는 것이 맞습니다.** 그건 한 덩어리입니다.

**⑤ 화면 «안» 탭에는 뒤로가기를 두지 마세요.**
탭처럼 생겼는데 실제로는 다른 장으로 넘어가고, 간 장에는 「‹ 돌아가기」가 붙어 있었습니다.
누를 때마다 없던 손잡이가 생기면서 **화면이 그만큼 아래로 밀립니다.**
- 탭으로 오갈 화면들은 **뒤로가기 유무를 통일**하세요. 탭이면 넷 다 없는 쪽이 맞습니다.
- 탭 하나가 «지금 보고 있는 그 화면»을 가리키고 있지는 않은지도 보세요. 눌러도 아무 일이 없습니다.

**⑥ 동그란 버튼을 가로로 늘이지 마세요.**
`border-radius: 50%` 인 34px 짜리 아이콘 버튼에 `width: auto` 를 얹으면 **타원**이 됩니다.
- 넓은 글자 버튼이 필요하면 **일반 버튼 모양**을 쓰고, 옆 버튼과 **키를 맞추세요.**
- 나란히 선 버튼끼리 키가 다르면 흠입니다. 다만 「큰 버튼 + 보통 버튼」을 **일부러** 짝지은 것은 괜찮습니다.

**⑦ 세로로 세운 flex 칸에서는 배지와 버튼이 늘어납니다.**
`display:flex; flex-direction:column` 에 `align-items` 를 안 주면 기본값이 `stretch` 라
배지도 버튼도 **칸 폭 그대로 늘어납니다.** 40px 이어야 할 배지가 767px 이 된 적이 있습니다.
```css
.promo .in > .badge, .promo .in > .btn { align-self: flex-start; }
```
- 글(제목·설명)은 넓게 두고 **배지와 버튼만** 제 몸에 맞추세요.

**⑧ 배지를 글 옆에 붙이려면 블록으로 만드세요.**
`14:31<div class="unread">2</div>` 에서 배지가 `inline-grid` 라 **시각 옆에 나란히** 섰습니다.
`margin-top` 을 준 걸 보면 원래는 밑에 놓을 셈이었는데, 인라인이라 안 먹었습니다.
```css
.unread { display: grid; width: max-content; margin: 4px 0 0 auto; }
```

**⑨ 좌우로 미는 줄에는 막대를 보이지 마세요.**
카드를 옆으로 미는 줄에 «‹ ›» 화살표를 붙여 놓고 막대까지 같이 보이면 밑에 회색 띠가 깔립니다.
```css
.rail { scrollbar-width: none; }
.rail::-webkit-scrollbar { display: none; }
```
- ⛔ **표에는 쓰지 마세요.** 표에는 화살표가 없어서 막대를 감추면 «넘길 수 있다는 것»을 알 길이 사라집니다.
  표는 애초에 **좁은 칸에 넣지 않는 것**이 답입니다.

**⑩ 나란한 썸네일은 크기가 같아야 합니다.**
한 줄에 선 카드의 사진 높이가 593 · 250 · 84px 로 제각각이면 목록이 무너져 보입니다.
- 목록 안 사진은 **비율(`aspect-ratio`)을 정해** 한 벌로 맞추세요.
#### 화면 목록으로 가는 길은 한 곳에만

만든 화면을 훑어보려면 목록이 필요합니다. 그런데 그 목록을 **화면 안에 늘어놓으면**
사는 분 눈에는 사이트의 일부로 보입니다.

```
이렇게 두지 마세요 — 화면 위쪽에 줄줄이
  [CO-01 · 강의 목록] [CO-02 · 검색 결과 없음] [CO-03 · 강의 상세] [CO-04 · 결제] …
```

**진짜 사이트에는 이런 줄이 없습니다.** `CO-02 · 검색 결과 없음` 은 만드는 사람의 말이지
쓰는 사람의 말이 아닙니다. 화면 ID 가 손님 눈에 보이는 것부터가 어긋납니다.

- **한 귀퉁이에 작게 하나만 둡니다.** 오른쪽 아래에 떠 있는 `화면 목록` 단추 하나면 됩니다.
  누르면 `화면목록.html` 로 갑니다 — index.html(홈)이 아닙니다.
- **그 단추는 화면 내용 위에 떠 있게** 두고, 본문 흐름에는 끼워 넣지 않습니다.
  본문에 들어가면 그것도 사이트의 일부가 됩니다.
- **화면 ID 를 손님이 보는 자리에 쓰지 않습니다.** 제목·탭·빵부스러기 어디에도요.
  ID 는 우리가 화면을 세는 이름이지 서비스의 말이 아닙니다.
- 화면끼리 오가는 길은 **그 서비스의 진짜 메뉴와 버튼**으로 만듭니다.
  「강의 목록 → 강의 상세」는 강의 카드를 눌러서 가지, 탭으로 가지 않습니다.

### 이미지 · 썸네일
- 아직 사진이 없으므로 이미지 자리는 **테마 색이 아니라 옅은 파스텔**로 채우세요. 테마 색으로 칠하면 화면이 그 색 덩어리로 뒤덮여 디자인이 안 보입니다.
- 배경 `#EEF2F7` · `#F2EFF7` · `#EDF4F1` · `#F7F1EC` · `#F2F4F7` 중 하나를 카드마다 돌려 쓰고, 테두리 `#E3E8F0` 1px, 글자색 `#8B94A6` 13px.
- 자리 안에 `이미지 영역 (무엇 · 권장 가로×세로)` 형식으로 적으세요. 예: `이미지 영역 (매장 대표 · 권장 1200×900)` · `이미지 영역 (프로필 · 권장 400×400)` · `이미지 영역 (배너 · 권장 1600×600)`
- 이미지 자리는 테마 색으로 칠하지 않는다. 옅은 파스텔 한 톤 + 1px 테두리로 둔다.
- 자리 안에 무엇이 들어갈 이미지인지와 권장 크기를 글자로 적는다.
- 적어 둔 크기의 **비율을 실제로 지킨다** — `400×400`이라 적었으면 정사각으로 보여야 한다(aspect-ratio).
- 특히 프로필·썸네일처럼 크기가 고정된 자리는, 가로로 나란히 놓아도 늘어나지 않게 한다. 목록·행 안에 넣을 때 `flex`가 잡아당겨 타원이 되는 일이 잦으니, 만든 뒤 실제로 열어 모양을 확인한다.
- 썸네일처럼 작아서 글자가 안 들어가면 크기만 적거나 아이콘 하나만 둔다.
- **「사진 위 겹침」 카드에서는 사진이 들어오기 전까지 어둠막을 걸지 않는다.** 옅은 자리표시자 위에 어두운 막을 덮으면 안에 적은 글이 묻혀 «깨진 그림»으로 보인다. 제목·태그는 자리표시자 아래에 본문 색으로 두었다가, 실제 사진을 넣을 때 위로 올린다.
- 실제 사진을 넣기 전까지 이 규칙을 유지한다 — 그래야 어디에 무엇을 넣어야 하는지 보인다.


## 7. 만들고 나서 — 눈으로 보고, 눌러 보고, 고치기
> **여기까지 해야 다 만든 것입니다.** 화면을 다 그렸어도 눌러서 안 움직이면 안 만든 것과 같습니다.
> **세 가지를 봅니다 — ① 화면연결·레이아웃·UI 를 눈으로 ② 뒤로가기·버튼·탭·썸네일·배너를 직접 눌러서 ③ 데이터가 화면끼리 맞는지.**
> 훑어보는 것이 아니라 **세어서** 확인하고, **나온 오류는 그 자리에서 고칩니다.** 하나라도 남으면 고치고 다시 셉니다.

### 7-1. 화면연결 · 레이아웃 · UI 를 «눈으로» 봅니다
- **화면 목록에서 시작해 링크를 눌러 다닙니다.** 주소창에 직접 쳐 넣지 마세요 — 그러면 화면이 서로 이어지는지가 안 보입니다.
- 묶음마다 대표 화면을 열어 **무너진 곳 · 겹친 곳 · 잘린 글 · 화면 밖으로 나간 것**을 봅니다.
- 가로 스크롤바가 생기면 어딘가 넘친 것입니다. 넓은 화면에서도 한 번 봅니다.
- 사이드바가 있는 짜임이면 **본문이 사이드바에 가리지 않는지** 봅니다.

### 7-2. 눌러도 아무 일 없는 것이 0개여야 합니다
- `href="#"` · `href="javascript:void(0)"` · 빈 `href` 는 **하나도 남기지 않습니다.**
- **페이지 번호, 달력 날짜, 시간 칸, 이전·다음** — 여기가 제일 자주 빈 채로 남습니다. 실제로 눌러 보세요.
- 아직 만들 화면이 없으면 «지금은 안 되는 것»으로 보이게 두세요(흐리게 + 눌리지 않게). 눌리는데 아무 일도 없으면 고장으로 보입니다.
- 단추(`<button>`)도 같습니다. 눌렀을 때 **화면이 바뀌거나, 값이 바뀌거나, 안내가 뜨거나** 셋 중 하나는 일어나야 합니다.


### 7-3. 거르는 단추는 «목록이 실제로 줄어야» 합니다
- **필터 칩·탭·정렬**을 누르면 색만 바뀌는 것으로는 안 됩니다. **보이는 항목이 실제로 달라져야** 합니다.
- 칩에 개수를 적었으면(`기본 교육 2`) 누른 뒤 **그 수만큼만 보여야** 합니다. 적힌 숫자와 보이는 수가 다르면 그 자리는 거짓말이 됩니다.
- 거르면 하나도 안 남는 조건도 눌러 보세요. **「결과가 없습니다」 화면**이 나와야 합니다 — 빈 채로 두면 고장으로 보입니다.
- 정렬(최신순·낮은 요금순)도 같습니다. 순서가 **눈에 보이게** 바뀌어야 합니다.

⛔ **안 줄어들면 열에 아홉은 이것입니다** — `[hidden]` 은 브라우저 기본값이라 `.row { display: flex }` 한 줄에도 집니다. 「지금 2개 보고 있어요」로 숫자만 바뀌고 목록은 그대로면, CSS 에 `[hidden] { display: none !important; }` 이 있는지 보세요.


### 7-4. 썸네일 · 배너 · 탭도 «직접» 눌러 봅니다
- **썸네일과 배너**는 그림이라 눌러 보기를 잊기 쉽습니다. 눌러서 어디로 가는지 하나씩 확인하세요.
- 카드 사진 위에 얹은 단추(찜 하트·빠른 담기)는 **사진에 덮여 안 눌리는 일**이 잦습니다.
  겹쳐 놓은 단추에는 `z-index` 를 주고, 눌러서 **정말 그 단추가 눌리는지** 확인하세요.
  (그 자리 맨 위에 무엇이 있는지는 `document.elementFromPoint(x, y)` 로 알 수 있습니다.)
- **탭**을 눌렀을 때 내용이 화면 밖에 있으면 손님은 아무것도 안 바뀐 줄 압니다. 탭 줄이 화면에 보이게 옮겨 주세요.
- **쪽 번호**는 눌렀을 때 목록이 실제로 달라져야 합니다. 걸러서 한 쪽에 다 들어가면 쪽 번호를 감추고, 조건이 바뀌면 1쪽으로 되돌립니다.
- 한 번으로 끝나는 단추(쿠폰 받기·신청하기)는 누른 뒤 **글자가 바뀌고 다시 안 눌려야** 합니다. 몇 번이고 눌리면 받았는지 알 수 없습니다.

### 7-5. 뒤로가기는 4장에 적힌 그 화면으로 갑니다
- 29개 화면에 뒤로가기가 있고, 나머지 8개(메뉴 첫 화면)에는 **없어야** 합니다.
- 화면마다 4장의 `뒤로가기` 항목과 **하나씩 대 봅니다.** 「대충 상위 메뉴로」가 아니라 적힌 그 화면입니다.
- 여러 단계로 이어지는 화면(예약→결제→완료)에서 특히 어긋납니다. 완료 화면에서 뒤로 갔을 때 결제가 다시 시작되면 안 됩니다.

### 7-6. 화면 목록과 실제가 같은지 셉니다
- 만들어진 화면이 **37개** 인지 셉니다. 목록에는 있는데 안 만든 화면, 만들었는데 목록에 없는 화면 둘 다 0이어야 합니다.
- 화면끼리 잇는 링크가 **실제로 있는 화면**을 가리키는지 셉니다. 없는 화면을 가리키는 링크는 0이어야 합니다.
- **`화면목록.html` 이 37개를 «다» 가리키는지** 셉니다. 홈에서 닿는 것만 넣으면 나머지는 영영 못 봅니다.
- **화면 목록으로 가는 단추가 37개 화면 «전부»에 있는지** 셉니다. 한 화면이라도 빠지면 거기서 길이 끊깁니다.


### 7-7. 데이터가 화면끼리 «서로 맞는지» 봅니다
> 화면 하나만 보면 다 맞아 보입니다. **두 화면을 나란히 놓아야** 어긋난 것이 보입니다.

- **앞 화면에서 고른 것이 뒤 화면에 그대로 넘어가는지** — 수량·옵션·할부·기간을 골라 놓고 다음 화면에서 확인하세요.
  고른 값이 안 넘어가면 뒤 화면에 그 값을 아예 적지 마세요. **틀린 값보다 없는 게 낫습니다.**
- **같은 값이 여러 화면에 나오면 다 같은지** — 목록의 「12/18 · 67%」가 상세에서는 40%면 하나는 거짓말입니다.
- **합계가 항목의 합과 맞는지** — 「남은 일 59건」이 1+24+32+2 와 맞는지, 표의 인원 합이 위 카드의 총원과 맞는지.
- **N/M 과 「남은 것」이 맞는지** — 12/18 이면 남은 것은 6개입니다. 전체 수를 그대로 적어 두기 쉽습니다.
- **적어 둔 개수와 실제로 보이는 개수가 맞는지** — 「전체 284개」라고 해 놓고 걸러 보니 2개면, 그 말을 화면에 적으세요(「284개 가운데 8개만 보여 주는 견본입니다」).
- **날짜와 그 옆의 말이 맞는지** — 「이번 주」가 붙은 것이 정말 이번 주인지, 「D-3」이 그 날짜와 맞는지, 요일이 달력과 맞는지.

⛔ **숫자 하나를 고치면 그 숫자로 계산되는 값을 «전부» 따라가세요.**
차시 수를 30에서 18로 고치면서 진도와 남은 차시를 안 고치면, 같은 것이 화면마다 다르게 보입니다. **반쪽만 고치면 안 고친 것보다 나쁩니다.**

### 7-8. 폰 폭에서 다시 한 번
- 위 「좁은 화면에서 넘치지 않게」를 **다 만든 뒤에 다시** 확인합니다. 화면이 늘어나면서 새로 넘치는 곳이 생깁니다.


### 7-9. 기계로 재기 — 이 글을 화면마다 돌리세요
> **눈으로 세지 마세요.** 아래 글이 «옮겨 다니며 봐야 보이는 것»을 대신 재 줍니다 —
> 푸터가 본문에 맞붙었는지 · 덩어리 간격이 어긋났는지 · 버튼과 배지가 늘어났는지 ·
> 좌우로 미는 칸에 막대가 드러났는지 · 표가 제 칸을 넘쳤는지.

**돌리는 법** — 편한 것 하나만 고르세요.
- 브라우저 개발자도구 콘솔에 그대로 붙여 넣기
- 화면마다 <script> 로 잠깐 끼워 넣고 결과를 찍기
- 헤드리스 브라우저로 열어 결과를 document.title 에 담아 꺼내기

```js
(() => {
  const 흠 = [], 적기 = (칸, 말) => { const k = "[" + 칸 + "] " + 말; if (!흠.includes(k)) 흠.push(k); };
  const 보이나 = (el) => { const s = getComputedStyle(el);
    if (s.display === "none" || s.visibility === "hidden") return false;
    const r = el.getBoundingClientRect(); return r.width > 2 && r.height > 2; };
  const 이름 = (el) => el.tagName.toLowerCase() +
    (typeof el.className === "string" && el.className ? "." + el.className.trim().split(/ +/)[0] : "");
  const 반 = (n) => Math.round(n);
  const 본문 = document.querySelector("main") || document.body;
  const bs = getComputedStyle(본문);
  const 콘텐츠폭 = 반(본문.getBoundingClientRect().width - parseFloat(bs.paddingLeft) - parseFloat(bs.paddingRight));
  const 세로막대 = 반(window.innerWidth - document.documentElement.clientWidth);
  const 푸터 = document.querySelector("footer");

  // ① GNB 가 위에 붙어 있나
  const 상단바 = document.querySelector("header, nav");
  if (상단바) { const p = getComputedStyle(상단바).position;
    if (p !== "sticky" && p !== "fixed") 적기("고정", 이름(상단바) + " 가 position:" + p + " — 굴리면 따라 올라갑니다"); }

  // ② 본문과 푸터 사이가 붙었나 (그릇이 아니라 «안 마지막 알맹이»까지 잰다)
  if (푸터 && 푸터.previousElementSibling) {
    let 끝 = 푸터.previousElementSibling;
    if (보이나(끝) && !/sticky|fixed/.test(getComputedStyle(끝).position)) {
      for (let i = 0; i < 6; i++) {
        const 안 = [...끝.children].filter((c) => 보이나(c) && getComputedStyle(c).position === "static");
        if (!안.length) break; 끝 = 안[안.length - 1];
      }
      const 틈 = 반(푸터.getBoundingClientRect().top - 끝.getBoundingClientRect().bottom);
      if (틈 >= 0 && 틈 < 16) 적기("간격", "본문과 푸터 사이가 " + 틈 + "px — " + 이름(끝) + " 바로 아래 푸터");
    }
  }

  // ③ 덩어리 사이 간격이 그 칸의 «리듬»을 벗어났나
  for (const 부모 of [본문, ...본문.querySelectorAll("*")]) {
    if (!보이나(부모) || 부모.closest("table, svg")) continue;
    const ps = getComputedStyle(부모);
    if (/flex|grid/.test(ps.display) && ps.flexDirection !== "column") continue;
    const 아이 = [...부모.children].filter((c) => 보이나(c) && getComputedStyle(c).position !== "absolute");
    if (아이.length < 3) continue;
    const 틈들 = [];
    for (let i = 1; i < 아이.length; i++) {
      const a = 아이[i - 1].getBoundingClientRect(), b = 아이[i].getBoundingClientRect();
      if (b.top < a.bottom - 2) continue;
      틈들.push({ v: 반(b.top - a.bottom), 앞: 아이[i - 1], 뒤: 아이[i] });
    }
    if (틈들.length < 3) continue;
    const 벌 = 틈들.filter((x) => x.v >= 8);
    if (벌.length < 틈들.length * 0.6) continue;
    const 리듬 = 틈들.map((x) => x.v).sort((a, b) => a - b)[Math.floor(틈들.length / 2)];
    for (const x of 틈들) {
      const 붙 = x.v <= 2, 좁 = 리듬 >= 20 && x.v < 리듬 * 0.45;
      if (!붙 && !좁) continue;
      // 같은 «종류»가 이어지는 줄은 붙는 게 맞다. 뒤에 붙은 여백 클래스(mb3·total)까지
      // 견주면 "card mb3" 와 "card mb6" 를 서로 다른 것으로 봐서 헛짚는다.
      if ((x.앞.className || "").trim().split(/ +/)[0] === (x.뒤.className || "").trim().split(/ +/)[0]) continue;
      if (x.뒤 === 푸터) continue;
      if (!붙 && /^h[1-4]$/.test(x.앞.tagName.toLowerCase())) continue;  // 제목 밑 부제는 붙는 게 맞다
      적기("간격", 이름(x.뒤) + " 와 위 " + 이름(x.앞) + " 사이 " + x.v + "px (이 칸 리듬은 " + 리듬 + "px)");
    }
  }

  // ④ 버튼·배지가 늘어났나 / 동그란 버튼이 타원이 됐나
  const 부품 = [...document.querySelectorAll(".badge, .btn, button, [class*=badge], [class*=btn]")].filter((el) => {
    if (!보이나(el)) return false;
    // 버튼을 «담는 칸»(.btns 같은 것)은 부품이 아니다 — 넓은 게 맞다
    if (el.querySelector(".badge, .btn, button")) return false;
    // 일부러 칸을 꽉 채우라고 시킨 것
    if (/block|full|wide/.test(el.className || "")) return false;
    return true;
  });
  const 무리 = new Map();
  const 열쇠 = (el) => (el.className || "").trim().split(/ +/)[0] || el.tagName;
  for (const el of 부품) { const k = 열쇠(el); if (!무리.has(k)) 무리.set(k, []); 무리.get(k).push(el); }
  for (const el of 부품) {
    const s = getComputedStyle(el), r = el.getBoundingClientRect();
    if (s.borderTopLeftRadius.includes("%") && parseFloat(s.borderTopLeftRadius) >= 40 && r.width / r.height > 1.5)
      적기("버튼", 이름(el) + " 는 동그란 버튼인데 " + 반(r.width) + "x" + 반(r.height) + " 로 늘어나 타원이 됐습니다");
    const rg = document.createRange(); rg.selectNodeContents(el);
    const 글폭 = rg.getBoundingClientRect().width; if (!글폭) continue;
    const 또래 = 무리.get(열쇠(el)) || [];
    const 또래폭 = 또래.map((x) => x.getBoundingClientRect().width).sort((a, b) => a - b)[Math.floor(또래.length / 2)];
    if (또래.length >= 2 && r.width < 또래폭 * 2.5) continue;             // 다 같이 넓으면 일부러 그런 것
    const 남 = 반(r.width - 글폭 - parseFloat(s.paddingLeft) - parseFloat(s.paddingRight));
    if (남 > 140) 적기("늘어남", 이름(el) + " 가 " + 반(r.width) + "px 인데 글은 " + 반(글폭) +
      "px — 부모가 flex 세로칸이면 align-items 를 주세요");
  }

  // ⑤ 좌우로 미는 칸에 «막대»가 드러났나
  for (const el of document.querySelectorAll("*")) {
    if (!보이나(el)) continue;
    const s = getComputedStyle(el);
    if (!/auto|scroll/.test(s.overflowX) || el.scrollWidth <= el.clientWidth + 2) continue;
    const 두께 = el.offsetHeight - el.clientHeight - parseFloat(s.borderTopWidth) - parseFloat(s.borderBottomWidth);
    if (두께 > 2) 적기("막대", 이름(el) + " 에 가로 막대가 " + 반(두께) + "px 드러납니다");
  }

  // ⑥ 표가 제 칸을 넘쳤나
  for (const t of document.querySelectorAll("table")) {
    if (!보이나(t) || !t.parentElement) continue;
    const 넘침 = 반(t.scrollWidth - t.parentElement.clientWidth);
    if (넘침 > 4) 적기("표", "표가 제 칸보다 " + 넘침 + "px 넓습니다");
  }

  return JSON.stringify({
    화면: location.pathname.split("/").pop() || "(첫 화면)",
    콘텐츠폭, 세로막대,
    뒤로가기: !!document.querySelector(".back, [class*=back]"),
    탭이가는곳: [...document.querySelectorAll("[data-go], .tab[href], .tabs a")]
      .map((x) => x.getAttribute("data-go") || x.getAttribute("href")).filter(Boolean),
    흠,
  }, null, 1);
})()
```

**`흠` 에 담겨 나온 것은 그 화면에서 바로 고치세요.** 하나도 안 남을 때까지 다시 돌립니다.

**화면 «하나»로는 알 수 없는 것 셋** — 여러 화면의 결과를 모아 견주세요.
- **콘텐츠폭** 이 화면마다 다르면 옮길 때 글이 좌우로 밀립니다. 모든 화면이 한 값이어야 합니다.
- **세로막대** 가 어떤 화면은 0, 어떤 화면은 15 면 그만큼 화면이 좌우로 튑니다. `html { scrollbar-gutter: stable; }` 로 잡으세요.
- **뒤로가기** 가 `탭이가는곳` 으로 이어진 화면들 사이에서 갈리면 안 됩니다. 탭이면 넷 다 없는 쪽이 맞습니다.

### 7-10. 나온 오류는 «그 자리에서» 고칩니다
> 적어 두고 넘어가면 다음에 또 나옵니다. 찾은 김에 고치는 것이 제일 쌉니다.

- 고친 뒤에는 **그 화면을 다시 눌러** 정말 고쳐졌는지 봅니다. 고쳤다고 생각만 하고 넘어가는 일이 잦습니다.
- **한 화면에서 고친 것이 다른 화면을 깨뜨리지 않았는지** 봅니다. 공통 CSS·공통 스크립트를 고쳤다면 특히 그렇습니다.
  (짜임이 다른 화면 — 사이드바형과 상단바형 — 에 같은 규칙을 씌우면 한쪽이 무너집니다.)
- 같은 뿌리에서 난 것이면 **한 곳만 고치고 끝내지 말고** 같은 자리를 다 찾아 고칩니다.
- 고칠 수 없는 것(자료가 없어서, 화면이 아직 없어서)은 **화면에 솔직히 적습니다.** 「견본이라 어느 것을 눌러도 이 화면이 열립니다」처럼요.

### 7-11. 무엇을 확인했는지 적어 주세요
- 위 항목을 **숫자로** 적어 남깁니다 — 「눌러 본 화면 N개 · 빈 링크 0개 · 반응 없는 단추 0개 · 안 걸러지는 필터 0개 · 안 눌리는 썸네일·배너 0개 · 뒤로가기 어긋남 0개 · 화면 목록 단추 없는 화면 0개 · 화면끼리 안 맞는 숫자 0개 · 가로 넘침 0개 · **고친 것 N건**」.
- 숫자를 적을 수 없으면 아직 확인한 것이 아닙니다.

### 바꿀 값은 한 곳에
- **상호** · **주소** · **전화번호** · **이메일** · **영업시간** · **로고·대표 이미지** — 손님이 받아서 **제일 먼저 자기 것으로 바꾸는 값**입니다.
- 위 값은 **데이터 파일 한 곳**에 모으고, 화면은 전부 그 값을 불러 씁니다.
- 화면 코드·헤더·푸터는 물론 **페이지 제목(`<title>`)과 메타 설명까지** 그 값을 씁니다 — 여기가 제일 자주 빠집니다.
- 안내 문구 안에 상호가 섞여 있어도(예: 「내비게이션에 ‘○○’를 넣으세요」) 글자로 적지 말고 값을 끼워 넣습니다.
- 다 만든 뒤 **상호를 다른 이름으로 한 번 바꿔 보고** 옛 이름이 남는 곳이 있는지 찾아 보세요. 한 곳이라도 남으면 아직 안 끝난 것입니다.
### 파일을 어디에 두나

```
완성화면/
  index.html              홈 화면 (손님이 처음 보는 화면)
  화면목록.html            우리가 검수용으로 보는 «전체 화면 목록»
  pages/
    HO-01.html            화면 하나에 파일 하나 · 이름은 화면 ID 그대로
    CO-03.html
  assets/
    css/style.css         스타일 한 벌
    js/app.js             동작 한 벌
```

- **`pages/` 안에서는 `../assets/css/style.css` 로 부릅니다.** `assets/...` 라고 쓰면 `pages/assets/...` 를 찾다가 못 찾아서 **스타일이 통째로 안 붙습니다** — 글꼴도 여백도 격자도 전부 날아갑니다.
- ⭐ **`index.html` 은 «전체 화면 목록»입니다. 홈 화면이 아닙니다.** 이 폴더는 손님이 검수하는 **시안**이라, 열자마자 몇 개를 만들었는지 한눈에 보여야 합니다. 홈 화면은 `pages/` 안에 다른 화면들과 나란히 둡니다.
- **`index.html` 에는 화면을 «하나도 빼지 않고»** 메뉴별로 묶어 넣습니다. 화면 ID 와 이름을 함께 적어, 거기서 아무 화면이나 바로 열 수 있어야 합니다. 홈에서 닿는 것만 넣으면 나머지는 영영 못 봅니다.
- `index.html` 은 `완성화면/` 바로 아래 둡니다. 거기서는 `assets/...` 그대로 부르면 됩니다.
- **스타일과 동작은 각각 한 벌**입니다. 화면마다 따로 만들면 44장이 서로 달라집니다.
- 화면끼리 오갈 때는 `CO-03.html` 처럼 **같은 폴더 안 이름**으로 겁니다.
- 다 만든 뒤 **화면 하나를 브라우저로 직접 열어 보세요.** 글꼴이 기본 고딕으로 보이면 경로가 틀린 것입니다.

### 추천 스택
- 정적 다중 페이지(HTML/CSS) 또는 컴포넌트 기반(React·Next.js·Vue 등) 중 프로젝트 규모에 맞게 선택하세요. 화면이 많고 상태가 복잡하면 컴포넌트 기반을 권장합니다.
