/**
 * 임시 저장(만들다 만 프로젝트)을 몇 개까지 남길지.
 *
 * "AI팩 만들기"를 누를 때마다 빈 프로젝트가 하나씩 생겨서, 그냥 두면 끝없이 쌓인다
 * (실제로 한 계정에 64개까지 쌓였다 — 2026-08-03). 새로 만들 때마다 오래된 것부터
 * 정리해 이 개수로 유지한다(application/prune-draft-projects.ts).
 *
 * 화면(서버·클라이언트)이 같이 읽는 값이라 순수 상수만 두는 파일로 뺐다 —
 * application 쪽에 두면 클라이언트 컴포넌트가 서버 코드를 끌어온다.
 */
export const DRAFT_KEEP = 10;
