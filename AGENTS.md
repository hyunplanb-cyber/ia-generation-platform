<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

---

# ⛔ 결제 경로 — 개발은 «구독», 종량제는 «손님 몫»만

**2026-08-24 사장님 지시.** 이 규칙은 돈이 걸려 있어 다른 무엇보다 앞선다.

| 무엇을 할 때 | 어디서 나가야 하나 |
| --- | --- |
| **여기서 하는 개발·분석·검수·에이전트 작업** | 사장님의 **Claude Max 구독** 주간 사용량 |
| **손님이 홈페이지에서 AI팩을 만들 때** | 앤트로픽 콘솔의 **API 종량제 잔액** |

## 절대 하지 않는 것

- `ANTHROPIC_API_KEY` · `ANTHROPIC_AUTH_TOKEN` 을 **셸·환경변수·설정 파일에 내보내지 않는다.**
  하나라도 있으면 Claude Code 가 구독이 아니라 **종량제**로 돈다.
- `settings.json` 에 `apiKeyHelper` 를 넣지 않는다.
- **개발용 스크립트에서 앤트로픽 SDK 를 직접 부르지 않는다.** 모델에게 뭘 물어야 하면
  이 대화(구독) 안에서 한다. 2026-08-24 에 그런 찌꺼기(`_작업/판정.mjs`)를 지웠다.

## 반대로, 이것은 «있어야 맞는» 것이다 — 지우면 서비스가 죽는다

- **Vercel 의 `ANTHROPIC_API_KEY`** — 손님이 AI팩을 만들 때 쓰는 열쇠다.
- **`.env.local` 의 `ANTHROPIC_API_KEY`** — 로컬에서 앱을 띄워 볼 때 같은 몫이다.
- **`adapters/` · `app/` · `application/` · `lib/` 의 API 호출** — 그게 손님 몫이다.

## 확인하는 법

```bash
npx tsx 결제경로.mts        # 새는 곳이 있으면 1 로 끝난다
```

## ⚠ 열쇠에 «만료»를 걸지 않는다 (아래에 규칙이 하나 더 있다)

2026-07-15 에 만든 열쇠가 **1개월 만료**로 설정돼 있어 8/15 에 죽었다.
그 뒤 **9일 동안** 아무도 몰랐고, 8/20 에 손님 한 분이 두 번 눌러 보고 그냥 갔다.
새 열쇠를 만들 때 만료는 **없음(Never)** 으로 둔다.

---

# ⛔ 손님 화면에 나가는 글 — 「눈앞의 그 손님」을 박아 넣지 마라

**2026-09-01 에 같은 사고를 두 번 냈다.** 진단 처방 예시를 다듬다가 그때 보고 있던
손님 사이트의 실제 내용이 그대로 박혔고, **다른 업종 손님 화면에 떠서** 「왜 남의 내용이
나오죠?」가 됐다. 한 번 고치고 두 항목 뒤에 또 했다.

  · 「기본 상담은 무료이고, **시술 비용은 30만원부터**입니다」
  · 「**고도 난시 99% 개선** (2016년 아시아·태평양 굴절 레이저 심포지엄 발표)」

## 규칙

- **손님 모두가 보는 글**(배점표 · 처방 · 예시 코드)에는 특정 업종·회사·수치를 넣지 않는다.
  채워 넣을 자리는 `여기에` · `OO` · `회사 이름` 처럼 **표시**를 남긴다.
- 문구를 다듬을 때 **「이 글이 누구에게 보이나」**를 먼저 묻는다.
  한 사이트를 보며 쓴 문장은 그 사이트 것이 섞이기 쉽다.
- 타입검사도 빌드도 이걸 못 잡는다. 문법은 멀쩡하고 화면도 잘 뜨기 때문이다.

## 확인하는 법

```bash
npx tsx 검수-진단문구.mts        # 새는 곳이 있으면 1 로 끝난다
```

배점표·처방을 손댔으면 **끝났다고 말하기 전에 반드시 돌린다.**
이 검사는 ① 특정 손님 내용 ② 근거 없는 배점 ③ 축 만점 100 ④ 「뺀 항목」 중복까지 함께 본다.

# ⛔ 배점·기준은 «근거»가 있을 때만 넣고 뺀다

`lib/diagnose/` 의 배점은 논문·공식 문서에 매여 있다. 그럴듯해 보인다는 이유로
항목을 넣거나 빼지 마라. **2026-09-01 에 `<summary>` 를 감으로 넣을 뻔했고,
「출처 인용」을 근거 없이 뺐다가 논문에 +28% 로 측정돼 있는 걸 뒤늦게 확인했다.**

- 넣을 때: `criteria.ts` 의 `source` 에 근거 문서를 반드시 단다. 근거가 없으면 `ours` 로 적고
  **공개 배점표에 「우리 판단」이라고 그대로 드러낸다.**
- 뺄 때: `EXCLUDED` 에 왜 뺐는지와 근거를 적는다.
- ⚠ **우리가 가진 근거는 대부분 «글»을 대상으로 잰 것이다.** HTML 태그로 재면 정확해 보이지만
  논문이 잰 것과 어긋난다(`<blockquote>` 를 세다가 따옴표만 쓴 글을 0점 준 적이 있다).
