# 작업지시 — 다운로드 시 오푸스 보강

2026-08-01 결정. **이 문서만 읽고 그대로 진행하면 된다.** 앞선 대화는 없다고 보면 된다.

---

## 왜 하는가

지금 AI팩은 **처음부터 끝까지 하이쿠**로 만든다.

| 하는 일 | 지금 모델 | 파일 |
|---|---|---|
| IA·화면 생성 | `claude-haiku-4-5` | `adapters/generation/llm/claude-ia-generator.ts:10` |
| 화면 프롬프트 | `claude-haiku-4-5` | `adapters/prompt/llm/claude-prompt-generator.ts:4` |
| 검수 | `claude-haiku-4-5` | `adapters/verify/llm/claude-verifier.ts:9` |

그런데 **판매하는 AI팩은 사람이 Opus로 만든 것**이다. 그래서 두 문제가 동시에 생긴다.

1. **39,000~69,000원을 내고 다운로드한 고객이 받는 게 하이쿠 결과물이다.** 그게 본체 상품인데.
2. **"만들어진 문서를 단순 다운로드만 하는데 39,000원?"** 이라는 값 근거의 약함.

**다운로드할 때 오푸스로 본문을 다시 쓰면 둘 다 풀린다.** 다운로드가 "내보내기"가 아니라 **최종 제작**이 된다.

---

## 설계 — 재생성이 아니라 "보강"이다

고객의 진짜 걱정은 이것이다.

> 미리보기 보고 괜찮아서 받았는데 다른 결과물이 나오면 어쩌지?

**문구로 덮지 말고 설계로 푼다.** 뼈대는 잠그고 살만 붙인다.

| | 다운로드 때 | |
|---|---|---|
| 화면 목록 · 화면 개수 · 화면ID · 화면명 | **잠근다** | 미리보기와 100% 같아야 한다 |
| 메뉴 구조 | **잠근다** | 위와 같음 |
| 기능정의 · AI 프롬프트 본문 | **오푸스가 다시 쓴다** | 더 촘촘하게 |
| 사용자가 손으로 고친 칸 | **건드리지 않는다** | Story 3.6(재실행 시 수정 보존) 로직 재사용 |

이러면 "다른 게 나오면 어쩌지"가 성립하지 않는다. 화면 구성이 그대로니까.

**고객에게 보여줄 문구(정직해진다):**
> 화면 구성은 미리보기 그대로, 각 화면의 설명만 더 좋은 모델로 다시 씁니다.

---

## 모델과 비용

| | 입력 | 출력 |
|---|---|---|
| Haiku 4.5 | $1 / 1M | $5 / 1M |
| **Opus 4.8** (`claude-opus-4-8`) | $5 / 1M | $25 / 1M |

추정 원가(환율 1,400원):

| | 하이쿠(미리보기) | 오푸스(다운로드) | 합계 | 받는 돈 | 원가 비중 |
|---|---|---|---|---|---|
| 30~50화면 | 220원 | 약 1,100원 | 1,320원 | 39,000원 | 3.4% |
| 100~150화면 | 630원 | 약 3,700원 | 4,330원 | 69,000원 | 6.3% |

**추정치다. 한 번 돌려보고 Anthropic 콘솔의 실제 토큰으로 검증할 것.**

**프롬프트 캐싱을 반드시 켠다.** 같은 시스템 프롬프트 + 컨셉이 30번 반복되므로, 입력 비용이 90% 가까이 떨어진다(100~150화면 기준 3,700원 → 약 3,200원).

---

## 기술 제약 — 60초 벽

`adapters/generation/llm/claude-ia-generator.ts:15` 주석에 실측이 남아 있다.

> 한 번에 전부 쓰게 하면 글자 수만큼 시간이 쌓여(실측 117초) 서버 제한 60초를 넘겼다.
> 나눠서 동시에 쓰면 가장 느린 묶음 하나만 기다리면 된다. (117초 → 37초)

지금은 `SCREENS_PER_CALL = 5`로 피하고 있다. **오푸스는 하이쿠보다 느리므로 이 벽에 다시 부딪힐 수 있다.**

- 먼저 5개 그대로 재보고, 넘치면 3개로 줄인다.
- 그래도 안 되면 백그라운드 작업으로 돌리고 "준비되면 받으세요"로 바꾼다(마지막 수단 — UX가 나빠진다).

---

## 할 일

1. **오푸스 보강 어댑터 신규** — `adapters/generation/llm/` 아래. 기존 하이쿠 생성기와 **분리**한다(생성은 계속 하이쿠).
   - 입력: 확정된 화면 목록(ID·이름) + 컨셉 + 기존 기능정의·프롬프트
   - 출력: 기능정의·프롬프트 본문만
   - 화면 수가 입력과 출력에서 다르면 **실패로 처리**한다(뼈대가 흔들리면 안 됨)
2. **다운로드 흐름에 끼우기** — `application/download.ts`
3. **손으로 고친 칸 보존** — Story 3.6 로직 확인 후 재사용
4. **프롬프트 캐싱 적용**
5. **실제 원가·소요 시간 측정 후 위 표 갱신**

---

## 하지 말 것

- **생성(미리보기)을 오푸스로 바꾸지 않는다.** 무료 체험 남용을 하이쿠로 싸게 막는 게 설계 의도다.
- **화면 개수·ID·이름을 오푸스가 바꾸게 두지 않는다.** 미리보기와 어긋나면 클레임이 된다.
- **다운로드 크레딧 차감 코드를 여기서 붙이지 않는다.** 그건 PG 연동(작업 8)과 함께 간다. 지금 `downloadCost()`는 화면에 숫자만 보여주고 실제 차감은 없다(`spendCredits` 호출처는 생성 3곳 + 프리셋 2곳뿐).

---

## 이 다음 작업

- **검수도 같은 패턴** — 다운로드 시 오푸스로 **추가 검사**. 단 **기존 판정은 유지하고 새로 찾은 것만 더한다**(덮어쓰면 "통과였는데 실패로 바뀜"이 되어 클레임). 판정이 곧 상품이라 뒤집으면 안 된다.
- **크몽 3단 등록** — 스탠다드 64,000 / 플러스 103,000 / 프리미엄 201,000 (홈 가격 ÷ 0.8, 크몽 수수료 약 20%)

---

## 참고 — 오늘 이미 끝낸 것 (건드리지 말 것)

- **크레딧**: 1크레딧 = 100원 유지. 생성 10/23, 다운로드 390/690, 무료 35크레딧 (`lib/credits.ts`)
- **등급**: 사다리가 아니라 **2×2**. 스탠다드 50,900 / 플러스 82,200 / 디럭스 106,800 / 프리미엄 160,400 (`lib/packages.ts`의 `PlanId` 주석 참고)
- **디자인 프리셋**: AI를 쓰지 않는다. AI팩 zip과 별개이고 따로 과금한다. 판매본 사양은 `salePresetConfig()`
