# 작업지시 — 판매 패키지 제작

2026-08-01 기준. **이 문서만 읽고 그대로 진행하면 된다.**

기존 `package-template.mts` 헤더 주석은 **3등급 시절 것이라 낡았다.** 여기가 최신이다.

---

## 1. 등급은 사다리가 아니라 2×2다

축이 둘이다 — **설계 깊이 × 완성 화면 유무**.

|  | 문서만 | + 검수 시나리오 + 완성 화면(HTML) |
|---|---|---|
| **2뎁스** (30~50화면) | 스탠다드 **50,900** | 디럭스 **106,800** |
| **3뎁스** (100~150화면) | 플러스 **82,200** | 프리미엄 **160,400** |

**가격 원칙: 판매가 = 크레딧으로 직접 만들 때 드는 돈.** 재고는 남의 컨셉이라는 약점이 있어서, 직접 만들기보다 비싸면 "그럼 직접 만들지"가 된다.

**이름은 일부러 사다리처럼 뒀다.** 2×2를 대놓고 보여주면 구매자가 표를 만들어 계산한다. 대신 **홈에서 넷을 한 줄에 나란히 놓지 않는다.**

**디럭스는 아직 만들 수 없다.** 30~50화면짜리 완성 HTML이 없다. 만들면 `lib/packages.ts`의 `makePlans(..., { base: 43 })`로 살아난다. **없는 걸 팔지 않는다.**

값과 구조의 단일 출처는 `lib/packages.ts`의 `PlanId` 주석이다. 손으로 옮겨 적지 말 것.

---

## 2. 디자인 프리셋 3종 — 판매팩 전용 사양

### 판매팩과 유저 생성물은 다른 물건이다

| | 무엇이 들어가나 | 값 |
|---|---|---|
| **유저가 만들 때** | **본인이 고른 1벌** — 테마·색·글꼴·모서리·밀도·모드 전부 커스텀 | 10,900원 |
| **판매팩** | **기본값 3벌** — 손대지 않은 설정, 그중 하나를 골라 씀 | 10,900원 |

**판매 3종은 커스텀 3벌이 아니라 기본값 3벌**이고, 구매자는 어차피 하나만 쓴다. "고를 수 있게 셋을 넣어 드린다"가 정확한 설명이라 **1벌 값이 맞다.** 3벌 값(32,700원)을 매기지 않는다.

**AI팩 생성·다운로드에는 디자인이 들어가지 않는다.** 플랫폼에서 프리셋은 `/dashboard/[projectId]/preset`에서 따로 만들고 따로 받고 따로 차감한다(`application/preset.ts`). `lib/export/`에 프리셋 관련 파일이 없는 게 그 이유다.

### 사양 — `salePresetConfig()` 하나로 고정

```ts
// lib/design-presets.ts
salePresetConfig(style) = { ...defaultPresetConfig(style), font: "pretendard" }
```

| 항목 | 값 | 왜 |
|---|---|---|
| **테마** | 업종에 맞는 3종 (또는 랜덤 3종) | `PackageDef.presetStyles`에서 고른다 |
| **포인트 색상** | 그 테마의 **첫 번째 색** | `primarySwatchesFor(style)[0]` |
| **글꼴** | **프리텐다드 고정** | 판매본에 페이퍼로지·명조가 섞이면 "이 폰트를 어디서 받나"가 먼저 걸린다 |
| **모서리** | **테마 기본값 그대로** | 파스텔의 둥근·모노의 각진 모서리는 그 테마의 성격이다. 통일하면 셋의 차이가 색뿐이 되어 나란히 놓았을 때 구분이 안 된다 |
| **밀도** | **넉넉하게**(cozy) | 카드 내부 24px · 섹션 간격 40px |
| **모드** | **라이트** | 다크는 넣지 않는다 |

**모서리만 예외라는 점을 잊지 말 것.** 처음엔 전부 "기본"으로 통일하려다, 테마 개성이 사라져서 되돌렸다.

### 테마 6종 중 3종을 고른다

| key | 이름 | 성격 |
|---|---|---|
| `navy` | 모던 네이비 | 신뢰감 있고 정돈된, 실무형 |
| `mono` | 미니멀 모노 | 색을 뺀 여백·활자 중심 |
| `pastel` | 소프트 파스텔 | 부드럽고 친근한 분위기 |
| `retro` | 레트로 페이퍼 | 종이 질감 · 오렌지 & 틸 |
| `forest` | 내추럴 그린 | 차분한 자연 · 친환경 |
| `coral` | 코럴 선셋 | 밝고 따뜻한 생기 |

**지금은 세 업종 모두 `["navy", "mono", "pastel"]`로 같다.** 업종에 맞게 바꾸려면 `lib/packages.ts`의 `presetStyles` 배열만 고치면 된다. 바꾸면 **해당 업종의 프리셋 파일도 다시 만들어야 한다**(아래 참고).

`presetFits`는 그 3종이 이 업종에서 어디에 어울리는지를 적은 문구다. **`presetStyles`와 순서가 같아야 한다.**

### 프리셋 파일의 실제 위치

판매 zip은 **미리 만들어 둔 파일을 복사**한다. 코드가 생성하지 않는다.

```
판매용_템플릿/{업종}/디자인프리셋/
  프리셋_01_모던네이비.md / .json
  프리셋_02_미니멀모노.md / .json
  프리셋_03_소프트파스텔.md / .json
  프리셋_미리보기.html          ← 3종 비교
```

`package-template.mts`의 `presetsFrom`이 이 폴더를 가리킨다. 심화판(상세IA)은 기본판 폴더에서 가져다 쓴다 — 업종당 한 벌뿐이라서.

**주의: `판매용_템플릿/`은 `.gitignore`에 걸려 있다**(45번 줄). 프리셋 파일을 고쳐도 git에 안 들어간다. 되돌리려면 OneDrive 이력을 봐야 한다.

**점검 이력(2026-08-01):** 21개 파일 중 모던 네이비 7개만 카드 20px · 섹션 32px로 좁아서 24px · 40px로 고쳤다. 나머지는 이미 사양에 맞았다. 미니멀 모노는 24px · 48px로 오히려 더 넉넉해서 그대로 뒀다 — *여백과 활자*가 그 테마의 정체성이라 좁히면 성격이 사라진다.

---

## 3. 아직 안 된 것 — `package-template.mts`가 3등급을 모른다

zip 패키징 스크립트가 **스탠다드 / 디럭스 / 프리미엄** 세 칸 기준으로 짜여 있다. 새 4등급(**스탠다드 / 플러스 / 디럭스 / 프리미엄**)과 이름도 뜻도 어긋난다.

| 옛 이름 | 지금 뜻 |
|---|---|
| 스탠다드 | 스탠다드 (그대로) |
| **디럭스** | → **플러스** (3뎁스 문서만) |
| 프리미엄 | 프리미엄 (그대로) |
| — | **디럭스** (2뎁스 + 검수 + HTML) — 새 칸, 아직 물건 없음 |

**고칠 것:**
1. 헤더 주석을 2×2로 다시 쓴다
2. 등급 키를 `lib/packages.ts`의 `PlanId`에서 가져온다 — 손으로 적지 않는다
3. **등급별로 무엇이 들어가고 빠지는지** 표를 만든다

| | 문서 8종 | 스펙팩 | 프리셋 3종 | 검수 시나리오 | 완성 화면(HTML) |
|---|---|---|---|---|---|
| 스탠다드 | ✅ 2뎁스 | ✅ | ✅ | — | — |
| 플러스 | ✅ 3뎁스 | ✅ | ✅ | — | — |
| 디럭스 | ✅ 2뎁스 | ✅ | ✅ | ✅ | ✅ |
| 프리미엄 | ✅ 3뎁스 | ✅ | ✅ | ✅ | ✅ |

4. 화면 수가 `lib/packages.ts`의 값과 다르면 **패키징을 실패시킨다**(지금도 그렇게 되어 있다 — 유지할 것)

---

## 4. 크몽은 3단 고정

크몽 패키지는 **STANDARD / DELUXE / PREMIUM 세 칸**뿐이라 넷을 다 못 올린다. 화면 규모가 커지는 **일직선**으로 셋만 고른다 — 2×2를 크몽에 넣으면 반드시 혼란이 난다.

| 크몽 칸 | 홈 상품 | 홈가 | **크몽가** (÷0.8) |
|---|---|---|---|
| STANDARD | 스탠다드 | 50,900 | **64,000** |
| DELUXE | **플러스** | 82,200 | **103,000** |
| PREMIUM | 프리미엄 | 160,400 | **201,000** |

수수료 약 20%를 얹은 값이다. **크몽이 더 비싼 건 정상이다** — 거기서 파는 건 에스크로와 노출이다. 홈페이지가 더 싼 게 자연스러운 유입 장치가 된다.

디럭스(2뎁스 + HTML)는 **홈페이지 전용**으로 둔다.

---

## 하지 말 것

- **디럭스를 만들어 팔지 않는다** — 30~50화면 완성 HTML이 없다
- **프리셋을 3벌 값으로 매기지 않는다** — 기본값 3벌이지 커스텀 3벌이 아니다
- **모서리를 통일하지 않는다** — 테마 기본값 그대로 둔다
- **AI팩 zip에 디자인 프리셋을 넣지 않는다** — 별개 상품이고 따로 차감한다
- **가격·화면 수를 문서나 이미지에 손으로 적지 않는다** — `lib/packages.ts`에서 읽는다. 실제로 브리프 화면의 "4크레딧"이 손으로 적혀 있어서, 값을 10으로 올린 뒤에도 화면에만 옛 값이 남았다

---

## 참고

- 상세 페이지 8단 흐름: `_작업지시_상세페이지_재구성.md` — **낡았다.** `plan=deluxe`가 이제 `plan=plus`다. 흐름만 참고할 것
- 다운로드 오푸스 보강: `_작업지시_다운로드_오푸스보강.md`
- 크몽 등록 가이드: `판매용_템플릿/_배포/{업종}/크몽_등록가이드.md`
- 패키징: `npx tsx package-template.mts` (전체) / `... travel-premium` (하나만)
