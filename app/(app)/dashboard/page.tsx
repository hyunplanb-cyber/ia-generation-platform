// 임시 placeholder — Story 1.4에서 실제 대시보드(프로젝트 목록)로 교체된다.
// 이 파일이 바뀌어도 위의 (app)/layout.tsx(GNB)는 그대로 유지된다.
export default function AppPlaceholderPage() {
  return (
    <div className="mx-auto max-w-6xl px-6 py-12">
      <p className="text-muted-foreground">여기는 로그인 후 화면입니다.</p>
    </div>
  );
}
