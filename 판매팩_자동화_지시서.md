# 판매팩 자동화 지시서

매주 월요일 11시에 이 문서를 읽고 **다음 안 끝난 칸 하나**를 한다.
사람이 완성화면을 한 장씩 열어 보지 않는다는 전제로 도는 일이다.
그러니 **검사기를 통과하지 못한 것은 끝난 것이 아니다.**

파는 물건이다. 10만원을 낸 사람이 열어 보고 실망하면 끝이다.

---

## 절대 규칙 — 어길 바에는 그 주를 건너뛴다

1. **`완성화면/` 안의 파일을 지우지 않는다.** 사람이 만든 것이 섞여 있다.
   새 업종의 새 폴더에만 쓴다. `build-all.mts` 가 전후 지문을 떠서 감시한다 —
   지문이 달라지면 스스로 멈추고 실패로 끝난다. 그때는 `git` 으로 되돌린다.
2. **값을 지어내지 않는다.** 색·간격·크기는 반드시 `lib/design-presets.ts` 와
   각 팩의 `스펙팩/가이드_*.json` 에서 읽어 쓴다. 눈대중으로 정하지 않는다.
3. **검사가 통과하지 못한 것은 «절대» 배포하지 않는다.** ⭐ 2026-08-10 에 규칙이 바뀌었다.
   전에는 「실서버에 올리지 않는다」였다. 지금은 **B 회차가 배포까지 한다**(사장님 결정).
   그래서 이 줄이 브레이크의 전부다 — 검사기 4개 FAIL 0 이 아니면 **B 회차를 중단하고**
   브랜치에 둔 채로 보고한다. 「거의 다 됐으니 올리자」는 없다. 파는 물건이다.
4. **못 한 것을 했다고 적지 않는다.** 검사기 출력 숫자를 그대로 보고한다.
   FAIL 이 남았으면 남았다고 적고 끝낸다.

---

## 업종은 «조사해서» 고른다 — 사람에게 묻지 않는다

자동화다. 「무엇을 만들까요」를 사장님께 묻는 순간 자동화가 아니다(2026-08-10 에 물었다가 바로잡혔다).

### 고르는 네 관문 — 넷 다 통과해야 만든다

**① 지금 뜨는 업종·서비스를 조사한다 (시장성)**
웹 검색으로 근거를 찾는다. 시장이 자라는지, 창업이 느는지.
찾은 근거를 **이 문서에 URL 과 함께 적는다** — 다음 회차가 같은 조사를 되풀이하지 않게.

**② 주제나 업종이 «명확»한가**
한 문장으로 「누가 무엇을 하는 곳」이라고 말할 수 있어야 한다.
「통합 비즈니스 관리자 시스템」은 **떨어진다** — 누구의 무엇인지가 없다.
그래서 판매 보류로 정해져 있었고(`package-template.mts` 주석), 그걸 놓치고 만들었다가 버렸다.

**③ 우리 손님이 실제로 적어 본 말을 본다** ⭐ 가장 가까운 수요다

```bash
npx tsx 조사-생성키워드.mts        # 최근 90일
npx tsx 조사-생성키워드.mts 30     # 최근 30일
```

우리 앱에 들어와 컨셉을 적어 본 사람들의 말을 센다.
**바깥 통계보다 훨씬 가깝다** — 이미 우리를 찾아왔던 사람들이기 때문이다.
「우리가 아직 못 만든 것」으로 걸러 주니, 거기 있는 말이 곧 후보다.

- 테스트로 적은 것(「ㅇㅇ」·「ㄹㄹㄹ」)은 스크립트가 걷어낸다
- **누가 적었는지는 보지 않는다.** 낱말의 수만 센다
- 표본이 적으면 「쓸 수 없습니다」라고 스스로 말한다. 그때는 ①만으로 고른다

2026-08-10 첫 조사 — 90일 28건 중 테스트 16건을 빼니 12건.
그중 우리가 못 만든 것 8건에 **인테리어 시공 · 필라테스 스튜디오 예약 ·
전자책·디지털콘텐츠 판매 · 콘텐츠 자동화 툴 · 병원**이 있었다.

**④ 카페24·아임웹으로 «안 되는가»** ← 가장 자주 걸리는 관문
되는 것을 만들면 우리는 값으로 진다. **실제로 되는지 찾아본다.**
2026-08-10 조사에서 후보 둘이 여기서 떨어졌다.

| 후보 | 왜 떨어졌나 |
|---|---|
| 정기배송 구독몰 | 카페24 가 **정기결제를 무료로** 제공한다 |
| 유료 멤버십 커뮤니티 | 아임웹 「회원그룹 이용권」이 결제→등급→콘텐츠 열람 제한까지 한다 |

기록: 카페24 정기배송 <https://www.cafe24.com/commerce/manage/periodic.html> ·
아임웹 회원그룹 이용권 <https://imweb.me/faq?mode=view&category=29&category2=40&idx=71371>

> ③에 많이 나왔다고 그대로 만들지 않는다. **네 관문을 다 지나야 한다.**
> 많이 찾는다고 우리가 이기는 칸은 아니다 — 반려동물 쇼핑몰은 ③에 나왔지만
> 일반 쇼핑몰이라 ④에서 떨어진다.

---

## 한 회차에 무엇을 하나 — «두 회차에 한 업종을 다 끝내고 판다»

> ⚠ **2026-08-10 에 크게 바뀌었다.** 전에는 「한 주에 작은 칸 하나」였다.
> 그렇게 하니 한 업종을 파는 데 여덟 주가 걸렸다. 사장님이 바로잡았다 —
> **「일주일에 한 칸이 아니라, 2주에 한 업종 4등급이 완성되어 홈페이지에 배포되는 것」.**
> 아래 A·B 두 회차가 그 뜻이다. **작은 칸을 하나씩 세지 않는다.**

**매주 월요일에 돌되, 두 번이 한 짝이다.** A 로 시작해 B 로 끝내고, 다음 업종의 A 로 넘어간다.

| 회차 | 무엇을 | 끝났을 때 |
|---|---|---|
| **A** (홀수 회) | 업종 고르기 → 재료(`template-data`·`-deep`) → 프리셋 → 배선 → 굽기 → **디럭스 완성화면** | 4등급 문서가 다 나오고 디럭스 화면까지 선다 |
| **B** (짝수 회) | **프리미엄 완성화면** → 검사 4개 → 눌러 보기 → 캡처·영상 → 진열 배선·값 → **머지·배포** → 실서버 확인 | **홈페이지에서 팔린다** |

**둘로 나눈 까닭** — 완성화면이 디럭스 40장 안팎, 프리미엄 130~160장이다.
한 회차에 200장을 만들려 들면 중간에 시간이 모자라 멈추거나, 멈추기 싫어 대충 만든다.
**대충 만든 것을 파는 것이 가장 나쁘다.** 그래서 무게가 비슷하게 갈랐다.

### A 회차에 하는 일 — 「팔 준비가 반쯤 된 상태」로 끝낸다

1. 업종 고르기 — 위 「네 관문」. 근거 URL 을 이 문서에 적는다
2. `template-data-<키>.ts` — 8~9메뉴 37~43화면
3. `template-data-<키>-deep.ts` — 잎사귀를 3뎁스로
4. `build-design-presets.mts` 의 `TARGETS` 에 색 3 × 레이아웃 2 를 넣고 프리셋을 굽는다
5. **배선 네 군데** (아래 절) — 여기까지 하면 4등급 문서가 다 나온다
6. `npx tsx build-all.mts <키>` 로 굽는다
7. **디럭스 완성화면** — 스펙팩(07)을 보고 한 장씩
8. 브랜치 `pack/<키>-<날짜>` 에 커밋. **A 회차는 배포하지 않는다**

### B 회차에 하는 일 — 「팔리는 상태」로 끝낸다

1. **프리미엄 완성화면** — 같은 스펙의 상세 IA 판
2. **검사 4개** — FAIL 0 이 아니면 **여기서 멈춘다.** 배포하지 않고 보고한다
3. 눈으로 10장 (첫 화면 반드시 포함) + 탭·잠긴 버튼 눌러 보기
4. `npx tsx 팩재료-캡처영상.mts <키>` — **뽑은 캡처를 열어 본다**
5. 진열 — `lib/packages.ts` 의 `LISTED_IDS` 에 키를 넣고, 값 4등급을 적는다
   (**값은 「직접 만들 때 드는 크레딧과 같은 값」**. 지어내지 않는다)
6. 소개 문구가 **이웃 팩과 겹치지 않는지** 본다 (아래 「겹치는 팩」)
7. `npx tsx package-template.mts --zip` — 판매 zip 을 다시 굽는다.
   **이걸 빼먹으면 손님이 사도 내려받지 못한다**(2026-08-10 에 걸렸다)
8. `main` 에 머지하고 밀어 **배포**한다
9. **실서버에서 눈으로 확인한다** — 팩 목록에 칸이 늘었나, `/packages/<키>` 가 열리나,
   갈래 숫자가 맞나. 확인 못 하면 배포는 끝난 것이 아니다
10. 진행 표시와 발견기록을 고치고 보고한다

### 겹치는 팩 — 진열 전에 반드시 본다

새 팩을 세울 때 **이웃 팩과 소개 문구가 겹치면 손님이 못 고른다.**
새 팩의 낱말로 `lib/packages.ts` 와 `build-design-presets.mts` 를 뒤져,
다른 팩 소개에 같은 낱말이 있으면 **양쪽 문구를 갈라 적는다.**

> 실제로 걸린 것 — 매칭 팩 프리셋 소개에 「이사·**인테리어**처럼 금액이 큰 분야」가 있다.
> 인테리어 팩을 나란히 세우면 헷갈린다.
> **매칭은 「여러 업체를 이어 주는 플랫폼」, 인테리어는 「내 업체 하나의 사이트」** 로 갈라 적는다.

---

## 지금 어디까지 왔나

진행 표시: `[ ]` 안 함 · `[~]` 하다 말았음(이어서 한다) · `[x]` 끝

### 1) `rental` 장비 렌탈·대여 예약 플랫폼 → 갈래 「커머스·거래」 — ✅ 다 끝났다

> **1-a 부터 1-h 까지 전부 끝났다(2026-08-10).** 24칸에 진열까지 됐다.
> **다음 주에는 아래 「2) 다음 업종」 부터 시작한다.**

**세 관문을 다 지났다(2026-08-10 조사).**

- ① 주제 — 「캠핑·카메라 장비를 날짜를 잡아 빌려주고 돌려받는 곳」. 한 문장으로 말해진다.
- ② 수요 — 캠핑 장비 시장 2026 년 220.8 억 달러, 연 5.92% 성장.
  캠핑카 렌탈 «플랫폼» 은 연 9.2% 로 더 빠르다.
  <https://www.mordorintelligence.kr/industry-reports/camping-equipment-market> ·
  <https://www.360researchreports.com/ko/market-reports/motorhome-rental-platform-market-210944>
- ③ SaaS 로 안 됨 — 쇼핑몰 재고는 「몇 개 남았나」인데, 렌탈 재고는 **「이 날짜에 비었나」**다.
  같은 물건이 날짜마다 다르게 잡힌다. 여기에 반납·연체료·보증금·왕복 배송·파손 점검이 붙는다.
  카페24·아임웹의 상품 모델로는 표현이 안 된다.

재료부터 만들어야 한다.

- [x] **1-a.** `template-data-rental.ts` — 8메뉴 37화면 363요건 (2026-08-10)
- [x] **1-b.** `template-data-rental-deep.ts` — 잎사귀 124개 → 3뎁스 161화면 933요건 (2026-08-10)
- [x] **1-c.** `TARGETS` 에 `rental`·`rental-deep` — 내추럴그린·미니멀모노·레트로페이퍼 ×
      검색중심형·목록중심형 (2026-08-10)
- [x] **1-d.** `build-all`·`build-template`·`build-template-deep` 배선 후 굽기 완료 (2026-08-10)
- [x] **1-e.** 디럭스 완성화면 37장 — 내추럴 그린 × 검색중심형 (2026-08-10 완료)
- [x] **1-f.** 프리미엄 완성화면 161장 — 레트로 페이퍼 × 목록중심형 (2026-08-10 완료)
- [x] **1-g.** 캡처·영상 재료 — 캡처 16장 · 영상 4편 (2026-08-10 완료)
      뽑는 김에 HO-01 의 결함 두 개를 찾아 고쳤다 — 아래 「캡처와 영상 재료」 절
- [x] **1-h.** 배선 네 군데 + 검사 4개 통과 + 눌러 보기 (2026-08-10 완료)

> 1-e·1-f 는 각각 하루치 일이다. 한 주에 하나씩 잡는다.
> 만들 때 `_작업/장비렌탈_대여예약/07_AI빌드_스펙팩.json`(37) 과
> `_작업/장비렌탈_대여예약_상세IA/07_AI빌드_스펙팩.json`(161) 을 각각 쓴다.

이 팩에서 꼭 살릴 화면 — **날짜별 가용 재고 달력 · 반납 처리 · 연체·파손 정산 · 보증금 환급.**
이 넷이 「SaaS 로 안 되는 것」의 증거다. 여기가 얇으면 이 팩을 만든 뜻이 없다.

### 2) `interior` 인테리어 시공 견적·시공관리 → 갈래 「커머스·거래」

**네 관문을 다 지났다(2026-08-10 조사).** 아래 「어떻게 골랐나」에 근거가 있다.

**A 회차 — `[~]` 하다 말았다. 다음 회차가 이어서 끝낸다.**

- [x] 업종 고르기 — 네 관문 통과, 근거 기록 (2026-08-10)
- [x] `template-data-interior.ts` — 9메뉴 39화면 (2026-08-10)
- [x] `TARGETS` 에 `interior`·`interior-deep` — 미니멀모노·모던네이비·레트로페이퍼 ×
      사진 중심형·대시보드형, 프리셋 구움 (2026-08-10)
- [ ] `template-data-interior-deep.ts` — 잎사귀를 3뎁스로 펼친 상세 IA
- [ ] 배선 네 군데 + `npx tsx build-all.mts interior` 로 굽기
- [ ] **디럭스 완성화면 39장** — 미니멀 모노 × 사진 중심형

**B 회차 — A 가 끝난 다음 회차. 여기서 판다.**

- [ ] **프리미엄 완성화면** — 레트로 페이퍼 × 대시보드형
- [ ] 검사 4개 **FAIL 0** (아니면 여기서 멈추고 배포하지 않는다)
- [ ] 눈으로 10장 (첫 화면 포함) + 탭·잠긴 버튼 눌러 보기
- [ ] `npx tsx 팩재료-캡처영상.mts interior` + **캡처 열어 보기**
- [ ] 진열 — `LISTED_IDS` 에 `interior`, 값 4등급
- [ ] **매칭 팩과 소개 문구 가르기** (위 「겹치는 팩」 — 이 팩은 여기가 걸려 있다)
- [ ] `npx tsx package-template.mts --zip`
- [ ] `main` 머지 → **배포** → 실서버 확인

> A 회차(2026-08-10)가 왜 반만 됐나 — 그날은 아직 「한 주에 작은 칸 하나」 규칙으로 돌았다.
> 업종 고르기·재료·프리셋까지 하고 끝냈다. 규칙이 바뀌었으니 다음 회차가 A 의 남은 셋을
> 마저 하고, 그 다음 회차가 B 를 한다. **이번 업종만 세 회차가 된다.** 다음 업종부터 두 회차다.

이 팩에서 꼭 살릴 화면 — **공정표(철거→설비→목공→타일→도배→마루→준공) ·
현장 사진 일지 · 추가공사 변경 견적 승인 · 준공 검수와 하자보수 접수.**
이 넷이 「SaaS 로 안 되는 것」의 증거다. 여기가 얇으면 이 팩을 만든 뜻이 없다.
견적 계산기만 두껍고 공정이 얇으면 **카페24 앱 하나에 지는 팩**이 된다(④ 참고).

#### 어떻게 골랐나 — 네 관문

**① 시장성** — 인테리어 시장 25조원 규모. 그중 **리모델링·개조가 2025년 시장의 47.85%**,
2031년까지 **연 11.78%** 로 가장 빠르게 자란다. 인테리어 디자인 서비스 시장 자체는 연 5.83%.
<https://www.mordorintelligence.kr/industry-reports/interior-design-services-market> ·
<http://www.conslove.co.kr/news/articleView.html?idxno=72748>

**② 주제가 명확한가** — 「집을 고치는 공사를 맡아 견적부터 준공·하자보수까지 굴리는
인테리어 시공 업체의 사이트」. 한 문장으로 말해진다.
**플랫폼이 아니라 «한 업체»의 사이트**라는 것이 이 팩의 자리다.

**③ 손님이 적은 말** — `npx tsx 조사-생성키워드.mts` (90일 28건 → 테스트 16건 빼고 12건).
두 번 이상 나온 낱말은 아직 없다(표본 부족). 다만 **2026-08-03 에 그대로 적혀 있었다** —
「나는 인테리어 업자야. 주로 화장실 인테리어와 타일 시공을 주로 해.」
**손님이 아니라 «업자»가 적었다.** 이 사람이 원한 것은 남을 이어 주는 플랫폼이 아니라
**자기 일감을 받는 자기 사이트**다. 그래서 매칭 팩이 아니라 새 팩이 맞다.

**④ 카페24·아임웹으로 안 되는가** — 여기가 아슬아슬했다. **반쯤은 된다.**

| 되는 것 | 어디서 |
|---|---|
| 면적 넣으면 금액 나오는 계산기 + 견적요청 수집 | 카페24 앱 「면적계산기 & 맞춤견적」 **월 1만원** |

그 앱은 커튼·블라인드·장판·벽지의 **면적 × 단가 곱셈**이고 **결제도 없는 계산기**다.
시공 일정·현장 방문 예약은 아예 없다. 아임웹은 **시간 단위 상담 예약이 어렵다고
스스로 적어 두었다**(쇼핑 기능으로 우회하라고 안내한다).

안 되는 것 — **여러 날에 걸친 공정표**, 공정별 진행 사진 일지, 공사 도중 생기는
**추가공사 변경 견적과 승인**, 계약금·중도금·잔금 분할, 준공 검수 체크리스트,
하자보수 A/S 접수. 쇼핑몰의 「상품 한 개를 장바구니에 담아 결제」 모델로는 표현이 안 된다.

> 근거: 카페24 앱 <https://store.cafe24.com/kr/apps/1656> ·
> 아임웹 시간 단위 예약 <https://imweb.me/faq?mode=view&category=30&category2=50&idx=71407>

#### 매칭 팩과 얼마나 겹치나 — 재 봤다 (겹침 낮음)

지난 회차가 「매칭과 결이 겹칠 수 있다」고 남겨 둔 숙제다. 메뉴를 세어 봤다.

| | 매칭(숨고형) | 인테리어 시공 |
|---|---|---|
| 누구의 사이트 | **여러 고수를 이어 주는 플랫폼**(3자) | **한 시공 업체**의 사이트(2자) |
| 견적 | 여러 고수가 보낸 것을 **비교**한다 | 내가 조건을 넣으면 **계산되어 나온다** |
| 돈 | 고수가 견적 보낼 때 **크레딧 차감** | 계약금·중도금·잔금 **분할 청구** |
| 일이 시작된 뒤 | 채팅으로 조율, 끝나면 정산 | **공정표·현장 일지·추가공사·준공·하자보수** |

겹치는 메뉴는 홈·계정·후기 셋뿐이고, 이 셋은 **어느 팩에나 있는 것**이다.
매칭에 있는 「요청서 단계별 질문 · 고수 찾기 · 받은 견적 비교 · 크레딧 충전 · 고수센터」는
여기 하나도 없고, 여기 있는 「공정표 · 현장 일지 · 추가공사 승인 · 하자보수」는
매칭에 하나도 없다. **다른 팩으로 본다.**

⚠ 다만 매칭의 프리셋 소개 글에 「이사·**인테리어**처럼 금액이 큰 분야」가 적혀 있다
(`build-design-presets.mts` 의 `matching.fits[0]`). 두 팩을 나란히 진열할 때
**손님이 헷갈릴 자리**다. 진열할 때 사람이 한 번 볼 것 — 「매칭은 플랫폼, 인테리어는
내 업체 사이트」가 소개 문구에서 갈라져 보여야 한다.

#### 떨어진 후보들 — 다음 회차가 되풀이하지 않게

| 후보 | 어느 관문에서 | 왜 |
|---|---|---|
| 전자책·디지털콘텐츠 판매 | ④ | 카페24·아임웹이 디지털 상품 판매를 기본으로 한다 |
| 필라테스 스튜디오 예약 | ② 겹침 | 뷰티샵 예약 팩과 「예약」 뼈대가 거의 같다. 회차권 차감만 다르다 |
| 병원 예약 | ④ 약함 | 아임웹·카페24 로 예약 붙은 병원 홈페이지가 실제로 만들어져 팔리고 있다. 진짜 안 되는 칸(차트·수납)은 의료법상 우리가 팔 자리가 아니다 |
| 콘텐츠 자동화 마케팅 툴 | ② | 「누가 무엇을 하는 곳」이 아니라 도구다. `admin` 이 모호해서 보류된 것과 같은 결 |

### 3) 다음 업종 — 아직 안 정했다

2)가 끝나는 주에 **네 관문으로 다시 조사해서** 정한다.
③의 표본이 아직 적으니(90일 12건) 그때 다시 돌려 본다 —
두 번 이상 나온 낱말이 생겼으면 그것이 가장 센 실마리다.

> 두 등급의 프리셋을 **일부러 다르게** 둔다. 같은 설계로 얼마나 다른 얼굴이
> 나오는지 나란히 보여 주는 것이 이 팩의 값이다. 여섯 업종이 모두 그렇게 되어 있다.
>
> ⚠ **업종마다 받는 프리셋이 정해져 있다.** 아무 색이나 고를 수 없다.
> `build-design-presets.mts` 의 `TARGETS` 에 업종별로 색 3개·레이아웃 2개가 박혀 있다.
> 처음에 admin 이 안 받는 색(`forest`·`retro`)을 적어 뒀다가 만들 때 알았다(2026-08-10).
> **고르기 전에 `TARGETS` 를 먼저 본다.** 새 업종이면 거기 칸부터 만든다.
>
> **스펙이 레이아웃을 이긴다.** 기능정의서가 「좌측 사이드 내비」를 못 박는데
> 고른 레이아웃이 상단 GNB 면, 레이아웃을 바꾼다.
> 기능정의서는 사는 사람과의 약속이다.
>
> 바이올렛(`pastel`)은 안 쓴다. 본문 대비 1.01 로 재서 폐기한 색이다.

### 보류함 — 만들지 않는다

| 업종 | 왜 |
|---|---|
| `admin` 통합 비즈니스 관리자 시스템 | **주제가 모호해 판매 보류.** 2026-08-10 에 모르고 디럭스 37화면을 만들었다. 만든 것은 `pack/admin-20260810` 브랜치(GitHub 에 올려 뒀다)와 커밋 `1c8adbe` 에 있다 — 우리 관리자 화면 서비스를 열 때 참고용으로만 본다. **판매팩 폴더에서는 뺐고 굽는 목록에서도 뺐다.** 되굽고 싶으면 `npx tsx build-all.mts admin` 처럼 이름을 직접 대야 한다. |
| 일반 종합 쇼핑몰 | 카페24와 정면으로 붙는 칸이다. 일부러 피한다. |
| 정기배송 구독몰 | 카페24 정기결제가 무료다. |
| 유료 멤버십 커뮤니티 | 아임웹 회원그룹 이용권이 기본형을 덮는다. |

---

## 완성화면 만드는 법

이미 만들어 둔 다섯 업종이 본이다. **가장 가까운 것을 통째로 베껴 고치는 것이 가장 빠르고 안전하다.**

- 백오피스·표 중심이면 → `판매용_템플릿/_판매팩/매칭_프리미엄/완성화면/`
- 카드·목록 중심이면 → `판매용_템플릿/_판매팩/공동구매_프리미엄/완성화면/`

폴더 짜임새:

```
<라벨>_<등급>/완성화면/
  index.html            화면 목록(자동 생성)
  pages/                화면 HTML (자동 생성 — 손으로 고치지 않는다)
  assets/               app.js · base.css
  스펙팩/
    07_AI빌드_스펙팩.json      ← _작업/ 에서 복사
    가이드_<번호>_<이름>.json   ← 고른 색 프리셋
    레이아웃_<글자>_<이름>.json  ← 고른 레이아웃
  build/
    generate.mjs        스펙팩 JSON → pages/*.html
    data.mjs            그 업종의 가짜 데이터(이름·값 등)
    ui.mjs              공용 조각(카드·표·탭·버튼)
    pages-*.mjs         화면별 본문 만드는 함수들
    check.mjs
```

만들고 나면 `node build/generate.mjs` 를 사이트 루트에서 돌린다.

### 여기서 반드시 밟은 지뢰 — `레이아웃견본_발견기록.md` 에 자세히 있다

1. **화면 이름 두 체계가 갈린다.** 화면 안에서는 `HO-01` 로 짧게 쓰고
   파일 이름은 `HO0101` 이다. `short2spec` / `toPageId` 로 반드시 이어 준다.
   안 이으면 **끊어진 링크가 100개 단위로 생긴다** (실제로 220개 났다).
2. **잠긴 버튼은 `disabled` 만으로 안 열린다.** 여는 장치를 붙여야 한다.
   **팩마다 이름이 다르니 그 팩의 `assets/js/app.js` 를 먼저 열어 확인한다** —
   `data-unlock="버튼id"` 를 쓰는 팩도 있고 `data-gate`/`data-gated` 를 쓰는 팩도 있다.
   「확인 후 열립니다」라고 써 놓고 여는 길이 없으면 거짓말이 된다.
3. **탭과 몸통은 «같은 상자» 안에 있어야 한다.** ⭐ 2026-08-10 에 또 밟았다.
   `app.js` 는 눌린 탭의 `.tabs` 를 찾아 그 **부모 안에서만** `[data-pane-body]` 를 찾는다.
   탭을 `<div class="mt8">` 에, 몸통을 그 «옆» `<div class="mt6">` 에 두었더니
   **탭은 색만 바뀌고 몸통은 그대로였다.** 화면은 멀쩡해 보이고 검사기 넷도 다 통과했다.
   눌러 보고서야 나왔다 — 「눌러 보기」를 지우면 안 되는 까닭이다.
   (속성 이름도 `data-pane` 이다. `data-panes` 라고 쓰면 조용히 아무 일도 안 한다.)
4. **개발자 패널을 기본으로 펼치지 않는다.** 손님이 볼 화면이 아니다.
5. **일괄 치환(`sed`·`perl`)을 넓게 걸지 않는다.** 탭 고치려던 치환이 표 머리글까지
   건드려 화면에 `[object Object]` 가 찍힌 적이 있다. 파일 단위로 좁게 고친다.
6. **잠기는 버튼은 `<a>` 로 만들 수 없다.** ⭐⭐ `<a>` 에는 `disabled` 가 없다.
   `btn()` 이 href 가 있으면 `<a>` 를 만들면서 id·잠금을 «조용히 버려», 「동의하면 열립니다」라고
   써 놓은 버튼이 **처음부터 열려 있었다**(2026-08-10, 렌탈 팩에서 5군데).
   잠글 것이 있으면 `<button data-go="…">` 로 만들고 옮기는 일은 app.js 가 한다.
7. **숫자끼리 아귀를 맞춘다.** 대시보드의 「오늘 매출」과 그래프의 오늘 값,
   사이드바 배지 수와 화면 안 건수가 따로 놀면 **사는 사람이 먼저 알아챈다.**
   `data.mjs` 한곳에서 계산해 쓰고, 다 만든 뒤 한 번 훑는다.
   달력에서 «고른 기간»에 막힌 날이 끼어 있는데 요약이 「내내 가능」이라고 한 적이 있다.
8. **재는 자에도 헛경보가 있다.** `<button>` 안에 flex 를 쓰면 브라우저가 `scrollWidth` 를
   6px 쯤 크게 잡는다. 자식 폭 합이 맞으면 넘친 것이 아니다 — 자식을 직접 재서 갈라낸다.
9. **감싸는 상자에 두 글자짜리 이름을 붙이지 않는다.** ⭐⭐ 2026-08-10, 렌탈 홈.
   히어로 속통을 `class="in"` 으로 감쌌는데 `.in` 은 그 팩의 **입력칸(input) 클래스**였다.
   `height:42px` 가 그대로 먹어서 속통이 42px 로 굳고, 안에 든 288px 이 **밖으로 흘러넘쳐**
   아래 카드 위에 겹쳐 그려졌다. 화면 위쪽 절반이 뭉개진 채로 팔리고 있었다.
   `.in` `.on` `.go` `.sel` 같은 짧은 이름은 **이미 쓰이고 있다고 보고** `hero-in` 처럼 붙인다.
   새 상자 이름을 지으면 **그 팩의 `base.css` 에서 먼저 찾아본다.**
10. **CSS 가 기다리는 이름과 생성기가 붙이는 이름은 서로 모른다.** ⭐⭐ 같은 날 같은 화면.
   `.searchbar .go` 를 만들어 두고 생성기는 `btn-w`(폭 꽉 채우기)를 붙였다.
   버튼이 검색바 아래로 내려가 한 줄을 통째로 차지했다.
   **CSS 에 규칙을 쓰면 그 이름을 실제로 붙이는지 화면에서 확인한다.** 이것이 세 번째다
   (잠긴 버튼 `<a>` · 탭과 몸통 상자 · 그리고 이것). 둘을 «이름»으로만 이으면 조용히 어긋난다.
11. **같은 문구를 여러 곳에 적으면 반드시 갈라진다.** 화면목록 「먼저 읽어 주세요」 안내가
   `build-template.mts` 와 `build-template-deep.mts` 에 한 벌씩 있었고, 그래서 손님이
   직접 만들어 받는 zip 에는 **아예 안 들어갔다**(2026-08-09). 지금은
   `lib/export/template-verify.ts` 한 벌이다. 다만 **완성화면을 굽는 `generate.mjs` 11개가
   각자 박아 두고 있다** — 팩 안에서 혼자 도는 파일이라 공용 모듈을 못 읽는다.
   이 문구를 고칠 일이 생기면 **열한 곳을 같이 고친다.**
12. **끊어진 링크에는 «반대쪽»이 있다.** ⭐ 2026-08-10, 인테리어 `template-data` 를 쓰면서.
   1번은 「가리키는 곳이 없는 버튼」인데, 그 거울로 **「아무도 안 가리키는 화면」**이 있다.
   `es5`(견적 견주기)·`pr5`(준공 검수)·`pr6`(진행 중 없음) 셋이 그랬다.
   화면은 만들어지고 파일도 생기므로 **`check-pack` 은 통과한다** — 스펙에 있는 화면이
   전부 있으니까. 그런데 손님은 **거기까지 갈 길이 없다.** 힘들여 만든 화면이 아무도
   못 보는 채로 팔린다. 특히 **빈 상태·검수·견주기처럼 「곁가지」 화면**이 잘 이렇게 된다.
   `template-data` 를 다 쓰면 `btns` 가 가리키는 ref 를 모아 **어느 ref 가 그 모음에
   없는지** 한 번 센다. 열 줄짜리 스크립트면 된다.

---

## 화면을 만드는 김에 캡처와 영상 재료를 같이 뽑는다

**따로 시간을 내면 안 하게 된다.** 화면이 막 만들어진 그 자리에서 뽑는다.
(2026-08-10 첫 회에 이 절을 통째로 빠뜨렸다. 그래서 **코드로 옮겼다** — 아래 한 줄이면 된다.)

```bash
npx tsx 팩재료-캡처영상.mts rental        # 팩 키로 불러도 되고 「장비렌탈」로 불러도 된다
```

굽는 차례(`build-all.mts`)의 **6번**에 들어가 있다.
`npx tsx build-all.mts rental` 처럼 **업종을 대고 부르면 저절로 뽑힌다.**
업종 없이 전체를 구울 때는 시간이 오래 걸려 건너뛰는데, **건너뛰었다고 화면에 말한다.**
조용히 넘어가지 않는다 — 조용히 넘어갔다가 첫 회를 통째로 빠뜨렸다.

| 무엇 | 어디에 | 어디 쓰나 |
|---|---|---|
| 화면 캡처 세로(PNG 1440×1150) | `_마케팅/릴스영상/_화면캡처/{업종}_{등급}_{색}/` | 영상 재료 |
| 화면 캡처 가로(PNG 1440×1000) | 〃 `/가로/` | 카드뉴스에 넣을 그림 |
| 넘어가는 영상(mp4) | `_마케팅/릴스영상/{n}. {업종}/{등급}_16x9.mp4` | 유튜브·홈페이지 |
| 〃 | 〃 `/{등급}_4x5.mp4` | 인스타 피드·릴스 |
| 자동 넘김 페이지 | `{팩}/완성화면/00_자동넘김.html` | 화면 녹화용 |

### 스스로 정하는 것 — 손이 갈 자리를 없앤다

- **어느 화면을 찍나** — 메뉴마다 한 장. 8~10장이면 서비스가 한 바퀴 돈다.
  전부 찍으면 3뎁스 팩에서 161장이 되고, 골라 적으면 팩마다 손이 간다.
- **어느 화면을 앞세우나** — `index.html` 위쪽 「이 팩의 알맹이」에 적힌 화면이 그 메뉴에 있으면
  그것을 쓴다. 자랑할 화면은 팩이 스스로 적어 두었다. (렌탈은 「날짜별 재고 달력」)
- **차례** — 스펙팩(07)의 메뉴 차례. 손님이 서비스를 쓰는 차례가 그것이다.
- **여백 색** — 그 팩의 가이드 JSON `background`. **지어내지 않는다.**

### 여기서 밟은 것

- **GIF 로 내지 않는다. 처음부터 mp4 로 낸다.** 인스타는 GIF 를 받으면 어차피 mp4 로 바꾼다.
- ⚠ **한글 폴더에서 헤드리스 크롬은 조용히 아무것도 안 만든다.**
  완성화면을 통째로 `%TEMP%` 아래로 복사해 두고 거기서 찍는다.
- ⚠ **한 프로세스에서 서버를 열고 `execFileSync` 로 크롬을 부르면 영영 멈춘다.**
  크롬이 보낸 요청을 받아 줄 차례가 안 온다. 10분을 아무 소리 없이 기다렸다.
- ⚠ **창을 1800px 로 길게 잡으면 짧은 화면은 아래가 빈 채로 나온다.**
  1150px — 손님이 처음 보는 만큼 — 으로 잡으면 어느 화면이든 꽉 찬다.
- ⚠ **「화면 정보」 패널이 홍보물에 그대로 들어간다.** 우리 확인용이지 손님 것이 아니다.
  원본을 고치지 말고 «옮겨 놓은 사본»의 CSS 에만 `.dev{display:none}` 을 덧붙인다.
- 폴더 이름에 **업종·등급·색**이 드러나게 둔다. 나중에 「어느 팩의 어느 화면인가」를 알아야 한다.

### 여기까지가 이 루틴의 몫이다 — 편집은 SNS 루틴이 한다

**판매팩 자동화는 «재료»를 릴스 폴더에 넣어 두는 데까지다.**
자막·고양이·영상틀·길이 맞추기·유튜브 올리기는 **월요일 11시 SNS 루틴**(`weekly-sns-content`)이 한다.

- 여기서 나오는 mp4 는 **자막 없이 2.6초씩 넘어가기만 하는 재료**다. 그대로 올릴 것이 아니다.
- 4:5 는 데스크톱 화면(1440px)을 줄여 넣은 것이라 **폰에서는 글자가 작다.**
  그건 흠이 아니라 재료의 성질이다 — 확대 컷과 자막은 SNS 루틴이 얹는다.
- 그러니 **여기서 편집을 시작하지 않는다.** 두 루틴이 같은 영상을 두 번 만들게 된다.

넣는 자리만 정확히 지키면 된다. SNS 루틴이 이 경로를 보고 찾아간다.

```
판매용_템플릿/_마케팅/릴스영상/
├── _화면캡처/{업종}_{등급}_{색}/        캡처 세로 (+ /가로/)
└── {n}. {업종}/{등급}_16x9.mp4          영상 (+ _4x5.mp4)
```

### 뽑은 것은 반드시 «열어 본다»

캡처를 열어 보다가 **팩의 진짜 결함 두 개**를 찾았다(2026-08-10, 렌탈 HO-01).
검사기 넷이 다 통과시킨 화면이었다. 자세한 것은 `레이아웃견본_발견기록.md` 에 있다.
**홍보물을 만드는 일이 곧 마지막 검사다.** 안 열어 보면 결함을 홍보하게 된다.

---

## 배선하는 곳 네 군데 — 하나라도 빠지면 조용히 안 팔린다

| 파일 | 넣을 것 |
|---|---|
| `build-all.mts` | `업종들` 배열에 키 |
| `package-template.mts` | `INDUSTRIES` 에 `{ key, label, title, base, deep, siteBase, siteDeep }` |
| `lib/packages.ts` | `PACKAGES` 에 `PackageDef` + `category` + `LISTED_IDS` 에 키 |
| `lib/packages.ts` | 가격(등급 4개) — **직접 만들 때 드는 크레딧과 같은 값**으로 |

`label` 은 `lib/packages.ts` 의 `fileLabel` 과 **반드시 같아야 한다.**
어긋나면 `package-template.mts` 가 스스로 멈춘다(그러라고 넣어 둔 검사다).

`category` 는 `lib/packages.ts` 의 `CATEGORIES` 에서 고른다 —
렌탈은 `commerce`(커머스·거래)다.

### 저절로 따라 들어가는 것 — 새로 배선하지 않는다

새 업종을 배선하면 아래는 **알아서 같이 들어간다.** 손대지 않는다.

| 무엇 | 어디서 오나 |
|---|---|
| `09_사이트_내놓는_법.html` (모든 등급) | `_마케팅/부록_사이트_내놓는_법.html` — **원본 한 벌.** `package-template.mts` 가 팩과 `public/guide/deploy-guide.html` 두 곳으로 복사한다 |
| `02_IA_화면목록.xlsx` 의 「먼저 읽어 주세요」 | `lib/export/template-verify.ts` 의 `SCREEN_LIST_NOTE` |
| `08_검수시나리오.xlsx` 표지 안내 | 〃 `CHECK_NOTE_FULL` |

**고칠 일이 생기면 원본만 고친다.** 팩 안의 사본을 고치면 다음에 구울 때 덮인다.
`public/guide/` 에 두는 이름이 **영문인 까닭**은 한글 파일 이름이 주소가 되면 404 가 나기 때문이다.
손님이 받는 zip 안에서만 한글 이름으로 되돌린다.

---

## 검사 — 이 순서를 바꾸지 않는다

```bash
npx tsx check-presets.mts                  # 1. 가이드가 스스로 앞뒤가 맞나
npm run pack                               # 2. 재료 다시 굽기 + 빠진 칸 세기
npx tsx check-design.mts "<팩 폴더>"        # 3. 화면이 가이드대로인가
npx tsx check-pack.mts  "<팩 폴더>"         # 4. 화면이 스펙대로인가 + 버튼이 여나
npx tsx package-template.mts --zip         # 5. 판매 zip 다시 굽기 (B 회차)
npx tsx check-zip.mts                      # 6. **손님이 받는 파일이 성한가**
npx tsc --noEmit                           # 7. 타입
```

**6번은 앞의 셋이 «못 보는 자리»를 본다.**
1·3·4 는 만드는 «재료»를 본다 — 스펙, 가이드, 화면. 아무도 zip 을 열어 보지 않았다.
그래서 2026-08-10 에 **뷰티샵 디럭스가 `완성화면/index.html` 없이 6일을 팔렸다.**
화면 49장은 다 있는데 들어갈 문이 없었다. 검사기 넷이 다 통과시킨 상태였다.

6번이 보는 것 — 「파일이 있다」가 아니라 **「열린다」**.
0바이트 · 안 열리는 엑셀 · 깨진 pptx · 파싱 안 되는 json(BOM 포함) · 빈 html ·
짝 없는 프리셋 · **없는 화면을 가리키는 목록** · 아무도 안 가리키는 화면 ·
옛 파일이 섞여 나감 · 진열됐는데 zip 이 없음.

> 처음 돌렸을 때 바로 세 건이 나왔다 — 여행 두 팩의 `pageids.json` 에 BOM 이 붙어
> `JSON.parse` 가 깨졌고, 뷰티샵 zip 에 `index_old.html` 이 딸려 나가고 있었다.

**1번을 건너뛰지 않는다.** 틀린 가이드로 화면을 재면 전부 통과한다.
가이드에 「이 색 위 글자」 칸이 없어서 두 벌의 버튼 글자가 안 읽혔던 적이 있다(대비 1.01).

**통과 기준: 3·4번 FAIL 0건.** WARN 은 하나하나 보고 「왜 괜찮은지」를 적는다.
헛경보를 내는 검사기는 없는 것만 못하니, 규칙이 틀렸으면 규칙을 고친다.

### 검사기가 못 보는 것 — 사람 대신 반드시 눈으로 본다

검사기는 **자기가 재기로 한 것만** 잰다. 만든 화면 중 **10장을 무작위로 골라**
브라우저로 열고 다음을 본다. 판단이 서지 않으면 스크린샷을 보고서에 붙인다.

- 첫 화면 · 목록 화면 · 상세 화면 · 폼 화면 · 빈 상태 화면이 한 장씩은 들어가게 고른다
- **첫 화면은 무작위에 맡기지 않는다. 반드시 넣는다.** 손님이 제일 먼저 보는 화면이다.
  2026-08-10 에 렌탈 홈이 10장에 안 들어가서, 위쪽 절반이 뭉개진 채로 통과했다.
- 글자가 상자를 뚫고 나가지 않는가
- **덩어리끼리 겹쳐 그려지지 않았는가** — 검사기 셋 중 **어느 것도 「어디에 그려졌나」를 안 잰다.**
  값이 맞고 파일이 있으면 통과다. 겹침은 실제로 그려 봐야만 안다.
- 표가 짜부라지지 않았는가 (사이드바 안에 또 2단을 넣어 겪은 적이 있다)
- 눌러 봤을 때 탭·버튼이 실제로 반응하는가
- 「① 목록 ② 규칙을 적은 표 ③ 다음 행동 버튼」 세 덩어리가 서 있는가
- **폰 폭(375~390px)에서 오른쪽으로 삐져나가지 않는가** ⭐ 검사기가 아예 못 재는 것이다
  브라우저 pane 을 375 로 줄여 본다. **첫 화면부터** 본다.
  ⚠ **헤드리스 크롬으로는 못 잰다** — `--window-size=390` 을 줘도 482px 로 열린다(창 최소 폭).
  482 에서는 이 결함이 안 나온다. 재려면 pane 이나 CDP `setDeviceMetricsOverride` 를 써야 한다.
  자주 나오는 까닭 — **폭이 px 로 박힌 칸이 옆 칸을 0 까지 짜부라뜨린다.**
  `wrap-row` 여도 줄이 안 바뀐다. 옆 칸에 `min-width` 를 줘야 그제야 내려간다.

> **캡처를 뽑는 절(위쪽)이 이 눈으로 보기의 «두 번째 그물»이다.**
> 10장에서 놓친 것을 캡처에서 잡은 적이 있다. 둘 다 한다.

> **다른 루틴이 「팩에 결함이 있다」고 보고해 오면 그대로 옮기지 말고 «재 본다».**
> 2026-08-10 에 SNS 루틴이 「8화면 중 7화면이 잘린다, 뷰티샵·여행도 같다」고 했는데
> 재 보니 **129장 중 1장**이었다. 짚은 까닭도 틀렸다. 재는 데 5분이면 된다.

### 우리 사이트 화면을 고쳤을 때 — 이건 검사기가 아예 없다

판매팩에는 검사기 넷이 있지만 **우리 홈페이지에는 하나도 없다.**
그래서 홈·무료·대시보드 같은 우리 화면을 고치면 **반드시 스크린샷을 찍어 눈으로 본다.**
「글자가 들어갔나」와 「가로로 안 삐져나오나」만 재고 넘어가면 반드시 사고가 난다.

찍은 화면에서 **옆에 있는 덩어리와 다섯 가지를 견준다.**

1. **폭** — 같은 구역의 형제들과 실제 렌더 폭이 같은가 (`getBoundingClientRect().width` 로 재서 숫자로 확인)
2. **정렬** — 형제들이 왼쪽 정렬이면 나도 왼쪽인가 (`margin:0 auto` 를 나 혼자 쓰고 있지 않은가)
3. **모서리·테두리** — 그 구역이 쓰는 `border-radius`·테두리 값을 그대로 쓰는가
4. **말투** — 그 페이지 어디에도 없는 장식(이모지 동그라미, 색 굵은 세로줄 따위)을 나 혼자 들여오지 않았는가
5. **줄바꿈** — 낱말이 가운데서 잘리거나 마지막 줄에 한두 글자만 남지 않는가
   (한글은 `word-break: keep-all` 을 넣는다. 좁은 폭에서 특히 잘 깨진다)

**넓은 화면과 좁은 화면 두 장을 다 찍는다.** 1440 과 375.
좁은 화면에서 가로 스크롤이 생기면, 내가 넣은 것 때문인지 원래 있던 것 때문인지
`display:none` 으로 껐다 켜서 `scrollWidth` 를 재어 가른다. 남 탓하기 전에 재서 가른다.

> 2026-08-09 홈 메인에 안내 덩어리를 넣으면서, 옆 형제는 974px 왼쪽 정렬인데
> 나 혼자 780px 가운데 정렬 · 모서리 14px · 왼쪽 청록 줄 · 이모지 동그라미로 넣었다.
> DOM 에 글자가 있는 것과 삐져나오지 않는 것만 확인하고 **화면을 안 봤다.**
> 스크린샷 한 장이면 보였을 것이다.

---

## 끝내는 법

1. `레이아웃견본_발견기록.md` 에 **그 회차에 새로 배운 것**을 적는다.
   잘된 것 말고 **걸린 것**을 적는다. 안 걸렸으면 「안 걸렸다」 한 줄이면 된다.
2. 이 문서의 진행 표시를 고친다(`[ ]` → `[x]`). **날짜는 오늘 날짜로 적는다** —
   2026-08-10 에 오늘 한 일을 「2026-08-17」로 적었다가 스스로 고쳤다. 다음 주 날짜를 적지 않는다.
3. 커밋한다.
   - **A 회차** — `pack/<키>-<날짜>` 브랜치에 커밋. 밀지 않는다.
   - **B 회차** — 브랜치를 `main` 에 머지하고 밀어 **배포**한다. 그리고 실서버를 눈으로 본다.
4. 보고에 반드시 넣을 것:
   - **이번이 A 회차인지 B 회차인지, 그리고 다음 회차가 무엇인지**
   - 만든 화면 수 (등급별)
   - 검사기 4개의 FAIL·WARN 숫자 **그대로**
   - 눈으로 본 10장과 캡처에서 이상했던 것
   - (B 회차면) **배포 주소와 실서버에서 확인한 것** — 팩 수, `/packages/<키>` 응답
   - 사람이 해야 할 다음 한 가지

## 아직 큐에 없는 것 — 2026-08-10 에 이야기만 하고 끝난 것

> 대화에서만 오간 것은 **다음 회차의 내가 모른다.** 그래서 여기 적어 둔다.
> 여기 있는 것은 **아직 「이번 주에 할 것」이 아니다** — 사장님이 순서를 정하면 위로 올라간다.

### ① 앱 가이드 — 사장님이 하자고 한 것 (2026-08-10)

「사이트 내놓는 법」의 형제. 팩에 `10_앱으로_내놓는_법.html` 로 들어가고 24칸에 자동 배선.

- ⚠ **첫 장이 「웹뷰만으로는 애플 심사에서 떨어진다」라야 한다.** 「반응형이니 앱도 됩니다」는 반만 사실이다.
  통과하려면 푸시·카메라·생체인증·오프라인 중 하나는 있어야 한다. 이걸 안 쓰면
  손님이 심사에서 떨어지고 우리를 원망한다 — 「결제 심사 두 달」을 미리 말해 준 것과 같은 자리다.
- 권한(안드 13+ 알림 · iOS ATT · 카메라 · 위치) 각각 **「왜 필요한지」 문구를 스토어에 적어야 한다**
- 개인정보처리방침 URL 없으면 **등록 자체가 안 된다**
- 아이콘(1024²) · 스크린샷 규격 · 소개 글자 수 · 심사 기간 · 안드 데이터 안전 양식
- **우리가 자동으로 만들 수 있는 것** — 완성화면이 있으니 스토어 스크린샷을 뽑고,
  디자인프리셋에 주색이 있으니 아이콘 초안도 만든다. 캡처 스크립트를 조금만 손보면 된다
- `deviceMode` 에 「모바일 웹(앱)」이 **이미 있다.** 지금은 스펙팩 한 줄만 바뀐다.
  여기에 앱에만 있는 화면(권한 요청·알림 설정·오프라인)과 「뒤로가기를 화면 안에」를 붙인다

### ② 그 밖에 제안만 해 둔 것

| 무엇 | 왜 | 크기 |
|---|---|---|
| **사러 오는 길** | 만드는 쪽은 자동인데 90일에 손님 12명. 무료 샘플 받은 뒤 다음 걸음이 없고, 24칸 중 고르기도 손님 몫이다 | 중 |
| **검수 상품** | 오늘 만든 캡처·겹침 찾기·「검사기가 못 보는 것」 목록이 이미 그 원형이다. 우리 팩에서 실제로 결함을 잡았다 | 큼 |
| **문의 챗봇** | 값은 있으나 **문의가 늘고 나서**. 환불·결제를 잘못 답하면 돈 문제가 된다 | 중 |

### ③ B 회차 전에 사장님이 정할 것

- **매칭 팩과 인테리어 팩의 소개 문구 가르기.** 매칭 프리셋 소개에 「이사·인테리어처럼
  금액이 큰 분야」가 이미 있다. 나란히 세우면 손님이 못 고른다.
  갈래는 정해져 있다 — **매칭은 「여러 업체를 이어 주는 플랫폼」, 인테리어는 「내 업체 하나의 사이트」.**
  문구가 정해지면 2-h 배선 때 그대로 넣는다.

---

## 사람이 하는 것 — 자동화가 손대지 않는다

- **크몽 등록 자료**(썸네일·상세페이지) — 크몽은 손으로 올린다.
- **값 «정책»** — 정가는 원가 기준으로 유지한다. 깎을 일이 있으면 할인으로 한다.
  루틴은 이 정책대로 **계산해서 넣을 뿐** 새 정책을 만들지 않는다.
- **잘못 나간 것 내리기** — 배포가 자동이 된 만큼, 되돌리는 것은 사람이 판단한다.

> ⚠ **2026-08-10 에 「진열」과 「배포」가 이 목록에서 빠졌다.** 이제 B 회차가 한다.
> 대신 브레이크가 하나뿐이다 — **검사기 4개 FAIL 0.** 그것이 안 되면 배포하지 않는다.
> 「거의 다 됐으니」로 넘어가는 순간, 사람이 보지 않는 물건이 팔린다.

---

*이 문서가 곧 자동화의 기억이다. 매주 고쳐 가며 쓴다.*
