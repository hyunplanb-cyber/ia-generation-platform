# 작업 지시 — AI팩 상세 페이지를 크몽 상세와 같은 구성으로

대상: `app/(marketing)/packages/[packageId]/page.tsx` (현재 1,036줄 · 19섹션)
목표: 크몽 상세 이미지와 **같은 흐름 8섹션**으로 재구성

기준 구현이 이미 있다 → `build-kmong-images.mts`
문구·수치·순서를 거기서 그대로 가져오면 된다. **새로 쓰지 말 것.**

---

## 목표 구성

| # | 섹션 | 내용 | 현재 상태 |
|---|---|---|---|
| 0 | 히어로 | 제목·배지·지표 4개 | 있음 (유지) |
| 1 | 등급 | 카드 3장 (등급명·가격·화면수·설명) | 표 형태 → **카드로 교체** |
| 2 | 영상·데모 | 유튜브 + 데모 링크 | 있음 (위치만 이동) |
| 3 | **WHY** | 사이트 하나 만들 때 개발 단계에서 꼭 요구되는 내용 + 실무기간 3×2 | 표로 있음 → **3×2 칩으로** |
| 4 | **SAMPLE** | 이런 산출물이 들어 있어요 — 9종 실물 | **신규** ★가장 큰 작업 |
| 5 | **HOW** | 한 줄 프롬프트로 만든 사이트, 완벽할까요? + 144/899/65 + 채팅 | 부분 있음 → 재배치 |
| 6 | **SCOPE** | 개발에 꼭 필요한 부분까지 + 개발사에게/AI에게 + 개발 추가 작업 | 있음 → **두 목록 추가** |
| 7 | **TRAP** | AI는 이런 부분을 놓칠 수 있어요 + 함정 3 + 마무리 두 줄 | 있음 → 문구 교체 |
| 8 | **FILES** | 등급별로 이렇게 들어 있어요 — 체크 매트릭스 | **신규** |
| 9 | FAQ · 유의사항 · 구매 CTA | | 있음 (유지) |

## 없앨 섹션

현재 있는 것 중 아래는 SAMPLE로 흡수되거나 빠진다.

- 받으시는 산출물 (→ SAMPLE)
- 디자인 프리셋 3종 (→ SAMPLE 마지막 카드)
- 검수 시나리오 (→ SAMPLE)
- 이미 만들어 둔 화면 N개 (→ SAMPLE)
- 사이트 컨셉 / 메뉴 구조 / 3뎁스 펼침 / 예외 화면 / 화면 목록 / 화면별 프롬프트
  → SAMPLE의 `IA 화면목록` 카드 하나로 대체. 전체 목록이 필요하면 접기(details)로.
- 이런 분께 맞아요 → 유지하거나 TRAP 앞에 짧게

## SAMPLE 섹션 상세 (신규 · 9카드)

`build-kmong-images.mts`의 `03_산출물_예시` 블록을 JSX로 옮긴다. 카드 순서:

1. IA 화면목록 — 화면ID·화면명·기능정의·프롬프트 4행
2. 기능정의서 — 요구사항ID(업무-기능-구성 3단)·업무·구성 4행, **메뉴를 흩어서** 뽑을 것
3. 메뉴구조 / 4. FLOW 흐름도 — 2열
5. WBS — 간트 (한 줄 폭)
6. AI 빌드 스펙팩 — 코드 블록
7. 검수 시나리오 — `프리미엄` 태그
8. 완성 화면 HTML — index 목록, `프리미엄` 태그
9. 디자인 프리셋 3종 — 색 칩 3장

**수치는 반드시 `lib/packages.ts`에서 읽을 것.** 손으로 적으면 사이트와 어긋난다.

## 반드시 지킬 것

- 등급(`?plan=`)을 바꾸면 아래 내용이 전부 그 등급 기준으로 바뀌는 동작 유지
- 프리미엄 전용(검수·완성화면)은 `selected.verify` / `selected.siteScreens`로 분기
- LMS·뷰티는 프리미엄이 없다 — 그 등급 카드가 안 나와야 한다
- 크몽 이미지와 달리 **홈페이지에는 도메인·CTA를 넣어도 된다** (외부 유도 제한은 크몽만)

## 검증

```bash
npx tsc --noEmit
npx eslint "app/(marketing)/packages"
npm run build
```

프로덕션 배포 후 세 URL을 눈으로 확인:
- `/packages/travel?plan=standard`
- `/packages/travel?plan=premium`
- `/packages/lms?plan=deluxe` (프리미엄 없음 확인)

## 참고 — 알려진 이슈

로컬 `npm run dev`가 Turbopack 글롭 파싱 오류로 뜨지 않는다(`globals.css` 관련).
`npm run build`는 정상이므로 빌드로 확인하거나 이 문제를 먼저 고칠 것.
