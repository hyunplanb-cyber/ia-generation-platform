# 레이아웃 견본에서 나온 것

뼈대마다 실물 5장을 만들어 보고, 글로만 있던 가이드가 정말 되는지 확인한 기록.
만드는 사람은 `build-layout-samples.mts`. 뼈대·카드 CSS 는 `lib/design-presets.ts` 에서 그대로 가져온다.

| 뼈대 | 매달린 레이아웃 | 상태 |
|---|---|---|
| grid | 검색중심 · 볼드 | 실물 있었음 |
| **sidebar** | 목록중심 · 대시보드 · 에디토리얼 | **✅ 2026-08-08** |
| **multi** | 사진중심 · 좌우분할 | **✅ 2026-08-08** |
| **single** | 여백중심 | **✅ 2026-08-08** |
| **bento** | 벤토그리드 | **✅ 2026-08-08** |

**다섯 뼈대 전부 실물로 확인했다.**

---

## 고친 것

### 1. 간격 값 세 개가 없었다 → 넣음
`--card-pad` · `--row-h` · `--btn-gap` 이 아무 데도 없었다.
없어서 **내가 16px · 52px · 10px 을 지어냈다.** 반응형 기준 px 을 안 적었을 때
모델 셋이 각각 다른 숫자를 만든 것과 같은 일이다. `gridBaseCss()` 에 넣었다.

**그중 둘은 틀렸다 — 같은 날 완성화면을 재서 되돌렸다.**

| 값 | 내가 지어낸 것 | 지금 값 | 근거 |
|---|---|---|---|
| `--card-pad` | 16px | **24px** | 완성화면 일곱 칸이 전부 24px |
| `--btn-gap` | 10px | **8px** | 완성화면 일곱 칸이 전부 8px |
| `--row-h` | 52px | 52px | 그대로 |

**일곱 번 만들어 정착한 값이 내가 한 번 지어낸 값보다 근거가 세다.**
이게 이 문서 전체에서 가장 자주 되풀이되는 사고다 — 없으면 지어내고, 지어낸 건
그럴듯해서 틀린 줄을 모른다. 색 대비 1.01 도 같은 종류였다(아래 「색 대비」).

### 2. 지표 카드 4개를 놓으려면 뼈대를 깨야 했다 → `.stats` 로 분리
sidebar 뼈대는 `.cards` 를 3칸으로 고정하는데 대시보드 지표는 4개가 기본이다.
인라인으로 덮어써야 했다 — 즉 뼈대 규칙을 어겼다.
지표 줄은 뼈대와 상관없이 도는 물건이라 `.stats` 로 따로 뺐다(4→2→1칸).

### 3. 없는 값을 쓰면 멈추게 했다
`build-layout-samples.mts` 안에 검사를 넣었다.
가이드에 없는 `var(--x)` 를 쓰거나 폴백(`var(--x, 16px)`)을 쓰면 **에러로 멈춘다.**
폴백을 허용하면 값을 지어내고도 조용히 넘어가서, 견본이 가이드를 앞서가 버린다.

---

### 4. 문서와 코드가 서로 다른 숫자를 말했다 → 코드에 맞춤 ⭐
아홉 레이아웃을 전부 대조했더니 **세 곳**이 어긋나 있었다.

| 레이아웃 | 글에 적혀 있던 것 | 코드 | 고친 값 |
|---|---|---|---|
| 사진 중심형 | 매거진형 **2열** | 3/2/1 | **3열** |
| 좌우 분할형 | **2열** 카드 | 3/2/1 | **3열** |
| 여백 중심형 | **2열** | 1/1/1 | **1열**(본문 760px) |

코드를 정답으로 삼았다 — `cardWidth()` 로 실제 픽셀이 계산돼 나오는 쪽이고,
글의 「2열」은 근거가 없었다. 여백 중심형은 뼈대가 본문을 760px 로 가두므로
1열이 맞다(읽히는 화면이라는 설명과도 맞다).

**다시 어긋나지 않게 `check-presets.mts` 를 남겼다.** 팩을 굽기 전에 돌린다.
```
npx tsx check-presets.mts
```

### 4-1. 그 검사기가 더 큰 걸 찾았다 ⭐⭐
카드 폭 숫자 자체가 틀려 있었다. **뼈대 둘이 본문을 더 좁히는데 계산이 그걸 몰랐다.**

| 뼈대 | 왜 좁아지나 | 가이드에 적혀 나가던 값 | 실제 |
|---|---|---|---|
| sidebar | 왼쪽에 260 + 32 를 떼어 준다 | 453px | **356px** |
| single | 본문을 760px 로 가둔다 | 1392px | **760px** |

목록중심형·대시보드형·에디토리얼형 **셋이 전부 453px 로 나가고 있었다.**
그대로 만들면 카드가 100px 씩 넘쳐서 화면이 안 맞는다.

`cardWidth(cols, vw, structure)` 로 뼈대를 받게 고치고, 부르는 곳 여덟 군데를 전부 바꿨다.

---

## multi · single · bento 에서 나온 것 — 다섯 개 다 고쳤다

(2026-08-09 에 제목을 고쳤다. 원래 「아직 안 고친 것」이었는데 아래 다섯이 전부 고쳐진 뒤에도
제목만 그대로였다. **문서가 자기 상태를 틀리게 말하고 있었다** — 이 문서가 내내 잡아 온
「글과 코드가 싸운다」의 가장 민망한 판이다.)

### 5. 카드 「안에」 무엇을 어떻게 넣는지가 없었다 → `html` 칸을 만듦 ⭐
`THUMBS.overlay` 는 「사진 위 겹침」이라 돼 있고 CSS 도 있었다.
CSS 는 `.thumb > img` · `.body` 같은 이름을 **이미 요구하고 있었는데**,
그걸 알려주는 데가 아무 데도 없었다.

그래서 내가 만든 견본은 글자가 **사진 아래**에 붙었다. 겹침이 아니다.
**그런데 아무 경고 없이 그럴듯하게 나왔다** — 이게 제일 위험하다.
숫자가 틀린 건 재보면 나오는데, 이건 틀린 줄을 모른다.

`THUMBS` 여섯 개 전부에 **`html` 칸**을 넣어 요구하는 구조를 값으로 준다.
특히 overlay 는 `.body` 가 **`.thumb` 「안」**에 들어가야 겹쳐진다 —
밖에 두면 위치 기준이 사라져 엉뚱한 데로 날아간다. 주석으로 못 박았다.

```html
<article class="card overlay">
  <div class="thumb">
    <img src="사진.jpg" alt="">
    <div class="body">          <!-- .thumb 「안」이다 -->
      <h3>제목</h3><p class="meta">태그 · 태그</p>
    </div>
  </div>
</article>
```

견본 생성기도 이 구조로 만들도록 고쳤다. 다시 구워 보니 글자가 사진 위에 얹힌다.

### 6. 홈의 모자이크 규칙이 없었다 → `.mosaic` 신설
`LAYOUTS.showcase.hero` 는 「사진 모자이크(큰 1장 + 작은 4장)」이라는데
뼈대에 그런 규칙이 없어서 같은 크기 격자만 나왔다.

`gridBaseCss()` 에 `.mosaic` 을 넣었다. **첫 장이 큰 것이다** — class 로 지정하지 않는다.
그래야 순서가 곧 규칙이 되고, 「어느 걸 크게 할지」를 매번 안 정해도 된다.

**만들다가 하나 더 나왔다.** 1:1 카드로 모자이크를 짜니 큰 칸 사진이 646px 인데
두 줄 높이는 628px 이라 **18px 이 넘쳐서 큰 칸 제목이 화면 밖으로 밀려났다.**
큰 칸에서는 사진이 카드 비율을 버리고 남는 자리를 채우게 했다.

```css
.mosaic > :first-child > .thumb{ aspect-ratio: auto; flex: 1; min-height: 0; }
```

### 7. 상세 화면의 대표 사진 규칙이 없었다 → `.detail-img` 신설
카드 비율(4:3)을 본문 폭 1392px 에 적용하면 사진 세로가 **1044px** 이 되어
노트북 화면을 통째로 덮었다. 가로로 눕히고(16:9) 세로에 상한(420px)을 뒀다.
420px 은 1440 화면에서 사진 아래 내용이 같이 보이는 선이다.

### 8. 벤토 뼈대가 카드 이름과 같은 class 를 쓰고 있었다 ⭐
`STRUCTURES.bento` 는 크게 둘 칸에 **`.card.wide` · `.card.tall`** 을 붙이게 돼 있었다.
그런데 **wide 와 tall 은 카드 「종류」 이름이기도 하다**(16:9 와이드 카드, 3:4 세로 카드).

벤토에 와이드 카드를 쓰는 조합이 생기면 두 규칙이 같은 선택자에 걸려 터진다.
지금은 벤토가 정사각 카드를 써서 **사고가 안 났을 뿐**이다.
칸을 몇 개 먹느냐는 카드 모양과 다른 축이므로 **`.span-x` · `.span-y`** 로 이름을 갈랐다.

### 9. 주석 안의 백틱이 파일을 깨뜨렸다
`gridBaseCss()` 는 통째로 템플릿 문자열이라 **주석 안에 백틱을 써도 문자열이 거기서 끊긴다.**
`showcase.hero` 를 백틱으로 감쌌다가 빌드가 죽었다.
styled-jsx 주석에서 같은 사고를 한 번 겪었는데 또 했다. 파일에 주석으로 못 박아 뒀다.

---

## 5장이 맞는 수인가 — 맞았다

`--row-h` 와 `--btn-gap` 은 **4번(목록 비어있음)과 5번(입력 폼)에서만** 나왔다.
홈·목록·상세 셋만 만들었으면 못 찾았다. **3장으로는 부족하다는 게 증명됐다.**

## 자동 넘김 페이지

각 폴더의 `00_자동넘김.html` — 4초마다 다음 화면으로 넘어간다.
열어놓고 화면 녹화만 누르면 SNS 영상 재료가 된다.
캡처는 `_마케팅/릴스영상/_화면캡처/` 에도 같이 남긴다.

---

# 색 대비 — 2026-08-08 잰 값

바탕 `CONTENT_BG #F0EFEB` 기준. WCAG 상대휘도로 계산.
`swatches` 는 `[주색, 강조, 진한글자, 옛배경]` 순. ← **이 배열은 아래 ①②에서 없앴다.**

| 테마 | 본문 글자 | 주색 글자 | 주색 위 흰글자 | 강조 글자 | 강조 위 흰글자 |
|---|---|---|---|---|---|
| 모던 네이비 | 13.56 ✅ | 7.43 ✅ | 8.55 ✅ | 2.26 ✗ | 2.60 ✗ |
| 미니멀 모노 | **3.95 △** | 16.41 ✅ | 18.88 ✅ | 2.71 ✗ | 3.12 △ |
| **일렉트릭 바이올렛** | **1.01 ✗✗** | 4.94 ✅ | 5.68 ✅ | 1.23 ✗ | 1.41 ✗ |
| 레트로 페이퍼 | 13.42 ✅ | 2.85 ✗ | 3.28 △ | **5.27 ✅** | **6.07 ✅** |
| 내추럴 그린 | 12.87 ✅ | 4.36 △ | 5.02 ✅ | 2.68 ✗ | 3.09 △ |
| 코럴 선셋 | 13.15 ✅ | 2.73 ✗ | 3.14 △ | 1.87 ✗ | 2.15 ✗ |

✅ 4.5 이상(본문 가능) · △ 3.0 이상(큰 글자만) · ✗ 3.0 미만(글자에 못 씀)

## 고친 것 ①  「진한 글자색」 자리에 연한 색이 들어 있었다 — 2026-08-08 ✅

`swatches[2]` 는 「진한 글자색」 자리인데 바이올렛만 연한 민트 `#DFF5EC` 가 들어 있었다.
**대비 1.01 — 그 팩으로 만들면 본문 글자가 안 보인다.**
모노도 같은 자리에 회색 `#767676`(3.95) 이 들어 있어 본문에 모자랐다.

**값을 새로 만들지 않았다.** 여섯 중 넷은 이미 `swatches[2]` 와 `PRESETS.colors` 의
`text(본문)` 이 같은 값이었다 — 규칙은 이미 있었고 둘만 어긋나 있던 것이다. 그 규칙에 맞췄다.

| 테마 | 있던 값 | 바꾼 값 | 어디서 가져왔나 | 대비 |
|---|---|---|---|---|
| 일렉트릭 바이올렛 | `#DFF5EC` (1.01) | **`#1F2024`** | 이 테마의 `text(본문)` · `globals.css --foreground` | **14.14** |
| 미니멀 모노 | `#767676` (3.95) | **`#111111`** | 이 테마의 `text(본문)` | **16.41** |

## 고친 것 ②  「글자용 강조색」을 여섯 테마에 다 넣었다 ✅

강조색은 여섯 중 **레트로만** 글자로 쓸 수 있었다(5.27). 나머지 다섯은 배경·테두리 전용인데
주는 값이 하나뿐이라 **받는 쪽이 그걸 글자에도 썼다.** 짐작이 아니라 실제로 세 곳이 그러고 있었다 —
견본 생성기의 배지, 프리셋 미리보기의 배지, 판매팩 비교 HTML 의 배지.

카드뉴스2.css 가 이미 `--orange`(배경) / `--orange-text`(글자) 로 갈라 둔 그 방식이다.
글로 「강조를 글자에 쓰지 마세요」라고 부탁하는 대신 **쓸 수 있는 값을 따로 준다.**

**여기서도 값을 지어내지 않았다.** 네 값 전부 이미 저장소 안에 있던 색이다.

| 테마 | accent (배경 전용) | accentText (글자용) | 어디서 가져왔나 | 대비 |
|---|---|---|---|---|
| 모던 네이비 | `#FF7A30` (2.26) | **`#A84B28`** | 모노가 이미 들고 있던 `accent-text` | **4.93** |
| 미니멀 모노 | `#D97757` (2.71) | **`#A84B28`** | 원래 있던 값 — 배열에만 안 실려 있었다 | **4.93** |
| 일렉트릭 바이올렛 | `#FFD54A` (1.23) | **`#8A5A00`** | `app/globals.css --warning` | **5.15** |
| 레트로 페이퍼 | `#0E6F60` (5.27) | **`#0E6F60`** | 그대로 — 여섯 중 유일하게 글자로 쓸 수 있다 | **5.27** |
| 내추럴 그린 | `#65A30D` (2.68) | **`#166534`** | `PRIMARY_SWATCHES_BY_STYLE.forest[1]` | **6.20** |
| 코럴 선셋 | `#F59E0B` (1.87) | **`#8A5A00`** | `app/globals.css --warning` | **5.15** |

**골라 놓고 못 쓴 값들** — 후보를 다 재 보니 그럴듯한데 모자란 게 많았다.
`--primary-on-soft #B4551E`(4.29)·`--pastel-yellow-foreground #8A6D00`(4.28) 은
홈페이지의 **연한 틴트 배경**(`#FBE7D3` 등) 위에서 고른 값이라 종이색 위에서는 4.5 를 못 넘는다.
카드뉴스의 `--orange-text #C25D17` 도 3.75 다. **배경이 달라지면 다시 재야 한다.**
내추럴 그린은 올리브를 글자까지 낮춘 값이 저장소에 없었다(가장 가까운 `#4D7C0F` 가 4.34).
그래서 그 테마가 이미 들고 있는 진한 초록을 썼다 — 색 성격은 조금 잃지만 지어내지는 않았다.

### `swatches` 배열을 없앴다 — 이게 진짜 원인이다

`[주색, 강조, 진한글자, 옛배경]` 네 칸짜리 배열이라 **자리의 뜻이 이름에 없었다.**
그래서 세 번째에 민트가 들어가 있어도 읽는 쪽에서 알 방법이 없었다. 이름으로 바꿨다.

```ts
primary / accent / accentText / ink
```

네 번째 「옛배경」은 2026-08-05 에 이미 버린 값인데 배열에만 남아 있었다. 지웠다.

**읽는 쪽 다섯 군데를 같이 고쳤다.**

| 어디 | 무엇이 잘못돼 있었나 |
|---|---|
| `build-layout-samples.mts` | `const [primary, accent, ink] = P.swatches` — 순서에 기대고 있었다. 배지 글자에 강조 원색을 씀 |
| `build-design-presets.mts` | 판매팩 비교 HTML 의 배지 글자에 강조 원색을 씀. 색 표에 `accent-text` 를 넣음 |
| `packages/[packageId]/page.tsx` | 밝기를 눈대중으로 재서(`0.299R+0.587G+0.114B > 180`) 밝으면 본문색으로 떨어뜨리고 있었다. **파스텔은 배지 색이 통째로 사라졌고, 코럴은 문턱을 못 넘어 1.87 짜리가 그대로 글자로 나갔다.** 함수를 지웠다 |
| `preset/preset-form.tsx` | `th[1]`·`th[2]` 로 자리를 집고, 배지 글자에 강조 원색을 씀 |
| `brief/brief-form.tsx` | 색 동그라미를 `key={색}` 으로 그리고 있었다 — 모노는 주색과 본문색이 둘 다 `#111111` 이라 같은 key 가 두 번 나온다. 자리로 셈 |

「강조는 배경에만」이라는 문장은 `ACCENT_RULE` 한 벌로 두고 **파는 팩과 유저 팩이 같이 읽는다.**
두 벌로 적으면 반드시 갈라진다는 걸 2026-08-06 에 이미 겪었다.

### 어두운 배경은 반대다 — 재서 고르게 했다

`accentText` 는 밝은 종이색 위에서 읽히라고 진하게 잡은 값이라 어두운 바탕에 그대로 얹으면
거꾸로 안 보인다(파스텔의 `#8A5A00` 은 어두운 배경 위 3.19). 거기서는 강조 원색이 오히려 잘 읽힌다
(`#FFD54A` 는 13.39). `accentTextOn(테마, 배경)` 이 **둘 중 나은 쪽을 재서 고른다.**
다크모드 프리셋 문서는 이제 강조 원색을 글자용으로 적어 내보낸다.

### 검사기가 색도 재게 했다

`check-presets.mts` 는 레이아웃 칸 수만 보고 있었다. **색을 안 보니 이번 사고를 못 잡았다.**
이제 여섯 테마의 `ink`·`accentText` 를 종이색 위에서 재고, 4.5 아래면 멈춘다.
`DESIGN_OPTIONS` 와 `PRESETS.colors` 가 같은 값을 두 벌 들고 있으므로 **둘이 같은지도 본다.**
`accent` 는 재지 않는다 — 다섯이 전부 걸리는데 그건 틀린 게 아니라 배경용이라서다.

```
색 대비 — 바탕 #F0EFEB 위 (4.5 이상이어야 본문)
· navy    모던 네이비      본문 13.56 · 글자용 강조  4.93 · (강조  2.26 배경 전용)
· mono    미니멀 모노      본문 16.41 · 글자용 강조  4.93 · (강조  2.71 배경 전용)
· pastel  일렉트릭 바이올렛   본문 14.14 · 글자용 강조  5.15 · (강조  1.23 배경 전용)
· retro   레트로 페이퍼     본문 13.42 · 글자용 강조  5.27 · (강조  5.27 배경 전용)
· forest  내추럴 그린      본문 12.87 · 글자용 강조  6.20 · (강조  2.68 배경 전용)
· coral   코럴 선셋       본문 13.15 · 글자용 강조  5.15 · (강조  1.87 배경 전용)
```

`npm run pack` 으로 다시 구웠다. **완성화면 928개 지문 `e9c6623a5e89c11d` 전후 같다.**
뷰티 판매 페이지에서 실제로 그려진 값도 확인했다 — 배지 바탕은 강조 15% 농도,
글자는 `#8A5A00` · `#A84B28` · `#8A5A00`.

---

# 읽기 폭 — 2026-08-09

## 글에만 있던 숫자가 셋이었다 ✅

「색까지 끝났으니 가이드 완료인가」를 확인하려고 **레이아웃 설명글의 px 숫자를 코드 계산과
전부 대조했다.** 칸 수·카드 폭은 다 맞았는데, 대조하다가 **아예 코드에 근거가 없는 값**이 나왔다.

| 레이아웃 | 글에 적혀 있던 읽기 폭 | 코드 근거 |
|---|---|---|
| 좌우 분할형 상세 | 760px | ✅ `본문폭()` · single 뼈대 CSS |
| 에디토리얼형 상세 | **720px** | ❌ 없음 |
| 여백 중심형 상세 | **680px** | ❌ 없음 |

카드 폭 453px 사고와 같은 종류다. 다른 점이 하나 있는데 — **453 은 한때 맞는 값이었다가
뼈대가 바뀌며 어긋난 것**이고, 720·680 은 **처음부터 아무 근거가 없었다.** 검사기가
「N열」만 보고 있어서 못 잡았다.

**게다가 720 은 이미 `BREAKPOINTS.narrow` 의 값이다.** 같은 숫자가 「휴대폰 기준선」과
「읽기 폭」 두 뜻으로 쓰이고 있었다 — 벤토의 `.card.wide` 가 카드 종류 이름과 겹쳐
터질 뻔했던 8번과 같은 모양이다. 이름을 붙여 갈랐다.

## 완성화면을 재 봤는데 — 거기엔 답이 없었다

늘 하던 대로 완성화면에서 재서 정하려고 했다. `.wrap-narrow`(한 단으로 좁히는 컨테이너)를
여덟 팩에서 재니 **일곱이 880px** 이었다(하나는 920). `--card-pad` 24px 때와 똑같은 모양이라
그대로 쓸 뻔했다.

**그런데 그 880px 은 글을 담는 자리가 아니었다.** 열어 보니 예약 완료 화면·폼처럼
**상자**를 담는 자리였다. 긴 글을 읽는 화면은 완성화면에 거의 없다 —
긴 문단이 다섯 개 넘는 화면이 928개 중 **네 개**뿐이다. 우리가 만든 건 거래하는 화면이지
읽는 화면이 아니다.

**그래서 재서 정할 수 있는 값이 아니었다.** 새 숫자를 만들지 않고,
**이미 코드가 쓰고 있던 760** 을 남겼다. `READING_WIDTH` 하나로 묶고 네 자리가 그걸 참조한다.

- 880 은 「좁은 페이지 폭」이라는 **다른 자리**다. 읽기 폭과 섞지 않는다.
- 여백 중심형은 페이지 자체가 760 으로 갇히므로(single 뼈대),
  **그 상세가 880 이 되면 자기 페이지를 넘는 모순**이 된다. 한 값이어야 하는 이유다.

「재서 정한다」가 늘 답을 주지는 않는다. **없는 걸 억지로 재면 엉뚱한 자리의 값을 가져온다.**
이번엔 재 봤더니 없다는 것이 답이었다.

## 검사기가 읽기 폭도 본다

`check-presets.mts` 가 글의 「최대 폭 NNNpx」·「본문 폭 NNNpx」를 `READING_WIDTH` 와 대조한다.
지금은 글이 상수를 끼워 넣으므로 어긋날 수가 없지만, **나중에 사람이 숫자를 손으로 다시 적을 수 있다.**
720 을 적으면 휴대폰 기준선과 겹친다는 것까지 말해 준다.

일부러 옛 값(720·680)으로 되돌려 검사기가 정말 잡는지 확인했다 — 두 곳 다 잡고 1 로 멈춘다.

```
✗ magazine  에디토리얼형 …
    detail에 「최대 폭 720px」 — 읽기 폭은 760px 하나입니다(READING_WIDTH).
    720 은 휴대폰 기준선 값이라 읽기 폭으로 쓰면 두 뜻이 겹칩니다
✗ calm      여백 중심형 …
    detail에 「최대 폭 680px」 — 읽기 폭은 760px 하나입니다(READING_WIDTH)
```

`npm run pack` 으로 다시 구웠다. **완성화면 928개 지문 `e9c6623a5e89c11d` 그대로.**
생성된 20칸에 680·720 은 하나도 남아 있지 않다.

---

### 남은 것 하나 → 2026-08-09 에 닫았다 ✅

**레트로를 다크모드로 쓰면 강조 틸이 어두운 배경 위 3.12** 였다.
어두운 바탕용 강조를 `accentTextDark` 로 열었다. 여기서도 값을 지어내지 않았다 —
카드뉴스2.css 의 `--teal #1AA48F` 가 바로 그 틸의 「배경용」 짝이고, 어두운 바탕 위 **6.08** 이다.

| 테마 | 어두운 바탕 글자용 | 대비 | 어디서 |
|---|---|---|---|
| 모던 네이비 | `#FF7A30` | 7.28 | 강조 원색 그대로 |
| 미니멀 모노 | `#D97757` | 6.06 | 강조 원색 그대로 |
| 일렉트릭 바이올렛 | `#FFD54A` | 13.39 | 강조 원색 그대로 |
| **레트로 페이퍼** | **`#1AA48F`** | **6.08** | **카드뉴스2.css `--teal`** |
| 내추럴 그린 | `#65A30D` | 6.12 | 강조 원색 그대로 |
| 코럴 선셋 | `#F59E0B` | 8.81 | 강조 원색 그대로 |

**밝은 바탕과 어두운 바탕이 서로 반대다.** 레트로 틸이 그 증거다 —
`#0E6F60` 은 밝은 데서 5.27 인데 어두운 데서 3.12, `#1AA48F` 는 밝은 데서 2.70 인데 어두운 데서 6.08.
**한 색으로는 둘 다 못 한다.** 그래서 강조가 세 자리가 됐다.

| 자리 | 언제 |
|---|---|
| `accent` | 배경·배지 바탕·띠·형광펜 — 글자 아님 |
| `accent-text` | 밝은 바탕 위 글자 |
| `accent-text-dark` | 어두운 바탕 위 글자 |

**「다크모드면 이것」식으로 분기하지 않는다.** 배경은 다크 스위치 말고도 달라진다 —
짙은 히어로, 하단 고정 바, 사진 위 어둠막. 자리마다 사람이 고르게 두면 또 틀린다.
`accentTextOn(테마, 배경)` 에 **배경을 주면 재서 골라 준다.**

검사기는 두 바탕에서 각각 재고, **고르는 함수가 정말 읽히는 값을 내는지도 확인한다** —
값이 맞아도 고르는 쪽이 틀리면 소용이 없어서다.
색 표가 세 벌(DESIGN_OPTIONS · PRESETS.colors · 판매팩 생성기)이라 **셋이 같은지도 본다.**

---

# 자동화 전 점검 — 2026-08-09

20칸을 자동으로 늘리기 전에, **지금 있는 것이 정말 맞는지** 전수로 쟀다.
자동화는 맞는 것도 복사하지만 틀린 것도 똑같이 복사한다.

## 튼튼했던 것

**설계 문서 다섯 종이 같은 화면 수를 말한다 — 20/20 팩.**
02 IA · 03 기능정의서 · 07 스펙팩(md) · 07 스펙팩(json) · 04 WBS 를 전부 대조했다.

**완성화면이 설계와 화면ID 단위로 맞는다 — 8/8 팩.**
여행_프리미엄 144개 설계 ↔ 144개 HTML, 남거나 모자란 화면 0개.
자동으로 화면을 만들려면 이게 먼저 서 있어야 하는데, 이미 서 있다.

**레이아웃 설명글의 px 숫자도 코드 계산과 다 맞는다.** 453·356 사고의 재발 없음.

## 고친 것 ①  검사기가 헛경보를 내고 있었다 ⭐ 세 번째 이름 겹침

`check-design.mts` 를 완성화면 네 팩에 돌리니 팩마다 **간격 21~32군데 + 카드 37~124장**이
걸렸다. 처음엔 「완성화면이 가이드를 안 지킨다」고 읽었는데, **열어 보니 아니었다.**

**간격** — 걸린 자리가 하나도 격자가 아니었다.

```css
.btn{ display:inline-flex; gap:6px }   /* 버튼 안 아이콘과 글자 사이 */
.delta{ gap:3px }                      /* 숫자 옆 화살표 */
```

가이드가 말하는 건 「카드를 늘어놓는 격자」다. 버튼 안 6px 은 격자가 아니다.
규칙 덩어리를 통째로 보고 **`display:grid` 인 것만** 재게 고쳤다. → 팩당 2~4군데로 줄었고,
남은 건 `.cal-grid`(달력)·`.idx-list` 처럼 **진짜 격자**다.

**카드** — 같은 낱말이 두 뜻이었다.

| | 우리 가이드 | 완성화면 |
|---|---|---|
| `.card` | **사진+글자 카드** (안에 `.thumb`+`.body`) | **상자(패널)** (안에 `.card-hd`/`.card-bd`/`.card-ft`) |
| 사진 자리 | `.thumb` | `.ph-thumb` · `.ph-sq` · `.ph-circle` |
| 격자 | `.cards` | `.g5` · `.rail` |

가이드는 이미 **「사진 없는 상자는 이 규칙 밖」**이라고 적어 두었다.
즉 완성화면은 규칙을 **어긴 게 아니라 애초에 이 규칙의 대상이 아니었다.**
검사기가 우리 어휘로 남의 어휘를 읽어서 난 헛경보다.

**이 사고는 이번이 세 번째다.**

| 겹친 이름 | 두 뜻 | 언제 |
|---|---|---|
| `.card.wide` | 카드 종류(16:9) ↔ 칸 수(2칸) | 08-08 |
| `720px` | 휴대폰 기준선 ↔ 읽기 폭 | 08-09 |
| `.card` | 사진+글자 카드 ↔ 상자(패널) | 08-09 |

**이름이 겹치면 반드시 어디선가 터진다.** 앞의 둘은 이름을 갈라 고쳤고,
이번 것은 두 어휘가 각자 일관되므로 **검사기가 어느 어휘인지 먼저 가리게** 했다.

> **헛경보를 내는 검사기는 없는 것만 못하다.** 몇 번 겪으면 사람이 전부 무시하게 되고,
> 그때부터는 진짜 경보도 안 보인다. 그래서 이걸 제일 먼저 고쳤다.

## 고친 것 ②  가이드가 표 한 줄 높이를 두 값으로 말했다

격자 CSS 는 `--row-h: 52px`, 밀도 눈금은 48px(넉넉)/40px(컴팩트).
**같은 「표 한 줄 높이」를 가이드가 두 값으로 말하고 있었다.**

완성화면을 재니 `--row-h` 를 정의한 곳이 **48px 과 40px** — 정확히 밀도 눈금의 두 값이다.
**52 는 아무도 안 쓴다.**

왜 이것만 남았는지도 분명하다. `--card-pad`(24)·`--btn-gap`(8)은 이미 「넉넉하게」와
같은 값인데, **그 둘은 완성화면을 재서 고쳤고 `--row-h` 만 안 고쳤기** 때문이다
(어제 21번 줄 표에 「52px 그대로」라고 적은 그 칸이다 — 그때 대조를 안 했다).

격자 CSS 가 밀도 눈금에서 값을 가져오게 바꿨다. **이제 셋 다 한 곳에서 나온다.**
검사기가 들고 있던 기준값(24/52/8)도 손으로 적힌 것이라, 가이드가 실제로 내보내는
CSS 에서 뽑아 쓰게 고쳤다 — **검사기가 틀린 기준으로 재고 있었다.**

## 고친 것 ③  WBS 가 전 화면 일괄 2일이었다

132화면짜리 표의 「기간(일)」 칸이 **전부 `2`** 였다. `sequentialWorkdaySlots(…, 2)` 로
일괄 배분한 값이다. 기획을 아는 구매자는 그 표를 보는 순간 자동으로 채운 값인 걸 안다 —
**나머지가 아무리 좋아도 문서 전체의 신뢰가 거기서 깎인다.**

여기서도 값을 지어내지 않았다. **이미 그 화면이 들고 있는 것**을 셌다.

```
무게 = 요건 수(기능정의를 쪼갠 개수) + 버튼(다른 화면으로) + 동작(화면 안에서 끝나는)
일수 = 올림(무게 / 5), 1~4일로 막음
```

기능정의서·스펙팩이 이미 쓰고 있는 그 숫자다. 실측하니 화면당 무게가 **5~21** 로 갈린다.

| 팩 | 전 | 후 |
|---|---|---|
| LMS_스탠다드 | 2일 × 44 | 2일 27 · 3일 15 · 4일 2 |
| LMS_프리미엄 | 2일 × 132 | 1일 61 · 2일 64 · 3일 7 |
| 여행_디럭스 | 2일 × 43 | 1일 2 · 2일 23 · 3일 12 · 4일 6 |

5로 나누는 것은 「하루에 요건 다섯 남짓」이라는 뜻이다. **우리가 모르는 값을 아는 척하지
않는다** — 규칙을 문서에 적어 사는 사람이 자기 팀 속도로 바꿔 쓰게 한다.

## 고친 것 ④  스펙팩 1장의 색이 검사기 사정권 밖이었다

테마 색은 다 재고 있었는데 **업종마다 손으로 적은 색**은 아무도 안 재고 있었다.
전수로 재니 셋이 「포인트」라면서 글자로 못 쓰는 값이었다.

| 업종 | 적혀 있던 것 | 대비 | 갈라 준 글자용 | 어디서 |
|---|---|---|---|---|
| LMS | 포인트 오렌지 `#FF7A30` | **2.26** | `#A84B28` (4.93) | 이 색은 **모던 네이비의 강조색 그 자체**다. 그 테마의 `accentText` 를 그대로 |
| 관리자 | 포인트 블루 `#2D6CDF` | **4.22** | `#3B5BA5` (5.66) | 모던 네이비 주색 후보 — 색조가 같다 |
| 공동구매 | 포인트 코랄레드 `#FF5A5F` | **2.65** | `#B42318` (5.71) | 홈페이지 `--danger` |

관리자 4.22 는 이번 전수 검사에서 처음 나왔다 — 아슬하게 모자라는 값이라 표본만 봐서는 안 걸린다.

**왜 위험한 자리였나.** 스펙팩은 「프리셋이 없다면 1장의 디자인 컨셉을 토큰화하라」고 말한다.
즉 프리셋 없이 쓰는 손님에게 **이 한 줄이 유일한 색 지시**다.
공동구매는 **할인율과 남은 시간이 화면의 주인공**인데, 그걸 2.65 짜리 색으로 쓰라고 한 셈이었다.

색을 바꾸지 않고 **배경용/글자용으로 갈라 적었다** — 업종의 인상은 그 색이 만드는 것이라
바꾸면 안 된다. 강조색을 두 자리로 가른 것과 같은 처방이다.

```
포인트 코랄레드(#FF5A5F — 배경·배지 전용, 글자에는 #B42318)
```

`check-presets.mts` 가 여섯 업종의 컨셉 문장에서 색을 뽑아 잰다.
「배경」이라 적힌 색은 재지 않고(배경은 연한 게 맞다), 「글자에는 #…」는 반드시 잰다.

창을 좁게 잡은 이유가 있다. 넓게 잡았더니 「베이지(`#F5EFE9`) **배경**에 딥 플럼(`#7A3B5D`) 포인트」에서
**앞 색의 「배경」이 뒤 색까지 덮어 플럼을 안 재고 넘어갔다.** 지금은 6.99 라 안 걸리지만,
다음에 연한 포인트 색이 오면 조용히 통과한다 — **헛경보보다 나쁜 쪽**이라 좁혔다.

일부러 옛 문구로 되돌려 확인했다 — 잡고 1 로 멈춘다.

```
✗ 공동구매  #FF5A5F 2.65
    #FF5A5F 이 2.65 입니다 — 「포인트」로 적으면 글자에 쓰입니다.
    배경 전용이면 「배경·배지 전용, 글자에는 #…」처럼 갈라 적으세요
```

## 고친 것 ⑤  읽기 폭이 스펙팩에 없었다

읽기 폭을 상수로 묶었는데 **그게 닿는 곳이 디자인프리셋뿐**이었다.
스펙팩(07)에는 「콘텐츠 영역 최대 1440px」만 있고 **글을 좁히라는 말이 없었다.**
프리셋 없이 스펙팩만 쓰면 상세 본문이 1440px 을 꽉 채운다 — 한 줄 90자가 넘으면
다음 줄 첫 글자를 눈이 못 찾는다.

**`COMMON_RULES` 에 넣었다.** 스펙팩과 판매팩 가이드가 **이미 같이 읽는 자리**라
한 벌만 적으면 양쪽에 다 간다. 새 절을 따로 쓰지 않은 이유가 이것이다 —
따로 쓰면 갈라진다는 걸 2026-08-06 에 겪었다.

| 어디 | 무엇 |
|---|---|
| 스펙팩 「공통 요소」 | 한 줄 요약 + 아래 절 가리키기 |
| 스펙팩 「읽기 폭」 절 | `COMMON_RULES` 본문 (왜 좁히는지까지) |
| 스펙팩 JSON | `common.readingWidth` — 기계가 읽는 쪽에도 값으로 |
| 판매팩 가이드 | 「콘텐츠 영역」에서 가리키고, 본문은 같은 `COMMON_RULES` |
| 무료 샘플 | 같이 따라 들어갔다 |

**20칸 전부 + 무료 샘플**에 들어간 것을 확인했다. 가이드에서 두 번 말하지 않는지도 봤다 —
가리키는 줄 하나, 본문 하나다.

만들다가 하나 고쳤다. 공통 요소 줄을 JSON 의 설명 문장에서 잘라 쓰려고 정규식을 썼더니
**「가운데 정렬」이 한 줄에 두 번** 들어갔다. 문장을 잘라 붙이지 말고 그냥 새로 쓰는 게 맞다.

## 아직 안 고친 것

| 무엇 | 왜 남겼나 |
|---|---|
| 프리미엄 완성화면 2칸 공백 | LMS·매칭. 디럭스는 5/5인데 상위 등급이 빈다 |
| 공동구매·매칭 여덟 칸 미등록 | `lib/packages.ts` 에 beauty·lms·travel 셋뿐 |

---

# 값을 다시 쟀다 — 2026-08-09

## 코드의 가격 원칙은 옳다

`lib/packages.ts` 에 적혀 있다 — **「직접 만들면 드는 크레딧과 같게 맞췄다.
재고는 남의 컨셉이라 직접 만들기보다 비싸면 살 이유가 없다.」**

검산하니 정확히 맞는다.

```
스탠다드 = AI팩(12+390) + 디자인프리셋(12+99) = 513크레딧 = 51,300원 ✓
플러스   = AI팩(28+690) + 디자인프리셋(12+99) = 829크레딧 = 82,900원 ✓
```

## 그런데 가격이 두 벌이다

| | 스탠다드 | 플러스 | 디럭스 | 프리미엄 |
|---|---|---|---|---|
| **코드** (`packages.ts`) | 51,300 | 82,900 | 108,000 | 163,800 |
| **가격표 v4** | 41,300 | 62,900 | 88,000 | 122,800 |

v4 는 다운로드를 290/490 크레딧으로 잡았는데 코드는 **390/690** 이다.
**색 표가 세 벌이라 갈렸던 것과 같은 모양이다.** 어느 쪽이 맞는지 정해야 한다.

## 원칙이 두 등급에서 거꾸로 걸려 있다

무료 35크레딧을 빼면 **직접 만들 때 실제로 내는 돈은 47,800원**이다.
그런데 스탠다드는 51,300원 —

> **남의 업종 설계도를, 내 컨셉으로 직접 만드는 것보다 3,500원 더 주고 사는 셈이다.**

원칙이 스스로 어겨졌다. 반대로 **완성화면은 플랫폼에서 아예 못 만든다.**
대체 불가라 값을 받을 자리인데, 디럭스는 44장을 56,700원(장당 1,289원)에 얹고 있다.

## 제안 — 정가는 그대로, 「재고 할인」을 상시로

「값은 깎지 말고 할인으로」를 지키는 방식이다. 정가는 크레딧 등가로 두어 플랫폼과
어긋나지 않게 하고, 판매가만 내린다.

| 등급 | 정가 | 판매가 | 왜 |
|---|---|---|---|
| 스탠다드 | 51,300 | **35,000** | 직접 만들기(47,800)보다 확실히 싸야 산다 |
| 플러스 | 82,900 | **59,000** | 같은 논리 |
| 디럭스 | 108,000 | **그대로** | 완성화면은 직접 못 만든다 — 깎을 이유가 없다 |
| 프리미엄 | 163,800 | **그대로** | 같음 |

할인 문구는 정직하게 — **「남의 업종 컨셉이라 그만큼 뺍니다.」**
변명이 아니라 실제 이유고, 그래서 지켜진다.

## 냉정하게 매긴 등급

| 층 | 평가 | 근거 |
|---|---|---|
| 설계 문서 정합성 | **상** | 20/20 팩 다섯 문서 일치 |
| 완성화면 ↔ 설계 | **상** | 8/8 팩 화면ID 정확히 일치 |
| 화면 스펙 구체성 | **상** | 뒤로가기·버튼 이동·화면 안 동작·프롬프트까지 |
| 디자인 가이드 | **상** | 네 축이 코드 한 곳 + 자동 검사 |
| 완성화면 품질 | **중** | 실물은 좋으나 가이드와 다른 어휘 |
| WBS | **하 → 중** | 일괄 2일이었다. 이번에 고침 |

설계 문서 층은 지금 값을 받아도 되는 수준이다. **20팩 전수 대조에서 어긋난 게 하나도
없는 것**은 손으로 만드는 기획자가 내기 어려운 정합성이다.

깎이던 건 WBS 한 장 때문이었다 — 132화면이 전부 「2일」인 표는 나머지가 아무리 좋아도
「자동생성이네」라는 인상을 한 번에 만든다. **가격을 올리는 것보다 이게 먼저였다.**

## 이 가이드가 끝나면 — 판매팩 자동 생성으로 간다

색까지 정리되면 **새 업종 팩을 매주 하나씩 자동으로 늘리는 것**이 다음 일이다.
가이드가 흔들리는 상태로 자동화하면 틀린 값이 팩마다 복사된다.
그래서 가이드를 먼저 세우고 있는 것이다 — 순서가 이유가 있다.

정해진 것
- 돌리는 때: **수요일 오전 11시** (월요일 SNS 와 이틀 띄운다)
- 새 업종 하나 = 4등급 문서 생성에 **2~3시간**
- 순서(안): 관리자툴·내부 업무 → 구독·멤버십 → 렌탈·대여 → 견적·상담 접수
  기준은 「카페24로 안 되는 맞춤형만」. 일반 쇼핑몰은 넣지 않는다.
- **완성화면도 자동으로 만든다.** 내가 처음엔 「그건 사람 몫」이라고 적었는데
  사용자가 바로잡았다 — **가이드가 세워지면 만들 수 있다**는 것이 자동화의 전제다.
  가이드를 먼저 세우는 진짜 이유가 이것이다.

## 화면을 만들면서 캡처와 영상 자료를 같이 남긴다

팩을 만드는 김에 **SNS 재료를 함께 뽑는다.** 따로 시간을 내면 안 하게 된다.

| 무엇 | 어디에 | 어디 쓰나 |
|---|---|---|
| **화면 캡처(PNG)** | `_마케팅/릴스영상/_화면캡처/{업종}_{색}/` | 카드뉴스에 넣을 그림 |
| **넘어가는 영상(mp4)** | `_마케팅/릴스영상/{n}. {업종}/` | 릴스·쇼츠·유튜브 |

- **GIF 로 내지 않는다. 처음부터 mp4 로 낸다.** 인스타는 GIF 를 받으면 어차피 mp4 로
  바꾼다(직접 확인함). `_작업/카드영상.mjs` 가 쓰는 방식 그대로면 된다.
- 화면이 저절로 넘어가는 페이지(`00_자동넘김.html`)를 팩마다 같이 만든다.
  4초마다 다음 화면으로 넘어가므로 **화면 녹화만 누르면 영상 재료가 된다.**
  `build-layout-samples.mts` 가 이미 만들고 있으니 팩 생성기에도 같은 것을 붙인다.
- 캡처는 업종·등급·색이 폴더 이름에 드러나게 둔다. 나중에 카드뉴스에서 찾아 쓸 때
  「어느 팩의 어느 화면인가」를 알아야 한다.
- **한글 폴더에서 헤드리스 크롬은 조용히 아무것도 안 만든다.** `%TEMP%` 아래
  한글 없는 자리에서 찍고 옮긴다. 창 높이는 목표보다 200px 크게 잡고 잘라낸다.

사람이 클로드 코드에 파일을 넣고 만들어지는 과정 영상은 **사용자가 직접 찍어**
`릴스영상` 폴더에 올린다. 그건 자동화 대상이 아니다.

먼저 할 것 — 공동구매·매칭 두 업종은 폴더에 4등급이 다 있는데
`lib/packages.ts` 에 등록이 안 돼 홈페이지에서 못 판다. **여덟 칸이 놀고 있다.**
새 업종을 늘리기 전에 이것부터 거는 게 맞다(한 시간이면 된다).

---

# 결제·가격 — 2026-08-09

카드사 심사(충전업종) 준비를 하다가 값과 결제 구조를 같이 손봤다.
근거는 토스가 준 두 문서다 — **「홈페이지 결제경로 제작 가이드(충전업종용)」** 와 **계약과정 FAQ**.

## 우리가 몰랐던 규칙 셋

| 규칙 | 어디에 | 우리 상태였던 것 |
|---|---|---|
| **임의 금액입력 충전 불가** | 가이드 유의사항 3 | 직접 입력 충전이 있었다 |
| **1회 10만원 이하 · 옵션 선택형** | 가이드 유의사항 3 | `CUSTOM_MAX_WON` 이 **100만원**(열 배) |
| **가격 미표시는 입점 불가** | FAQ 2 | `PACKAGE_PRICES_PUBLIC = false` |

「10만원」은 **총액이 아니라 한 번에 고르는 금액의 상한**이고,
토스 «한도»가 아니라 **사이트를 그렇게 만들어야 심사를 통과한다**는 구현 요건이다.
공개 정책의 토스페이 한도(1회 200만원)와는 다른 이야기다.

## 테스트 키로 심사받는 동안이 위험하다

심사는 테스트 결제창으로 받아도 된다(유의사항 6). 그런데 **테스트 키는 돈을 안 받고도
「결제 성공」이라고 답한다.** 크레딧은 진짜로 쌓이고, 그걸로 생성·다운로드를 하면
**우리 돈이 나간다.**

| 유저가 하는 것 | 우리가 내는 돈 |
|---|---|
| 충전(테스트) | 0원 — DB 숫자일 뿐 |
| 생성 | 400~900원 |
| 다운로드 | 2,645~9,716원 |

**한 번 10만원 충전(1,300크레딧)으로 기본 생성을 108번 하면 43,200원**이다. 횟수 제한도 없었다.
처음엔 「다운로드가 진짜 위험」이라고 봤는데 **틀렸다** — 적게 받는 쪽(생성 12크레딧)이
많이 돌아가서 총액이 4~5배 크다. 단가만 보고 판단하면 이렇게 뒤집힌다.

**금액을 낮추는 걸로는 못 막는다.** 얼마짜리든 손님 눈에 결제창이 뜨면
「결제됐는데 카드에서 안 빠지는」 일이 생기고 그건 설명할 방법이 없다.

FAQ 10번이 길을 열어 뒀다 — **「심사관이 확인할 수 있는 테스트 계정을 전달해도 된다.」**
그래서 **결제를 allowlist 계정에만 연다**(`BILLING_ALLOWLIST`). 노출이 0이 된다.

## 값을 두 번 내렸다

| 등급 | 낱개 합 | ① v4 다운로드 인하 | ② 20% 더 |
|---|---|---|---|
| 스탠다드 | 513 | 413 | **330** |
| 플러스 | 829 | 629 | **503** |
| 디럭스 | 1,080 | 880 | **704** |
| 프리미엄 | 1,638 | 1,228 | **982** |

②의 까닭 — 낱개 합 그대로면 **직접 만들기와 값이 완전히 같아진다.**
그런데 직접 만들기는 **무료 크레딧으로 만들어 보고 마음에 들 때 결제**하는데
팩은 **사야 본다.** 같은 값이면 손님은 리스크 없는 쪽을 고른다.

**팩은 이미 만들어 둔 파일이라 한계원가가 0**이라 깎을 여유가 있다.
가격정책표의 별도구매 「원가」 열(3,400/10,900/3,900/12,900)은 **과계상**이다 —
그건 우리가 «한 번» 낸 제작비지, 팔 때마다 나가는 돈이 아니다.

**딸려 온 효과가 컸다.** 프리미엄이 982크레딧이 되어 **1회 충전(99,000원 = 1,287)에 들어온다.**
전에는 1,228이라 두 번 충전해야 했다. 앞으로 값을 올릴 때 이 선을 넘기면
결제 허들이 다시 생긴다.

## 팩을 크레딧으로 사게 했다

전에는 카드 직결제라 **지갑이 둘**이었다 — 충전해 둔 크레딧이 남았는데 팩은 또 현금을 냈다.
하나로 합쳤다. 카드 직결제는 걷어냈다(같은 팩에 값이 둘로 보이면 비싼 쪽이 「속는 길」이 된다).

부수 효과로 **「포인트 사용처」가 둘이 됐다** — 만들기, 그리고 팩 구매.
충전업종 심사가 사용처 흐름을 요구하는데 전에는 하나뿐이었다.

값 표시도 크레딧으로 바꿨다(`formatPackPrice`). `priceKrw ÷ 100` 으로 계산해 내므로
크레딧과 원을 두 벌로 적을 일이 없다.

## 약관에 세 조항을 넣었다

| 요건 | 넣은 곳 |
|---|---|
| 환불은 결제한 수단으로 | 제7조④ |
| 회원간 양도 불가 | 제6조⑥ |
| 구매 시점 결제 금액 기준 환불 | 제7조③(수정) |

**양도 금지는 제4조②(계정 양도 금지)와 다른 조항이다.** 계정을 못 넘겨도
크레딧만 주고받는 길이 열려 있으면 환금 수단이 된다 — 카드사가 충전업종을 조이는 이유가 그것이다.
보너스 크레딧은 환불 대상에서 뺐다(안 적으면 30% 보너스까지 현금으로 돌려줘야 한다는 해석이 선다).

## 아직 안 정한 것

**제7조① 「미사용 크레딧은 구매일로부터 7일 이내」** 를 그대로 둘지.
가이드 예시 문구에는 기한이 없고, 유의사항의 「1년을 초과할 수 없다」는 **상한**이지 하한이 아니다.
7일도 규정 위반은 아니지만 심사관이 「미사용분 환불이 사실상 막혀 있다」고 볼 여지가 있다.
**선불충전금 잔액 환불 의무는 법률 문제라 단정하지 않는다** — 계약담당자에게 물어볼 것.

## 캡처는 반드시 실제 도메인에서

유의사항 2 — **「모든 캡처 화면은 도메인 주소(URL)가 보이도록 캡처해 주세요.」**
`localhost` 로 찍으면 반려된다. 배포한 뒤 실제 도메인에서 찍어야 한다.

---

# 2026-08-09 (2) — 심사용 계정과 결제경로 문서

## 심사관에게만 결제를 열었다

테스트 키는 **돈을 안 받고도 「결제 성공」이라고 답한다.** 그 상태로 결제를 전체 공개하면
아무나 공짜 크레딧을 받는다. 크레딧 자체는 우리 DB 숫자라 0원이지만
**생성·다운로드는 진짜 돈이 나간다.**

금액을 낮추는 걸로는 못 막는다 — 얼마짜리든 손님 눈에 결제창이 뜨면
「결제됐는데 카드에서 안 빠지는」 일이 생기고, 그건 설명할 방법이 없다.

토스 계약과정 FAQ 10번이 길을 열어 뒀다 — 비회원 확인 **또는**
「심사관이 확인할 수 있는 테스트 계정 전달」 둘 중 하나면 된다.
그래서 `BILLING_ALLOWLIST` 에 적힌 계정에만 연다. **노출 금액이 0이 된다.**

구글 로그인만 있어서 심사관에게 줄 계정이 없었다. `emailAndPassword` 를 켜되
`disableSignUp` 으로 **가입은 막고 로그인만** 열었다 — 일반 손님 눈에는 변화가 없다.

> ⚠ `disableSignUp` 은 서버쪽 `auth.api.signUpEmail()` 도 막는다.
> (아니라고 짐작했다가 better-auth 소스를 열어 보고 확인했다.)
> 그래서 계정 만들기 스크립트가 **자기 프로세스 안에서만** `ALLOW_REVIEW_SIGNUP` 을 켠다.

## 전역 스위치 하나가 심사에 필요한 그림을 지우고 있었다 ⭐ 네 번째 이름 겹침

충전·팩 구매는 「심사관 계정에만」으로 바꿨는데 **생성·다운로드 15군데는 옛 전역
스위치(`CREDITS_OPEN`)를 그대로 보고 있었다.** 심사관 화면에 「전체 다운로드 · 준비 중」이 떴다.

더 나쁜 게 있었다. 스위치가 꺼져 있으면 다운로드를 **공짜로** 처리한다.

```ts
if (!CREDITS_OPEN) return { ok: true }   // 전부 무료
```

크레딧이 안 빠지니 **「이 포인트를 어디에 쓰는가」가 화면에 나타나지 않는다** —
충전업종 심사가 요구하는 바로 그 그림이 안 나온다.

`creditsOpenForMe()` 하나로 묶었다. 전체 공개면 누구에게나 참이라 **평상시와 심사 기간이
같은 함수로 맞는다.** 앞의 세 번(`.card.wide`·`720px`·`.card`)과 같은 병이다 —
**같은 것을 두 군데서 판단하면 반드시 갈라진다.**

## 횟수만 막고 금액을 안 막았다

`REVIEW_MAX_CHARGES = 2` 로 횟수는 잠갔는데 금액이 열려 있었다.
9만 9천원을 두 번 고르면 **2,178크레딧 — 22만원어치**다. 계획(5만원 두 번, 1,100)의 두 배.

`REVIEW_CHARGE_WON = 50000` 으로 **한 칸만** 실제 결제되게 했다.
나머지는 **감추지 않고 「결제 심사중」**으로 둔다 — 감추면 라인업이 한 칸으로 보여
가이드 유의사항 3의 「옵션 선택형」이 약해지고, 심사관도 우리가 팔려는 구성을 못 본다.

## 파일은 받아졌는데 로딩이 안 멈췄다

```ts
window.location.href = <다운로드 주소>
return               // ← setBusy(false) 에 영영 못 닿는다
```

다운로드 주소로 보내는 것은 **화면을 넘기는 것이 아니다.** 파일만 내려오고 페이지는
그 자리에 남는다. `router.refresh()` 도 같이 넣었다 — 안 하면 방금 산 사람에게
여전히 「크레딧으로 받기」가 보여 두 번 사는 줄 알고 놀란다.

## 결제경로 문서 12장 — 전부 통과

`build-pg-doc.mts` 로 뼈대를 굽고(표지 + 10단계 + 점검표), 캡처를 끼워 넣었다.
장마다 **「이 화면에 반드시 보여야 하는 것」**을 적어 둬서 무엇을 찍을지 헷갈리지 않는다.

13장 전부 **주소창이 보인다** — 반려 1순위 항목이 해결됐다.
특히 잔액이 **35 → 585 → 82 → 70** 으로 움직인 게 화면에 남았다.

```
무료 35 + 충전 550 − 팩 503 − 생성 12 = 70
```

**「포인트를 어디에 쓰는가」가 숫자로 증명된다.** 위의 다운로드 문을 고친 게 여기서 값을 했다.

> `.mts` 에서 `new pptxgen()` 이 「is not a constructor」로 죽는다.
> 엄격 ESM 이라 이름공간 객체가 온다 — `(ns as {default?}).default ?? ns` 로 받아야 한다.
> (`.ts` 는 그냥 된다.)

## 테스트 계정은 캡처 뒤에 반드시 되돌린다

캡처를 뜨느라 **`paid` 충전이 2건 쌓였다.** `REVIEW_MAX_CHARGES = 2` 라서
그대로 두면 **심사관이 로그인해도 충전 버튼이 처음부터 막혀 있다.**

되돌릴 때 **범위를 반만 잡으면 장부가 깨진다.** 충전분(+1,100)만 지우면 잔액이 −770 이 된다.
원장은 `free` 한 줄만 남기고 전부 지워야 35크레딧짜리 새 계정이 된다.
팩 구매 기록(`pack_order`)도 같이 지워야 버튼이 「다운로드」가 아닌 **「503크레딧으로 받기」**로 보인다.
로그인용 `account` 행은 **어느 경우에도 건드리지 않는다** — 지우면 심사관이 로그인을 못 한다.

## 화면의 시각이 아홉 시간 밀려 있었다 ⭐ 다섯 곳이 같은 병

크레딧 사용 내역의 충전 시각이 **오후 8:16 인데 「오전 11:16」으로** 찍혔다.
PPT 캡처를 다시 보다가 사람이 눈으로 잡아냈다 — **검사기가 없는 영역이었다.**

두 가지가 겹쳤다.

| | |
|---|---|
| DB(Neon)의 시간대가 **GMT** | `defaultNow()` 가 UTC 벽시계를 넣는다. 컬럼도 `timestamp`(시간대 없음)라 「어느 지역 시각인지」 정보가 아예 없다 |
| 그리는 곳이 **서버 컴포넌트** | 베르셀 서버는 UTC 로 산다 |

함정은 **`ko-KR` 을 적었으니 한국 시각이 나오겠거니** 한 것이다.

```
Intl.DateTimeFormat("ko-KR", { dateStyle: "medium", timeStyle: "short" })
                    └ 지역만 정한다. 시간대는 «그 기계 것»을 쓴다.
```

**지역(어떻게 쓰나)과 시간대(몇 시인가)는 다른 것이다.** 한국말로 예쁘게 찍히고
숫자는 UTC 그대로였다. 그래서 「한국어로 잘 나오네」 하고 아무도 안 봤다.

재서 확인했다.

```
── 베르셀처럼 UTC 로 돌렸을 때
  고치기 전   2026. 8. 9. 오전 11:16     ← 아홉 시간 밀림
  고친 뒤     2026. 8. 9. 오후 8:16

── 손님 브라우저(한국)처럼 돌렸을 때
  고치기 전   2026. 8. 9. 오후 8:16
  고친 뒤     2026. 8. 9. 오후 8:16
```

아래 블록이 **덤으로 잡힌 것**을 보여준다. 고치기 전에는 같은 순간이 서버에서
「오전 11:16」, 브라우저에서 「오후 8:16」 — **글자가 갈라졌다.**
하이드레이션 어긋남을 부르는 자리였다.

**다섯 군데가 각자 포맷을 적고 있었고 다섯 군데 모두 같은 병이었다.**
`.card.wide`·`720px`·`.card`·`CREDITS_OPEN` 에 이어 다섯 번째다.
`lib/when.ts` 한 벌(`날짜`·`날짜시각`)로 묶고 시간대를 못 박았다.

> ⚠ 새 화면에서 날짜를 보여줄 때 `Intl.DateTimeFormat` 을 **직접 쓰지 말 것.**
>   `lib/when.ts` 를 부른다. 시간대를 못 박으면 서버든 브라우저든 같은 값이 나온다.

**오늘 찍은 캡처는 다시 안 찍는다.** 시각이 실제 화면과 어긋나지만,
다시 찍으려면 충전을 또 해야 하고 **심사관 몫 2회 중 하나가 날아간다.**
심사관은 자기가 직접 충전해 보고 확인하지, 남의 스크린샷 시각을 우리 서버 시계와
대조하지 않는다. 충전 2회를 온전히 남기는 쪽이 낫다.

## 가이드가 「흰 글자」를 시키고 있었다 ⭐⭐ 방향이 하나뿐이었다

판매팩 20칸을 자동화하기 전에 **완성화면이 새 가이드대로인지** 확인하다가 나왔다.

먼저 좋은 소식 — **여백·간격은 이미 맞는다.** 화면은 08-07 한 배치이고 가이드는 08-08~09에
바뀌었는데도 그렇다. 그날 정렬을 **반대 방향으로** 했기 때문이다.

```
08-08 0c3c5b4  카드 여백과 버튼 사이를 «완성화면이 쓰던 값으로» 옮긴다
```

문제는 색이었다. 여섯 테마의 「버튼(주요)」가 **전부 「흰 글자」**라고 적혀 있었다.

```
주색 배경 위 흰 글자
  navy 8.55 ✅   mono 18.88 ✅   pastel 5.68 ✅
  forest 5.02 ✅  retro 3.28 ✗   coral 3.14 ✗

강조색 배경 위 흰 글자 — 여섯 중 «다섯»이 미달
  navy 2.60 ✗ · mono 3.12 ✗ · pastel 1.41 ✗ · forest 3.09 ✗ · coral 2.15 ✗
```

**이건 화면을 만든 쪽 잘못이 아니다.** 가이드가 흰 글자를 시켰고 AI는 시킨 대로 했다.
시키지 않은 것을 안 했다고 나무랄 수 없다.

원인은 **방향이 하나뿐**이었던 것이다. 오늘 아침에 넣은 `accentText` 는
「이 색을 **글자로** 쓸 때」다. 그런데 「이 색 **위에** 글자를 얹을 때」는 아무도 안 정했다.
같은 색을 두고 물어볼 것이 둘인데 하나만 답해 두었다.

`textOn(bg)` 과 `on-primary`·`on-accent` 를 넣었다. **재서 고른다** —
「밝은 색이면 검정」식 눈대중은 포레스트(#15803D, 흰 5.02 / 검정 3.76)에서 틀린다.
색 표의 두 칸도 손으로 안 적고 계산해 채운다. 색을 바꾸면 글자색이 저절로 따라온다.

고친 뒤 열두 조합이 전부 5.0 이상이다.

| | 주색 위 | 강조색 위 |
|---|---|---|
| navy | 흰색 8.55 | 먹색 7.26 |
| mono | 흰색 18.88 | 먹색 6.05 |
| pastel | 흰색 5.68 | 먹색 13.37 |
| retro | **먹색 5.76** | 흰색 6.07 |
| forest | 흰색 5.02 | 먹색 6.11 |
| coral | **먹색 6.01** | 먹색 8.79 |

**「흰 글자」가 일곱 군데에 적혀 있었다** — 여섯 테마 + 유저용 문서 생성기 한 곳.
파는 팩과 유저가 받는 팩이 각자 문서를 만들다 생긴 것으로, 같은 병의 재발이다.

## 검사기가 색을 안 보고 있었다 ⭐

가장 자주 고친 것이 색인데 `check-design.mts` 는 **간격·카드구조·비율만** 봤다.
그래서 완성화면 여덟 세트가 가이드대로인지 **사람이 손으로 재야 했다.**
500장을 눈으로 볼 수는 없다 — 자동화하려면 기계가 봐야 한다.

검사 9번(색 대비)을 넣었다. **헛경보를 안 내는 것이 이 검사의 전부다.**
손으로 재 보니 「미달」로 보이는 것 대부분이 정상이었다.

| 걸린 것 | 실제 |
|---|---|
| `.toast .act` 1.83 | 토스트는 **어두운 팝업**이다 |
| `.btn[disabled]` 2.52 | 비활성은 WCAG가 최소치에서 **빼 준다** |
| `.map .pin.on` 1.15 | 강조색 **배경** 위 흰 글자 |
| `.logo .em` 3.14 | **로고라 글자가 크다** |

그래서 넷을 지킨다.

- **바탕을 조상까지 거슬러 찾는다** — `.toast` 가 정한 배경을 `.act` 가 쓴다
- **글자 크기도 조상에서 물려받는다** — 안 하면 `.logo .em` 이 본문으로 잡힌다
- **비활성·플레이스홀더는 뺀다**
- **기준을 두 층으로 나눈다** — 3.0 밑은 무엇이든 FAIL, 3.0~4.5 는 WARN

마지막이 핵심이다. CSS만 봐서는 그 규칙이 **글자에 붙는지 아이콘에 붙는지 확실히 모른다.**
모르는 것을 FAIL로 올리면 사람이 검사기를 통째로 무시하게 된다 — 격자 간격에서 이미 겪었다.

## 여덟 세트를 고쳐 색 대비 0건으로 맞췄다

검사기를 믿을 수 있게 만든 뒤 여덟 세트를 돌리고, 걸린 것을 값으로 고쳤다.

**고친 것**

| 자리 | 전 | 후 |
|---|---|---|
| `.accent` 유틸 (5팩) | `var(--accent)` 1.23~2.26 | `var(--acc-text)` **4.93~5.33** |
| 여행_디럭스 `.btn-accent`·`.state-bar .tag`·`.dot` | 흰 글자 1.41 | **먹색 13.37** |
| 별점 채움 (뷰티2·공동구매1) | 3.20·4.36 | **4.51·4.65** |
| 별점 빈 칸 (4팩) | 1.06~1.18 | **3.05~3.12** |
| LMS `--pri-ink`·`--acc-ink` | 4.36 | **4.67·5.15** |
| LMS `.ph` 안내 글자 | 2.77 | **4.58** |

값은 하나도 지어내지 않았다. **팩이 이미 가진 값**(`--star`·`--acc-text`)을 쓰거나,
**색조를 지키며 기준을 넘을 때까지 어둡게** 계산했다.

`.accent` 가 특히 뼈아프다 — **같은 배치에서 만든 여덟 세트가 갈렸다.**
공동구매_프리미엄은 `var(--acc-text)`, LMS는 `var(--acc-ink)` 로 «제대로» 했는데
나머지 다섯은 배경용 색을 글자로 썼다. 가이드가 안 정해 줬으니 갈리는 게 당연하다.
그리고 이건 여행_프리미엄 7화면·공동구매_프리미엄 17화면에서 **실제로 쓰이고 있었다.**

**손대지 않은 것 둘** — 검사기 예외로 못 박았다.

- **네이버 로그인 버튼** `#fff` on `#03C75A` 2.25 — 네이버 «공식 조합»이라
  바꾸면 네이버 버튼으로 안 보인다. WCAG 도 로고타입은 면제한다.
  (카카오 `#FEE500` + 진한 갈색은 자기 가이드가 이미 읽히는 조합이라 안 걸린다.)
- **구분선** `.sep` — 글자가 아니라 단계 사이 선이다. 순수 장식은 규정 대상이 아니다.

## 검사기가 「모른다」고 말할 줄 알아야 했다 ⭐

색 검사를 붙이고 처음 돌렸을 때 32건이 나왔다. 대부분 헛경보였고,
줄여 나가는 과정 자체가 교훈이었다.

| 헛경보 | 왜 | 고친 방법 |
|---|---|---|
| `.hero` 1.15 | **그라데이션 배경**을 못 읽고 페이지 바탕으로 착각 | 그림 배경이면 건너뛴다 |
| `.gnb.dark .logo` 1.00 | `.gnb` 만 보고 흰 배경으로 읽음 | 붙은 class(`.gnb.dark`)를 열쇠로 |
| `.acc-item.on .acc-q` 1.00 | **`.on` 이라는 흔한 수식어**가 남의 부품 배경을 물어 옴 | 부품 이름(첫 class)만 본다 |
| `.tab.on .cnt` 1.71 | 같은 이름이 자리마다 **다른 배경** | 충돌하면 「모른다」로 두고 안 잰다 |
| `.logo .em` 3.14 | 글자 크기를 조상에서 안 물려받음 | 크기도 거슬러 찾는다 |

마지막 줄이 핵심이다. **모르는 것과 없는 것은 다르다.**
`background:linear-gradient(...)` 를 「배경 없음」으로 읽으면 히어로 배너가 전부 걸린다.
같은 class 가 두 배경을 가지면 먼저 만난 쪽으로 정해 버리는 것도 같은 병이다.

**모르는 것을 FAIL 로 올리면 사람이 검사기를 통째로 무시하게 된다.**
자동화하려고 만든 검사기가 자동화를 막는다.

## 판매팩 20칸 — 18칸 등록, 12칸 진열

| 업종 | 등급 | 진열 |
|---|---|---|
| 여행 | 4 | ✅ |
| 뷰티샵 | 4 | ✅ |
| 공동구매 | 4 | ✅ (2026-08-09에 더함) |
| LMS | 3 (프리미엄 없음) | — |
| 매칭 | 3 (프리미엄 없음) | — |

**LMS 디럭스가 그냥 빠져 있었다.** 완성화면 44개가 팩 안에 «있는데»
`makePlans` 에 `site` 를 안 넘겨서 등급 자체가 안 생겼다. 화면이 있어도
등록을 안 하면 없는 것과 같다.

남은 2칸(LMS·매칭 프리미엄)은 **3뎁스 완성화면이 없다.** 스펙팩을 AI 코딩툴에
넣어 만들어야 하고, 해외투어 144화면이 약 40분 걸렸다. 만든 뒤
`package-template.mts` 의 `siteDeep` 에 경로를 적으면 자동으로 붙는다.

## 판매팩 20칸이 다 찼다 — 2026-08-10

마지막 두 칸(LMS·매칭 프리미엄)의 완성화면 291장을 만들었다.

| 업종 | 디럭스 | 프리미엄 | 프리셋이 갈린 자리 |
|---|---|---|---|
| 여행 | 43 | 144 | 소프트 파스텔 → 모던 네이비 |
| 뷰티샵 | 49 | 136 | 소프트 파스텔 → 코럴 선셋 |
| 공동구매 | 37 | 125 | 코럴 선셋 → 미니멀 모노 |
| **LMS** | 44 | **132** | 코럴 선셋+대시보드형 → **모던 네이비+에디토리얼형** |
| **매칭** | 43 | **159** | 모던 네이비+좌우분할형 → **미니멀 모노+목록중심형** |

두 등급의 프리셋을 **일부러** 다르게 둔다. 같은 스펙팩이라도 프리셋을 갈아 끼우면
화면이 이렇게 달라진다는 것을 나란히 놓고 보여 주는 것이 이 팩의 값이다.

**프리미엄이 비싼 까닭이 하위 화면에 있다.** 정상 흐름은 누구나 그리지만
「이어보기가 없는 사람의 홈」, 「견적이 0개인 화면」, 「크레딧이 모자라 못 보내는 화면」은
시키지 않으면 아무도 안 만든다. 그 291장이 여기 있다.

## 「스펙팩대로 만들었나」를 보는 자를 만들었다 ⭐⭐

검사기가 둘 있었는데 **셋째가 비어 있었다.**

```
check-presets  가이드끼리 말이 맞나        (글 vs 코드)
check-design   화면이 그 가이드대로인가     (색·간격·구조)
check-pack     스펙팩이 시킨 것을 화면이 다 했나   ← 없었다
```

만들자마자 **LMS 프리미엄 132장이 «전부» 링크가 죽어 있는 것**을 잡았다.
2뎁스 빌더는 `CL-03` 으로 거는데 3뎁스 팩의 파일은 `CL0301.html` 이다.
**화면 사이를 아예 오갈 수 없었다.** 매칭에서도 88장이 같은 병이었다.

**눈으로는 안 보인다.** 눌러야 보이고, 291장을 다 눌러 볼 수는 없다.

그 밖에 잡은 것

| 무엇 | 원인 |
|---|---|
| 이름이 `undefined` | `name`/`email` 이 아니라 `nm`/`mail` |
| 표 머리글이 `[object Object]` | **내 일괄 치환**이 탭 항목을 고치며 표 머리글까지 바꿈 |
| 높이가 `NaN` | `phFix` 에 비율 문자열을 넘김(픽셀을 받는다) |
| `PRO.nm` 이 `undefined` | `PRO` 가 객체가 아니라 **함수** |
| 「확인 후 열립니다」 버튼 셋 | 그냥 `disabled` 라 영영 안 열림 |

**팩마다 부품 어휘가 다르다.** `card` 는 LMS 에서 본문이 먼저인데 매칭에서는 제목이 먼저고,
고수 데이터는 `rate` 가 아니라 `r` 이며, `phAva` 는 크기가 먼저다.
한 팩에서 익힌 어법을 다른 팩에 그대로 쓰면 안 된다.

### 검사 규칙을 한 번 «되돌렸다»

처음엔 잠긴 버튼을 전부 FAIL 로 올렸다. 그게 너무 뻣뻣했다 —
「수료 조건 미충족」 화면의 발급 버튼은 **잠겨 있는 것이 맞다.**
그건 이 화면에서 풀 수 있는 것이 아니라 다른 화면에서 채워야 한다.

그래서 무는 것을 **말과 행동이 다를 때**로 좁혔다 —
「확인 후 열립니다」라고 해 놓고 여는 길이 없을 때만 문다.

**헛경보를 내는 검사기는 없는 것만 못하다.** 격자 간격에서 한 번, 색 대비에서 또 한 번,
여기서 세 번째로 같은 교훈을 얻었다.

## 상태색 셋이 검사를 한 번도 안 받고 있었다

`success`·`warning`·`danger` 는 lib 이 아니라 `build-design-presets.mts` 에만 있다.
그래서 대비 검사 사정권 밖이었다.

```
warning #B4761A  3.29 ✗    warning #9A6700  4.23 ✗    success #1A7F37  4.41 ✗
```

**「경고」라고 써 놓은 글자가 정작 안 읽혔다.** 셋을 4.6 이상으로 올리고,
검사기가 **그 파일의 값을 직접 읽어** 재게 했다.
두 벌인 것을 합치기 전까지는 적어도 «갈라진 채로 틀리지는» 않게 한다.

## 심사 절차 — 1~2달 기다린다. 먼저 보낼 곳은 없다

토스에서 온 메일(제목 「서비스 신청 검토 일정 안내 (약 1~2달 소요)」)이 정확히 이렇게 말한다.

- **최초 안내까지 영업일 기준 약 1~2달**
- 그 기간 동안 결제경로 파일을 PPT로 **「미리 준비」**해 달라 — «제출»이 아니다
- **※ 본 메일은 발신전용으로 회신이 어렵습니다**

**보낼 곳이 아예 없다.** 담당자가 연락해 오면 그때 낸다.

메일이 시킨 준비물과 우리 상태 (2026-08-09 기준 전부 끝)

| 시킨 것 | 우리 |
|---|---|
| 결제 가능한 상품 1개 이상 | AI팩(플러스) + 크레딧 충전 |
| 하단 사업자정보 5개 | 상호·대표자·등록번호·주소·연락처 |
| 결제창 연동 | 토스 SDK, 결제창 확인 |
| 결제경로 PPT | 12장, 표지에 테스트 계정 포함 |
| 입점 제한 업종 확인 | 충전업종 가이드대로 고침 |

연락처가 휴대폰(010)이어도 된다 — 메일이 「휴대폰번호도 등록할 수 있어요」라고 직접 적었다.

**빌링 상점아이디를 신청서에 넣었다면 빼는 게 낫다.** 우리는 충전할 때마다 한 번씩
결제하므로 정기결제(빌링)가 필요 없는데, 상점아이디가 있으면 안 쓸 결제창까지
연동을 요구받는다.

> ⚠ **홈페이지 변경 금지는 지금이 아니다.** 담당자가 연락해서 심사가 걸린 뒤부터다.
>   1~2달 동안은 자유롭게 고쳐도 된다. (이 둘을 한 번 헷갈려서
>   「지금부터 건드리지 마세요」라고 잘못 말한 적이 있다.)

## 오류 문구를 고치러 갔다가 «안 막힌 문»을 찾았다 ⭐

3회째 충전에 뜨는 「주문을 만들지 못했어요」를 고치려고 `createChargeOrder` 를 열었더니
이렇게 되어 있었다.

```ts
import { billingOpenFor, chargeableNow, REVIEW_MODE, REVIEW_MAX_CHARGES } from "@/lib/flags";
                          └ 가져다 놓고 «한 번도 안 쓴다»
```

**22만원 구멍을 막았다고 적어 둔 그 커밋에서, 서버쪽 자물쇠를 안 채웠다.**
커밋 메시지에는 「화면만 막지 않는다 — 서버 액션은 팩 id 만 알면 부를 수 있어서
거기서도 막는다」고 써 놓고, 실제로는 import 만 하고 부르지 않았다.
화면은 「결제 심사중」으로 잠겨 있으니 다 된 줄 알았던 것이다.

**타입 검사가 안 잡는다.** 안 쓴 import 를 오류로 보지 않게 되어 있다.
「고쳤다」는 커밋 메시지가 「고쳐졌다」의 증거가 아니라는 것 —
오늘 검사기를 만들며 배운 것과 같은 교훈이 결제에서 되풀이됐다.

고치고 재 봤다.

```
  심사 모드 켬   0.5만🔒  1만🔒  2만🔒  5만✅  9.9만🔒
  평상시       0.5만✅  1만✅  2만✅  5만✅  9.9만✅
```

평상시는 다섯 칸이 다 열리므로 심사가 끝나면 그대로 돌아간다.

**문구도 바꿨다** — 「주문을 만들지 못했어요」 → **「결제 준비 중이에요.」**
주문이 안 만들어지는 까닭은 셋(계정에 안 열림 · 횟수 소진 · 지금 안 되는 금액)인데
손님에게는 셋 다 같은 뜻이다. 「만들지 못했어요」는 **고장으로 읽혀서 다시 누르게 만든다.**
잠긴 칸에 적힌 「결제 심사중」과 같은 말로 맞췄다.

## 팔기 전에 반드시 재는 세 가지 ⭐⭐ 자동화의 전제

자동화로 팩을 찍어 내기로 했다. 사람이 912장을 한 장씩 열어 볼 수는 없다.
그러면 **사람 대신 재는 것**이 있어야 한다. 오늘 그 셋을 다 만들었고,
셋 다 «눈으로는 안 보이던 것»을 잡았다. 이 기록은 그 셋을 다시 잊지 않기 위한 것이다.

파는 물건이다. 10만원을 낸 사람이 열어 보고 실망하면 끝이다.
**아래 셋 중 하나라도 건너뛰고 판 팩은, 팔지 않은 것보다 나쁘다.**

### ① 기능명세서의 말이 스펙팩에 다 들어갔나 — `check-pack.mts`

기능정의서에 적어 놓고 화면에는 없는 기능이 생긴다.
사는 사람은 **기능정의서를 먼저 읽고** 「이게 있구나」 하고 산다.
그런데 화면에 없으면, 그건 빠진 게 아니라 **거짓말이 된다.**

재는 것: 스펙↔파일 1:1 · 끊어진 링크 · 안 채워진 값 · 너무 얇은 화면 ·
기능정의서 낱말이 화면에 나오는가 · 잠긴 버튼이 실제로 열리는가.

오늘 이 검사기가 잡은 것:

```
끊어진 링크 220개   LMS 132 + 매칭 88
                    2뎁스 이름(HO-01)으로 3뎁스 파일(HO0101)을 부르고 있었다
[object Object]     내가 돌린 일괄 치환이 표 머리글까지 건드렸다
```

**둘 다 눈으로는 안 보인다.** 링크는 눌러 봐야 알고, 220개를 다 눌러 볼 수는 없다.

### ② 버튼이 정말 반응하나

「확인 후 열립니다」라고 적어 놓고 **여는 길이 없는 버튼**이 있었다.
`disabled` 만 걸어 놓고 `data-gate` 를 안 붙인 것이다 — 영영 안 열린다.
탭도 마찬가지였다. `data-panes-set` 이라고 썼는데 app.js 는 `data-pane-set` 을 본다.
**한 글자 때문에 탭이 아무 일도 안 했다.** 화면은 멀쩡해 보인다.

무는 기준은 «말과 행동이 다를 때»뿐이다. 그냥 잠긴 버튼은 정상이다 —
처음에 다 잡았다가 헛경보가 쏟아졌다. **헛경보를 내는 검사기는 없는 것만 못하다.**

### ③ 디자인이 가이드대로인가 — `check-design.mts` + `check-presets.mts`

가이드가 「흰 글자」라고만 시키고 있었다. 그래서 레트로·코랄 두 벌은
**버튼 글자가 안 읽혔다**(대비 1.01). 화면이 틀린 게 아니라 **가이드에 칸이 없었다.**
상태색 셋(success/warning)은 한 번도 검사를 받은 적이 없었다.

그래서 재는 자를 둘로 나눴다.

```
check-presets  가이드가 스스로 앞뒤가 맞나   (가이드 ↔ 가이드)
check-design   만든 화면이 가이드대로인가    (화면 ↔ 가이드)
check-pack     만든 화면이 스펙대로인가      (화면 ↔ 스펙)
```

**가이드를 먼저 재야 한다.** 틀린 가이드로 화면을 검사하면 전부 통과한다.

### 팔기 전 순서

```
1. check-presets   가이드가 맞나          ← 여기서 틀리면 아래가 다 헛수고
2. npm run pack    재료가 빠진 칸 없나
3. check-design    화면이 가이드대로인가
4. check-pack      화면이 스펙대로인가 + 버튼이 여나
5. 화면 몇 장 직접 눌러 보기               ← 검사기가 못 보는 「어색함」은 사람만 본다
```

5번을 지우지 마라. 검사기는 **자기가 재기로 한 것만** 잰다.
오늘 하루에 여섯 번째로 배운 것 — **두 벌로 적으면 반드시 갈라진다.**
검사기도 마찬가지다. 재는 자와 만드는 자가 같은 것을 안 보면 갈라진다.

## 갈래로 고르고 말로 찾게 했다 — 2026-08-10

팩이 20칸이 되니 목록이 벽이 됐다. 카테고리 6개를 뒀다 —
예약·일정 / 커머스·거래 / 중개·매칭 / 교육·학습 / 커뮤니티·콘텐츠 / 관리·업무도구.
**아임웹 분류를 그대로 안 썼다.** 저쪽은 「쇼핑몰·포트폴리오」처럼 *만드는 물건* 기준인데
우리 팩은 *하는 일* 기준이라 「예약」과 「매칭」이 갈린다.

**비어 있는 갈래는 아예 안 보인다.** 지금은 4갈래만 뜬다(8/4/4/4).
0개짜리 칸을 보여 주면 손님이 눌러 보고 헛걸음한다.

찾기를 제목만으로 하면 안 된다는 것도 확인했다.

```
「미용실」로 찾기 →  4개 나옴
                     팩 이름은 「뷰티샵 예약 플랫폼」이다. 「미용실」이 안 들어 있다.
```

업종·소개·SEO 키워드·대상을 다 합쳐 두고 찾는다(`searchText`).
**손님이 치는 말과 우리가 붙인 이름은 다르다.**

## 「무를 수 없다」를 받기 전에 한 번 멈춰 세운다

전자상거래법은 디지털 콘텐츠의 청약철회를 제한할 수 있게 해 준다.
단, **제한한다는 사실을 «사기 전에» 알렸을 때만** 그렇다.
안 알렸으면 제한이 무효가 되어 **전부 환불해야 한다.**
약관 제7조에만 적어 둔 것은 알린 것으로 안 쳐 준다 — 아무도 안 읽는다.

체크박스가 아니라 **창**으로 만들었다. 결제 화면 아래쪽 체크박스는 눈으로 넘긴다.
창은 한 번 멈춘다. **멈추는 것이 목적이다.**

```
받으신 뒤에는 교환·환불이 되지 않습니다
디지털 콘텐츠 특성상 구매·다운로드 후 교환 및 반품(환불)이 불가합니다.
  982크레딧이 빠지고 바로 파일이 내려옵니다.      ← 무엇에 동의하는지
내용이 맞는지는 상세 페이지의 미리보기에서 받기 전에 확인하실 수 있습니다.
  [동의하고 982크레딧으로 받기]
  더 살펴볼게요                                    우측 상단 [X]
```

**⚠ 창만 띄우고 뒤에서 이미 사고 있으면 아무 의미가 없다.**
버튼을 누르면 창부터 열고, 사는 일은 동의를 누른 뒤에 시작한다.
로그인 안 한 분은 창보다 **먼저** 가입으로 보낸다 — 동의부터 받아 놓으면
돌아왔을 때 동의가 사라져 두 번 묻게 된다.

이미 산 사람의 재다운로드에는 **안 묻는다.** 값을 치른 뒤라 물을 일이 없다.
0크레딧짜리(무료 견본)에도 안 묻는다 — 잃을 게 없는데 겁부터 주는 꼴이다.

재 봤다 — 초점이 동의 버튼에 들어가고, ESC 로 닫히고, 열려 있는 동안 뒤가 안 밀린다.
**닫는 길이 하나뿐이면 갇힌다.**

## 자동화 1주차 — 관리자 시스템 디럭스 37장 · 2026-08-10

첫 회다. 「사람이 한 장씩 안 본다」를 전제로 만들어 봤다.
검사기 넷을 다 통과시킨 뒤, **눌러 보고 나서야 나온 것이 하나 있다.**

### 탭이 색만 바뀌고 몸통은 그대로였다 ⭐⭐ 검사기 넷이 다 놓쳤다

고객 상세(CU-02)에 탭 네 개를 뒀다. 눌러 보니 **탭에 밑줄만 옮겨 가고
아래 내용은 안 바뀐다.** 까닭은 이랬다.

```html
<div class="mt8"> [탭들] </div>      ← 탭은 여기
<div class="mt6"> [몸통들] </div>    ← 몸통은 «옆» 상자에
```

`app.js` 는 눌린 탭의 `.tabs` 를 찾아 **그 부모 안에서만** 몸통을 찾는다.
상자가 갈라져 있으니 찾을 게 없었다. 한 상자로 합치니 됐다.

**이걸 잡은 것은 검사기가 아니다.** check-design 통과, check-pack 0건,
타입 통과, 화면도 멀쩡해 보였다. 지시서에 적어 둔 「10장 눌러 보기」가 잡았다.

> 화면이 «있다»와 «된다»는 다른 말이다. 검사기는 앞엣것만 잰다.

### 검사기가 가이드의 반만 보고 있었다 ⭐

백오피스는 한 화면에 많이 담아야 해서 밀도를 **컴팩트**로 골랐다.
가이드가 두 벌(넉넉하게 / 컴팩트)을 주고, 그중 하나를 고른 것이다.
그런데 검사기가 걸었다.

```
△ --card-pad 이 16px 입니다 — 가이드는 24px
△ --row-h   이 40px 입니다 — 가이드는 48px
```

재 보니 16px·40px 은 **가이드의 컴팩트 값과 정확히 같았다.**
`gridBaseCss()` 가 기본값(넉넉하게)만 내보내는데, 검사기가 그걸 유일한 답으로 알고 있었다.

**가이드대로 골랐는데 걸리는 검사기**는 헛경보다. 둘 중 하나면 통과시키고
어느 쪽을 골랐는지만 알리게 고쳤다. 대신 **한 파일 안에서 둘이 섞이면** 무는다 —
그건 고른 게 아니라 흘린 것이다.

### `calc(var(--hair) * 2)` 를 눈대중으로 봤다

달력·시간표는 격자 사이를 1px 띄우고 배경이 비치게 해서 선을 만든다.
그건 «간격»이 아니라 «선 두께»다. 이름을 주고(`--hair`) 눈금과 따로 뒀다.

그랬더니 `calc(var(--hair) * 2)` 가 걸렸다 — 검사기가 `var(` 를 **맨 앞에서만** 봤다.
눈금에서 계산한 값도 눈금이다. 어디에 있든 보게 고쳤다.

### 값이 서로 안 맞았다

대시보드 「오늘 매출 345,000원」과 그래프의 오늘 막대 486,000원이 달랐다.
사이드바 배지도 주문 3·재고 2인데 화면 안은 2·3이었다.
**둘 다 내가 손으로 따로 적은 값이다.** 한곳에서 계산해 쓰게 고쳤다.

사는 사람은 이런 걸 가장 먼저 본다. 화면이 예쁜지보다 **숫자가 말이 되는지**를 본다.

### 그래서 이번 주 결과

```
만든 화면      37장 (디럭스 · 2뎁스)
check-presets  통과
npm run pack   완성화면 1327개 그대로 · 21칸 모두 새 규칙
check-design   FAIL 0 · WARN 5
check-pack     FAIL 0 · WARN 0
tsc            통과
눌러 본 것     11장 — 가로 넘침 0 · 글자 잘림 0 · 짜부라진 표 0
               탭·잠긴 버튼·모달·토글·달력 모두 반응함(탭은 고친 뒤)
```

## 기존 열 팩은 아직 디자인 검사를 못 넘는다 ⚠ 따로 손봐야 한다

새 팩을 재면서 기존 열 팩도 같이 재 봤더니 **전부 FAIL 이 있다.**

```
LMS 디럭스 1 · LMS 프리미엄 1 · 공동구매 디럭스 2 · 공동구매 프리미엄 2
매칭 디럭스 1 · 매칭 프리미엄 1 · 뷰티샵 디럭스 2 · 뷰티샵 프리미엄 2
여행 디럭스 1 · 여행 프리미엄 1
```

**내가 검사기를 고쳐서 생긴 게 아니다.** 고치기 전 검사기로 다시 재 봤고
숫자가 같았다(그러려고 되돌려 봤다). 원래 있던 것이다.

내용은 두 가지다.

- **간격에 숫자를 직접 적은 자리** — `3px`, `4px`, `8px`, `10px 14px` 등
- **색 대비 미달** — 공동구매·뷰티샵의 `.rate-in .st b` (별점 입력 별),
  글자 `#908784` 가 옅은 바탕 위에서 2점대

전에 「여덟 세트를 고쳐 색 대비 0건으로 맞췄다」고 적어 뒀는데,
**지금 다시 재면 0건이 아니다.** 그때 무엇을 고쳤는지, 그 뒤 무엇이 되돌아갔는지
다시 봐야 한다. 이번 주 일(새 팩 만들기)과 섞으면 둘 다 흐려지니 따로 잡는다.

> 「고쳤다」고 적어 둔 것이 「고쳐져 있다」의 증거가 아니다 —
> 결제 자물쇠에서 배운 것과 같은 교훈이 디자인에서 되풀이됐다.
> **믿을 것은 기록이 아니라 지금 다시 잰 값이다.**

## 첫 회에 업종을 잘못 골랐다 ⭐⭐ 근거는 코드 안에 있었다

관리자 시스템 디럭스 37화면을 다 만들고 나서 들었다 —
**「비즈니스 관리자는 주제가 모호해서 안 만들기로 한 건」**이라고.

내가 못 본 게 아니다. **읽고도 넘겼다.**

```
package-template.mts:61
  있는 것만 디럭스·프리미엄이 생긴다. 비즈니스관리(admin)는 판매 보류라 뺐다.
                                                  └ 「판매 보류」라고 적혀 있다
```

이 줄이 든 파일을 열어서 `INDUSTRIES` 를 읽었다. 그런데 **「아직 배선이 안 된 것」**으로
읽었다. 「보류」는 «안 하기로 정한 것»인데 «아직 안 한 것»으로 본 것이다.

빈 갈래(관리·업무도구)가 눈에 띄니 그걸 채우고 싶었고,
채울 재료가 이미 있으니 손이 갔다. **재료가 있다는 것이 만들 이유가 되지 못한다.**

> 코드 주석에 적힌 «결정»과 «상태»를 구별해서 읽어야 한다.
> 「보류」·「일부러」·「하지 않는다」는 상태가 아니라 결정이다.

같은 회차에 두 가지를 더 빠뜨렸다. 둘 다 이 기록에 이미 적혀 있던 것이다.

- **돌리는 때는 수요일 오전 11시**라고 적어 뒀는데 월요일 SNS 바로 뒤에 붙였다
- **팩을 만들 때 캡처와 영상 재료를 같이 뽑는다**고 적어 뒀는데 아예 안 했다

세 가지 다 «기록을 안 읽어서»가 아니라 «읽고 흘려서» 생겼다.
그래서 지시서에 넣었다 — 만들기 전에 **보류함을 먼저 본다.**

## 자동화가 사람에게 「무엇을 만들까요」를 물으면 자동화가 아니다 ⭐

업종을 잘못 골랐다는 말을 듣고, 다음 업종 후보 셋을 만들어 **골라 달라고 물었다.**
바로 잡혔다 — **「이걸 나한테 물어보는게 맞니? 자동화인데」.**

맞는 말이다. 매주 사람이 골라 줘야 도는 것은 자동화가 아니라 예약된 심부름이다.
그래서 지시서에 **고르는 세 관문**을 넣고, 고르는 일 자체를 자동화 안으로 넣었다.

① 주제가 한 문장으로 말해지는가 · ② 지금 사람들이 찾는가 ·
③ **카페24·아임웹으로 안 되는가**

### ③번이 가장 자주 걸린다 — 실제로 찾아봐야 한다

세 관문으로 후보를 재 봤더니 **미리 적어 둔 순서의 앞 둘이 떨어졌다.**

| 후보 | 결과 | 까닭 |
|---|---|---|
| 정기배송 구독몰 | ✗ | 카페24 가 **정기결제를 무료로** 제공한다 |
| 유료 멤버십 커뮤니티 | ✗ | 아임웹 「회원그룹 이용권」이 결제→등급→콘텐츠 열람 제한까지 한다 |
| 장비 렌탈·대여 | ✓ | 쇼핑몰 재고 모델로 표현이 안 된다 |

「카페24로 안 되는 것만」은 예전에 정한 기준인데, **정말 안 되는지는 그때 안 재 봤다.**
적어 둔 순서를 그대로 따랐으면 카페24가 무료로 주는 것을 10만원에 팔 뻔했다.

렌탈이 남은 까닭은 **재고의 뜻이 다르기 때문**이다.
쇼핑몰 재고는 「몇 개 남았나」인데 렌탈 재고는 **「이 날짜에 비었나」**다.
같은 물건이 날짜마다 다르게 잡히고, 거기에 반납·연체료·보증금·왕복 배송·파손 점검이 붙는다.
이건 상품 모델을 바꿔야 하는 일이라 SaaS 가 못 따라온다.

시장도 자라는 중이다 — 캠핑 장비 2026 년 220.8 억 달러(연 5.92%),
캠핑카 렌탈 **플랫폼**은 연 9.2% 로 더 빠르다.

> 기준을 세워 두는 것과 기준으로 재 보는 것은 다른 일이다.
> **「고쳤다」가 「고쳐져 있다」의 증거가 아니듯, 「기준이 있다」도 「기준대로 골랐다」의 증거가 아니다.**

## 고르라고 내놓고 못 만드는 색이 있었다 ⭐⭐ 두 벌로 적은 일곱 번째

렌탈 팩 프리셋을 고르며 **레트로 페이퍼**를 골랐다. 굽는 순간 죽었다.

```
Error: 프리셋 정의가 없어요: retro (있는 것: navy, mono, pastel, forest, coral)
```

`lib/design-presets.ts` 는 색을 **6종** 내놓는다. 화면에서 고를 수 있고,
대비도 재 놨고, 판매 페이지에도 6종이라고 적혀 있다.
그런데 실제로 굽는 `build-design-presets.mts` 는 **자기 목록을 따로 들고 있었고
거기엔 5종뿐**이었다. 레트로만 없었다.

**`check-presets` 는 그때까지 계속 통과를 찍고 있었다.**
두 목록을 견줘 본 적이 없어서다. 이 검사기는 「글과 코드가 같은 말을 하나」를
보라고 만든 것인데, 정작 **「내놓은 것을 만들 수 있나」는 안 보고 있었다.**

> 고르라고 내놓고 못 만드는 것은 **없는 것보다 나쁘다.**
> 없으면 처음부터 안 고른다. 있는 줄 알고 고르면 **다 골라 놓고 나서** 안다.

두 가지를 했다.

1. **검사를 넣었다** — 가이드가 내놓는 색을 하나씩 굽는이의 목록과 견준다.
   없으면 「고르면 굽다가 죽습니다」로 무는다.
2. **빠져 있던 레트로 정의를 채웠다** — 색·크기·모서리를 `lib/design-presets.ts` 에서
   그대로 옮겼다. 지어낸 값은 없다.

이걸로 **여섯 색이 다 구워진다.** 렌탈 프리미엄이 그 첫 손님이다.

같은 병의 일곱 번째다 — `.card.wide` · `720px` · `.card` · `CREDITS_OPEN` ·
`Intl.DateTimeFormat` · 디자인 프리셋 색표 · 그리고 이번.
**두 벌로 적으면 반드시 갈라진다.** 이제는 갈라지는지 재는 자를 함께 두는 것 말고
방법이 없다는 쪽으로 굳어졌다.

## 렌탈 설계를 만들었다 — 왜 이 업종인가

```
2뎁스   8메뉴 · 37화면 · 363요건 · 화면이동 102개
3뎁스   잎사귀 124개 → 161화면 · 933요건
```

화면을 고르게 나누지 않고 **네 곳에 무게를 실었다.**
날짜별 가용 재고 달력 · 반납 접수 · 연체·파손 정산 · 보증금 환급.

이 넷이 「카페24로 안 되는 것」의 증거이기 때문이다.
쇼핑몰 재고는 「몇 개 남았나」인데 **렌탈 재고는 「이 날짜에 비었나」**다.
같은 물건이 날짜마다 다르게 잡히고, 거기에 반납·연체료·보증금·왕복 배송·파손 점검이 붙는다.
상품 모델 자체가 다르다.

**여기가 얇으면 이 팩을 만든 뜻이 없다.** 그래서 지시서에도 그렇게 적어 뒀다.

설계하며 알게 된 것 하나 — 렌탈은 **같은 화면이 시간에 따라 다른 얼굴**이 된다.
「이용 중」 화면이 반납 하루 전·당일·연체 중에 각각 달라야 한다.
그래서 3뎁스 잎사귀가 상태(state) 쪽으로 유난히 촘촘해졌다.
여행(101개)보다 많은 124개가 나온 까닭이다.

## 우리에게 이미 있던 재료를 안 쓰고 있었다 ⭐⭐

업종을 고를 때 바깥 시장 조사만 봤다. 그런데 **우리 앱에 들어와 컨셉을 적어 본
사람들의 말**이 데이터베이스에 그대로 남아 있었다. `project.concept` 이다.

그 사람들은 이미 우리를 찾아왔던 사람이다. **바깥 통계보다 훨씬 가까운 수요다.**
특히 «우리가 못 만들어 준 것»이 거기 남아 있다.

세어 보는 스크립트를 만들었다 — `조사-생성키워드.mts`.

### 처음 돌렸더니 16/28 이 우리가 눌러 본 자국이었다

```
최근 90일 · 프로젝트 28건
  테스트로 보이는 것 16건    ㅇㅇ · ㅎㅎ · ㄹㄹㄹ · uuu · ㅊㄹㄹ
  읽을 만한 것 12건
    이미 만든 업종으로 덮임 4건 (33%)
    아직 못 만든 것 8건       ← 여기가 후보다
```

**처음엔 이걸 안 걸러서 「못 만든 업종 24건」으로 나왔다.**
있지도 않은 수요를 읽을 뻔했다. 자모만 있거나(ㅇㅇ), 같은 글자만 있거나(ㄹㄹㄹ),
여섯 자도 안 되면 컨셉이 아니다. 걸러 내는 규칙을 넣었다.

> **없는 신호를 있다고 읽는 것이 가장 나쁘다.**
> 데이터가 있다는 것과 읽을 만하다는 것은 다른 말이다.

### 걸러 내니 진짜가 나왔다

```
2026-08-03  나는 인테리어 업자야. 주로 화장실 인테리어와 타일 시공을 주로 해.
2026-07-31  콘텐츠 자동화 마케팅 툴 — 컨셉 입력하면 블로그·인스타·유튜브 콘텐츠 생성
2026-07-24  전자책, 디지털콘텐츠 판매하는 사이트
2026-07-22  동네 필라테스 스튜디오 예약 사이트
2026-07-16  컬러북 이미지를 보여 주는 갤러리 사이트
2026-07-14  병원사이트
```

**사장님이 예로 든 「인테리어」가 실제로 거기 있었다.**
지시서에 「다음 실마리는 견적·상담 접수」라고 막연히 적어 뒀는데,
이제는 «누가 무엇을 하려다 못 하고 갔는지»를 알고 고르게 됐다.

### 관문을 넷으로 다시 짰다

① 뜨는 업종 조사(시장성) → ② 주제가 명확한가 → ③ **우리 손님 생성 키워드** →
④ 카페24·아임웹으로 안 되는가

③이 새로 들어온 자리다. 다만 **③에 많이 나왔다고 그대로 만들지 않는다.**
반려동물 쇼핑몰도 ③에 나왔지만 일반 쇼핑몰이라 ④에서 떨어진다.

### 사람과 이어 붙이지 않는다

이 스크립트는 **누가 적었는지 보지 않는다.** 낱말의 수만 센다.
예시를 보여줄 때도 앞 60자만 자르고 계정 정보는 함께 내지 않는다.
업종을 고르는 데 필요한 것은 낱말의 수이지 누구인지가 아니다.

## 있는 것을 못 찾아 새로 쓸 뻔했다 ⭐

「배포하는 법 매뉴얼을 팩에 같이 넣어 달라」는 말씀을 듣고 저장소를 뒤졌다.
`*배포*`·`*매뉴얼*`·`*deploy*` 이름으로 찾고, HTML 안의 Vercel·깃허브·도메인도 찾고,
git 이력에서 지워진 파일까지 봤는데 **없다고 결론지었다.**

그래서 새로 쓰기 시작했다. 여덟 절짜리 안내서를 다 쓴 참에 사장님이 경로를 주셨다 —
`판매용_템플릿/_마케팅/부록_사이트_내놓는_법.html`.

**열어 보니 1,344줄 90KB짜리 완성본이었다.** 그것도 내가 쓴 것보다 훨씬 좋았다.

```
내가 쓴 것 (8절)          원래 있던 것 (12절 이상)
 배포 · 도메인 · 환경변수   배포 · 도메인 · 환경변수
 오류 대처 · 점검표          + 회원가입·로그인 붙이기
                            + 데이터 저장 (Neon)
                            + 결제 받기 «심사 두 달의 진실»
                            + 사진·영상 만들기
                            + Claude API 붙이기
                            + GitHub Actions 로 자동 검사
                            + 잘못 올렸을 때 되돌리기
```

**왜 못 찾았나.** 이름에 「배포」도 「매뉴얼」도 없었다 — 「**내놓는**」이었다.
파일 안에도 「배포」 대신 「세상에 올리기」라고 적혀 있었다.
나는 **개발자 말로 찾고 있었고, 그 문서는 손님 말로 쓰여 있었다.**

> 우리가 「손님 말로 쓰자」고 정해 둔 원칙이, 정작 우리 자신의 검색을 막았다.

찾는 말을 세 갈래로 넓혔어야 했다 — 개발자 말(배포·deploy) · 손님 말(올리기·내놓기) ·
그 문서가 다룰 «주제»(도메인·호스팅). 마지막 것으로 찾았으면 나왔을 것이다.

내가 쓴 초안은 지웠다. **원본이 더 낫고, 두 벌을 두면 반드시 갈라진다.**

### 붙인 자리 넷

| 어디 | 무엇 |
|---|---|
| 판매팩 24칸 | `09_사이트_내놓는_법.html` — 모든 등급에 |
| 홈 메인 | 「만들고 나서 막히는 자리도 적어 뒀어요」 |
| 무료 샘플 | 「직접 만들어 파일을 받으시면 이런 것도 드려요」 |
| 만들기 화면 | 받기 버튼 옆에 함께 들어간다는 안내 |
| 손님이 받는 zip | `사이트_내놓는_법.html` 로 함께 |

**원본은 한 벌뿐이다.** `_마케팅/부록_사이트_내놓는_법.html` 을 `npm run pack` 이
판매팩과 `public/guide/` 두 곳으로 복사한다. 고칠 때는 원본만 고치면 된다.

### 한글 파일 이름이 주소가 되면 404 가 난다

`public/guide/사이트_내놓는_법.html` 로 뒀더니 브라우저에서 **404** 였다.
인코딩이 어긋난다. **주소가 되는 파일은 영문 이름**으로 두고(`deploy-guide.html`),
손님이 받는 zip 안에서만 한글 이름으로 되돌렸다. 보이는 이름은 그대로다.

한글 폴더에서 헤드리스 크롬이 조용히 실패하던 것과 같은 결의 문제다.
**한글은 사람이 읽는 자리에만 두고, 기계가 지나는 길에는 영문을 둔다.**

---

## 우리 사이트에는 검사기가 없다 — 그래서 눈으로 봐야 한다 (2026-08-09)

홈 메인에 「사이트 내놓는 법」 안내 덩어리를 넣었다가 다시 만들었다.
넣은 것 자체는 맞는데, **그 페이지의 말투가 아니었다.**

| | 옆에 있던 형제들 | 내가 넣은 것 |
|---|---|---|
| 실제 폭 | 974px | **780px** |
| 정렬 | 왼쪽 | **`margin:0 auto` 가운데** |
| 모서리 | 6px 또는 각진 띠 | **14px** |
| 테두리 | 1px 종이선 | **왼쪽에 4px 청록 줄** |
| 장식 | 없음 | **동그라미 안에 이모지** |

이모지 동그라미는 그 페이지 **어디에도 없는 모양**이었다.
혼자만 좁고, 혼자만 가운데 있고, 혼자만 둥글었다.

### 왜 못 봤나 — 두 가지만 재고 넘어갔다

- 글자가 DOM 에 들어갔나 → 들어갔다
- 가로로 삐져나오나 → 안 삐져나온다

**화면을 안 봤다.** 스크린샷 한 장이면 보였을 것을, 존재와 넘침만 재고 통과시켰다.

> 판매팩에는 검사기가 넷 있다. **우리 홈페이지에는 하나도 없다.**
> 검사기가 없는 자리일수록 눈이 유일한 검사기인데, 나는 거기서만 눈을 감았다.

「화면이 있다」와 「화면이 된다」는 다른 말이라고 적어 놓고,
정작 **「화면이 어울린다」**는 재지 않았다. 세 번째 말이 하나 더 있었다.

### 고친 방법 — 새 모양을 짓지 않고, 옆에서 베꼈다

같은 구역의 `.thesis`·`.chip` 이 쓰는 값을 그대로 가져왔다.
`var(--card)` 바탕 · `1px solid var(--paper-line)` · `border-radius: 6px` ·
작은 청록 꼬리표 · 왼쪽 글 + 오른쪽 목록의 2단(`1fr 300px`).
**폭을 안 정했다** — 형제들처럼 그냥 꽉 차게 뒀더니 974px 로 저절로 맞았다.

> 폭을 직접 적는 순간 형제들과 갈라진다. 안 적는 것이 맞추는 방법이었다.

### 좁은 화면에서 낱말이 잘렸다

375px 에서 「만들고 나서 **막히 / 는** 자리도 적어 뒀어요」로 끊겼다.
한글은 기본값이 아무 데서나 자른다. `word-break: keep-all` 을 제목·본문·목록에 넣었다.
데스크톱에서는 안 보이고 좁은 화면에서만 나온다 — **두 폭을 다 찍어야 하는 이유다.**

### 가로 스크롤은 재서 가른다

375px 에서 `scrollWidth` 442px. 내 것인 줄 알았는데,
`.ship` 을 `display:none` 으로 껐다 켜 봤더니 **442px 그대로**였다.
원래 있던 `.tpl-page`·`.tpl` 이 범인이다. **남 탓하기 전에 껐다 켜서 잰다.**

### 규칙으로 남긴 것

`판매팩_자동화_지시서.md` 「우리 사이트 화면을 고쳤을 때」 절에 넣었다 —
고치면 1440·375 두 장을 찍고, 옆 덩어리와 **폭·정렬·모서리·말투·줄바꿈** 다섯을 견준다.

---

## 화면목록 안내가 «파는 사람»에게만 가고 있었다 (2026-08-09)

「지시문이 3,000~5,000줄이라 어긋날 수 있으니 화면목록으로 확인하세요」 문구는
판매팩 엑셀에는 들어가 있었는데, **직접 만들어 받는 손님에게는 한 곳도 안 갔다.**

| 자리 | 전 | 후 |
|---|---|---|
| 판매팩 `02_IA_화면목록.xlsx` | 있음 | 그대로 |
| 손님 zip `IA_화면목록_*.xlsx` | **없음** | 「먼저 읽어 주세요」 시트 추가 |
| 앱 화면목록 화면 | **없음** | 목록 위에 안내 덩어리 추가 |

문구가 **build-template.mts 와 build-template-deep.mts 에 한 벌씩** 있었다.
두 벌이라 세 번째 자리를 만들 때 「어디서 가져오지」가 애매했고,
그래서 손님 쪽은 아예 안 만들어졌다. **두 벌인 것 자체가 빠짐의 원인이었다.**

`lib/export/template-verify.ts` 로 한 벌만 남기고 셋이 가져다 쓴다.
엑셀은 줄 배열이 필요하고 화면은 문단이 필요해서, 조각(`CHECK_WHY`·
`CHECK_AGENCY_IA`·`CHECK_FIXIT_TAIL`)을 내놓고 각자 엮게 했다.
조각을 안 내놓으면 화면 쪽이 자기 말로 다시 적게 되고, 그러면 또 갈라진다.

### 아직 남은 열한 벌

완성화면을 굽는 `generate.mjs` 11개가 이 문구를 각자 박아 두고 있다.
지금은 **열한 벌이 모두 같은 말**이다(대조해서 확인했다).
`.mjs` 는 팩 안에서 혼자 도는 파일이라 공용 모듈을 못 읽는다 —
문구를 고칠 일이 생기면 **열한 곳을 같이 고쳐야 한다.** 지금은 적어만 둔다.

### 좁은 화면에서 또 낱말이 잘렸다

앱 안내 덩어리에서 「쉽게 찾 / 아 수정할」로 끊겼다. Tailwind 는 `break-keep`.
**한글 문단을 새로 넣을 때마다 나온다** — 이제 넣을 때 같이 넣는다.

---

## 홍보용 캡처를 찍다가 팩의 결함 두 개를 찾았다 (2026-08-10, 렌탈 HO-01)

검사기 넷을 다 통과한 화면이었다. **캡처를 열어 보고서야 보였다.**

### ① 감싸는 상자 이름이 «입력칸» 이름과 부딪혔다

히어로를 `<section class="hero"><div class="in">` 로 감쌌다.
그런데 `.in` 은 이 팩에서 **입력칸(input) 클래스**다.

```css
.in,.sel,.ta{ width:100%; height:42px; ... }   /* 입력칸 */
.hero .in{ max-width:1200px; margin:0 auto; }  /* 히어로 속통 — 이름이 겹쳤다 */
```

`height:42px` 가 그대로 먹어서 속통이 42px 로 굳었다.
안에 든 제목·설명·검색바·숫자지표 288px 이 **상자 밖으로 흘러넘쳤고**,
아래 카테고리 카드 위에 겹쳐 그려졌다. 화면 위쪽 절반이 뭉개진 상태로 팔리고 있었다.

**감싸는 상자에 두 글자짜리 이름을 붙이지 않는다.** `hero-in` 으로 고쳤다.
`.in` `.on` `.go` 같은 이름은 이미 쓰이고 있을 확률이 높다.

### ② 버튼이 CSS 가 기다리던 이름을 안 달고 있었다

```css
.searchbar .go{ flex:none; min-width:110px }   /* 검색바 오른쪽 끝 버튼 */
```
CSS 는 `.go` 를 기다리는데 생성기는 `btn-w`(폭 꽉 채우기)를 붙였다.
그래서 「찾아보기」가 검색바 **아래로 내려가 한 줄을 통째로 차지**했다.
`go` 로 바꾸고 `btn-w` 와 `height:100%` 를 뺐다 — `align-items:stretch` 가 이미 하고 있었다.

> **CSS 가 기다리는 이름과 생성기가 붙이는 이름은 서로 모른다.**
> 이번이 세 번째다(잠긴 버튼 `<a>`, 탭과 몸통 상자, 그리고 이것).
> 만드는 쪽과 꾸미는 쪽이 «이름»으로만 이어져 있으면 조용히 어긋난다.

### 왜 검사기 넷이 다 통과시켰나

| 검사기 | 재는 것 | 이걸 못 본 까닭 |
|---|---|---|
| check-presets | 가이드끼리 앞뒤 | 화면을 안 본다 |
| check-design | 화면이 가이드 색·간격대로인가 | **겹쳤는지는 안 잰다** |
| check-pack | 스펙대로 화면이 있나 · 버튼이 여나 | 있고, 열린다 |
| 눈으로 10장 | 사람이 본다 | HO-01 이 그 10장에 없었다 |

세 검사기 모두 **「어디에 그려졌나」는 재지 않는다.** 값이 맞고 파일이 있으면 통과다.
겹침은 «실제로 그려 봐야» 아는 것이고, 그리는 일을 하는 것은 캡처뿐이다.

> **홍보물을 만드는 일이 곧 마지막 검사다.** 이제 굽는 차례 6번에 들어가 있다.

### 캡처를 뽑으면서 밟은 것 넷

- **한 프로세스에서 서버를 열고 `execFileSync` 로 크롬을 부르면 영영 멈춘다.**
  크롬 요청을 받아 줄 차례가 안 온다. 아무 소리 없이 10분을 기다렸다.
  완성화면을 통째로 `%TEMP%` 로 복사하고 `file://` 로 여는 쪽이 맞다.
- **창을 1800px 로 잡으면 짧은 화면은 아래가 절반 빈다.** 1150px 로 잡으니 다 찼다.
- **「화면 정보」 패널이 홍보물에 그대로 들어간다.** 사본 CSS 에만 `.dev{display:none}`.
- **차례를 파일 이름순으로 하면 홈이 네 번째로 간다**(BK·CS·CT·HO…).
  스펙팩(07)의 메뉴 차례를 쓴다 — 손님이 서비스를 쓰는 차례가 그것이다.

---

## 지키는 장치가 «다음 주의 나»를 막을 뻔했다 (2026-08-10)

캡처·영상 뽑기를 굽는 차례 6번에 넣었다. 그런데 6번이 완성화면 폴더에
`00_자동넘김.html` 을 **새로 만든다.** 그 바로 뒤에 완성화면 지문 검사가 있었다.

```
1~5. 굽기 →  6. 캡처·영상 (파일 +1)  →  지문 검사  →  ✗ 멈춤
```

**새 업종을 처음 구울 때마다** 파일이 하나 늘어나므로,
지운 것이 하나도 없는데 「완성화면이 달라졌습니다. 사람이 만든 화면입니다.
지우지 마세요」로 멈춘다. 다음 주 첫 실행이 그대로 실패했을 것이다.

이미 있는 팩을 다시 구울 때는 파일 크기가 같아 **통과한다.**
그래서 오늘 렌탈로 시험할 때는 안 걸렸다 — **새 업종에서만 터지는 자리**였다.
`00_자동넘김.html` 을 일부러 지우고 「새 업종 첫 굽기」와 같은 상태를 만들어서야 나왔다.

### 고친 방법 — 검사를 6번 «앞»으로 옮겼다

이 검사가 지키려는 것은 **「1~5번이 사람이 만든 화면을 건드렸나」**다.
6번은 자기가 만든 것을 하나 더할 뿐이니 검사 밖이 맞다.

> **지키는 장치를 새로 만든 단계 뒤에 두면, 그 장치가 새 단계를 범인으로 잡는다.**
> 단계를 차례에 끼워 넣을 때는 **그 단계가 무엇을 «만드는지»** 보고,
> 만드는 것이 있으면 감시 장치보다 뒤로 뺀다.

### 새 단계를 끼울 때 스스로 물을 것

- 이 단계가 파일을 **만드나 · 지우나 · 고치나**
- 그 자리를 **감시하는 장치**가 있나
- 그 장치가 이 단계 **앞에 있나 뒤에 있나**
- **이미 있는 것으로 시험하면 안 걸리고 새것으로만 걸리는 자리**는 아닌가

---

## 관문 ④는 «예·아니오»가 아니었다 (2026-08-10, 인테리어 업종 고르기)

다음 업종을 네 관문으로 골랐다. **④ 「카페24·아임웹으로 안 되는가」에서 반쯤 걸렸다.**

찾아보니 카페24 스토어에 **「면적계산기 & 맞춤견적」 앱**이 있었다(월 1만원).
면적을 넣으면 금액이 나오고 견적 요청까지 모아 준다 —
인테리어 견적 사이트의 **첫 화면 두 장이 그대로 된다.**

여기서 그냥 「④ 탈락」으로 던졌으면 업종을 놓쳤을 것이다. 갈라 보니 이랬다.

| | 앱이 하는 것 | 앱이 못 하는 것 |
|---|---|---|
| 견적 | 면적 × 단가 곱셈 | 실측 뒤 확정, 항목별 켜고 끄기 |
| 돈 | **결제 없음** | 계약금·중도금·잔금 분할 |
| 공사 | 없음 | 공정표, 현장 사진 일지, 추가공사 변경 견적 |
| 끝난 뒤 | 없음 | 준공 검수, 하자보수 접수 |

> **④는 「되나 안 되나」가 아니라 「어느 쪽이 더 무거운가」다.**
> 되는 쪽에 팩의 무게를 실으면 월 1만원짜리 앱에 진다.
> 그래서 이 팩의 알맹이를 **공정표·사진 일지·추가공사 승인·하자보수** 넷으로 못 박고
> 지시서와 `template-data-interior.ts` 머리에 같이 적어 두었다.

### 지난 회차가 남긴 실마리를 그대로 믿지 않았다

지시서에는 「가장 앞선 실마리는 인테리어」라고 이미 적혀 있었다.
그대로 만들 수도 있었지만 같은 칸에 **「매칭 팩과 겹칠 수 있으니 그때 재 보라」**고
숙제가 붙어 있었다. 재 보니 겹치는 메뉴는 홈·계정·후기 셋뿐이었다.

가른 것은 **누가 그 말을 적었나**였다.

```
2026-08-03  나는 인테리어 업자야. 주로 화장실 인테리어와 타일 시공을 주로 해.
```

**손님이 아니라 «업자»가 적었다.** 남을 이어 주는 플랫폼(매칭)을 원한 것이 아니라
**자기 일감을 받는 자기 사이트**를 원한 것이다. 두 팩이 갈리는 지점이 여기였다.

> `조사-생성키워드.mts` 는 **낱말의 수만 센다**(누가 적었는지는 안 본다 — 그게 규칙이다).
> 그래서 「인테리어 1건」으로만 보였다. 하지만 **문장 자체에 «업자야»가 들어 있었다.**
> 세는 것과 읽는 것은 다른 일이다. 숫자가 적을 때는 **문장을 읽어야** 한다.

### 표본이 12건이면 ③은 「고르는 근거」가 아니라 「실마리」다

90일 28건 중 테스트 16건을 빼니 12건. 두 번 이상 나온 낱말이 하나도 없었다.
스크립트가 스스로 「표본이 적습니다」라고 말한다.

이럴 때 ③은 **후보를 «내놓는» 자리**이고, 고르는 일은 ①·②·④가 한다.
③에서 함께 나온 넷은 이렇게 떨어졌다 — 전자책은 ④(카페24가 기본 제공),
필라테스는 뷰티샵 팩과 뼈대가 같음, 병원은 ④가 약함(아임웹으로 실제로 만들어 팔고 있다),
콘텐츠 자동화 툴은 ②(도구라서 「누가 무엇을 하는 곳」이 없다 — `admin` 과 같은 결).

---

## 「아무도 안 가리키는 화면」 — 끊어진 링크의 거울 (2026-08-10)

`template-data-interior.ts` 를 다 쓰고 ref 를 세어 봤다.
**39화면 · 중복 없음 · 끊어진 링크 0건.** 그런데 하나 더 세어 보니 셋이 걸렸다.

```
아무도 안 가리키는 화면: es5, pr5, pr6
```

- `es5` 견적 견주기
- `pr5` 준공 검수 체크리스트
- `pr6` 공사 진행 – 진행 중 없음

지시서 1번 지뢰는 **「가리키는 곳이 없는 버튼」**이다(220개 났던 그것).
이건 그 **거울**이다 — 화면은 만들어지고 파일도 생기니 `check-pack` 은 통과한다.
스펙에 있는 화면이 전부 있으니까. 그런데 **손님은 거기까지 갈 길이 없다.**

> 힘들여 만든 화면이 **아무도 못 보는 채로 팔린다.**
> 하필 걸린 셋이 빈 상태·검수·견주기 — **곁가지 화면**이었다.
> 본줄기(홈→목록→상세→폼)는 저절로 이어지는데 곁가지는 안 이어진다.

### 세는 법 — 열 줄이면 된다

```ts
const refs = new Set(모든화면.map(s => s.ref));
const 가리킨곳 = new Set(모든화면.flatMap(s => (s.btns ?? []).map(([, to]) => to)));
[...refs].filter(r => !가리킨곳.has(r));   // ← 아무도 안 가리키는 화면
```

같은 자리에서 **끊어진 링크**도 같이 나온다(`가리킨곳` 중 `refs` 에 없는 것).
**두 방향을 한 번에 센다.** `template-data` 를 새로 쓸 때마다 돌린다.

### 검사기 넷은 이걸 못 잡는다 — 또 하나 늘었다

| 검사기 | 왜 못 보나 |
|---|---|
| check-presets | 화면을 안 본다 |
| check-design | 색·간격만 잰다 |
| check-pack | **스펙에 있는 화면이 다 있으면 통과**다. 갈 길이 있는지는 안 잰다 |
| 눈으로 10장 | 못 가는 화면은 애초에 눈에 안 띈다 |

「화면이 있다」·「화면이 된다」·「화면이 어울린다」에 이어
**「화면에 갈 수 있다」**가 넷째였다.

---

## SNS 루틴이 팩의 결함을 찾아 왔다 — 재 보니 «한 장»이었다 (2026-08-10)

SNS 루틴이 「휴대폰에서 깨짐」 영상을 만들려고 팩 화면을 폰 폭으로 열어 보다가
**「완성화면이 390px 에서 오른쪽이 잘린다. 장비렌탈 8화면 중 7화면, 뷰티샵·여행도 같다」**
고 보고했다. 재 봤다.

| 팩 | 잰 화면 | 넘친 화면 |
|---|---|---|
| 장비렌탈 디럭스 | 37 | **1** (HO-01, +106px) |
| 뷰티샵 디럭스 | 49 | 0 |
| 여행 디럭스 | 43 | 0 |

**보고가 넘쳤다.** 「8 중 7」도 「뷰티샵·여행도 같다」도 사실이 아니었다.
보고가 짚은 까닭(`min-width:248px`)도 틀렸다 — 그 규칙은 `.filters .search` 것이고
이 일과 상관이 없다.

> **다른 루틴의 보고도 재 보고 받는다.** 사람이 한 말이든 다른 루틴이 한 말이든
> 「어디가 몇 px」이 없으면 그대로 옮기지 않는다. 재면 5분이다.

### 그래도 하나는 진짜였고, 하필 제일 중요한 화면이었다

**홈(HO-01) 「찾아오시는 길」 카드가 106px 삐져나갔다.**

```html
<div class="row wrap-row">
  <div style="width:280px">지도 자리</div>   <!-- px 로 박혀 있다 -->
  <div class="grow"><dl class="kv left">주소 …</dl></div>
</div>
```

`.grow{flex:1;min-width:0}` 이라 **9px 까지 짜부라졌고**, 그 안의 주소 글자가 상자 밖으로
흘러나가 481px 자리에 그려졌다. `wrap-row` 인데도 줄이 안 바뀐 까닭은 —
**flex 가 「grow 는 0 까지 줄일 수 있으니 둘 다 들어간다」고 판단**하기 때문이다.
줄바꿈은 «못 줄이는 것»이 있어야 일어난다.

```css
.row.wrap-row > .grow{min-width:220px}   /* 이만큼은 못 줄인다 → 그제야 아래로 내려간다 */
.row.wrap-row > *{max-width:100%}
```

넣으니 106 → 0. **오늘 HO-01 에서만 결함이 셋 나왔다**(속통 이름 충돌 · 버튼 클래스 ·
이번 것). 첫 화면이 제일 복잡한데 「10장 무작위」에 잘 안 걸린다.

### 검사기를 만들려다 접었다 — 헤드리스 크롬은 390px 을 못 연다

`check-mobile.mts` 를 만들다가 **버리고 지웠다.** 까닭을 남긴다.

- `--window-size=390,900` 을 줘도 실제 뷰포트는 **482px** 로 열린다.
  `--headless` · `=old` · `=new` 셋 다 같고, `--force-device-scale-factor` 로도 안 내려간다.
  창에 최소 폭(500px 쯤)이 걸려 있다. **482 에서는 이 결함이 안 나온다** — 딱 맞게 들어간다.
- iframe 을 390px 로 만들어 그 안을 재는 방법은 브라우저에서는 되는데,
  `--dump-dom` 이 **iframe onload 보다 먼저 찍어 가서** 헤드리스에서는 결과가 안 나온다.
- `html,body{width:390px}` 로 묶어 재면 sticky 헤더가 뷰포트(482) 기준으로 그려져
  **모든 화면에서 헛경보**가 났다.

> **헛경보를 내는 검사기는 없는 것만 못하다**(우리가 적어 둔 규칙 그대로다).
> 그래서 안 만든 채로 두고, 대신 **눈으로 보기 목록에 넣었다** —
> 브라우저 pane 을 375·390 으로 줄여 첫 화면부터 본다. pane 은 진짜 뷰포트를 준다.

다음에 이걸 다시 만들려는 사람에게 — **헤드리스가 아니라 CDP(개발자 도구 프로토콜)의
`Emulation.setDeviceMetricsOverride` 를 써야 한다.** 창 크기로는 안 된다.

---

## 파는 물건을 아무도 안 열어 보고 있었다 (2026-08-10)

**뷰티샵 디럭스 팩이 `완성화면/index.html` 없이 엿새를 팔렸다.**
화면 49장은 다 들어 있는데 **들어갈 문이 없었다.** 사는 사람은 폴더를 열고
`pages/` 안의 파일을 하나씩 눌러 봐야 했다.

`generate.mjs` 는 멀쩡히 `index.html` 을 쓰고 있었다. **파일만 사라져 있었다** —
8월 4일에 `index_old.html` 로 이름을 바꾸고 새로 안 구운 것이다.

### 검사기 넷이 다 통과시켰다

| 검사기 | 보는 것 |
|---|---|
| check-presets | 가이드끼리 말이 맞나 |
| check-design | 화면이 가이드대로인가 |
| check-pack | 화면이 스펙대로인가 |
| **아무도** | **손님이 받는 zip 이 성한가** |

셋 다 **만드는 «재료»**를 본다. 스펙·가이드·화면 파일. 그런데 손님이 받는 것은
그것들을 «담은 zip» 이다. **재료가 성해도 담다가 빠질 수 있다.**

> **만드는 것을 재는 자와 «건네는 것»을 재는 자는 다르다.**
> 우리는 만드는 쪽만 넷을 두고, 건네는 쪽은 하나도 안 두고 있었다.

### `check-zip.mts` — 손님처럼 열어 본다

「파일이 있다」가 아니라 **「열린다」**를 본다. 처음 돌리자 바로 세 건이 나왔다.

| 무엇 | 어디 |
|---|---|
| `pageids.json` 에 **BOM** 이 붙어 `JSON.parse` 가 깨짐 | 여행 디럭스·프리미엄 |
| `index_old.html` 이 zip 에 딸려 나감 | 뷰티샵 디럭스 |

BOM 은 눈에 안 보인다. 파일을 열어 보면 멀쩡하고, 크기도 정상이고, 텍스트 편집기도
아무 말 안 한다. **파싱해 봐야만 안다.** 「열어 본다」가 「있다」와 다른 까닭이 이것이다.

### 지우지 않고 «안 담았다»

`index_old.html` 은 지우고 싶었지만 **완성화면 안의 파일은 안 지운다**는 규칙이 있다
(전에 두 번 잃었다). 그래서 `package-template.mts` 가 **zip 에 담을 때만 뺀다.**
팩 폴더에는 그대로 있고 손님에게만 안 간다.

> 지우는 것과 안 보내는 것은 다르다. **되돌릴 수 있는 쪽을 고른다.**

### 다음에 또 볼 자리

- 엑셀이 «열리고 안에 값이 있나» — 시트만 있고 비어 있을 수 있다
- pptx 는 속이 zip 이다. 열어서 `ppt/presentation.xml` 이 있는지 본다
- 목록이 가리키는 화면이 다 있나 (**그리고 아무도 안 가리키는 화면이 있나** — 지뢰 12)
- 진열은 됐는데 zip 이 없나 — 사도 못 받는다

---

## 앱 가이드를 만들었다 — 그리고 형제 문서의 결함을 하나 찾았다 (2026-08-10)

「사이트 내놓는 법」의 형제로 **「앱으로 내놓는 법」**을 썼다. 24칸 모든 등급에
`10_앱으로_내놓는_법.html` 로 들어간다.

### 첫 장을 「등록 방법」으로 시작하지 않았다

사장님이 물었다 — 「우선 그 안내서에 앱 등록가이드만 작성해준다는거지?」
**등록 방법만 쓰면 손님이 심사에서 떨어진다.** 그래서 순서를 이렇게 잡았다.

1. **정말 앱이 필요한가** — 안 만들어도 되는 경우가 많다
2. **사이트를 그대로 감싸면 떨어집니다** — 애플 심사 지침 4.2
3. **권한 — 묻기 전에 왜 필요한지 말해야 합니다**
4. 등록 전 준비물 → 5·6. 애플·구글 → 7. 떨어졌을 때 → 8. 점검표

「반응형이니 앱으로도 됩니다」는 **반만 사실**이다. 웹뷰로 감싼 앱은 애플에서
「웹사이트를 다시 포장한 것」으로 반려된다. 안드로이드는 대체로 통과하기 때문에
**구글에서 됐으니 애플도 되겠지** 하다가 걸린다.
이걸 안 쓰고 등록 방법만 주면, 손님은 두 달을 쓰고 우리를 원망한다.
「결제 심사 두 달」을 미리 말해 준 것과 같은 자리다.

### 규정은 찾아보고 썼다

파는 물건에 들어가는 글이라 기억으로 쓰지 않았다. 애플 4.2 · 구글 대상 API 수준
(2026-08-31 부터 신규·업데이트는 안드로이드 16) · 아이콘 1024/512 · 개인정보처리방침
URL 필수 — 다 확인했다. 문서 끝에 **「2026년 8월 기준」과 「올리기 전 공식 안내를
한 번 확인하라」**를 적었다. 스토어 정책은 자주 바뀐다.

### 스타일시트에 «없는 클래스»를 썼다

형제 문서의 `<style>` 을 통째로 가져다 쓰면서 `.warn` 과 `<blockquote>` 를 썼는데
**그 스타일시트에 없는 것들이었다.** 민무늬 문단으로 나왔다.
있는 것은 `.tip`(오렌지 세로줄 + 라벨) 과 `.ours`(청록 상자) 였다.

> **스타일시트를 빌려 쓸 때는 «있는 클래스»부터 세어 본다.**
> 브라우저로 열어 `getComputedStyle` 로 재 보면 5분이면 안다 —
> 배경·테두리·여백이 전부 0 이면 그 클래스는 없는 것이다.

### 그러다 «형제 문서»의 결함이 나왔다 ⭐

`.ours .lb` 배지가 **어두운 모드에서 대비 1.45** 였다.

```css
.ours .lb{ background:var(--teal-deep); color:#FBF6E9; }
```

밝은 모드에서는 `--teal-deep` 이 어두운 청록(#0E6F60)이라 5.62 로 멀쩡하다.
그런데 어두운 모드에서 `--teal-deep` 이 **밝은 청록(#7FE0CE)으로 뒤집히는데
글자색은 #FBF6E9 로 박혀 있어** 밝은 데 밝은 글자가 된다.

`--on-teal` 변수를 만들어 모드마다 뒤집었다. 1.45 → **11.51**.
**두 문서 다 고쳤다** — 「사이트 내놓는 법」은 이미 팔리고 있던 것이다.

> **색을 변수로 쓰면서 그 «위의 글자»만 박아 두면, 모드가 바뀔 때 뒤집힌다.**
> 바탕이 변수면 글자도 변수라야 한다. 오렌지를 글자로 썼다가 대비 2.85 였던 것과
> 같은 결인데, 이번엔 **모드에 따라 나타났다 사라졌다** 해서 더 안 보였다.

---

## 앱 가이드 나머지 둘 — 스펙팩 분기와 스토어 재료 (2026-08-10)

### ① 「모바일 웹(앱)」을 골라도 스펙팩이 한 줄만 달랐다

`deviceMode` 에 「모바일 웹(앱)」이 **원래 있었다.** 그런데 스펙팩에서는
`- **디바이스**: 모바일 웹(앱)` 한 줄로 끝이었다. 화면 구성은 PC 웹과 똑같이 나갔다.

앱은 브라우저가 아니다. 주소창이 없으니 뒤로가기를 화면 안에 둬야 하고,
폰 기능을 쓰려면 물어야 하고, 거절당했을 때 보여줄 화면이 있어야 한다.
**그 화면들이 설계도에 없으면 AI 는 만들지 않는다 — 시키지 않은 것은 안 나온다.**

`2-1. 앱으로 낼 때 더 필요한 것` 절을 붙였다. 화면 넷(권한 안내 · 권한 거부됨 ·
알림 설정 · 연결 끊김)과 규칙 여섯(뒤로가기 · 하단 탭 · 44px · 아래 여백 …).
**반응형·PC 면 이 절이 통째로 안 나온다** — 안 쓸 것을 적어 두면 쓸데없는 화면이 생긴다.

### ② 스토어 재료 — «완성품»을 넣으려다 잘못을 깨달았다 ⭐

아이콘과 스크린샷을 뽑는 스크립트를 만들어 팩에 넣으려 했다. 잘 나왔다.
그런데 넣기 직전에 알았다 — **그 그림들은 우리 견본 사이트(「빌려쓰기」) 화면이다.**

> 손님은 자기 이름·자기 화면으로 앱을 만든다.
> **남의 가게 이름이 박힌 스크린샷은 스토어에 못 올린다.**
> 「자동으로 만들어 드립니다」가 사실이 아니게 되는 자리였다.

그래서 뒤집었다 — 팩에는 **«손님이 자기 화면으로 뽑는 틀»** 을 넣는다.

| 넣는 것 | 무엇 |
|---|---|
| `아이콘_만들기.html` | 글자·색을 화면에서 고치고 1024/512 로 바꿔 가며 캡처 |
| `스크린샷_만들기.html` | 자기 화면 주소를 넣으면 폰 틀에 얹힌다. 100%/줄임 전환 |
| `견본_아이콘.png` · `견본_스크린샷.png` | 「이렇게 생깁니다」 한 장씩 |

1MB → **196KB.** 팩 zip 은 493KB → 674KB.

### 그러면서 밟은 것 셋

- **헤드리스 창 크기를 그대로 믿었다.** `--window-size=1024,1024` 로 찍었더니
  오른쪽 16px·아래 90px 에 흰 띠가 남았다. **크게 찍어 왼쪽 위부터 잘라 내면** 사라진다.
- **한 글자 아이콘에 자간을 줬다.** `letter-spacing` 은 마지막 글자 «뒤»에도 붙는다.
  그만큼 글상자가 좁아져 획이 12px 밀렸다. 눈으로는 「좀 치우쳤나?」 정도라 넘길 뻔했는데,
  **획의 네 모서리를 재 보니** 524·512 였다. 자간을 빼니 512·512.
- **스크린샷 판이 1290px 이라 화면보다 넓었다.** 캡처하기 어려워
  「줄여서 보기 / 캡처할 크기(100%)」 두 버튼을 넣었다.
  `transform:scale` 은 **자리를 안 줄이므로** 싸개 높이를 따로 잡아 줘야 아래가 안 빈다.

> **「좀 치우친 것 같다」는 재서 숫자로 바꾼다.** 오늘 세 번째로 같은 교훈이다 —
> 눈으로 보는 것이 첫걸음이고, 재는 것이 두 번째다. 눈만으로는 12px 을 못 가른다.

### 나머지 10칸을 돌리다 — 「글로 적힌 것」과 「실제 색」이 갈라져 있었다

12칸에 스토어 재료를 뽑으면서 여행 팩에서 멈췄다. 어느 가이드를 썼는지 알아내려고
`index.html` 의 문장을 읽고 있었는데, **팩마다 말이 달랐다.**

```
렌탈  「가이드 01 내추럴 그린 × 레이아웃 A 검색 중심형」
여행  「가이드 프리셋 03 — 소프트 파스텔」
```

문장 모양을 넓혀 잡았더니 이번엔 **더 나쁜 일**이 생겼다 —
index 는 세 프리셋을 «다 소개»하기 때문에, 이름으로 찾으면 **첫 번째가 걸린다.**
여행이 모던네이비로 나왔다. 조용히 틀린 색으로.

> **글로 적힌 것에서 「무엇을 썼나」를 읽지 않는다. 소개하는 글과 쓰는 글은 다르다.**

`base.css` 의 `--primary` 로 바꿨다. **화면이 실제로 그려진 색**이라 틀릴 수가 없다.
못 맞추면 조용히 첫 번째를 집지 않고 **멈추고 알린다** —
화면과 가이드가 갈라졌다는 뜻이기 때문이다.

바꾸고 나니 여행 디럭스는 `#5B4FE5` = **일렉트릭 바이올렛**이었다.
index 문장의 「소프트 파스텔」은 **옛 이름이 남은 것**이다(색은 같다).
파는 문서에 옛 이름이 남아 있는 셈이니 다음에 손볼 자리다.

---

## 인테리어 디럭스 39장을 격리 worktree 에서 만들었다 (2026-08-17)

### 격리된 worktree 는 gitignore 대상 폴더를 안 들고 온다 ⭐

`_작업/`·`판매용_템플릿/_마케팅/`은 `.gitignore` 에 있다. 원래 작업 디렉터리에서
`build-design-presets.mts` 를 미리 돌려 뒀어도, **이 회차가 도는 격리 worktree 에는
그 산출물이 하나도 안 딸려 왔다.** `판매용_템플릿/_판매팩/`(구운 판매팩들)은 어째서인지
같이 왔는데 `_작업/`·`_마케팅/`만 빠져 있었다 — 아마 워크트리를 만들 때 뜬 스냅숏 시점이나
크기 기준으로 갈렸을 것이다. `template-data-interior-deep.ts` 도 커밋 전 파일이라 마찬가지로
없었다.

**대처한 방법** — 지어내지 않고 «복원»했다.
- `template-data-interior-deep.ts` — 오케스트레이터가 대화로 준 원문을 그대로 새로 썼다.
- `_작업/인테리어시공_견적상담/디자인프리셋/` — `build-design-presets.mts interior`
  (·`interior-deep`)를 다시 돌렸다. 「다시 굽지 마라」는 원칙은 «이미 있는 걸 되풀이해서
  값이 흔들릴까 봐»인데, 이 스크립트는 `TARGETS`(코드)에서 결정적으로 같은 값을 낸다 —
  없는 것을 새로 만드는 것과 «지어내는 것»은 다르다.
- `판매용_템플릿/_마케팅/부록_*.html` — 이미 배포되어 커밋된 `public/guide/*.html`
  (`package-template.mts` 가 예전에 거기서 복사해 간 결과물)에서 **거꾸로** 복사했다.
  원본이 없어졌어도 이미 배포된 사본이 진짜 값을 들고 있었다.

> **다음에 격리 worktree 에서 이 루틴을 돌릴 사람에게** — 시작하자마자
> `_작업/`·`판매용_템플릿/_마케팅/` 이 있는지부터 `ls` 로 확인해라. 없으면 이 절을 따라라.

### Windows Git Bash 는 한글 경로 argv 를 셸 커맨드(`ls`·`cp`·`find`)에서 자주 깨뜨린다 ⭐

같은 한글 경로라도 `node`(그 자체)·`npx tsx *.mts`·Read/Write/Glob 도구는 멀쩡히 읽는데
`ls`·`cp`·`find` 같은 coreutils 에 그대로 넘기면 인코딩이 깨져 「없는 파일」로 나왔다.
글꼴 파일을 옮기려다 두 번 헛돌았다.

**해법** — 한글 경로가 필요한 파일 조작은 **Node 스크립트를 새로 만들어** 그 안에
경로를 문자열로 박아 두고(파일 내용은 Write 도구가 항상 UTF-8 로 정확히 쓴다),
`node 그스크립트.mjs` 로 인자 없이 부른다. `npx tsx *.mts "한글경로"` 처럼 **node/tsx 가
직접 받는 인자**는 멀쩡하다 — 문제는 git-bash 가 감싸는 coreutils 뿐이다.

### grid-template-columns:1fr 하나만 두면 좁은 화면에서 통째로 삐져나간다 ⭐⭐

`OW-01`~`OW-06`(현장 관리) 여섯 화면이 같은 `.owner-shell` 뼈대를 쓰는데,
좁은 화면(1080px 이하) 미디어쿼리에서 `grid-template-columns:1fr` 로 1단으로 바꿔 줬다.
375px 로 재 보니 `document.documentElement.clientWidth` 는 375 인데
`document.body.scrollWidth` 는 **782** — 화면이 통째로 두 배 가까이 흘렀다.

까닭 — **`1fr` 트랙은 `minmax(0,1fr)` 없이는 안의 내용(표·문단 등)의 최소고유폭만큼
넓어진다.** 폭이 좁아져도 안 줄어드는 것이다. 다른 곳에서 익힌 「좁은 화면에서 옆 칸이
짜부라지지 않게 `min-width` 를 준다」교훈의 **거울상**이다 — 거긴 넓어지면 안 되는데
넓어졌고, 여긴 좁아져야 하는데 안 좁아졌다. 두 경우 다 flex/grid 트랙의 기본 최소폭이
`auto`(내용 기준)라 생기는 문제다.

`grid-template-columns:minmax(0,1fr)` 로 고치니 375 = 375 로 딱 맞았다.
**grid 에 `1fr` 하나만 적을 때는 반드시 `minmax(0,1fr)` 을 의심한다.**

### 잠금 해제 스코프를 두 상자로 갈라 두면 영영 안 열린다 ⭐

동의 체크 여러 개 → 버튼 하나를 여는 자리(계약서 서명·추가공사 승인·하자보수 완료 확인·
회원가입)에서 체크박스 묶음과 버튼을 각각 **다른** `data-agree-scope` 상자에 넣었다.
`app.js` 의 `syncUnlockAll(scope)` 은 `scope.querySelectorAll('[data-unlock-all]')` 로
**같은 상자 안**에서만 버튼을 찾는다 — 체크박스 셋을 다 체크해도 버튼이 든 다른 상자는
못 본다. 화면은 멀쩡해 보이고 브라우저로 눌러 봐야 나온다(탭·몸통 지뢰와 같은 결).

**체크박스와 그 체크가 여는 버튼은 반드시 «하나의» `data-agree-scope` 상자 안에 함께
두어야 한다.** 네 화면(CT-01·PR-04·AS-03·AU-02) 전부 이 실수를 했다가
브라우저에서 체크 → 클릭으로 직접 눌러 보고서야 잡았다. 검사기 셋 다 이런 자리를 안 잰다.

### 이번엔 헤드리스 크롬이 한글 폴더에서도 잘 됐다

지난 기록엔 「한글 폴더에서 헤드리스 크롬은 조용히 아무것도 안 만든다」고 적혀 있었는데,
`check-design.mts`·`check-pack.mts` 를 `판매용_템플릿/_만드는중/인테리어_디럭스/완성화면`
(한글 경로)에 대고 그대로 돌리니 **잘 됐다.** 다른 상황(`--dump-dom` + iframe 교차 로드
타이밍)에서만 걸리는 문제였던 듯하다 — 매번 의심하되, 매번 진짜로 재 보고 판단한다.

### 시험 삼아 돌린 검사 결과

- `check-design.mts "…인테리어_디럭스/완성화면" showcase` — FAIL 1 · WARN 5.
  남은 FAIL 1건은 이미지 자리표 글자색(`#8B94A6` on `#EEF2F7`, 대비 2.71)인데,
  `lib/design-presets.ts` 의 `IMAGE_PLACEHOLDER`(전 업종 공용 상수)에서 그대로 온 값이다.
  이 업종에서 새로 만든 문제가 아니라 플랫폼 공통 값의 기존 갭이다 — 고치려면
  `lib/design-presets.ts` 를 바꿔야 하는데, 그러면 다른 업종 화면도 다 같이 바뀌므로
  이번 회차 범위 밖으로 남겨 둔다.
- `check-pack.mts "…인테리어_디럭스"` — **FAIL 0** · WARN 6(모두 「글이 짧다」).
  ES-01(질문 하나씩 묻는 단계형 폼)·CT-04(오류 화면)·PR-06(빈 화면)·AU-01~03(로그인류)
  — 전부 화면 성격상 원래 글이 짧아야 하는 화면들이다.

### 사진을 «눈으로» 다시 보니 절반이 자리와 안 맞았다 (2026-08-17, 사장님 지적으로 재검토)

배치.csv 를 열어 25장을 대조표로 붙여 놓고 보니, **전부 스타일링이 끝난 완성 방 사진**이었다
(소파·침대·주방이 다 정돈된 스톡 사진). 공사 중인 손·연장·철거 현장은 한 장도 없었다.

`이미지-끼우기.mts` 는 자리표 라벨을 5가지 역할규칙(`/프로필|고수…/` 등)으로 가르는데,
이 규칙들은 다른 업종 어휘(「시술 전」·「매장」·「프로필」)로 만들어져 있어 인테리어의
자기 말(「시공 전」·「접수 사진」·「처리 전」·「누수 흔적」·「공정표」·「평면 도면」·「오늘 사진」·
「현장 사진」)은 **하나도 안 걸리고 전부 기본역할(시술)로 떨어졌다.** 그 결과 123곳 전부가
같은 사진 풀에서 돌려 쓰기로 채워졌는데, 그중 절반 가까이가 실제로는 안 맞았다:

| 자리 | 왜 안 맞나 |
|---|---|
| CT-01 「평면 도면」·「자재 목록」·「공정표」 | 사진 자리가 아니라 도면·표 자리다. 지도·차트와 같은 종류인데 그 규칙엔 안 걸린다 |
| CS-02 「시공 전 사진」 | 우리 사진은 전부 «다 끝난 뒤» 상태다. 「전」자리에 넣으면 비포·애프터가 같은 사진이 된다 |
| AS-03 「접수 사진」·「처리 전」, PR-04 「누수 흔적 사진」 | 손님이 신고하는 «문제»(누수·파손) 사진이어야 하는데 예쁘게 꾸민 방을 넣으면 거짓말이 된다 |
| PR-02 「오늘 사진」, PR-03 「현장 사진」(30곳!)·「같은 각도 사진」, OW-03 「현장 사진」 | 이 팩의 알맹이(공사 진행·현장 일지)인데, 정작 «공사 중»을 보여줄 사진이 없다 |

**고친 법** — 정규식을 넓히지 않고 `_이미지/팩용/인테리어/짝.csv` 를 새로 만들어
자리 이름별로 개별 `(비움)` 을 적었다(매칭 팩 「짐 사진」 선례와 같은 메커니즘, 2026-08-11 기록 참고).
`--빼기` 로 되돌린 뒤 다시 끼우니 123곳 → **65곳**으로 줄었고, 나머지는 정직하게
회색 자리표로 남았다. `check-사진.mts` 는 여전히 FAIL 0(칸 안에 있다는 것만 재지,
「맞는 내용인가」는 안 잰다 — 그래서 이번처럼 눈으로 다시 봐야 잡힌다).

⚠ **역할규칙 정규식이 그 업종 어휘를 모르면 다 기본역할로 떨어진다** — 이건 조용히 일어나서
스크립트도 검사기도 「잘 됐다」고 말한다. 새 업종에 사진을 끼운 뒤에는 배치.csv 숫자만 보지 말고
**대조표를 만들어 25장 전부를 실제로 보고**, 자리 이름(「어떤 자리」열)과 사진 내용이 실제로
맞는지 하나씩 대조해야 한다. 맞는 사진이 없는 자리는 정규식을 억지로 늘리지 말고
`짝.csv` 에 그 자리 이름 그대로 `(비움)` 을 적는 것이 가장 안전하다.
