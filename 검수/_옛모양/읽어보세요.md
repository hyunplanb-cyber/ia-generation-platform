# 여기 있는 CSV 는 «옛 모양»입니다 — 본보기로 삼지 마세요

8칸짜리(`날짜,업종,묶음,항목번호,무엇이틀렸나,어떻게고쳤나,상태,근거파일`)입니다.
지금 모양은 7칸(`날짜,루틴,팩,항목,무엇,근거파일,고침여부`)이고,
`판매용_템플릿/검수공통.md` 5절에 적혀 있습니다.

2026-08-19 에 `pack-qa-spec-a` 가 이 파일들을 옆에서 보고 옛 모양으로 썼습니다.
그래서 마무리가 마지막 칸을 「고침여부」로 읽어 고침 3칸을 못 봤습니다.
같은 일이 없게 이 폴더로 옮겨 두었습니다.

모양이 맞는지는 `node _작업/검수표검사.mjs` 가 잽니다.

## 2026-09-01 — 뿌리에 있던 `검수기록.csv` 도 여기로 옮겼습니다

뿌리(`검수기록.csv`)의 누적본은 **2026-08-18 에서 멈춰 있었습니다.** 8칸짜리 옛 모양이고,
`상태` 칸에 지금은 쓰지 않는 값(`사람확인필요`)이 들어 있습니다.
`weekly-pack-review` 시절의 기록이라 다시 쓸 일이 없어 `검수기록_2026-08-18까지.csv` 로 옮겼습니다.

⭐ **누적본은 이제 `검수/검수기록.csv` 하나뿐입니다.** 마무리 회차는 거기에만 덧붙입니다.
(분배표가 두 회차째 「둘 중 어디에 적을지 못 박아라」고 물어 온 자리입니다 — 여기서 못 박습니다.)
